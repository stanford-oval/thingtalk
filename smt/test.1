* => @gmail.send_email, ((subject =~ "0123456789" && message = "0123456789") || to = "bob@stanford.edu"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789" && message =~ "23456789") || message =~ "ABCDEFGHIJ" || (subject = "0123456789" && message = "ABCDEFGHIJ")) 
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_everything)), (((section = enum(capital_weather_gang) && link = "http://www.google.com"^^tt:url) || (section = enum(powerpost) && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @gmail.send_email(subject=v_title, message=v_title, to="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @imgflip.list, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.list(), (name =~ "0123456788") , v_name := name, v_picture_url := picture_url => @dropbox.move(old_name=v_name, new_name="9876543210") ;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @bodytrace.source(), (weight <= 155lb) , v_time := time, v_weight := weight => @heatpad.set_power(power=enum(on)) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @car.honk_horn() ;
}
====
now => @onedrive.upload_picture, (file_name =~ "23456789" || file_name = "ABCDEFGHIJ" || (file_name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @onedrive.upload_picture(file_name="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), ((title = "0123456789" && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && wind_speed <= 5kmph && humidity > 7 && cloudiness = 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @dropbox.create_new_folder(folder_name=v_weather) ;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description = "0123456788" && organizer = "0123456788" && location = "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((summary =~ "ABCDEFGHIJ" && sequence < 7 && organizer =~ "0123456788") || sequence >= 42)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @linkedin.get_profile(), ((formatted_name =~ "ABCDEFGHIJ" && headline = "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @xkcd.get_comic, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "0123456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @thermostat.humidity(), (value < 7) , v_time := time, v_value := value => @xkcd.get_comic(number=v_value), (((number > 42 && title =~ "0123456788" && link = "http://www.abc.def"^^tt:url) || (number >= 11 && link = "http://www.google.com"^^tt:url) || (number <= 14 && title =~ "9876543210"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@twitter.direct_message, (message =~ "0123456789" || __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => notify;
}
====
@icalendar.event_begin, ((end_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && organizer = "ABCDEFGHIJ" && location =~ "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && description =~ "9876543210" && organizer = "0123456788") || (summary =~ "9876543210" && organizer = "12345") || (summary =~ "0123456788" && description = "0123456788" && sequence = 11 && organizer =~ "0123456789" && location = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((summary = "12345" && organizer =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.list_folder(folder_name=v_organizer), (((folder_name =~ "23456789" && file_name = "0123456789" && is_folder = false && file_size = 5KB && full_path = "12345") || (folder_name =~ "ABCDEFGHIJ" && is_folder = false && file_size > 5KB))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @almond_bike_market.post, (title =~ "9876543210" && poster =~ "0123456789") 
;;
Main() {
    @washington_post.new_blog_post(section=enum(politics)), ((section = enum(politics) && title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @almond_bike_market.post(title="ABCDEFGHIJ", price=7, info="12345", poster="ABCDEFGHIJ") ;
}
====
now => @thecatapi.get, (count > 14 && link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id = "ABCDEFGHIJ" && count > 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @fitbit.recordweight(weight=82kg) ;
}
====
* => * => @lg_webos_tv.unmute, true 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @builtin.get_random_between(low=55, high=7), ((low >= 11 && high < 11 && random <= 42)) , v_random := random => @lg_webos_tv.unmute() ;
}
====
@weatherapi.monitor, (humidity <= 55 && cloudiness < 14 && status = enum(snowy)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), ((temperature < 73F && wind_speed > 25mph && cloudiness < 7 && fog >= 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @linkedin.share(status=v_weather) ;
}
====
now => * => @thermostat.set_target_temperature, value > 73F 
;;
Main() {
    now => @gmail.get_latest(), (label = "9876543210") , v_from := from, v_subject := subject, v_label := label => @thermostat.set_target_temperature(value=22C) ;
}
====
@sportradar.soccer_us_tourney, (tournament_full_name = "12345" && tournament_league_name = "ABCDEFGHIJ" && away_alias = "12345" && home_alias =~ "23456789" && home_name =~ "ABCDEFGHIJ" && game_status = enum(scheduled) && away_points <= 7 && home_points > 14) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => * => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="9876543210"), (((tournament_full_name =~ "23456789" && tournament_league_name = "23456789" && home_alias = "9876543210" && home_name =~ "0123456789" && game_status = enum(inprogress) && away_points <= 7) || (tournament_full_name = "9876543210" && tournament_league_name = "12345" && away_alias =~ "0123456789" && home_alias =~ "0123456788" && home_name =~ "9876543210" && scheduled_time =~ "9876543210" && away_points >= 11 && home_points < 14))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @almond_bike_market.ask_seller(id=v_away_alias, property=v_away_alias, value=v_home_name, public=true), ((property =~ "0123456789" && value =~ "23456789" && answer = "23456789")) , v_answer := answer => notify;
}
====
* => @almond_bike_market.ask_seller, (id = "ABCDEFGHIJ" && value =~ "23456789") , v_answer := answer => notify
;;
Main() {
    @omlet.incomingmessage(), (((type = enum(text) && message = "ABCDEFGHIJ") || message = "0123456788" || type = enum(text))) , v_type := type, v_message := message => @almond_bike_market.ask_seller(id="23456789", property="23456789", value="0123456788", public=false), (((value =~ "12345" && answer = "12345") || id =~ "0123456789" || (id =~ "0123456789" && public = true))) , v_answer := answer => notify;
}
====
@github.new_issue_comment, (repo_name =~ "9876543210" && issue_number < 14 && body = "9876543210" && time = makeDate(1462320000000)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => notify
;;
Main() {
    @github.new_issue_comment(repo_name="23456789"), true , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @ytranslate.detect_language(text=v_body), ((text =~ "23456789" || (text = "9876543210" && detected_language = "en"^^tt:iso_lang_code("English")) || text = "12345" || text = "0123456788" || detected_language = "it"^^tt:iso_lang_code("Italian") || text =~ "0123456789")) , v_detected_language := detected_language => notify;
}
====
* => @builtin.get_time, true , v_time := time => notify
;;
Main() {
    @builtin.timer(interval=30min), true  => @builtin.get_time(), ((time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1487030400000))) , v_time := time => notify;
}
====
@sleep-tracker.getsleep, (time = makeDate(1487030400000) && place = $context.location.current_location && asleepTime = makeDate(1462320000000) && duration < 30min && light > 2h && deep < 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && duration <= 30min && rem <= 30min && deep <= 2h) || (time = makeDate(1462320000000) && place = $context.location.work && asleepTime = makeDate(1462320000000) && light = 30min && deep <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
@builtin.timer, true  => *
;;
Main() {
    @builtin.timer(interval=2h), ((interval = 2h || interval <= 2h))  => @onedrive.upload_picture(file_name="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @gmail.send_email, (subject = "0123456788" && message =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="0123456788", message="23456789") ;
}
====
@tumblr.new_photo, ((blog_name = "ABCDEFGHIJ" && caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || caption = "23456789") , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => *
;;
Main() {
    @tumblr.new_photo(blog_name="ABCDEFGHIJ"), ((date = makeDate(1462320000000) && caption = "23456789" && link = "http://www.google.com"^^tt:url)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @twitter.unfollow(user_name="alice"^^tt:username) ;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @giphy.get_tag(tag=v_message), ((tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
* => @lg_webos_tv.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @washington_post.new_blog_post(section=enum(morning_mix)), (((title =~ "12345" && link = "http://www.google.com"^^tt:url) || title = "0123456788")) , v_title := title, v_link := link => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
@rss.new_post, (title = "ABCDEFGHIJ" || (title =~ "12345" && link = "http://www.google.com"^^tt:url) || link = "http://www.abc.def"^^tt:url || title =~ "23456789" || title = "9876543210") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), ((title = "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @uber.price_estimate(start=$context.location.work, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((low_estimate > 55 && duration >= 2h)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@almond_bike_market.monitor, price > 7  => * => notify
;;
Main() {
    @almond_bike_market.monitor(info="9876543210", price=11), (price = 42)  => @instagram.get_pictures(count=7), (count < 7) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @nasa.apod, true , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    now => @nasa.apod(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "12345" && description =~ "ABCDEFGHIJ") || (title =~ "ABCDEFGHIJ" && description = "9876543210"))) , v_title := title, v_description := description, v_picture_url := picture_url => @bluetooth_speaker.set_volume(percent=42) ;
}
====
@reddit_front_page.newpost, (link = "http://www.google.com"^^tt:url && category = "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ" && description =~ "0123456789" && sequence >= 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @almond_dates.search, poster =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), (((watched_is_home = false && away_name =~ "9876543210" && home_name =~ "0123456788") || (other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_name =~ "0123456788" && home_name = "23456789" && scheduled_time = makeDate(1462320000000) && away_points <= 7 && home_points > 55) || (watched_is_home = true && away_name =~ "0123456789" && game_status = enum(scheduled) && home_points >= 55 && result = enum(lose)))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @almond_dates.search(), ((interest = "0123456788" && poster = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), ((message =~ "23456789" || (channel = "lol"^^tt:hashtag && from = "alice"^^tt:username && message =~ "0123456789"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @bluetooth_speaker.set_sink() ;
}
====
@phone.receive_sms, body =~ "ABCDEFGHIJ" , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @fitbit.getsteps(), ((steps <= 55 || (date = makeDate(1487030400000) && steps < 14))) , v_date := date, v_steps := steps => notify;
}
====
* => * => @omlet.send_picture, (to = "alice"^^tt:username && caption = "12345") 
;;
Main() {
    @thermostat.humidity(), (value > 42) , v_time := time, v_value := value => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
@twitter.my_tweet, ((text = "23456789" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify
;;
Main() {
    @twitter.my_tweet(), ((contains(hashtags, "cat"^^tt:hashtag) || (text = "9876543210" && contains(hashtags, "lol"^^tt:hashtag)) || in_reply_to = "bob"^^tt:username || (text =~ "0123456789" && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify;
}
====
@google_drive.new_drive_file, file_name = "23456789" , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name = "23456789" || file_name = "12345")) , v_file_name := file_name => notify;
}
====
* => @onedrive.rename_file, (new_name =~ "12345" || (old_name = "0123456789" && new_name = "9876543210")) 
;;
Main() {
    @gmail.receive_primary_email(), (((from_name =~ "0123456789" && from_address = "bob@stanford.edu"^^tt:email_address) || (from_address = "alice@gmail.com"^^tt:email_address && subject = "12345"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @onedrive.rename_file(old_name=v_from_name, new_name=v_snippet) ;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && bodyFat <= 55 && bmi <= 7) || (weight < 155lb && leanMass = 42 && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight >= 82kg && leanMass >= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @phone.get_gps, ((altitude <= 1000m && bearing <= 7) || speed > 25mph || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude <= 1000m && bearing <= 7 && speed >= 25mph) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && bearing < 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 42 && estimated_diameter_min <= 1000m && relative_velocity < 25mph && closest_distance_to_earth > 1000m) || (estimated_diameter_min <= 1000m && estimated_diameter_max <= 42cm && relative_velocity >= 5kmph && closest_distance_to_earth > 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.get_gps(), ((bearing > 14 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @bing.web_search, true , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="23456789"), (((query =~ "0123456789" && title =~ "0123456788") || title = "23456789")) , v_title := title, v_description := description, v_link := link => notify;
}
====
@builtin.at, true  => * => *
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @thermostat.get_humidity(), (value <= 11) , v_value := value => @builtin.hello() ;
}
====
@icalendar.event_begin, ((start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && description =~ "0123456789" && location = "0123456789") || description = "ABCDEFGHIJ" || (sequence = 7 && organizer = "12345") || summary =~ "9876543210" || (summary =~ "ABCDEFGHIJ" && description = "23456789" && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((summary = "12345" && description =~ "12345" && sequence > 7 && location =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_dates.search(), ((interest =~ "0123456789" && message = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @twitter.search_by_hashtag, ((count > 14 && text = "0123456788" && contains(hashtags, "lol"^^tt:hashtag)) || (query_hashtag = "cat"^^tt:hashtag && count > 7 && text =~ "23456789" && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @bodytrace.source(), (weight >= 82kg) , v_time := time, v_weight := weight => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag), ((query_hashtag = "funny"^^tt:hashtag && text = "9876543210" && contains(hashtags, "cat"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@almond_dates.monitor, (message = "12345" || (poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (interest = "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || (title =~ "0123456789" && updated = makeDate(1487030400000)) || updated = makeDate(1487030400000) || title = "12345" || updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "0123456788" || file_name = "0123456788" || file_name =~ "12345")) , v_file_name := file_name => @wall_street_journal.get_opinions(), ((link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @facebook.post_picture, (caption = "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption =~ "12345") 
;;
Main() {
    @washington_post.new_blog_post(section=enum(fact_checker)), (title =~ "23456789") , v_title := title, v_link := link => @facebook.post_picture(picture_url=v_title, caption="9876543210") ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @almond_bike_market.ask_seller(id="23456789", property="12345", value="12345", public=true), ((id =~ "0123456789" && property = "0123456788" && value =~ "0123456789")) , v_answer := answer => @security-camera.set_power(power=enum(on)) ;
}
====
now => * => @speaker.set_volume, percent >= 14 
;;
Main() {
    now => @nasa.apod(), ((title =~ "ABCDEFGHIJ" || title =~ "23456789")) , v_title := title, v_description := description, v_picture_url := picture_url => @speaker.set_volume(percent=14) ;
}
====
@security-camera.new_event, start_time = makeDate(1462320000000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
@almond_couch_market.monitor, (title = "23456789" && price >= 7) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "23456789" && price >= 7)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@fitness-tracker.getbody, ((time = makeDate(1487030400000) && weight <= 82kg && leanMass <= 55 && bodyFat >= 42 && bmi < 7) || (weight >= 155lb && leanMass >= 7 && bodyFat < 55 && bmi < 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && bmi < 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @slack.send, channel = "cat"^^tt:hashtag 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.current_location && heartrate >= 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @slack.send(channel="lol"^^tt:hashtag, message="0123456789") ;
}
====
* => @giphy.get_tag, true , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
now => * => @almond_bike_market.post, (title = "ABCDEFGHIJ" && price >= 11 && info = "0123456789") 
;;
Main() {
    now => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @almond_bike_market.post(title="23456789", price=14, info="9876543210", poster="9876543210") ;
}
====
* => @thermostat.set_minmax_temperature, high > 22C 
;;
Main() {
    @almond_bike_market.monitor(info="9876543210"), (((info = "9876543210" && price > 42) || price <= 55))  => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
* => @facebook.post, (status = "ABCDEFGHIJ" || status = "12345") 
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1487030400000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @facebook.post(status="ABCDEFGHIJ") ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && has_motion = false) || (has_sound = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || start_time = makeDate(1487030400000) || (has_sound = true && has_motion = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @onedrive.create_file, (file_name = "12345" && contents =~ "9876543210") 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @onedrive.create_file(file_name="ABCDEFGHIJ", contents="0123456788") ;
}
====
now => * => @fitbit.recordweight, weight <= 155lb 
;;
Main() {
    now => @thermostat.get_humidity(), ((value >= 11 || value < 14)) , v_value := value => @fitbit.recordweight(weight=82kg) ;
}
====
@holidays.us_holiday_event, true , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @holidays.next_us_holiday_event(), (summary = "0123456789") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @fitbit.getbody, ((weight < 82kg && bmi > 11 && fat < 14) || fat < 42) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((weight < 155lb && bmi >= 7 && fat <= 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@omlet.newmessage, (type = enum(text) || (type = enum(picture) && message =~ "23456789")) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.newmessage(), (((type = enum(picture) && message = "0123456789") || (type = enum(picture) && message = "ABCDEFGHIJ"))) , v_type := type, v_message := message => @dropbox.open(file_name="0123456788"), ((file_name =~ "0123456788" || url =~ "23456789")) , v_url := url => notify;
}
====
* => @thecatapi.get, count >= 11 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @heatpad.set_power, true 
;;
Main() {
    @icalendar.event_begin(), ((location =~ "9876543210" || (summary = "0123456789" && organizer =~ "0123456789" && location =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thecatapi.get(), ((count > 7 || image_id = "23456789")) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @heatpad.set_power(power=enum(off)) ;
}
====
now => @imgflip.list, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.list(), ((name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @facebook.post(status=v_name) ;
}
====
@github.new_milestone, (repo_name = "ABCDEFGHIJ" || (user = "alice"^^tt:username && description = "0123456789" && time = makeDate(1462320000000))) , v_user := user, v_description := description, v_time := time => notify
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), (((user = "bob"^^tt:username && description = "9876543210") || repo_name =~ "9876543210" || (repo_name = "12345" && description = "9876543210"))) , v_user := user, v_description := description, v_time := time => notify;
}
====
now => * => @thermostat.set_hvac_mode, (mode = enum(heat_cool) || mode = enum(cool) || mode = enum(cool)) 
;;
Main() {
    now => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="23456789"), ((source_language = "en"^^tt:iso_lang_code("English") || target_language = "zh"^^tt:iso_lang_code("Chinese") || (source_language = "en"^^tt:iso_lang_code("English") && text = "23456789" && translated_text = "ABCDEFGHIJ") || (source_language = "it"^^tt:iso_lang_code("Italian") && text =~ "12345" && translated_text =~ "0123456788"))) , v_source_language := source_language, v_translated_text := translated_text => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
* => @youtube.search_channel, (channel_id = "ABCDEFGHIJ" && title =~ "0123456788") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.search_channel(query="0123456788"), ((query = "ABCDEFGHIJ" && channel_id =~ "0123456788" && title = "0123456788")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@almond_couch_market.monitor, (title =~ "12345" && price = 11 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (price > 55) , v_title := title, v_price := price, v_contact := contact => @onedrive.upload_picture(file_name=v_title, picture_url=v_title) ;
}
====
now => @ytranslate.translate, source_language = "zh"^^tt:iso_lang_code("Chinese") , v_source_language := source_language, v_translated_text := translated_text => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="0123456788"), (text =~ "0123456789") , v_source_language := source_language, v_translated_text := translated_text => @linkedin.share(status="23456789") ;
}
====
* => @almond_bike_market.post, (info =~ "0123456788" && poster =~ "ABCDEFGHIJ") 
;;
Main() {
    @nasa.asteroid(), ((name = "12345" && estimated_diameter_min <= 42cm && estimated_diameter_max <= 1000m && closest_distance_to_earth > 42cm && orbiting_body = "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.post(price=v_asteroid_id, title="0123456789", info="0123456788", poster="ABCDEFGHIJ") ;
}
====
@fitness-tracker.getbody, ((weight <= 155lb && leanMass <= 7 && bodyFat < 14 && bmi <= 7) || (time = makeDate(1487030400000) && bodyFat = 14 && bmi <= 55) || (time = makeDate(1487030400000) && place = $context.location.work && leanMass = 55 && bodyFat <= 7 && bmi < 55) || (weight < 82kg && leanMass < 14 && bmi <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), (((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass < 11 && bodyFat <= 11 && bmi = 14) || (weight < 82kg && leanMass > 14 && bodyFat <= 14) || leanMass >= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @security-camera.set_power(power=enum(off)) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (has_person = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
* => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && bearing <= 55 && speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @youtube.source(), ((channel_id =~ "0123456789" && channel_title =~ "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @phone.get_gps(), ((altitude > 1000m && bearing < 11)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @security-camera.set_power, power = enum(on) 
;;
Main() {
    @gmail.receive_email(), ((subject =~ "0123456789" || (subject = "ABCDEFGHIJ" && date = makeDate(1487030400000)) || (subject =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && snippet =~ "12345"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @security-camera.set_power(power=enum(off)) ;
}
====
* => @tumblr-blog.post_text, title =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @tumblr-blog.post_text(title="0123456789", body="23456789") ;
}
====
now => * => @linkedin.share, status = "0123456789" 
;;
Main() {
    now => @linkedin.share(status="0123456788") ;
}
====
now => * => @tumblr-blog.post_text, ((title =~ "ABCDEFGHIJ" && body =~ "ABCDEFGHIJ") || body = "23456789") 
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.work && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @tumblr-blog.post_text(title="0123456788", body="ABCDEFGHIJ") ;
}
====
@washington_post.new_blog_post, (title = "23456789" || title = "23456789") , v_title := title, v_link := link => @slack.updateChannelTopic, true 
;;
Main() {
    @washington_post.new_blog_post(section=enum(fact_checker)), true , v_title := title, v_link := link => @slack.updateChannelTopic(channel="lol"^^tt:hashtag, topic="0123456789") ;
}
====
@omlet.newmessage, message = "23456789" , v_feed := feed, v_type := type, v_message := message => * => @facebook.post_picture, (caption = "9876543210" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || caption = v_message || caption = "0123456789") 
;;
Main() {
    @omlet.newmessage(), (message =~ "23456789") , v_type := type, v_message := message => @phone.get_gps(), ((altitude <= 42cm && bearing < 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
now => @twitter.search, (query = "0123456789" && count < 11 && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search(query="9876543210", in_reply_to="bob"^^tt:username), (((query =~ "0123456788" && count > 42 && text =~ "23456789") || (query =~ "23456789" && count >= 11))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@onedrive.file_created, file_name =~ "23456789" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), (file_name =~ "0123456789") , v_file_name := file_name => @giphy.get_tag(tag=v_file_name), true , v_picture_url := picture_url => notify;
}
====
now => @fitbit.getsteps, steps < 14 , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), ((date = makeDate(1487030400000) && steps <= 11)) , v_date := date, v_steps := steps => @almond_bike_market.post(price=v_steps, title="ABCDEFGHIJ", info="9876543210", poster="9876543210") ;
}
====
now => * => @speaker.set_volume, percent > 11 
;;
Main() {
    now => @nasa.rover(), ((count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @speaker.set_volume(percent=v_count) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "9876543210" && headline =~ "23456789" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name=v_headline) ;
}
====
@wall_street_journal.us_business, ((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456788" || title =~ "23456789") , v_title := title, v_link := link => notify
;;
Main() {
    @wall_street_journal.us_business(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => notify;
}
====
* => @almond_couch_market.search, ((title =~ "9876543210" && price > 11) || (title =~ "ABCDEFGHIJ" && price > 14)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && description = "12345" && sequence <= 14) || (end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence < 11 && organizer =~ "ABCDEFGHIJ"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_couch_market.search(), ((price < 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => * => @linkedin.share, true 
;;
Main() {
    @xkcd.new_comic(), ((number < 42 && title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @almond_bike_market.search(info="0123456788"), true  => @linkedin.share(status="9876543210") ;
}
====
* => @nasa.apod, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight >= 82kg)) , v_time := time, v_weight := weight => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@github.new_issue_comment, true , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue_comment(repo_name="ABCDEFGHIJ"), ((repo_name = "0123456788" && issue_number < 11)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @builtin.get_random(), (number >= 7) , v_number := number => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @slack.send, (message = "23456789" || message = "ABCDEFGHIJ" || message =~ "0123456788") 
;;
Main() {
    now => @slack.send(channel="lol"^^tt:hashtag, message="ABCDEFGHIJ") ;
}
====
now => * => @security-camera.set_power, power = enum(on) 
;;
Main() {
    now => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @security-camera.set_power(power=enum(on)) ;
}
====
@google_drive.new_drive_file, file_name = "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @tumblr-blog.post_picture(caption=v_file_name, picture_url=v_file_name) ;
}
====
* => @imgflip.generate, ((text_top = "0123456789" && text_bottom = "ABCDEFGHIJ" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_bottom =~ "12345" && name =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "12345" && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @imgflip.generate(template=v_description, text_top=v_summary, text_bottom=v_description), ((template =~ "ABCDEFGHIJ" && text_top =~ "ABCDEFGHIJ")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address || message = "0123456788" || (to = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || message = "0123456789" || subject =~ "12345" || (subject = "12345" && message =~ "9876543210") || to = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 42 && name =~ "9876543210" && estimated_diameter_min >= 42cm && relative_velocity < 5kmph && closest_distance_to_earth <= 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @gmail.send_picture(to=v_orbiting_body, subject=v_orbiting_body, picture_url=v_name, message="9876543210") ;
}
====
now => @instagram.get_pictures, count < 14 , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(), ((count <= 11 && caption = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && filter = "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @twitter.unfollow(user_name="alice"^^tt:username) ;
}
====
* => @phone.get_gps, (bearing = 55 && speed > 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @lg_webos_tv.lower_volume, true 
;;
Main() {
    @almond_bike_market.monitor(), ((info = "0123456788" || price <= 42))  => @phone.get_gps(), ((altitude >= 42cm || (altitude > 1000m && bearing > 14) || (location = $context.location.work && altitude >= 42cm && bearing >= 42) || (bearing > 14 && speed = 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @lg_webos_tv.lower_volume() ;
}
====
@youtube.source, ((channel_id =~ "12345" && channel_title =~ "0123456789" && video_id = "0123456788") || (channel_id =~ "12345" && channel_title =~ "23456789" && video_id =~ "12345" && video_url = "http://www.abc.def"^^tt:url) || channel_id =~ "0123456788" || (channel_id =~ "23456789" && channel_title = "9876543210" && video_id = "0123456789") || (channel_title =~ "0123456789" && title = "12345" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456789" && channel_title = "9876543210" && video_id =~ "0123456788")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => *
;;
Main() {
    @youtube.source(), (((channel_id =~ "12345" && channel_title =~ "12345") || channel_title = "ABCDEFGHIJ")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @onedrive.rename_file(old_name=v_channel_id, new_name=v_title) ;
}
====
now => * => @almond_bike_market.post, true 
;;
Main() {
    now => @almond_bike_market.post(title="23456789", price=11, info="0123456788", poster="23456789") ;
}
====
@phone.gps, ((bearing < 11 && speed >= 5kmph) || (bearing <= 7 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    @phone.gps(), ((altitude = 42cm && bearing > 7 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @builtin.discover() ;
}
====
@phdcomics.new_post, (title =~ "0123456788" || (title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (((title =~ "23456789" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_couch_market.search(), ((contact = "+16501234567"^^tt:phone_number || (price = 55 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @gmail.send_email, ((to = "bob@stanford.edu"^^tt:email_address && subject = "ABCDEFGHIJ") || subject = "23456789" || to = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "0123456788" || (image_id = "9876543210" && count >= 14))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="0123456788") ;
}
====
@reddit_front_page.newpost, ((title = "0123456789" && link = "http://www.abc.def"^^tt:url && user = "0123456788" && category = "0123456788") || (title =~ "9876543210" && category = "0123456788")) , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title =~ "0123456788" && user =~ "0123456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
now => * => @gmail.send_email, (to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && message =~ "12345")) 
;;
Main() {
    now => @builtin.get_random_between(low=11, high=11), true , v_random := random => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="0123456789") ;
}
====
* => @thecatapi.get, (count = 14 && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "12345") , v_file_name := file_name => @thecatapi.get(), ((image_id = "12345" && count = 11 && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate > 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @nasa.rover(), (count >= 11) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @heatpad.set_power(power=enum(off)) ;
}
====
@yahoofinance.stock_quote, (ask_price > 7 || (ask_price < 42 && bid_price < 11) || ask_price > 7 || bid_price <= 14 || (company_name =~ "23456789" && ask_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && ask_price > 14 && bid_price <= 7) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "12345" && ask_price <= 7 && bid_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && bid_price > 7) || (ask_price < 11 && bid_price < 14) || bid_price >= 14) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), true , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @bing.web_search(query=v_company_name), ((title = "0123456788" && description =~ "9876543210")) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => * => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    @wall_street_journal.opinions(), true , v_title := title, v_link := link => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @fitbit.getsteps, true , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), ((date = makeDate(1462320000000) && steps < 7)) , v_date := date, v_steps := steps => notify;
}
====
@activity-tracker.getmove, (distance >= 1000m && activeTime <= 30min && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), true , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @linkedin.get_profile(), ((headline =~ "23456789" && industry = "23456789" && contains(positions, "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((message =~ "23456789" && poster =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @bluetooth_speaker.raise_volume() ;
}
====
* => @google_drive.create_new_drive_file, file_name = "12345" 
;;
Main() {
    now => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@activity-tracker.getmove, (distance >= 1000m && activeTime <= 30min && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "0123456789" && distance >= 42cm && steps > 42)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @icalendar.list_events(), ((summary =~ "0123456788" && organizer = "0123456789" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @onedrive.rename_file, (old_name = "0123456788" && new_name = "ABCDEFGHIJ") 
;;
Main() {
    now => @onedrive.rename_file(old_name="0123456788", new_name="ABCDEFGHIJ") ;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 73F && wind_speed >= 25mph && humidity > 11 && fog > 11 && weather =~ "9876543210") || (temperature < 73F && cloudiness < 14 && fog <= 14 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed <= 5kmph && humidity < 7 && fog >= 7 && weather = "12345") || (temperature <= 22C && wind_speed <= 5kmph && humidity = 7 && fog < 11))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @wall_street_journal.get_opinions(), ((link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight > 82kg) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight = 155lb)) , v_time := time, v_weight := weight => notify;
}
====
* => @nasa.rover, (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    @jawbone_up.getheartrate(), (((time = makeDate(1487030400000) && place = $context.location.current_location && heartrate <= 11) || time = makeDate(1487030400000) || heartrate >= 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @nasa.rover(), (camera_used =~ "0123456788") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@yahoofinance.stock_div, (company_name =~ "0123456789" && yield >= 55) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "23456789" && yield < 7 && ex_div_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
* => @linkedin.get_profile, (headline = "ABCDEFGHIJ" && contains(positions, "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "12345") , v_file_name := file_name => @linkedin.get_profile(), ((formatted_name = "23456789" && specialties =~ "0123456788")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @weatherapi.moon, (location = $context.location.work && date = makeDate(1487030400000)) , v_date := date, v_phase := phase => notify
;;
Main() {
    @thermostat.humidity(), ((value < 42 || value >= 42)) , v_time := time, v_value := value => @weatherapi.moon(location=$context.location.home), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && date = makeDate(1462320000000))) , v_date := date, v_phase := phase => notify;
}
====
now => * => @almond_bike_market.post, ((title =~ "23456789" && poster =~ "0123456789") || (title = "ABCDEFGHIJ" && poster = "12345") || (title = "0123456788" && price < 42 && info =~ "9876543210")) 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @almond_bike_market.post(title="0123456788", price=11, info="0123456788", poster="9876543210") ;
}
====
* => @gmail.get_latest, label = "23456789" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @almond_bike_market.monitor(info="ABCDEFGHIJ"), (price < 42)  => @gmail.get_latest(), ((subject = "12345" || label =~ "23456789")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@scale.source, true , v_time := time, v_value := value => * => @parklon_heatpad.set_power, true 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, (day = "23456789" && distance < 1000m && steps < 42 && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "12345" && steps < 14 && activeTime >= 2h && inactiveTime >= 30min && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
* => @omlet.send_to, ((to = "bob"^^tt:username && message = "23456789") || (to = "bob"^^tt:username && message =~ "ABCDEFGHIJ")) 
;;
Main() {
    @thermostat.humidity(), ((value <= 42 || value < 14)) , v_time := time, v_value := value => @omlet.send_to(to="alice"^^tt:username, message="0123456789") ;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature > 73F && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 7 && fog < 7 && weather = "0123456788") || (temperature = 73F && wind_speed > 5kmph && fog > 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((temperature <= 73F && wind_speed <= 25mph && humidity < 14 && fog < 42 && weather = "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.search(), (price = 42) , v_title := title, v_price := price, v_contact := contact => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
@yahoofinance.stock_div, ((stock_id = "msft"^^tt:stock_id("Microsoft") && yield < 42 && div = 11 && pay_date = makeDate(1487030400000)) || (company_name = "ABCDEFGHIJ" && yield <= 11 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((company_name = "0123456788" && yield > 42)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="0123456788") ;
}
====
* => @almond_couch_market.post, ((price > 55 && contact = "+16501234567"^^tt:phone_number) || (title =~ "ABCDEFGHIJ" && price = 11)) 
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @almond_couch_market.post(title="9876543210", price=42, contact="+16501234567"^^tt:phone_number) ;
}
====
* => @linkedin.get_profile, (headline =~ "0123456789" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && summary =~ "23456789" && description = "23456789" && sequence >= 42 && organizer =~ "ABCDEFGHIJ" && location =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @linkedin.get_profile(), ((formatted_name =~ "23456789" && specialties =~ "ABCDEFGHIJ" && contains(positions, "0123456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @slack.send_picture(channel=v_specialties, caption=v_headline, picture_url=v_industry) ;
}
====
* => @thermostat.set_minmax_temperature, ((low <= 73F && high >= 22C) || low >= 73F || high > 73F || (low < 73F && high < 22C)) 
;;
Main() {
    now => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
* => @security-camera.get_url, link = "http://www.google.com"^^tt:url , v_link := link => notify
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => notify;
}
====
@yahoofinance.stock_div, ((stock_id = "msft"^^tt:stock_id("Microsoft") && yield < 42 && div = 11 && pay_date = makeDate(1487030400000)) || (company_name = "ABCDEFGHIJ" && yield <= 11 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((company_name =~ "ABCDEFGHIJ" && yield >= 11 && div < 42 && pay_date = makeDate(1462320000000) && ex_div_date = makeDate(1487030400000)) || (stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "9876543210" && div > 55))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @speaker.set_sink() ;
}
====
* => @bing.web_search, (title = "0123456789" && description =~ "23456789") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="9876543210"), (((query = "0123456789" && description = "0123456789") || query = "12345")) , v_title := title, v_description := description, v_link := link => notify;
}
====
@heartrate-monitor.getheartrate, (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate <= 14)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate > 42) , v_time := time, v_place := place, v_heartrate := heartrate => @thermostat.get_humidity(), true , v_value := value => notify;
}
====
* => @onedrive.delete_file, true 
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="sta"^^sportradar:ncaafb_team("Stanford Cardinals")), ((other_team_abbr = "sta"^^sportradar:ncaafb_team("Stanford Cardinals") && away_name = "12345" && home_name =~ "0123456789" && home_points > 14)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @onedrive.delete_file(file_name=v_home_name) ;
}
====
now => @google_drive.create_new_drive_file, (file_name =~ "0123456788" || file_name = "12345") 
;;
Main() {
    now => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "12345" || file_name =~ "9876543210") 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag), ((query_hashtag = "funny"^^tt:hashtag && count = 7 && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @google_drive.create_new_drive_file(file_name=v_text) ;
}
====
now => * => @heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @youtube.search_videos(query="12345"), ((video_id = "0123456788" && channel_title = "0123456789" && description =~ "0123456789" && count < 11 && video_url = "http://www.abc.def"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @heatpad.set_power(power=enum(off)) ;
}
====
@phdcomics.new_post, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @lg_webos_tv.play_url(url=v_link) ;
}
====
now => @thermostat.get_temperature, (value >= 73F || value <= 22C || value > 73F) , v_value := value => notify
;;
Main() {
    now => @thermostat.get_temperature(), (value >= 22C) , v_value := value => notify;
}
====
* => @linkedin.share, true 
;;
Main() {
    @twitter.my_tweet(), (contains(hashtags, "funny"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @linkedin.share(status=v_text) ;
}
====
@scale.source, value > 155lb , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value > 82kg)) , v_time := time, v_value := value => notify;
}
====
now => * => @builtin.sorry, true 
;;
Main() {
    now => @almond_bike_market.search(price=55), true  => @builtin.sorry() ;
}
====
* => @phone.get_gps, ((location = $context.location.work && bearing <= 14 && speed <= 25mph) || (location = $context.location.work && bearing = 55 && speed < 5kmph) || (altitude < 42cm && speed > 5kmph) || (altitude > 1000m && bearing = 7) || (altitude > 1000m && bearing < 7 && speed < 5kmph) || (altitude < 42cm && bearing < 7 && speed = 25mph) || (altitude < 42cm && bearing < 7) || (altitude > 1000m && speed < 5kmph) || (altitude <= 42cm && bearing = 14 && speed >= 25mph) || (altitude < 42cm && speed < 5kmph) || (altitude = 1000m && bearing < 7 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@nasa.asteroid, (asteroid_id = 14 && name = "9876543210" && estimated_diameter_min < 1000m && estimated_diameter_max <= 1000m && relative_velocity = 5kmph && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), ((name = "ABCDEFGHIJ" && estimated_diameter_min >= 1000m && estimated_diameter_max <= 42cm && dangerous = true && relative_velocity > 5kmph && closest_distance_to_earth < 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @speaker.raise_volume() ;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => @slack.setPresence, (presence = enum(away) || presence = enum(away) || presence = enum(away)) 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @slack.setPresence(presence=enum(auto)) ;
}
====
* => @slack.updateChannelTopic, ((channel = "cat"^^tt:hashtag && topic =~ "0123456788") || topic = "0123456788" || topic = "ABCDEFGHIJ" || (channel = "lol"^^tt:hashtag && topic =~ "0123456788")) 
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @slack.updateChannelTopic(channel=v_file_name, topic="12345") ;
}
====
* => @twitter.search_by_hashtag, from = "alice"^^tt:username , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @bodytrace.source(), ((weight >= 155lb || weight = 155lb)) , v_time := time, v_weight := weight => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="bob"^^tt:username), (((query_hashtag = "funny"^^tt:hashtag && count < 7 && text =~ "0123456788" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username) || (count > 42 && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @holidays.next_uk_holiday_event, (date = makeDate(1462320000000) && summary = "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_uk_holiday_event(), (((date = makeDate(1487030400000) && summary = "0123456789" && description =~ "12345") || (summary =~ "23456789" && description = "23456789") || summary = "0123456788" || (summary =~ "9876543210" && description = "0123456788") || summary = "23456789" || summary = "12345" || description = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @speaker.raise_volume() ;
}
====
* => @omlet.send_to, true 
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight >= 82kg)) , v_time := time, v_weight := weight => @omlet.send_to(to="bob"^^tt:username, message="23456789") ;
}
====
now => @xkcd.get_comic, (number < 7 && title = "23456789" && link = "http://www.google.com"^^tt:url && alt_text =~ "23456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(), ((number < 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "12345")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@phdcomics.new_post, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (title = "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => @fitbit.recordweight(weight=155lb) ;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude > 1000m && bearing = 42 && speed = 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.home && altitude < 1000m && bearing <= 42)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @onedrive.rename_file(old_name="0123456788", new_name="0123456788") ;
}
====
@xkcd.new_comic, (number > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), (title =~ "12345") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
@github.new_commit, (repo_name =~ "23456789" && time = makeDate(1487030400000)) , v_user := user, v_message := message, v_time := time => * => *
;;
Main() {
    @github.new_commit(repo_name="0123456789"), (((repo_name = "9876543210" && user = "bob"^^tt:username) || (user = "alice"^^tt:username && message =~ "0123456788" && time = makeDate(1462320000000)))) , v_user := user, v_message := message, v_time := time => @thermostat.get_humidity(), true , v_value := value => @phone.set_ringer(mode=enum(vibrate)) ;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @omlet.incomingmessage(), (message =~ "0123456789") , v_type := type, v_message := message => @heatpad.set_power(power=enum(off)) ;
}
====
* => @onedrive.create_file, true 
;;
Main() {
    now => @onedrive.create_file(file_name="23456789", contents="9876543210") ;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && weight >= 155lb && leanMass <= 42 && bmi > 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="23456789", message="9876543210") ;
}
====
* => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210")) 
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), (((blog_name =~ "0123456789" && title = "12345" && body =~ "12345") || (blog_name =~ "23456789" && title = "9876543210" && contains(tags, "lol"^^tt:hashtag) && body = "0123456789") || (blog_name =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
* => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    now => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@almond_bike_market.monitor, (info = "12345" || (info =~ "0123456789" && price > 11) || price <= 42)  => *
;;
Main() {
    @almond_bike_market.monitor(info="0123456788"), (((info =~ "23456789" && price <= 14) || price > 11))  => @lg_webos_tv.lower_volume() ;
}
====
@bodytrace.source, (weight < 82kg || (time = makeDate(1487030400000) && weight >= 155lb)) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1487030400000) && weight > 155lb) || (time = makeDate(1487030400000) && weight <= 155lb) || (time = makeDate(1462320000000) && weight < 155lb))) , v_time := time, v_weight := weight => @fitbit.getbody(), ((weight = 82kg && bmi <= 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && weight <= 82kg) || weight < 155lb || (time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1462320000000) && weight <= 82kg) || weight < 155lb)) , v_time := time, v_weight := weight => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="12345") ;
}
====
now => * => @dropbox.create_new_folder, (folder_name =~ "12345" || folder_name = "12345" || folder_name =~ "0123456789") 
;;
Main() {
    now => @almond_bike_market.detail(id="ABCDEFGHIJ"), true  => @dropbox.create_new_folder(folder_name="23456789") ;
}
====
@sleep-tracker.getsleep, ((awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration >= 2h && rem < 30min && deep > 2h) || (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && light < 2h && deep >= 30min) || (awakeTime = makeDate(1462320000000) && duration <= 30min && light <= 30min && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.home && awakeTime = makeDate(1462320000000) && duration < 2h && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @almond_bike_market.post, info =~ "23456789" 
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1462320000000) && summary =~ "12345" && description =~ "12345") || (description = "23456789" && sequence <= 55))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_bike_market.post(title=v_summary, info=v_summary, poster=v_summary, price=7) ;
}
====
* => @thermostat.set_hvac_mode, mode = enum(heat_cool) 
;;
Main() {
    @youtube.source(), (((channel_title =~ "ABCDEFGHIJ" && video_url = "http://www.abc.def"^^tt:url) || (channel_id = "9876543210" && channel_title = "0123456789"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
* => @gmail.send_picture, (to = "alice@gmail.com"^^tt:email_address && message =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @sleep-tracker.getsleep(), (((asleepTime = makeDate(1487030400000) && rem <= 30min && deep > 2h) || (time = makeDate(1462320000000) && place = $context.location.home && asleepTime = makeDate(1487030400000) && duration < 2h && rem >= 2h && deep >= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @almond_bike_market.post, (info =~ "12345" || (info =~ "ABCDEFGHIJ" && poster = "0123456788")) 
;;
Main() {
    @twitter.my_tweet(), (text =~ "23456789") , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @almond_bike_market.post(title=v_text, info=v_text, poster=v_text, price=14) ;
}
====
now => * => @builtin.debug_log, message =~ "23456789" 
;;
Main() {
    now => @builtin.debug_log(message="0123456789") ;
}
====
* => @phone.get_gps, ((location = $context.location.home && altitude = 42cm && bearing <= 7) || (altitude > 1000m && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight >= 82kg && leanMass = 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @phone.get_gps(), ((altitude < 1000m && bearing > 7)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @builtin.get_random_between, (low >= 55 && high <= 11 && random <= 14) , v_random := random => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "9876543210" && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.get_random_between(low=42, high=42), ((low <= 14 && high = 7 && random <= 7)) , v_random := random => notify;
}
====
now => * => @github.comment_issue, (repo_name =~ "23456789" && issue_number > 7 && body = "12345") 
;;
Main() {
    now => @github.comment_issue(repo_name="0123456788", issue_number=55, body="0123456788") ;
}
====
* => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456788") ;
}
====
* => @weatherapi.sunrise, sunset = makeDate(1487030400000) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && date = makeDate(1462320000000)) || (location = $context.location.home && sunrise = makeDate(1462320000000)) || sunrise = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @uber.time_estimate, true , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (estimate > 30min) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @gmail.send_picture, (to = "alice@gmail.com"^^tt:email_address && subject =~ "23456789" && message =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456788", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@youtube.source, (title =~ "9876543210" && video_url = "http://www.abc.def"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "23456789" && title = "ABCDEFGHIJ" && video_id =~ "12345") || video_url = "http://www.google.com"^^tt:url || (channel_id =~ "9876543210" && channel_title =~ "12345" && title = "12345"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @dropbox.get_space_usage(), (((used > 5KB && total > 5KB) || used <= 20MB || (used >= 5KB && total >= 20MB))) , v_used := used, v_total := total => notify;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1487030400000) && duration = 2h && rem < 2h && light <= 2h && deep <= 30min) || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && light > 2h && deep < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((duration <= 30min && rem < 30min && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
@sleep-tracker.getsleep, ((duration < 30min && light < 30min && deep > 2h) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && awakeTime = makeDate(1487030400000) && duration = 30min && rem = 2h && light > 30min && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration < 2h && rem <= 30min && light > 2h && deep <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), ((asleepTime = makeDate(1487030400000) && duration <= 30min && rem > 30min && light > 2h && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @light-bulb.alert_long() ;
}
====
* => @omlet.send_to, true 
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @omlet.send_to(to=v_message, message=v_message) ;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "9876543210" || file_name =~ "ABCDEFGHIJ" || file_name = "9876543210" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @uber.price_estimate(start=$context.location.work, end=$context.location.current_location), (((start = $context.location.current_location && low_estimate >= 42 && high_estimate > 11 && surge > 14 && duration = 30min && distance < 42cm) || (uber_type =~ "0123456788" && low_estimate <= 14 && high_estimate = 42 && currency_code =~ "0123456788" && duration > 30min) || (start = $context.location.home && uber_type = "ABCDEFGHIJ" && high_estimate >= 42 && currency_code =~ "0123456788" && surge < 11 && duration < 2h && distance = 42cm))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @lg_webos_tv.set_volume, (percent <= 14 || percent >= 42 || percent < 55 || percent < 14) 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @lg_webos_tv.set_volume(percent=14) ;
}
====
@sleep-tracker.getsleep, light >= 2h , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => @car.honk_horn, true 
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && rem > 30min && light >= 2h && deep > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @car.honk_horn() ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name = "12345" || file_name = "0123456788" || file_name =~ "0123456788" || file_name = "23456789") 
;;
Main() {
    now => @almond_couch_market.search(), ((price < 11 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
@almond_bike_market.monitor, info = "23456789"  => * => notify
;;
Main() {
    @almond_bike_market.monitor(), (info = "12345")  => @icalendar.list_events(), ((sequence < 11 && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@github.new_commit, (time = makeDate(1487030400000) || (user = "alice"^^tt:username && message = "23456789" && time = makeDate(1487030400000)) || time = makeDate(1487030400000)) , v_user := user, v_message := message, v_time := time => notify
;;
Main() {
    @github.new_commit(repo_name="9876543210"), (((repo_name =~ "0123456789" && user = "alice"^^tt:username && message = "9876543210" && time = makeDate(1462320000000)) || repo_name =~ "ABCDEFGHIJ")) , v_user := user, v_message := message, v_time := time => notify;
}
====
@github.new_commit, (repo_name = "9876543210" && user = "bob"^^tt:username) , v_user := user, v_message := message, v_time := time => * => *
;;
Main() {
    @github.new_commit(repo_name="ABCDEFGHIJ"), (((repo_name =~ "0123456788" && message =~ "0123456789") || message =~ "9876543210" || repo_name =~ "0123456789" || (repo_name = "ABCDEFGHIJ" && message = "12345" && time = makeDate(1487030400000)) || (repo_name =~ "9876543210" && user = "bob"^^tt:username) || (repo_name = "9876543210" && message =~ "ABCDEFGHIJ"))) , v_user := user, v_message := message, v_time := time => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ" && sequence >= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && organizer = "0123456788") || (end_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && sequence <= 11 && organizer = "9876543210" && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((description = "23456789" && sequence > 14 && location = "23456789") || (start_date = makeDate(1487030400000) && summary = "23456789" && description =~ "ABCDEFGHIJ" && sequence > 55 && organizer =~ "ABCDEFGHIJ"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.play_url(url=v_organizer) ;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && distance = 1000m && steps >= 55 && inactiveTime > 30min) || (steps = 7 && activeTime <= 30min && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day =~ "0123456788" && distance <= 42cm && steps = 11 && inactiveTime < 30min && calories > 500kcal) || (updateTime = makeDate(1462320000000) && day = "23456789" && distance <= 42cm && steps >= 7 && activeTime < 2h && inactiveTime >= 2h && calories > 500kcal) || (day = "0123456789" && distance <= 42cm && steps <= 55 && activeTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), (((distance >= 42cm && steps < 7 && activeTime >= 30min && calories <= 500kcal) || (updateTime = makeDate(1487030400000) && distance < 42cm && steps >= 42 && activeTime = 30min && inactiveTime > 2h) || (day = "23456789" && steps > 14 && activeTime <= 2h && inactiveTime > 2h && calories >= 500kcal) || (distance < 42cm && activeTime >= 2h && calories < 500kcal) || (steps < 14 && activeTime = 2h && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @facebook.post(status="9876543210") ;
}
====
* => @security-camera.get_url, link = "http://www.abc.def"^^tt:url , v_link := link => @fitbit.recordweight, (weight < 155lb || weight > 82kg || weight = 155lb || weight > 155lb || weight >= 155lb || weight <= 155lb) 
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1487030400000) && summary = "12345" && description = "23456789" && sequence = 14 && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @security-camera.get_url(), true , v_link := link => @fitbit.recordweight(weight=155lb) ;
}
====
@security-camera.new_event, start_time = makeDate(1487030400000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @car.honk_horn() ;
}
====
* => @gmail.get_latest, label = "0123456789" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((subject = "23456789" || label =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @google_drive.create_new_drive_file(file_name=v_interest) ;
}
====
* => @almond_dates.search, ((message = "23456789" && poster = "ABCDEFGHIJ") || (interest =~ "0123456788" && poster = "9876543210") || (interest =~ "0123456788" && poster = "0123456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((duration < 30min && rem < 30min && light < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @almond_dates.search(), (message =~ "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@weatherapi.monitor, (location = $context.location.work && temperature > 22C && humidity > 11 && cloudiness > 42 && fog > 11 && weather =~ "9876543210" && status = enum(sunny)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((temperature <= 73F && wind_speed = 25mph && cloudiness > 14 && fog < 55) || (wind_speed > 5kmph && humidity > 11 && cloudiness <= 55 && weather =~ "9876543210" && status = enum(snowy)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @google_drive.create_new_drive_file, file_name =~ "0123456789" 
;;
Main() {
    @sleep-tracker.getsleep(), (((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && asleepTime = makeDate(1462320000000) && duration < 30min && rem = 2h && light < 2h && deep <= 30min) || (place = $context.location.work && awakeTime = makeDate(1487030400000) && duration >= 2h && rem > 30min && light < 2h && deep >= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @bluetooth_speaker.set_volume(percent=55) ;
}
====
* => @wall_street_journal.get_opinions, (title =~ "23456789" || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "12345" && category = "0123456788")) , v_title := title, v_link := link, v_user := user, v_category := category => @wall_street_journal.get_opinions(), ((title = "0123456789" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@phone.gps, ((altitude <= 42cm && bearing > 55) || (altitude = 42cm && bearing >= 7 && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude >= 42cm && bearing > 42 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @dropbox.open(file_name="23456789"), ((url = "ABCDEFGHIJ" || file_name = "9876543210" || (file_name = "23456789" && url = "0123456789"))) , v_url := url => notify;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_sound = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name="12345") ;
}
====
* => @almond_couch_market.search, (price >= 42 || (price >= 55 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price >= 42) || (title =~ "23456789" && price = 14)) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @slack.receive(), ((channel = "lol"^^tt:hashtag && from = "alice"^^tt:username && message =~ "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => @github.comment_issue(issue_number=v_price, body=v_title, repo_name="9876543210") ;
}
====
now => @linkedin.get_profile, (formatted_name =~ "23456789" && headline =~ "0123456788" && industry =~ "9876543210") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "ABCDEFGHIJ" || (formatted_name = "0123456788" && headline =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (industry = "ABCDEFGHIJ" && contains(positions, "23456789")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@instagram.new_picture, (link = "http://www.google.com"^^tt:url || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @weatherapi.sunrise(location=v_location), (sunset = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
now => @almond_dates.post, (interest =~ "0123456789" && poster =~ "23456789") 
;;
Main() {
    now => @almond_dates.post(interest="0123456789", message="ABCDEFGHIJ", poster="9876543210", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @security-camera.get_url, true , v_link := link => notify
;;
Main() {
    @omlet.incomingmessage(), (type = enum(picture)) , v_type := type, v_message := message => @security-camera.get_url(), true , v_link := link => notify;
}
====
@phone.receive_sms, body =~ "ABCDEFGHIJ" , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), (body = "9876543210") , v_from := from, v_body := body => notify;
}
====
@reddit_front_page.newpost, (category = "23456789" || (title = "0123456789" && link = "http://www.abc.def"^^tt:url) || (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url || (user =~ "0123456788" && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url && user =~ "23456789") || (title = "ABCDEFGHIJ" && user =~ "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
* => @twitter.follow, (user_name = "alice"^^tt:username || user_name = "alice"^^tt:username) 
;;
Main() {
    @almond_dates.monitor(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @twitter.follow(user_name=v_poster) ;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description = "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1462320000000) && sequence <= 11 && organizer = "0123456789") || (description =~ "0123456789" && sequence >= 11 && organizer =~ "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @ytranslate.detect_language(text=v_location), (text =~ "0123456788") , v_detected_language := detected_language => notify;
}
====
now => @almond_bike_market.search, (price > 14 || (info = "23456789" && price > 42) || info =~ "ABCDEFGHIJ" || price < 14 || info =~ "12345" || (info = "12345" && price > 42))  => notify
;;
Main() {
    now => @almond_bike_market.search(price=14), true  => notify;
}
====
@almond_couch_market.monitor, price <= 7 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), ((price < 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @speaker.lower_volume() ;
}
====
now => * => @slack.updateChannelPurpose, channel = "funny"^^tt:hashtag 
;;
Main() {
    now => @xkcd.random_comic(), (((number < 55 && title = "0123456788") || (number <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text =~ "23456789") || (number = 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "23456789"))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @slack.updateChannelPurpose(channel=v_alt_text, purpose=v_alt_text) ;
}
====
@thermostat.temperature, true , v_time := time, v_value := value => * => @thermostat.set_target_temperature, value <= 73F 
;;
Main() {
    @thermostat.temperature(), ((value <= 22C || time = makeDate(1462320000000))) , v_time := time, v_value := value => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => @thermostat.set_target_temperature(value=73F) ;
}
====
@bodytrace.source, ((time = makeDate(1462320000000) && weight < 155lb) || (time = makeDate(1462320000000) && weight <= 82kg) || (time = makeDate(1462320000000) && weight >= 82kg) || weight <= 82kg) , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), ((weight = 82kg || (time = makeDate(1487030400000) && weight >= 82kg) || weight < 155lb)) , v_time := time, v_weight := weight => notify;
}
====
@google_drive.new_drive_file, (file_name = "23456789" || file_name = "9876543210" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1487030400000) && summary =~ "23456789" && description = "0123456788" && organizer = "12345" && location = "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @github.comment_issue, ((repo_name = v_description && issue_number = v_sequence) || body = "9876543210") 
;;
Main() {
    @icalendar.event_begin(), ((summary = "23456789" && description =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @github.comment_issue(repo_name=v_location, issue_number=v_sequence, body=v_location) ;
}
====
@washington_post.new_blog_post, (title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(powerpost)), ((link = "http://www.google.com"^^tt:url || (section = enum(the_plum_line) && title = "23456789" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
* => @tumblr-blog.post_picture, caption = "0123456788" 
;;
Main() {
    @wall_street_journal.us_business(), (title =~ "9876543210") , v_title := title, v_link := link => @tumblr-blog.post_picture(caption=v_title, picture_url=v_title) ;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @builtin.get_random_between(low=14, high=11), ((low > 42 && high >= 7 && random >= 7)) , v_random := random => notify;
}
====
now => * => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @icalendar.list_events(), (sequence <= 55) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @instagram.get_pictures, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)) || (caption =~ "0123456789" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), ((count = 11 && caption = "23456789" && link = "http://www.google.com"^^tt:url && filter =~ "0123456789" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @instagram.get_pictures, ((count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && link = "http://www.abc.def"^^tt:url && filter =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) || (count <= 55 && caption =~ "12345" && filter =~ "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), (div < 11) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @instagram.get_pictures(count=v_yield), (((count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && filter =~ "0123456789" && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (count >= 55 && caption =~ "12345" && filter = "23456789" && location = $context.location.work))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @tumblr-blog.post_text, (title =~ "0123456789" || (title =~ "0123456789" && body =~ "12345")) 
;;
Main() {
    @fitness-tracker.getbody(), (((weight >= 155lb && leanMass > 14 && bodyFat > 11 && bmi <= 14) || (leanMass > 11 && bodyFat = 11 && bmi > 11) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight >= 155lb))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @tumblr-blog.post_text(title="0123456789", body="ABCDEFGHIJ") ;
}
====
now => * => @almond_dates.post, (interest =~ "ABCDEFGHIJ" && message = "12345") 
;;
Main() {
    now => @almond_dates.post(interest="0123456789", message="23456789", poster="ABCDEFGHIJ", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    @twitter.my_tweet(), (((text = "0123456789" && in_reply_to = "alice"^^tt:username) || text =~ "12345" || (text = "23456789" && contains(hashtags, "cat"^^tt:hashtag)) || text =~ "ABCDEFGHIJ" || in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @wall_street_journal.get_opinions(), ((link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => @bluetooth_speaker.set_volume(percent=14) ;
}
====
@almond_dates.monitor, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.set_power, true 
;;
Main() {
    @almond_dates.monitor(), (((interest = "0123456788" && message = "0123456789" && poster =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (message = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.set_power(power=enum(on)) ;
}
====
now => @xkcd.random_comic, ((number > 42 && title =~ "9876543210" && alt_text = "ABCDEFGHIJ") || (number <= 55 && link = "http://www.abc.def"^^tt:url && alt_text =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@slack.receive, timestamp =~ "23456789" , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), ((channel = "cat"^^tt:hashtag && from = "alice"^^tt:username && message = "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @heatpad.set_power(power=enum(off)) ;
}
====
@github.new_issue_comment, (repo_name =~ "23456789" && issue_number > 55 && body = "ABCDEFGHIJ" && time = makeDate(1462320000000)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => notify
;;
Main() {
    @github.new_issue_comment(repo_name="0123456789"), ((issue_number = 55 && time = makeDate(1462320000000))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @almond_bike_market.search(info="0123456788"), true  => notify;
}
====
now => @dropbox.open, (file_name = "0123456788" && url = "0123456789") , v_url := url => *
;;
Main() {
    now => @dropbox.open(file_name="23456789"), (((file_name = "0123456789" && url =~ "9876543210") || file_name =~ "0123456788" || (file_name = "0123456788" && url =~ "23456789"))) , v_url := url => notify;
}
====
* => @youtube.search_videos, ((query = "ABCDEFGHIJ" && video_id =~ "0123456788" && title = "ABCDEFGHIJ" && count <= 7) || (channel_title =~ "23456789" && title = "ABCDEFGHIJ" && description = "0123456789" && count > 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @car.honk_horn, true 
;;
Main() {
    now => @youtube.search_videos(query="0123456788"), ((query = "0123456788" && channel_title = "9876543210" && count >= 7)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @car.honk_horn() ;
}
====
* => * => @tumblr-blog.post_text, true 
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text(title=v_message, body=v_poster) ;
}
====
@rss.new_post, link = "http://www.abc.def"^^tt:url , v_title := title, v_link := link => @thecatapi.get, image_id =~ "0123456788" , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @rss.new_post(), ((title = "9876543210" || title =~ "0123456788")) , v_title := title, v_link := link => @thecatapi.get(), (count < 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @security-camera.set_power(power=enum(off)) ;
}
====
* => @phone.get_gps, ((altitude > 42cm && bearing <= 14 && speed < 25mph) || (altitude >= 42cm && bearing >= 14 && speed >= 25mph) || (altitude <= 42cm && bearing = 7 && speed < 25mph) || (location = $context.location.work && altitude < 42cm && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (time = makeDate(1487030400000)) , v_time := time, v_place := place, v_heartrate := heartrate => @phone.get_gps(), (((altitude < 1000m && bearing > 55 && speed >= 5kmph) || location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && speed >= 5kmph) || (altitude <= 42cm && speed <= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @icalendar.list_events, (start_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "12345" && location =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @builtin.thank_you, true 
;;
Main() {
    now => @icalendar.list_events(), ((description =~ "0123456788" && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @builtin.thank_you() ;
}
====
* => @nasa.apod, ((title =~ "0123456788" && description =~ "0123456789") || description = "0123456788" || description = "23456789") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @bodytrace.source(), (weight < 82kg) , v_time := time, v_weight := weight => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, (place = $context.location.current_location && heartrate <= 14) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.work || heartrate < 7 || heartrate >= 14 || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate >= 11))) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => * => @omlet.send_picture, (to = "alice"^^tt:username && caption = "12345") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "23456789" && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
now => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && subject =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @almond_couch_market.search(), (((title = "ABCDEFGHIJ" && price > 11) || price = 55)) , v_title := title, v_price := price, v_contact := contact => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @dropbox.move, new_name =~ "0123456789" 
;;
Main() {
    now => @dropbox.move(old_name="0123456789", new_name="ABCDEFGHIJ") ;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @heatpad.set_power(power=enum(on)) ;
}
====
* => @bing.image_search, (title =~ "23456789" && link = "http://www.google.com"^^tt:url) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456788"), ((tournament_league_name =~ "0123456789" && home_alias =~ "ABCDEFGHIJ" && home_name =~ "ABCDEFGHIJ" && away_points >= 55)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @bing.image_search(query=v_home_name), ((query = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width < 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => @bing.web_search, description = "0123456789" , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="ABCDEFGHIJ"), (((query = "9876543210" && title =~ "0123456789" && description = "ABCDEFGHIJ") || (query =~ "12345" && description = "23456789" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_description := description, v_link := link => notify;
}
====
@gmail.receive_email, (from_name = "ABCDEFGHIJ" && date = makeDate(1487030400000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), (((from_name =~ "ABCDEFGHIJ" && from_address = "bob@stanford.edu"^^tt:email_address && contains(labels, "23456789")) || (from_name =~ "12345" && from_address = "alice@gmail.com"^^tt:email_address && contains(labels, "ABCDEFGHIJ") && snippet =~ "9876543210"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
now => * => @almond_dates.post, ((interest = "23456789" && message = "0123456789") || (poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "12345" && message = "9876543210")) 
;;
Main() {
    now => @youtube.search_channel(query="12345"), ((channel_id =~ "0123456788" || channel_id = "ABCDEFGHIJ")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @almond_dates.post(interest=v_description, message=v_title, poster=v_title, phone=v_title) ;
}
====
@security-camera.new_event, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) || has_person = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
now => @holidays.next_us_holiday_event, (summary =~ "9876543210" && description = "0123456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary =~ "0123456789" && description = "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight >= 155lb)) , v_time := time, v_weight := weight => @imgflip.list(), (((name = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || name = "23456789")) , v_name := name, v_picture_url := picture_url => @omlet.send_to(to="bob"^^tt:username, message="0123456789") ;
}
====
@holidays.uk_holiday_event, date = makeDate(1462320000000) , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((summary = "23456789" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => @dropbox.list_folder(folder_name=v_description), ((file_name =~ "9876543210" && is_folder = true && last_modified = makeDate(1487030400000) && file_size = 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), (title =~ "0123456788") , v_title := title, v_link := link => @giphy.get_tag(tag=v_title), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @speaker.play_music() ;
}
====
* => @imgflip.generate, ((text_top = "0123456789" && text_bottom = "ABCDEFGHIJ" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_bottom =~ "12345" && name =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1462320000000) && has_sound = false) || (has_sound = true && has_person = false))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @imgflip.generate(template="12345", text_top="0123456789", text_bottom="0123456788"), ((text_bottom = "12345" && name = "0123456789")) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @holidays.next_us_holiday_event, ((summary =~ "9876543210" && description =~ "0123456789") || description =~ "9876543210") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((date = makeDate(1462320000000) && summary = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @thermostat.set_target_temperature, (value < 73F || value = 22C) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @thermostat.set_target_temperature(value=73F) ;
}
====
* => @linkedin.get_profile, ((industry =~ "9876543210" && contains(positions, "9876543210") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "9876543210" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "ABCDEFGHIJ" && industry =~ "12345" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "23456789" && industry =~ "9876543210" && specialties =~ "ABCDEFGHIJ" && contains(positions, "ABCDEFGHIJ") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @builtin.debug_log(message="9876543210") ;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description =~ "0123456788" && sequence <= 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((summary = "0123456788" && description =~ "23456789" && sequence < 11) || (end_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description = "0123456788" && organizer =~ "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@slack.receive, ((timestamp = "23456789" && from = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (timestamp = "23456789" && from = "bob"^^tt:username && message = "23456789") || timestamp =~ "0123456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((channel = "funny"^^tt:hashtag && timestamp =~ "23456789" && message = "12345")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @uber.price_estimate(start=$context.location.current_location, end=$context.location.work), ((high_estimate >= 42 && surge >= 42 && duration >= 30min && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @weatherapi.current, ((wind_speed = 5kmph && humidity > 11 && fog >= 42) || (wind_speed >= 5kmph && cloudiness > 11 && weather = "9876543210") || (location = $context.location.work && temperature < 22C && wind_speed > 25mph && cloudiness > 11) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed >= 5kmph && humidity < 42 && weather =~ "23456789") || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature >= 22C && cloudiness <= 7 && fog < 11 && weather = "0123456789") || (temperature < 73F && wind_speed <= 25mph && humidity >= 55 && fog > 7 && weather = "0123456789") || (temperature >= 73F && humidity < 42 && cloudiness <= 11) || (temperature >= 73F && wind_speed <= 25mph && humidity = 42 && cloudiness < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((temperature <= 22C && wind_speed <= 25mph && fog > 14 && weather = "23456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
* => @twitter.follow, (user_name = "alice"^^tt:username || user_name = "bob"^^tt:username || user_name = "alice"^^tt:username) 
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "9876543210" && description = "ABCDEFGHIJ" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @twitter.follow(user_name=v_summary) ;
}
====
@rss.new_post, (title = "0123456789" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @almond_couch_market.post(title=v_title, contact=v_title, price=14) ;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1462320000000) && summary = "23456789") || summary =~ "12345") , v_date := date, v_summary := summary, v_description := description => @uber.price_estimate, (uber_type = v_description && high_estimate > 11 && surge > 55 && duration >= 2h && distance >= 1000m) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @holidays.uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @uber.price_estimate(start=$context.location.work, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && low_estimate < 42 && high_estimate < 42 && currency_code = "0123456789" && duration > 30min) || (start = $context.location.work && end = $context.location.work && uber_type = "ABCDEFGHIJ" && high_estimate < 14 && currency_code =~ "12345" && surge < 14 && duration < 30min && distance <= 1000m) || (end = $context.location.home && uber_type =~ "23456789" && low_estimate <= 55 && distance > 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => * => @phone.notify, ((title = "23456789" && message =~ "12345") || (title =~ "0123456788" && message =~ "12345")) 
;;
Main() {
    @github.new_milestone(repo_name="0123456788"), (repo_name = "12345") , v_user := user, v_description := description, v_time := time => @phone.notify(message=v_description, title="9876543210") ;
}
====
@instagram.new_picture, link = "http://www.abc.def"^^tt:url , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.abc.def"^^tt:url && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @twitter.search_by_hashtag, ((count > 11 && text =~ "9876543210" && contains(urls, "http://www.abc.def"^^tt:url)) || (count = 42 && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="alice"^^tt:username, in_reply_to="alice"^^tt:username), ((count <= 42 && contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @dropbox.create_new_folder(folder_name=v_text) ;
}
====
@scale.source, (value >= 155lb || (time = makeDate(1487030400000) && value < 155lb)) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (value <= 82kg) , v_time := time, v_value := value => @xkcd.get_comic(), ((number < 55 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @google_drive.create_new_drive_file, file_name =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @builtin.get_date(), (date = makeDate(1462320000000)) , v_date := date => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
* => @almond_bike_market.post, (poster =~ "0123456789" || (title = "0123456788" && price <= 11 && info =~ "ABCDEFGHIJ")) 
;;
Main() {
    @bodytrace.source(), (time = makeDate(1487030400000)) , v_time := time, v_weight := weight => @almond_bike_market.post(title="0123456788", price=7, info="0123456789", poster="23456789") ;
}
====
now => @gmail.get_latest, subject = "12345" , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((subject =~ "9876543210" && label = "0123456789") || (subject =~ "12345" && label =~ "0123456789") || (subject = "ABCDEFGHIJ" && label =~ "23456789"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @fitbit.getsteps, (steps < 7 || (date = makeDate(1462320000000) && steps <= 7)) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), ((date = makeDate(1487030400000) || date = makeDate(1462320000000))) , v_date := date, v_steps := steps => notify;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @heatpad.set_power(power=enum(on)) ;
}
====
* => @weatherapi.moon, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || (location = $context.location.current_location && phase =~ "ABCDEFGHIJ") || location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && date = makeDate(1487030400000)) || date = makeDate(1487030400000)) , v_date := date, v_phase := phase => notify
;;
Main() {
    now => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (location = $context.location.current_location) , v_date := date, v_phase := phase => notify;
}
====
now => @almond_couch_market.search, price < 14 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((title = "0123456788" && price > 14 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @lg_webos_tv.unmute() ;
}
====
* => @fitbit.recordweight, (weight < 155lb || weight >= 82kg || weight = 82kg) 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "0123456789" || (message = "12345" && poster = "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @fitbit.recordweight(weight=82kg) ;
}
====
* => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @security-camera.set_power(power=enum(off)) ;
}
====
@weatherapi.monitor, (location = $context.location.home && temperature > 73F && humidity >= 55 && cloudiness >= 7 && fog <= 7 && weather =~ "12345" && status = enum(sleety)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @ytranslate.detect_language, true , v_detected_language := detected_language => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature > 73F && wind_speed <= 25mph && cloudiness < 14 && fog >= 7 && status = enum(drizzling))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @ytranslate.detect_language(text="12345"), (text = "0123456788") , v_detected_language := detected_language => notify;
}
====
* => @weatherapi.sunrise, sunset = makeDate(1487030400000) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @builtin.at(time=makeTime(7,30)), true  => @weatherapi.sunrise(location=$context.location.work), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@icalendar.event_begin, ((start_date = makeDate(1462320000000) && summary =~ "0123456789" && description =~ "0123456788" && sequence >= 55 && organizer =~ "12345" && location =~ "9876543210") || (start_date = makeDate(1462320000000) && summary = "9876543210" && description = "9876543210" && sequence < 55)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1487030400000) && summary =~ "0123456788" && sequence > 14 && organizer = "0123456788" && location =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @imgflip.generate(template=v_location, text_top=v_location, text_bottom=v_description), ((name =~ "0123456788" || (template =~ "23456789" && text_top = "12345") || (text_bottom =~ "23456789" && name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || template =~ "9876543210" || (template = "0123456788" && text_bottom = "0123456789" && name =~ "23456789"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@github.new_milestone, (repo_name = "23456789" || (repo_name =~ "23456789" && description =~ "12345" && time = makeDate(1462320000000))) , v_user := user, v_description := description, v_time := time => * => notify
;;
Main() {
    @github.new_milestone(repo_name="12345"), ((user = "alice"^^tt:username || (repo_name = "ABCDEFGHIJ" && time = makeDate(1487030400000)))) , v_user := user, v_description := description, v_time := time => notify;
}
====
* => @almond_dates.search, (interest =~ "ABCDEFGHIJ" || poster =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @wall_street_journal.us_business(), ((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @almond_dates.search(), ((interest =~ "9876543210" && message =~ "23456789" && poster = "23456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @linkedin.get_profile, ((headline = "9876543210" && industry =~ "0123456788" && specialties = "ABCDEFGHIJ") || (industry =~ "9876543210" && contains(positions, "ABCDEFGHIJ") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "ABCDEFGHIJ" && headline =~ "23456789" && industry =~ "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), (((headline = "ABCDEFGHIJ" && industry =~ "12345") || headline =~ "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@tumblr.new_text, (blog_name = "0123456788" && body = "ABCDEFGHIJ") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), ((blog_name = "9876543210" || (blog_name = "0123456789" && title = "23456789" && link = "http://www.google.com"^^tt:url))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (start = $context.location.current_location) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @dropbox.move, new_name =~ "12345" 
;;
Main() {
    now => @dropbox.move(old_name="ABCDEFGHIJ", new_name="0123456789") ;
}
====
* => @dropbox.move, (old_name = "ABCDEFGHIJ" && new_name =~ "12345") 
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => @dropbox.move(old_name="23456789", new_name="23456789") ;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "9876543210" || message = "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.thank_you() ;
}
====
now => @onedrive.rename_file, old_name =~ "23456789" 
;;
Main() {
    now => @onedrive.rename_file(old_name="0123456789", new_name="9876543210") ;
}
====
now => * => @twitter.unfollow, true 
;;
Main() {
    now => @weatherapi.current(location=$context.location.current_location), (((location = $context.location.current_location && humidity >= 11 && cloudiness < 11 && weather =~ "0123456788") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature = 73F && cloudiness <= 55 && fog < 42) || (humidity > 42 && weather = "0123456788") || (temperature > 22C && wind_speed < 5kmph && humidity < 42 && cloudiness < 11 && fog < 11 && weather = "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
now => @tumblr-blog.post_text, (title =~ "12345" || (title =~ "0123456789" && body = "0123456789") || (title =~ "0123456788" && body = "0123456788") || body =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @tumblr-blog.post_text(title="ABCDEFGHIJ", body="0123456789") ;
}
====
* => @bing.web_search, ((title = "ABCDEFGHIJ" && description = "12345") || (query =~ "23456789" && description =~ "ABCDEFGHIJ") || (query = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description =~ "9876543210" && sequence = 55 && organizer =~ "9876543210") || (description = "23456789" && sequence <= 55))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @bing.web_search(query=v_location), ((query = "ABCDEFGHIJ" && title =~ "23456789" && description = "12345")) , v_title := title, v_description := description, v_link := link => notify;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), (value > 82kg) , v_time := time, v_value := value => @thermostat.get_humidity(), true , v_value := value => notify;
}
====
now => @almond_dates.post, (interest = "12345" && message = "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_dates.post(interest="12345", message="23456789", poster="0123456788", phone="+15551234567"^^tt:phone_number) ;
}
====
now => @almond_dates.post, ((message =~ "12345" && phone = "+15551234567"^^tt:phone_number) || poster = "9876543210" || (interest = "12345" && message =~ "12345" && poster = "ABCDEFGHIJ")) 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="0123456788", poster="12345", phone="+16501234567"^^tt:phone_number) ;
}
====
@wall_street_journal.world_news, title =~ "9876543210" , v_title := title, v_link := link => *
;;
Main() {
    @wall_street_journal.world_news(), true , v_title := title, v_link := link => notify;
}
====
* => @instagram.get_pictures, (filter =~ "23456789" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate = 42 || time = makeDate(1487030400000) || heartrate <= 7 || heartrate < 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @instagram.get_pictures(), ((count <= 55 && caption =~ "ABCDEFGHIJ" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@icalendar.event_begin, summary =~ "ABCDEFGHIJ" , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), (((summary =~ "23456789" && sequence <= 14 && location =~ "9876543210") || (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && sequence <= 11 && location = "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@phdcomics.new_post, (title =~ "9876543210" || (title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "12345" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title = "23456789") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title =~ "23456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @xkcd.random_comic(), ((number = 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@washington_post.new_blog_post, (section = enum(right_turn) && title =~ "12345") , v_title := title, v_link := link => * => @light-bulb.set_power, true 
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_everything)), ((section = enum(the_plum_line) && title = "0123456788")) , v_title := title, v_link := link => @light-bulb.set_power(power=enum(on)) ;
}
====
now => @dropbox.create_new_folder, (folder_name =~ "0123456789" || folder_name = "9876543210") 
;;
Main() {
    now => @dropbox.create_new_folder(folder_name="23456789") ;
}
====
now => * => @builtin.cool, true 
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), (((temperature = 73F && humidity >= 11 && cloudiness < 42 && fog < 7) || (temperature <= 22C && wind_speed > 5kmph && humidity <= 11 && weather = "0123456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @builtin.cool() ;
}
====
@almond_bike_market.monitor, (info = "23456789" || info =~ "ABCDEFGHIJ" || (info =~ "0123456789" && price < 42))  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=14), true  => @instagram.get_pictures(), ((count <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @almond_dates.search, (interest = "9876543210" && message =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text, (title =~ v_message && body =~ v_interest) 
;;
Main() {
    now => @almond_dates.search(), (((poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && message =~ "ABCDEFGHIJ") || (interest =~ "0123456788" && poster =~ "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text(body=v_poster, title="9876543210") ;
}
====
now => * => @almond_couch_market.post, (title =~ "0123456788" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @thermostat.get_humidity(), ((value > 14 || value = 55 || value = 11 || value = 42)) , v_value := value => @almond_couch_market.post(title="ABCDEFGHIJ", price=55, contact="+16501234567"^^tt:phone_number) ;
}
====
* => @bing.web_search, (link = "http://www.google.com"^^tt:url || (query = "0123456788" && description =~ "12345")) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), (((ask_price > 55 && bid_price > 11) || ask_price = 7)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @bing.web_search(query=v_company_name), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify;
}
====
@omlet.newmessage, ((type = enum(text) && message =~ "ABCDEFGHIJ") || type = enum(text)) , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
* => @linkedin.get_profile, (formatted_name =~ "ABCDEFGHIJ" && headline =~ "12345" && industry = "23456789" && specialties =~ "0123456789" && contains(positions, "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @almond_bike_market.monitor(price=55), true  => @linkedin.get_profile(), (((formatted_name = "12345" && headline = "23456789" && industry = "ABCDEFGHIJ" && specialties = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @slack.setPresence, presence = enum(away) 
;;
Main() {
    now => @slack.setPresence(presence=enum(away)) ;
}
====
* => @ytranslate.detect_language, ((text =~ "0123456788" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || text =~ "23456789" || text = "ABCDEFGHIJ" || text = "ABCDEFGHIJ") , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="9876543210"), true , v_detected_language := detected_language => notify;
}
====
@weatherapi.monitor, ((wind_speed > 25mph && humidity >= 11 && cloudiness <= 11) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature > 73F && humidity = 11 && fog <= 55) || (temperature >= 22C && wind_speed <= 5kmph && humidity <= 42 && fog <= 42 && weather = "9876543210") || (location = $context.location.work && temperature < 73F && humidity > 55 && cloudiness <= 11 && fog <= 14 && weather =~ "0123456789" && status = enum(raining))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((temperature > 73F && wind_speed = 25mph && humidity <= 14 && weather = "23456789" && status = enum(raining)) || (temperature > 22C && cloudiness < 55 && fog > 55 && weather = "0123456788"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate < 11 || time = makeDate(1487030400000))) , v_time := time, v_place := place, v_heartrate := heartrate => @imgflip.generate(template="ABCDEFGHIJ", text_top="12345", text_bottom="0123456788"), ((template =~ "23456789" && name = "23456789")) , v_name := name, v_picture_url := picture_url => @heatpad.set_power(power=enum(on)) ;
}
====
@weatherapi.monitor, ((humidity = 7 && cloudiness >= 55 && weather = "0123456789" && status = enum(windy)) || (temperature <= 73F && wind_speed <= 5kmph && humidity <= 11 && cloudiness < 7 && fog < 55 && weather =~ "ABCDEFGHIJ") || (temperature <= 22C && wind_speed <= 25mph && humidity <= 55 && status = enum(sunny))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @security-camera.set_power, true 
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((wind_speed = 5kmph && fog = 55 && weather =~ "ABCDEFGHIJ") || (temperature <= 73F && wind_speed >= 25mph && humidity >= 55 && weather = "0123456788" && status = enum(sunny)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @security-camera.set_power(power=enum(off)) ;
}
====
* => * => @phone.call, true 
;;
Main() {
    @jawbone_up.getmove(), (((day =~ "ABCDEFGHIJ" && distance <= 42cm && steps > 7 && activeTime > 30min && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && distance = 42cm && steps > 7 && activeTime >= 30min && inactiveTime >= 2h && calories <= 500kcal) || (steps >= 55 && activeTime > 2h && inactiveTime = 30min && calories <= 500kcal) || (day =~ "0123456788" && distance <= 1000m && steps >= 14 && activeTime < 30min) || day =~ "0123456789")) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.get_random_between(low=v_steps, high=v_steps), ((low < 11 && high > 7 && random > 42)) , v_random := random => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
* => @lg_webos_tv.set_volume, percent >= 11 
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), ((body =~ "0123456789" || (blog_name =~ "12345" && title =~ "23456789" && body =~ "0123456788"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @lg_webos_tv.set_volume(percent=11) ;
}
====
@slack.receive, ((timestamp = "0123456788" && from = "alice"^^tt:username) || (channel = "lol"^^tt:hashtag && timestamp = "23456789" && message = "23456789") || (channel = "lol"^^tt:hashtag && timestamp = "23456789" && message =~ "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), (from = "bob"^^tt:username) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @speaker.raise_volume() ;
}
====
now => @thermostat.get_temperature, (value > 22C || value < 22C || value >= 73F || value < 73F) , v_value := value => notify
;;
Main() {
    now => @thermostat.get_temperature(), ((value <= 22C || value >= 73F)) , v_value := value => notify;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && label = "23456789") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), ((label =~ "9876543210" || label = "12345")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @dropbox.list_folder, ((is_folder = false && file_size < 5KB) || (folder_name = "0123456788" && file_name = "0123456789" && file_size = 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="9876543210"), (((is_folder = true && last_modified = makeDate(1487030400000) && file_size <= 5KB) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456788" && is_folder = true && file_size < 20MB && full_path = "0123456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
now => * => @speaker.set_volume, percent >= 14 
;;
Main() {
    now => @nasa.apod(), (((title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "0123456789" && description =~ "0123456788") || (title = "12345" && description =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => @speaker.set_volume(percent=11) ;
}
====
@reddit_front_page.newpost, (category =~ "9876543210" || (user =~ "ABCDEFGHIJ" && category =~ "0123456788") || title =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title = "9876543210" && category = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
* => @youtube.search_videos, (query = "9876543210" && channel_id = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @youtube.search_videos(query="0123456789"), ((query = "9876543210" && video_id = "0123456788" && channel_title = "23456789" && description = "12345" && count >= 55)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
now => * => @github.add_email, true 
;;
Main() {
    now => @bing.image_search(query="12345"), (((query =~ "0123456789" && title =~ "12345" && width < 55 && height <= 55) || (query =~ "12345" && width > 14 && height > 14))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @github.add_email(email=v_title) ;
}
====
* => @twitter.search_by_hashtag, ((count >= 42 && text = "0123456789") || (count > 14 && text =~ "9876543210" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature = 73F && humidity <= 42 && cloudiness >= 55 && fog <= 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @twitter.search_by_hashtag(query_hashtag=v_weather, count=v_fog, from=v_weather), (((count >= 55 && from = "alice"^^tt:username) || (count > 14 && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @dropbox.create_new_folder, (folder_name = "23456789" || folder_name = "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_dates.search(), (poster = "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @dropbox.create_new_folder(folder_name=v_message) ;
}
====
* => @bing.web_search, (link = "http://www.google.com"^^tt:url || (query = "0123456788" && description =~ "12345")) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    now => @bing.web_search(query="9876543210"), (description =~ "12345") , v_title := title, v_description := description, v_link := link => @twitter.follow(user_name=v_title) ;
}
====
* => @ytranslate.translate, (text =~ "0123456788" || (source_language = "zh"^^tt:iso_lang_code("Chinese") && translated_text = "0123456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @scale.source(), (value > 155lb) , v_time := time, v_value := value => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="12345"), (translated_text =~ "0123456789") , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@tumblr.new_text, (blog_name = "9876543210" || blog_name = "12345" || (title = "0123456789" && contains(tags, "cat"^^tt:hashtag) && body =~ "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="0123456788"), (link = "http://www.google.com"^^tt:url) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @phone.call(number=v_title) ;
}
====
now => @phone.get_gps, ((location = $context.location.current_location && bearing >= 55 && speed < 25mph) || speed < 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (((bearing <= 11 && speed <= 5kmph) || bearing <= 11)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @reddit_front_page.newpost(), ((title = "ABCDEFGHIJ" && user = "23456789" && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @dropbox.create_new_folder(folder_name="23456789") ;
}
====
now => @builtin.get_date, (date = makeDate(1462320000000) || date = makeDate(1487030400000)) , v_date := date => *
;;
Main() {
    now => @builtin.get_date(), true , v_date := date => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
@youtube.source, channel_title =~ "12345" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((title =~ "12345" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @lg_webos_tv.raise_volume() ;
}
====
* => @almond_bike_market.post, info = "0123456788" 
;;
Main() {
    @almond_bike_market.monitor(info="0123456789"), (((info = "0123456788" && price >= 55) || (info =~ "ABCDEFGHIJ" && price >= 11)))  => @almond_bike_market.post(title="ABCDEFGHIJ", price=7, info="12345", poster="23456789") ;
}
====
@tumblr.new_text, (blog_name = "9876543210" || blog_name = "12345" || (title = "0123456789" && contains(tags, "cat"^^tt:hashtag) && body =~ "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((blog_name = "23456789" && title =~ "ABCDEFGHIJ" && body = "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @phone.call(number=v_title) ;
}
====
now => * => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @thecatapi.get(), (((count > 42 && link = "http://www.google.com"^^tt:url) || (image_id =~ "12345" && count <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @linkedin.share(status=v_image_id) ;
}
====
* => @uber.time_estimate, (start = $context.location.work && uber_type =~ "23456789" && estimate < 2h) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1487030400000) || value > 55)) , v_time := time, v_value := value => @uber.time_estimate(start=$context.location.work), (((uber_type =~ "9876543210" && estimate > 2h) || estimate < 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @tumblr-blog.post_picture, caption = "23456789" 
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "ABCDEFGHIJ" && price = 55) || (title = "0123456789" && price >= 11) || price >= 42)) , v_title := title, v_price := price, v_contact := contact => @tumblr-blog.post_picture(caption="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @icalendar.list_events, ((summary =~ "23456789" && location = "0123456788") || (start_date = makeDate(1487030400000) && summary = "12345" && description =~ "0123456789" && sequence >= 7 && location = "0123456789") || (end_date = makeDate(1462320000000) && summary = "23456789" && description = "23456789" && organizer =~ "0123456789" && location =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_partisan)), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => @icalendar.list_events(), (start_date = makeDate(1462320000000)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @fitbit.getbody, ((bmi < 11 && fat = 7) || (weight < 82kg && bmi < 14) || (weight >= 155lb && bmi <= 55) || (weight >= 82kg && fat < 14) || (weight = 155lb && bmi <= 7 && fat >= 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), true , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@almond_couch_market.monitor, (contact = "+15551234567"^^tt:phone_number || (title =~ "ABCDEFGHIJ" && price < 11)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (((title = "0123456788" && price = 55) || (title = "23456789" && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, message =~ "9876543210" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @phone.gps(), ((altitude <= 1000m && bearing >= 42 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_dates.search(), (((poster = "9876543210" && phone = "+16501234567"^^tt:phone_number) || poster =~ "9876543210" || (interest = "0123456788" && poster =~ "ABCDEFGHIJ") || (message =~ "12345" && poster =~ "9876543210"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.abc.def"^^tt:url && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @thecatapi.get(), (count >= 55) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @github.add_email(email=v_image_id) ;
}
====
* => * => @almond_dates.post, true 
;;
Main() {
    @security-camera.new_event(), (((has_sound = true && has_motion = true && has_person = false) || (has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @phone.get_gps(), ((altitude > 1000m && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_dates.post(interest="0123456788", message="ABCDEFGHIJ", poster="12345", phone="+16501234567"^^tt:phone_number) ;
}
====
now => * => @omlet.send_picture, (caption =~ "23456789" || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345")) 
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="23456789"), (((source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "it"^^tt:iso_lang_code("Italian") && text = "0123456788" && translated_text = "0123456788") || text =~ "23456789")) , v_source_language := source_language, v_translated_text := translated_text => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
* => @thecatapi.get, (image_id =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @icalendar.list_events, ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && sequence >= 42 && organizer = "0123456788" && location =~ "12345") || (description =~ "0123456788" && sequence < 14 && organizer =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && description = "9876543210" && sequence >= 42 && organizer = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @car.honk_horn() ;
}
====
* => @xkcd.random_comic, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @github.new_issue(repo_name="0123456789"), (((user = "bob"^^tt:username && number < 14 && time = makeDate(1487030400000)) || (repo_name =~ "0123456789" && user = "bob"^^tt:username && number > 42) || (title = "12345" && time = makeDate(1462320000000)) || (number <= 11 && body = "ABCDEFGHIJ") || (repo_name = "0123456788" && user = "bob"^^tt:username && time = makeDate(1487030400000)) || (repo_name =~ "ABCDEFGHIJ" && number = 42 && body =~ "23456789"))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @xkcd.random_comic(), ((number <= 42 && alt_text =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @nasa.rover, (camera_used =~ "ABCDEFGHIJ" || count < 7 || (count <= 11 && camera_used =~ "ABCDEFGHIJ")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(wonkblog)), ((section = enum(the_fix) && title =~ "0123456788")) , v_title := title, v_link := link => @nasa.rover(), ((date_taken = makeDate(1487030400000) && count < 7 && camera_used = "23456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => @speaker.set_volume, (percent < 7 || percent = 14) 
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="ABCDEFGHIJ"), ((tournament_league_name =~ "0123456789" && away_name = "0123456788" && home_name =~ "12345" && scheduled_time =~ "12345" && home_points > 42)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @speaker.set_volume(percent=v_away_points) ;
}
====
now => @thermostat.get_humidity, value >= 7 , v_value := value => *
;;
Main() {
    now => @thermostat.get_humidity(), (value >= 42) , v_value := value => @onedrive.upload_picture(file_name="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@onedrive.file_created, (file_name =~ "23456789" || file_name = "9876543210" || file_name =~ "0123456789") , v_file_name := file_name => notify
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => notify;
}
====
* => * => @almond_bike_market.post, ((price >= 42 && poster = "0123456789") || title =~ "0123456788") 
;;
Main() {
    @xkcd.new_whatif(), (title = "9876543210") , v_title := title, v_link := link => @uber.price_estimate(start=$context.location.work, end=$context.location.home), (((start = $context.location.home && end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "0123456789" && low_estimate >= 55 && surge <= 42 && duration > 2h && distance >= 1000m) || (high_estimate >= 55 && currency_code = "0123456788" && surge < 42 && distance <= 42cm) || (uber_type = "9876543210" && low_estimate >= 14 && high_estimate > 55 && currency_code = "ABCDEFGHIJ" && surge >= 7 && duration < 30min && distance >= 42cm))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @almond_bike_market.post(price=v_high_estimate, title="ABCDEFGHIJ", info="23456789", poster="0123456789") ;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary = "0123456789" && location = "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "12345" && sequence <= 42 && organizer = "ABCDEFGHIJ") || organizer =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary = "12345" && sequence < 7 && organizer =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.unmute() ;
}
====
@wall_street_journal.lifestyle, ((title = "0123456788" && link = "http://www.abc.def"^^tt:url) || title =~ "9876543210") , v_title := title, v_link := link => notify
;;
Main() {
    @wall_street_journal.lifestyle(), true , v_title := title, v_link := link => notify;
}
====
* => @facebook.post_picture, caption =~ "23456789" 
;;
Main() {
    @twitter.source(), ((text = "23456789" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="ABCDEFGHIJ") ;
}
====
* => @thermostat.set_minmax_temperature, high < 73F 
;;
Main() {
    @twitter.source(), (contains(hashtags, "cat"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @thermostat.set_minmax_temperature(low=22C, high=73F) ;
}
====
now => * => @speaker.set_volume, (percent >= 14 || percent <= 14) 
;;
Main() {
    now => @twitter.search(query="ABCDEFGHIJ", in_reply_to="alice"^^tt:username), ((count > 14 && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @speaker.set_volume(percent=11) ;
}
====
* => @twitter.follow, user_name = "alice"^^tt:username 
;;
Main() {
    now => @twitter.follow(user_name="bob"^^tt:username) ;
}
====
@omlet.incomingmessage, message =~ "23456789" , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (((type = enum(picture) && message =~ "12345") || message =~ "23456789")) , v_type := type, v_message := message => @icalendar.list_events(), (((end_date = makeDate(1487030400000) && description = "12345" && organizer =~ "0123456788") || (summary =~ "0123456788" && description = "12345" && organizer =~ "23456789" && location =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@washington_post.new_blog_post, title = "0123456788" , v_title := title, v_link := link => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(right_turn)), ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify;
}
====
now => @linkedin.get_profile, industry = "0123456789" , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @linkedin.get_profile(), (((formatted_name = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || formatted_name = "0123456788")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => * => @omlet.send_picture, ((to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") || to = "bob"^^tt:username) 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((end = $context.location.work && low_estimate >= 42 && high_estimate = 55 && currency_code =~ "12345" && surge >= 42 && duration <= 30min && distance >= 1000m) || (low_estimate > 42 && high_estimate <= 14 && surge > 42 && duration >= 2h && distance >= 42cm))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @omlet.send_picture(to=v_uber_type, picture_url=v_currency_code, caption=v_currency_code) ;
}
====
* => * => @light-bulb.set_power, (power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="bar"^^sportradar:eu_soccer_team("Barcellona")), ((watched_is_home = true && home_points <= 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @light-bulb.set_power(power=enum(on)) ;
}
====
* => @dropbox.create_new_folder, true 
;;
Main() {
    @activity-tracker.getmove(), (((day = "0123456788" && distance < 1000m && steps <= 14 && activeTime < 30min && inactiveTime > 2h && calories <= 500kcal) || (day =~ "9876543210" && distance <= 42cm && activeTime > 30min && inactiveTime >= 30min && calories < 500kcal) || (distance < 42cm && steps < 42 && activeTime < 2h && inactiveTime >= 30min && calories < 500kcal) || (day = "0123456789" && inactiveTime >= 2h && calories = 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @dropbox.create_new_folder(folder_name="12345") ;
}
====
@nasa.asteroid, ((name = "0123456789" && estimated_diameter_max < 42cm && dangerous = true && relative_velocity > 25mph) || (asteroid_id < 14 && estimated_diameter_min <= 1000m && closest_distance_to_earth <= 42cm) || (asteroid_id > 55 && estimated_diameter_min > 42cm && estimated_diameter_max < 42cm && closest_distance_to_earth <= 42cm && orbiting_body =~ "ABCDEFGHIJ")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => @thermostat.set_minmax_temperature, low >= 22C 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 11 && name =~ "ABCDEFGHIJ" && estimated_diameter_min >= 1000m && estimated_diameter_max <= 1000m && closest_distance_to_earth <= 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @thermostat.set_minmax_temperature(low=22C, high=73F) ;
}
====
* => @nasa.rover, (count < 14 && camera_used =~ "ABCDEFGHIJ") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @almond_bike_market.monitor(price=14), (price <= 55)  => @nasa.rover(), (((date_taken = makeDate(1462320000000) && camera_used =~ "0123456789") || (count <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@almond_dates.monitor, (interest = "0123456788" && message = "12345" && poster = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "ABCDEFGHIJ" && message = "0123456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @wall_street_journal.get_opinions, (link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @heatpad.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, ((day = "0123456788" && distance > 1000m && steps >= 55 && inactiveTime <= 30min) || (day = "9876543210" && steps > 42 && activeTime <= 2h && inactiveTime > 2h && calories <= 500kcal) || (day = "12345" && distance >= 42cm && steps < 7 && activeTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((day =~ "ABCDEFGHIJ" && distance > 42cm && steps <= 55 && inactiveTime >= 30min) || (updateTime = makeDate(1462320000000) && day = "0123456788" && activeTime >= 2h && inactiveTime <= 30min && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_couch_market.search(), (((title = "23456789" && price <= 42) || price < 42 || contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @omlet.send_to, ((to = "bob"^^tt:username && message =~ "9876543210") || message =~ "0123456789" || to = "bob"^^tt:username || message = "ABCDEFGHIJ") 
;;
Main() {
    @slack.receive(), (((from = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && from = "alice"^^tt:username && message = "23456789"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @omlet.send_to(to=v_timestamp, message=v_timestamp) ;
}
====
now => @dropbox.list_folder, (last_modified = makeDate(1487030400000) && file_size < 5KB && full_path =~ "0123456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), (((file_name =~ "0123456788" && is_folder = true && file_size <= 20MB && full_path = "23456789") || (folder_name = "12345" && file_name = "0123456788" && file_size = 20MB && full_path = "9876543210") || (folder_name =~ "23456789" && file_name = "0123456789" && last_modified = makeDate(1462320000000) && file_size <= 5KB && full_path = "ABCDEFGHIJ"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @thermostat.get_humidity, value > 11 , v_value := value => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456789"), (((tournament_full_name = "0123456789" && away_name =~ "0123456789" && home_name = "ABCDEFGHIJ" && game_status = enum(closed) && away_points < 14 && home_points > 14) || (tournament_full_name = "12345" && tournament_league_name =~ "9876543210" && away_name = "9876543210" && home_name = "0123456789" && game_status = enum(scheduled) && home_points < 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @thermostat.get_humidity(), true , v_value := value => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && weight <= 82kg) || weight < 155lb || (time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @car.honk_horn() ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1487030400000) && duration <= 2h && light <= 2h && deep = 2h) || (asleepTime = makeDate(1462320000000) && duration <= 30min && rem >= 30min && light <= 2h && deep < 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @github.comment_issue(repo_name="ABCDEFGHIJ", issue_number=11, body="9876543210") ;
}
====
@xkcd.new_comic, (number > 14 && link = "http://www.google.com"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @xkcd.new_comic(), ((number > 14 && title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text(title="0123456788", body="ABCDEFGHIJ") ;
}
====
now => * => @almond_bike_market.post, ((title =~ "0123456789" && price >= 11 && info =~ "0123456789" && poster = "0123456789") || (title =~ "23456789" && price > 55 && info = "0123456788")) 
;;
Main() {
    now => @almond_bike_market.post(title="0123456788", price=11, info="9876543210", poster="9876543210") ;
}
====
@onedrive.file_created, (file_name =~ "9876543210" || file_name =~ "23456789" || file_name =~ "9876543210") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), (file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => @thermostat.get_hvac_state(), ((mode = enum(cool) || mode = enum(off) || mode = enum(cool))) , v_mode := mode, v_state := state => notify;
}
====
@activity-tracker.getmove, ((day =~ "9876543210" && inactiveTime < 30min && calories >= 500kcal) || (day = "9876543210" && distance >= 42cm && steps <= 55 && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((distance >= 42cm && steps <= 14 && activeTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.notify(message=v_day) ;
}
====
* => @giphy.get, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), (((blog_name = "9876543210" && title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && contains(tags, "funny"^^tt:hashtag)) || (date = makeDate(1487030400000) && title = "0123456788"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@google_drive.new_drive_file, (file_name = "ABCDEFGHIJ" || file_name = "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
* => @lg_webos_tv.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @scale.source(), ((value > 82kg || value >= 155lb || value < 155lb || value < 155lb)) , v_time := time, v_value := value => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
now => @imgflip.list, ((name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || name =~ "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => notify;
}
====
@twitter.my_tweet, (contains(hashtags, "funny"^^tt:hashtag) || (text = "23456789" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @car.honk_horn, true 
;;
Main() {
    @twitter.my_tweet(), true , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @car.honk_horn() ;
}
====
@youtube.source, ((channel_id = "9876543210" && channel_title = "ABCDEFGHIJ" && title =~ "23456789" && video_id =~ "9876543210") || (channel_title =~ "ABCDEFGHIJ" && title =~ "9876543210" && video_id =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_id =~ "ABCDEFGHIJ" && channel_title =~ "0123456789" && title = "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @security-camera.set_power(power=enum(off)) ;
}
====
@washington_post.new_article, (link = "http://www.abc.def"^^tt:url && description = "ABCDEFGHIJ") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(lifestyle)), ((section = enum(opinions) && description = "23456789")) , v_title := title, v_link := link, v_description := description => @phone.send_sms(to=v_title, body=v_title) ;
}
====
now => * => @builtin.discover, true 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=7, in_reply_to="bob"^^tt:username), (((query_hashtag = "cat"^^tt:hashtag && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username) || (count > 42 && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @builtin.discover() ;
}
====
* => @uber.price_estimate, (end = $context.location.current_location && uber_type = "12345" && low_estimate > 11 && duration > 30min && distance >= 42cm) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "0123456789" || file_name =~ "23456789" || file_name = "12345")) , v_file_name := file_name => @uber.price_estimate(start=$context.location.work, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((end = $context.location.work && uber_type =~ "0123456788" && low_estimate >= 7 && surge <= 7 && duration >= 30min && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => @heatpad.set_power, power = enum(on) 
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @giphy.get_tag(tag=v_title), ((tag = "lol"^^tt:hashtag || (tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_picture_url := picture_url => @heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @fitness-tracker.getbody(), ((weight >= 82kg && leanMass <= 42 && bodyFat < 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_dates.search(), (((interest = "12345" && message = "23456789") || poster = "12345" || message =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @almond_dates.search, (interest =~ "ABCDEFGHIJ" || (message =~ "ABCDEFGHIJ" && poster = "0123456788" && phone = "+16501234567"^^tt:phone_number) || (message = "23456789" && poster =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @fitbit.recordweight, weight <= 82kg 
;;
Main() {
    now => @almond_dates.search(), ((interest = "0123456788" && message = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @fitbit.recordweight(weight=82kg) ;
}
====
* => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count <= 55 && text =~ "9876543210" && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @slack.receive(), (message =~ "0123456788") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @twitter.search_by_hashtag(query_hashtag=v_channel, from=v_timestamp, in_reply_to=v_message), ((text = "23456789" && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @twitter.search, ((count >= 11 && text =~ "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username) || (query =~ "23456789" && count >= 7 && text = "0123456789" && from = "alice"^^tt:username) || (query =~ "23456789" && text = "0123456789" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @wall_street_journal.lifestyle(), ((title =~ "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @twitter.search(query=v_title, from=v_title), ((query =~ "ABCDEFGHIJ" && text = "9876543210" && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude < 42cm && bearing >= 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.current_location && altitude > 42cm && bearing <= 7 && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.post(title="0123456788", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @linkedin.get_profile, ((industry =~ "9876543210" && contains(positions, "9876543210") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "9876543210" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "ABCDEFGHIJ" && industry =~ "12345" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "9876543210" && industry =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @speaker.raise_volume() ;
}
====
* => @github.add_email, true 
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1487030400000) && value < 22C)) , v_time := time, v_value := value => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @security-camera.set_power(power=enum(off)) ;
}
====
* => @onedrive.delete_file, file_name = "12345" 
;;
Main() {
    @omlet.newmessage(), (message =~ "0123456789") , v_type := type, v_message := message => @onedrive.delete_file(file_name=v_message) ;
}
====
now => @weatherapi.sunrise, date = makeDate(1487030400000) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@fitness-tracker.getbody, true , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight >= 82kg && leanMass <= 55) || (time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass <= 14 && bodyFat > 55 && bmi >= 14))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @gmail.get_latest(), ((label = "0123456789" || (from = "bob@stanford.edu"^^tt:email_address && subject =~ "12345") || subject =~ "12345")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name = "ABCDEFGHIJ" || file_name = "0123456789" || file_name = "0123456788") 
;;
Main() {
    now => @dropbox.get_space_usage(), ((total > 5KB || (used < 5KB && total >= 20MB))) , v_used := used, v_total := total => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && has_sound = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (has_person = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @dropbox.open(file_name="0123456789"), true , v_url := url => notify;
}
====
* => @thecatapi.get, count >= 7 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @activity-tracker.getmove(), ((steps <= 14 && activeTime > 2h && inactiveTime <= 30min && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thecatapi.get(), ((count < 55 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @github.add_email, (email = "bob@stanford.edu"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    @instagram.new_picture(), (caption =~ "ABCDEFGHIJ") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @github.add_email(email=v_filter) ;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((message = "0123456788" && poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => * => @onedrive.rename_file, old_name = "0123456789" 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @onedrive.rename_file(old_name="ABCDEFGHIJ", new_name="23456789") ;
}
====
now => @speaker.lower_volume, true 
;;
Main() {
    now => @speaker.lower_volume() ;
}
====
now => @ytranslate.translate, ((source_language = "zh"^^tt:iso_lang_code("Chinese") && text = "9876543210") || translated_text = "0123456788") , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="9876543210"), true , v_source_language := source_language, v_translated_text := translated_text => @car.honk_horn() ;
}
====
now => @holidays.next_uk_holiday_event, (summary = "ABCDEFGHIJ" && description =~ "9876543210") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((date = makeDate(1462320000000) || description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@slack.receive, (channel = "lol"^^tt:hashtag && timestamp = "12345" && message =~ "0123456788") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((channel = "lol"^^tt:hashtag && from = "alice"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @giphy.get_tag(tag=v_timestamp), ((tag = "funny"^^tt:hashtag || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
@nasa.asteroid, (name =~ "0123456789" && estimated_diameter_min >= 1000m && estimated_diameter_max >= 1000m && relative_velocity > 25mph && closest_distance_to_earth > 42cm && orbiting_body = "ABCDEFGHIJ") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 55 && name =~ "0123456788" && estimated_diameter_min > 42cm && estimated_diameter_max < 1000m && dangerous = true && relative_velocity >= 25mph) || (estimated_diameter_max < 1000m && relative_velocity < 5kmph && closest_distance_to_earth <= 1000m && orbiting_body =~ "0123456789") || (asteroid_id > 7 && name =~ "9876543210" && dangerous = true && closest_distance_to_earth >= 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
now => * => @bluetooth_speaker.raise_volume, true 
;;
Main() {
    now => @bluetooth_speaker.raise_volume() ;
}
====
now => @gmail.get_latest, from = "bob@stanford.edu"^^tt:email_address , v_from := from, v_subject := subject, v_label := label => @lg_webos_tv.set_volume, percent <= 14 
;;
Main() {
    now => @gmail.get_latest(), (subject =~ "9876543210") , v_from := from, v_subject := subject, v_label := label => @lg_webos_tv.set_volume(percent=11) ;
}
====
* => * => @almond_couch_market.post, (title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @activity-tracker.getmove(), ((day = "0123456789" && distance = 1000m && steps <= 42 && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.get_time(), (time = makeDate(1487030400000)) , v_time := time => @almond_couch_market.post(title="12345", price=11, contact="+15551234567"^^tt:phone_number) ;
}
====
@github.new_commit, (repo_name =~ "23456789" && time = makeDate(1487030400000)) , v_user := user, v_message := message, v_time := time => * => *
;;
Main() {
    @github.new_commit(repo_name="0123456789"), (user = "alice"^^tt:username) , v_user := user, v_message := message, v_time := time => @icalendar.list_events(), ((summary =~ "0123456789" && sequence >= 42 && organizer =~ "ABCDEFGHIJ" && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @car.honk_horn() ;
}
====
* => @builtin.notify, (message =~ "12345" || message = "ABCDEFGHIJ" || message = "0123456788" || message = "0123456789" || message = "12345" || message =~ "23456789") 
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration <= 2h && light < 30min && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @builtin.notify(message="0123456788") ;
}
====
@twitter.my_tweet, text =~ "12345" , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => * => notify
;;
Main() {
    @twitter.my_tweet(), true , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
* => @linkedin.get_profile, (formatted_name = "23456789" && industry = "0123456788" && specialties =~ "ABCDEFGHIJ") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @github.new_issue_comment(repo_name="ABCDEFGHIJ"), (((user = "alice"^^tt:username && issue_number >= 42 && body = "12345" && time = makeDate(1462320000000)) || (user = "bob"^^tt:username && issue_number > 7 && time = makeDate(1487030400000)) || (repo_name = "0123456788" && user = "alice"^^tt:username && issue_number >= 55 && body =~ "9876543210" && time = makeDate(1462320000000)))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @linkedin.get_profile(), ((headline = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @thecatapi.get, ((count > 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id = "0123456789" && count < 55) || count > 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name = "ABCDEFGHIJ" && ask_price < 42 && bid_price > 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @thecatapi.get(), ((image_id =~ "0123456788" && count > 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @builtin.get_time, (time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1462320000000)) , v_time := time => notify
;;
Main() {
    @gmail.receive_important_email(), ((snippet = "ABCDEFGHIJ" || subject =~ "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.get_time(), true , v_time := time => notify;
}
====
* => * => @phone.notify, ((title = "23456789" && message =~ "12345") || (title =~ "0123456788" && message =~ "12345")) 
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "ABCDEFGHIJ" && user =~ "0123456789") || (title = "0123456789" && category =~ "12345") || (title =~ "12345" && user = "ABCDEFGHIJ"))) , v_title := title, v_link := link, v_user := user, v_category := category => @phone.notify(title=v_category, message=v_category) ;
}
====
* => @bluetooth_speaker.raise_volume, true 
;;
Main() {
    @gmail.receive_primary_email(), (snippet =~ "0123456788") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @bluetooth_speaker.raise_volume() ;
}
====
* => @uber.time_estimate, ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && estimate < 2h) || (uber_type = "0123456788" && estimate <= 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), (estimate > 30min) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => @xkcd.get_comic, ((title = "23456789" && link = "http://www.google.com"^^tt:url && alt_text =~ "23456789") || (number <= 7 && title = "0123456789" && alt_text =~ "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(number=55), ((number >= 55 && alt_text =~ "9876543210")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @gmail.send_picture(to=v_alt_text, subject=v_title, picture_url=v_title, message="23456789") ;
}
====
@tumblr.new_photo, (link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => *
;;
Main() {
    @tumblr.new_photo(blog_name="9876543210"), ((contains(tags, "lol"^^tt:hashtag) || link = "http://www.google.com"^^tt:url || (blog_name = "12345" && date = makeDate(1462320000000)) || (blog_name = "12345" && date = makeDate(1462320000000) && contains(tags, "funny"^^tt:hashtag) && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (blog_name = "ABCDEFGHIJ" && date = makeDate(1487030400000) && caption =~ "0123456789" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || link = "http://www.abc.def"^^tt:url || caption = "12345" || contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="ABCDEFGHIJ", message="ABCDEFGHIJ") ;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), (((ask_price <= 14 && bid_price <= 42) || (company_name = "23456789" && ask_price <= 7 && bid_price < 55))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @security-camera.set_power(power=enum(off)) ;
}
====
now => @wall_street_journal.get_opinions, (link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title =~ "0123456789") , v_title := title, v_link := link, v_updated := updated => @lg_webos_tv.unmute() ;
}
====
now => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    now => @builtin.get_date(), (date = makeDate(1462320000000)) , v_date := date => @security-camera.set_power(power=enum(on)) ;
}
====
@holidays.us_holiday_event, (summary =~ "0123456788" && description =~ "23456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.us_holiday_event(), (((date = makeDate(1487030400000) && description = "23456789") || (summary =~ "23456789" && description = "ABCDEFGHIJ") || (summary = "ABCDEFGHIJ" && description = "0123456788") || description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@bodytrace.source, (time = makeDate(1462320000000) && weight >= 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => notify;
}
====
* => @dropbox.create_new_folder, true 
;;
Main() {
    @xkcd.new_comic(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @dropbox.create_new_folder(folder_name=v_title) ;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((summary =~ "9876543210" && sequence <= 11 && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @omlet.send_to(to=v_organizer, message=v_description) ;
}
====
@xkcd.new_comic, (number >= 11 && title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @xkcd.new_comic(), ((number >= 42 && title =~ "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @phone.set_ringer(mode=enum(vibrate)) ;
}
====
@activity-tracker.getmove, (steps > 11 && activeTime >= 30min && inactiveTime > 30min && calories > 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((steps <= 14 && activeTime > 2h && inactiveTime > 2h && calories > 500kcal) || (distance >= 1000m && steps = 7 && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.get_random(), true , v_number := number => notify;
}
====
now => * => @twitter.unfollow, true 
;;
Main() {
    now => @almond_bike_market.ask(id="12345", property="9876543210"), (id =~ "0123456789")  => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
* => @instagram.get_pictures, (count < 55 && caption = "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate > 7) , v_time := time, v_place := place, v_heartrate := heartrate => @instagram.get_pictures(count=v_heartrate), ((count = 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "23456789" && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@scale.source, (value <= 82kg || value >= 155lb) , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => notify;
}
====
now => @wall_street_journal.get_opinions, title =~ "23456789" , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456788")) , v_title := title, v_link := link, v_updated := updated => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
@google_drive.new_drive_file, file_name = "12345" , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name = "ABCDEFGHIJ" || file_name =~ "12345" || file_name =~ "9876543210" || file_name =~ "9876543210")) , v_file_name := file_name => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && estimate <= 30min) || (uber_type = "ABCDEFGHIJ" && estimate > 2h) || (start = $context.location.work && estimate = 30min))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@wall_street_journal.technology, title = "0123456789" , v_title := title, v_link := link => * => notify
;;
Main() {
    @wall_street_journal.technology(), (title = "12345") , v_title := title, v_link := link => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
now => * => @omlet.send_picture, (to = "alice"^^tt:username && caption = "9876543210") 
;;
Main() {
    now => @youtube.search_videos(query="0123456789"), ((query = "0123456788" && channel_id = "0123456789" && title = "0123456788" && count <= 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @omlet.send_picture(to=v_channel_title, picture_url=v_description, caption=v_channel_title) ;
}
====
@sportradar.ncaafb_team, (watched_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && watched_is_home = false && away_name =~ "0123456789" && home_name =~ "ABCDEFGHIJ" && away_points >= 42) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), ((home_name =~ "0123456788" && scheduled_time = makeDate(1487030400000) && away_points <= 7 && home_points = 14)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
* => @thermostat.get_hvac_state, ((mode = enum(off) && state = enum(heating)) || (mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => *
;;
Main() {
    now => @thermostat.get_hvac_state(), ((mode = enum(heat_cool) || state = enum(off) || mode = enum(heat))) , v_mode := mode, v_state := state => @security-camera.set_power(power=enum(on)) ;
}
====
@sportradar.ncaafb_team, (home_name =~ "ABCDEFGHIJ" && away_points > 55) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), ((watched_is_home = true && away_name = "0123456789")) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
@fitness-tracker.getbody, (place = $context.location.current_location && weight > 82kg && leanMass > 7) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((leanMass = 55 && bodyFat < 11 && bmi < 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @uber.price_estimate(end=v_place, start=$context.location.home), ((low_estimate < 11 && currency_code = "23456789" && surge >= 55 && duration < 2h && distance >= 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@instagram.new_picture, (filter =~ "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((caption =~ "0123456788" || (caption = "0123456789" && filter =~ "0123456789" && location = $context.location.home))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
* => @thecatapi.get, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id =~ "23456789" && count = 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "ABCDEFGHIJ" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @twitter.my_tweet(), ((text = "ABCDEFGHIJ" || text = "0123456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @thecatapi.get(), ((image_id = "9876543210" && count <= 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @almond_bike_market.post, ((price >= 42 && poster = "0123456789") || title =~ "0123456788") 
;;
Main() {
    @xkcd.new_whatif(), (title =~ "9876543210") , v_title := title, v_link := link => @nasa.rover(), (((count > 55 && camera_used =~ "0123456788") || (date_taken = makeDate(1487030400000) && count <= 11 && camera_used =~ "ABCDEFGHIJ") || count <= 42 || (count > 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count = 55)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @almond_bike_market.post(title=v_camera_used, info=v_camera_used, poster=v_camera_used, price=11) ;
}
====
* => @ytranslate.detect_language, text =~ "0123456788" , v_detected_language := detected_language => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval >= 30min)  => @ytranslate.detect_language(text="12345"), (((text =~ "12345" && detected_language = "en"^^tt:iso_lang_code("English")) || (text = "9876543210" && detected_language = "en"^^tt:iso_lang_code("English")))) , v_detected_language := detected_language => notify;
}
====
now => @youtube.search_videos, (query =~ "12345" && video_id = "12345" && channel_title =~ "0123456788" && description = "ABCDEFGHIJ" && count <= 7 && video_url = "http://www.google.com"^^tt:url) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="12345"), ((video_id =~ "9876543210" && channel_id = "0123456788" && description = "9876543210" && count < 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @dropbox.open(file_name="9876543210"), true , v_url := url => @gmail.send_picture(to=v_url, subject=v_url, message=v_url, picture_url=v_url) ;
}
====
now => * => @builtin.configure, true 
;;
Main() {
    now => @xkcd.get_comic(), (((number > 7 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "0123456789") || (number >= 55 && title =~ "9876543210" && alt_text = "0123456789") || (number >= 42 && title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "9876543210") || (number > 14 && title = "9876543210" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @builtin.configure(device="facebook"^^tt:device("Facebook")) ;
}
====
now => * => @builtin.notify, message = "23456789" 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag), ((query_hashtag = "funny"^^tt:hashtag && count >= 7 && contains(hashtags, "funny"^^tt:hashtag) && from = "bob"^^tt:username && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @builtin.notify(message=v_text) ;
}
====
@weatherapi.monitor, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature >= 73F && wind_speed >= 25mph && humidity < 55 && cloudiness < 14 && status = enum(windy)) || (location = $context.location.work && wind_speed <= 25mph && humidity < 42 && cloudiness > 7 && fog < 55 && weather =~ "0123456789") || (wind_speed >= 5kmph && humidity > 11 && cloudiness < 11)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.search, ((title =~ v_weather && price < v_cloudiness && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && price = v_cloudiness && contact = "+16501234567"^^tt:phone_number) || (title = "12345" && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature < 73F && wind_speed < 25mph && humidity >= 7 && cloudiness > 42 && weather = "0123456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.search(), ((title = "12345" && price <= 42 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude > 1000m && bearing = 42 && speed = 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((altitude = 42cm && bearing >= 14 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @onedrive.create_file(file_name="0123456789", contents="0123456789") ;
}
====
@weatherapi.monitor, (wind_speed >= 5kmph && humidity = 14 && cloudiness <= 11 && fog > 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature < 73F && humidity >= 7 && cloudiness <= 42 && fog < 11) || (location = $context.location.home && temperature < 22C && wind_speed > 25mph && humidity > 11 && cloudiness < 14))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.send_picture(to=v_weather, subject=v_weather, message=v_weather, picture_url=v_weather) ;
}
====
* => @builtin.thank_you, true 
;;
Main() {
    @twitter.direct_message(), ((message = "12345" && __reserved = false)) , v_from := from, v_message := message, v___reserved := __reserved => @builtin.thank_you() ;
}
====
@almond_couch_market.monitor, (title = "ABCDEFGHIJ" && price < 42) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price > 42 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @holidays.next_us_holiday_event, (date = makeDate(1462320000000) && summary =~ "0123456788") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @instagram.get_pictures, ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789" && filter = "0123456789") || (count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @security-camera.new_event(), (((has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || has_motion = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @instagram.get_pictures(count=11), (caption = "12345") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@almond_dates.monitor, ((interest = "23456789" && poster =~ "0123456789") || (message = "0123456789" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), (((poster = "12345" && phone = "+16501234567"^^tt:phone_number) || (message =~ "9876543210" && poster =~ "ABCDEFGHIJ"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && deep < 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration < 30min && light > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
now => * => @builtin.configure, true 
;;
Main() {
    now => @youtube.search_videos(query="9876543210"), (((query = "23456789" && video_id =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count < 55) || (query =~ "9876543210" && video_id =~ "0123456788" && channel_title = "ABCDEFGHIJ" && title = "ABCDEFGHIJ" && description =~ "0123456788" && count <= 55))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @builtin.configure(device="scale"^^tt:device("my scale")) ;
}
====
@builtin.at, (time = makeTime(15,0) || time = makeTime(15,0) || time = makeTime(7,30) || time = makeTime(20,30) || time = makeTime(20,30))  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @uber.time_estimate(start=$context.location.current_location), ((uber_type = "0123456788" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @dropbox.move, ((old_name =~ "12345" && new_name =~ "9876543210") || old_name = "0123456789") 
;;
Main() {
    now => @uber.time_estimate(start=$context.location.current_location), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "0123456789" && estimate > 30min)) , v_uber_type := uber_type, v_estimate := estimate => @dropbox.move(old_name="ABCDEFGHIJ", new_name="9876543210") ;
}
====
* => @almond_bike_market.ask_seller, (value =~ "0123456788" && answer = "9876543210") , v_answer := answer => notify
;;
Main() {
    now => @almond_bike_market.ask_seller(id="12345", property="ABCDEFGHIJ", value="0123456789", public=true), ((id = "23456789" && answer = "ABCDEFGHIJ")) , v_answer := answer => notify;
}
====
now => * => @bluetooth_speaker.play_music, true 
;;
Main() {
    now => @weatherapi.current(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature >= 73F && wind_speed <= 25mph && humidity <= 55 && fog <= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @bluetooth_speaker.play_music() ;
}
====
now => @ytranslate.translate, text = "0123456789" , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="ABCDEFGHIJ"), ((text = "9876543210" && translated_text = "ABCDEFGHIJ")) , v_source_language := source_language, v_translated_text := translated_text => @lg_webos_tv.lower_volume() ;
}
====
@twitter.direct_message, (message =~ "0123456789" || __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), (message = "0123456788") , v_from := from, v_message := message, v___reserved := __reserved => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1487030400000) && organizer = "12345" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), ((summary = "23456789" && sequence < 42 && location = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @wall_street_journal.get_opinions(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_updated := updated => @onedrive.upload_picture(file_name=v_title, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@weatherapi.monitor, ((temperature <= 22C && wind_speed = 25mph && humidity >= 7 && cloudiness >= 11 && weather = "0123456788") || (temperature < 22C && wind_speed <= 5kmph && humidity > 55 && fog > 11 && weather =~ "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((temperature >= 73F && wind_speed = 5kmph && cloudiness <= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @holidays.next_uk_holiday_event(), (((date = makeDate(1487030400000) && summary = "23456789" && description =~ "0123456788") || description =~ "23456789" || (date = makeDate(1487030400000) && description = "ABCDEFGHIJ") || (summary = "ABCDEFGHIJ" && description = "0123456789") || (summary =~ "0123456789" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @nasa.apod, (description = "ABCDEFGHIJ" || (title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "23456789" && description =~ "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (((title = "0123456789" && description =~ "12345") || (title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@scale.source, (time = makeDate(1487030400000) && value < 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1487030400000) && value > 155lb)) , v_time := time, v_value := value => @nasa.apod(), ((title = "ABCDEFGHIJ" && description = "ABCDEFGHIJ")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @omlet.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && caption = "ABCDEFGHIJ") || (to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788")) 
;;
Main() {
    @slack.receive(), true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @omlet.send_picture(to=v_from, picture_url=v_timestamp, caption=v_message) ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && duration <= 2h && rem = 2h && light < 30min && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @dropbox.list_folder(folder_name="23456789"), (((folder_name = "23456789" && file_name =~ "0123456788" && is_folder = false && full_path =~ "23456789") || (file_name =~ "12345" && file_size = 20MB && full_path = "ABCDEFGHIJ") || (folder_name =~ "0123456788" && file_size <= 20MB && full_path =~ "0123456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @ytranslate.translate, ((source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "zh"^^tt:iso_lang_code("Chinese")) || text = "9876543210" || (text =~ "12345" && translated_text = "9876543210") || text =~ "ABCDEFGHIJ" || target_language = "zh"^^tt:iso_lang_code("Chinese")) , v_source_language := source_language, v_translated_text := translated_text => @gmail.send_picture, ((subject = "ABCDEFGHIJ" && message =~ "0123456789") || (to = "bob@stanford.edu"^^tt:email_address && subject =~ v_translated_text && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="0123456788"), (text = "0123456788") , v_source_language := source_language, v_translated_text := translated_text => @gmail.send_picture(to=v_translated_text, subject=v_translated_text, message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@washington_post.new_blog_post, true , v_title := title, v_link := link => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(fact_checker)), ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify;
}
====
* => @instagram.get_pictures, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345") || (count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag)) || (count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @wall_street_journal.markets(), ((title = "0123456788" || title = "12345" || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title = "0123456789")) , v_title := title, v_link := link => @instagram.get_pictures(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => * => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    now => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
@google_drive.new_drive_file, file_name =~ "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @builtin.discover() ;
}
====
now => * => @linkedin.share, (status =~ "0123456788" || status =~ "ABCDEFGHIJ" || status =~ "9876543210") 
;;
Main() {
    now => @twitter.search(query="0123456789", count=55, from="bob"^^tt:username), (((query = "0123456789" && text =~ "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "bob"^^tt:username) || (query =~ "ABCDEFGHIJ" && count >= 7 && text =~ "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @linkedin.share(status=v_text) ;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name = "12345" || file_name = "9876543210") 
;;
Main() {
    @reddit_front_page.newpost(), ((title =~ "0123456789" && user = "0123456788" && category =~ "0123456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @google_drive.create_new_drive_file(file_name=v_category) ;
}
====
@almond_dates.monitor, ((interest =~ "0123456789" && poster = "0123456788" && phone = "+16501234567"^^tt:phone_number) || interest =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "0123456789" && poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || interest =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_bike_market.search(info=v_message, price=42), true  => notify;
}
====
now => * => @almond_dates.post, (interest =~ "ABCDEFGHIJ" && message = "12345") 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="ABCDEFGHIJ", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    @xkcd.new_comic(), ((number >= 11 && title =~ "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @light-bulb.color_loop() ;
}
====
@fitness-tracker.getbody, (weight > 155lb && bodyFat <= 55 && bmi = 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && weight <= 155lb && leanMass < 42 && bodyFat > 11 && bmi < 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @nasa.apod(), ((description = "ABCDEFGHIJ" || (title =~ "12345" && description =~ "12345") || (title =~ "0123456788" && description =~ "ABCDEFGHIJ"))) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@weatherapi.monitor, (wind_speed <= 25mph && cloudiness >= 11 && fog <= 11 && status = enum(raining)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @linkedin.get_profile, (formatted_name =~ "ABCDEFGHIJ" && headline = "0123456788") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((temperature < 22C && humidity = 42 && cloudiness = 7 && fog < 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @linkedin.get_profile(), ((formatted_name =~ "12345" || (formatted_name = "12345" && headline =~ "12345" && contains(positions, "ABCDEFGHIJ")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @dropbox.open, (file_name = "0123456788" && url = "0123456789") , v_url := url => *
;;
Main() {
    now => @dropbox.open(file_name="12345"), ((file_name = "9876543210" && url =~ "9876543210")) , v_url := url => @builtin.notify(message=v_url) ;
}
====
now => @gmail.get_latest, ((from = "bob@stanford.edu"^^tt:email_address && subject =~ "23456789") || from = "alice@gmail.com"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), (((from = "alice@gmail.com"^^tt:email_address && label = "23456789") || label = "9876543210")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_person = true) || has_person = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1487030400000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @phone.get_gps(), (speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@builtin.at, true  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @youtube.list_subscriptions(), (thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@icalendar.event_begin, (start_date = makeDate(1462320000000) && description =~ "0123456789" && sequence <= 55) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "12345" && description =~ "9876543210" && sequence = 42 && organizer =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @icalendar.event_begin(), (((summary = "12345" && sequence < 42) || (summary = "12345" && sequence < 55) || (end_date = makeDate(1462320000000) && description = "23456789" && sequence < 55) || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && description = "0123456789" && location =~ "23456789") || (start_date = makeDate(1462320000000) && summary = "12345" && description = "12345" && sequence >= 11))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @imgflip.list(), (name = "23456789") , v_name := name, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @facebook.post_picture, (caption = "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption =~ "12345") 
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((wind_speed >= 5kmph && humidity <= 7 && cloudiness = 11 && fog < 14 && status = enum(snowy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @facebook.post_picture(picture_url=v_weather, caption=v_weather) ;
}
====
* => @twitter.follow, true 
;;
Main() {
    @gmail.receive_important_email(), ((subject =~ "23456789" && snippet = "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @twitter.follow(user_name=v_subject) ;
}
====
@icalendar.event_begin, ((end_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && location = "9876543210") || (end_date = makeDate(1487030400000) && description = "23456789" && sequence >= 55 && location =~ "0123456789") || (start_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && description = "9876543210" && organizer = "ABCDEFGHIJ") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "0123456788" && sequence < 11 && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((summary =~ "0123456789" && description = "12345" && location =~ "9876543210") || (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && summary = "0123456789" && sequence >= 14) || (start_date = makeDate(1462320000000) && description =~ "9876543210" && location = "12345") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && description =~ "9876543210" && sequence <= 7 && organizer = "12345") || (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && sequence >= 14 && organizer = "12345" && location =~ "9876543210") || (end_date = makeDate(1487030400000) && description = "0123456789" && sequence <= 7 && organizer = "ABCDEFGHIJ" && location =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @youtube.search_videos(query="9876543210"), ((channel_id =~ "12345" && channel_title = "ABCDEFGHIJ" && title = "0123456789" && count > 55)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && bodyFat > 42 && bmi < 7) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass = 14 && bodyFat >= 42 && bmi >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((place = $context.location.home && weight <= 82kg && bmi <= 55) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight >= 82kg && leanMass > 42 && bodyFat <= 55 && bmi = 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @builtin.get_date(), true , v_date := date => notify;
}
====
now => @thecatapi.get, (image_id = "0123456788" && count < 14 && link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "9876543210" && count >= 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @fitbit.getbody, ((weight >= 82kg && bmi <= 55) || (weight <= 155lb && bmi >= 55 && fat >= 14) || (weight >= 82kg && bmi >= 14 && fat <= 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), (bmi >= 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @twitter.search_by_hashtag, ((count > 11 && text =~ "9876543210" && contains(urls, "http://www.abc.def"^^tt:url)) || (count = 42 && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @twitter.search_by_hashtag(query_hashtag=v_message, from=v_message, in_reply_to=v_message), (((count = 7 && text = "ABCDEFGHIJ" && contains(urls, "http://www.abc.def"^^tt:url)) || (count > 55 && text =~ "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username) || (count = 11 && from = "bob"^^tt:username) || (text =~ "ABCDEFGHIJ" && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @security-camera.set_power(power=enum(on)) ;
}
====
* => * => @linkedin.share, true 
;;
Main() {
    @security-camera.new_event(), ((has_sound = false || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || has_motion = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @weatherapi.current(location=$context.location.work), (((temperature = 22C && cloudiness <= 7 && weather = "ABCDEFGHIJ") || (temperature > 73F && humidity > 11 && fog < 14 && weather = "ABCDEFGHIJ"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @linkedin.share(status="12345") ;
}
====
* => @almond_couch_market.search, (title =~ "9876543210" && price > 55 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @icalendar.event_begin(), (((summary = "12345" && description = "0123456788") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && description =~ "9876543210" && sequence = 42 && organizer = "ABCDEFGHIJ"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_couch_market.search(), ((price > 14 || (title =~ "0123456789" && price <= 7))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @slack.updateChannelPurpose, purpose = "ABCDEFGHIJ" 
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="chc"^^sportradar:mlb_team("Chicago Cubs")), ((watched_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && away_name = "23456789" && game_status = enum(halftime) && game_inning =~ "23456789" && away_runs = 7 && home_runs > 7)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @slack.updateChannelPurpose(channel=v_scheduled_time, purpose=v_home_name) ;
}
====
now => @giphy.get_tag, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || tag = "lol"^^tt:hashtag) , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), ((tag = "cat"^^tt:hashtag || tag = "cat"^^tt:hashtag || tag = "funny"^^tt:hashtag || tag = "lol"^^tt:hashtag || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
* => @phone.get_gps, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 42cm && bearing = 55) || (location = $context.location.current_location && altitude >= 42cm && bearing <= 14 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @jawbone_up.getmove(), (((day = "23456789" && activeTime > 30min && inactiveTime < 2h && calories <= 500kcal) || (day =~ "0123456789" && distance >= 42cm && steps <= 14 && activeTime > 30min && calories >= 500kcal) || (day =~ "0123456788" && activeTime = 30min && inactiveTime < 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.get_gps(), ((altitude <= 1000m && bearing = 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@xkcd.new_comic, ((number = 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number > 42 && title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 42 && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @xkcd.new_comic(), ((number < 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
@almond_bike_market.monitor, (info =~ "12345" && price < 14)  => * => notify
;;
Main() {
    @almond_bike_market.monitor(info="23456789", price=55), ((price > 11 || (info = "ABCDEFGHIJ" && price <= 14) || (info = "ABCDEFGHIJ" && price >= 42)))  => @imgflip.list(), (name =~ "12345") , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @weatherapi.moon, (phase = "23456789" || phase = "0123456789") , v_date := date, v_phase := phase => *
;;
Main() {
    now => @weatherapi.moon(location=$context.location.current_location), true , v_date := date, v_phase := phase => @tumblr-blog.post_picture(caption=v_phase, picture_url=v_phase) ;
}
====
@scale.source, value >= 82kg , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), ((value > 155lb || value >= 82kg)) , v_time := time, v_value := value => notify;
}
====
@nasa.asteroid, (asteroid_id > 42 && estimated_diameter_min = 1000m && estimated_diameter_max <= 42cm && relative_velocity > 25mph && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((name =~ "9876543210" && estimated_diameter_min <= 42cm && estimated_diameter_max = 1000m && relative_velocity >= 25mph && closest_distance_to_earth >= 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @dropbox.open(file_name="23456789"), ((file_name = "0123456788" && url =~ "ABCDEFGHIJ")) , v_url := url => notify;
}
====
* => @thermostat.set_target_temperature, true 
;;
Main() {
    @builtin.timer(interval=2h), ((interval >= 30min || interval > 2h))  => @thermostat.set_target_temperature(value=22C) ;
}
====
@rss.new_post, (title = "9876543210" || (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || (title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => notify
;;
Main() {
    @rss.new_post(), ((link = "http://www.abc.def"^^tt:url || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || title = "0123456788")) , v_title := title, v_link := link => notify;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature >= 73F && humidity = 7 && cloudiness = 14 && fog <= 55 && weather =~ "ABCDEFGHIJ") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature <= 22C && humidity <= 14 && cloudiness <= 14 && fog >= 7 && status = enum(snowy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_dates.search(), ((interest = "ABCDEFGHIJ" || (interest =~ "23456789" && message =~ "23456789" && poster =~ "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @bluetooth_speaker.set_volume(percent=11) ;
}
====
now => @phone.get_gps, (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 1000m && bearing < 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 42cm && bearing >= 42 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @wall_street_journal.get_opinions, ((title = "12345" && updated = makeDate(1462320000000)) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000)) || title =~ "0123456789" || (title =~ "ABCDEFGHIJ" && updated = makeDate(1462320000000)) || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || (title = "0123456788" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || (title =~ "12345" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @builtin.timer(interval=30min), ((interval > 2h || interval <= 30min || interval <= 2h))  => @wall_street_journal.get_opinions(), ((title = "23456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @gmail.receive_primary_email(), (((from_address = "alice@gmail.com"^^tt:email_address && subject =~ "0123456788") || subject =~ "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @heatpad.set_power(power=enum(on)) ;
}
====
@xkcd.new_comic, title =~ "12345" , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), ((number >= 42 && title = "0123456789")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @xkcd.random_comic(), ((number = 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "23456789")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @gmail.send_email, (subject = "23456789" && message = "ABCDEFGHIJ") 
;;
Main() {
    @wall_street_journal.lifestyle(), true , v_title := title, v_link := link => @gmail.send_email(to=v_title, subject=v_title, message=v_title) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "ABCDEFGHIJ" || file_name =~ "23456789" || file_name =~ "12345") 
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "23456789" && count = 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @google_drive.create_new_drive_file(file_name=v_image_id) ;
}
====
@almond_dates.monitor, (interest = "0123456789" && message =~ "23456789" && poster = "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "9876543210" || (message =~ "23456789" && poster =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "23456789" && message = "23456789" && poster = "23456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @security-camera.get_snapshot, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => notify
;;
Main() {
    @github.new_commit(repo_name="12345"), ((repo_name =~ "12345" || (repo_name = "23456789" && time = makeDate(1462320000000)))) , v_user := user, v_message := message, v_time := time => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
@almond_bike_market.monitor, (price < 11 || info =~ "9876543210" || price > 42)  => notify
;;
Main() {
    @almond_bike_market.monitor(), true  => notify;
}
====
* => @phone.get_gps, ((altitude <= 1000m && bearing >= 55) || (location = $context.location.home && altitude >= 42cm) || (altitude <= 42cm && bearing <= 55 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @scale.source(), (value = 155lb) , v_time := time, v_value := value => @phone.get_gps(), (((bearing >= 7 && speed < 25mph) || speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @twitter.search_by_hashtag, (count > 55 && text =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @scale.source(), (value > 155lb) , v_time := time, v_value := value => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=7), ((query_hashtag = "lol"^^tt:hashtag || (count <= 7 && text =~ "0123456789" && from = "bob"^^tt:username && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@thermostat.temperature, ((time = makeDate(1462320000000) && value >= 73F) || (time = makeDate(1462320000000) && value > 73F)) , v_time := time, v_value := value => notify
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1462320000000) || value = 73F)) , v_time := time, v_value := value => notify;
}
====
@almond_bike_market.monitor, (info =~ "12345" && price <= 11)  => *
;;
Main() {
    @almond_bike_market.monitor(), (((info = "0123456789" && price <= 11) || (info =~ "9876543210" && price >= 7)))  => @bluetooth_speaker.lower_volume() ;
}
====
now => @builtin.get_random_between, ((low > 7 && high > 14 && random > 55) || (low > 11 && high = 42) || random = 55) , v_random := random => *
;;
Main() {
    now => @builtin.get_random_between(low=11, high=14), ((high < 55 && random > 7)) , v_random := random => @heatpad.set_power(power=enum(on)) ;
}
====
* => @omlet.send_picture, (caption = "23456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @sportradar.nba_team(watched_team_alias="gsw"^^sportradar:nba_team("Golden State Warriors")), ((home_name =~ "23456789" && game_status = enum(halftime) && home_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @omlet.send_picture(to=v_home_name, picture_url=v_away_name, caption=v_home_name) ;
}
====
* => @imgflip.generate, true , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @phone.gps(), (((location = $context.location.home && bearing < 7 && speed < 5kmph) || (altitude > 42cm && bearing > 42))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @imgflip.generate(template="0123456789", text_top="0123456789", text_bottom="ABCDEFGHIJ"), (text_bottom = "12345") , v_name := name, v_picture_url := picture_url => notify;
}
====
@security-camera.new_event, (has_motion = true || (start_time = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @instagram.get_pictures(count=42), ((count <= 14 && filter = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @xkcd.random_comic, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text = "12345") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 42 && title = "0123456788" && link = "http://www.abc.def"^^tt:url && alt_text =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @dropbox.move, new_name =~ "0123456789" 
;;
Main() {
    now => @dropbox.move(old_name="9876543210", new_name="23456789") ;
}
====
now => * => @omlet.send_picture, (to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789") 
;;
Main() {
    now => @xkcd.get_comic(number=55), (((link = "http://www.google.com"^^tt:url && alt_text = "ABCDEFGHIJ") || (title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "0123456789"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @omlet.send_picture(to=v_title, caption=v_title, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || title = "12345" || title =~ "23456789" || title =~ "0123456789" || link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ" || title = "23456789") , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @security-camera.set_power(power=enum(off)) ;
}
====
* => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    @xkcd.new_comic(), true , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @fitbit.recordweight(weight=82kg) ;
}
====
@rss.new_post, (title =~ "12345" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), ((title = "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @linkedin.get_profile(), ((formatted_name =~ "9876543210" && headline = "0123456789" && industry =~ "9876543210" && specialties =~ "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @omlet.send_to(to=v_specialties, message=v_specialties) ;
}
====
* => @fitbit.recordweight, (weight >= 155lb || weight = 82kg || weight >= 82kg || weight > 82kg || weight <= 82kg) 
;;
Main() {
    @github.new_issue_comment(repo_name="23456789"), (((repo_name = "0123456788" && user = "bob"^^tt:username && issue_number > 55 && body = "23456789") || (repo_name =~ "0123456788" && issue_number < 7 && body =~ "12345"))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @fitbit.recordweight(weight=82kg) ;
}
====
now => @linkedin.get_profile, ((headline =~ "ABCDEFGHIJ" && specialties = "23456789" && contains(positions, "ABCDEFGHIJ") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "12345" && industry =~ "23456789" && specialties = "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @builtin.notify, true 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "0123456788" && contains(positions, "23456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @builtin.notify(message="23456789") ;
}
====
now => * => @omlet.send_picture, (to = "alice"^^tt:username && caption =~ "23456789") 
;;
Main() {
    now => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed < 25mph && humidity > 55 && cloudiness < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @omlet.send_picture(to=v_weather, picture_url=v_weather, caption=v_weather) ;
}
====
now => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && subject =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@gmail.receive_email, ((subject =~ "0123456789" && snippet = "12345") || (from_name = "9876543210" && from_address = "bob@stanford.edu"^^tt:email_address)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), ((subject =~ "0123456789" && snippet =~ "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @imgflip.generate(template=v_snippet, text_top=v_from_name, text_bottom=v_from_name), ((template =~ "9876543210" && name = "9876543210")) , v_name := name, v_picture_url := picture_url => notify;
}
====
@jawbone_up.getsleep, ((duration > 30min && rem < 30min) || (rem > 2h && light <= 30min && deep > 2h) || (duration > 30min && rem < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @linkedin.share, status = "9876543210" 
;;
Main() {
    @jawbone_up.getsleep(), (((time = makeDate(1462320000000) && duration >= 2h && rem > 30min) || (place = $context.location.work && asleepTime = makeDate(1487030400000) && duration >= 2h && light > 30min && deep >= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @linkedin.share(status="23456789") ;
}
====
* => @slack.updateChannelPurpose, purpose = "0123456789" 
;;
Main() {
    @youtube.source(), (channel_title =~ "23456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @slack.updateChannelPurpose(channel=v_channel_id, purpose=v_channel_id) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(), ((info =~ "0123456789" && price <= 11))  => notify;
}
====
now => * => @omlet.send_to, to = "alice"^^tt:username 
;;
Main() {
    now => @almond_couch_market.search(), ((price >= 42 || (title =~ "ABCDEFGHIJ" && price <= 11))) , v_title := title, v_price := price, v_contact := contact => @omlet.send_to(to=v_title, message=v_title) ;
}
====
* => @almond_bike_market.search, (price < 11 || (info = "0123456789" && price = 14) || price <= 55 || info = "9876543210")  => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 55 && estimated_diameter_min < 1000m && estimated_diameter_max = 1000m && dangerous = false && closest_distance_to_earth > 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.search(info=v_orbiting_body, price=11), true  => notify;
}
====
* => @builtin.debug_log, (message = "9876543210" || message =~ "0123456788") 
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @builtin.debug_log(message=v_file_name) ;
}
====
* => * => @phone.notify, true 
;;
Main() {
    @jawbone_up.getmove(), ((day = "ABCDEFGHIJ" && distance <= 1000m && steps < 7 && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.notify(title="12345", message="0123456788") ;
}
====
@phone.receive_sms, true , v_from := from, v_body := body => * => *
;;
Main() {
    @phone.receive_sms(), ((from = "+15551234567"^^tt:phone_number || from = "+15551234567"^^tt:phone_number)) , v_from := from, v_body := body => @car.honk_horn() ;
}
====
* => @almond_dates.search, (message = "9876543210" || poster = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight <= 155lb && bodyFat <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_dates.search(), ((poster =~ "12345" || (interest =~ "0123456788" && message =~ "0123456789" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "9876543210" && message = "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@scale.source, value <= 82kg , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (((time = makeDate(1487030400000) && value > 82kg) || (time = makeDate(1462320000000) && value > 82kg))) , v_time := time, v_value := value => notify;
}
====
now => @imgflip.list, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => @twitter.post_picture(picture_url=v_name, caption="9876543210") ;
}
====
now => @thecatapi.get, ((image_id = "23456789" && link = "http://www.google.com"^^tt:url) || (image_id = "23456789" && count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (count <= 7 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "0123456789" && count > 42 && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @facebook.post(status=v_image_id) ;
}
====
* => @gmail.get_latest, label = "23456789" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @xkcd.new_whatif(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @gmail.get_latest(), (subject =~ "9876543210") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @dropbox.create_new_folder, folder_name =~ "0123456788" 
;;
Main() {
    now => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
@activity-tracker.getmove, (day = "ABCDEFGHIJ" && steps < 14 && activeTime < 30min && calories > 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((distance >= 1000m && steps <= 14 && activeTime < 30min && inactiveTime <= 30min && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thermostat.get_humidity(), (value < 42) , v_value := value => notify;
}
====
now => * => @gmail.send_email, (subject =~ "ABCDEFGHIJ" && message =~ "23456789") 
;;
Main() {
    now => @bing.image_search(query="23456789"), ((query = "9876543210" && width <= 55 && height >= 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @gmail.send_email(to=v_title, subject=v_title, message="0123456788") ;
}
====
* => @speaker.raise_volume, true 
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), ((watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_points = 11 && home_points >= 14 && result = enum(lose))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @speaker.raise_volume() ;
}
====
@phone.gps, (altitude >= 42cm || (altitude > 1000m && bearing >= 42 && speed < 25mph) || speed > 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude <= 1000m && bearing <= 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ" && sequence >= 55 && location =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "0123456789") , v_file_name := file_name => @speaker.set_sink() ;
}
====
now => @fitbit.getbody, (weight > 82kg && bmi < 14) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((bmi > 7 && fat > 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    now => @nasa.apod(), (((title =~ "0123456788" && description =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "23456789" && description =~ "12345"))) , v_title := title, v_description := description, v_picture_url := picture_url => @light-bulb.color_loop() ;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && asleepTime = makeDate(1462320000000) && light > 2h) || (time = makeDate(1487030400000) && place = $context.location.home && light < 2h && deep <= 30min) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => *
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && rem = 30min && light >= 30min && deep > 2h) || (duration <= 30min && rem > 30min && deep >= 2h) || (asleepTime = makeDate(1462320000000) && duration >= 30min && rem >= 30min && light = 30min) || (place = $context.location.current_location && awakeTime = makeDate(1487030400000) && rem > 30min && deep >= 30min) || (time = makeDate(1487030400000) && rem > 30min && light > 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @car.honk_horn() ;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @nasa.asteroid(), ((estimated_diameter_min <= 1000m && relative_velocity <= 25mph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @bing.image_search(query=v_orbiting_body), (width > 11) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @gmail.send_email(to=v_title, subject=v_title, message="ABCDEFGHIJ") ;
}
====
* => @xkcd.get_comic, (number >= 11 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @xkcd.get_comic(number=42), ((title =~ "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@almond_couch_market.monitor, ((title =~ "0123456789" && price = 7 && contact = "+15551234567"^^tt:phone_number) || price >= 42) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "23456789" && price < 7)) , v_title := title, v_price := price, v_contact := contact => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @youtube.search_channel(query="23456789"), ((query =~ "23456789" && description = "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => * => @builtin.hello, true 
;;
Main() {
    now => @builtin.hello() ;
}
====
@sleep-tracker.getsleep, ((duration <= 2h && rem <= 2h && light < 2h && deep <= 2h) || (place = $context.location.home && light <= 30min && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration > 30min && rem <= 2h && light < 30min && deep = 30min) || (awakeTime = makeDate(1462320000000) && duration >= 2h && rem < 2h && light >= 2h && deep < 30min) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration >= 2h && rem < 30min && light < 2h && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && rem >= 30min && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
now => * => @thermostat.set_target_temperature, (value < 73F || value = 22C) 
;;
Main() {
    now => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @google_drive.create_new_drive_file, file_name = "0123456788" 
;;
Main() {
    @builtin.timer(interval=2h), true  => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
* => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && weight >= 155lb && leanMass <= 42 && bmi > 11) || (time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass >= 42 && bodyFat >= 42) || (time = makeDate(1487030400000) && bodyFat <= 7) || (weight <= 82kg && leanMass <= 7 && bodyFat >= 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @weatherapi.current(location=$context.location.work), ((location = $context.location.work && temperature < 22C && humidity <= 14 && cloudiness > 7 && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @fitbit.recordweight(weight=82kg) ;
}
====
* => @thecatapi.get, (image_id =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @jawbone_up.getmove(), ((steps <= 14 && activeTime <= 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thecatapi.get(), ((image_id = "0123456788" || count = 14 || (image_id =~ "12345" && count < 14))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@nasa.asteroid, (asteroid_id > 42 && name = "9876543210" && estimated_diameter_min < 42cm && estimated_diameter_max > 42cm && relative_velocity <= 25mph && closest_distance_to_earth <= 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id < 7 && estimated_diameter_min = 42cm && estimated_diameter_max >= 42cm && relative_velocity > 5kmph && closest_distance_to_earth > 1000m) || (estimated_diameter_max = 1000m && relative_velocity <= 25mph && closest_distance_to_earth < 42cm && orbiting_body = "9876543210"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @thecatapi.get(), (((image_id = "12345" && count >= 11) || (image_id = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count <= 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@xkcd.new_comic, (title =~ "12345" || number > 7) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @xkcd.new_comic(), ((number >= 55 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @youtube.channels_by_category, (channel_id = "0123456788" || (channel_id =~ "ABCDEFGHIJ" && title =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(comedy)), ((channel_id = "9876543210" && title =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @security-camera.get_snapshot, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    now => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
@reddit_front_page.newpost, (title =~ "9876543210" && user =~ "12345" && category =~ "12345") , v_title := title, v_link := link, v_user := user, v_category := category => @heatpad.set_power, true 
;;
Main() {
    @reddit_front_page.newpost(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_user := user, v_category := category => @heatpad.set_power(power=enum(off)) ;
}
====
@rss.new_post, (title =~ "ABCDEFGHIJ" || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || title = "0123456788") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && weight <= 82kg) || weight < 155lb || (time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @youtube.channels_by_category(category_id=enum(gaming)), ((channel_id =~ "0123456789" && title =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @lg_webos_tv.play_url(url=v_channel_id) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.home), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@onedrive.file_created, true , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @thermostat.set_target_temperature(value=22C) ;
}
====
@holidays.us_holiday_event, true , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1462320000000) && summary = "23456789" && description = "0123456789")) , v_date := date, v_summary := summary, v_description := description => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@twitter.direct_message, (from = "bob"^^tt:username && message = "12345") , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), (((message =~ "0123456788" && __reserved = false) || (from = "bob"^^tt:username && message =~ "0123456789"))) , v_from := from, v_message := message, v___reserved := __reserved => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), true , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary =~ "0123456788") || summary = "12345" || (start_date = makeDate(1487030400000) && description =~ "12345" && sequence > 55 && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((summary = "12345" && organizer = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && sequence <= 7) || (end_date = makeDate(1462320000000) && sequence > 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    now => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && description =~ "23456789" && organizer = "9876543210") || (description = "9876543210" && sequence >= 7 && organizer =~ "9876543210") || (end_date = makeDate(1462320000000) && summary =~ "12345" && description =~ "12345" && sequence >= 7) || location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @github.add_email(email=v_summary) ;
}
====
@rss.new_post, (title =~ "12345" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @speaker.raise_volume() ;
}
====
* => @twitter.send_direct_message, (to = "bob"^^tt:username && message =~ "0123456789") 
;;
Main() {
    @sleep-tracker.getsleep(), ((rem <= 30min && light <= 30min && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @twitter.send_direct_message(to="bob"^^tt:username, message="0123456788") ;
}
====
* => @almond_couch_market.post, (price >= 11 || (title =~ "ABCDEFGHIJ" && price <= 11 && contact = "+16501234567"^^tt:phone_number) || price < 42 || (title = "9876543210" && price = 42)) 
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @almond_couch_market.post(title="12345", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@xkcd.new_comic, (number > 14 && link = "http://www.google.com"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @xkcd.new_comic(), ((number <= 11 && title =~ "9876543210" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @phone.send_sms(to=v_title, body=v_title) ;
}
====
* => @twitter.search_by_hashtag, ((count > 55 && text = "9876543210" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || (contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag), ((query_hashtag = "lol"^^tt:hashtag && count >= 14 && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @linkedin.share(status="12345") ;
}
====
@tumblr.new_photo, ((blog_name = "23456789" && caption =~ "ABCDEFGHIJ") || (blog_name = "12345" && date = makeDate(1487030400000) && caption = "0123456789") || (caption =~ "ABCDEFGHIJ" && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), ((caption = "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @giphy.get_tag(tag="lol"^^tt:hashtag), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @xkcd.get_comic(number=11), (((number <= 7 && title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456788") || (number = 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "9876543210"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@github.new_issue, ((number > 14 && title = "0123456789" && body =~ "12345") || repo_name =~ "ABCDEFGHIJ" || (repo_name = "0123456788" && user = "alice"^^tt:username && number >= 14 && body = "23456789") || (repo_name = "9876543210" && number > 11 && title = "0123456789" && body = "0123456789") || (user = "alice"^^tt:username && number > 7 && title =~ "9876543210" && time = makeDate(1462320000000))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue(repo_name="12345"), (((user = "bob"^^tt:username && number <= 7 && title = "12345") || (repo_name =~ "23456789" && user = "bob"^^tt:username && title =~ "0123456789") || (number > 7 && body =~ "12345") || (repo_name = "9876543210" && number <= 14 && title =~ "12345" && body =~ "23456789"))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @builtin.sorry() ;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && user = "0123456788" && category = "9876543210") || category = "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), ((title = "9876543210" && link = "http://www.abc.def"^^tt:url && user =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_couch_market.search(), ((price > 42 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @car.honk_horn() ;
}
====
now => @wall_street_journal.get_opinions, ((link = "http://www.google.com"^^tt:url && updated = makeDate(1462320000000)) || (title = "0123456788" && updated = makeDate(1462320000000)) || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => @omlet.send_to(to=v_title, message=v_title) ;
}
====
* => @almond_bike_market.search, ((info = "0123456788" && price >= 55) || (info = "ABCDEFGHIJ" && price > 42))  => notify
;;
Main() {
    now => @almond_bike_market.search(price=55), (price < 42)  => notify;
}
====
* => * => @builtin.sorry, true 
;;
Main() {
    @icalendar.event_begin(), ((summary = "9876543210" && sequence > 42 && organizer =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @builtin.sorry() ;
}
====
* => @almond_bike_market.ask_seller, ((id = "12345" && property = "0123456789" && public = true) || (id =~ "0123456788" && property =~ "0123456789") || (id = "ABCDEFGHIJ" && property = "12345" && value = "9876543210" && public = false && answer = "9876543210")) , v_answer := answer => notify
;;
Main() {
    now => @almond_bike_market.ask_seller(id="9876543210", property="ABCDEFGHIJ", value="12345", public=false), ((property = "9876543210" && value = "0123456788" && public = false && answer = "12345")) , v_answer := answer => notify;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(national)), (((section = enum(local) && link = "http://www.google.com"^^tt:url && description = "0123456788") || (title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && description = "23456789") || (section = enum(business) && title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || (title = "9876543210" && link = "http://www.abc.def"^^tt:url && description = "ABCDEFGHIJ"))) , v_title := title, v_link := link, v_description := description => @phone.get_gps(), (bearing <= 14) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.follow(user_name="bob"^^tt:username) ;
}
====
* => @uber.time_estimate, ((uber_type = "12345" && estimate <= 2h) || (start = $context.location.work && uber_type =~ "9876543210" && estimate = 30min) || (start = $context.location.work && uber_type =~ "0123456788" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((uber_type =~ "23456789" || uber_type = "ABCDEFGHIJ")) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @gmail.get_latest, ((from = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && label =~ "9876543210") || (from = "alice@gmail.com"^^tt:email_address && label =~ "ABCDEFGHIJ") || from = "alice@gmail.com"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1487030400000) && rem < 30min && light >= 30min && deep <= 2h) || (time = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && rem > 30min && deep = 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @gmail.get_latest(), (subject =~ "0123456789") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @lg_webos_tv.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @jawbone_up.getbody(), ((time = makeDate(1462320000000) && place = $context.location.home && weight <= 82kg && bodyFat = 11 && bmi <= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
* => * => @tumblr-blog.post_picture, true 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="bar"^^sportradar:eu_soccer_team("Barcellona")), ((watched_team_alias = "bar"^^sportradar:eu_soccer_team("Barcellona") && away_name =~ "0123456788" && home_name =~ "0123456788" && home_points <= 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @instagram.get_pictures(count=v_home_points), ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && filter =~ "12345")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @tumblr-blog.post_picture(caption=v_filter, picture_url=v_filter) ;
}
====
now => * => @light-bulb.color_loop, true 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=42, from="bob"^^tt:username, in_reply_to="alice"^^tt:username), ((count > 14 && text =~ "12345")) , v_text := text, v_hashtags := hashtags, v_urls := urls => @light-bulb.color_loop() ;
}
====
now => * => @car.honk_horn, true 
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="9876543210"), ((target_language = "zh"^^tt:iso_lang_code("Chinese") && translated_text = "12345")) , v_source_language := source_language, v_translated_text := translated_text => @car.honk_horn() ;
}
====
now => @builtin.cool, true 
;;
Main() {
    now => @builtin.cool() ;
}
====
* => @xkcd.get_comic, (number >= 11 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    @fitness-tracker.getbody(), ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight < 82kg && leanMass >= 14 && bodyFat = 42 && bmi > 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @xkcd.get_comic(number=v_bmi), ((link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456788")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @builtin.notify(message=v_alt_text) ;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((yield > 14 && div = 55 && pay_date = makeDate(1462320000000)) || (div <= 42 && pay_date = makeDate(1487030400000) && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @icalendar.list_events(), (((sequence >= 7 && organizer = "23456789") || (end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence < 11))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_bike_market.post(title=v_location, price=v_sequence, info=v_description, poster=v_summary) ;
}
====
* => @dropbox.list_folder, ((is_folder = false && file_size < 5KB) || (folder_name = "0123456788" && file_name = "0123456789" && file_size = 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="9876543210"), (((folder_name =~ "ABCDEFGHIJ" && file_size <= 5KB) || (file_name =~ "ABCDEFGHIJ" && file_size >= 20MB))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @bluetooth_speaker.raise_volume() ;
}
====
* => @bing.web_search, (query =~ "0123456789" && description =~ "0123456788") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @phone.gps(), ((altitude >= 1000m || (location = $context.location.current_location && bearing <= 11))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @bing.web_search(query="9876543210"), ((query = "ABCDEFGHIJ" || (query =~ "0123456788" && title = "0123456789" && link = "http://www.google.com"^^tt:url) || (query = "ABCDEFGHIJ" && title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => @almond_dates.search, (poster = "12345" || message = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "ABCDEFGHIJ" && message =~ "23456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @linkedin.share(status=v_poster) ;
}
====
* => @nasa.apod, true , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @phone.receive_sms(), (body =~ "9876543210") , v_from := from, v_body := body => @nasa.apod(), ((title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
now => @almond_couch_market.search, (price <= 42 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (price > 7) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(on)) ;
}
====
* => @xkcd.get_comic, ((number < 42 && title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text = "9876543210") || (number > 55 && alt_text = "9876543210") || (number >= 42 && title = "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((summary = "0123456788" && description = "12345")) , v_date := date, v_summary := summary, v_description := description => @xkcd.get_comic(number=55), ((number >= 7 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text =~ "0123456788")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), ((timestamp =~ "9876543210" && from = "bob"^^tt:username && message =~ "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @car.honk_horn() ;
}
====
* => @almond_dates.post, ((message =~ "0123456788" && poster =~ "12345") || message = "9876543210") 
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "23456789" && user =~ "0123456789") || (title = "12345" && link = "http://www.google.com"^^tt:url && user = "12345") || (user = "ABCDEFGHIJ" && category =~ "9876543210") || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_dates.post(message=v_category, poster=v_category, phone=v_category, interest="0123456788") ;
}
====
@icalendar.event_begin, (summary = "ABCDEFGHIJ" && description =~ "12345" && sequence <= 42 && organizer =~ "0123456789" && location =~ "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1462320000000) && summary = "0123456788" && description =~ "23456789" && sequence < 55 && organizer = "9876543210" && location = "9876543210") || description = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && weight > 82kg && bodyFat > 7 && bmi > 14) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => @lg_webos_tv.set_volume, true 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.home && weight = 82kg && leanMass > 55 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @lg_webos_tv.set_volume(percent=v_bmi) ;
}
====
@activity-tracker.getmove, ((distance >= 1000m && steps < 7 && activeTime < 30min && inactiveTime > 30min && calories >= 500kcal) || (activeTime <= 30min && inactiveTime <= 30min && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && distance <= 1000m && steps > 55 && activeTime < 2h) || (updateTime = makeDate(1487030400000) && distance <= 1000m && steps <= 11 && activeTime <= 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "9876543210" && distance = 1000m && steps = 7 && activeTime > 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @instagram.get_pictures, ((count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && link = "http://www.abc.def"^^tt:url && filter =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) || (count <= 55 && caption =~ "12345" && filter =~ "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @slack.receive(), (channel = "funny"^^tt:hashtag) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @instagram.get_pictures(count=11), ((count < 11 && caption = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @weatherapi.sunrise, ((date = makeDate(1462320000000) && sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000)) || sunrise = makeDate(1487030400000) || (date = makeDate(1462320000000) && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => *
;;
Main() {
    @wall_street_journal.technology(), ((title =~ "23456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @weatherapi.sunrise(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (sunset = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @fitbit.getsteps, steps >= 55 , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), (steps >= 11) , v_date := date, v_steps := steps => @builtin.configure(device="scale"^^tt:device("my scale")) ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @youtube.search_channel(query="12345"), ((title =~ "ABCDEFGHIJ" && description =~ "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @security-camera.set_power(power=enum(off)) ;
}
====
* => @thecatapi.get, (count <= 55 || (image_id =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @thermostat.temperature(), (value >= 73F) , v_time := time, v_value := value => @thecatapi.get(), ((image_id = "0123456789" && count <= 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@youtube.source, ((channel_id =~ "12345" && channel_title =~ "0123456789" && video_id = "0123456788") || (channel_id =~ "12345" && channel_title =~ "23456789" && video_id =~ "12345" && video_url = "http://www.abc.def"^^tt:url) || channel_id =~ "0123456788" || (channel_id =~ "23456789" && channel_title = "9876543210" && video_id = "0123456789") || (channel_title =~ "0123456789" && title = "12345" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456789" && channel_title = "9876543210" && video_id =~ "0123456788")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => *
;;
Main() {
    @youtube.source(), ((channel_id =~ "12345" && channel_title =~ "0123456788" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@thermostat.humidity, (value <= 55 || value >= 7 || value <= 11 || value > 7) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1462320000000) || value = 7)) , v_time := time, v_value := value => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
now => * => @gmail.send_email, (to = "alice@gmail.com"^^tt:email_address && message =~ "12345") 
;;
Main() {
    now => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="12345") ;
}
====
@omlet.incomingmessage, ((type = enum(text) && message = "0123456788") || message =~ "12345") , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (((type = enum(text) && message = "23456789") || message = "23456789" || message =~ "0123456789")) , v_type := type, v_message := message => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && sequence <= 42 && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    @almond_couch_market.monitor(), ((price > 55 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(off)) ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @youtube.source(), ((channel_id =~ "12345" && title =~ "23456789" && video_id =~ "23456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @car.honk_horn() ;
}
====
* => @weatherapi.current, (location = $context.location.current_location && humidity <= 55 && cloudiness > 42 && fog >= 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate > 11 || heartrate = 55 || heartrate <= 14 || heartrate >= 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @weatherapi.current(location=v_place), ((temperature < 73F && wind_speed = 25mph && humidity <= 11 && fog <= 11)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
@gmail.receive_primary_email, (from_name =~ "0123456788" && from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1462320000000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_primary_email(), (((subject =~ "23456789" && contains(labels, "12345")) || (from_name = "9876543210" && snippet =~ "23456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @wall_street_journal.get_opinions(), ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => @twitter.search, ((query = "9876543210" && count < 55) || (count <= 55 && text = "ABCDEFGHIJ" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query =~ "12345" && count <= 42 && text =~ "ABCDEFGHIJ" && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="0123456788", from="alice"^^tt:username), (((count <= 55 && text =~ "9876543210" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query =~ "9876543210" && count < 42 && text =~ "0123456789" && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @light-bulb.color_loop() ;
}
====
now => * => @gmail.send_picture, ((to = "alice@gmail.com"^^tt:email_address && message = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || message =~ "12345" || message = "ABCDEFGHIJ" || message = "9876543210") 
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(cooling) || state = enum(heating) || mode = enum(heat))) , v_mode := mode, v_state := state => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@wall_street_journal.world_news, title =~ "9876543210" , v_title := title, v_link := link => *
;;
Main() {
    @wall_street_journal.world_news(), (title =~ "0123456789") , v_title := title, v_link := link => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
@icalendar.event_begin, (description =~ "0123456788" || (start_date = makeDate(1487030400000) && description =~ "9876543210" && location = "ABCDEFGHIJ") || (start_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description =~ "23456789" && sequence > 14 && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((summary = "23456789" && sequence <= 14 && location = "23456789") || summary = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => * => @twitter.send_direct_message, true 
;;
Main() {
    @thermostat.temperature(), (value >= 22C) , v_time := time, v_value := value => @twitter.send_direct_message(to="bob"^^tt:username, message="0123456789") ;
}
====
@holidays.us_holiday_event, (date = makeDate(1462320000000) && description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => * => *
;;
Main() {
    @holidays.us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @speaker.play_music() ;
}
====
@onedrive.file_modified, file_name =~ "0123456789" , v_file_name := file_name => notify
;;
Main() {
    @onedrive.file_modified(), ((file_name = "12345" || file_name =~ "9876543210")) , v_file_name := file_name => notify;
}
====
@jawbone_up.getmove, (day =~ "23456789" && activeTime <= 2h && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @jawbone_up.getmove(), ((distance = 42cm && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @ytranslate.translate(target_language=v_day, text=v_day), ((translated_text = "0123456789" || (source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "en"^^tt:iso_lang_code("English") && text = "0123456789" && translated_text = "ABCDEFGHIJ"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@instagram.new_picture, (filter = "0123456789" && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    @instagram.new_picture(), ((caption =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
now => @weatherapi.current, ((cloudiness <= 42 && fog > 14 && weather = "ABCDEFGHIJ") || (wind_speed >= 5kmph && cloudiness > 55 && fog > 11 && weather =~ "23456789") || (location = $context.location.current_location && wind_speed <= 5kmph && cloudiness >= 42 && fog < 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @dropbox.move, (old_name = "9876543210" || new_name =~ "0123456789" || (old_name =~ "9876543210" && new_name = v_weather) || old_name = "9876543210") 
;;
Main() {
    now => @weatherapi.current(location=$context.location.work), ((wind_speed < 25mph && humidity = 7 && cloudiness < 55 && fog <= 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @dropbox.move(old_name=v_weather, new_name=v_weather) ;
}
====
now => @giphy.get_tag, true , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => @bluetooth_speaker.lower_volume() ;
}
====
@github.new_commit, true , v_user := user, v_message := message, v_time := time => notify
;;
Main() {
    @github.new_commit(repo_name="12345"), ((repo_name = "12345" || time = makeDate(1462320000000))) , v_user := user, v_message := message, v_time := time => notify;
}
====
now => * => @almond_couch_market.post, ((title = "0123456789" && price >= 55) || price <= 11 || (title = "9876543210" && price = 55 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @almond_couch_market.post(title="12345", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
* => @wall_street_journal.get_opinions, (title =~ "23456789" || (title = "0123456788" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @holidays.uk_holiday_event(), (((date = makeDate(1462320000000) && summary = "23456789" && description =~ "12345") || (summary = "ABCDEFGHIJ" && description = "23456789") || (summary =~ "23456789" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => @wall_street_journal.get_opinions(), ((title = "12345" || title = "0123456789")) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @wall_street_journal.get_opinions, ((title = "ABCDEFGHIJ" && updated = makeDate(1487030400000)) || link = "http://www.abc.def"^^tt:url || title = "23456789" || title = "23456789" || updated = makeDate(1462320000000)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @wall_street_journal.markets(), true , v_title := title, v_link := link => @wall_street_journal.get_opinions(), (((title =~ "12345" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000)) || title = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @twitter.search_by_hashtag, ((query_hashtag = "cat"^^tt:hashtag && count <= 11) || (text =~ "9876543210" && in_reply_to = "bob"^^tt:username) || (count >= 7 && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username) || (count < 42 && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @heatpad.set_power, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 55 && name =~ "9876543210" && estimated_diameter_max > 42cm && closest_distance_to_earth < 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @twitter.search_by_hashtag(query_hashtag=v_orbiting_body, count=v_asteroid_id, from=v_orbiting_body, in_reply_to=v_orbiting_body), ((query_hashtag = "cat"^^tt:hashtag && count <= 11 && text =~ "12345" && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @heatpad.set_power(power=enum(off)) ;
}
====
now => * => @omlet.send_picture, (caption =~ "23456789" || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345")) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && description = "0123456788") || description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
* => * => @builtin.cool, true 
;;
Main() {
    now => @almond_dates.search(), ((interest = "23456789" && message = "23456789" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.cool() ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), ((interest = "0123456789" && message = "9876543210" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @fitbit.getbody(), (((weight >= 82kg && bmi <= 14) || (weight <= 155lb && bmi >= 7) || (weight >= 82kg && bmi = 7 && fat >= 42))) , v_weight := weight, v_bmi := bmi, v_fat := fat => @builtin.discover() ;
}
====
now => @nasa.apod, (title = "12345" || title = "9876543210" || (title = "12345" && description = "23456789") || title = "0123456789") , v_title := title, v_description := description, v_picture_url := picture_url => @heatpad.set_power, power = enum(off) 
;;
Main() {
    now => @nasa.apod(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_description := description, v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
@wall_street_journal.us_business, (link = "http://www.abc.def"^^tt:url || title = "12345" || title =~ "9876543210" || link = "http://www.google.com"^^tt:url || title = "23456789" || title = "0123456789" || title =~ "12345" || link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => @speaker.set_volume, percent <= 55 
;;
Main() {
    @wall_street_journal.us_business(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @speaker.set_volume(percent=7) ;
}
====
@almond_bike_market.monitor, ((info = "0123456788" && price <= 55) || (info =~ "9876543210" && price <= 55))  => *
;;
Main() {
    @almond_bike_market.monitor(info="12345"), true  => @slack.setPresence(presence=enum(auto)) ;
}
====
now => @phone.notify, message =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @phone.notify(title="12345", message="ABCDEFGHIJ") ;
}
====
@yahoofinance.stock_div, yield <= 14 , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((company_name = "23456789" && div >= 55) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "23456789" && yield < 14 && div > 55 && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_dates.search(), ((message = "9876543210" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 1000m)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight, true 
;;
Main() {
    @builtin.timer(interval=30min), ((interval = 2h || interval <= 30min))  => @phone.get_gps(), (((altitude <= 1000m && bearing >= 42) || (altitude >= 42cm && bearing > 7) || bearing < 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight(weight=155lb) ;
}
====
* => @almond_couch_market.post, true 
;;
Main() {
    @xkcd.new_whatif(), ((title = "ABCDEFGHIJ" || title =~ "9876543210" || link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ")) , v_title := title, v_link := link => @almond_couch_market.post(title="9876543210", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @imgflip.generate, (template =~ "ABCDEFGHIJ" && text_top = "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @wall_street_journal.lifestyle(), ((title = "0123456788" || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456789" || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @imgflip.generate(template="0123456788", text_top="ABCDEFGHIJ", text_bottom="9876543210"), ((template = "0123456788" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
@xkcd.new_comic, (number >= 7 && link = "http://www.abc.def"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => @lg_webos_tv.play_url, true 
;;
Main() {
    @xkcd.new_comic(), ((title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @wall_street_journal.get_opinions(), (title =~ "9876543210") , v_title := title, v_link := link, v_updated := updated => @lg_webos_tv.play_url(url=v_title) ;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && bodyFat <= 55 && bmi <= 7) || (weight < 155lb && leanMass = 42 && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && bodyFat < 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @twitter.sink, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @twitter.source(), (((text =~ "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag)) || (contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username && in_reply_to = "alice"^^tt:username) || (text = "0123456789" && in_reply_to = "alice"^^tt:username && __reserved = false) || (text =~ "9876543210" && contains(hashtags, "lol"^^tt:hashtag)))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @twitter.sink(status="12345") ;
}
====
@instagram.new_picture, (caption =~ "0123456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((caption = "9876543210" && filter = "0123456788" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @speaker.raise_volume() ;
}
====
@builtin.timer, interval <= 2h  => notify
;;
Main() {
    @builtin.timer(interval=30min), true  => notify;
}
====
@almond_couch_market.monitor, ((price >= 11 && contact = "+15551234567"^^tt:phone_number) || contact = "+15551234567"^^tt:phone_number || (price < 55 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "ABCDEFGHIJ" && price > 55) || price > 55 || (title = "12345" && contact = "+16501234567"^^tt:phone_number) || (title =~ "ABCDEFGHIJ" && price > 55 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @holidays.next_us_holiday_event, (summary = "9876543210" && description = "0123456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((description = "23456789" || summary = "9876543210" || (date = makeDate(1462320000000) && summary = "23456789") || (summary =~ "12345" && description = "ABCDEFGHIJ") || description = "9876543210" || (date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description =~ "9876543210"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@almond_dates.monitor, ((interest = "9876543210" && poster =~ "23456789") || (message =~ "ABCDEFGHIJ" && poster =~ "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((phone = "+16501234567"^^tt:phone_number || phone = "+15551234567"^^tt:phone_number || (message =~ "23456789" && poster = "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @almond_couch_market.post, title =~ "0123456788" 
;;
Main() {
    @builtin.at(time=makeTime(7,30)), ((time = makeTime(15,0) || time = makeTime(15,0)))  => @almond_couch_market.post(title="ABCDEFGHIJ", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
* => @security-camera.get_snapshot, true , v_picture_url := picture_url => *
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
now => @tumblr-blog.post_text, body = "12345" 
;;
Main() {
    now => @tumblr-blog.post_text(title="0123456788", body="9876543210") ;
}
====
* => @fitbit.recordweight, weight <= 82kg 
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @fitbit.recordweight(weight=82kg) ;
}
====
@phone.receive_sms, ((from = "+16501234567"^^tt:phone_number && body =~ "9876543210") || (from = "+15551234567"^^tt:phone_number && body = "9876543210") || (from = "+16501234567"^^tt:phone_number && body =~ "0123456789") || body =~ "9876543210") , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => notify;
}
====
* => @nasa.rover, (count >= 42 && camera_used = "9876543210") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @light-bulb.color_loop() ;
}
====
* => @almond_couch_market.post, price >= 14 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @almond_couch_market.post(title=v_file_name, price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
now => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption = "9876543210") 
;;
Main() {
    now => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="ABCDEFGHIJ") ;
}
====
@tumblr.new_text, ((blog_name =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && contains(tags, "lol"^^tt:hashtag) && body = "ABCDEFGHIJ") || body = "ABCDEFGHIJ") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), ((blog_name = "ABCDEFGHIJ" && date = makeDate(1487030400000) && title = "0123456788" && body =~ "12345")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @icalendar.list_events(), ((sequence >= 14 && organizer = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@youtube.source, (channel_title =~ "12345" || video_url = "http://www.abc.def"^^tt:url || (channel_title =~ "23456789" && video_id =~ "9876543210") || (channel_id = "0123456789" && channel_title = "ABCDEFGHIJ" && title =~ "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), (video_id = "23456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @lg_webos_tv.lower_volume() ;
}
====
now => * => @tumblr-blog.post_text, (title = "9876543210" || title =~ "23456789" || body =~ "0123456789" || title =~ "0123456788" || title =~ "12345") 
;;
Main() {
    now => @xkcd.get_comic(), (((number > 7 && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "9876543210") || alt_text = "0123456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @tumblr-blog.post_text(title=v_title, body=v_title) ;
}
====
* => @gmail.get_latest, ((from = "bob@stanford.edu"^^tt:email_address && subject =~ "0123456789" && label =~ "0123456788") || (from = "alice@gmail.com"^^tt:email_address && subject =~ "23456789") || (subject =~ "9876543210" && label =~ "9876543210") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @gmail.send_picture, true 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@fitness-tracker.getbody, ((weight >= 155lb && bmi < 55) || (leanMass <= 42 && bodyFat < 7 && bmi = 42) || (time = makeDate(1487030400000) && weight >= 155lb && leanMass <= 55 && bodyFat <= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), (bodyFat < 7) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ" && sequence >= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && organizer = "0123456788") || (end_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && sequence <= 11 && organizer = "9876543210" && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.home && duration <= 2h && light <= 30min && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && sequence <= 55 && organizer = "23456789") || (sequence > 7 && organizer = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @bluetooth_speaker.lower_volume() ;
}
====
* => @imgflip.generate, ((text_top = "23456789" && text_bottom = "ABCDEFGHIJ" && name = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top = "9876543210" && name =~ "12345") || (template =~ "ABCDEFGHIJ" && text_top = "ABCDEFGHIJ" && text_bottom = "9876543210")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="0123456789", text_top="0123456788", text_bottom="0123456788"), ((text_top =~ "23456789" && name =~ "9876543210")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @holidays.next_uk_holiday_event, (date = makeDate(1487030400000) && summary =~ "0123456788" && description =~ "0123456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "0123456788" && yield < 55 && div < 7 && pay_date = makeDate(1487030400000) && ex_div_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @holidays.next_uk_holiday_event(), (description =~ "12345") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@reddit_front_page.newpost, user = "ABCDEFGHIJ" , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "ABCDEFGHIJ" && category = "0123456789") || (title = "ABCDEFGHIJ" && category =~ "0123456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @holidays.next_uk_holiday_event(), ((description =~ "9876543210" || (date = makeDate(1487030400000) && description =~ "12345") || (date = makeDate(1487030400000) && summary = "ABCDEFGHIJ") || summary = "9876543210" || (date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @almond_bike_market.post, (price >= 42 && info = "9876543210") 
;;
Main() {
    @wall_street_journal.lifestyle(), (((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "0123456789")) , v_title := title, v_link := link => @almond_bike_market.post(title=v_title, info=v_title, price=11, poster="ABCDEFGHIJ") ;
}
====
now => @thermostat.get_temperature, true , v_value := value => *
;;
Main() {
    now => @thermostat.get_temperature(), true , v_value := value => @dropbox.create_new_folder(folder_name="0123456788") ;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && filter = "0123456788" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && filter =~ "0123456789") || caption = "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @lg_webos_tv.raise_volume, true 
;;
Main() {
    now => @lg_webos_tv.raise_volume() ;
}
====
* => * => @almond_bike_market.post, ((title =~ "23456789" && price >= 55 && info = "0123456789" && poster =~ "ABCDEFGHIJ") || (title =~ "12345" && price < 7 && poster = "9876543210")) 
;;
Main() {
    @almond_couch_market.monitor(), (price <= 11) , v_title := title, v_price := price, v_contact := contact => @instagram.get_pictures(count=v_price), (((count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && filter =~ "ABCDEFGHIJ" && location = $context.location.work) || (count >= 11 && filter =~ "ABCDEFGHIJ") || (count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_bike_market.post(title="12345", price=11, info="9876543210", poster="9876543210") ;
}
====
* => * => @tumblr-blog.post_picture, true 
;;
Main() {
    now => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), ((file_name =~ "0123456788" && file_size >= 5KB && full_path = "0123456788")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @tumblr-blog.post_picture(caption=v_file_name, picture_url=v_file_name) ;
}
====
@xkcd.new_whatif, true , v_title := title, v_link := link => notify
;;
Main() {
    @xkcd.new_whatif(), (((title =~ "12345" && link = "http://www.google.com"^^tt:url) || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => notify;
}
====
@jawbone_up.getsleep, (time = makeDate(1462320000000) && place = $context.location.work && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && duration < 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @jawbone_up.getsleep(), ((time = makeDate(1462320000000) && duration > 2h && light >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @nasa.rover(), (count <= 42) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
now => * => @thermostat.set_target_temperature, value = 22C 
;;
Main() {
    now => @fitbit.getbody(), ((weight >= 155lb && fat <= 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @thermostat.set_target_temperature(value=73F) ;
}
====
now => @dropbox.open, (file_name = "9876543210" || (file_name =~ "ABCDEFGHIJ" && url = "23456789") || file_name =~ "0123456788" || file_name =~ "12345") , v_url := url => @car.honk_horn, true 
;;
Main() {
    now => @dropbox.open(file_name="ABCDEFGHIJ"), (((file_name = "0123456789" && url = "23456789") || (file_name =~ "23456789" && url =~ "23456789") || url =~ "9876543210")) , v_url := url => @car.honk_horn() ;
}
====
* => @phone.call_emergency, true 
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), (((watched_is_home = true && away_name =~ "12345" && home_name =~ "23456789" && away_points >= 11 && home_points > 55) || (watched_team_alias = "gsw"^^sportradar:nba_team("Golden State Warriors") && other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && watched_is_home = true && away_name =~ "0123456788" && home_name = "23456789" && away_points < 7 && home_points < 7) || (other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_name =~ "0123456788" && home_points <= 55 && result = enum(unclosed)) || (away_name = "ABCDEFGHIJ" && away_points = 42 && home_points <= 42 && result = enum(unclosed)))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @phone.call_emergency() ;
}
====
@almond_dates.monitor, ((interest = "23456789" && message = "0123456789" && phone = "+16501234567"^^tt:phone_number) || (interest = "9876543210" && poster =~ "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @imgflip.generate(template=v_interest, text_top=v_message, text_bottom="0123456789"), ((template =~ "0123456789" && name = "23456789")) , v_name := name, v_picture_url := picture_url => notify;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description =~ "0123456788" && sequence <= 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), ((summary = "0123456789" && description = "0123456789" && location =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && category =~ "12345") || category = "0123456788" || (title = "12345" && user = "0123456788" && category =~ "0123456788") || (title =~ "23456789" && link = "http://www.google.com"^^tt:url && user =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => *
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "ABCDEFGHIJ" && category = "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @light-bulb.alert_long() ;
}
====
* => @instagram.get_pictures, ((count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = $context.location.home) || (count > 11 && caption = "12345" && filter =~ "0123456788" && contains(hashtags, "funny"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "9876543210" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name = "23456789" && bid_price > 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @instagram.get_pictures(count=v_bid_price), (((count >= 42 && caption = "23456789" && filter = "0123456789") || (filter =~ "12345" && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @almond_dates.search, poster = "9876543210" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "0123456789" && price >= 7) || price < 11 || price >= 11)) , v_title := title, v_price := price, v_contact := contact => @almond_dates.search(), (poster = "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @slack.send_picture, ((caption =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || caption = "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @github.new_milestone(repo_name="23456789"), (((description =~ "23456789" && time = makeDate(1462320000000)) || (description =~ "23456789" && time = makeDate(1487030400000)))) , v_user := user, v_description := description, v_time := time => @slack.send_picture(channel=v_description, caption=v_description, picture_url=v_description) ;
}
====
* => @google_drive.create_new_drive_file, (file_name = "0123456788" || file_name =~ "9876543210" || file_name = "0123456789" || file_name =~ "0123456789" || file_name = "0123456788") 
;;
Main() {
    @icalendar.event_begin(), (((summary = "9876543210" && description = "9876543210" && sequence <= 7 && organizer = "0123456789" && location = "0123456789") || (start_date = makeDate(1487030400000) && summary =~ "0123456789" && organizer =~ "0123456789" && location =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
now => @thermostat.get_humidity, value >= 42 , v_value := value => notify
;;
Main() {
    now => @thermostat.get_humidity(), ((value = 42 || value >= 55)) , v_value := value => notify;
}
====
* => @nasa.apod, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @jawbone_up.getbody(), (((weight < 155lb && bodyFat <= 11 && bmi = 14) || (weight = 155lb && leanMass >= 42 && bodyFat > 11) || (weight <= 82kg && leanMass > 7 && bodyFat <= 14 && bmi < 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @nasa.apod(), (((description =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || description = "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) && heartrate >= 42) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_dates.search, (interest =~ "12345" && message =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && heartrate < 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_dates.search(), ((poster =~ "0123456788" || (interest =~ "0123456789" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) || message = "0123456788" || (message =~ "0123456789" && poster =~ "23456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @instagram.get_pictures, (link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @wall_street_journal.markets(), ((title =~ "0123456789" || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url) || (title = "12345" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => @instagram.get_pictures(count=14), ((count > 55 && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && location = $context.location.work) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @tumblr-blog.post_picture(caption=v_filter, picture_url=v_picture_url) ;
}
====
@instagram.new_picture, (caption = "9876543210" && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && link = "http://www.abc.def"^^tt:url && filter =~ "12345" && location = $context.location.current_location) || (caption =~ "23456789" && filter = "9876543210"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @uber.price_estimate(start=$context.location.home, end=$context.location.current_location), ((end = $context.location.home && uber_type =~ "9876543210" && low_estimate < 14 && currency_code = "0123456789" && distance < 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    @onedrive.file_created(), (file_name =~ "9876543210") , v_file_name := file_name => @linkedin.share(status=v_file_name) ;
}
====
* => @uber.time_estimate, (estimate < 30min || (start = $context.location.work && uber_type = "12345") || start = $context.location.work) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    @builtin.at(time=makeTime(20,30)), ((time = makeTime(7,30) || time = makeTime(20,30) || time = makeTime(7,30)))  => @uber.time_estimate(start=$context.location.current_location), ((uber_type = "23456789" && estimate = 2h)) , v_uber_type := uber_type, v_estimate := estimate => @heatpad.set_power(power=enum(on)) ;
}
====
@thermostat.humidity, time = makeDate(1487030400000) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), (value <= 42) , v_time := time, v_value := value => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "12345")) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @gmail.get_latest, ((from = "bob@stanford.edu"^^tt:email_address && label = "0123456788") || subject =~ "ABCDEFGHIJ") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && label =~ "23456789") || subject =~ "12345")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => * => @almond_bike_market.post, (info = "23456789" || (price < 55 && info = "23456789")) 
;;
Main() {
    now => @almond_dates.search(), (((interest =~ "ABCDEFGHIJ" && poster = "0123456789") || interest = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_bike_market.post(title=v_message, info=v_message, poster=v_message, price=14) ;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    @xkcd.new_comic(), ((number >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @giphy.get_tag(tag=v_title), true , v_picture_url := picture_url => notify;
}
====
@instagram.new_picture, ((caption = "ABCDEFGHIJ" && filter =~ "12345" && contains(hashtags, "lol"^^tt:hashtag)) || (filter = "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => @fitbit.recordweight, true 
;;
Main() {
    @instagram.new_picture(), ((caption =~ "12345" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=155lb) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(info="ABCDEFGHIJ", price=14), ((price > 11 || (info = "0123456788" && price >= 42)))  => @speaker.raise_volume() ;
}
====
@fitness-tracker.getbody, ((weight >= 155lb && leanMass > 7 && bodyFat <= 14 && bmi > 42) || (time = makeDate(1462320000000) && weight <= 82kg && bodyFat > 11 && bmi > 7) || (weight < 155lb && leanMass = 7 && bodyFat >= 42 && bmi <= 7) || (weight > 155lb && leanMass >= 42 && bodyFat > 11 && bmi >= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.current_location && weight = 155lb && leanMass > 42 && bodyFat >= 11 && bmi >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
now => * => @light-bulb.alert_long, true 
;;
Main() {
    now => @youtube.list_subscriptions(), (thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @light-bulb.alert_long() ;
}
====
now => @gmail.send_picture, subject = "12345" 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="0123456789", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @wall_street_journal.get_opinions, updated = makeDate(1462320000000) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title =~ "0123456789") , v_title := title, v_link := link, v_updated := updated => @lg_webos_tv.mute() ;
}
====
* => @dropbox.move, ((old_name =~ "23456789" && new_name = "9876543210") || new_name =~ "0123456788") 
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), ((watched_team_abbr = "sta"^^sportradar:ncaafb_team("Stanford Cardinals") && home_name =~ "ABCDEFGHIJ" && result = enum(unclosed))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @dropbox.move(old_name=v_home_name, new_name=v_home_name) ;
}
====
now => * => @speaker.set_volume, (percent <= 14 || percent >= 7 || percent < 55 || percent > 14) 
;;
Main() {
    now => @speaker.set_volume(percent=55) ;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, from="alice"^^tt:username), ((count > 55 && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @heatpad.set_power(power=enum(on)) ;
}
====
@phone.receive_sms, true , v_from := from, v_body := body => * => *
;;
Main() {
    @phone.receive_sms(), ((from = "+16501234567"^^tt:phone_number || from = "+16501234567"^^tt:phone_number || (from = "+15551234567"^^tt:phone_number && body =~ "0123456788") || from = "+16501234567"^^tt:phone_number)) , v_from := from, v_body := body => @icalendar.list_events(), (((description = "12345" && sequence > 14 && organizer =~ "23456789") || (start_date = makeDate(1462320000000) && description = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @twitter.post_picture(caption=v_summary, picture_url=v_organizer) ;
}
====
* => * => @almond_couch_market.post, (title = "0123456789" && price <= 14) 
;;
Main() {
    @icalendar.event_begin(), ((summary = "ABCDEFGHIJ" && organizer = "9876543210" && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @holidays.next_us_holiday_event(), ((summary =~ "12345" || (date = makeDate(1462320000000) && summary = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => @almond_couch_market.post(title=v_description, contact=v_description, price=42) ;
}
====
@yahoofinance.stock_div, (yield < 7 && div > 55) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((company_name = "12345" && yield >= 11 && div > 42 && pay_date = makeDate(1462320000000)) || (company_name = "ABCDEFGHIJ" && yield >= 42 && div > 11 && ex_div_date = makeDate(1487030400000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
@security-camera.new_event, (has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @linkedin.get_profile(), (((formatted_name = "0123456788" && headline = "9876543210" && specialties =~ "9876543210") || (formatted_name = "9876543210" && headline = "23456789" && industry = "23456789" && specialties = "0123456788") || (formatted_name =~ "0123456789" && headline =~ "0123456788" && specialties =~ "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @lg_webos_tv.play_url, url = "http://www.abc.def"^^tt:url 
;;
Main() {
    now => @lg_webos_tv.play_url(url="http://www.google.com"^^tt:url) ;
}
====
now => @fitbit.getbody, ((weight >= 155lb && bmi >= 55 && fat >= 55) || (bmi <= 42 && fat < 11) || fat > 14 || (weight >= 155lb && fat >= 7) || (bmi <= 11 && fat > 11) || (weight < 155lb && bmi >= 7 && fat = 11) || fat = 11 || bmi >= 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => *
;;
Main() {
    now => @fitbit.getbody(), ((weight >= 82kg && fat >= 11)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @light-bulb.color_loop() ;
}
====
* => @tumblr-blog.post_text, title =~ "ABCDEFGHIJ" 
;;
Main() {
    @twitter.source(), ((in_reply_to = "alice"^^tt:username || contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @tumblr-blog.post_text(title=v_text, body=v_text) ;
}
====
@heartrate-monitor.getheartrate, (heartrate > 7 || time = makeDate(1487030400000) || (place = $context.location.work && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate < 42)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
@almond_couch_market.monitor, (price >= 55 || (title =~ "23456789" && price <= 42) || (title = "ABCDEFGHIJ" && price <= 55)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (((title = "ABCDEFGHIJ" && price >= 7) || (title =~ "ABCDEFGHIJ" && contact = "+16501234567"^^tt:phone_number) || contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @instagram.get_pictures(count=v_price), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @icalendar.list_events, (sequence <= 42 && location = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @omlet.newmessage(), ((message =~ "ABCDEFGHIJ" || message =~ "ABCDEFGHIJ")) , v_type := type, v_message := message => @icalendar.list_events(), ((summary =~ "23456789" && description = "0123456789" && sequence < 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @onedrive.delete_file, file_name = "23456789" 
;;
Main() {
    @onedrive.file_created(), (file_name = "9876543210") , v_file_name := file_name => @onedrive.delete_file(file_name="0123456789") ;
}
====
* => @ytranslate.detect_language, text =~ "12345" , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="0123456788"), true , v_detected_language := detected_language => notify;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    @almond_dates.monitor(), (((message = "9876543210" && poster =~ "12345") || phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @giphy.get_tag(tag=v_interest), true , v_picture_url := picture_url => @lg_webos_tv.mute() ;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_description := description => @heatpad.set_power(power=enum(on)) ;
}
====
@nasa.asteroid, (estimated_diameter_min > 42cm && estimated_diameter_max >= 42cm && dangerous = true && closest_distance_to_earth > 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 55 && name =~ "12345" && estimated_diameter_min >= 42cm && estimated_diameter_max >= 42cm && orbiting_body = "23456789") || (name =~ "0123456788" && estimated_diameter_min < 42cm && closest_distance_to_earth < 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
now => @phone.get_gps, (bearing >= 11 && speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (((altitude = 42cm && bearing < 14 && speed = 5kmph) || (location = $context.location.work && altitude < 42cm && bearing <= 42))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@almond_couch_market.monitor, (price < 14 || title =~ "9876543210") , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((price >= 11 || (title = "9876543210" && price > 55))) , v_title := title, v_price := price, v_contact := contact => @bluetooth_speaker.play_music() ;
}
====
* => @instagram.get_pictures, (count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "ABCDEFGHIJ") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @instagram.get_pictures(), (((count < 14 && caption =~ "12345" && link = "http://www.abc.def"^^tt:url) || (count > 11 && caption = "0123456788" && link = "http://www.google.com"^^tt:url && filter = "12345" && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
* => @fitbit.getbody, (bmi >= 55 && fat = 11) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), ((issue_number = 11 && body = "23456789")) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @fitbit.getbody(), (((bmi = 55 && fat >= 42) || (weight = 155lb && fat > 11) || (weight = 155lb && bmi <= 55))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @linkedin.share, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @github.new_issue_comment(repo_name="0123456789"), (((issue_number < 11 && time = makeDate(1487030400000)) || repo_name = "0123456789" || issue_number > 42)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @linkedin.share(status=v_body) ;
}
====
* => @gmail.get_latest, (subject = "0123456789" && label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @gmail.get_latest(), (label =~ "12345") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @twitter.search_by_hashtag, ((contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username) || (count < 7 && text = "23456789" && in_reply_to = "bob"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count < 11 && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight < 155lb)) , v_time := time, v_weight := weight => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, count=14, from="bob"^^tt:username), ((count < 7 && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((interest = "23456789" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => notify;
}
====
* => @almond_couch_market.post, (price >= 11 || (title =~ "ABCDEFGHIJ" && price <= 11 && contact = "+16501234567"^^tt:phone_number) || price < 42 || (title = "9876543210" && price = 42)) 
;;
Main() {
    @github.new_commit(repo_name="ABCDEFGHIJ"), ((user = "bob"^^tt:username && message =~ "23456789")) , v_user := user, v_message := message, v_time := time => @almond_couch_market.post(title=v_message, contact=v_message, price=14) ;
}
====
now => * => @slack.updateChannelPurpose, purpose =~ "9876543210" 
;;
Main() {
    now => @bing.image_search(query="23456789"), ((query = "9876543210" && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 7 && height >= 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @slack.updateChannelPurpose(channel="lol"^^tt:hashtag, purpose="0123456788") ;
}
====
@xkcd.new_comic, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (number <= 11 && title = "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @almond_dates.post, interest = "12345" 
;;
Main() {
    @xkcd.new_comic(), (link = "http://www.abc.def"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @almond_dates.post(interest=v_title, message=v_title, phone=v_title, poster="23456789") ;
}
====
* => @instagram.get_pictures, ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789" && filter = "0123456789") || (count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @almond_dates.monitor(), (poster =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @instagram.get_pictures(count=14), (((count = 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter = "12345") || (count < 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && filter =~ "ABCDEFGHIJ" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@sleep-tracker.getsleep, (rem <= 2h && light < 2h && deep < 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && place = $context.location.current_location && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration > 2h && rem <= 30min) || (time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && duration >= 30min && light <= 2h && deep < 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @ytranslate.translate, ((target_language = "zh"^^tt:iso_lang_code("Chinese") && text = "0123456789" && translated_text =~ "9876543210") || (text =~ "ABCDEFGHIJ" && translated_text =~ "0123456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="9876543210"), (((text = "23456789" && translated_text = "0123456788") || (target_language = "en"^^tt:iso_lang_code("English") && text = "ABCDEFGHIJ" && translated_text =~ "0123456788"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@instagram.new_picture, ((caption = "12345" && filter = "ABCDEFGHIJ") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456788" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight, weight >= 155lb 
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789") || (caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=82kg) ;
}
====
now => * => @almond_bike_market.post, ((title = "0123456789" && price <= 7) || (title =~ "9876543210" && price > 14) || (title =~ "ABCDEFGHIJ" && price > 14 && info =~ "ABCDEFGHIJ" && poster =~ "9876543210") || (title =~ "0123456788" && price = 7 && info = "0123456788") || (title =~ "0123456789" && price >= 42 && poster =~ "12345")) 
;;
Main() {
    now => @almond_bike_market.detail(id="0123456789"), ((id =~ "0123456789" || id =~ "9876543210" || id =~ "9876543210"))  => @almond_bike_market.post(title="ABCDEFGHIJ", price=55, info="12345", poster="ABCDEFGHIJ") ;
}
====
* => @xkcd.get_comic, (number >= 11 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(number=7), (((number <= 7 && title = "12345") || (number = 42 && link = "http://www.abc.def"^^tt:url && alt_text =~ "12345"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @facebook.post(status=v_alt_text) ;
}
====
* => @phone.get_gps, (altitude > 42cm && bearing <= 14 && speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && asleepTime = makeDate(1462320000000) && duration >= 30min && light = 30min && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @phone.get_gps(), ((altitude > 42cm && bearing = 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @bing.web_search, (link = "http://www.google.com"^^tt:url || (query = "0123456788" && description =~ "12345")) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    @jawbone_up.getheartrate(), (heartrate >= 11) , v_time := time, v_place := place, v_heartrate := heartrate => @bing.web_search(query="12345"), (((query =~ "12345" && description =~ "0123456788") || title = "12345" || (description =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1462320000000) && summary = "12345" && description =~ "9876543210") || (date = makeDate(1462320000000) && summary =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @wall_street_journal.lifestyle(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => @holidays.next_uk_holiday_event(), ((summary = "ABCDEFGHIJ" && description = "12345")) , v_date := date, v_summary := summary, v_description := description => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="12345") ;
}
====
* => @omlet.send_picture, ((to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || to = "bob"^^tt:username) 
;;
Main() {
    @twitter.direct_message(), (from = "bob"^^tt:username) , v_from := from, v_message := message, v___reserved := __reserved => @omlet.send_picture(picture_url=v_message, caption=v_message, to="alice"^^tt:username) ;
}
====
* => @almond_bike_market.post, true 
;;
Main() {
    @reddit_front_page.newpost(), ((title = "12345" && link = "http://www.google.com"^^tt:url && user = "0123456789" && category = "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_bike_market.post(title="9876543210", price=11, info="23456789", poster="0123456788") ;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    @sleep-tracker.getsleep(), ((duration <= 30min && light >= 2h && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @weatherapi.moon(location=v_place), (date = makeDate(1487030400000)) , v_date := date, v_phase := phase => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
now => @thermostat.get_temperature, value >= 22C , v_value := value => notify
;;
Main() {
    now => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
@washington_post.new_blog_post, (title = "0123456788" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(powerpost)), ((link = "http://www.abc.def"^^tt:url || title =~ "12345" || title = "12345")) , v_title := title, v_link := link => notify;
}
====
* => @dropbox.open, url =~ "ABCDEFGHIJ" , v_url := url => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "12345" && channel_title = "12345") || (channel_title = "0123456788" && video_id = "12345" && video_url = "http://www.google.com"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @dropbox.open(file_name=v_title), true , v_url := url => notify;
}
====
now => @tumblr-blog.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (caption = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || caption = "9876543210") 
;;
Main() {
    now => @tumblr-blog.post_picture(caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@icalendar.event_begin, ((start_date = makeDate(1487030400000) && description = "9876543210" && sequence >= 7 && organizer = "0123456789") || (summary =~ "9876543210" && description = "23456789" && location =~ "23456789") || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence <= 14 && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "0123456789" && sequence = 42)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thermostat.get_hvac_state(), (mode = enum(off)) , v_mode := mode, v_state := state => notify;
}
====
@icalendar.event_begin, ((end_date = makeDate(1462320000000) && summary = "12345" && description =~ "23456789" && sequence >= 11) || (start_date = makeDate(1487030400000) && description = "0123456789" && sequence > 55 && location = "0123456789") || description =~ "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @light-bulb.set_power, true 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && summary =~ "0123456788" && organizer = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @light-bulb.set_power(power=enum(off)) ;
}
====
now => @wall_street_journal.get_opinions, ((title =~ "0123456789" && updated = makeDate(1487030400000)) || link = "http://www.abc.def"^^tt:url || title =~ "12345" || title = "0123456788" || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @dropbox.create_new_folder, folder_name = "9876543210" 
;;
Main() {
    @builtin.timer(interval=2h), (interval <= 2h)  => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
* => @linkedin.get_profile, (headline =~ "0123456788" || (formatted_name = "9876543210" && headline = "ABCDEFGHIJ")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "9876543210" && headline = "ABCDEFGHIJ" && industry = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @google_drive.create_new_drive_file, file_name = "23456789" 
;;
Main() {
    now => @gmail.get_latest(), (subject =~ "0123456789") , v_from := from, v_subject := subject, v_label := label => @google_drive.create_new_drive_file(file_name=v_label) ;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "0123456788" && specialties =~ "ABCDEFGHIJ") || industry =~ "12345") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @tumblr.new_photo(blog_name="0123456788"), ((blog_name = "0123456788" && caption = "12345" && link = "http://www.google.com"^^tt:url && contains(tags, "funny"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @youtube.list_subscriptions, (title =~ "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.list_subscriptions(), (title =~ "9876543210") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @security-camera.get_url, link = "http://www.google.com"^^tt:url , v_link := link => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && awakeTime = makeDate(1462320000000) && duration < 30min && rem < 30min && light >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @security-camera.get_url(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_link := link => notify;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && category =~ "12345") || (title = "9876543210" && user =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (category =~ "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
@weatherapi.monitor, (temperature < 73F && humidity < 42 && cloudiness < 42 && weather =~ "0123456789") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((temperature >= 22C && wind_speed = 25mph && humidity < 11) || (temperature <= 22C && humidity >= 7 && cloudiness = 11 && fog < 55 && weather = "ABCDEFGHIJ") || (temperature = 22C && wind_speed < 25mph && cloudiness >= 14 && fog >= 7 && status = enum(sleety)) || (temperature >= 73F && humidity = 55 && cloudiness <= 14 && fog > 14 && weather = "0123456788") || (wind_speed < 25mph && humidity < 11 && cloudiness > 7 && fog <= 42 && status = enum(sleety)) || (temperature < 73F && wind_speed <= 5kmph && cloudiness >= 42))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @linkedin.get_profile(), ((formatted_name =~ "0123456789" && industry =~ "0123456788" && specialties =~ "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @onedrive.create_file, true 
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "9876543210" || (title = "12345" && price >= 42 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @onedrive.create_file(file_name="0123456788", contents="12345") ;
}
====
* => * => @almond_couch_market.post, contact = "+15551234567"^^tt:phone_number 
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.google.com"^^tt:url || (title =~ "12345" && link = "http://www.google.com"^^tt:url && category = "23456789") || (user = "ABCDEFGHIJ" && category = "0123456789") || title =~ "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_couch_market.post(title=v_title, contact=v_title, price=7) ;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @scale.source(), ((value >= 82kg || value > 155lb)) , v_time := time, v_value := value => @dropbox.open(file_name="23456789"), ((file_name = "12345" && url =~ "ABCDEFGHIJ")) , v_url := url => @google_drive.create_new_drive_file(file_name=v_url) ;
}
====
@nasa.asteroid, (asteroid_id > 55 && name =~ "0123456788" && estimated_diameter_min <= 42cm && estimated_diameter_max < 1000m && closest_distance_to_earth < 1000m && orbiting_body = "0123456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 42 && name =~ "0123456788" && estimated_diameter_min >= 42cm && relative_velocity = 25mph && closest_distance_to_earth = 42cm && orbiting_body = "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
@heartrate-monitor.getheartrate, (place = $context.location.home && heartrate > 7) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (((time = makeDate(1487030400000) && heartrate > 7) || (place = $context.location.work && heartrate >= 11))) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
@omlet.newmessage, (message = "23456789" || (type = enum(text) && message =~ "0123456789")) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.newmessage(), (type = enum(picture)) , v_type := type, v_message := message => @weatherapi.moon(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((phase =~ "9876543210" || phase =~ "0123456789" || date = makeDate(1462320000000))) , v_date := date, v_phase := phase => notify;
}
====
now => * => @speaker.lower_volume, true 
;;
Main() {
    now => @nasa.apod(), (((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title = "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => @speaker.lower_volume() ;
}
====
now => @youtube.search_videos, ((channel_id =~ "0123456788" && title = "0123456788" && description = "9876543210" && count < 14 && video_url = "http://www.google.com"^^tt:url) || (query =~ "9876543210" && video_id = "0123456789" && channel_id = "9876543210" && count = 55) || (query =~ "9876543210" && channel_id =~ "12345" && description = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (query =~ "0123456788" && channel_id = "23456789" && channel_title = "ABCDEFGHIJ" && description =~ "9876543210" && count <= 7)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="23456789"), ((video_id = "0123456788" && description = "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@bodytrace.source, (weight = 155lb || (time = makeDate(1487030400000) && weight < 155lb)) , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => notify;
}
====
* => @builtin.get_time, (time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time => *
;;
Main() {
    @github.new_issue(repo_name="0123456789"), (((user = "bob"^^tt:username && number > 7 && title = "0123456788" && body =~ "0123456788" && time = makeDate(1487030400000)) || (repo_name =~ "9876543210" && body = "ABCDEFGHIJ" && time = makeDate(1487030400000)))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @builtin.get_time(), true , v_time := time => notify;
}
====
* => @parklon_heatpad.set_power, true 
;;
Main() {
    @youtube.source(), ((channel_title =~ "0123456788" || (channel_title = "0123456789" && video_id = "0123456788" && video_url = "http://www.abc.def"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @bing.image_search, ((title = "0123456789" && link = "http://www.abc.def"^^tt:url && width > 7 && height <= 7) || (width > 42 && height < 11) || (title =~ "12345" && link = "http://www.abc.def"^^tt:url && width >= 11 && height < 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="12345"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width < 14 && height >= 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @nasa.rover, (count <= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @holidays.uk_holiday_event(), (summary = "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => @nasa.rover(), (((count < 42 && camera_used =~ "0123456788") || (count < 14 && camera_used =~ "0123456788"))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@sleep-tracker.getsleep, (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && duration > 30min && rem > 30min && light = 2h && deep >= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((duration <= 30min && rem <= 30min && light > 30min && deep <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @builtin.get_date, true , v_date := date => *
;;
Main() {
    now => @builtin.get_date(), true , v_date := date => @security-camera.set_power(power=enum(on)) ;
}
====
@sleep-tracker.getsleep, ((duration < 30min && light < 30min && deep > 2h) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && awakeTime = makeDate(1487030400000) && duration = 30min && rem = 2h && light > 30min && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration < 2h && rem <= 30min && light > 2h && deep <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && asleepTime = makeDate(1462320000000) && rem <= 30min && deep >= 30min) || (awakeTime = makeDate(1487030400000) && duration <= 2h && rem <= 30min && light >= 30min && deep <= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
* => * => @tumblr-blog.post_picture, true 
;;
Main() {
    @gmail.receive_primary_email(), ((subject = "0123456789" && date = makeDate(1462320000000))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @tumblr-blog.post_picture(caption=v_snippet, picture_url=v_snippet) ;
}
====
* => @giphy.get_tag, (tag = "cat"^^tt:hashtag || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    @onedrive.file_created(), (file_name =~ "0123456788") , v_file_name := file_name => @giphy.get_tag(tag=v_file_name), ((tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, ((place = $context.location.current_location && heartrate < 42) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate > 7)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && heartrate <= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.search(query="ABCDEFGHIJ"), ((query = "12345" && count < 7 && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@sleep-tracker.getsleep, ((place = $context.location.work && awakeTime = makeDate(1462320000000) && duration < 30min && rem = 2h && deep = 30min) || (time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && deep < 2h) || (asleepTime = makeDate(1487030400000) && duration > 2h && rem > 30min && light = 30min && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @car.honk_horn, true 
;;
Main() {
    @sleep-tracker.getsleep(), ((awakeTime = makeDate(1487030400000) && rem <= 2h && light < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @car.honk_horn() ;
}
====
* => @thecatapi.get, true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @wall_street_journal.technology(), (title =~ "0123456789") , v_title := title, v_link := link => @thecatapi.get(), ((image_id = "0123456789" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @phone.call_emergency, true 
;;
Main() {
    now => @instagram.get_pictures(), ((count < 55 && caption =~ "ABCDEFGHIJ")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @phone.call_emergency() ;
}
====
* => @gmail.get_latest, (subject = "23456789" && label = "12345") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((label =~ "23456789" || subject = "0123456788" || (subject =~ "12345" && label =~ "23456789"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => * => @almond_dates.post, true 
;;
Main() {
    @wall_street_journal.technology(), (title = "ABCDEFGHIJ") , v_title := title, v_link := link => @imgflip.list(), (name =~ "9876543210") , v_name := name, v_picture_url := picture_url => @almond_dates.post(message=v_name, poster=v_name, phone=v_name, interest="ABCDEFGHIJ") ;
}
====
@nasa.asteroid, (asteroid_id < 42 && name =~ "0123456788" && estimated_diameter_min >= 42cm && estimated_diameter_max > 42cm && relative_velocity < 5kmph && closest_distance_to_earth < 1000m && orbiting_body =~ "ABCDEFGHIJ") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 14 && estimated_diameter_min <= 42cm && estimated_diameter_max < 42cm && relative_velocity <= 25mph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_couch_market.search(), ((title =~ "0123456789" && price < 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @wall_street_journal.get_opinions, (updated = makeDate(1487030400000) || (title = "9876543210" && updated = makeDate(1462320000000)) || title = "0123456788") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title = "9876543210") , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @parklon_heatpad.set_power, power = enum(off) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @builtin.get_time, (time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time => *
;;
Main() {
    @xkcd.new_whatif(), ((title = "0123456789" || (title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456788")) , v_title := title, v_link := link => @builtin.get_time(), (time = makeDate(1462320000000)) , v_time := time => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
@slack.receive, message =~ "9876543210" , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "12345" && message =~ "9876543210")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @ytranslate.translate(target_language=v_message, text=v_message), (((source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "it"^^tt:iso_lang_code("Italian")) || (source_language = "it"^^tt:iso_lang_code("Italian") && translated_text =~ "12345"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @builtin.get_random, true , v_number := number => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1487030400000) && duration < 2h && rem > 2h && light <= 30min && deep < 30min) || (place = $context.location.current_location && awakeTime = makeDate(1462320000000) && rem < 2h && light < 2h && deep > 30min) || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && asleepTime = makeDate(1462320000000) && duration <= 2h && rem < 2h && light >= 30min && deep < 30min) || (asleepTime = makeDate(1462320000000) && duration = 2h && rem <= 2h && light <= 2h) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && duration > 2h && rem >= 2h && deep >= 30min) || (awakeTime = makeDate(1487030400000) && rem < 30min && light <= 30min && deep < 30min) || (time = makeDate(1462320000000) && rem = 30min && light = 30min && deep > 2h) || (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration <= 2h && rem <= 2h && deep < 2h) || (awakeTime = makeDate(1462320000000) && duration >= 30min && rem >= 30min && light >= 30min && deep <= 30min) || (awakeTime = makeDate(1462320000000) && duration >= 2h && light < 2h && deep < 30min) || (rem = 2h && light = 30min) || (place = $context.location.home && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration <= 30min && rem > 2h && light <= 30min) || (time = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && duration <= 2h && rem <= 30min && light <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @builtin.get_random(), ((number <= 14 || number >= 42)) , v_number := number => notify;
}
====
now => @almond_couch_market.post, (price < 7 || title =~ "0123456789" || price < 14) 
;;
Main() {
    now => @almond_couch_market.post(title="0123456788", price=55, contact="+16501234567"^^tt:phone_number) ;
}
====
@gmail.receive_email, ((from_name =~ "0123456789" && date = makeDate(1487030400000)) || (from_name = "23456789" && snippet =~ "23456789") || date = makeDate(1487030400000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => *
;;
Main() {
    @gmail.receive_email(), (date = makeDate(1487030400000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @slack.updateChannelPurpose(channel=v_subject, purpose="12345") ;
}
====
* => @imgflip.list, name = "ABCDEFGHIJ" , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @github.new_milestone(repo_name="23456789"), ((user = "alice"^^tt:username || (repo_name =~ "12345" && user = "bob"^^tt:username))) , v_user := user, v_description := description, v_time := time => @imgflip.list(), (name =~ "23456789") , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1487030400000) && summary =~ "12345" && description =~ "23456789") || (date = makeDate(1487030400000) && summary = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @jawbone_up.getbody(), (((weight > 155lb && leanMass >= 11 && bodyFat < 42) || (place = $context.location.current_location && weight > 82kg && bodyFat >= 7 && bmi >= 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @holidays.next_uk_holiday_event(), ((date = makeDate(1487030400000) && description = "23456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @twitter.search_by_hashtag, ((count > 55 && in_reply_to = "bob"^^tt:username) || (count < 42 && contains(hashtags, "funny"^^tt:hashtag)) || (query_hashtag = "funny"^^tt:hashtag && text = "23456789" && from = "bob"^^tt:username) || (text =~ "0123456789" && contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, from="bob"^^tt:username, in_reply_to="bob"^^tt:username), ((count < 11 || (text =~ "0123456788" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA"), end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "ABCDEFGHIJ" && low_estimate > 11 && surge >= 7 && duration >= 2h && distance = 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @google_drive.create_new_drive_file(file_name=v_currency_code) ;
}
====
now => @giphy.get_tag, (tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), ((tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @lg_webos_tv.play_url(url="http://www.google.com"^^tt:url) ;
}
====
@heartrate-monitor.getheartrate, heartrate <= 14 , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate < 7 || heartrate < 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@fitness-tracker.getbody, (place = $context.location.current_location && weight = 155lb && leanMass < 14 && bmi >= 42) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight < 155lb && leanMass >= 7 && bodyFat <= 7 && bmi > 11) || (time = makeDate(1462320000000) && weight >= 155lb && leanMass = 7 && bodyFat <= 11))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@rss.new_post, (title =~ "12345" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), ((title =~ "0123456788" || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @omlet.send_picture(to=v_title, picture_url=v_title, caption=v_title) ;
}
====
* => @gmail.get_latest, ((subject =~ "23456789" && label =~ "12345") || (from = "bob@stanford.edu"^^tt:email_address && label = "12345")) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @twitter.my_tweet(), ((text = "23456789" && contains(hashtags, "cat"^^tt:hashtag) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @gmail.get_latest(), ((label =~ "ABCDEFGHIJ" || (from = "alice@gmail.com"^^tt:email_address && label =~ "ABCDEFGHIJ") || (subject = "0123456789" && label =~ "ABCDEFGHIJ"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @ytranslate.translate, (text = "12345" && translated_text = "23456789") , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="0123456789"), ((source_language = "zh"^^tt:iso_lang_code("Chinese") && target_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
now => * => @almond_dates.post, (interest = "12345" && message = "0123456789" && poster =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => @almond_dates.post(interest="9876543210", message="0123456789", poster="ABCDEFGHIJ", phone="+16501234567"^^tt:phone_number) ;
}
====
@omlet.incomingmessage, (message =~ "9876543210" || type = enum(text)) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (type = enum(picture)) , v_type := type, v_message := message => @weatherapi.current(location=$context.location.home), (((wind_speed = 5kmph && cloudiness = 11 && fog > 11 && weather =~ "0123456789") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && humidity < 11 && fog = 7))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
now => * => @tumblr-blog.post_text, ((title = "0123456788" && body =~ "9876543210") || body = "0123456789") 
;;
Main() {
    now => @builtin.get_random_between(low=14, high=55), true , v_random := random => @tumblr-blog.post_text(title="0123456789", body="23456789") ;
}
====
@gmail.receive_email, snippet =~ "ABCDEFGHIJ" , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_email(), ((from_address = "alice@gmail.com"^^tt:email_address && subject =~ "9876543210" && date = makeDate(1487030400000) && contains(labels, "0123456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
now => @weatherapi.sunrise, (sunrise = makeDate(1487030400000) && sunset = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.home), (location = $context.location.current_location) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@heartrate-monitor.getheartrate, (place = $context.location.work && heartrate <= 14) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.home && heartrate < 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @bing.web_search(query="12345"), ((query = "12345" && title =~ "9876543210")) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => @nasa.rover, ((count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "12345") || count = 14 || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @thermostat.humidity(), (value <= 14) , v_time := time, v_value := value => @nasa.rover(), (camera_used = "12345") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => @github.add_email, (email = "bob@stanford.edu"^^tt:email_address || email = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    @scale.source(), ((time = makeDate(1487030400000) && value < 82kg)) , v_time := time, v_value := value => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
now => * => @github.comment_issue, (issue_number > 42 || (issue_number < 11 && body =~ "12345")) 
;;
Main() {
    now => @linkedin.get_profile(), ((industry = "9876543210" && specialties =~ "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @github.comment_issue(repo_name=v_specialties, body=v_formatted_name, issue_number=55) ;
}
====
@instagram.new_picture, ((contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.current_location) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "ABCDEFGHIJ") || (contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => * => @gmail.send_picture, (subject =~ "9876543210" || subject = "23456789" || (subject =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @icalendar.list_events(), (((summary = "ABCDEFGHIJ" && sequence >= 55 && location = "0123456788") || (end_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && sequence > 7 && location = "0123456788") || (end_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && organizer = "ABCDEFGHIJ" && location =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @gmail.send_picture(to=v_description, subject=v_description, message=v_description, picture_url=v_description) ;
}
====
* => @security-camera.get_snapshot, true , v_picture_url := picture_url => notify
;;
Main() {
    @gmail.receive_important_email(), (((from_name = "12345" && subject = "23456789") || (from_name = "0123456788" && subject = "0123456788" && snippet =~ "ABCDEFGHIJ"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
* => @phone.get_gps, ((altitude <= 1000m && bearing <= 7) || speed > 25mph || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude <= 1000m && bearing <= 7 && speed >= 25mph) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && bearing < 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @almond_dates.monitor(), (((message =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (message =~ "0123456789" && poster = "ABCDEFGHIJ") || (interest =~ "0123456788" && message =~ "ABCDEFGHIJ" && poster = "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @phone.get_gps(), (bearing >= 14) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @bing.image_search, (height > 42 || (query =~ "23456789" && title =~ "23456789" && height < 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @wall_street_journal.technology(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @bing.image_search(query=v_title), (((link = "http://www.google.com"^^tt:url && height <= 55) || (query =~ "23456789" && width <= 14 && height > 7))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@omlet.incomingmessage, message =~ "23456789" , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
now => * => @github.add_email, (email = "alice@gmail.com"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @thecatapi.get(), (count > 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @github.add_email(email=v_image_id) ;
}
====
@holidays.us_holiday_event, ((date = makeDate(1462320000000) && summary =~ "0123456788" && description = "9876543210") || (summary =~ "9876543210" && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), ((description = "12345" || (date = makeDate(1487030400000) && description =~ "12345"))) , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.detail(id="0123456789"), (id = "ABCDEFGHIJ")  => notify;
}
====
* => @phone.get_gps, (location = $context.location.work && altitude = 42cm && speed >= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance >= 42cm && steps <= 11 && activeTime <= 2h && inactiveTime < 30min && calories = 500kcal) || (updateTime = makeDate(1462320000000) && day = "0123456789" && distance <= 42cm && steps = 7 && activeTime <= 30min && calories > 500kcal) || (updateTime = makeDate(1462320000000) && distance >= 1000m && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.get_gps(), ((altitude < 1000m && bearing <= 42)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @nasa.rover, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "9876543210") || (count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 11 && camera_used = "ABCDEFGHIJ") || (date_taken = makeDate(1487030400000) && count = 7 && camera_used = "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    now => @nasa.rover(), true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@almond_dates.monitor, poster =~ "9876543210" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, heartrate = 7 , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (time = makeDate(1462320000000)) , v_time := time, v_place := place, v_heartrate := heartrate => @bing.image_search(query="ABCDEFGHIJ"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height >= 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @giphy.get, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
@tumblr.new_photo, ((blog_name =~ "9876543210" && caption =~ "ABCDEFGHIJ" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || caption =~ "ABCDEFGHIJ") , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), (((blog_name =~ "0123456789" && date = makeDate(1487030400000) && caption =~ "0123456788" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (blog_name =~ "0123456788" && contains(tags, "funny"^^tt:hashtag)) || (blog_name =~ "9876543210" && caption = "9876543210") || (date = makeDate(1487030400000) && link = "http://www.abc.def"^^tt:url) || (blog_name = "23456789" && caption = "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (date = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@slack.receive, ((channel = "cat"^^tt:hashtag && message = "ABCDEFGHIJ") || (timestamp = "12345" && message =~ "12345") || from = "bob"^^tt:username) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), (((channel = "lol"^^tt:hashtag && timestamp = "0123456789" && from = "bob"^^tt:username) || (timestamp = "12345" && message =~ "0123456788"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
now => @phone.get_gps, (speed > 5kmph || (altitude < 1000m && bearing >= 14 && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((altitude > 42cm || (location = $context.location.current_location && bearing <= 7))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @facebook.post(status="23456789") ;
}
====
now => @gmail.get_latest, (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456789") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((label =~ "12345" || (subject =~ "ABCDEFGHIJ" && label =~ "9876543210") || subject =~ "23456789" || (subject = "ABCDEFGHIJ" && label = "23456789"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@phdcomics.new_post, (link = "http://www.google.com"^^tt:url || title = "12345" || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => notify
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, (updateTime = makeDate(1462320000000) && day = "0123456789" && steps >= 7 && activeTime > 30min && inactiveTime > 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && distance <= 1000m && steps >= 11 && activeTime < 2h && inactiveTime > 30min && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @bluetooth_speaker.lower_volume() ;
}
====
* => @twitter.search, ((count >= 42 && text = "0123456789" && contains(hashtags, "funny"^^tt:hashtag) && from = "bob"^^tt:username) || (query = "9876543210" && count < 55 && text =~ "0123456789") || (query =~ "23456789" && count <= 14 && in_reply_to = "alice"^^tt:username) || (count < 55 && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search(query="0123456788", count=42), ((query =~ "0123456789" && count <= 55 && text =~ "0123456789" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @almond_bike_market.post, (title =~ "9876543210" && price = 42 && info =~ "0123456788" && poster =~ "0123456789") 
;;
Main() {
    now => @thermostat.get_hvac_state(), ((mode = enum(heat) || state = enum(off))) , v_mode := mode, v_state := state => @almond_bike_market.post(title="23456789", price=42, info="12345", poster="12345") ;
}
====
* => @twitter.post_picture, true 
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight > 82kg)) , v_time := time, v_weight := weight => @twitter.post_picture(caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@phdcomics.new_post, true , v_title := title, v_link := link, v_picture_url := picture_url => @linkedin.share, (status =~ "0123456788" || status =~ "0123456788") 
;;
Main() {
    @phdcomics.new_post(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title =~ "ABCDEFGHIJ" || (title = "9876543210" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @linkedin.share(status="23456789") ;
}
====
@onedrive.file_modified, file_name = "0123456789" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "12345") , v_file_name := file_name => notify;
}
====
@omlet.incomingmessage, message = "ABCDEFGHIJ" , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), ((type = enum(text) && message =~ "23456789")) , v_type := type, v_message := message => @linkedin.get_profile(), ((industry = "0123456788" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @omlet.incomingmessage(), ((message = "9876543210" || message =~ "23456789" || message = "12345" || message = "0123456788" || message =~ "9876543210" || message = "9876543210")) , v_type := type, v_message := message => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
@rss.new_post, title = "0123456789" , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), ((title =~ "ABCDEFGHIJ" || title = "ABCDEFGHIJ")) , v_title := title, v_link := link => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="9876543210"), ((source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "zh"^^tt:iso_lang_code("Chinese") && text = "9876543210" && translated_text = "0123456788")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@phone.gps, (location = $context.location.current_location && altitude < 1000m && bearing < 55 && speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), ((location = $context.location.current_location && altitude < 1000m && bearing < 42 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.post_picture(caption="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @almond_couch_market.post, ((price > 42 && contact = "+16501234567"^^tt:phone_number) || price <= 14) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1462320000000) && summary =~ "9876543210" && description =~ "9876543210") || (summary = "0123456788" && description = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => @almond_couch_market.post(title="0123456788", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
@sportradar.nba_team, ((watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && watched_is_home = false && game_status = enum(scheduled) && home_points <= 11) || (watched_team_alias = "gsw"^^sportradar:nba_team("Golden State Warriors") && other_team_alias = "gsw"^^sportradar:nba_team("Golden State Warriors") && watched_is_home = true && away_name = "0123456789" && away_points >= 7 && home_points > 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), (((watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && home_name = "0123456789" && game_status = enum(halftime) && scheduled_time = makeDate(1487030400000) && home_points < 11) || (home_name = "9876543210" && away_points > 14))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @builtin.get_random_between(low=v_away_points, high=v_away_points), (((high >= 14 && random > 11) || (low < 14 && high > 11 && random > 7))) , v_random := random => notify;
}
====
now => * => @almond_dates.post, (interest =~ "9876543210" && poster =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => @almond_dates.post(interest="0123456789", message="9876543210", poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
@twitter.my_tweet, (text = "9876543210" && in_reply_to = "bob"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @thecatapi.get, ((image_id =~ "0123456789" && count = 11 && link = "http://www.abc.def"^^tt:url) || (image_id = "0123456789" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count <= 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @twitter.my_tweet(), ((text =~ "0123456789" || (text = "0123456788" && contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@jawbone_up.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @jawbone_up.getheartrate(), ((time = makeDate(1487030400000) && heartrate <= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
@security-camera.new_event, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (start_time = makeDate(1462320000000) && has_motion = false && has_person = false) || (has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1462320000000) && has_person = true) || (has_sound = false && has_motion = false && has_person = false) || start_time = makeDate(1462320000000))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @tumblr-blog.post_picture(picture_url=v_picture_url, caption="12345") ;
}
====
* => @youtube.list_subscriptions, (channel_id = "ABCDEFGHIJ" && title =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    @bodytrace.source(), (time = makeDate(1462320000000)) , v_time := time, v_weight := weight => @youtube.list_subscriptions(), ((channel_id = "ABCDEFGHIJ" && title =~ "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @fitbit.recordweight(weight=82kg) ;
}
====
@activity-tracker.getmove, (day = "23456789" && steps > 11 && activeTime >= 30min && calories <= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), (activeTime >= 30min) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.cool() ;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 22C && humidity > 55 && cloudiness < 11 && fog > 55 && weather = "23456789" && status = enum(sleety)) || (location = $context.location.work && humidity < 14 && cloudiness > 42 && fog < 14 && weather =~ "0123456789" && status = enum(drizzling)) || (cloudiness > 42 && fog < 7 && weather =~ "9876543210" && status = enum(sleety)) || (temperature <= 73F && wind_speed < 25mph && humidity < 14 && fog > 14 && weather =~ "9876543210") || (temperature >= 22C && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), ((wind_speed = 25mph && cloudiness <= 14 && fog >= 7 && weather =~ "23456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @security-camera.set_power(power=enum(on)) ;
}
====
@scale.source, time = makeDate(1462320000000) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), (((time = makeDate(1487030400000) && value >= 155lb) || (time = makeDate(1462320000000) && value > 82kg))) , v_time := time, v_value := value => @bluetooth_speaker.set_sink() ;
}
====
* => @linkedin.get_profile, (headline =~ "0123456789" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "9876543210" && headline =~ "0123456789" && specialties =~ "0123456788")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @phone.notify(title=v_specialties, message=v_specialties) ;
}
====
now => @omlet.send_to, ((to = "bob"^^tt:username && message =~ "12345") || message =~ "9876543210") 
;;
Main() {
    now => @omlet.send_to(to="bob"^^tt:username, message="0123456789") ;
}
====
now => * => @onedrive.create_file, ((file_name =~ "ABCDEFGHIJ" && contents = "ABCDEFGHIJ") || contents =~ "12345") 
;;
Main() {
    now => @ytranslate.detect_language(text="12345"), (text =~ "9876543210") , v_detected_language := detected_language => @onedrive.create_file(file_name="12345", contents="0123456789") ;
}
====
@almond_couch_market.monitor, ((title = "9876543210" && contact = "+15551234567"^^tt:phone_number) || price <= 14) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "0123456788" && price >= 42 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @almond_bike_market.detail(id="23456789"), ((id = "9876543210" || id =~ "0123456788" || id =~ "0123456789" || id =~ "ABCDEFGHIJ"))  => notify;
}
====
* => @bing.image_search, (query = "ABCDEFGHIJ" && title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && width > 11 && height <= 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), ((query = "12345" && title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && width < 14 && height <= 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@instagram.new_picture, ((filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (filter = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => *
;;
Main() {
    @instagram.new_picture(), (link = "http://www.google.com"^^tt:url) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @car.honk_horn() ;
}
====
now => * => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "0123456788" && count >= 14) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (count < 42 && link = "http://www.abc.def"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @twitter.sink(status=v_message) ;
}
====
* => @car.honk_horn, true 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && description =~ "0123456789" && sequence >= 42 && location =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @car.honk_horn() ;
}
====
@thermostat.humidity, (value >= 11 || (time = makeDate(1487030400000) && value < 42)) , v_time := time, v_value := value => *
;;
Main() {
    @thermostat.humidity(), true , v_time := time, v_value := value => @security-camera.set_power(power=enum(on)) ;
}
====
@security-camera.new_event, (has_sound = true || (start_time = makeDate(1487030400000) && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
@omlet.newmessage, (type = enum(picture) || message =~ "23456789" || message = "12345" || message =~ "9876543210") , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @omlet.send_picture(to=v_message, picture_url=v_message, caption=v_message) ;
}
====
@heartrate-monitor.getheartrate, (place = $context.location.home && heartrate > 7) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (place = $context.location.current_location) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
now => @almond_bike_market.search, (price < 14 || price > 42 || price > 7 || price < 7 || (info = "12345" && price <= 42) || price > 55 || price < 55)  => notify
;;
Main() {
    now => @almond_bike_market.search(price=11), ((price <= 7 || price < 14))  => notify;
}
====
@rss.new_post, ((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "9876543210" || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (title = "23456789") , v_title := title, v_link := link => @bing.image_search(query="ABCDEFGHIJ"), (((title = "23456789" && width <= 11) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => * => @omlet.send_to, (to = "bob"^^tt:username && message = "0123456789") 
;;
Main() {
    now => @twitter.search(query="ABCDEFGHIJ"), ((count >= 55 && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @omlet.send_to(to="bob"^^tt:username, message="ABCDEFGHIJ") ;
}
====
* => @dropbox.open, (file_name =~ "12345" && url =~ "23456789") , v_url := url => *
;;
Main() {
    now => @dropbox.open(file_name="ABCDEFGHIJ"), (file_name =~ "0123456789") , v_url := url => @builtin.hello() ;
}
====
@yahoofinance.stock_div, ((stock_id = "msft"^^tt:stock_id("Microsoft") && yield < 42 && div = 11 && pay_date = makeDate(1487030400000)) || (company_name = "ABCDEFGHIJ" && yield <= 11 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((yield > 7 && div >= 7 && pay_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @tumblr-blog.post_text(body=v_company_name, title="12345") ;
}
====
* => @builtin.get_time, (time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time => *
;;
Main() {
    now => @builtin.get_time(), ((time = makeDate(1462320000000) || time = makeDate(1462320000000))) , v_time := time => @slack.setPresence(presence=enum(auto)) ;
}
====
* => @security-camera.get_url, true , v_link := link => *
;;
Main() {
    @almond_bike_market.monitor(info="23456789"), (price < 55)  => @security-camera.get_url(), true , v_link := link => @light-bulb.color_loop() ;
}
====
@onedrive.file_modified, file_name = "23456789" , v_file_name := file_name => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => notify;
}
====
* => @dropbox.open, (file_name =~ "ABCDEFGHIJ" && url =~ "0123456788") , v_url := url => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((wind_speed < 5kmph && humidity > 14 && weather =~ "12345")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @dropbox.open(file_name=v_weather), (file_name = "0123456789") , v_url := url => notify;
}
====
@security-camera.new_event, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (start_time = makeDate(1462320000000) && has_motion = false && has_person = false) || (has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (has_sound = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "23456789" && updated = makeDate(1487030400000)) || (title =~ "9876543210" && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || (title = "9876543210" && link = "http://www.abc.def"^^tt:url) || (title = "12345" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @scale.source(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => @wall_street_journal.get_opinions(), ((updated = makeDate(1462320000000) || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @almond_couch_market.post, (title =~ "0123456788" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_bike_market.detail(id="9876543210"), true  => @almond_couch_market.post(title="12345", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@heartrate-monitor.getheartrate, (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || heartrate >= 42) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.work || (time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate = 14))) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_bike_market.search(info="9876543210", price=55), (price <= 42)  => notify;
}
====
now => @icalendar.list_events, (summary = "23456789" && location =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @parklon_heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @icalendar.list_events(), ((summary =~ "23456789" && description =~ "12345" && sequence < 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @gmail.get_latest, (subject = "9876543210" && label =~ "12345") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), ((subject =~ "23456789" && label =~ "12345")) , v_from := from, v_subject := subject, v_label := label => @omlet.send_to(to=v_label, message="12345") ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @youtube.list_subscriptions(), (description = "ABCDEFGHIJ") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @google_drive.create_new_drive_file(file_name=v_channel_id) ;
}
====
* => @bing.image_search, (title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && height >= 55) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @security-camera.set_power, power = enum(on) 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_sound = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @bing.image_search(query="0123456788"), ((query = "0123456789" && title = "0123456788" && width <= 11)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @security-camera.set_power(power=enum(on)) ;
}
====
now => * => @almond_couch_market.post, (price < 14 || (price > 11 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @bing.web_search(query="0123456788"), ((title =~ "0123456789" || query =~ "ABCDEFGHIJ")) , v_title := title, v_description := description, v_link := link => @almond_couch_market.post(contact=v_description, title="12345", price=11) ;
}
====
* => @fitbit.getbody, (bmi <= 55 && fat >= 14) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @instagram.new_picture(), ((caption =~ "0123456789" && link = "http://www.google.com"^^tt:url && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.getbody(), (weight < 82kg) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @uber.time_estimate, ((uber_type = "0123456788" && estimate >= 30min) || (uber_type = "9876543210" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((estimate >= 2h || (start = $context.location.work && uber_type = "12345" && estimate <= 2h))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @tumblr-blog.post_text, body = "ABCDEFGHIJ" 
;;
Main() {
    @phone.receive_sms(), ((body = "0123456788" || body = "23456789")) , v_from := from, v_body := body => @tumblr-blog.post_text(title="9876543210", body="12345") ;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary =~ "0123456788" && description = "ABCDEFGHIJ" && sequence >= 55 && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @almond_dates.post, message =~ "0123456788" 
;;
Main() {
    @gmail.receive_email(), (((from_name = "12345" && subject =~ "ABCDEFGHIJ" && date = makeDate(1487030400000) && contains(labels, "9876543210") && snippet =~ "23456789") || (subject =~ "23456789" && contains(labels, "ABCDEFGHIJ") && snippet =~ "23456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @almond_dates.post(interest=v_snippet, message=v_snippet, poster=v_subject, phone=v_subject) ;
}
====
@almond_dates.monitor, poster = "23456789" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "12345" && message =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @weatherapi.sunrise(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (date = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@onedrive.file_created, file_name =~ "0123456788" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
now => @uber.price_estimate, ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && end = $context.location.work && low_estimate < 14 && currency_code =~ "9876543210" && surge < 11 && distance < 42cm) || (uber_type = "9876543210" && low_estimate < 42 && currency_code = "9876543210" && surge > 11 && distance >= 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.current_location, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((low_estimate >= 42 && high_estimate < 55 && currency_code =~ "23456789" && duration = 30min && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @builtin.get_random_between, (low >= 55 && high <= 11 && random <= 14) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=55, high=14), (((low >= 7 && random <= 7) || (high >= 7 && random >= 42) || (low >= 55 && high < 14) || (low < 55 && high < 14))) , v_random := random => notify;
}
====
* => * => @heatpad.set_power, true 
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="0123456788", text_bottom="23456789"), ((text_top = "23456789" || (template = "9876543210" && text_bottom = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "ABCDEFGHIJ" && text_top = "23456789") || (text_bottom =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top =~ "9876543210" && text_bottom = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template =~ "ABCDEFGHIJ" && text_top =~ "23456789" && text_bottom = "23456789" && name = "ABCDEFGHIJ"))) , v_name := name, v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
@xkcd.new_comic, (number = 42 || number <= 7) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), (number >= 55) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @holidays.next_us_holiday_event(), (date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => * => @almond_couch_market.post, (title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.post(title="ABCDEFGHIJ", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
now => @thecatapi.get, ((image_id =~ "9876543210" && count = 11) || (image_id = "0123456788" && count >= 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id = "0123456788" && count > 55) || count < 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@activity-tracker.getmove, ((day =~ "23456789" && distance > 1000m && activeTime >= 30min) || (distance >= 42cm && steps > 42 && inactiveTime >= 30min) || (day =~ "23456789" && distance < 42cm && steps >= 11 && activeTime <= 2h) || (distance >= 42cm && steps = 7 && inactiveTime >= 30min) || (day = "0123456788" && distance > 42cm && steps > 42 && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "ABCDEFGHIJ" && distance >= 1000m && steps < 55 && activeTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.get_gps(), ((location = $context.location.work && altitude <= 1000m && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @dropbox.list_folder, (folder_name = "12345" && full_path = "9876543210") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @dropbox.list_folder(folder_name=v_file_name), ((file_size > 5KB || (is_folder = true && last_modified = makeDate(1462320000000) && full_path = "23456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @gmail.get_latest, label = "0123456789" , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @almond_bike_market.monitor(info="0123456789"), (price >= 7)  => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @bluetooth_speaker.set_volume(percent=55) ;
}
====
* => @thermostat.set_minmax_temperature, ((low <= 73F && high > 22C) || low <= 73F || low = 73F || low > 73F) 
;;
Main() {
    @slack.receive(), ((from = "alice"^^tt:username && message = "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @thermostat.set_minmax_temperature(low=73F, high=73F) ;
}
====
* => @github.add_email, email = "alice@gmail.com"^^tt:email_address 
;;
Main() {
    @rss.new_post(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @github.add_email(email=v_title) ;
}
====
* => @twitter.follow, true 
;;
Main() {
    @sleep-tracker.getsleep(), ((light > 30min && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @twitter.follow(user_name="alice"^^tt:username) ;
}
====
now => * => @github.comment_issue, (repo_name = "9876543210" && issue_number <= 14 && body = "12345") 
;;
Main() {
    now => @instagram.get_pictures(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @github.comment_issue(repo_name=v_filter, body=v_filter, issue_number=14) ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @wall_street_journal.us_business(), (title = "12345") , v_title := title, v_link := link => @security-camera.set_power(power=enum(on)) ;
}
====
* => @almond_bike_market.post, ((title = "9876543210" && price < 55 && info = "12345" && poster =~ "0123456788") || (title = "0123456788" && price = 7 && info =~ "0123456789") || (price <= 14 && info =~ "12345" && poster = "0123456789")) 
;;
Main() {
    @youtube.source(), ((channel_title =~ "23456789" && title =~ "23456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_bike_market.post(title=v_video_id, info=v_video_id, poster=v_channel_title, price=7) ;
}
====
@bodytrace.source, (weight < 155lb || weight >= 155lb) , v_time := time, v_weight := weight => @slack.updateChannelPurpose, true 
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @slack.updateChannelPurpose(channel="lol"^^tt:hashtag, purpose="12345") ;
}
====
@fitness-tracker.getbody, (bodyFat = 55 && bmi <= 14) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((leanMass >= 55 && bodyFat >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @tumblr-blog.post_picture, (caption = "12345" || (caption = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @thermostat.temperature(), (((time = makeDate(1462320000000) && value < 73F) || value = 73F || value <= 73F)) , v_time := time, v_value := value => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@washington_post.new_article, ((title = "23456789" && description =~ "23456789") || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && description =~ "9876543210") || (title = "0123456789" && description =~ "12345") || (title =~ "12345" && description =~ "23456789")) , v_title := title, v_link := link, v_description := description => * => notify
;;
Main() {
    @washington_post.new_article(section=enum(national)), ((title = "0123456789" && description =~ "ABCDEFGHIJ")) , v_title := title, v_link := link, v_description := description => @phone.get_gps(), ((altitude >= 1000m && bearing > 42)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @imgflip.list, name = "ABCDEFGHIJ" , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name = "12345" && bid_price >= 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @twitter.search, (query =~ "9876543210" && count <= 55 && text = "23456789") , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), ((watched_is_home = false && away_name =~ "12345" && scheduled_time = makeDate(1487030400000) && away_points < 11 && home_points = 55 && result = enum(win))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @twitter.search(query=v_away_name, from=v_home_name, in_reply_to=v_away_name), ((query = "0123456788" && count = 42)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@youtube.source, channel_title =~ "12345" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((title =~ "12345" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @xkcd.random_comic(), ((title = "0123456789" && alt_text =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @google_drive.create_new_drive_file, file_name =~ "12345" 
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="ABCDEFGHIJ"), ((tournament_full_name = "0123456789" && tournament_league_name =~ "0123456789" && home_name =~ "9876543210" && scheduled_time =~ "23456789" && away_points < 11 && home_points <= 7)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @google_drive.create_new_drive_file(file_name=v_away_alias) ;
}
====
@rss.new_post, title =~ "ABCDEFGHIJ" , v_title := title, v_link := link => * => @almond_bike_market.post, ((price <= 14 && info = "23456789" && poster =~ v_title) || price > 7) 
;;
Main() {
    @rss.new_post(), (((title = "12345" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ" || (title = "9876543210" && link = "http://www.abc.def"^^tt:url) || title =~ "12345")) , v_title := title, v_link := link => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @almond_bike_market.post(title="9876543210", price=7, info="12345", poster="0123456788") ;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && weight < 155lb && leanMass <= 14 && bodyFat <= 55 && bmi >= 42) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((leanMass >= 55 && bmi >= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @youtube.search_videos, ((video_id = "23456789" && channel_title = "ABCDEFGHIJ" && count < 11) || (video_id = "12345" && channel_title =~ "23456789" && title = "0123456789" && description = "ABCDEFGHIJ" && count < 55) || (query =~ "0123456789" && channel_id = "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && count >= 14 && video_url = "http://www.google.com"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @phone.gps(), ((altitude = 1000m && bearing < 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @youtube.search_videos(query="23456789"), ((video_id = "9876543210" && channel_id =~ "0123456788" && channel_title = "ABCDEFGHIJ" && description =~ "9876543210" && count >= 55)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
now => * => @tumblr-blog.post_text, (title = "12345" || title =~ "9876543210" || title = "0123456789" || body = "23456789") 
;;
Main() {
    now => @almond_bike_market.ask(id="0123456789", property="ABCDEFGHIJ"), ((id = "12345" && property = "23456789"))  => @tumblr-blog.post_text(title="0123456788", body="0123456789") ;
}
====
@almond_bike_market.monitor, price < 42  => * => notify
;;
Main() {
    @almond_bike_market.monitor(info="0123456788", price=11), ((price > 42 || price > 11 || info =~ "23456789"))  => notify;
}
====
now => * => @tumblr-blog.post_text, (title =~ "0123456789" || (title = "12345" && body =~ "9876543210") || title = "23456789") 
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text(title=v_message, body=v_poster) ;
}
====
@instagram.new_picture, (filter =~ "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((filter =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @heatpad.set_power(power=enum(off)) ;
}
====
* => @almond_dates.search, (interest =~ "23456789" && poster = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), (time = makeTime(20,30))  => @almond_dates.search(), (((message = "0123456789" && poster = "12345") || interest = "ABCDEFGHIJ" || message = "12345" || (interest = "0123456789" && message = "0123456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @github.add_email, (email = "alice@gmail.com"^^tt:email_address || email = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @github.add_email(email=v_summary) ;
}
====
now => @weatherapi.sunrise, (location = $context.location.home && sunrise = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => *
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.home), (date = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @lg_webos_tv.unmute() ;
}
====
* => @phone.send_sms, true 
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name =~ "23456789" && ask_price < 11 && bid_price < 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @phone.send_sms(to="+15551234567"^^tt:phone_number, body="ABCDEFGHIJ") ;
}
====
now => * => @onedrive.upload_picture, (file_name = "0123456788" || (file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || file_name =~ "0123456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @weatherapi.moon(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((date = makeDate(1462320000000) && phase = "ABCDEFGHIJ")) , v_date := date, v_phase := phase => @onedrive.upload_picture(file_name="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@jawbone_up.getbody, ((leanMass <= 11 && bmi < 55) || (weight >= 82kg && leanMass < 14 && bodyFat < 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => *
;;
Main() {
    @jawbone_up.getbody(), ((weight < 155lb && leanMass >= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
* => @instagram.get_pictures, ((count >= 7 && caption =~ "ABCDEFGHIJ" && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) || (caption =~ "0123456788" && filter = "12345" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @rss.new_post(), (title =~ "0123456788") , v_title := title, v_link := link => @instagram.get_pictures(), (((count = 42 && caption = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789") || (count < 42 && caption =~ "12345" && link = "http://www.abc.def"^^tt:url && filter = "9876543210" && location = $context.location.home))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
now => * => @github.comment_issue, (issue_number > 55 && body = "12345") 
;;
Main() {
    now => @github.comment_issue(repo_name="ABCDEFGHIJ", issue_number=14, body="ABCDEFGHIJ") ;
}
====
now => @almond_dates.search, message = "ABCDEFGHIJ" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((message =~ "23456789" || (interest =~ "23456789" && message =~ "23456789") || (interest = "12345" && poster =~ "9876543210") || (interest = "23456789" && message =~ "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && label = "12345") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @github.add_email(email=v_label) ;
}
====
@gmail.receive_important_email, ((from_name = "0123456788" && snippet = "ABCDEFGHIJ") || (from_address = "bob@stanford.edu"^^tt:email_address && subject =~ "12345" && snippet = "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_important_email(), ((from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1487030400000))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@gmail.receive_email, (from_name = "ABCDEFGHIJ" && from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1462320000000) && contains(labels, "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), (((from_name = "9876543210" && from_address = "alice@gmail.com"^^tt:email_address && contains(labels, "12345") && snippet = "0123456789") || (from_name =~ "ABCDEFGHIJ" && subject = "0123456788") || (subject =~ "12345" && contains(labels, "0123456788") && snippet = "23456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@tumblr.new_photo, (blog_name = "23456789" && caption =~ "0123456789" && link = "http://www.google.com"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="9876543210"), (blog_name =~ "ABCDEFGHIJ") , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @almond_couch_market.search(), (((title =~ "12345" && price <= 55 && contact = "+15551234567"^^tt:phone_number) || price >= 14)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @facebook.post, status = "ABCDEFGHIJ" 
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "23456789" && inactiveTime > 2h && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @facebook.post(status=v_day) ;
}
====
now => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    now => @instagram.get_pictures(count=14), ((count < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=155lb) ;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @jawbone_up.getsleep(), ((place = $context.location.home && asleepTime = makeDate(1487030400000) && duration < 2h && rem > 2h && light <= 2h && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @thermostat.get_temperature(), (value > 73F) , v_value := value => @dropbox.create_new_folder(folder_name="ABCDEFGHIJ") ;
}
====
now => @bing.image_search, (title = "9876543210" && width < 7 && height > 7) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="12345"), ((query = "0123456789" && title =~ "0123456789" && link = "http://www.google.com"^^tt:url && width < 11 && height > 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(local)), (((section = enum(politics) && title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && description = "9876543210") || title = "0123456788")) , v_title := title, v_link := link, v_description := description => @tumblr-blog.post_text(title="12345", body="23456789") ;
}
====
* => @phone.get_gps, ((location = $context.location.home && altitude <= 42cm && speed < 5kmph) || (altitude > 1000m && bearing > 11 && speed <= 5kmph) || altitude <= 1000m || (bearing >= 11 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @phone.gps(), ((altitude <= 1000m && bearing < 14 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.get_gps(), true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@nasa.asteroid, (asteroid_id < 42 && name =~ "0123456788" && estimated_diameter_min >= 42cm && estimated_diameter_max > 42cm && relative_velocity < 5kmph && closest_distance_to_earth < 1000m && orbiting_body =~ "ABCDEFGHIJ") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((estimated_diameter_max <= 1000m && dangerous = true && relative_velocity > 5kmph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @xkcd.random_comic(), (((number >= 42 && link = "http://www.abc.def"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@tumblr.new_text, (date = makeDate(1487030400000) && body = "12345") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="ABCDEFGHIJ"), ((blog_name = "ABCDEFGHIJ" && title =~ "0123456789" && contains(tags, "cat"^^tt:hashtag) && body = "ABCDEFGHIJ")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @bluetooth_speaker.play_music() ;
}
====
now => @ytranslate.detect_language, (detected_language = "zh"^^tt:iso_lang_code("Chinese") || (text = "0123456789" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || detected_language = "it"^^tt:iso_lang_code("Italian") || text = "0123456788" || (text = "0123456788" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || (text = "ABCDEFGHIJ" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || detected_language = "en"^^tt:iso_lang_code("English")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="ABCDEFGHIJ"), ((text = "0123456788" && detected_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_detected_language := detected_language => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), (((folder_name =~ "0123456788" && file_name = "23456789" && last_modified = makeDate(1462320000000) && file_size = 20MB && full_path =~ "9876543210") || (is_folder = true && file_size > 20MB && full_path = "9876543210"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1462320000000) && weight > 82kg) || weight <= 82kg || (time = makeDate(1487030400000) && weight < 155lb) || (time = makeDate(1462320000000) && weight = 82kg) || time = makeDate(1487030400000) || weight >= 82kg)) , v_time := time, v_weight := weight => @heatpad.set_power(power=enum(on)) ;
}
====
@security-camera.new_event, start_time = makeDate(1462320000000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((has_sound = false && has_motion = true && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
* => * => @github.comment_issue, true 
;;
Main() {
    @omlet.incomingmessage(), ((message =~ "ABCDEFGHIJ" || (type = enum(picture) && message =~ "12345"))) , v_type := type, v_message := message => @instagram.get_pictures(), ((caption =~ "23456789" && filter =~ "9876543210" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @github.comment_issue(repo_name=v_filter, body=v_filter, issue_number=7) ;
}
====
now => @security-camera.get_url, true , v_link := link => *
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @speaker.raise_volume() ;
}
====
* => @uber.price_estimate, (currency_code =~ "9876543210" && surge < 55 && duration <= 2h) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @xkcd.new_comic(), ((number <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @uber.price_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA"), end=$context.location.home), ((currency_code =~ "0123456789" && surge >= 42 && duration > 2h && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => @icalendar.list_events, ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && sequence >= 42 && organizer = "0123456788" && location =~ "12345") || (description =~ "0123456788" && sequence < 14 && organizer =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary = "0123456789" && description =~ "23456789" && organizer = "0123456789" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @phone.call(number=v_description) ;
}
====
@security-camera.new_event, start_time = makeDate(1487030400000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @bluetooth_speaker.set_volume(percent=42) ;
}
====
* => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), ((title =~ "23456789" && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @wall_street_journal.get_opinions(), ((title =~ "ABCDEFGHIJ" || (link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000)))) , v_title := title, v_link := link, v_updated := updated => @phone.set_ringer(mode=enum(normal)) ;
}
====
@almond_bike_market.monitor, (info = "0123456789" && price > 11)  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=11), ((price > 42 || (info = "0123456789" && price <= 7) || (info = "0123456788" && price > 42)))  => @almond_bike_market.search(), true  => notify;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (time = makeDate(1487030400000)) , v_time := time, v_weight := weight => @twitter.search(query="0123456789", count=55, from="alice"^^tt:username), ((query = "0123456789" && text = "23456789" && contains(hashtags, "funny"^^tt:hashtag) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @almond_dates.post(interest="9876543210", message="12345", poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
* => * => @builtin.debug_log, (message = "0123456789" || message =~ "12345") 
;;
Main() {
    now => @builtin.debug_log(message="12345") ;
}
====
@xkcd.new_comic, (title =~ "12345" || number > 7) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @xkcd.new_comic(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @almond_bike_market.ask, ((id = "23456789" && property = "23456789") || id =~ "23456789" || id =~ "9876543210")  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="ABCDEFGHIJ", property="23456789"), ((property =~ "23456789" || property =~ "0123456789" || property = "ABCDEFGHIJ"))  => notify;
}
====
@nasa.asteroid, (asteroid_id >= 42 && name = "ABCDEFGHIJ" && estimated_diameter_min > 1000m && estimated_diameter_max >= 1000m && dangerous = false && closest_distance_to_earth > 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((name =~ "0123456788" && estimated_diameter_max = 1000m && dangerous = true && closest_distance_to_earth > 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
now => @twitter.search, ((query = "9876543210" && count < 55) || (count <= 55 && text = "ABCDEFGHIJ" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query =~ "12345" && count <= 42 && text =~ "ABCDEFGHIJ" && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="ABCDEFGHIJ", count=42), (((query = "0123456789" && count = 14 && from = "bob"^^tt:username) || (count >= 14 && text = "9876543210" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @builtin.notify(message="0123456789") ;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude < 42cm && bearing >= 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), (((bearing < 11 && speed < 5kmph) || (altitude < 42cm && bearing > 42))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @speaker.lower_volume() ;
}
====
* => * => @dropbox.move, true 
;;
Main() {
    now => @dropbox.move(old_name="9876543210", new_name="ABCDEFGHIJ") ;
}
====
now => @xkcd.random_comic, (number >= 11 && alt_text = "23456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @bing.web_search, (title =~ "12345" && link = "http://www.google.com"^^tt:url) , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="ABCDEFGHIJ"), (((query =~ "9876543210" && title = "ABCDEFGHIJ" && description = "ABCDEFGHIJ") || (query = "9876543210" && description =~ "ABCDEFGHIJ") || link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => @twitter.unfollow, true 
;;
Main() {
    @xkcd.new_comic(), ((number = 11 && title =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @twitter.unfollow(user_name=v_title) ;
}
====
* => @lg_webos_tv.raise_volume, true 
;;
Main() {
    @thermostat.humidity(), (value <= 55) , v_time := time, v_value := value => @lg_webos_tv.raise_volume() ;
}
====
@phdcomics.new_post, (link = "http://www.google.com"^^tt:url || title = "0123456788") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @ytranslate.translate(target_language=v_title, text=v_title), ((text =~ "12345" && translated_text = "9876543210")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @dropbox.open, (file_name = "23456789" || file_name = "0123456788") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="0123456788"), ((file_name = "0123456789" && url =~ "0123456789")) , v_url := url => notify;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature > 73F && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 7 && fog < 7 && weather = "0123456788") || (temperature = 73F && wind_speed > 5kmph && fog > 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((location = $context.location.current_location && temperature <= 73F && wind_speed > 5kmph && cloudiness = 55 && fog > 7 && weather =~ "9876543210" && status = enum(windy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
now => @linkedin.get_profile, ((specialties = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "0123456788" && industry =~ "23456789" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (industry = "ABCDEFGHIJ" && specialties =~ "0123456788")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((headline = "0123456789" && industry = "0123456789" && contains(positions, "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @gmail.send_email, ((to = "alice@gmail.com"^^tt:email_address && message =~ "23456789") || subject =~ "0123456788") 
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), ((watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && game_inning =~ "23456789" && away_runs >= 14)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @gmail.send_email(subject=v_game_inning, message=v_home_name, to="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @icalendar.list_events, (end_date = makeDate(1487030400000) && sequence <= 11) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((summary =~ "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && sequence < 14 && organizer = "9876543210" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.lower_volume() ;
}
====
* => @phone.call, number = "+15551234567"^^tt:phone_number 
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1487030400000) && distance < 1000m && steps >= 55 && activeTime = 30min && inactiveTime = 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
* => @nasa.apod, true , v_title := title, v_description := description, v_picture_url := picture_url => @parklon_heatpad.set_power, true 
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_bike_market.post, ((title =~ "12345" && price > 55 && info =~ "ABCDEFGHIJ") || (price > 11 && info =~ "0123456789" && poster =~ "0123456788") || (title =~ "9876543210" && price <= 11 && info = "12345" && poster = "12345") || info =~ "ABCDEFGHIJ" || (price > 55 && info = "23456789")) 
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "9876543210" && price > 55) || price > 11)) , v_title := title, v_price := price, v_contact := contact => @almond_bike_market.post(info=v_title, title="0123456788", price=42, poster="9876543210") ;
}
====
@omlet.incomingmessage, (type = enum(text) && message = "0123456789") , v_feed := feed, v_type := type, v_message := message => * => *
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @gmail.send_email(to=v_message, subject=v_message, message=v_message) ;
}
====
* => @thermostat.get_hvac_state, ((mode = enum(off) && state = enum(heating)) || (mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => *
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(off) || state = enum(heating) || state = enum(cooling) || state = enum(off) || state = enum(cooling))) , v_mode := mode, v_state := state => @builtin.configure(device="twitter"^^tt:device("Twitter")) ;
}
====
@almond_dates.monitor, interest =~ "12345" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @imgflip.list, ((name = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @almond_dates.monitor(), (phone = "+16501234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @imgflip.list(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || name =~ "ABCDEFGHIJ")) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @nasa.apod, (title =~ "12345" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || description = "0123456789") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (title = "ABCDEFGHIJ") , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
now => @youtube.list_subscriptions, ((description =~ "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || channel_id = "12345" || (channel_id = "9876543210" && title =~ "0123456788" && description =~ "23456789") || thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.list_subscriptions(), (((channel_id =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || description =~ "0123456789")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @almond_couch_market.post(title=v_channel_id, price=55, contact="+15551234567"^^tt:phone_number) ;
}
====
@wall_street_journal.lifestyle, (link = "http://www.abc.def"^^tt:url || title =~ "23456789" || (title =~ "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @slack.updateChannelPurpose, (channel = "funny"^^tt:hashtag || channel = "cat"^^tt:hashtag) 
;;
Main() {
    @wall_street_journal.lifestyle(), true , v_title := title, v_link := link => @slack.updateChannelPurpose(channel=v_title, purpose="12345") ;
}
====
* => @thecatapi.get, ((image_id = "0123456789" && count = 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "23456789" && count = 55)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @activity-tracker.getmove(), (steps > 14) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thecatapi.get(), (((image_id =~ "0123456789" && count <= 55) || (count >= 55 && link = "http://www.abc.def"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @almond_dates.post, (message =~ "0123456789" && poster = "0123456788") 
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @almond_dates.post(interest="0123456788", message="9876543210", poster="9876543210", phone="+15551234567"^^tt:phone_number) ;
}
====
@yahoofinance.stock_div, ((div < 7 && ex_div_date = makeDate(1462320000000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && yield >= 14 && ex_div_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (yield < 11) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
@holidays.uk_holiday_event, (date = makeDate(1462320000000) || (summary = "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ") || (date = makeDate(1462320000000) && summary = "12345" && description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((summary = "23456789" && description =~ "9876543210")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @phone.send_sms, (body = "ABCDEFGHIJ" || to = "+16501234567"^^tt:phone_number) 
;;
Main() {
    @washington_post.new_blog_post(section=enum(powerpost)), ((link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ" || (section = enum(right_turn) && title = "23456789"))) , v_title := title, v_link := link => @phone.send_sms(to="+15551234567"^^tt:phone_number, body="0123456789") ;
}
====
@jawbone_up.getsleep, ((time = makeDate(1487030400000) && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && rem > 2h && light = 2h && deep > 30min) || (place = $context.location.home && rem > 2h && light > 30min && deep <= 30min) || (rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @jawbone_up.getsleep(), (((awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration > 30min && light <= 2h && deep >= 30min) || (time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration = 30min && rem >= 2h && light <= 30min) || (asleepTime = makeDate(1487030400000) && duration > 2h && light < 2h && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    @nasa.asteroid(), (((estimated_diameter_min <= 42cm && estimated_diameter_max <= 42cm && relative_velocity <= 25mph && closest_distance_to_earth >= 42cm) || (estimated_diameter_min = 1000m && closest_distance_to_earth > 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @holidays.next_us_holiday_event(), ((date = makeDate(1462320000000) || description =~ "9876543210" || (date = makeDate(1462320000000) && summary =~ "23456789"))) , v_date := date, v_summary := summary, v_description := description => @facebook.post(status="9876543210") ;
}
====
now => * => @bluetooth_speaker.lower_volume, true 
;;
Main() {
    now => @bluetooth_speaker.lower_volume() ;
}
====
@sportradar.ncaafb_team, (watched_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && watched_is_home = false && away_name =~ "0123456789" && home_name =~ "ABCDEFGHIJ" && away_points >= 42) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="sta"^^sportradar:ncaafb_team("Stanford Cardinals")), (((home_name =~ "9876543210" && scheduled_time = makeDate(1487030400000) && away_points >= 14 && home_points < 55) || (other_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && away_name = "9876543210" && away_points <= 7 && home_points > 42))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @imgflip.list(), (name = "9876543210") , v_name := name, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, ((day =~ "9876543210" && inactiveTime < 30min && calories >= 500kcal) || (day = "9876543210" && distance >= 42cm && steps <= 55 && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "ABCDEFGHIJ" && steps <= 7 && activeTime >= 30min && inactiveTime < 30min && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => * => @github.add_email, (email = "alice@gmail.com"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
* => @thecatapi.get, (image_id = "12345" || count > 55 || (image_id = "0123456788" && count = 14)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @thecatapi.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @slack.updateChannelTopic, true 
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => @slack.updateChannelTopic(channel="funny"^^tt:hashtag, topic="0123456789") ;
}
====
@phdcomics.new_post, (title =~ "0123456788" || (title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (title = "12345") , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @onedrive.rename_file, (new_name =~ "12345" || (old_name = "0123456789" && new_name = "9876543210")) 
;;
Main() {
    now => @onedrive.rename_file(old_name="12345", new_name="12345") ;
}
====
* => @dropbox.move, ((old_name =~ "23456789" && new_name = "9876543210") || new_name =~ "0123456788") 
;;
Main() {
    @gmail.receive_important_email(), (snippet =~ "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @dropbox.move(old_name=v_snippet, new_name=v_from_name) ;
}
====
* => @xkcd.get_comic, (title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "0123456788") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @jawbone_up.getbody(), (((time = makeDate(1462320000000) && leanMass <= 55 && bodyFat = 11 && bmi = 11) || (weight > 82kg && leanMass = 55 && bodyFat < 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @xkcd.get_comic(), ((alt_text = "12345" || (number >= 42 && alt_text =~ "12345") || (number > 14 && link = "http://www.google.com"^^tt:url && alt_text = "12345"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @dropbox.move, (new_name = "9876543210" || new_name =~ "0123456789") 
;;
Main() {
    now => @dropbox.move(old_name="0123456788", new_name="23456789") ;
}
====
@icalendar.event_begin, (summary =~ "0123456789" && description =~ "12345" && sequence <= 14) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1487030400000) && sequence <= 55)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@thermostat.temperature, (value >= 22C || value <= 22C) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), ((value <= 22C || value >= 73F)) , v_time := time, v_value := value => notify;
}
====
now => @twitter.search, (query =~ "0123456789" && count <= 14 && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="0123456789", count=11, from="bob"^^tt:username, in_reply_to="bob"^^tt:username), (((query = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag)) || (query =~ "9876543210" && in_reply_to = "bob"^^tt:username) || (count < 55 && contains(urls, "http://www.abc.def"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @almond_couch_market.search, ((title =~ "9876543210" && price = 7 && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && price = 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @gmail.receive_email(), (((from_name =~ "23456789" && date = makeDate(1487030400000) && snippet = "12345") || (from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1487030400000)) || (from_name =~ "0123456789" && snippet =~ "12345"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @almond_couch_market.search(), (((title =~ "9876543210" && price < 14 && contact = "+16501234567"^^tt:phone_number) || (price <= 55 && contact = "+16501234567"^^tt:phone_number) || price > 7)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @giphy.get_tag, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get_tag(tag="funny"^^tt:hashtag), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || tag = "cat"^^tt:hashtag || (tag = "funny"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_picture_url := picture_url => notify;
}
====
now => @omlet.send_to, (message = "12345" || message =~ "9876543210") 
;;
Main() {
    now => @omlet.send_to(to="alice"^^tt:username, message="0123456789") ;
}
====
now => @thermostat.get_hvac_state, mode = enum(heat_cool) , v_mode := mode, v_state := state => notify
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(cooling) || mode = enum(heat_cool))) , v_mode := mode, v_state := state => notify;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="bob"^^tt:username), (((count = 11 && text = "23456789" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username) || (count < 55 && text = "12345" && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @facebook.post(status="9876543210") ;
}
====
now => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count > 11 && text =~ "12345") , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag), ((count >= 42 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @twitter.post_picture(caption=v_text, picture_url=v_text) ;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && distance > 1000m && steps < 11 && activeTime <= 30min && inactiveTime > 2h) || (day =~ "ABCDEFGHIJ" && distance > 1000m && steps = 11 && activeTime = 30min && calories >= 500kcal) || (day =~ "0123456788" && distance < 1000m && steps < 14 && calories > 500kcal) || (day = "9876543210" && distance < 42cm && steps < 42 && activeTime > 30min && inactiveTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @security-camera.get_url, (link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url) , v_link := link => notify
;;
Main() {
    @activity-tracker.getmove(), ((distance <= 1000m && steps < 42 && activeTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @security-camera.get_url(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_link := link => notify;
}
====
@almond_couch_market.monitor, (price > 14 || contact = "+15551234567"^^tt:phone_number || (title =~ "12345" && contact = "+16501234567"^^tt:phone_number) || (price >= 7 && contact = "+15551234567"^^tt:phone_number) || price >= 7) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (price = 7) , v_title := title, v_price := price, v_contact := contact => @fitbit.getsteps(), (((date = makeDate(1487030400000) && steps <= 11) || steps >= 42 || steps < 42)) , v_date := date, v_steps := steps => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
@phone.gps, ((altitude <= 1000m && bearing < 42) || (altitude >= 42cm && bearing >= 14 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 42cm && bearing > 42 && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @ytranslate.detect_language, (text =~ "9876543210" && detected_language = "it"^^tt:iso_lang_code("Italian")) , v_detected_language := detected_language => notify
;;
Main() {
    @jawbone_up.getheartrate(), (((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (time = makeDate(1487030400000) && heartrate >= 11) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate >= 55) || time = makeDate(1462320000000))) , v_time := time, v_place := place, v_heartrate := heartrate => @ytranslate.detect_language(text="9876543210"), ((text =~ "0123456789" && detected_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_detected_language := detected_language => notify;
}
====
* => @thermostat.set_minmax_temperature, high > 22C 
;;
Main() {
    @phone.gps(), true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
* => @tumblr-blog.post_text, true 
;;
Main() {
    @phone.gps(), (speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @tumblr-blog.post_text(title="0123456788", body="23456789") ;
}
====
now => @nasa.rover, (count < 42 || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (date_taken = makeDate(1487030400000) && count < 11 && camera_used =~ "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), (count > 55) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @lg_webos_tv.unmute() ;
}
====
* => @ytranslate.detect_language, (text =~ "0123456788" && detected_language = "en"^^tt:iso_lang_code("English")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="9876543210"), ((text = "ABCDEFGHIJ" && detected_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_detected_language := detected_language => notify;
}
====
@nasa.asteroid, (name = "0123456789" && estimated_diameter_max < 42cm && relative_velocity >= 25mph && closest_distance_to_earth < 1000m && orbiting_body = "9876543210") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), (((asteroid_id = 11 && name =~ "0123456789" && estimated_diameter_min > 42cm && estimated_diameter_max < 1000m && relative_velocity >= 25mph && orbiting_body = "0123456788") || (name =~ "ABCDEFGHIJ" && estimated_diameter_min >= 1000m && estimated_diameter_max >= 42cm && relative_velocity = 5kmph && orbiting_body = "0123456788"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @lg_webos_tv.play_url(url=v_name) ;
}
====
@onedrive.file_modified, file_name = "23456789" , v_file_name := file_name => @dropbox.create_new_folder, true 
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "0123456789") , v_file_name := file_name => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
now => * => @lg_webos_tv.unmute, true 
;;
Main() {
    now => @thermostat.get_temperature(), (value > 22C) , v_value := value => @lg_webos_tv.unmute() ;
}
====
* => @instagram.get_pictures, ((count >= 7 && caption =~ "ABCDEFGHIJ" && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) || (caption =~ "0123456788" && filter = "12345" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(count=42), ((caption = "ABCDEFGHIJ" && filter = "9876543210" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @car.honk_horn() ;
}
====
* => @phone.send_sms, (body = "ABCDEFGHIJ" || to = "+16501234567"^^tt:phone_number) 
;;
Main() {
    @thermostat.humidity(), ((value <= 42 || value > 55)) , v_time := time, v_value := value => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="0123456788") ;
}
====
@security-camera.new_event, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || has_motion = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_motion = false || (start_time = makeDate(1487030400000) && has_motion = false))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @twitter.search(query="ABCDEFGHIJ", count=42, from="bob"^^tt:username), ((count > 11 && text =~ "0123456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @xkcd.random_comic, (number >= 42 && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 55 && title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @instagram.get_pictures, (count <= 55 && link = "http://www.google.com"^^tt:url && location = $context.location.home) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @instagram.get_pictures(), (((count > 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456789") || filter = "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@reddit_front_page.newpost, (title = "ABCDEFGHIJ" && user = "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (user = "ABCDEFGHIJ") , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
@activity-tracker.getmove, ((steps >= 55 && activeTime >= 30min && inactiveTime < 2h) || (day =~ "0123456789" && distance <= 42cm && inactiveTime < 2h && calories < 500kcal) || (updateTime = makeDate(1462320000000) && distance <= 1000m && steps > 11 && activeTime <= 2h && inactiveTime > 2h && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @activity-tracker.getmove(), ((distance > 1000m && steps < 7 && activeTime > 30min && inactiveTime < 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @almond_bike_market.search, (price < 42 || price >= 42 || price >= 7)  => notify
;;
Main() {
    @thermostat.humidity(), (value = 11) , v_time := time, v_value := value => @almond_bike_market.search(), ((info = "0123456788" || (info = "0123456788" && price = 14)))  => notify;
}
====
* => @thermostat.get_humidity, (value < 42 || value = 11 || value < 14 || value = 14) , v_value := value => notify
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @thermostat.get_humidity(), ((value >= 14 || value >= 11)) , v_value := value => notify;
}
====
@thermostat.humidity, value < 55 , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), true , v_time := time, v_value := value => @builtin.get_random_between(low=v_value, high=v_value), (((low <= 14 && high > 55 && random < 11) || random = 14 || (low < 42 && high <= 7 && random > 7))) , v_random := random => notify;
}
====
@yahoofinance.stock_quote, ((company_name =~ "9876543210" && ask_price >= 55) || (stock_id = "msft"^^tt:stock_id("Microsoft") && bid_price >= 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name =~ "0123456789" && ask_price < 11 && bid_price <= 7)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
* => @weatherapi.sunrise, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && sunrise = makeDate(1462320000000)) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), (location = $context.location.home) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && day = "9876543210" && distance > 1000m && steps <= 55 && activeTime <= 30min && calories <= 500kcal) || (steps < 42 && activeTime > 2h && inactiveTime <= 2h && calories > 500kcal) || (distance = 42cm && steps <= 7 && activeTime <= 30min && calories = 500kcal) || (distance < 1000m && steps > 55 && inactiveTime < 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1487030400000) && day = "9876543210" && distance <= 42cm && steps = 11 && activeTime = 2h && inactiveTime > 2h && calories <= 500kcal) || (updateTime = makeDate(1487030400000) && day = "ABCDEFGHIJ" && distance > 1000m && steps >= 14 && activeTime <= 2h))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @security-camera.get_url(), true , v_link := link => notify;
}
====
* => @almond_bike_market.post, ((title = "0123456789" && price < 7 && info =~ "0123456789") || (price >= 14 && poster =~ "12345")) 
;;
Main() {
    @github.new_issue_comment(repo_name="0123456789"), ((repo_name = "0123456789" && issue_number >= 55 && body =~ "23456789" && time = makeDate(1462320000000))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @almond_bike_market.post(title=v_body, price=v_issue_number, poster=v_body, info="0123456789") ;
}
====
now => @almond_bike_market.ask, property = "23456789"  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="0123456788", property="23456789"), ((id =~ "0123456789" || (id = "12345" && property =~ "9876543210")))  => notify;
}
====
now => @nasa.apod, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (description = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title = "9876543210" || (title =~ "0123456788" && description =~ "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    now => @nasa.apod(), (((description = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => @builtin.notify(message="ABCDEFGHIJ") ;
}
====
@almond_dates.monitor, (interest =~ "12345" || message = "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @car.honk_horn, true 
;;
Main() {
    @almond_dates.monitor(), (((message = "9876543210" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number) || (message = "0123456788" && poster =~ "0123456788") || (interest = "12345" && message = "9876543210" && poster = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && message =~ "23456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @car.honk_horn() ;
}
====
@phdcomics.new_post, title = "12345" , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @dropbox.get_space_usage(), (used > 20MB) , v_used := used, v_total := total => notify;
}
====
now => @youtube.list_subscriptions, (channel_id = "9876543210" && description =~ "9876543210") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.list_subscriptions(), ((channel_id = "0123456789" && description = "0123456788")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => * => @almond_couch_market.post, (title =~ "0123456788" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "9876543210")) , v_date := date, v_summary := summary, v_description := description => @almond_couch_market.post(title=v_summary, contact=v_summary, price=11) ;
}
====
* => @almond_couch_market.post, ((title = "0123456789" && price >= 11 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price = 55 && contact = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && filter = "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_couch_market.post(title=v_caption, contact=v_caption, price=14) ;
}
====
@almond_couch_market.monitor, ((price >= 11 && contact = "+15551234567"^^tt:phone_number) || contact = "+15551234567"^^tt:phone_number || (price < 55 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "9876543210" && price >= 42 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="12345"), (((source_language = "en"^^tt:iso_lang_code("English") && target_language = "it"^^tt:iso_lang_code("Italian") && translated_text = "12345") || (source_language = "en"^^tt:iso_lang_code("English") && target_language = "zh"^^tt:iso_lang_code("Chinese") && translated_text = "9876543210") || (target_language = "en"^^tt:iso_lang_code("English") && text = "0123456789" && translated_text =~ "0123456789") || text =~ "23456789")) , v_source_language := source_language, v_translated_text := translated_text => @fitbit.recordweight(weight=82kg) ;
}
====
* => @uber.time_estimate, true , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @almond_bike_market.monitor(price=14), ((info = "ABCDEFGHIJ" || (info =~ "9876543210" && price <= 14)))  => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (start = $context.location.current_location) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @fitbit.recordweight, weight >= 155lb 
;;
Main() {
    now => @thermostat.get_hvac_state(), ((mode = enum(heat_cool) || mode = enum(off))) , v_mode := mode, v_state := state => @fitbit.recordweight(weight=155lb) ;
}
====
@youtube.source, ((channel_id = "ABCDEFGHIJ" && title =~ "0123456789") || (channel_id =~ "12345" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_title =~ "0123456788" && title =~ "ABCDEFGHIJ" && video_id = "ABCDEFGHIJ" && video_url = "http://www.abc.def"^^tt:url) || (channel_id = "ABCDEFGHIJ" && channel_title = "0123456789" && title = "0123456788") || (channel_id = "23456789" && channel_title = "23456789" && title = "9876543210" && video_id =~ "0123456788"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@github.new_milestone, (time = makeDate(1487030400000) || (repo_name = "9876543210" && user = "bob"^^tt:username && description = "0123456788")) , v_user := user, v_description := description, v_time := time => * => notify
;;
Main() {
    @github.new_milestone(repo_name="ABCDEFGHIJ"), ((repo_name = "12345" && description = "23456789")) , v_user := user, v_description := description, v_time := time => @nasa.rover(), (((count < 42 && camera_used =~ "0123456788") || (date_taken = makeDate(1462320000000) && count > 14 && camera_used =~ "12345"))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@almond_dates.monitor, poster =~ "ABCDEFGHIJ" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "0123456788" && poster = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @thecatapi.get(), (((image_id =~ "0123456789" && count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @linkedin.share, status = "9876543210" 
;;
Main() {
    now => @linkedin.share(status="23456789") ;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @fitbit.recordweight(weight=82kg) ;
}
====
now => @almond_dates.search, ((interest = "0123456788" && message =~ "23456789") || (interest =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ") || (message = "0123456789" && poster = "ABCDEFGHIJ") || (message = "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((message = "9876543210" && poster = "12345" && phone = "+16501234567"^^tt:phone_number) || poster = "0123456788" || (interest =~ "12345" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "ABCDEFGHIJ" && poster =~ "12345" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @speaker.set_volume, percent <= 42 
;;
Main() {
    now => @linkedin.get_profile(), ((specialties = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @speaker.set_volume(percent=55) ;
}
====
now => @dropbox.list_folder, ((folder_name = "12345" && file_size = 20MB) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456789" && full_path =~ "9876543210") || (file_name =~ "ABCDEFGHIJ" && is_folder = true && file_size <= 20MB) || (folder_name =~ "23456789" && file_name = "12345") || (folder_name = "12345" && file_name = "23456789" && last_modified = makeDate(1462320000000) && file_size >= 5KB && full_path =~ "0123456789")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="23456789"), (((folder_name =~ "0123456788" && file_name = "ABCDEFGHIJ" && last_modified = makeDate(1462320000000) && file_size > 5KB) || (folder_name = "23456789" && file_name = "9876543210" && file_size > 5KB) || (folder_name =~ "0123456789" && file_name =~ "12345" && is_folder = true && full_path = "0123456788"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @onedrive.upload_picture(file_name="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @wall_street_journal.get_opinions, ((title =~ "0123456789" && updated = makeDate(1487030400000)) || link = "http://www.abc.def"^^tt:url || title =~ "12345" || title = "0123456788" || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title =~ "0123456789" && updated = makeDate(1487030400000)) || (title = "23456789" && updated = makeDate(1462320000000)) || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @thermostat.set_target_temperature, value >= 22C 
;;
Main() {
    now => @instagram.get_pictures(), ((count = 7 && caption = "23456789" && filter = "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @ytranslate.detect_language, text =~ "23456789" , v_detected_language := detected_language => *
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1462320000000) && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @ytranslate.detect_language(text=v_summary), true , v_detected_language := detected_language => @onedrive.rename_file(old_name="0123456789", new_name="ABCDEFGHIJ") ;
}
====
now => * => @facebook.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1487030400000) && description = "0123456788") || description = "9876543210" || (date = makeDate(1487030400000) && summary =~ "12345" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => @facebook.post_picture(picture_url=v_summary, caption=v_summary) ;
}
====
now => * => @almond_dates.post, (poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @nasa.apod(), (description = "12345") , v_title := title, v_description := description, v_picture_url := picture_url => @almond_dates.post(message=v_title, poster=v_title, phone=v_title, interest="12345") ;
}
====
@yahoofinance.stock_quote, ask_price > 55 , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), ((stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "9876543210")) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @builtin.sorry() ;
}
====
now => * => @twitter.sink, (status =~ "23456789" || status = "23456789") 
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((end = $context.location.work && surge >= 42 && duration > 30min && distance = 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @twitter.sink(status=v_uber_type) ;
}
====
now => @almond_couch_market.search, ((title = "0123456788" && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && contact = "+15551234567"^^tt:phone_number) || (title = "ABCDEFGHIJ" && price <= 42 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title = "ABCDEFGHIJ" && price < 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => notify;
}
====
* => * => @almond_couch_market.post, ((title =~ "ABCDEFGHIJ" && price > 14) || (price >= 42 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @bodytrace.source(), (weight <= 155lb) , v_time := time, v_weight := weight => @almond_couch_market.post(title="ABCDEFGHIJ", price=55, contact="+16501234567"^^tt:phone_number) ;
}
====
* => @linkedin.share, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @holidays.us_holiday_event(), (((date = makeDate(1487030400000) && description = "0123456789") || (date = makeDate(1462320000000) && summary =~ "12345"))) , v_date := date, v_summary := summary, v_description := description => @linkedin.share(status=v_summary) ;
}
====
now => @dropbox.list_folder, ((folder_name = "12345" && file_size = 20MB) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456789" && full_path =~ "9876543210") || (file_name =~ "ABCDEFGHIJ" && is_folder = true && file_size <= 20MB) || (folder_name =~ "23456789" && file_name = "12345") || (folder_name = "12345" && file_name = "23456789" && last_modified = makeDate(1462320000000) && file_size >= 5KB && full_path =~ "0123456789")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="23456789"), ((folder_name =~ "23456789" && file_size = 5KB && full_path =~ "12345")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @car.honk_horn() ;
}
====
* => @facebook.post_picture, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
* => * => @twitter.post_picture, true 
;;
Main() {
    @almond_dates.monitor(), ((interest = "0123456788" && poster = "9876543210" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @twitter.post_picture(caption=v_message, picture_url=v_message) ;
}
====
@jawbone_up.getmove, (day =~ "ABCDEFGHIJ" && distance <= 1000m && steps < 14 && activeTime < 2h && inactiveTime > 2h && calories <= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => @slack.setPresence, true 
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1487030400000) && day = "0123456789" && steps < 42 && activeTime <= 30min && inactiveTime >= 2h && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="0123456788"), ((translated_text = "ABCDEFGHIJ" || (source_language = "en"^^tt:iso_lang_code("English") && target_language = "en"^^tt:iso_lang_code("English") && text =~ "9876543210" && translated_text =~ "0123456789"))) , v_source_language := source_language, v_translated_text := translated_text => @slack.setPresence(presence=enum(auto)) ;
}
====
* => @bing.image_search, (link = "http://www.abc.def"^^tt:url || (query =~ "0123456789" && width <= 42) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height = 11) || (query =~ "0123456789" && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && height < 7) || (query = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width > 14 && height <= 55) || (title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && height < 7) || (query =~ "ABCDEFGHIJ" && title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && width > 11 && height < 14) || (query = "12345" && title =~ "23456789" && width < 7) || (link = "http://www.abc.def"^^tt:url && width <= 7 && height = 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @builtin.sorry, true 
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), ((title =~ "12345" && link = "http://www.abc.def"^^tt:url && width < 42 && height >= 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @builtin.sorry() ;
}
====
* => * => @omlet.send_picture, (to = "alice"^^tt:username && caption = "12345") 
;;
Main() {
    now => @dropbox.get_space_usage(), (total >= 20MB) , v_used := used, v_total := total => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
* => @instagram.get_pictures, (filter =~ "23456789" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @instagram.get_pictures(), (link = "http://www.abc.def"^^tt:url) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @thecatapi.get, image_id = "ABCDEFGHIJ" , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @thecatapi.get(), (((image_id =~ "ABCDEFGHIJ" && count > 55 && link = "http://www.google.com"^^tt:url) || (image_id = "ABCDEFGHIJ" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@fitness-tracker.getbody, ((leanMass <= 14 && bmi > 14) || (leanMass <= 11 && bodyFat > 42) || (weight >= 82kg && leanMass >= 14 && bodyFat <= 42) || (time = makeDate(1487030400000) && place = $context.location.home && weight > 82kg && bmi = 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && place = $context.location.work && weight >= 155lb && leanMass >= 14 && bmi >= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @linkedin.get_profile(), ((formatted_name =~ "0123456788" && specialties = "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @almond_bike_market.ask, id = "23456789"  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="12345", property="9876543210"), (property =~ "9876543210")  => notify;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 73F && wind_speed >= 25mph && humidity > 11 && fog > 11 && weather =~ "9876543210") || (temperature < 73F && cloudiness < 14 && fog <= 14 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature >= 73F && cloudiness < 14 && fog >= 14) || (temperature = 73F && wind_speed <= 5kmph && humidity > 11 && cloudiness <= 42 && fog < 14))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.get_latest(), (((subject = "ABCDEFGHIJ" && label =~ "9876543210") || subject = "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => * => @gmail.send_email, (subject = "0123456788" || (subject = "0123456788" && message =~ "0123456789") || (to = "alice@gmail.com"^^tt:email_address && subject = "9876543210")) 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="alice"^^tt:username), ((query_hashtag = "funny"^^tt:hashtag && count < 14 && contains(hashtags, "lol"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @gmail.send_email(to=v_text, subject=v_text, message="23456789") ;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
* => @almond_couch_market.search, ((title =~ "0123456789" && price < 7 && contact = "+16501234567"^^tt:phone_number) || title =~ "23456789" || (title = "9876543210" && price < 11)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @washington_post.new_article(section=enum(lifestyle)), ((title =~ "12345" || (section = enum(national) && description = "0123456788") || (section = enum(world) && title = "9876543210"))) , v_title := title, v_link := link, v_description := description => @almond_couch_market.search(), (price > 55) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @fitbit.recordweight, weight < 82kg 
;;
Main() {
    now => @dropbox.list_folder(folder_name="0123456788"), ((last_modified = makeDate(1487030400000) && file_size <= 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @fitbit.recordweight(weight=82kg) ;
}
====
now => @twitter.sink, (status = "12345" || status =~ "23456789") 
;;
Main() {
    now => @twitter.sink(status="23456789") ;
}
====
@thermostat.temperature, (value < 73F || value < 73F || (time = makeDate(1462320000000) && value > 22C) || value > 22C || value >= 73F) , v_time := time, v_value := value => notify
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1487030400000) || time = makeDate(1462320000000) || (time = makeDate(1487030400000) && value < 73F))) , v_time := time, v_value := value => notify;
}
====
* => @github.add_email, (email = "bob@stanford.edu"^^tt:email_address || email = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "23456789" || (interest = "12345" && poster = "ABCDEFGHIJ"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @github.add_email(email=v_message) ;
}
====
@scale.source, (value >= 155lb || (time = makeDate(1487030400000) && value < 155lb)) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="ABCDEFGHIJ"), ((source_language = "it"^^tt:iso_lang_code("Italian") && translated_text = "0123456789")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@rss.new_post, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @security-camera.get_url(), true , v_link := link => notify;
}
====
* => @twitter.search, (count < 7 && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="9876543210", from="alice"^^tt:username), ((query =~ "ABCDEFGHIJ" && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
@youtube.source, ((channel_id =~ "0123456789" && channel_title = "0123456788" && video_id =~ "0123456789") || (channel_id =~ "0123456789" && channel_title = "0123456789" && title = "0123456789") || video_id = "12345" || (channel_title =~ "0123456789" && title = "12345" && video_id =~ "9876543210") || title = "0123456788") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((title = "12345" && video_id = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@omlet.incomingmessage, (message = "9876543210" || message = "12345") , v_feed := feed, v_type := type, v_message := message => notify
;;
Main() {
    @omlet.incomingmessage(), (type = enum(picture)) , v_type := type, v_message := message => notify;
}
====
now => * => @almond_dates.post, ((interest =~ "23456789" && message = "0123456789") || (message = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (interest = "9876543210" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    now => @almond_dates.post(interest="ABCDEFGHIJ", message="23456789", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
@security-camera.new_event, (has_sound = true || (start_time = makeDate(1462320000000) && has_sound = false && has_person = true) || (has_motion = false && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (start_time = makeDate(1462320000000) && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_sound = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_sound = false && has_motion = true && has_person = true))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @github.comment_issue(repo_name="9876543210", issue_number=14, body="0123456788") ;
}
====
* => @imgflip.generate, ((template = "23456789" && text_top =~ "12345") || (template =~ "0123456789" && text_top = "0123456788" && name = "0123456788") || template = "23456789") , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @wall_street_journal.markets(), ((link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || title =~ "9876543210" || title =~ "0123456789")) , v_title := title, v_link := link => @imgflip.generate(template=v_title, text_top=v_title, text_bottom=v_title), (name =~ "12345") , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @tumblr-blog.post_text, (title =~ "23456789" && body =~ "0123456788") 
;;
Main() {
    @xkcd.new_comic(), (number >= 55) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text(title="12345", body="12345") ;
}
====
@nasa.asteroid, (name = "ABCDEFGHIJ" && estimated_diameter_max < 1000m && dangerous = true && relative_velocity <= 5kmph && closest_distance_to_earth < 42cm) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id = 55 && estimated_diameter_min <= 42cm && estimated_diameter_max <= 1000m && closest_distance_to_earth > 42cm && orbiting_body = "ABCDEFGHIJ") || (estimated_diameter_min = 1000m && relative_velocity <= 25mph))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => @almond_dates.post, ((interest =~ "9876543210" && message =~ "ABCDEFGHIJ") || (interest =~ "12345" && message = "9876543210" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="23456789", poster="0123456788", phone="+16501234567"^^tt:phone_number) ;
}
====
now => @almond_couch_market.search, ((price <= 11 && contact = "+16501234567"^^tt:phone_number) || price < 42 || title = "12345" || (title = "9876543210" && price < 7) || title =~ "23456789") , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (title =~ "0123456789") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (((title = "23456789" && price < 42 && contact = "+16501234567"^^tt:phone_number) || title = "12345")) , v_title := title, v_price := price, v_contact := contact => @twitter.follow(user_name="bob"^^tt:username) ;
}
====
* => * => @phone.send_sms, body = "ABCDEFGHIJ" 
;;
Main() {
    @gmail.receive_important_email(), (((from_name = "23456789" && from_address = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && snippet = "12345") || (from_address = "alice@gmail.com"^^tt:email_address && contains(labels, "9876543210")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @phone.send_sms(to="+15551234567"^^tt:phone_number, body="23456789") ;
}
====
* => @bing.web_search, description = "0123456789" , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @bing.web_search(query=v_title), (((query =~ "12345" && title = "9876543210") || (query = "23456789" && title =~ "23456789") || (title = "0123456788" && description =~ "9876543210" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_description := description, v_link := link => notify;
}
====
now => * => @omlet.send_to, (to = "alice"^^tt:username || (to = "bob"^^tt:username && message =~ "ABCDEFGHIJ")) 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @omlet.send_to(to="alice"^^tt:username, message="23456789") ;
}
====
* => @almond_dates.post, ((interest =~ "0123456788" && poster = "9876543210") || (interest =~ "0123456788" && poster = "23456789") || (interest = "23456789" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = $context.location.current_location && fog >= 7 && weather =~ "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_dates.post(interest=v_weather, message=v_weather, poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
@almond_couch_market.monitor, (title =~ "9876543210" && price = 14) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 55 || (title = "23456789" && price < 14))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @almond_couch_market.search(), ((title = "12345" || price < 14)) , v_title := title, v_price := price, v_contact := contact => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && leanMass > 14 && bodyFat < 14 && bmi > 11) || (place = $context.location.home && bmi < 11) || (place = $context.location.current_location && weight = 155lb && leanMass < 42 && bodyFat > 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => *
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.current_location && weight < 155lb && leanMass < 55 && bodyFat >= 42 && bmi > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @twitter.post_picture(caption="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || title = "12345" || title =~ "23456789" || title =~ "0123456789" || link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ" || title = "23456789") , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (title =~ "12345") , v_title := title, v_link := link => @facebook.post_picture(picture_url=v_title, caption=v_title) ;
}
====
@activity-tracker.getmove, (distance = 42cm && steps >= 14 && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((distance < 42cm && activeTime < 30min && inactiveTime >= 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@rss.new_post, true , v_title := title, v_link := link => @thecatapi.get, (count <= 14 || (count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @rss.new_post(), ((title = "0123456788" || (title =~ "12345" && link = "http://www.abc.def"^^tt:url) || title =~ "23456789")) , v_title := title, v_link := link => @thecatapi.get(), ((image_id =~ "ABCDEFGHIJ" && count = 11 && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @tumblr-blog.post_text, ((title = "0123456788" && body =~ "ABCDEFGHIJ") || (title =~ "12345" && body = "0123456789") || (title =~ "0123456789" && body = "0123456788")) 
;;
Main() {
    now => @thermostat.get_humidity(), true , v_value := value => @tumblr-blog.post_text(title="0123456789", body="0123456789") ;
}
====
* => * => @phone.notify, true 
;;
Main() {
    now => @xkcd.get_comic(number=14), (((number = 11 && title =~ "ABCDEFGHIJ") || (number <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @phone.notify(title=v_title, message=v_alt_text) ;
}
====
now => * => @gmail.send_email, (to = "alice@gmail.com"^^tt:email_address && message =~ "23456789") 
;;
Main() {
    now => @weatherapi.current(location=$context.location.work), ((location = $context.location.current_location && temperature = 22C && wind_speed >= 5kmph && humidity < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @gmail.send_email(subject=v_weather, message=v_weather, to="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @uber.price_estimate, (low_estimate >= 14 && surge > 11 && duration >= 30min) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.home, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((start = $context.location.home && uber_type = "9876543210" && low_estimate = 14 && high_estimate <= 42 && currency_code = "23456789" && surge < 7 && duration > 2h && distance <= 1000m) || (start = $context.location.current_location && end = $context.location.work && uber_type =~ "12345" && high_estimate >= 42 && currency_code = "0123456789" && distance >= 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@sleep-tracker.getsleep, (asleepTime = makeDate(1487030400000) && duration <= 2h && rem < 2h && light < 30min && deep < 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && duration >= 30min && rem >= 30min && light >= 2h && deep < 2h) || (awakeTime = makeDate(1462320000000) && duration < 2h && rem <= 2h && light < 2h && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    @rss.new_post(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => @wall_street_journal.get_opinions(), ((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @almond_couch_market.post, title = "0123456788" 
;;
Main() {
    now => @youtube.search_videos(query="0123456789"), (((query =~ "12345" && video_id =~ "9876543210" && channel_id = "12345" && title = "ABCDEFGHIJ") || (query =~ "ABCDEFGHIJ" && video_id =~ "0123456788" && channel_title = "12345" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count < 14))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @almond_couch_market.post(price=v_count, contact=v_video_id, title="9876543210") ;
}
====
now => * => @tumblr-blog.post_text, (body =~ "23456789" || title =~ "12345") 
;;
Main() {
    now => @almond_bike_market.search(), ((price > 11 || (info = "0123456788" && price < 14)))  => @tumblr-blog.post_text(title="9876543210", body="9876543210") ;
}
====
@reddit_front_page.newpost, (user =~ "12345" || (title =~ "ABCDEFGHIJ" && user = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (((user =~ "9876543210" && category = "12345") || (user =~ "12345" && category =~ "0123456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @linkedin.get_profile(), ((formatted_name = "23456789" && industry = "ABCDEFGHIJ" && specialties =~ "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@washington_post.new_blog_post, link = "http://www.abc.def"^^tt:url , v_title := title, v_link := link => * => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_partisan)), (title = "9876543210") , v_title := title, v_link := link => @builtin.get_date(), true , v_date := date => notify;
}
====
* => @speaker.set_volume, (percent > 42 || percent >= 7) 
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1462320000000) && weight > 155lb) || (time = makeDate(1487030400000) && weight > 155lb) || weight < 155lb || weight >= 155lb)) , v_time := time, v_weight := weight => @speaker.set_volume(percent=14) ;
}
====
@instagram.new_picture, ((caption =~ "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.home) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter =~ "9876543210" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "0123456788" && contains(hashtags, "funny"^^tt:hashtag)) || contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @gmail.get_latest(), (((subject = "9876543210" && label = "0123456789") || from = "bob@stanford.edu"^^tt:email_address)) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @almond_bike_market.post, info = "0123456788" 
;;
Main() {
    @heartrate-monitor.getheartrate(), (place = $context.location.current_location) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_bike_market.post(price=v_heartrate, title="12345", info="9876543210", poster="23456789") ;
}
====
@icalendar.event_begin, ((sequence >= 55 && organizer =~ "ABCDEFGHIJ") || (start_date = makeDate(1462320000000) && organizer =~ "12345" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1462320000000) && summary = "0123456788" && description =~ "23456789" && organizer = "0123456789" && location = "0123456789") || end_date = makeDate(1487030400000) || (start_date = makeDate(1487030400000) && sequence > 55))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @car.honk_horn() ;
}
====
@youtube.source, ((channel_id = "0123456789" && title = "0123456788" && video_id =~ "0123456788" && video_url = "http://www.abc.def"^^tt:url) || (channel_title = "0123456789" && title =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((video_id = "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @twitter.follow(user_name=v_channel_id) ;
}
====
now => @giphy.get, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @lg_webos_tv.raise_volume() ;
}
====
@youtube.source, channel_title =~ "12345" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((channel_id =~ "12345" && channel_title = "ABCDEFGHIJ" && title = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @google_drive.create_new_drive_file(file_name=v_channel_id) ;
}
====
@xkcd.new_comic, ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 14 && title = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @xkcd.new_comic(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @speaker.play_music() ;
}
====
* => @fitbit.recordweight, true 
;;
Main() {
    @almond_couch_market.monitor(), ((price < 7 || contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @fitbit.recordweight(weight=82kg) ;
}
====
@gmail.receive_email, ((from_name = "23456789" && subject = "0123456788" && date = makeDate(1487030400000) && contains(labels, "0123456789") && snippet =~ "0123456789") || (contains(labels, "9876543210") && snippet =~ "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), (((subject = "ABCDEFGHIJ" && date = makeDate(1487030400000) && snippet = "9876543210") || (from_name =~ "9876543210" && subject = "12345" && date = makeDate(1487030400000) && snippet = "ABCDEFGHIJ") || (from_address = "bob@stanford.edu"^^tt:email_address && subject = "23456789" && snippet = "ABCDEFGHIJ") || (subject =~ "ABCDEFGHIJ" && date = makeDate(1487030400000) && contains(labels, "9876543210") && snippet =~ "0123456789") || (from_name =~ "0123456789" && date = makeDate(1487030400000) && snippet =~ "ABCDEFGHIJ"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @bing.image_search(query=v_from_name), ((query =~ "0123456788" && title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && height > 11)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@wall_street_journal.us_business, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @almond_dates.post(interest=v_title, message=v_title, poster=v_title, phone=v_title) ;
}
====
* => @dropbox.move, new_name =~ "23456789" 
;;
Main() {
    now => @dropbox.move(old_name="12345", new_name="0123456788") ;
}
====
now => @almond_couch_market.search, (price > 7 || (title = "ABCDEFGHIJ" && price > 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price = 11 || (price <= 42 && contact = "+15551234567"^^tt:phone_number) || (price >= 42 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @tumblr-blog.post_picture(caption=v_title, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @bluetooth_speaker.raise_volume, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate >= 14 || (time = makeDate(1487030400000) && heartrate <= 11))) , v_time := time, v_place := place, v_heartrate := heartrate => @bluetooth_speaker.raise_volume() ;
}
====
now => @instagram.get_pictures, (count = 14 && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(), (((count < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || (count >= 11 && caption = "0123456789" && filter =~ "9876543210" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (filter = "12345" && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.current_location))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=155lb) ;
}
====
now => @bing.image_search, (title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width = 11 && height >= 7) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="23456789"), (((title =~ "23456789" && width <= 55 && height > 55) || (query =~ "ABCDEFGHIJ" && title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 14))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => @dropbox.list_folder, ((folder_name = "0123456789" && file_name = "23456789" && file_size >= 20MB && full_path =~ "0123456788") || file_name = "23456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), ((folder_name =~ "0123456788" && is_folder = false && last_modified = makeDate(1487030400000) && file_size <= 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@washington_post.new_article, (link = "http://www.abc.def"^^tt:url && description = "ABCDEFGHIJ") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(world)), ((section = enum(national) && title = "12345")) , v_title := title, v_link := link, v_description := description => notify;
}
====
now => @bing.image_search, ((title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height > 7) || (query =~ "12345" && title = "0123456789" && width <= 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @builtin.configure, true 
;;
Main() {
    now => @bing.image_search(query="9876543210"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && height > 11)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @builtin.configure(device=v_title) ;
}
====
* => @onedrive.rename_file, (new_name =~ "12345" || (old_name = "0123456789" && new_name = "9876543210")) 
;;
Main() {
    now => @onedrive.rename_file(old_name="9876543210", new_name="ABCDEFGHIJ") ;
}
====
* => @speaker.set_volume, (percent > 42 || percent >= 7) 
;;
Main() {
    @wall_street_journal.world_news(), ((title =~ "12345" || title =~ "9876543210" || title = "23456789")) , v_title := title, v_link := link => @speaker.set_volume(percent=55) ;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @almond_bike_market.search(), (price < 14)  => notify;
}
====
@sportradar.soccer_us_tourney, ((tournament_full_name =~ "ABCDEFGHIJ" && tournament_league_name =~ "12345" && home_name = "0123456788" && game_status = enum(halftime) && scheduled_time =~ "0123456788" && home_points <= 42) || (tournament_search_term =~ "ABCDEFGHIJ" && tournament_full_name =~ "ABCDEFGHIJ" && away_alias = "ABCDEFGHIJ" && home_alias = "12345" && away_name = "ABCDEFGHIJ" && game_status = enum(inprogress) && home_points < 55)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="ABCDEFGHIJ"), (((tournament_full_name =~ "0123456788" && away_alias = "9876543210" && home_alias =~ "12345" && away_name = "0123456789" && home_name = "23456789" && game_status = enum(closed) && away_points = 42 && home_points > 14) || (tournament_full_name =~ "ABCDEFGHIJ" && tournament_league_name =~ "9876543210" && away_alias =~ "9876543210" && home_alias =~ "0123456788" && away_name = "9876543210"))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify;
}
====
* => @parklon_heatpad.set_power, power = enum(off) 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 42 && name = "12345" && estimated_diameter_min >= 42cm && estimated_diameter_max <= 42cm && dangerous = true && relative_velocity < 25mph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @uber.price_estimate, ((uber_type =~ "0123456788" && low_estimate = 11 && high_estimate < 11 && currency_code = "23456789" && distance < 42cm) || (end = $context.location.work && high_estimate >= 14 && currency_code = "9876543210" && surge < 11 && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((company_name =~ "23456789" && div < 7 && pay_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((uber_type =~ "12345" && high_estimate >= 14 && currency_code = "9876543210" && surge <= 14 && distance >= 42cm) || (low_estimate >= 42 && high_estimate < 7 && surge > 42 && duration < 30min && distance < 42cm) || (high_estimate <= 55 && currency_code = "12345" && distance < 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => * => @lg_webos_tv.lower_volume, true 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.current_location, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((start = $context.location.current_location && low_estimate < 14 && high_estimate <= 11 && surge > 55 && duration <= 2h && distance >= 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @lg_webos_tv.lower_volume() ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @gmail.receive_important_email(), (((from_address = "bob@stanford.edu"^^tt:email_address && subject = "0123456788") || (from_name =~ "ABCDEFGHIJ" && from_address = "alice@gmail.com"^^tt:email_address && snippet = "0123456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @car.honk_horn() ;
}
====
* => @imgflip.list, name = "0123456789" , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), (name =~ "9876543210") , v_name := name, v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1487030400000) && heartrate <= 11) , v_time := time, v_place := place, v_heartrate := heartrate => * => @twitter.send_direct_message, (message =~ "ABCDEFGHIJ" || (to = "alice"^^tt:username && message =~ "0123456788") || (to = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (to = "alice"^^tt:username && message =~ "0123456788")) 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.current_location && heartrate >= 42)) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.send_direct_message(to="bob"^^tt:username, message="9876543210") ;
}
====
now => @almond_dates.search, ((interest = "9876543210" && message =~ "23456789" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) || message = "23456789" || (interest = "ABCDEFGHIJ" && poster = "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((interest = "ABCDEFGHIJ" && message =~ "0123456788") || (interest =~ "9876543210" && message = "23456789") || (interest = "23456789" && message =~ "23456789" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@almond_dates.monitor, ((interest = "12345" && message =~ "0123456789" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "9876543210" && poster = "0123456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "0123456789" && poster =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @xkcd.random_comic, (number >= 42 && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((title = "0123456789" && alt_text =~ "23456789")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "ABCDEFGHIJ" || file_name =~ "23456789" || file_name =~ "12345") 
;;
Main() {
    now => @ytranslate.detect_language(text="ABCDEFGHIJ"), ((detected_language = "zh"^^tt:iso_lang_code("Chinese") || (text = "12345" && detected_language = "it"^^tt:iso_lang_code("Italian")) || (text =~ "0123456788" && detected_language = "en"^^tt:iso_lang_code("English")))) , v_detected_language := detected_language => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
* => @google_drive.create_new_drive_file, (file_name = "9876543210" || file_name =~ "23456789" || file_name = "23456789" || file_name = "0123456789") 
;;
Main() {
    @tumblr.new_photo(blog_name="23456789"), ((caption =~ "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @google_drive.create_new_drive_file(file_name=v_caption) ;
}
====
* => @almond_couch_market.post, ((title =~ "23456789" && price > 7) || price < 42 || (title =~ "9876543210" && price <= 14)) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((stock_id = "goog"^^tt:stock_id("Google") && yield < 42) || (stock_id = "goog"^^tt:stock_id("Google") && yield <= 55 && div >= 14 && pay_date = makeDate(1487030400000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && div > 14) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "23456789" && yield <= 55 && div <= 55 && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_couch_market.post(price=v_yield, title="23456789", contact="+15551234567"^^tt:phone_number) ;
}
====
now => @imgflip.generate, (text_top =~ "23456789" && name = "0123456788") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="0123456788", text_bottom="9876543210"), (((text_bottom = "0123456789" && name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_bottom = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "23456789"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@nasa.asteroid, (asteroid_id <= 11 && name =~ "12345" && estimated_diameter_min = 1000m && dangerous = false && relative_velocity > 25mph && closest_distance_to_earth < 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 7 && estimated_diameter_min <= 1000m && estimated_diameter_max > 42cm && relative_velocity <= 25mph && closest_distance_to_earth < 1000m && orbiting_body = "23456789") || (asteroid_id < 11 && name = "ABCDEFGHIJ" && estimated_diameter_max < 42cm && orbiting_body = "12345") || (asteroid_id > 42 && estimated_diameter_min > 1000m && dangerous = false && closest_distance_to_earth > 1000m) || (asteroid_id >= 55 && estimated_diameter_max < 42cm && relative_velocity < 5kmph && closest_distance_to_earth >= 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_dates.search(), (((message = "0123456789" && poster = "23456789") || (interest = "0123456788" && message = "0123456789" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "0123456788" && message =~ "ABCDEFGHIJ" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@google_drive.new_drive_file, (file_name = "ABCDEFGHIJ" || file_name = "12345" || file_name =~ "0123456789" || file_name = "0123456788") , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @car.honk_horn() ;
}
====
* => @almond_dates.search, (interest =~ "23456789" && poster = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((interest = "ABCDEFGHIJ" && poster =~ "12345") || poster = "23456789" || (interest = "0123456788" && message =~ "12345" && poster =~ "ABCDEFGHIJ"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
* => @gmail.send_picture, ((message = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (subject =~ "23456789" && message = "ABCDEFGHIJ") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (subject =~ "23456789" && message =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@scale.source, time = makeDate(1462320000000) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((value >= 155lb || value >= 155lb)) , v_time := time, v_value := value => notify;
}
====
now => * => @twitter.sink, true 
;;
Main() {
    now => @almond_bike_market.ask(id="9876543210", property="23456789"), (id = "0123456788")  => @twitter.sink(status="23456789") ;
}
====
now => * => @omlet.send_to, message =~ "0123456788" 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=$context.location.work), (((start = $context.location.current_location && high_estimate >= 7 && surge >= 7) || (low_estimate > 42 && high_estimate >= 55 && currency_code = "0123456789" && surge <= 7 && distance >= 1000m) || (high_estimate >= 55 && duration >= 30min && distance > 1000m) || (start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type =~ "0123456789" && low_estimate <= 42 && high_estimate <= 14 && currency_code =~ "12345" && surge > 7 && duration >= 30min))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @omlet.send_to(to=v_uber_type, message="ABCDEFGHIJ") ;
}
====
@security-camera.new_event, start_time = makeDate(1487030400000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((has_sound = true && has_motion = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @builtin.cool() ;
}
====
@youtube.source, (channel_id =~ "0123456788" || (channel_id =~ "ABCDEFGHIJ" && channel_title = "23456789" && title =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "0123456788" && channel_title =~ "0123456788" && video_id =~ "0123456788" && video_url = "http://www.abc.def"^^tt:url) || (channel_title =~ "12345" && video_url = "http://www.google.com"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@instagram.new_picture, ((filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (filter = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => *
;;
Main() {
    @instagram.new_picture(), (((caption = "0123456789" && contains(hashtags, "cat"^^tt:hashtag)) || (caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_bike_market.ask(id="9876543210", property="0123456789"), (((id = "0123456788" && property = "9876543210") || (id = "0123456789" && property =~ "12345")))  => @speaker.set_volume(percent=11) ;
}
====
@instagram.new_picture, (link = "http://www.google.com"^^tt:url || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter =~ "ABCDEFGHIJ" && location = $context.location.home) || (caption =~ "0123456789" && link = "http://www.google.com"^^tt:url && filter =~ "12345"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_bike_market.ask(property=v_caption, id="0123456788"), true  => notify;
}
====
* => @security-camera.get_snapshot, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => *
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @builtin.configure(device="twitter"^^tt:device("Twitter")) ;
}
====
* => @security-camera.set_power, true 
;;
Main() {
    @slack.receive(), ((timestamp =~ "0123456788" && from = "alice"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @security-camera.set_power(power=enum(off)) ;
}
====
* => @almond_bike_market.post, (title =~ "9876543210" && price > 14) 
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate < 42) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_bike_market.post(title="ABCDEFGHIJ", price=7, info="9876543210", poster="23456789") ;
}
====
@holidays.us_holiday_event, ((date = makeDate(1462320000000) && summary =~ "0123456788" && description = "9876543210") || (summary =~ "9876543210" && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), (description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => @builtin.get_random_between, (high >= 14 && random > 55) , v_random := random => *
;;
Main() {
    now => @builtin.get_random_between(low=14, high=55), true , v_random := random => @builtin.discover() ;
}
====
* => * => @almond_bike_market.post, (title =~ "23456789" && price >= 14 && info =~ "12345" && poster = "ABCDEFGHIJ") 
;;
Main() {
    @security-camera.new_event(), ((has_motion = true && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_bike_market.post(title="ABCDEFGHIJ", price=11, info="ABCDEFGHIJ", poster="9876543210") ;
}
====
now => * => @almond_couch_market.post, (price <= 14 || (title =~ "ABCDEFGHIJ" && price >= 42)) 
;;
Main() {
    now => @almond_couch_market.post(title="23456789", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
now => * => @almond_couch_market.post, (title = "9876543210" && price <= 42) 
;;
Main() {
    now => @almond_bike_market.search(), (price >= 42)  => @almond_couch_market.post(title="0123456788", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
now => @thermostat.get_humidity, (value > 7 || value = 55) , v_value := value => *
;;
Main() {
    now => @thermostat.get_humidity(), (value >= 14) , v_value := value => @tumblr-blog.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @tumblr-blog.post_text, (title =~ "0123456789" || title = "23456789") 
;;
Main() {
    now => @tumblr-blog.post_text(title="12345", body="0123456788") ;
}
====
@weatherapi.monitor, (temperature < 73F && humidity <= 14 && cloudiness > 11 && status = enum(drizzling)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature <= 73F && wind_speed > 5kmph && humidity = 42 && cloudiness > 55 && fog <= 14 && weather = "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @thermostat.get_humidity(), (value >= 11) , v_value := value => notify;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.work), (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_date := date, v_phase := phase => @security-camera.set_power(power=enum(on)) ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @wall_street_journal.markets(), (title =~ "9876543210") , v_title := title, v_link := link => @builtin.get_random(), (number >= 7) , v_number := number => @car.honk_horn() ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1462320000000) && value <= 11)) , v_time := time, v_value := value => @thermostat.get_humidity(), true , v_value := value => @car.honk_horn() ;
}
====
@yahoofinance.stock_quote, ((company_name = "0123456789" && bid_price >= 42) || (company_name =~ "23456789" && ask_price < 7 && bid_price >= 14)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((ask_price > 42 && bid_price = 11)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
@thermostat.temperature, (time = makeDate(1462320000000) && value = 73F) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), ((value = 22C || (time = makeDate(1487030400000) && value < 73F) || value < 22C || value < 22C)) , v_time := time, v_value := value => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => notify;
}
====
* => @almond_couch_market.search, ((title =~ "23456789" && price <= 11 && contact = "+15551234567"^^tt:phone_number) || price >= 11) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @phone.gps(), ((altitude >= 1000m && bearing > 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_dates.monitor, (poster = "23456789" || message = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @almond_dates.monitor(), ((interest = "ABCDEFGHIJ" && message = "9876543210" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @github.add_email(email=v_poster) ;
}
====
now => @nasa.rover, true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @tumblr-blog.post_picture, caption =~ "23456789" 
;;
Main() {
    now => @nasa.rover(), (((count <= 55 && camera_used =~ "12345") || (date_taken = makeDate(1487030400000) && count > 11 && camera_used = "9876543210"))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @tumblr-blog.post_picture(picture_url=v_picture_url, caption="23456789") ;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on)) 
;;
Main() {
    @twitter.source(), (((text = "23456789" && contains(hashtags, "cat"^^tt:hashtag)) || (text = "0123456788" && __reserved = false))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @thermostat.set_target_temperature, (value <= 73F || value >= 22C || value <= 22C || value < 73F || value >= 73F || value <= 73F || value <= 73F) 
;;
Main() {
    @wall_street_journal.technology(), (title = "12345") , v_title := title, v_link := link => @thermostat.set_target_temperature(value=73F) ;
}
====
@icalendar.event_begin, summary =~ "ABCDEFGHIJ" , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1487030400000) && summary =~ "12345" && description =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @onedrive.delete_file(file_name=v_organizer) ;
}
====
@gmail.receive_important_email, (from_name =~ "0123456789" && subject =~ "12345" && contains(labels, "0123456788") && snippet =~ "0123456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_important_email(), (((from_address = "alice@gmail.com"^^tt:email_address && subject =~ "23456789" && date = makeDate(1462320000000) && contains(labels, "23456789")) || (from_name = "9876543210" && subject =~ "9876543210"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.get_random(), true , v_number := number => notify;
}
====
* => * => @twitter.post_picture, true 
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.current_location && duration <= 30min && rem < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @linkedin.get_profile(), ((formatted_name =~ "23456789" || (formatted_name =~ "23456789" && headline =~ "23456789" && industry =~ "12345" && specialties = "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @twitter.post_picture(caption=v_formatted_name, picture_url=v_industry) ;
}
====
now => @fitbit.getbody, fat < 42 , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((weight > 82kg && bmi < 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @phone.call_emergency, true 
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "0123456789") , v_file_name := file_name => @phone.call_emergency() ;
}
====
* => @icalendar.list_events, (end_date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ" && description = "0123456789" && sequence < 42 && organizer = "0123456788" && location = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && leanMass <= 42 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @icalendar.list_events(), (((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "0123456789" && sequence <= 55 && organizer =~ "9876543210" && location = "0123456789") || (summary =~ "0123456789" && sequence = 55 && location =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @thecatapi.get, (image_id =~ "23456789" && count >= 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.current_location && duration >= 30min && rem < 30min && deep < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @thecatapi.get(), (((image_id = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id = "23456789" && count >= 42))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@slack.receive, (channel = "funny"^^tt:hashtag || (channel = "funny"^^tt:hashtag && timestamp =~ "12345" && message = "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && from = "alice"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (((channel = "funny"^^tt:hashtag && timestamp =~ "23456789") || (timestamp =~ "23456789" && from = "alice"^^tt:username && message = "ABCDEFGHIJ"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
@almond_couch_market.monitor, contact = "+16501234567"^^tt:phone_number , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "9876543210" || (title = "0123456789" && price <= 55 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @uber.time_estimate, ((uber_type = "12345" && estimate <= 2h) || (start = $context.location.work && uber_type =~ "9876543210" && estimate = 30min) || (start = $context.location.work && uber_type =~ "0123456788" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @wall_street_journal.lifestyle(), (title =~ "0123456788") , v_title := title, v_link := link => @uber.time_estimate(start=$context.location.home), ((uber_type = "0123456788" && estimate > 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || title =~ "0123456788") 
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), ((game_status = enum(closed) && away_points <= 11 && home_points > 55)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @tumblr-blog.post_text(body=v_away_name, title="12345") ;
}
====
* => @almond_bike_market.post, (title =~ "9876543210" && poster =~ "0123456789") 
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="sta"^^sportradar:ncaafb_team("Stanford Cardinals")), (((watched_team_abbr = "sta"^^sportradar:ncaafb_team("Stanford Cardinals") && scheduled_time = makeDate(1487030400000) && away_points = 55 && home_points > 14) || (game_status = enum(halftime) && result = enum(unclosed)) || (other_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && watched_is_home = false && away_name = "9876543210" && home_name = "12345" && game_status = enum(halftime) && home_points < 7))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @almond_bike_market.post(title="ABCDEFGHIJ", price=42, info="12345", poster="23456789") ;
}
====
@fitness-tracker.getbody, ((weight < 155lb && bodyFat = 55 && bmi <= 55) || (place = $context.location.current_location && weight = 155lb && leanMass >= 55 && bodyFat = 11 && bmi >= 14) || (weight = 82kg && bodyFat <= 14 && bmi >= 14) || (weight < 155lb && bmi > 11) || (time = makeDate(1487030400000) && weight > 155lb && leanMass >= 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @dropbox.create_new_folder, folder_name =~ "0123456789" 
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && bodyFat >= 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @dropbox.create_new_folder(folder_name="23456789") ;
}
====
now => @lg_webos_tv.mute, true 
;;
Main() {
    now => @lg_webos_tv.mute() ;
}
====
* => @google_drive.create_new_drive_file, file_name = "ABCDEFGHIJ" 
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && place = $context.location.current_location && duration <= 2h && light < 2h && deep <= 30min) || (duration <= 2h && deep <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@bodytrace.source, weight <= 82kg , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((weight >= 155lb || weight < 82kg)) , v_time := time, v_weight := weight => notify;
}
====
* => @instagram.get_pictures, (count > 55 && caption = "23456789" && link = "http://www.abc.def"^^tt:url && filter = "ABCDEFGHIJ") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @phdcomics.new_post(), (title = "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @instagram.get_pictures(count=42), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "ABCDEFGHIJ" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @youtube.channels_by_category, ((category_id = enum(lifestyle) && title =~ "0123456789") || (description =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_sound = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @youtube.channels_by_category(category_id=enum(from_tv)), ((channel_id =~ "ABCDEFGHIJ" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @almond_bike_market.ask, id = "23456789"  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="9876543210", property="ABCDEFGHIJ"), true  => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="12345"), (((source_language = "zh"^^tt:iso_lang_code("Chinese") && text = "ABCDEFGHIJ" && translated_text =~ "0123456789") || source_language = "en"^^tt:iso_lang_code("English"))) , v_source_language := source_language, v_translated_text := translated_text => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @wall_street_journal.opinions(), ((title =~ "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @car.honk_horn() ;
}
====
* => @imgflip.generate, (template = "ABCDEFGHIJ" && name =~ "0123456789") , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="ABCDEFGHIJ", text_bottom="9876543210"), ((text_bottom = "23456789" && name =~ "9876543210")) , v_name := name, v_picture_url := picture_url => @light-bulb.color_loop() ;
}
====
@google_drive.new_drive_file, file_name =~ "23456789" , v_file_name := file_name => @speaker.play_music, true 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "12345" || file_name =~ "0123456789")) , v_file_name := file_name => @speaker.play_music() ;
}
====
@rss.new_post, (title = "23456789" || (title = "23456789" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), ((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @lg_webos_tv.play_url(url=v_title) ;
}
====
@yahoofinance.stock_div, (company_name =~ "0123456789" && yield >= 55) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((company_name =~ "9876543210" && div < 14 && ex_div_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @almond_couch_market.search, price <= 11 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (price > 7) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "ABCDEFGHIJ" && message = "0123456789" && poster = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @car.honk_horn() ;
}
====
@weatherapi.monitor, ((temperature = 22C && wind_speed = 25mph && humidity < 14 && cloudiness > 55 && fog = 55 && weather =~ "ABCDEFGHIJ") || (temperature > 22C && fog > 55) || (wind_speed < 25mph && humidity < 7 && cloudiness > 11 && fog < 42 && status = enum(sleety))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 73F && wind_speed < 5kmph && status = enum(raining))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @xkcd.get_comic(number=v_cloudiness), ((number > 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @almond_couch_market.post, ((title = "0123456789" && price >= 11 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price = 55 && contact = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "9876543210" && price < 55 && contact = "+15551234567"^^tt:phone_number) || (title = "9876543210" && price <= 11))) , v_title := title, v_price := price, v_contact := contact => @almond_couch_market.post(title="23456789", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
now => * => @linkedin.share, (status =~ "9876543210" || status = "12345" || status =~ "23456789") 
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), ((width > 11 && height <= 11)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @linkedin.share(status=v_title) ;
}
====
now => * => @gmail.send_email, ((to = "alice@gmail.com"^^tt:email_address && message = "0123456788") || subject = "23456789" || subject =~ "0123456788") 
;;
Main() {
    now => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="0123456788", message="0123456788") ;
}
====
now => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @dropbox.get_space_usage(), (used <= 20MB) , v_used := used, v_total := total => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
* => @bing.image_search, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height <= 55) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="23456789"), ((height >= 55 || (query =~ "0123456789" && title =~ "23456789" && width > 11 && height < 55) || height > 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => * => @facebook.post, (status =~ "12345" || status = "0123456789" || status = "0123456788" || status = "0123456789" || status =~ "12345") 
;;
Main() {
    @rss.new_post(), (((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ" || (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @imgflip.list(), (name =~ "9876543210") , v_name := name, v_picture_url := picture_url => @facebook.post(status=v_name) ;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @phone.send_sms(to=v_file_name, body=v_file_name) ;
}
====
* => @builtin.notify, message = "9876543210" 
;;
Main() {
    @gmail.receive_important_email(), (((from_name =~ "23456789" && date = makeDate(1487030400000) && contains(labels, "0123456789")) || (from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1462320000000) && contains(labels, "12345") && snippet =~ "0123456789") || contains(labels, "12345"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.notify(message=v_subject) ;
}
====
now => * => @almond_dates.post, (poster =~ "0123456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @youtube.search_videos(query="0123456789"), ((channel_id = "23456789" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @almond_dates.post(interest=v_video_id, message=v_video_id, poster=v_video_id, phone=v_title) ;
}
====
now => @xkcd.random_comic, (number < 42 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number = 14 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @thecatapi.get, count > 14 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @thecatapi.get(), ((image_id =~ "9876543210" && count < 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @icalendar.event_begin(), (location =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file(file_name=v_organizer) ;
}
====
* => @gmail.send_email, subject = "12345" 
;;
Main() {
    @phone.gps(), ((altitude >= 1000m && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="23456789") ;
}
====
@almond_dates.monitor, poster = "23456789" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (((interest = "ABCDEFGHIJ" && poster = "0123456789") || (interest =~ "12345" && message = "23456789" && poster = "ABCDEFGHIJ"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @dropbox.move, ((old_name =~ "12345" && new_name =~ "9876543210") || old_name = "0123456789") 
;;
Main() {
    now => @nasa.apod(), (description = "12345") , v_title := title, v_description := description, v_picture_url := picture_url => @dropbox.move(old_name=v_title, new_name=v_title) ;
}
====
now => @youtube.channels_by_category, ((channel_id =~ "9876543210" && description =~ "12345") || (category_id = enum(news_and_politics) && channel_id =~ "23456789" && title =~ "23456789")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(causes)), ((category_id = enum(film_and_entertainment) && description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @imgflip.list(), (name = "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => @security-camera.set_power(power=enum(on)) ;
}
====
* => @nasa.rover, (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    @sportradar.soccer_us_team(watched_team_alias="tor"^^sportradar:us_soccer_team("Toronto FC")), (((other_team_alias = "tor"^^sportradar:us_soccer_team("Toronto FC") && watched_is_home = false && away_name = "9876543210" && home_name =~ "9876543210" && game_status = enum(scheduled) && away_points > 42 && home_points < 42) || (watched_is_home = false && away_name = "ABCDEFGHIJ" && away_points < 14))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @nasa.rover(), ((date_taken = makeDate(1487030400000) && count >= 55)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @lg_webos_tv.mute() ;
}
====
* => @almond_dates.search, (interest = "ABCDEFGHIJ" || poster = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "0123456789" || file_name =~ "23456789")) , v_file_name := file_name => @almond_dates.search(), ((poster =~ "23456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @instagram.get_pictures, (count <= 55 && caption =~ "ABCDEFGHIJ") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), ((count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @almond_dates.post, ((message = "9876543210" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number) || (poster = "23456789" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @xkcd.new_whatif(), ((title =~ "0123456788" || (title = "12345" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @almond_dates.post(interest="0123456788", message="ABCDEFGHIJ", poster="12345", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @thecatapi.get, image_id = "ABCDEFGHIJ" , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval < 30min)  => @thecatapi.get(), (link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@rss.new_post, (title = "9876543210" || (title = "0123456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (((title =~ "23456789" && link = "http://www.google.com"^^tt:url) || (title = "9876543210" && link = "http://www.google.com"^^tt:url) || title = "0123456788" || (title =~ "23456789" && link = "http://www.google.com"^^tt:url) || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "23456789")) , v_title := title, v_link := link => @ytranslate.detect_language(text="23456789"), true , v_detected_language := detected_language => notify;
}
====
now => @linkedin.get_profile, (formatted_name =~ "9876543210" && headline =~ "ABCDEFGHIJ" && specialties = "0123456788") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), (industry =~ "12345") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => * => @speaker.play_music, true 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @speaker.play_music() ;
}
====
now => * => @parklon_heatpad.set_power, power = enum(off) 
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "9876543210" && count < 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @light-bulb.set_power, true 
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1487030400000) && organizer =~ "9876543210" && location =~ "0123456789") || (sequence < 55 && organizer =~ "9876543210") || (summary =~ "23456789" && sequence = 55 && organizer =~ "ABCDEFGHIJ" && location = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @light-bulb.set_power(power=enum(off)) ;
}
====
@phdcomics.new_post, (title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "9876543210" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @uber.price_estimate(start=$context.location.work, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && low_estimate = 55 && high_estimate <= 42 && currency_code = "9876543210" && duration >= 2h) || (low_estimate <= 55 && distance < 1000m) || (end = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && low_estimate < 14 && currency_code =~ "12345" && surge > 7))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @tumblr-blog.post_text, body = "23456789" 
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), (repo_name =~ "ABCDEFGHIJ") , v_user := user, v_description := description, v_time := time => @tumblr-blog.post_text(title=v_description, body=v_description) ;
}
====
now => * => @light-bulb.color_loop, true 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @light-bulb.color_loop() ;
}
====
* => @twitter.search, ((count >= 11 && text =~ "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username) || (query =~ "23456789" && count >= 7 && text = "0123456789" && from = "alice"^^tt:username) || (query =~ "23456789" && text = "0123456789" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && ask_price > 11 && bid_price >= 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @twitter.search(query=v_company_name, count=v_ask_price, in_reply_to=v_company_name), ((query = "ABCDEFGHIJ" && count > 42 && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @uber.time_estimate, (uber_type =~ "12345" && estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((uber_type = "0123456789" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @phone.gps(), ((location = $context.location.current_location && altitude = 1000m && bearing <= 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="12345", message="0123456788") ;
}
====
* => @thecatapi.get, (image_id =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.abc.def"^^tt:url && category =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @thecatapi.get(), ((image_id =~ "12345" && count < 55 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @omlet.send_to, message =~ "0123456788" 
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @twitter.search(query=v_title, count=v_price, from="bob"^^tt:username), (((query = "23456789" && count < 7 && text =~ "9876543210" && contains(urls, "http://www.google.com"^^tt:url)) || (count = 42 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @omlet.send_to(message=v_text, to="bob"^^tt:username) ;
}
====
* => * => @omlet.send_picture, (to = "bob"^^tt:username && caption =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_couch_market.search(), ((price > 14 || title =~ "0123456788" || (title = "0123456788" && price < 55) || (price <= 7 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
now => @holidays.next_uk_holiday_event, (description = "9876543210" || (summary = "12345" && description = "9876543210") || (summary = "23456789" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @bluetooth_speaker.play_music() ;
}
====
@weatherapi.monitor, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature > 22C && humidity > 7 && cloudiness >= 7 && fog > 11) || (wind_speed >= 5kmph && humidity > 55 && weather =~ "0123456788")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), (((humidity < 11 && cloudiness >= 7 && fog < 42 && status = enum(sunny)) || (temperature > 22C && humidity < 42 && cloudiness < 7))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_bike_market.search(price=v_cloudiness), ((price <= 55 || price = 14 || price = 7))  => notify;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.current_location && weight >= 82kg && leanMass > 55 && bodyFat < 14 && bmi <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@washington_post.new_article, (link = "http://www.abc.def"^^tt:url && description = "ABCDEFGHIJ") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), true , v_title := title, v_link := link, v_description := description => @phone.set_ringer(mode=enum(normal)) ;
}
====
now => @almond_dates.search, (message =~ "ABCDEFGHIJ" && poster =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((interest =~ "0123456789" && message = "0123456789" && poster = "9876543210") || message = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@icalendar.event_begin, ((start_date = makeDate(1487030400000) && description = "9876543210" && sequence >= 7 && organizer = "0123456789") || (summary =~ "9876543210" && description = "23456789" && location =~ "23456789") || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence <= 14 && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (end_date = makeDate(1462320000000)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @xkcd.get_comic, (number > 55 && title =~ "0123456788" && alt_text =~ "12345") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(), (number < 7) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @github.comment_issue, (issue_number >= 7 && body = "12345") 
;;
Main() {
    now => @github.comment_issue(repo_name="9876543210", issue_number=55, body="0123456788") ;
}
====
@reddit_front_page.newpost, true , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "23456789" && category =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @bing.image_search(query=v_user), ((title = "23456789" && link = "http://www.abc.def"^^tt:url && height <= 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @linkedin.share, status =~ "9876543210" 
;;
Main() {
    @phdcomics.new_post(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @linkedin.share(status=v_title) ;
}
====
now => @almond_couch_market.search, (title =~ "ABCDEFGHIJ" && price <= 7 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title = "23456789" && price = 55)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @ytranslate.detect_language, (text = "23456789" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="0123456789"), (detected_language = "zh"^^tt:iso_lang_code("Chinese")) , v_detected_language := detected_language => notify;
}
====
@phone.receive_sms, true , v_from := from, v_body := body => * => *
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @twitter.unfollow(user_name=v_body) ;
}
====
* => * => @omlet.send_to, message =~ "0123456788" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @omlet.send_to(to=v_summary, message="0123456789") ;
}
====
@instagram.new_picture, ((link = "http://www.google.com"^^tt:url && filter = "ABCDEFGHIJ") || caption =~ "0123456788" || (caption =~ "0123456788" && link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.work) || (caption =~ "0123456788" && link = "http://www.google.com"^^tt:url) || (caption = "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.home) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((location = $context.location.home || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => * => @slack.setPresence, true 
;;
Main() {
    @sleep-tracker.getsleep(), (((asleepTime = makeDate(1462320000000) && duration >= 2h && rem < 2h && light < 30min) || (time = makeDate(1487030400000) && place = $context.location.home && light < 30min) || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1487030400000) && duration < 2h && rem < 2h && light < 2h && deep < 30min) || (place = $context.location.work && duration > 2h && deep < 2h) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1462320000000) && light > 30min && deep <= 30min) || (time = makeDate(1487030400000) && duration > 2h && light < 30min && deep <= 2h) || (time = makeDate(1487030400000) && place = $context.location.home && light = 2h && deep > 2h) || (duration >= 30min && rem < 2h && light >= 30min && deep < 30min) || (time = makeDate(1487030400000) && duration < 2h && rem < 2h && deep <= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @slack.setPresence(presence=enum(away)) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && updated = makeDate(1487030400000)) || (title = "12345" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ")) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@fitness-tracker.getbody, (weight > 82kg && leanMass < 14 && bodyFat <= 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => @dropbox.move, (new_name = "ABCDEFGHIJ" || (old_name =~ "12345" && new_name = "0123456788") || (old_name = "12345" && new_name = "ABCDEFGHIJ")) 
;;
Main() {
    @fitness-tracker.getbody(), ((leanMass = 7 && bodyFat > 55 && bmi = 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @dropbox.move(old_name="0123456788", new_name="0123456788") ;
}
====
@icalendar.event_begin, (summary =~ "9876543210" && description =~ "ABCDEFGHIJ" && sequence > 14 && organizer = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.set_power, (power = enum(off) || power = enum(on)) 
;;
Main() {
    @icalendar.event_begin(), ((summary = "12345" && organizer =~ "9876543210" && location =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => @lg_webos_tv.play_url, (url = "http://www.abc.def"^^tt:url || url = "http://www.abc.def"^^tt:url || url = "http://www.google.com"^^tt:url) 
;;
Main() {
    @bodytrace.source(), (weight >= 155lb) , v_time := time, v_weight := weight => @lg_webos_tv.play_url(url="http://www.google.com"^^tt:url) ;
}
====
now => @github.add_email, (email = "bob@stanford.edu"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
* => @imgflip.list, (name =~ "0123456788" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (name =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), ((other_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && away_name = "23456789" && home_name = "ABCDEFGHIJ" && scheduled_time = makeDate(1462320000000) && away_points <= 55)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @almond_couch_market.search, price >= 42 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @fitness-tracker.getbody(), (((weight <= 82kg && leanMass <= 14 && bodyFat < 42 && bmi > 11) || (weight >= 155lb && leanMass > 11 && bodyFat > 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_couch_market.search(), (contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @uber.time_estimate, ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && estimate < 2h) || (uber_type = "0123456788" && estimate <= 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @thermostat.temperature(), ((value >= 73F || (time = makeDate(1462320000000) && value < 22C) || time = makeDate(1487030400000))) , v_time := time, v_value := value => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), true , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @gmail.send_email, ((subject =~ "0123456789" && message = "0123456789") || to = "bob@stanford.edu"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789" && message =~ "23456789") || message =~ "ABCDEFGHIJ" || (subject = "0123456789" && message = "ABCDEFGHIJ")) 
;;
Main() {
    @onedrive.file_modified(), ((file_name = "12345" || file_name = "0123456788")) , v_file_name := file_name => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="0123456788") ;
}
====
@tumblr.new_text, (blog_name = "23456789" && link = "http://www.google.com"^^tt:url && body =~ "0123456789") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="ABCDEFGHIJ"), (((blog_name =~ "0123456788" && title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag) && body = "9876543210") || (blog_name = "23456789" && title =~ "23456789" && link = "http://www.google.com"^^tt:url && body = "23456789"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @almond_bike_market.ask(id=v_title, property=v_title), (property =~ "12345")  => notify;
}
====
now => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && subject = "0123456789") 
;;
Main() {
    now => @instagram.get_pictures(count=11), ((count < 55 && caption = "23456789" && filter = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @gmail.send_picture(picture_url=v_picture_url, to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="0123456789") ;
}
====
now => @gmail.get_latest, label =~ "23456789" , v_from := from, v_subject := subject, v_label := label => @fitbit.recordweight, (weight < 155lb || weight >= 155lb) 
;;
Main() {
    now => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && label =~ "0123456789")) , v_from := from, v_subject := subject, v_label := label => @fitbit.recordweight(weight=82kg) ;
}
====
* => * => @facebook.post, true 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature >= 22C && wind_speed <= 5kmph && humidity >= 55 && cloudiness > 11) || (location = $context.location.home && temperature >= 73F && wind_speed > 25mph && humidity < 55 && cloudiness < 11 && fog = 55 && status = enum(drizzling)) || (temperature >= 73F && wind_speed > 5kmph && humidity >= 7 && weather = "23456789") || (temperature <= 73F && cloudiness > 11 && fog <= 55 && weather = "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @youtube.search_channel(query="9876543210"), (description =~ "ABCDEFGHIJ") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @facebook.post(status=v_description) ;
}
====
* => @instagram.get_pictures, ((count >= 55 && caption =~ "ABCDEFGHIJ" && filter = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) || (count >= 7 && caption = "0123456788" && link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)) || (count >= 11 && caption = "23456789") || (count <= 7 && link = "http://www.google.com"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @jawbone_up.getheartrate(), ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_time := time, v_place := place, v_heartrate := heartrate => @instagram.get_pictures(), (((count <= 11 && caption = "12345" && link = "http://www.google.com"^^tt:url) || (count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @onedrive.delete_file, file_name = "0123456788" 
;;
Main() {
    now => @almond_couch_market.search(), ((price <= 14 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @onedrive.delete_file(file_name=v_title) ;
}
====
* => @github.comment_issue, (repo_name =~ "0123456789" && issue_number >= 55 && body = "23456789") 
;;
Main() {
    @builtin.timer(interval=30min), ((interval < 30min || interval < 2h))  => @github.comment_issue(repo_name="9876543210", issue_number=11, body="23456789") ;
}
====
now => * => @fitbit.recordweight, (weight >= 82kg || weight > 155lb || weight >= 82kg || weight = 155lb || weight < 155lb) 
;;
Main() {
    now => @dropbox.open(file_name="0123456789"), true , v_url := url => @fitbit.recordweight(weight=155lb) ;
}
====
now => @almond_bike_market.ask, true  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="0123456789", property="12345"), ((id = "12345" && property = "23456789"))  => notify;
}
====
* => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    @slack.receive(), (((channel = "cat"^^tt:hashtag && from = "bob"^^tt:username) || from = "bob"^^tt:username || (channel = "cat"^^tt:hashtag && message =~ "12345"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @fitbit.getsteps(), true , v_date := date, v_steps := steps => @fitbit.recordweight(weight=82kg) ;
}
====
@thermostat.temperature, time = makeDate(1462320000000) , v_time := time, v_value := value => * => *
;;
Main() {
    @thermostat.temperature(), (value >= 22C) , v_time := time, v_value := value => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
* => * => @almond_couch_market.post, ((title =~ "ABCDEFGHIJ" && price > 14) || (price >= 42 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @twitter.my_tweet(), (((contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || text =~ "ABCDEFGHIJ")) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @twitter.search(query=v_text), ((count = 14 && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @almond_couch_market.post(contact=v_text, title="12345", price=11) ;
}
====
@twitter.source, ((contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && __reserved = true) || from = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => notify
;;
Main() {
    @twitter.source(), (((contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username) || (contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => notify;
}
====
@activity-tracker.getmove, (day = "23456789" && distance < 1000m && steps < 42 && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), (((day = "12345" && distance >= 1000m && steps > 55 && activeTime < 2h) || (distance < 42cm && steps > 7 && activeTime < 30min && inactiveTime <= 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @twitter.sink(status="0123456788") ;
}
====
* => @google_drive.create_new_drive_file, (file_name = "0123456788" || file_name =~ "23456789" || file_name = "12345") 
;;
Main() {
    @omlet.newmessage(), ((type = enum(text) && message = "12345")) , v_type := type, v_message := message => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
* => @phone.call, number = "+16501234567"^^tt:phone_number 
;;
Main() {
    @security-camera.new_event(), ((has_motion = true && has_person = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
@thermostat.humidity, (value = 55 || time = makeDate(1462320000000) || (time = makeDate(1487030400000) && value >= 7) || value >= 14 || value < 14) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), ((value > 42 || value >= 55 || (time = makeDate(1487030400000) && value < 55) || value >= 55)) , v_time := time, v_value := value => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
now => @twitter.search_by_hashtag, (text = "0123456788" && contains(hashtags, "lol"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=42), ((count <= 55 && contains(hashtags, "lol"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @slack.updateChannelPurpose(channel=v_text, purpose=v_text) ;
}
====
* => @builtin.notify, (message = "12345" || message =~ "23456789" || message = "0123456789" || message =~ "12345" || message =~ "9876543210") 
;;
Main() {
    now => @builtin.notify(message="23456789") ;
}
====
@almond_couch_market.monitor, (contact = "+15551234567"^^tt:phone_number || (title =~ "ABCDEFGHIJ" && price < 11)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (((price <= 55 && contact = "+15551234567"^^tt:phone_number) || price > 14 || (price = 42 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @thermostat.get_hvac_state(), ((state = enum(off) || state = enum(cooling))) , v_mode := mode, v_state := state => notify;
}
====
now => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "0123456789" && message =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.set_power(power=enum(on)) ;
}
====
@phdcomics.new_post, title =~ "23456789" , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "9876543210" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @xkcd.get_comic(), ((number < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text = "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @lg_webos_tv.unmute, true 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((description = "12345" || description =~ "12345")) , v_date := date, v_summary := summary, v_description := description => @lg_webos_tv.unmute() ;
}
====
* => @thermostat.set_target_temperature, value > 73F 
;;
Main() {
    @builtin.timer(interval=30min), true  => @thermostat.set_target_temperature(value=73F) ;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @weatherapi.current(location=$context.location.work), ((wind_speed <= 5kmph && humidity >= 42 && cloudiness <= 11 && weather =~ "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@gmail.receive_important_email, (from_name =~ "ABCDEFGHIJ" && subject = "9876543210") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_important_email(), ((from_name =~ "23456789" && from_address = "bob@stanford.edu"^^tt:email_address && subject = "0123456788" && snippet = "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@rss.new_post, title = "12345" , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (title =~ "0123456789") , v_title := title, v_link := link => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, ((message =~ "12345" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @scale.source(), (value <= 155lb) , v_time := time, v_value := value => @almond_dates.search(), (((interest = "0123456789" && message = "0123456789" && poster =~ "0123456789") || (interest = "0123456788" && message = "0123456789" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @twitter.follow, user_name = "alice"^^tt:username 
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), (((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && estimate > 30min) || estimate >= 2h)) , v_uber_type := uber_type, v_estimate := estimate => @twitter.follow(user_name=v_uber_type) ;
}
====
now => @almond_dates.search, ((message =~ "0123456788" && poster = "0123456788") || (message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((interest = "0123456788" && message = "0123456788" && poster =~ "23456789") || poster =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @linkedin.get_profile, (formatted_name =~ "ABCDEFGHIJ" && headline =~ "12345" && industry = "23456789" && specialties =~ "0123456789" && contains(positions, "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @washington_post.new_article(section=enum(business)), (section = enum(national)) , v_title := title, v_link := link, v_description := description => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, interest =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @builtin.timer(interval=2h), true  => @almond_dates.search(), (((message = "0123456788" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number) || (interest = "23456789" && message = "9876543210" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @giphy.get_tag, true , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), (((tag = "funny"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || tag = "lol"^^tt:hashtag)) , v_picture_url := picture_url => notify;
}
====
* => @bing.web_search, ((query =~ "9876543210" && description = "9876543210" && link = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && description = "9876543210") || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="23456789"), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_description := description, v_link := link => notify;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(automotive)), true , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @heatpad.set_power(power=enum(off)) ;
}
====
@almond_bike_market.monitor, price < 42  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=7), (price < 11)  => notify;
}
====
@jawbone_up.getmove, (distance <= 42cm && steps >= 14 && activeTime <= 2h && inactiveTime >= 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @jawbone_up.getmove(), (((updateTime = makeDate(1462320000000) && activeTime < 2h && inactiveTime = 2h && calories > 500kcal) || (updateTime = makeDate(1487030400000) && day =~ "12345" && distance <= 42cm && steps < 7 && activeTime <= 30min && inactiveTime >= 2h && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @holidays.uk_holiday_event(), (((summary =~ "23456789" && description = "9876543210") || (date = makeDate(1487030400000) && description = "ABCDEFGHIJ") || (date = makeDate(1487030400000) && summary = "0123456789" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => @heatpad.set_power(power=enum(off)) ;
}
====
* => @thecatapi.get, (count < 7 && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @wall_street_journal.technology(), true , v_title := title, v_link := link => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@phone.receive_sms, true , v_from := from, v_body := body => * => *
;;
Main() {
    @phone.receive_sms(), (from = "+15551234567"^^tt:phone_number) , v_from := from, v_body := body => @fitbit.recordweight(weight=82kg) ;
}
====
@onedrive.file_modified, (file_name =~ "0123456789" || file_name = "9876543210" || file_name =~ "0123456789" || file_name = "9876543210") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), ((file_name =~ "0123456789" || file_name =~ "0123456789")) , v_file_name := file_name => @onedrive.upload_picture(file_name=v_file_name, picture_url=v_file_name) ;
}
====
@scale.source, time = makeDate(1462320000000) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), (value >= 82kg) , v_time := time, v_value := value => @builtin.notify(message="ABCDEFGHIJ") ;
}
====
now => @dropbox.open, (file_name = "12345" && url =~ "0123456789") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="9876543210"), true , v_url := url => notify;
}
====
@slack.receive, (timestamp =~ "0123456789" || timestamp = "0123456788" || timestamp = "0123456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), (((timestamp = "9876543210" && message =~ "12345") || channel = "lol"^^tt:hashtag)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name = "9876543210" || file_name =~ "23456789" || file_name = "23456789" || file_name = "0123456789") 
;;
Main() {
    @phone.receive_sms(), (from = "+16501234567"^^tt:phone_number) , v_from := from, v_body := body => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @almond_dates.search, (interest =~ "23456789" && poster = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((message = "0123456788" && poster = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @xkcd.random_comic, ((title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number <= 11 && link = "http://www.google.com"^^tt:url) || title =~ "9876543210" || (link = "http://www.google.com"^^tt:url && alt_text =~ "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @holidays.uk_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @xkcd.random_comic(), ((link = "http://www.abc.def"^^tt:url && alt_text = "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @thecatapi.get, (link = "http://www.abc.def"^^tt:url || (count < 55 && link = "http://www.google.com"^^tt:url) || (count <= 14 && link = "http://www.google.com"^^tt:url) || count >= 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (((count > 42 && link = "http://www.abc.def"^^tt:url) || image_id =~ "ABCDEFGHIJ")) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @bluetooth_speaker.set_volume, true 
;;
Main() {
    now => @xkcd.random_comic(), (((number >= 14 && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @bluetooth_speaker.set_volume(percent=v_number) ;
}
====
* => @uber.price_estimate, (uber_type =~ "0123456788" && currency_code =~ "0123456789" && surge = 42 && duration <= 30min && distance <= 42cm) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((end = $context.location.home && uber_type = "12345" && high_estimate < 7 && surge <= 14 && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @omlet.send_to, to = "alice"^^tt:username 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 7 && name =~ "12345" && closest_distance_to_earth = 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @omlet.send_to(message=v_name, to="bob"^^tt:username) ;
}
====
now => * => @google_drive.create_new_drive_file, file_name = "0123456789" 
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((uber_type = "9876543210" && estimate >= 2h) || (uber_type = "9876543210" && estimate < 30min))) , v_uber_type := uber_type, v_estimate := estimate => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
* => @instagram.get_pictures, (filter = "0123456788" || (count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), ((count < 14 || (count > 55 && caption =~ "12345" && location = $context.location.current_location) || (count < 7 && caption = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) || (count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@phdcomics.new_post, ((title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @thecatapi.get(), (count > 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @heatpad.set_power(power=enum(on)) ;
}
====
* => @twitter.unfollow, true 
;;
Main() {
    @github.new_issue(repo_name="12345"), true , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @twitter.unfollow(user_name=v_user) ;
}
====
* => @facebook.post, status =~ "0123456789" 
;;
Main() {
    now => @facebook.post(status="0123456788") ;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude > 1000m && bearing = 42 && speed = 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.current_location && altitude = 42cm && bearing = 42)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@builtin.at, time = makeTime(20,30)  => * => *
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @phone.get_gps(), ((bearing > 42 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@nasa.asteroid, (asteroid_id >= 55 && name =~ "0123456788" && estimated_diameter_min > 1000m && dangerous = false && closest_distance_to_earth > 42cm) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 7 && estimated_diameter_min >= 1000m && estimated_diameter_max < 1000m && relative_velocity > 25mph && closest_distance_to_earth > 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @nasa.rover(), true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => @almond_dates.search, message =~ "0123456789" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest = "0123456788" && message = "12345" && poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "9876543210" || (message =~ "ABCDEFGHIJ" && poster =~ "ABCDEFGHIJ") || (interest =~ "9876543210" && message =~ "23456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @thecatapi.get(), ((count < 55 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @security-camera.set_power(power=enum(off)) ;
}
====
@washington_post.new_blog_post, ((section = enum(fact_checker) && link = "http://www.abc.def"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => *
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_everything)), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @phone.call_emergency() ;
}
====
now => * => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject =~ "9876543210" && message =~ "23456789") 
;;
Main() {
    now => @almond_couch_market.search(), (price <= 7) , v_title := title, v_price := price, v_contact := contact => @gmail.send_email(subject=v_title, message=v_title, to="bob@stanford.edu"^^tt:email_address) ;
}
====
* => @bluetooth_speaker.set_volume, percent = 7 
;;
Main() {
    @gmail.receive_important_email(), ((from_name =~ "9876543210" && snippet = "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @bluetooth_speaker.set_volume(percent=55) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "9876543210" || file_name =~ "9876543210" || file_name =~ "0123456788" || file_name =~ "0123456788" || file_name = "0123456788" || file_name = "12345" || file_name =~ "12345" || file_name =~ "12345" || file_name =~ "0123456789" || file_name = "12345") 
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
@heartrate-monitor.getheartrate, (heartrate > 11 || (time = makeDate(1462320000000) && place = $context.location.home) || heartrate >= 55) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate = 7 || heartrate <= 14 || heartrate > 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @icalendar.list_events(), ((sequence <= 11 && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && weight = 155lb && leanMass > 14 && bodyFat < 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.home && weight <= 155lb && leanMass < 55 && bodyFat > 14 && bmi = 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => * => @facebook.post, status =~ "12345" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((description = "9876543210" || (summary =~ "12345" && description = "12345"))) , v_date := date, v_summary := summary, v_description := description => @facebook.post(status="0123456789") ;
}
====
now => @twitter.send_direct_message, (message = "ABCDEFGHIJ" || (to = "bob"^^tt:username && message =~ "23456789") || to = "alice"^^tt:username || (to = "bob"^^tt:username && message = "12345") || message = "0123456789" || message =~ "12345") 
;;
Main() {
    now => @twitter.send_direct_message(to="bob"^^tt:username, message="23456789") ;
}
====
now => @icalendar.list_events, (end_date = makeDate(1462320000000) && sequence > 11 && organizer = "9876543210" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && description =~ "0123456788" && sequence <= 7 && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @builtin.get_random(), ((number > 14 || number < 11)) , v_number := number => @heatpad.set_power(power=enum(on)) ;
}
====
now => * => @almond_bike_market.post, ((price >= 55 && info = "9876543210" && poster =~ "23456789") || (title = "23456789" && price < 14)) 
;;
Main() {
    now => @ytranslate.detect_language(text="23456789"), ((text = "0123456789" || (text = "0123456788" && detected_language = "zh"^^tt:iso_lang_code("Chinese")))) , v_detected_language := detected_language => @almond_bike_market.post(title="23456789", price=7, info="0123456789", poster="9876543210") ;
}
====
@instagram.new_picture, (filter = "23456789" && contains(hashtags, "funny"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption =~ "0123456788" && link = "http://www.google.com"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    @onedrive.file_created(), (file_name = "0123456789") , v_file_name := file_name => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
@thermostat.temperature, (value < 73F || value >= 22C) , v_time := time, v_value := value => notify
;;
Main() {
    @thermostat.temperature(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => notify;
}
====
* => @almond_bike_market.post, ((title = "0123456789" && price < 7 && info =~ "0123456789") || (price >= 14 && poster =~ "12345")) 
;;
Main() {
    @github.new_milestone(repo_name="ABCDEFGHIJ"), ((repo_name = "ABCDEFGHIJ" && user = "alice"^^tt:username)) , v_user := user, v_description := description, v_time := time => @almond_bike_market.post(poster=v_description, title="ABCDEFGHIJ", price=14, info="0123456788") ;
}
====
* => @almond_bike_market.ask, (property =~ "12345" || property =~ "0123456789")  => *
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @almond_bike_market.ask(id="9876543210", property="0123456789"), true  => notify;
}
====
now => * => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345")) 
;;
Main() {
    now => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
now => * => @omlet.send_picture, ((to = "alice"^^tt:username && caption = "9876543210") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456788") ;
}
====
* => @nasa.rover, (count >= 42 && camera_used = "9876543210") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), ((count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "23456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @builtin.cool() ;
}
====
* => @almond_bike_market.post, (info =~ "12345" || (info =~ "ABCDEFGHIJ" && poster = "0123456788")) 
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @almond_bike_market.post(title="12345", price=14, info="ABCDEFGHIJ", poster="0123456788") ;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    @jawbone_up.getmove(), (((distance < 1000m && steps = 11 && activeTime <= 2h && inactiveTime > 2h && calories = 500kcal) || (updateTime = makeDate(1487030400000) && distance >= 42cm && steps < 14 && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @light-bulb.color_loop() ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight >= 155lb) || weight >= 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((weight = 155lb || weight = 82kg)) , v_time := time, v_weight := weight => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((location = $context.location.current_location || phase =~ "0123456788" || (location = $context.location.work && date = makeDate(1462320000000) && phase =~ "12345"))) , v_date := date, v_phase := phase => notify;
}
====
@onedrive.file_created, (file_name =~ "23456789" || file_name = "0123456789") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), (file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => @fitbit.getbody(), ((weight >= 155lb && bmi > 55 && fat < 11)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature > 73F && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 7 && fog < 7 && weather = "0123456788") || (temperature = 73F && wind_speed > 5kmph && fog > 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = $context.location.work && temperature <= 73F && humidity < 14 && cloudiness > 14 && fog > 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @ytranslate.detect_language(text=v_weather), (((text =~ "23456789" && detected_language = "en"^^tt:iso_lang_code("English")) || (text =~ "23456789" && detected_language = "en"^^tt:iso_lang_code("English")))) , v_detected_language := detected_language => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
@youtube.source, channel_title =~ "12345" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((channel_title = "ABCDEFGHIJ" && video_id = "23456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @onedrive.delete_file(file_name=v_channel_id) ;
}
====
* => @icalendar.list_events, ((summary = "0123456788" && description = "23456789" && sequence < 11 && location =~ "23456789") || (end_date = makeDate(1462320000000) && description =~ "23456789" && sequence >= 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && summary = "12345" && sequence = 7 && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@xkcd.new_whatif, ((title =~ "0123456789" && link = "http://www.google.com"^^tt:url) || title = "9876543210" || (title =~ "12345" && link = "http://www.abc.def"^^tt:url) || title = "0123456789") , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), ((link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url || title =~ "0123456788" || (title = "12345" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => notify;
}
====
now => @bing.web_search, (query =~ "23456789" && description =~ "12345") , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    now => @bing.web_search(query="9876543210"), ((query = "9876543210" && description = "23456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_description := description, v_link := link => @dropbox.create_new_folder(folder_name=v_title) ;
}
====
* => @thermostat.set_minmax_temperature, ((low >= 22C && high > 73F) || low > 22C || high > 73F) 
;;
Main() {
    @security-camera.new_event(), (has_motion = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && organizer = "0123456788") || (end_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ" && sequence >= 14 && location =~ "23456789") || (start_date = makeDate(1462320000000) && description =~ "23456789" && organizer = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @phone.receive_sms(), ((from = "+16501234567"^^tt:phone_number && body = "12345")) , v_from := from, v_body := body => @icalendar.list_events(), (((start_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && sequence = 11) || (start_date = makeDate(1462320000000) && sequence <= 55 && organizer = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => * => @speaker.play_music, true 
;;
Main() {
    @scale.source(), (time = makeDate(1462320000000)) , v_time := time, v_value := value => @speaker.play_music() ;
}
====
@phdcomics.new_post, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title = "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @phone.get_gps(), ((altitude <= 1000m && bearing <= 11)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @bing.image_search, (title = "9876543210" && width < 7 && height > 7) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), (((query = "0123456788" && link = "http://www.google.com"^^tt:url && width < 7) || (query = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 14 && height = 11))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @linkedin.share(status="0123456789") ;
}
====
* => @wall_street_journal.get_opinions, ((link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || updated = makeDate(1487030400000) || link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || (title =~ "12345" && link = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && updated = makeDate(1462320000000)) || updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title = "12345" && updated = makeDate(1462320000000)) || (title =~ "0123456789" && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || updated = makeDate(1487030400000) || updated = makeDate(1487030400000) || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && updated = makeDate(1487030400000)))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@sleep-tracker.getsleep, (time = makeDate(1487030400000) && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration > 2h && rem > 30min && light < 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && duration < 30min && rem <= 2h && light < 2h && deep > 2h) || (rem >= 2h && light < 2h && deep > 30min) || (time = makeDate(1487030400000) && rem >= 30min && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @nasa.apod(), ((title = "9876543210" && description = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @instagram.get_pictures, ((count >= 14 && caption =~ "0123456788" && link = "http://www.abc.def"^^tt:url && filter = "ABCDEFGHIJ") || (count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789" && link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "9876543210" && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @thermostat.temperature(), ((value >= 73F || (time = makeDate(1487030400000) && value > 22C) || value <= 22C || time = makeDate(1487030400000))) , v_time := time, v_value := value => @instagram.get_pictures(), ((count = 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@gmail.receive_important_email, (subject =~ "9876543210" && date = makeDate(1462320000000) && snippet =~ "ABCDEFGHIJ") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_important_email(), ((subject =~ "0123456789" && snippet = "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
* => @thermostat.set_hvac_mode, mode = enum(heat_cool) 
;;
Main() {
    @twitter.my_tweet(), ((text = "23456789" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
* => @bing.image_search, (query = "0123456789" && title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width = 7 && height < 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="12345"), (((query =~ "12345" && link = "http://www.abc.def"^^tt:url && width >= 14) || width < 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @speaker.lower_volume() ;
}
====
now => @nasa.apod, (title =~ "23456789" || (title = "ABCDEFGHIJ" && description = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), ((title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@almond_couch_market.monitor, (title = "12345" && price = 14 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (title = "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => @youtube.list_subscriptions(), (((channel_id = "0123456788" && title = "23456789" && description = "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture || (description =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @fitbit.getbody, fat > 7 , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "23456789" && message = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @fitbit.getbody(), (((weight > 82kg && bmi < 55 && fat > 42) || (weight <= 155lb && bmi >= 42 && fat >= 55) || (weight < 82kg && bmi > 7 && fat >= 7))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @thecatapi.get, (count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text, (title =~ "0123456789" || (title = "23456789" && body = "ABCDEFGHIJ")) 
;;
Main() {
    now => @thecatapi.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text(title=v_image_id, body=v_image_id) ;
}
====
@tumblr.new_text, (blog_name = "9876543210" || blog_name = "12345" || (title = "0123456789" && contains(tags, "cat"^^tt:hashtag) && body =~ "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), (((blog_name =~ "12345" && link = "http://www.abc.def"^^tt:url) || (blog_name = "23456789" && date = makeDate(1487030400000) && contains(tags, "funny"^^tt:hashtag) && body =~ "0123456788"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @light-bulb.alert_long() ;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.current_location), ((phase =~ "23456789" || (location = $context.location.work && date = makeDate(1487030400000)) || (location = $context.location.work && phase =~ "0123456788") || date = makeDate(1487030400000))) , v_date := date, v_phase := phase => @google_drive.create_new_drive_file(file_name=v_phase) ;
}
====
* => @xkcd.random_comic, ((number >= 11 && title = "12345" && alt_text = "12345") || (number >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), (((stock_id = "aapl"^^tt:stock_id("Apple") && bid_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "0123456789" && ask_price <= 55) || (company_name = "0123456789" && ask_price >= 7 && bid_price <= 42) || (ask_price <= 42 && bid_price > 7))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @xkcd.random_comic(), (((number > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "ABCDEFGHIJ") || (number >= 14 && alt_text = "23456789"))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@yahoofinance.stock_quote, (ask_price = 42 || ask_price > 14) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), ((company_name =~ "0123456789" && ask_price > 14 && bid_price > 7)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @almond_bike_market.ask(id=v_company_name, property=v_company_name), ((property = "0123456788" || id =~ "12345" || property = "23456789" || (id = "23456789" && property = "0123456789") || id = "ABCDEFGHIJ" || (id =~ "ABCDEFGHIJ" && property =~ "12345")))  => notify;
}
====
now => @builtin.get_random_between, (low < 11 && high > 11 && random <= 7) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=14, high=55), ((low < 55 && high < 55 && random <= 55)) , v_random := random => notify;
}
====
* => @almond_couch_market.search, ((title =~ "12345" && price <= 7 && contact = "+15551234567"^^tt:phone_number) || (title =~ "12345" && price >= 11)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "9876543210") , v_file_name := file_name => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_bike_market.monitor, (price > 42 || (info = "23456789" && price >= 42) || price > 55)  => * => notify
;;
Main() {
    @almond_bike_market.monitor(), (((info =~ "23456789" && price < 42) || info = "12345" || price <= 11))  => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@youtube.source, (channel_id = "0123456788" && channel_title =~ "0123456789" && title = "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "12345" && channel_title = "12345" && video_id = "ABCDEFGHIJ" && video_url = "http://www.abc.def"^^tt:url) || (channel_title = "23456789" && video_id =~ "9876543210"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thecatapi.get(), ((count < 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@onedrive.file_modified, (file_name =~ "0123456789" || file_name = "9876543210" || file_name =~ "0123456789" || file_name = "9876543210") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), ((file_name =~ "12345" || file_name = "12345" || file_name =~ "0123456788" || file_name =~ "9876543210")) , v_file_name := file_name => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
@rss.new_post, (title = "ABCDEFGHIJ" || (title =~ "12345" && link = "http://www.google.com"^^tt:url) || link = "http://www.abc.def"^^tt:url || title =~ "23456789" || title = "9876543210") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @builtin.get_random(), (number = 11) , v_number := number => notify;
}
====
* => @almond_bike_market.post, (price < 7 || (title = "9876543210" && poster =~ "ABCDEFGHIJ") || (price < 14 && poster = "0123456788") || price < 14) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (div = 55) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_bike_market.post(price=v_yield, poster=v_company_name, title="0123456789", info="0123456789") ;
}
====
@security-camera.new_event, (has_sound = true || (start_time = makeDate(1487030400000) && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @builtin.get_date(), (date = makeDate(1462320000000)) , v_date := date => notify;
}
====
now => @speaker.play_music, true 
;;
Main() {
    now => @speaker.play_music() ;
}
====
* => @uber.time_estimate, ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && estimate < 2h) || (uber_type = "0123456788" && estimate <= 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @wall_street_journal.markets(), (title = "23456789") , v_title := title, v_link := link => @uber.time_estimate(start=$context.location.work), (estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && asleepTime = makeDate(1462320000000) && light > 2h) || (time = makeDate(1487030400000) && place = $context.location.home && light < 2h && deep <= 30min) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => *
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.work && asleepTime = makeDate(1462320000000) && duration < 30min && rem <= 30min && light < 2h && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @light-bulb.alert_long() ;
}
====
* => * => @phone.notify, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 42 && name = "ABCDEFGHIJ" && estimated_diameter_min <= 42cm && estimated_diameter_max >= 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.notify(title=v_orbiting_body, message=v_orbiting_body) ;
}
====
* => @heatpad.set_power, true 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @heatpad.set_power(power=enum(off)) ;
}
====
* => @instagram.get_pictures, (filter =~ "23456789" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), (((count < 55 && link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456788" && link = "http://www.google.com"^^tt:url && filter =~ "23456789") || (count <= 11 && filter = "9876543210" && location = $context.location.work) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter =~ "9876543210") || (count >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "ABCDEFGHIJ"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @almond_bike_market.ask_seller, (property = "9876543210" && value =~ "9876543210") , v_answer := answer => notify
;;
Main() {
    @wall_street_journal.markets(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => @almond_bike_market.ask_seller(id=v_title, property=v_title, value=v_title, public=false), (((id = "0123456789" && value = "9876543210" && answer = "9876543210") || (property =~ "23456789" && value = "23456789" && answer = "0123456789") || (id = "0123456789" && answer = "12345"))) , v_answer := answer => notify;
}
====
@xkcd.new_whatif, ((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title = "0123456788" || title =~ "ABCDEFGHIJ" || (title =~ "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify
;;
Main() {
    @xkcd.new_whatif(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => notify;
}
====
@slack.receive, (timestamp =~ "12345" && message =~ "0123456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "9876543210" && from = "bob"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @almond_bike_market.ask_seller(property=v_message, value=v_message, id="23456789", public=false), ((id =~ "ABCDEFGHIJ" && value =~ "ABCDEFGHIJ")) , v_answer := answer => notify;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "23456789") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "0123456789") , v_file_name := file_name => @gmail.send_picture(to=v_file_name, subject=v_file_name, picture_url=v_file_name, message="ABCDEFGHIJ") ;
}
====
@nasa.asteroid, (name = "23456789" && dangerous = true && relative_velocity <= 5kmph && closest_distance_to_earth <= 42cm && orbiting_body = "12345") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((estimated_diameter_min < 1000m && dangerous = true && relative_velocity >= 5kmph && orbiting_body = "12345") || (asteroid_id < 55 && estimated_diameter_min <= 42cm && estimated_diameter_max > 42cm && closest_distance_to_earth <= 1000m && orbiting_body =~ "23456789") || (asteroid_id = 55 && name = "ABCDEFGHIJ" && estimated_diameter_max > 1000m && relative_velocity > 25mph && closest_distance_to_earth >= 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @dropbox.list_folder(folder_name="0123456788"), ((folder_name = "9876543210" && file_name =~ "ABCDEFGHIJ" && is_folder = false && full_path = "9876543210")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
now => * => @fitbit.recordweight, (weight = 82kg || weight >= 155lb || weight >= 155lb) 
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @fitbit.recordweight(weight=155lb) ;
}
====
* => @linkedin.get_profile, ((formatted_name = "9876543210" && headline =~ "12345" && industry = "0123456788" && specialties =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "12345" && industry = "9876543210") || headline =~ "ABCDEFGHIJ" || (formatted_name = "9876543210" && industry =~ "0123456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.abc.def"^^tt:url || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && filter =~ "0123456788"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @linkedin.get_profile(), (((industry =~ "0123456788" && specialties = "ABCDEFGHIJ" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "12345" && industry = "ABCDEFGHIJ"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @dropbox.list_folder, (folder_name =~ "0123456789" && file_name = "23456789" && file_size < 5KB) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), ((folder_name = "0123456789" && file_size < 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @almond_dates.post, ((interest =~ "9876543210" && message = "0123456789" && poster =~ "0123456789") || (poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @almond_dates.post(message=v_body, poster=v_body, phone=v_from, interest="9876543210") ;
}
====
* => @uber.price_estimate, (end = $context.location.home && uber_type =~ "0123456789" && low_estimate > 11 && high_estimate >= 42 && surge <= 7 && duration > 30min) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.home, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((start = $context.location.current_location && uber_type = "0123456789" && low_estimate >= 11 && high_estimate <= 42 && currency_code = "9876543210" && surge >= 42 && duration <= 2h && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @thecatapi.get, count > 14 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @gmail.receive_primary_email(), (((from_name = "ABCDEFGHIJ" && subject =~ "12345") || (from_name = "9876543210" && date = makeDate(1462320000000) && contains(labels, "12345")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @thecatapi.get(), ((count = 55 || count < 14)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@instagram.new_picture, (link = "http://www.google.com"^^tt:url || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "23456789" && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @youtube.search_videos(query=v_filter), (((query =~ "0123456788" && title =~ "12345" && description = "0123456788" && count > 7) || (video_id = "ABCDEFGHIJ" && channel_id =~ "23456789" && channel_title =~ "12345" && description =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count <= 42) || (channel_title = "9876543210" && description = "ABCDEFGHIJ" && count < 14))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@wall_street_journal.us_business, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @wall_street_journal.us_business(), (title = "0123456788") , v_title := title, v_link := link => @almond_couch_market.search(), ((title =~ "0123456789" && price >= 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@onedrive.file_created, (file_name = "9876543210" || file_name =~ "12345") , v_file_name := file_name => @twitter.search, (text =~ "0123456788" && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @twitter.search(query=v_file_name, from=v_file_name, in_reply_to=v_file_name, count=55), (((count > 55 && text = "23456789" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username) || (query = "9876543210" && count > 11 && from = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @builtin.get_random_between, ((low <= 11 && high > 42) || (low > 11 && random >= 42) || (low <= 55 && high <= 11 && random >= 14) || random > 7 || (low < 14 && high = 42 && random < 7)) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=42, high=14), ((low < 42 && high > 14 && random >= 55)) , v_random := random => notify;
}
====
now => @gmail.get_latest, true , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @gmail.send_picture(to=v_label, message=v_label, picture_url=v_label, subject="0123456789") ;
}
====
* => @thecatapi.get, (image_id = "12345" || count > 55 || (image_id = "0123456788" && count = 14)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @thecatapi.get(), ((image_id = "23456789" || count > 55)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && label = "12345"))) , v_from := from, v_subject := subject, v_label := label => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @almond_bike_market.post, poster = "ABCDEFGHIJ" 
;;
Main() {
    now => @almond_bike_market.post(title="12345", price=14, info="9876543210", poster="0123456788") ;
}
====
now => * => @tumblr-blog.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((summary = "0123456788" || summary =~ "12345")) , v_date := date, v_summary := summary, v_description := description => @tumblr-blog.post_picture(caption=v_description, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@instagram.new_picture, ((caption =~ "12345" && link = "http://www.google.com"^^tt:url) || (caption = "12345" && filter =~ "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((filter =~ "0123456789" && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_couch_market.search(), ((title = "0123456788" || (title =~ "23456789" && price < 42 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="bar"^^sportradar:eu_soccer_team("Barcellona")), (((watched_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && other_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && away_points < 7 && home_points >= 7 && result = enum(draw)) || (watched_team_alias = "bar"^^sportradar:eu_soccer_team("Barcellona") && away_name =~ "9876543210" && home_name = "0123456788" && scheduled_time = makeDate(1462320000000) && away_points < 42))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @security-camera.set_power(power=enum(off)) ;
}
====
* => @phone.send_sms, ((to = "+15551234567"^^tt:phone_number && body =~ "0123456788") || (to = "+15551234567"^^tt:phone_number && body = "ABCDEFGHIJ") || body = "12345" || body =~ "0123456788") 
;;
Main() {
    now => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="23456789") ;
}
====
* => @phone.get_gps, (location = $context.location.home && altitude <= 42cm && bearing > 7 && speed >= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @wall_street_journal.markets(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => @phone.get_gps(), (((bearing = 42 && speed <= 5kmph) || (altitude > 1000m && bearing >= 7 && speed > 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @almond_couch_market.post, ((price <= 14 && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && price > 55)) 
;;
Main() {
    @gmail.receive_important_email(), (contains(labels, "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @almond_couch_market.post(title=v_snippet, contact=v_snippet, price=11) ;
}
====
* => @phone.get_gps, ((altitude < 42cm && bearing <= 14 && speed > 25mph) || (location = $context.location.home && altitude <= 1000m && speed >= 25mph) || (altitude <= 42cm && speed = 5kmph) || (altitude = 1000m && speed <= 5kmph) || bearing = 14) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @phdcomics.new_post(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_link := link, v_picture_url := picture_url => @phone.get_gps(), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 1000m && bearing < 42 && speed < 25mph) || (location = $context.location.home && altitude > 42cm && bearing > 14 && speed <= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@twitter.my_tweet, text =~ "12345" , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => * => notify
;;
Main() {
    @twitter.my_tweet(), ((text = "12345" && contains(hashtags, "cat"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @giphy.get_tag(tag="cat"^^tt:hashtag), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || tag = "lol"^^tt:hashtag || (tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_picture_url := picture_url => notify;
}
====
* => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @youtube.source(), ((channel_id =~ "0123456789" && channel_title =~ "9876543210" && title =~ "ABCDEFGHIJ" && video_id =~ "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @security-camera.set_power(power=enum(off)) ;
}
====
@xkcd.new_whatif, (link = "http://www.google.com"^^tt:url || title =~ "12345") , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
@sportradar.mlb_team, ((home_name =~ "ABCDEFGHIJ" && away_runs > 55 && home_runs > 14) || (away_name = "9876543210" && away_runs <= 55 && home_runs < 55 && result = enum(unclosed)) || (other_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && away_name =~ "9876543210" && home_name = "23456789" && scheduled_time = "23456789" && home_runs < 42) || (other_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && home_name =~ "0123456788" && scheduled_time = "0123456789" && away_runs <= 55 && home_runs >= 14)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => * => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), (((watched_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && other_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && game_status = enum(closed) && home_runs > 11) || (other_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && scheduled_time = "0123456789" && game_inning = "23456789" && away_runs <= 42 && home_runs > 7) || (other_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && home_name = "12345" && scheduled_time =~ "0123456789" && away_runs >= 11 && home_runs = 55) || (watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && watched_is_home = false && game_inning =~ "0123456789" && away_runs <= 7 && home_runs < 7 && result = enum(win)))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
* => @builtin.hello, true 
;;
Main() {
    @wall_street_journal.world_news(), ((title =~ "12345" || title =~ "0123456788")) , v_title := title, v_link := link => @builtin.hello() ;
}
====
now => * => @dropbox.create_new_folder, folder_name =~ "0123456788" 
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=$context.location.home), (((low_estimate > 42 && high_estimate > 14 && currency_code =~ "12345" && surge < 11 && duration < 30min && distance = 1000m) || (uber_type =~ "ABCDEFGHIJ" && low_estimate > 14 && high_estimate >= 11 && surge >= 7 && duration >= 30min && distance >= 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @dropbox.create_new_folder(folder_name=v_currency_code) ;
}
====
* => @github.add_email, true 
;;
Main() {
    @almond_couch_market.monitor(), ((contact = "+16501234567"^^tt:phone_number || (price <= 7 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @github.add_email(email=v_title) ;
}
====
@almond_couch_market.monitor, (price <= 42 || (title =~ "23456789" && price <= 7 && contact = "+16501234567"^^tt:phone_number) || (title =~ "0123456789" && price <= 55)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "12345" && price > 7) || contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @nasa.apod(), (((description = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "12345" || title = "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @almond_bike_market.ask_seller, (id = "ABCDEFGHIJ" && value =~ "23456789") , v_answer := answer => notify
;;
Main() {
    @thermostat.humidity(), ((value <= 11 || (time = makeDate(1462320000000) && value = 42))) , v_time := time, v_value := value => @almond_bike_market.ask_seller(id="0123456789", property="12345", value="0123456788", public=false), (((id =~ "9876543210" && answer = "ABCDEFGHIJ") || (id =~ "12345" && property = "12345" && value = "9876543210"))) , v_answer := answer => notify;
}
====
@heartrate-monitor.getheartrate, (heartrate <= 42 || (time = makeDate(1462320000000) && place = $context.location.current_location && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => *
;;
Main() {
    @heartrate-monitor.getheartrate(), (((place = $context.location.work && heartrate > 11) || (place = $context.location.current_location && heartrate <= 42))) , v_time := time, v_place := place, v_heartrate := heartrate => @phone.call_emergency() ;
}
====
* => @twitter.search_by_hashtag, (count > 55 && text =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @washington_post.new_article(section=enum(local)), ((section = enum(business) && title =~ "0123456789" && description = "23456789")) , v_title := title, v_link := link, v_description := description => @twitter.search_by_hashtag(query_hashtag=v_title, from=v_title), (((query_hashtag = "funny"^^tt:hashtag && count < 11 && from = "bob"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count = 14 && text = "9876543210" && contains(hashtags, "lol"^^tt:hashtag) && from = "alice"^^tt:username) || (count >= 42 && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @linkedin.share, (status = "12345" || status =~ "23456789" || status = "0123456788" || status = "12345" || status = "0123456789" || status = "23456789") 
;;
Main() {
    @jawbone_up.getsleep(), ((light > 30min && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
@bodytrace.source, ((time = makeDate(1462320000000) && weight > 155lb) || weight <= 82kg) , v_time := time, v_weight := weight => @car.honk_horn, true 
;;
Main() {
    @bodytrace.source(), (weight > 82kg) , v_time := time, v_weight := weight => @car.honk_horn() ;
}
====
now => * => @almond_dates.post, message =~ "23456789" 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @almond_dates.post(interest="ABCDEFGHIJ", message="0123456789", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @onedrive.rename_file, (old_name =~ "0123456789" && new_name = "12345") 
;;
Main() {
    now => @onedrive.rename_file(old_name="0123456788", new_name="9876543210") ;
}
====
now => * => @github.comment_issue, (repo_name =~ "0123456789" && issue_number < 7) 
;;
Main() {
    now => @bing.web_search(query="ABCDEFGHIJ"), ((query =~ "23456789" && title = "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => @github.comment_issue(repo_name=v_description, body=v_description, issue_number=7) ;
}
====
* => @almond_dates.search, interest =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @phdcomics.new_post(), (title = "0123456789") , v_title := title, v_link := link, v_picture_url := picture_url => @almond_dates.search(), ((interest = "9876543210" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @youtube.channels_by_category, ((category_id = enum(cooking) && channel_id =~ "23456789" && title = "9876543210") || (channel_id = "ABCDEFGHIJ" && title =~ "ABCDEFGHIJ" && description =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @nasa.asteroid(), (((estimated_diameter_max < 42cm && relative_velocity < 5kmph && closest_distance_to_earth = 1000m) || (asteroid_id = 55 && name =~ "0123456789" && estimated_diameter_min >= 1000m && estimated_diameter_max <= 1000m && closest_distance_to_earth > 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @youtube.channels_by_category(category_id=enum(diy)), ((thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture || (channel_id =~ "12345" && description =~ "0123456789") || description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => * => @twitter.sink, status =~ "0123456789" 
;;
Main() {
    now => @phone.get_gps(), (((location = $context.location.current_location && bearing > 42) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude > 42cm && speed >= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.sink(status="0123456789") ;
}
====
* => @onedrive.rename_file, (new_name =~ "12345" || (old_name =~ "12345" && new_name = "9876543210") || new_name = "0123456788") 
;;
Main() {
    now => @onedrive.rename_file(old_name="ABCDEFGHIJ", new_name="12345") ;
}
====
* => * => @almond_bike_market.post, (title =~ "23456789" && price >= 14 && info =~ "12345" && poster = "ABCDEFGHIJ") 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (summary = "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post(title=v_description, poster=v_description, price=14, info="23456789") ;
}
====
@fitness-tracker.getbody, ((place = $context.location.work && weight >= 82kg && leanMass >= 42 && bodyFat < 42) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass <= 14 && bmi >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((leanMass < 42 && bodyFat >= 42 && bmi = 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @youtube.search_videos(query="ABCDEFGHIJ"), (((query =~ "12345" && channel_id = "0123456789" && channel_title = "9876543210" && description =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 42) || (channel_id =~ "12345" && channel_title =~ "23456789" && title =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 7))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "12345" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "9876543210" && industry =~ "0123456788" && specialties =~ "0123456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((headline =~ "9876543210" && specialties =~ "0123456788")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @onedrive.rename_file, true 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((temperature > 73F && wind_speed = 5kmph && humidity < 11 && fog <= 55 && weather =~ "12345") || (temperature <= 22C && humidity >= 7 && cloudiness > 11 && fog = 7))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @onedrive.rename_file(new_name=v_weather, old_name="23456789") ;
}
====
* => @phone.get_gps, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 42cm && bearing = 55) || (location = $context.location.current_location && altitude >= 42cm && bearing <= 14 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @phone.get_gps(), ((altitude < 1000m && bearing > 14 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @youtube.channels_by_category, (description = "23456789" || (channel_id = "0123456789" && title =~ "0123456789")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(film_and_entertainment)), ((category_id = enum(animation) && channel_id = "0123456788" && title = "0123456788" && description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@builtin.timer, interval < 30min  => * => notify
;;
Main() {
    @builtin.timer(interval=2h), true  => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
now => @slack.send, message = "0123456789" 
;;
Main() {
    now => @slack.send(channel="cat"^^tt:hashtag, message="23456789") ;
}
====
@builtin.timer, interval < 30min  => * => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval > 30min)  => @builtin.get_random_between(low=14, high=7), (((low >= 42 && high > 14) || random <= 7)) , v_random := random => notify;
}
====
* => @uber.time_estimate, (uber_type = "23456789" || (start = $context.location.home && uber_type = "0123456788")) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@github.new_milestone, (user = "bob"^^tt:username && description =~ "9876543210") , v_user := user, v_description := description, v_time := time => @almond_bike_market.post, ((price <= 55 && info =~ "ABCDEFGHIJ" && poster =~ "12345") || (title = "0123456789" && price < 14 && poster = "12345")) 
;;
Main() {
    @github.new_milestone(repo_name="0123456788"), true , v_user := user, v_description := description, v_time := time => @almond_bike_market.post(title=v_description, price=42, info="0123456789", poster="12345") ;
}
====
@twitter.direct_message, ((from = "alice"^^tt:username && message = "0123456789") || __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @fitbit.getsteps(), true , v_date := date, v_steps := steps => notify;
}
====
@jawbone_up.getmove, (activeTime <= 30min && inactiveTime < 30min && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1487030400000) && day = "ABCDEFGHIJ" && steps <= 55 && activeTime = 2h && inactiveTime > 2h && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @youtube.channels_by_category, channel_id = "ABCDEFGHIJ" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @twitter.direct_message(), (message = "0123456789") , v_from := from, v_message := message, v___reserved := __reserved => @youtube.channels_by_category(category_id=enum(science)), ((channel_id = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @fitbit.getsteps, (steps > 11 || date = makeDate(1462320000000)) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), (steps >= 55) , v_date := date, v_steps := steps => notify;
}
====
@icalendar.event_begin, (summary = "9876543210" && sequence <= 42 && organizer =~ "0123456788" && location =~ "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((description =~ "23456789" && location =~ "23456789") || (start_date = makeDate(1487030400000) && summary = "23456789" && description = "9876543210") || (start_date = makeDate(1462320000000) && summary =~ "12345" && description =~ "12345" && sequence < 42 && organizer = "12345" && location = "9876543210") || (end_date = makeDate(1462320000000) && summary = "0123456788" && organizer = "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @speaker.set_volume, true 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id = 7 && name =~ "0123456789" && estimated_diameter_max >= 42cm && dangerous = false && relative_velocity >= 25mph && orbiting_body =~ "9876543210") || (asteroid_id >= 7 && name =~ "0123456789" && estimated_diameter_min <= 42cm && dangerous = false && closest_distance_to_earth > 42cm && orbiting_body = "ABCDEFGHIJ") || (asteroid_id > 7 && name = "23456789" && estimated_diameter_min >= 42cm && estimated_diameter_max > 1000m && relative_velocity < 5kmph && closest_distance_to_earth < 42cm) || (asteroid_id < 42 && name =~ "ABCDEFGHIJ" && estimated_diameter_min < 1000m && relative_velocity <= 25mph && orbiting_body =~ "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @speaker.set_volume(percent=7) ;
}
====
* => @almond_couch_market.post, (price >= 55 || title =~ "23456789" || title =~ "0123456788" || (title =~ "0123456789" && price < 7)) 
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && place = $context.location.current_location && weight <= 82kg && leanMass > 7 && bodyFat <= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_couch_market.post(price=v_leanMass, title="9876543210", contact="+16501234567"^^tt:phone_number) ;
}
====
@nasa.asteroid, ((asteroid_id <= 7 && name =~ "0123456788" && dangerous = false && relative_velocity > 25mph && closest_distance_to_earth >= 42cm && orbiting_body =~ "12345") || (asteroid_id < 42 && name =~ "ABCDEFGHIJ" && estimated_diameter_min >= 42cm && estimated_diameter_max < 1000m && closest_distance_to_earth = 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @twitter.sink, (status =~ "23456789" || status =~ "ABCDEFGHIJ" || status = "0123456789") 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 14 && name =~ "0123456789" && estimated_diameter_min < 1000m && estimated_diameter_max <= 42cm && relative_velocity >= 25mph && closest_distance_to_earth >= 42cm) || (name = "0123456789" && estimated_diameter_min <= 42cm && estimated_diameter_max <= 42cm && relative_velocity < 25mph && closest_distance_to_earth = 42cm && orbiting_body = "ABCDEFGHIJ"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
* => @tumblr-blog.post_text, (title =~ "23456789" && body = "23456789") 
;;
Main() {
    now => @tumblr-blog.post_text(title="0123456788", body="0123456789") ;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude < 42cm && bearing >= 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((bearing > 7 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="23456789", text_bottom="9876543210"), ((template =~ "12345" && name = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
now => @dropbox.get_space_usage, (total < 5KB || (used <= 5KB && total = 20MB) || used >= 20MB) , v_used := used, v_total := total => notify
;;
Main() {
    now => @dropbox.get_space_usage(), (total <= 20MB) , v_used := used, v_total := total => notify;
}
====
* => @google_drive.create_new_drive_file, file_name =~ "0123456788" 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "23456789" || file_name =~ "23456789" || file_name = "0123456788")) , v_file_name := file_name => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
now => * => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || title = "23456789" || title =~ "23456789") 
;;
Main() {
    now => @nasa.apod(), ((title =~ "9876543210" || (title = "12345" && description =~ "23456789"))) , v_title := title, v_description := description, v_picture_url := picture_url => @tumblr-blog.post_text(title=v_description, body=v_description) ;
}
====
now => @thecatapi.get, ((count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || image_id = "9876543210") , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || count >= 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @lg_webos_tv.set_volume(percent=v_count) ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (((place = $context.location.home && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration >= 30min && light < 30min) || (time = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration <= 2h && light <= 30min && deep = 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @bluetooth_speaker.set_sink() ;
}
====
now => * => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @security-camera.set_power(power=enum(off)) ;
}
====
@omlet.incomingmessage, ((type = enum(picture) && message = "23456789") || (type = enum(text) && message =~ "0123456788")) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (message = "9876543210") , v_type := type, v_message := message => @thecatapi.get(), (link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @builtin.get_random, (number <= 55 || number < 7) , v_number := number => notify
;;
Main() {
    now => @builtin.get_random(), (number > 11) , v_number := number => notify;
}
====
now => @almond_couch_market.search, ((title = "0123456788" && price <= 7) || (price < 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "0123456788" && price <= 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @almond_couch_market.post, ((title = "0123456789" && price >= 11 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price = 55 && contact = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @youtube.source(), ((channel_title = "0123456789" && title = "0123456788")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_couch_market.post(title=v_video_id, contact=v_channel_id, price=42) ;
}
====
now => @dropbox.open, ((file_name =~ "9876543210" && url =~ "0123456789") || url =~ "23456789") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="23456789"), (file_name =~ "23456789") , v_url := url => notify;
}
====
now => @holidays.next_uk_holiday_event, (date = makeDate(1487030400000) && summary =~ "12345" && description =~ "12345") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((summary =~ "23456789" && description = "0123456788")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@xkcd.new_whatif, true , v_title := title, v_link := link => *
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @twitter.unfollow(user_name=v_title) ;
}
====
* => @almond_bike_market.detail, true  => *
;;
Main() {
    now => @almond_bike_market.detail(id="0123456789"), true  => @bluetooth_speaker.set_sink() ;
}
====
now => @thecatapi.get, (count = 11 || image_id =~ "0123456788" || (count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count < 42 || (image_id =~ "9876543210" && count < 55 && link = "http://www.google.com"^^tt:url) || image_id =~ "ABCDEFGHIJ") , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (count >= 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@security-camera.new_event, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (start_time = makeDate(1462320000000) && has_motion = false && has_person = false) || (has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1462320000000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && has_sound = false))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @onedrive.create_file(file_name="9876543210", contents="9876543210") ;
}
====
@bodytrace.source, (weight >= 155lb || (time = makeDate(1462320000000) && weight > 155lb) || weight = 82kg) , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight > 155lb)) , v_time := time, v_weight := weight => notify;
}
====
* => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210")) 
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), (((away_name =~ "0123456789" && game_inning =~ "0123456789") || (away_name =~ "23456789" && home_name =~ "0123456788" && game_status = enum(halftime) && scheduled_time = "12345" && game_inning = "23456789" && away_runs > 42 && home_runs >= 14) || (other_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && away_name = "ABCDEFGHIJ" && scheduled_time =~ "0123456788" && home_runs > 42))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @facebook.post_picture(picture_url=v_home_name, caption=v_home_name) ;
}
====
* => @omlet.send_to, (message =~ "12345" || (to = "bob"^^tt:username && message = "0123456789") || message =~ "0123456789") 
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), ((watched_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && home_name = "23456789" && scheduled_time = makeDate(1462320000000) && away_points > 11)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @omlet.send_to(to=v_away_name, message=v_away_name) ;
}
====
now => * => @twitter.sink, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
now => * => @slack.send, (channel = "funny"^^tt:hashtag || (channel = "funny"^^tt:hashtag && message = "12345")) 
;;
Main() {
    now => @nasa.rover(), true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @slack.send(channel=v_camera_used, message="ABCDEFGHIJ") ;
}
====
* => @bing.web_search, (query =~ "ABCDEFGHIJ" || title =~ "12345") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @bing.web_search(query=v_title), (((query =~ "23456789" && title =~ "12345") || (title = "0123456788" && description = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify;
}
====
@wall_street_journal.markets, true , v_title := title, v_link := link => * => *
;;
Main() {
    @wall_street_journal.markets(), true , v_title := title, v_link := link => @uber.price_estimate(start=$context.location.home, end=$context.location.current_location), (((uber_type = "ABCDEFGHIJ" && low_estimate > 42 && high_estimate >= 42 && surge <= 11) || (end = $context.location.current_location && uber_type =~ "ABCDEFGHIJ" && low_estimate < 11 && high_estimate >= 11 && surge > 11 && duration <= 30min && distance >= 42cm) || (low_estimate > 42 && high_estimate <= 55 && duration < 30min))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@rss.new_post, title =~ "12345" , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (((title = "12345" && link = "http://www.google.com"^^tt:url) || title = "9876543210")) , v_title := title, v_link := link => @car.honk_horn() ;
}
====
@nasa.asteroid, (asteroid_id = 14 && name = "9876543210" && estimated_diameter_min < 1000m && estimated_diameter_max <= 1000m && relative_velocity = 5kmph && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), (((asteroid_id <= 42 && name =~ "23456789" && estimated_diameter_min >= 1000m && relative_velocity < 25mph && closest_distance_to_earth = 1000m) || (asteroid_id <= 11 && dangerous = false && relative_velocity > 5kmph) || (asteroid_id >= 14 && name = "9876543210" && estimated_diameter_min > 1000m && estimated_diameter_max = 42cm && relative_velocity = 5kmph && orbiting_body =~ "23456789"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.debug_log(message=v_orbiting_body) ;
}
====
@nasa.asteroid, ((asteroid_id < 42 && name =~ "0123456789" && estimated_diameter_min > 1000m && orbiting_body = "12345") || (name = "0123456789" && estimated_diameter_min = 1000m && estimated_diameter_max < 42cm && relative_velocity > 25mph && closest_distance_to_earth >= 42cm) || (asteroid_id > 14 && name = "0123456789" && estimated_diameter_max < 42cm && dangerous = true && relative_velocity <= 5kmph && closest_distance_to_earth < 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 11 && name =~ "0123456788" && estimated_diameter_max < 42cm && dangerous = false && relative_velocity < 25mph && closest_distance_to_earth <= 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @security-camera.set_power(power=enum(on)) ;
}
====
now => @almond_couch_market.search, (title =~ "9876543210" || title = "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (price > 42) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_bike_market.monitor, (info = "12345" || (info =~ "0123456789" && price > 11) || price <= 42)  => *
;;
Main() {
    @almond_bike_market.monitor(info="ABCDEFGHIJ"), true  => @fitbit.recordweight(weight=82kg) ;
}
====
* => @almond_couch_market.post, (price >= 11 || (title =~ "ABCDEFGHIJ" && price <= 11 && contact = "+16501234567"^^tt:phone_number) || price < 42 || (title = "9876543210" && price = 42)) 
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @almond_couch_market.post(title=v_title, price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), (((message = "23456789" && poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && message = "23456789" && poster =~ "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.color_loop() ;
}
====
now => @lg_webos_tv.set_volume, percent <= 11 
;;
Main() {
    now => @lg_webos_tv.set_volume(percent=14) ;
}
====
* => @lg_webos_tv.unmute, true 
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @lg_webos_tv.unmute() ;
}
====
@youtube.source, ((channel_id = "ABCDEFGHIJ" && title =~ "0123456789" && video_url = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456788" && channel_title = "9876543210") || (channel_title =~ "9876543210" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((title =~ "0123456789" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => @builtin.hello() ;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && place = $context.location.work && weight >= 155lb && bmi > 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight <= 82kg && leanMass >= 14 && bmi <= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @gmail.send_picture, ((subject =~ "12345" && message = "0123456789") || (to = "alice@gmail.com"^^tt:email_address && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="23456789", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@almond_couch_market.monitor, (title =~ "12345" && price = 11 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((price > 14 || price >= 42)) , v_title := title, v_price := price, v_contact := contact => @gmail.send_email(to=v_title, subject=v_title, message="ABCDEFGHIJ") ;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.set_power, true 
;;
Main() {
    now => @almond_dates.search(), ((interest = "12345" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.set_power(power=enum(on)) ;
}
====
@almond_bike_market.monitor, (price > 14 || price < 42 || info = "ABCDEFGHIJ")  => * => @linkedin.share, status = "9876543210" 
;;
Main() {
    @almond_bike_market.monitor(info="0123456789"), (info = "ABCDEFGHIJ")  => @linkedin.share(status="23456789") ;
}
====
now => @security-camera.get_url, true , v_link := link => *
;;
Main() {
    now => @security-camera.get_url(), ((link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_link := link => notify;
}
====
@phdcomics.new_post, (link = "http://www.google.com"^^tt:url || title = "12345" || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456789" || title = "0123456789")) , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
@scale.source, (time = makeDate(1487030400000) && value > 155lb) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @holidays.next_us_holiday_event(), ((description = "ABCDEFGHIJ" || (summary = "12345" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@thermostat.humidity, (value >= 11 || (time = makeDate(1487030400000) && value < 42)) , v_time := time, v_value := value => *
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1487030400000) && value > 7)) , v_time := time, v_value := value => notify;
}
====
* => @twitter.search_by_hashtag, (query_hashtag = "cat"^^tt:hashtag && text = "9876543210" && contains(urls, "http://www.google.com"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, in_reply_to="alice"^^tt:username), (count = 42) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @bing.web_search, true , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="9876543210"), (((title = "23456789" && link = "http://www.google.com"^^tt:url) || query = "12345")) , v_title := title, v_description := description, v_link := link => notify;
}
====
now => @twitter.follow, user_name = "bob"^^tt:username 
;;
Main() {
    now => @twitter.follow(user_name="alice"^^tt:username) ;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), (((title = "12345" && price < 42 && contact = "+15551234567"^^tt:phone_number) || (price >= 7 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @onedrive.create_file(file_name="ABCDEFGHIJ", contents="23456789") ;
}
====
now => @weatherapi.sunrise, (location = $context.location.home && sunrise = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => *
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @tumblr-blog.post_text, true 
;;
Main() {
    @phone.gps(), ((altitude >= 42cm && bearing = 55 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @tumblr-blog.post_text(title="0123456788", body="12345") ;
}
====
@nasa.asteroid, (asteroid_id >= 55 && name =~ "0123456788" && estimated_diameter_min > 1000m && dangerous = false && closest_distance_to_earth > 42cm) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id >= 7 && estimated_diameter_min > 42cm && estimated_diameter_max >= 1000m && dangerous = false && relative_velocity > 25mph && closest_distance_to_earth >= 42cm && orbiting_body =~ "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.get_gps(), ((bearing >= 7 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@instagram.new_picture, link = "http://www.abc.def"^^tt:url , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((caption =~ "9876543210" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.get_date(), true , v_date := date => notify;
}
====
@activity-tracker.getmove, ((day =~ "23456789" && distance > 1000m && activeTime >= 30min) || (distance >= 42cm && steps > 42 && inactiveTime >= 30min) || (day =~ "23456789" && distance < 42cm && steps >= 11 && activeTime <= 2h) || (distance >= 42cm && steps = 7 && inactiveTime >= 30min) || (day = "0123456788" && distance > 42cm && steps > 42 && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "12345" && distance <= 42cm && steps = 7 && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @dropbox.get_space_usage(), (((used >= 5KB && total < 20MB) || used >= 20MB)) , v_used := used, v_total := total => notify;
}
====
now => * => @car.honk_horn, true 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @car.honk_horn() ;
}
====
now => @almond_couch_market.search, price < 14 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "ABCDEFGHIJ" && price > 7) || (title = "12345" && price >= 14 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @builtin.debug_log(message="0123456788") ;
}
====
* => @holidays.next_us_holiday_event, (description =~ "23456789" || (date = makeDate(1462320000000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ") || (summary =~ "0123456788" && description = "0123456788") || description =~ "12345" || (summary =~ "0123456789" && description = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 7 && estimated_diameter_min >= 1000m && estimated_diameter_max > 1000m && relative_velocity = 25mph) || (name = "0123456789" && estimated_diameter_min > 42cm && dangerous = true && relative_velocity >= 5kmph && closest_distance_to_earth > 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@builtin.at, time = makeTime(15,0)  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @giphy.get_tag(tag="lol"^^tt:hashtag), (tag = "lol"^^tt:hashtag) , v_picture_url := picture_url => notify;
}
====
now => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && subject = "0123456789") 
;;
Main() {
    now => @phone.get_gps(), ((speed < 5kmph || (location = $context.location.current_location && speed >= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456788", message="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@rss.new_post, (title = "9876543210" || (title = "0123456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@sportradar.nba_team, (away_points < 7 && home_points > 7 && result = enum(unclosed)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), ((watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && scheduled_time = makeDate(1462320000000) && away_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count <= 55 && text =~ "9876543210" && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=42, from="bob"^^tt:username), (((query_hashtag = "lol"^^tt:hashtag && count <= 7 && text =~ "ABCDEFGHIJ") || (text =~ "9876543210" && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count <= 14 && in_reply_to = "bob"^^tt:username) || (count < 55 && contains(hashtags, "lol"^^tt:hashtag)) || (query_hashtag = "cat"^^tt:hashtag && count <= 7 && text =~ "0123456788" && contains(hashtags, "lol"^^tt:hashtag) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @uber.time_estimate, ((uber_type = "0123456788" && estimate >= 30min) || (uber_type = "9876543210" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((uber_type =~ "0123456788" || (start = $context.location.home && estimate > 2h) || (uber_type = "9876543210" && estimate >= 30min))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @bing.image_search, (query = "ABCDEFGHIJ" && title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && width > 11 && height <= 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="0123456789"), ((away_alias =~ "0123456789" && home_alias = "0123456788" && away_name =~ "23456789" && home_name = "ABCDEFGHIJ" && scheduled_time = "12345" && away_points > 42 && home_points > 42)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @bing.image_search(query=v_away_name), ((query = "23456789" && width > 42 && height < 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @icalendar.list_events, ((summary =~ "0123456789" && description = "12345" && sequence > 55 && location =~ "0123456789") || summary =~ "9876543210") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration <= 2h && rem = 2h && deep > 2h) || (duration < 2h && rem > 30min && deep >= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @icalendar.list_events(), (((summary =~ "ABCDEFGHIJ" && sequence <= 55 && organizer = "0123456789" && location = "0123456788") || (sequence < 14 && location =~ "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @dropbox.move, (old_name =~ "12345" || new_name =~ "9876543210" || new_name = "23456789") 
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), (((ask_price <= 14 && bid_price <= 14) || (stock_id = "aapl"^^tt:stock_id("Apple") && ask_price > 11))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @dropbox.move(old_name=v_company_name, new_name="ABCDEFGHIJ") ;
}
====
now => * => @google_drive.create_new_drive_file, file_name = "0123456789" 
;;
Main() {
    now => @ytranslate.detect_language(text="ABCDEFGHIJ"), (text =~ "0123456789") , v_detected_language := detected_language => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
* => @builtin.debug_log, (message = "9876543210" || message =~ "0123456788") 
;;
Main() {
    @washington_post.new_blog_post(section=enum(politics)), (((section = enum(powerpost) && title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || (section = enum(post_partisan) && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @builtin.debug_log(message=v_title) ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && label = "12345") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), (((watched_is_home = true && away_name =~ "0123456788" && game_status = enum(inprogress) && scheduled_time =~ "0123456788" && away_runs > 14 && home_runs >= 55) || (away_name = "ABCDEFGHIJ" && home_runs > 7))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @gmail.get_latest(), ((subject = "9876543210" || from = "alice@gmail.com"^^tt:email_address || label =~ "0123456789" || label = "12345" || (from = "bob@stanford.edu"^^tt:email_address && label =~ "ABCDEFGHIJ") || label =~ "9876543210")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@heartrate-monitor.getheartrate, (heartrate > 7 || time = makeDate(1487030400000) || (place = $context.location.work && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) || place = $context.location.work || heartrate > 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @fitbit.getbody(), (((weight < 82kg && bmi = 11 && fat < 7) || (weight < 82kg && bmi <= 55) || (bmi < 55 && fat < 14))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @activity-tracker.getmove(), ((distance <= 1000m && activeTime < 2h && inactiveTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @imgflip.generate(template=v_day, text_top=v_day, text_bottom=v_day), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (text_top = "12345" && name = "9876543210"))) , v_name := name, v_picture_url := picture_url => @car.honk_horn() ;
}
====
now => @fitbit.getsteps, steps = 14 , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), (((date = makeDate(1462320000000) && steps > 55) || steps = 11 || steps < 55 || steps > 42)) , v_date := date, v_steps := steps => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
* => @wall_street_journal.get_opinions, (title =~ "12345" || title =~ "12345") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "0123456789" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@nasa.asteroid, (estimated_diameter_min <= 1000m && estimated_diameter_max < 42cm && dangerous = true && relative_velocity <= 5kmph) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => @heatpad.set_power, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 7 && name = "23456789" && estimated_diameter_min < 1000m && dangerous = true && closest_distance_to_earth > 1000m && orbiting_body = "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @heatpad.set_power(power=enum(on)) ;
}
====
@tumblr.new_photo, (link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => *
;;
Main() {
    @tumblr.new_photo(blog_name="9876543210"), ((contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture) || (caption = "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || blog_name = "0123456788" || (blog_name =~ "12345" && date = makeDate(1462320000000) && caption = "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @uber.price_estimate, (low_estimate >= 14 && surge > 11 && duration >= 30min) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=$context.location.work), ((end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "12345" && low_estimate > 11 && high_estimate >= 55 && currency_code =~ "23456789" && duration > 2h)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), ((duration = 30min && light > 2h && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @light-bulb.alert_long() ;
}
====
* => @ytranslate.translate, (text =~ "9876543210" || (source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((blog_name =~ "ABCDEFGHIJ" && title = "0123456789" && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @ytranslate.translate(target_language=v_body, text=v_body), (text =~ "23456789") , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @builtin.get_time, (time = makeDate(1462320000000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time => *
;;
Main() {
    now => @builtin.get_time(), true , v_time := time => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
@phdcomics.new_post, title =~ "12345" , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @gmail.send_picture(to=v_title, subject=v_title, message=v_title, picture_url=v_picture_url) ;
}
====
now => * => @omlet.send_to, true 
;;
Main() {
    now => @omlet.send_to(to="alice"^^tt:username, message="12345") ;
}
====
now => * => @linkedin.share, true 
;;
Main() {
    now => @dropbox.get_space_usage(), ((used = 20MB || total >= 5KB)) , v_used := used, v_total := total => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
* => @security-camera.set_power, true 
;;
Main() {
    @fitness-tracker.getbody(), ((weight <= 155lb && leanMass > 42 && bodyFat >= 7 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @security-camera.set_power(power=enum(off)) ;
}
====
* => * => @phone.call, true 
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && weight >= 82kg && bodyFat = 14 && bmi >= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @bing.web_search(query="ABCDEFGHIJ"), (((query =~ "ABCDEFGHIJ" && title = "0123456788") || (query =~ "9876543210" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_description := description, v_link := link => @phone.call(number=v_description) ;
}
====
@tumblr.new_photo, (contains(tags, "lol"^^tt:hashtag) || (date = makeDate(1462320000000) && link = "http://www.google.com"^^tt:url)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify
;;
Main() {
    @tumblr.new_photo(blog_name="0123456789"), (((blog_name =~ "23456789" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (blog_name = "0123456789" && date = makeDate(1487030400000) && link = "http://www.google.com"^^tt:url && contains(tags, "lol"^^tt:hashtag) && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify;
}
====
@almond_couch_market.monitor, (price >= 11 || contact = "+15551234567"^^tt:phone_number || price > 7 || (title = "0123456788" && price > 11 && contact = "+16501234567"^^tt:phone_number) || price = 11 || (title = "0123456789" && price >= 42) || (title = "23456789" && price = 55)) , v_title := title, v_price := price, v_contact := contact => @parklon_heatpad.set_power, power = enum(off) 
;;
Main() {
    @almond_couch_market.monitor(), ((price > 11 || price = 7 || contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@phdcomics.new_post, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "9876543210" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @builtin.get_time(), true , v_time := time => notify;
}
====
* => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject = "12345") 
;;
Main() {
    @activity-tracker.getmove(), ((distance <= 1000m && activeTime >= 30min && inactiveTime <= 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @gmail.send_email(to=v_day, subject=v_day, message=v_day) ;
}
====
* => @thecatapi.get, (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @icalendar.event_begin(), (((summary = "0123456789" && description = "ABCDEFGHIJ" && sequence <= 55) || (start_date = makeDate(1487030400000) && summary = "23456789" && description = "0123456788" && location =~ "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thecatapi.get(), ((count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @omlet.send_picture, ((to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ") || caption = "9876543210") 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = $context.location.work && wind_speed = 25mph && cloudiness > 11 && weather =~ "0123456789" && status = enum(cloudy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @omlet.send_picture(picture_url=v_weather, caption=v_weather, to="bob"^^tt:username) ;
}
====
now => @nasa.rover, ((date_taken = makeDate(1462320000000) && count > 55) || (date_taken = makeDate(1462320000000) && camera_used =~ "ABCDEFGHIJ")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    now => @nasa.rover(), (((count < 55 && camera_used =~ "9876543210") || (date_taken = makeDate(1487030400000) && count <= 11) || (date_taken = makeDate(1487030400000) && count >= 14 && camera_used = "ABCDEFGHIJ") || count >= 7 || (date_taken = makeDate(1462320000000) && count <= 7))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
now => @thecatapi.get, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "12345" && count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count < 55)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @almond_couch_market.search, (price <= 42 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price > 14 || (title =~ "12345" && price <= 7 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @almond_dates.post(interest=v_title, message=v_title, poster=v_title, phone=v_title) ;
}
====
* => @almond_couch_market.search, ((title =~ "0123456789" && price >= 55) || (price < 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance > 1000m && inactiveTime > 30min && calories > 500kcal) || (updateTime = makeDate(1462320000000) && steps <= 7 && activeTime < 2h && inactiveTime <= 30min))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_couch_market.search(), (((title =~ "12345" && price = 42 && contact = "+16501234567"^^tt:phone_number) || contact = "+16501234567"^^tt:phone_number || (title =~ "0123456788" && price < 7))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@weatherapi.monitor, (wind_speed >= 5kmph && humidity = 14 && cloudiness <= 11 && fog > 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = $context.location.home && temperature >= 22C && humidity > 55 && cloudiness < 42 && fog > 11 && weather = "12345")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @bluetooth_speaker.lower_volume() ;
}
====
@almond_dates.monitor, ((message =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || message = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "23456789" && message = "12345") || (interest =~ "9876543210" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @fitbit.recordweight, weight < 82kg 
;;
Main() {
    now => @imgflip.list(), ((name = "12345" || name =~ "9876543210")) , v_name := name, v_picture_url := picture_url => @fitbit.recordweight(weight=82kg) ;
}
====
* => @builtin.get_random_between, ((low = 7 && high >= 11 && random <= 11) || random >= 7 || (low > 55 && high <= 14 && random >= 14) || (low <= 14 && high <= 7) || (low > 42 && high > 7) || low >= 14) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=11, high=11), ((high <= 11 && random > 7)) , v_random := random => notify;
}
====
now => @giphy.get_tag, true , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
now => * => @almond_couch_market.post, price = 55 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title =~ "0123456788") , v_title := title, v_link := link, v_updated := updated => @almond_couch_market.post(title=v_title, contact=v_title, price=14) ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || weight < 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight > 82kg) , v_time := time, v_weight := weight => notify;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @reddit_front_page.newpost(), (category = "9876543210") , v_title := title, v_link := link, v_user := user, v_category := category => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@almond_dates.monitor, (phone = "+15551234567"^^tt:phone_number || (message = "0123456788" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@nasa.asteroid, (name =~ "0123456788" && estimated_diameter_min = 42cm && dangerous = true && closest_distance_to_earth < 1000m && orbiting_body = "0123456788") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((estimated_diameter_max >= 42cm && dangerous = false && relative_velocity = 5kmph && closest_distance_to_earth >= 1000m) || (asteroid_id <= 42 && name =~ "23456789" && estimated_diameter_min < 1000m && estimated_diameter_max > 42cm && closest_distance_to_earth >= 42cm && orbiting_body =~ "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_date(), (date = makeDate(1487030400000)) , v_date := date => notify;
}
====
@sportradar.ncaambb_team, ((away_name =~ "9876543210" && home_name =~ "ABCDEFGHIJ" && game_status = enum(closed)) || (other_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name =~ "0123456789" && away_points <= 14 && home_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => *
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), (((home_name = "0123456788" && home_points >= 7) || (away_name = "0123456788" && home_name =~ "0123456788" && scheduled_time = makeDate(1487030400000) && away_points >= 42 && home_points = 55))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @bluetooth_speaker.play_music() ;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), ((title =~ "0123456789" || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url && description =~ "12345") || (section = enum(world) && title =~ "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_description := description => @lg_webos_tv.raise_volume() ;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="ABCDEFGHIJ", text_bottom="9876543210"), ((template = "0123456789" && name = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @facebook.post, status =~ "0123456789" 
;;
Main() {
    @slack.receive(), ((channel = "funny"^^tt:hashtag && message = "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @facebook.post(status="9876543210") ;
}
====
* => @dropbox.move, (old_name =~ "12345" || new_name =~ "9876543210" || new_name = "23456789") 
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @dropbox.move(old_name=v_title, new_name=v_title) ;
}
====
now => @gmail.get_latest, subject =~ "12345" , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), ((subject = "ABCDEFGHIJ" && label =~ "0123456789")) , v_from := from, v_subject := subject, v_label := label => @security-camera.set_power(power=enum(on)) ;
}
====
@scale.source, (time = makeDate(1487030400000) && value >= 155lb) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), (value < 82kg) , v_time := time, v_value := value => @almond_bike_market.post(title="ABCDEFGHIJ", price=14, info="9876543210", poster="23456789") ;
}
====
@almond_dates.monitor, ((interest =~ "0123456789" && poster = "23456789") || (interest = "0123456789" && poster =~ "12345") || poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "23456789" && poster = "9876543210" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && weight <= 82kg) || weight < 155lb || (time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @lg_webos_tv.lower_volume() ;
}
====
@weatherapi.monitor, (temperature > 22C && humidity <= 55 && cloudiness <= 55 && fog > 42) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @fitbit.recordweight, (weight >= 155lb || weight >= 155lb) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = $context.location.current_location && temperature < 73F && wind_speed >= 25mph && humidity >= 55 && weather =~ "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @fitbit.recordweight(weight=155lb) ;
}
====
* => @gmail.send_email, (subject = "0123456789" || (to = "alice@gmail.com"^^tt:email_address && subject = "0123456789" && message = "ABCDEFGHIJ") || message =~ "23456789" || (to = "bob@stanford.edu"^^tt:email_address && message = "0123456789")) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((wind_speed < 25mph && cloudiness < 7 && fog >= 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.send_email(subject=v_weather, message=v_weather, to="alice@gmail.com"^^tt:email_address) ;
}
====
@almond_couch_market.monitor, contact = "+15551234567"^^tt:phone_number , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "ABCDEFGHIJ" && price >= 14 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @tumblr-blog.post_picture, caption = "0123456788" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @tumblr-blog.post_picture(caption=v_description, picture_url=v_summary) ;
}
====
now => * => @github.add_email, (email = "alice@gmail.com"^^tt:email_address || email = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    now => @linkedin.get_profile(), ((headline =~ "23456789" && contains(positions, "ABCDEFGHIJ") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @github.add_email(email=v_industry) ;
}
====
@almond_bike_market.monitor, (price > 11 || price < 11 || info =~ "0123456788")  => * => notify
;;
Main() {
    @almond_bike_market.monitor(info="9876543210", price=14), (info = "0123456788")  => notify;
}
====
@jawbone_up.getmove, ((day = "ABCDEFGHIJ" && steps < 7 && activeTime <= 2h && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && distance <= 1000m && inactiveTime > 30min) || (day = "0123456789" && distance < 1000m && steps <= 55 && activeTime < 30min && inactiveTime <= 2h && calories > 500kcal) || (updateTime = makeDate(1462320000000) && activeTime > 30min && inactiveTime >= 30min && calories <= 500kcal) || (day = "ABCDEFGHIJ" && distance < 1000m && steps < 55 && activeTime >= 2h && inactiveTime > 2h && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1462320000000) && distance >= 1000m && steps <= 11 && activeTime >= 2h && inactiveTime < 30min && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@omlet.newmessage, true , v_feed := feed, v_type := type, v_message := message => * => @almond_dates.post, (message = "23456789" || message = "9876543210") 
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @almond_dates.post(interest=v_message, message=v_message, poster=v_message, phone="+15551234567"^^tt:phone_number) ;
}
====
now => @dropbox.create_new_folder, (folder_name = "0123456789" || folder_name = "23456789" || folder_name = "ABCDEFGHIJ" || folder_name = "ABCDEFGHIJ") 
;;
Main() {
    now => @dropbox.create_new_folder(folder_name="0123456788") ;
}
====
@scale.source, (value > 82kg || value <= 82kg) , v_time := time, v_value := value => @car.honk_horn, true 
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => @car.honk_horn() ;
}
====
* => * => @facebook.post, true 
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "ABCDEFGHIJ" && price >= 14) || (title = "0123456789" && price < 7 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @facebook.post(status="0123456789") ;
}
====
now => * => @almond_couch_market.post, (title = "ABCDEFGHIJ" && price = 11) 
;;
Main() {
    now => @almond_dates.search(), ((interest = "0123456788" && message = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_couch_market.post(title=v_poster, contact=v_poster, price=7) ;
}
====
@phone.gps, (altitude > 1000m && bearing > 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((bearing < 11 && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && sequence >= 7 && organizer =~ "ABCDEFGHIJ" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @almond_bike_market.search, (info = "0123456789" && price < 14)  => notify
;;
Main() {
    now => @almond_bike_market.search(info="23456789"), true  => notify;
}
====
* => @uber.time_estimate, estimate < 30min , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @wall_street_journal.technology(), true , v_title := title, v_link := link => @uber.time_estimate(start=$context.location.current_location), (uber_type = "23456789") , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @speaker.set_volume, (percent > 42 || percent >= 7) 
;;
Main() {
    @holidays.us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @speaker.set_volume(percent=42) ;
}
====
now => @nasa.apod, (title =~ "9876543210" && description =~ "0123456789") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (((title = "0123456788" && description = "0123456789") || (description =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@bodytrace.source, weight < 82kg , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @holidays.next_uk_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @almond_couch_market.post, ((title = "0123456789" && price >= 14) || contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_couch_market.post(title="ABCDEFGHIJ", price=55, contact="+15551234567"^^tt:phone_number) ;
}
====
@slack.receive, (timestamp = "0123456788" && from = "alice"^^tt:username) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "23456789" || (channel = "funny"^^tt:hashtag && from = "bob"^^tt:username))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @builtin.get_random(), true , v_number := number => notify;
}
====
now => * => @almond_dates.post, ((interest =~ "0123456789" && message =~ "12345") || interest = "0123456789" || interest =~ "0123456788") 
;;
Main() {
    now => @phone.get_gps(), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude <= 1000m && bearing <= 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_dates.post(interest="9876543210", message="ABCDEFGHIJ", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @google_drive.create_new_drive_file, file_name = "23456789" 
;;
Main() {
    @thermostat.temperature(), (((time = makeDate(1487030400000) && value > 22C) || value > 22C)) , v_time := time, v_value := value => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
@yahoofinance.stock_div, (company_name = "12345" && yield <= 7 && div <= 42) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((div > 42 && ex_div_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
@thermostat.temperature, (value >= 22C || value <= 22C) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), ((from = "alice"^^tt:username && message = "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @google_drive.create_new_drive_file(file_name=v_timestamp) ;
}
====
* => @almond_bike_market.post, info =~ "23456789" 
;;
Main() {
    @github.new_issue_comment(repo_name="0123456788"), (((repo_name =~ "23456789" && body = "ABCDEFGHIJ") || (repo_name = "0123456789" && issue_number >= 7 && body = "23456789" && time = makeDate(1487030400000)))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @almond_bike_market.post(price=v_issue_number, title="0123456788", info="23456789", poster="23456789") ;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.home, end=$context.location.home), ((high_estimate < 42 && currency_code = "23456789" && surge >= 14 && duration > 2h && distance < 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@phdcomics.new_post, ((title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @google_drive.create_new_drive_file, (file_name =~ "23456789" || file_name = "0123456788") 
;;
Main() {
    @phdcomics.new_post(), ((title =~ "9876543210" || title =~ "9876543210" || (title =~ "12345" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name=v_title) ;
}
====
@google_drive.new_drive_file, (file_name = "12345" || file_name = "0123456789" || file_name = "23456789" || file_name = "23456789" || file_name =~ "23456789" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "12345" || file_name = "ABCDEFGHIJ")) , v_file_name := file_name => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && subject = "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@slack.receive, ((timestamp =~ "9876543210" && from = "alice"^^tt:username && message = "ABCDEFGHIJ") || (channel = "cat"^^tt:hashtag && timestamp = "23456789" && message = "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (channel = "cat"^^tt:hashtag) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @almond_dates.search(), ((interest =~ "9876543210" && message =~ "23456789" && poster =~ "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @instagram.get_pictures, ((count < 11 && caption =~ "0123456788" && link = "http://www.google.com"^^tt:url) || (count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "lol"^^tt:hashtag)) || (count < 7 && caption =~ "ABCDEFGHIJ")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(), (((count <= 14 && link = "http://www.google.com"^^tt:url && filter =~ "12345" && contains(hashtags, "lol"^^tt:hashtag)) || (count > 14 && caption = "9876543210" && filter =~ "9876543210") || (count > 11 && caption =~ "12345" && link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @onedrive.upload_picture(file_name=v_filter, picture_url=v_picture_url) ;
}
====
* => @thermostat.set_target_temperature, value > 73F 
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @thermostat.set_target_temperature(value=22C) ;
}
====
@heartrate-monitor.getheartrate, (heartrate > 7 || place = $context.location.current_location) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @weatherapi.sunrise(location=v_place), ((sunrise = makeDate(1487030400000) || sunset = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @speaker.raise_volume, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.work && heartrate <= 42)) , v_time := time, v_place := place, v_heartrate := heartrate => @speaker.raise_volume() ;
}
====
now => @almond_bike_market.ask, (id =~ "12345" || property = "0123456789")  => *
;;
Main() {
    now => @almond_bike_market.ask(id="ABCDEFGHIJ", property="ABCDEFGHIJ"), true  => notify;
}
====
now => @nasa.apod, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (description = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title = "9876543210" || (title =~ "0123456788" && description =~ "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    now => @nasa.apod(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (description =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => @fitbit.getbody(), (bmi < 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@slack.receive, (timestamp =~ "0123456789" && from = "alice"^^tt:username && message = "9876543210") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "9876543210" && from = "alice"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @imgflip.generate(template=v_message, text_bottom=v_message, text_top="23456789"), ((text_top = "0123456788" && text_bottom =~ "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && summary =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), (stock_id = "msft"^^tt:stock_id("Microsoft")) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @icalendar.list_events(), (location =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@thermostat.temperature, value < 22C , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), (((time = makeDate(1462320000000) && value > 73F) || (time = makeDate(1487030400000) && value > 73F))) , v_time := time, v_value := value => @builtin.get_random_between(low=14, high=55), ((low < 14 && high > 14 && random <= 55)) , v_random := random => notify;
}
====
* => @linkedin.share, status = "12345" 
;;
Main() {
    @activity-tracker.getmove(), ((distance < 1000m && inactiveTime <= 2h && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @linkedin.share(status=v_day) ;
}
====
@security-camera.new_event, (has_motion = true || has_motion = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (((has_motion = true && has_person = true) || (has_motion = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @thermostat.get_hvac_state(), ((mode = enum(cool) || mode = enum(heat))) , v_mode := mode, v_state := state => notify;
}
====
@nasa.asteroid, (asteroid_id > 42 && name =~ "0123456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id = 7 && dangerous = true && relative_velocity >= 5kmph && closest_distance_to_earth <= 42cm && orbiting_body =~ "12345") || (asteroid_id < 14 && estimated_diameter_max >= 1000m && relative_velocity <= 25mph))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => @gmail.send_email, message =~ "23456789" 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature = 22C && wind_speed >= 5kmph && humidity < 55 && status = enum(cloudy)) || (temperature < 22C && wind_speed < 25mph && humidity <= 7 && cloudiness >= 7 && fog >= 7 && status = enum(raining)) || (temperature > 73F && wind_speed = 25mph && cloudiness <= 7 && fog > 42 && weather =~ "9876543210"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.send_email(to=v_weather, subject=v_weather, message=v_weather) ;
}
====
* => @almond_dates.post, (interest =~ "23456789" && poster = "23456789") 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "12345" && message =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_dates.post(message=v_message, poster=v_interest, phone=v_poster, interest="0123456789") ;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1487030400000) && duration = 2h && rem < 2h && light <= 2h && deep <= 30min) || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && light > 2h && deep < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((duration < 2h && light > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((has_sound = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @twitter.send_direct_message, (message = "ABCDEFGHIJ" || message =~ "9876543210") 
;;
Main() {
    now => @builtin.get_random_between(low=55, high=55), ((low < 7 && high = 11)) , v_random := random => @twitter.send_direct_message(to="bob"^^tt:username, message="0123456788") ;
}
====
* => @youtube.channels_by_category, ((channel_id =~ "23456789" && title = "0123456789" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "9876543210" && title =~ "ABCDEFGHIJ")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(sports)), (((channel_id =~ "12345" && description = "ABCDEFGHIJ") || (title =~ "9876543210" && description =~ "9876543210") || (category_id = enum(from_tv) && channel_id = "9876543210" && title = "12345" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @lg_webos_tv.mute() ;
}
====
@twitter.direct_message, true , v_from := from, v_message := message, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @gmail.get_latest, true , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @gmail.receive_email(), (subject =~ "9876543210") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @gmail.get_latest(), ((subject = "0123456789" && label = "0123456788")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @linkedin.get_profile, (headline =~ "ABCDEFGHIJ" && industry = "0123456789" && specialties = "23456789") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="chc"^^sportradar:mlb_team("Chicago Cubs")), ((watched_is_home = false && home_name = "ABCDEFGHIJ" && game_status = enum(halftime) && scheduled_time = "0123456788" && game_inning =~ "9876543210" && away_runs = 42)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @linkedin.get_profile(), ((formatted_name = "0123456788" && headline =~ "23456789" && industry = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @phone.get_gps, (altitude = 1000m && bearing < 42 && speed >= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @phone.get_gps(), ((altitude >= 42cm && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && sequence < 42 && organizer = "9876543210" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @builtin.hello() ;
}
====
now => * => @thermostat.set_minmax_temperature, ((low <= 22C && high > 73F) || (low <= 22C && high > 73F) || low <= 73F) 
;;
Main() {
    now => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
@activity-tracker.getmove, (day =~ "23456789" && steps = 42 && activeTime < 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "0123456788" && distance > 1000m && steps < 7 && activeTime < 2h && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @security-camera.get_url(), true , v_link := link => @speaker.set_sink() ;
}
====
* => @linkedin.share, status =~ "0123456788" 
;;
Main() {
    @xkcd.new_comic(), ((number < 7 && title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @linkedin.share(status="12345") ;
}
====
* => @linkedin.get_profile, ((formatted_name = "ABCDEFGHIJ" && headline =~ "ABCDEFGHIJ" && specialties =~ "23456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "23456789" && industry = "9876543210" && specialties = "9876543210" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.google.com"^^tt:url && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @linkedin.get_profile(), (((formatted_name = "0123456788" && headline =~ "23456789") || (formatted_name = "23456789" && contains(positions, "0123456789")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, (distance = 1000m && steps > 11 && inactiveTime = 2h && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((steps = 55 && inactiveTime <= 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@phdcomics.new_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @dropbox.list_folder(folder_name="23456789"), (((is_folder = false && file_size <= 5KB && full_path = "23456789") || (folder_name = "0123456789" && file_name =~ "23456789" && is_folder = true && file_size < 5KB && full_path = "12345") || file_size >= 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @almond_dates.post(message=v_file_name, poster=v_file_name, phone=v_file_name, interest="12345") ;
}
====
@yahoofinance.stock_div, (stock_id = "aapl"^^tt:stock_id("Apple") && yield <= 55 && div >= 11 && pay_date = makeDate(1487030400000)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name = "12345" && div < 14 && ex_div_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
* => @builtin.configure, true 
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), (((game_status = enum(halftime) && scheduled_time = makeDate(1462320000000)) || (watched_is_home = true && home_name = "9876543210") || (watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_name =~ "9876543210" && home_name = "ABCDEFGHIJ"))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @builtin.configure(device="scale"^^tt:device("my scale")) ;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((sequence = 42 || (end_date = makeDate(1462320000000) && description =~ "12345" && sequence > 7) || (end_date = makeDate(1487030400000) && description = "0123456788" && location = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @onedrive.upload_picture(file_name=v_summary, picture_url=v_organizer) ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration <= 30min && rem < 30min && light > 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && rem <= 30min && light <= 2h && deep = 2h) || (time = makeDate(1487030400000) && duration >= 30min && light < 2h && deep >= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @uber.price_estimate(start=v_place, end=v_place), (((low_estimate >= 14 && currency_code =~ "0123456789" && surge >= 14 && duration < 30min && distance < 1000m) || (start = $context.location.home && low_estimate < 42 && high_estimate >= 55 && currency_code =~ "0123456789" && surge < 55))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => * => @omlet.send_picture, (to = "bob"^^tt:username && caption =~ "ABCDEFGHIJ") 
;;
Main() {
    @almond_bike_market.monitor(price=14), ((info =~ "12345" && price > 14))  => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
@fitness-tracker.getbody, ((weight > 82kg && leanMass <= 7 && bodyFat < 14 && bmi <= 14) || (weight < 155lb && leanMass < 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && weight <= 82kg && bodyFat <= 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
now => * => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption = "23456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => @facebook.post_picture(picture_url=v_description, caption=v_title) ;
}
====
* => @ytranslate.detect_language, text =~ "0123456788" , v_detected_language := detected_language => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), (((watched_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && home_name =~ "0123456788" && home_points = 42) || (watched_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && other_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && away_name =~ "ABCDEFGHIJ" && home_name =~ "12345" && away_points < 11 && home_points < 55) || (watched_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && other_team_abbr = "sta"^^sportradar:ncaafb_team("Stanford Cardinals") && home_name =~ "0123456788" && game_status = enum(inprogress) && scheduled_time = makeDate(1487030400000) && away_points < 14 && home_points > 55))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @ytranslate.detect_language(text=v_away_name), true , v_detected_language := detected_language => notify;
}
====
@rss.new_post, title = "12345" , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (title =~ "0123456788") , v_title := title, v_link := link => @fitbit.getbody(), ((bmi >= 14 && fat >= 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @imgflip.list, (name =~ "23456789" || name =~ "9876543210") , v_name := name, v_picture_url := picture_url => @thermostat.set_minmax_temperature, (high < 22C || (low >= 22C && high < 73F) || low < 73F) 
;;
Main() {
    now => @imgflip.list(), ((name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=22C, high=73F) ;
}
====
* => @almond_couch_market.post, price >= 14 
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => @almond_couch_market.post(title="12345", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @thecatapi.get, ((count >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (((image_id = "12345" && count = 42 && link = "http://www.abc.def"^^tt:url) || (image_id =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || (image_id =~ "9876543210" && count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || count >= 55)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @dropbox.create_new_folder, folder_name = "0123456789" 
;;
Main() {
    now => @instagram.get_pictures(count=42), ((count < 14 && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @dropbox.create_new_folder(folder_name=v_filter) ;
}
====
* => @almond_couch_market.post, (price >= 55 || title =~ "23456789" || title =~ "0123456788" || (title =~ "0123456789" && price < 7)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((location = $context.location.work && temperature <= 22C && cloudiness < 11 && fog <= 42 && weather =~ "0123456788") || (location = $context.location.current_location && temperature >= 22C && wind_speed <= 5kmph && humidity <= 55 && weather =~ "23456789" && status = enum(sunny)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.post(title=v_weather, price=v_fog, contact=v_weather) ;
}
====
* => @security-camera.get_snapshot, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => *
;;
Main() {
    @xkcd.new_comic(), ((number >= 11 && title =~ "12345")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @dropbox.move(old_name="23456789", new_name="9876543210") ;
}
====
@gmail.receive_primary_email, (subject = "ABCDEFGHIJ" && snippet =~ "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_primary_email(), (((from_name = "12345" && subject =~ "23456789" && date = makeDate(1487030400000) && snippet =~ "12345") || (from_name = "0123456788" && from_address = "alice@gmail.com"^^tt:email_address && date = makeDate(1462320000000) && contains(labels, "0123456789")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @giphy.get_tag(tag=v_snippet), true , v_picture_url := picture_url => notify;
}
====
* => @wall_street_journal.get_opinions, (title =~ "23456789" || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000)) || (title =~ "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @security-camera.new_event(), (has_person = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @wall_street_journal.get_opinions(), ((title = "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @linkedin.share, (status =~ "0123456788" || status =~ "ABCDEFGHIJ" || status =~ "9876543210") 
;;
Main() {
    now => @nasa.rover(), (((date_taken = makeDate(1487030400000) && count > 14 && camera_used =~ "0123456789") || (count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @linkedin.share(status="12345") ;
}
====
@tumblr.new_text, ((blog_name =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && contains(tags, "lol"^^tt:hashtag) && body = "ABCDEFGHIJ") || body = "ABCDEFGHIJ") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="12345"), (((blog_name =~ "9876543210" && date = makeDate(1487030400000) && title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && body = "ABCDEFGHIJ") || (blog_name =~ "9876543210" && title =~ "23456789"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @dropbox.open(file_name="23456789"), true , v_url := url => notify;
}
====
* => @fitbit.recordweight, (weight < 155lb || weight = 82kg || weight > 155lb) 
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1462320000000) && value > 7)) , v_time := time, v_value := value => @fitbit.recordweight(weight=82kg) ;
}
====
@onedrive.file_modified, file_name = "0123456788" , v_file_name := file_name => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    @onedrive.file_modified(), ((file_name =~ "9876543210" || file_name = "9876543210")) , v_file_name := file_name => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
@rss.new_post, true , v_title := title, v_link := link => @security-camera.set_power, true 
;;
Main() {
    @rss.new_post(), (title = "12345") , v_title := title, v_link := link => @security-camera.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, ((day = "0123456788" && steps >= 55 && activeTime >= 2h && calories = 500kcal) || (distance > 1000m && inactiveTime >= 2h && calories >= 500kcal) || (distance <= 1000m && inactiveTime > 2h) || (day =~ "0123456788" && distance <= 42cm && steps <= 7 && activeTime = 30min && inactiveTime >= 2h) || (day =~ "0123456788" && distance > 1000m && steps > 7 && activeTime = 2h && inactiveTime <= 30min && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && steps < 55 && activeTime < 2h && inactiveTime < 2h && calories >= 500kcal) || (distance >= 1000m && steps = 42 && activeTime >= 30min) || (steps > 55 && activeTime = 2h && inactiveTime <= 2h && calories > 500kcal) || (day = "ABCDEFGHIJ" && distance >= 1000m && steps >= 42 && activeTime >= 2h && inactiveTime <= 30min) || (updateTime = makeDate(1462320000000) && steps >= 42 && activeTime >= 2h && inactiveTime <= 30min && calories > 500kcal) || (activeTime >= 2h && inactiveTime = 30min && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((day =~ "ABCDEFGHIJ" && steps = 55 && inactiveTime <= 30min) || (updateTime = makeDate(1462320000000) && distance <= 42cm && inactiveTime < 30min) || (distance <= 1000m && steps < 7 && activeTime < 2h && inactiveTime <= 2h))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @gmail.get_latest(), (((subject = "9876543210" && label =~ "23456789") || subject = "23456789")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @thermostat.set_minmax_temperature, high > 22C 
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), true , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => * => @slack.send_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), ((title =~ "0123456788" || (query =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width <= 7 && height > 11))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @slack.send_picture(channel=v_title, caption=v_title, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@google_drive.new_drive_file, (file_name =~ "0123456788" || file_name =~ "0123456788") , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @builtin.sorry() ;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => @speaker.set_sink, true 
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @speaker.set_sink() ;
}
====
* => * => @fitbit.recordweight, true 
;;
Main() {
    @wall_street_journal.markets(), ((link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @linkedin.get_profile(), ((formatted_name =~ "9876543210" && industry = "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @fitbit.recordweight(weight=155lb) ;
}
====
* => @lg_webos_tv.set_volume, (percent <= 14 || percent > 14 || percent < 7 || percent > 42) 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id <= 42 && estimated_diameter_max >= 1000m && dangerous = false && relative_velocity >= 5kmph && orbiting_body =~ "12345") || (asteroid_id <= 11 && name =~ "12345" && estimated_diameter_min >= 42cm && dangerous = true && relative_velocity <= 5kmph))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @lg_webos_tv.set_volume(percent=v_asteroid_id) ;
}
====
* => @twitter.search_by_hashtag, ((count = 14 && text = "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (count >= 55 && text =~ "12345" && contains(hashtags, "cat"^^tt:hashtag) && from = "bob"^^tt:username) || (count = 7 && text =~ "0123456788" && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @thermostat.humidity(), ((value > 7 || time = makeDate(1462320000000))) , v_time := time, v_value := value => @twitter.search_by_hashtag(count=v_value, query_hashtag="lol"^^tt:hashtag), (((count < 42 && text = "0123456789" && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username && in_reply_to = "alice"^^tt:username) || text =~ "9876543210")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @onedrive.rename_file, (new_name =~ "12345" || (old_name = "0123456789" && new_name = "9876543210")) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((yield < 14 && ex_div_date = makeDate(1462320000000)) || (company_name =~ "0123456789" && yield > 42 && div >= 7))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @onedrive.rename_file(old_name="23456789", new_name="9876543210") ;
}
====
@thermostat.humidity, true , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), true , v_time := time, v_value := value => @bing.web_search(query="12345"), (title =~ "9876543210") , v_title := title, v_description := description, v_link := link => notify;
}
====
now => @xkcd.get_comic, ((title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "23456789") || (title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "0123456789") || (number >= 14 && title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "ABCDEFGHIJ") || (number < 7 && title = "9876543210") || (number < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(), ((title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @almond_bike_market.post, ((title =~ "ABCDEFGHIJ" && price = 14 && info = "9876543210") || (title =~ "0123456788" && price = 7 && poster = "ABCDEFGHIJ") || (title = "9876543210" && info =~ "23456789")) 
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456789"), (((tournament_search_term = "12345" && tournament_full_name =~ "23456789" && tournament_league_name =~ "0123456788" && away_alias = "23456789" && home_alias =~ "ABCDEFGHIJ" && away_points = 11) || (tournament_search_term = "0123456788" && tournament_full_name =~ "12345" && away_name = "9876543210" && home_name = "9876543210" && game_status = enum(halftime) && home_points <= 55) || (tournament_search_term = "23456789" && tournament_full_name = "ABCDEFGHIJ" && tournament_league_name =~ "12345" && game_status = enum(inprogress) && home_points <= 7) || (tournament_search_term =~ "ABCDEFGHIJ" && tournament_league_name = "0123456788" && away_alias =~ "0123456789" && home_alias =~ "0123456789" && home_name = "0123456788" && scheduled_time = "0123456788" && away_points <= 42 && home_points < 55) || (tournament_full_name = "12345" && away_alias =~ "0123456788" && home_alias = "0123456789" && away_name =~ "0123456788" && home_points < 14) || (tournament_search_term = "23456789" && tournament_league_name = "0123456788" && away_alias = "23456789" && home_name = "ABCDEFGHIJ" && game_status = enum(scheduled) && away_points >= 14 && home_points <= 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @almond_bike_market.post(price=v_home_points, title="0123456788", info="ABCDEFGHIJ", poster="23456789") ;
}
====
* => @omlet.send_picture, true 
;;
Main() {
    now => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="ABCDEFGHIJ") ;
}
====
@google_drive.new_drive_file, (file_name = "ABCDEFGHIJ" || file_name =~ "ABCDEFGHIJ" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @almond_couch_market.search(), (((title = "0123456788" && price <= 42) || price <= 55)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @slack.send_picture, true 
;;
Main() {
    @almond_bike_market.monitor(info="12345"), (price <= 7)  => @slack.send_picture(channel="lol"^^tt:hashtag, caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@sportradar.ncaambb_team, (watched_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && other_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && watched_is_home = false && away_name =~ "9876543210" && home_name =~ "9876543210" && game_status = enum(halftime) && home_points <= 55) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), ((watched_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && other_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && scheduled_time = makeDate(1462320000000) && away_points > 7 && home_points > 55 && result = enum(unclosed))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "ABCDEFGHIJ" && headline = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || formatted_name = "0123456788") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "0123456788" && headline = "ABCDEFGHIJ")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @instagram.get_pictures, (count < 55 && caption = "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=42), ((caption = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @twitter.search, (count >= 7 && text =~ "23456789" && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search(query="0123456789", from="bob"^^tt:username), ((query =~ "23456789" && count >= 7 && text =~ "0123456789" && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @gmail.send_picture, (subject =~ "0123456788" && message = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1487030400000) || value > 42 || value > 11)) , v_time := time, v_value := value => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @instagram.get_pictures(), ((count > 55 && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @security-camera.set_power(power=enum(on)) ;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 14 || price > 55 || (title = "12345" && price > 42) || (title = "12345" && price <= 14 && contact = "+16501234567"^^tt:phone_number) || (title = "ABCDEFGHIJ" && price > 11 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(on)) ;
}
====
* => @imgflip.list, true , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    now => @xkcd.random_comic(), ((number > 7 && title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text =~ "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @dropbox.create_new_folder(folder_name=v_alt_text) ;
}
====
@omlet.newmessage, (type = enum(text) || message =~ "23456789" || (type = enum(text) && message =~ "ABCDEFGHIJ")) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.newmessage(), (type = enum(picture)) , v_type := type, v_message := message => @phone.get_gps(), (((altitude < 1000m && bearing >= 55 && speed <= 5kmph) || (altitude > 1000m && bearing < 42 && speed <= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @holidays.next_us_holiday_event, (date = makeDate(1487030400000) && summary = "0123456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), (description = "12345") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @bluetooth_speaker.lower_volume, true 
;;
Main() {
    now => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && summary = "12345" && location = "12345") || (summary =~ "0123456788" && description =~ "23456789" && sequence = 42 && organizer =~ "0123456789") || (description =~ "0123456788" && sequence < 7 && organizer = "12345" && location =~ "23456789") || (start_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "9876543210" && sequence > 42))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @bluetooth_speaker.lower_volume() ;
}
====
* => @ytranslate.detect_language, text =~ "23456789" , v_detected_language := detected_language => *
;;
Main() {
    now => @ytranslate.detect_language(text="0123456788"), (detected_language = "zh"^^tt:iso_lang_code("Chinese")) , v_detected_language := detected_language => @twitter.post_picture(caption="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@icalendar.event_begin, ((sequence < 7 && organizer = "ABCDEFGHIJ") || (start_date = makeDate(1462320000000) && summary = "0123456788" && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ" && sequence < 14 && organizer = "ABCDEFGHIJ" && location = "12345") || (end_date = makeDate(1487030400000) && description = "0123456789" && sequence <= 42 && organizer = "ABCDEFGHIJ" && location = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.list_folder(folder_name="12345"), (((file_name =~ "23456789" && file_size <= 20MB && full_path = "0123456788") || is_folder = true)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @thecatapi.get, (image_id =~ "0123456789" && count <= 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @jawbone_up.getbody(), ((time = makeDate(1487030400000) && weight > 155lb && leanMass > 42 && bodyFat <= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @omlet.send_to, message =~ "0123456788" 
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="9876543210"), (((tournament_full_name = "12345" && tournament_league_name = "0123456789" && home_alias =~ "0123456789" && away_name =~ "9876543210" && home_name = "0123456789" && home_points >= 55) || (away_alias = "12345" && home_name =~ "ABCDEFGHIJ" && game_status = enum(scheduled) && home_points > 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @thermostat.get_hvac_state(), ((mode = enum(heat) && state = enum(heating))) , v_mode := mode, v_state := state => @omlet.send_to(to="bob"^^tt:username, message="9876543210") ;
}
====
* => @phone.call_emergency, true 
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1487030400000) && distance > 1000m && steps > 7 && activeTime > 2h && inactiveTime < 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.call_emergency() ;
}
====
@sportradar.soccer_eu_tourney, (away_alias =~ "ABCDEFGHIJ" && home_alias = "23456789" && home_name = "23456789") , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="12345"), ((tournament_search_term = "0123456788" && away_alias = "12345" && home_alias =~ "ABCDEFGHIJ" && home_name =~ "23456789" && scheduled_time =~ "0123456789" && away_points <= 11 && home_points <= 55)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify;
}
====
* => * => @thermostat.set_minmax_temperature, (low < 22C || (low = 22C && high < 73F)) 
;;
Main() {
    now => @weatherapi.current(location=$context.location.current_location), (((location = $context.location.work && temperature = 73F && humidity >= 11 && cloudiness = 11 && fog < 7) || (wind_speed >= 5kmph && humidity >= 11 && cloudiness >= 7 && weather = "0123456788"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @thermostat.set_minmax_temperature(low=22C, high=73F) ;
}
====
now => @almond_dates.search, ((message = "0123456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "23456789" && message = "9876543210" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "0123456789" && message = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @nasa.rover, (count >= 42 && camera_used = "9876543210") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), (camera_used = "0123456789") , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => @icalendar.list_events, (description = "ABCDEFGHIJ" && organizer =~ "12345" && location =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @almond_bike_market.monitor(info="23456789"), (info =~ "12345")  => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && summary =~ "0123456788" && sequence >= 42 && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@slack.receive, ((timestamp = "12345" && from = "alice"^^tt:username) || (timestamp =~ "9876543210" && from = "alice"^^tt:username && message = "12345")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (((timestamp =~ "0123456789" && message =~ "0123456788") || timestamp =~ "0123456788" || message = "12345" || (timestamp = "0123456789" && message =~ "0123456789"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => @weatherapi.sunrise, ((location = $context.location.work && date = makeDate(1462320000000)) || sunset = makeDate(1462320000000) || (location = $context.location.work && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.current_location), (((date = makeDate(1462320000000) && sunset = makeDate(1487030400000)) || sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@twitter.my_tweet, ((text = "23456789" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify
;;
Main() {
    @twitter.my_tweet(), true , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify;
}
====
@almond_bike_market.monitor, (info =~ "0123456789" || price < 42 || (info = "ABCDEFGHIJ" && price > 7))  => * => @fitbit.recordweight, weight < 155lb 
;;
Main() {
    @almond_bike_market.monitor(price=42), (info = "23456789")  => @fitbit.recordweight(weight=82kg) ;
}
====
* => @almond_dates.search, (interest =~ "ABCDEFGHIJ" || poster =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @slack.receive(), true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @almond_dates.search(), (((interest =~ "12345" && message =~ "9876543210" && poster = "12345") || poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@weatherapi.monitor, ((temperature > 73F && humidity = 11 && fog > 14) || (temperature <= 22C && humidity = 7 && cloudiness > 7 && fog = 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature = 22C && wind_speed <= 5kmph && cloudiness > 42 && fog <= 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @omlet.incomingmessage(), (message =~ "0123456789") , v_type := type, v_message := message => @heatpad.set_power(power=enum(on)) ;
}
====
now => @youtube.list_subscriptions, title =~ "0123456789" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.list_subscriptions(), (((description =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "9876543210" && description =~ "9876543210") || description = "0123456788")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@yahoofinance.stock_div, (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "9876543210" && yield < 14 && div < 7 && pay_date = makeDate(1487030400000)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_dates.post, ((interest =~ "23456789" && poster =~ "0123456789") || (message = "12345" && poster = "0123456789" && phone = "+15551234567"^^tt:phone_number) || interest =~ "12345" || (message = "0123456789" && poster = "9876543210") || (interest =~ "0123456789" && poster = v_company_name) || (message = "12345" && poster =~ "12345") || poster =~ v_company_name || (interest = "ABCDEFGHIJ" && message = "12345")) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && div > 7 && ex_div_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_dates.post(interest=v_company_name, message=v_company_name, poster=v_company_name, phone="+16501234567"^^tt:phone_number) ;
}
====
* => @almond_bike_market.ask_seller, (property = "9876543210" && value =~ "9876543210") , v_answer := answer => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((yield > 55 && pay_date = makeDate(1487030400000)) || (company_name =~ "ABCDEFGHIJ" && div < 42 && pay_date = makeDate(1487030400000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_bike_market.ask_seller(id=v_company_name, property=v_company_name, value=v_company_name, public=false), ((property =~ "9876543210" && answer =~ "0123456788")) , v_answer := answer => notify;
}
====
* => @icalendar.list_events, ((summary =~ "23456789" && location = "0123456788") || (start_date = makeDate(1487030400000) && summary = "12345" && description =~ "0123456789" && sequence >= 7 && location = "0123456789") || (end_date = makeDate(1462320000000) && summary = "23456789" && description = "23456789" && organizer =~ "0123456789" && location =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @almond_dates.monitor(), ((poster = "ABCDEFGHIJ" || (interest =~ "9876543210" && poster = "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @icalendar.list_events(), ((sequence >= 42 && organizer =~ "ABCDEFGHIJ" && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @imgflip.generate, ((template =~ "23456789" && text_bottom = "23456789" && name =~ "0123456789") || (text_top =~ "0123456788" && text_bottom =~ "9876543210" && name = "ABCDEFGHIJ") || (text_top =~ "0123456788" && name = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="0123456789", text_top="23456789", text_bottom="ABCDEFGHIJ"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (text_top =~ "ABCDEFGHIJ" && name =~ "0123456788"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @almond_dates.search, ((message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "0123456789" && message =~ "ABCDEFGHIJ" && poster = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (((interest = "12345" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "9876543210" && poster =~ "0123456789") || (interest = "0123456789" && message =~ "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
@onedrive.file_created, file_name =~ "23456789" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), (file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => notify;
}
====
@almond_dates.monitor, (poster = "0123456789" || (message = "23456789" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (message = "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => notify;
}
====
@fitness-tracker.getbody, ((weight > 155lb && bmi <= 14) || (time = makeDate(1462320000000) && weight <= 155lb && leanMass < 42 && bodyFat <= 42 && bmi < 55) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 82kg && bodyFat > 11 && bmi > 42) || (time = makeDate(1487030400000) && weight = 82kg && leanMass >= 11 && bmi >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && place = $context.location.current_location && weight = 82kg && leanMass > 7 && bodyFat = 55 && bmi < 55) || (bodyFat < 42 && bmi < 14))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @bing.web_search, (title = "9876543210" && description =~ "0123456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    @almond_dates.monitor(), ((message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @bing.web_search(query="ABCDEFGHIJ"), (title = "0123456788") , v_title := title, v_description := description, v_link := link => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @thecatapi.get, (image_id =~ "9876543210" && count < 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), ((count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @almond_dates.search, message = "ABCDEFGHIJ" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "0123456788" && message = "23456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@phdcomics.new_post, (title = "23456789" || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "0123456789" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "12345" || (link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
* => * => @almond_bike_market.post, (title =~ "23456789" && price >= 14 && info =~ "12345" && poster = "ABCDEFGHIJ") 
;;
Main() {
    now => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="23456789"), ((text =~ "9876543210" || (source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "it"^^tt:iso_lang_code("Italian")))) , v_source_language := source_language, v_translated_text := translated_text => @almond_bike_market.post(info=v_translated_text, poster=v_translated_text, title="9876543210", price=14) ;
}
====
* => * => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    @reddit_front_page.newpost(), (((link = "http://www.abc.def"^^tt:url && user = "ABCDEFGHIJ") || (link = "http://www.google.com"^^tt:url && user = "9876543210") || user =~ "12345" || title =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @omlet.send_picture(to=v_title, picture_url=v_title, caption=v_title) ;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1462320000000) && summary = "12345" && description =~ "9876543210") || (date = makeDate(1462320000000) && summary =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @fitness-tracker.getbody(), ((weight <= 82kg && leanMass >= 42 && bodyFat > 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @holidays.next_uk_holiday_event(), (((date = makeDate(1487030400000) && summary =~ "9876543210" && description = "9876543210") || date = makeDate(1487030400000) || (date = makeDate(1462320000000) && description =~ "0123456788") || summary =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => @omlet.send_picture(picture_url=v_summary, caption=v_summary, to="alice"^^tt:username) ;
}
====
now => * => @phone.set_ringer, true 
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((location = $context.location.work && wind_speed >= 25mph && humidity < 11 && cloudiness <= 7 && fog <= 55 && weather =~ "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @phone.set_ringer(mode=enum(vibrate)) ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (section = enum(lifestyle)) , v_title := title, v_link := link, v_description := description => @car.honk_horn() ;
}
====
now => * => @almond_couch_market.post, (title = "23456789" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((date = makeDate(1462320000000) || (summary = "9876543210" && description =~ "23456789"))) , v_date := date, v_summary := summary, v_description := description => @almond_couch_market.post(contact=v_description, title="9876543210", price=7) ;
}
====
* => * => @builtin.thank_you, true 
;;
Main() {
    @builtin.timer(interval=30min), true  => @builtin.thank_you() ;
}
====
@security-camera.new_event, true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((has_sound = true || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (has_motion = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || has_sound = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @github.comment_issue(repo_name="23456789", issue_number=55, body="9876543210") ;
}
====
* => @dropbox.move, (old_name =~ "0123456789" && new_name = "ABCDEFGHIJ") 
;;
Main() {
    @youtube.source(), ((channel_title = "9876543210" && title =~ "0123456789" && video_id = "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @dropbox.move(old_name=v_channel_id, new_name=v_title) ;
}
====
* => @giphy.get_tag, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => notify
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1487030400000) && day = "0123456789" && distance >= 42cm && steps <= 55 && activeTime > 2h && inactiveTime <= 30min && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
* => @linkedin.get_profile, ((formatted_name = "0123456789" && headline = "9876543210" && industry =~ "23456789" && contains(positions, "0123456789")) || specialties = "ABCDEFGHIJ") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @thermostat.humidity(), (((time = makeDate(1487030400000) && value < 42) || value < 7)) , v_time := time, v_value := value => @linkedin.get_profile(), ((headline =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @linkedin.share, status =~ "12345" 
;;
Main() {
    now => @thermostat.get_temperature(), ((value > 22C || value < 73F || value < 73F)) , v_value := value => @linkedin.share(status="12345") ;
}
====
* => @thermostat.set_target_temperature, true 
;;
Main() {
    now => @thermostat.set_target_temperature(value=73F) ;
}
====
@scale.source, value >= 82kg , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), (value <= 82kg) , v_time := time, v_value := value => notify;
}
====
@bodytrace.source, (time = makeDate(1462320000000) && weight >= 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight >= 155lb) , v_time := time, v_weight := weight => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
now => @ytranslate.translate, (source_language = "zh"^^tt:iso_lang_code("Chinese") && target_language = "en"^^tt:iso_lang_code("English") && text = "ABCDEFGHIJ" && translated_text = "ABCDEFGHIJ") , v_source_language := source_language, v_translated_text := translated_text => @car.honk_horn, true 
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="23456789"), ((target_language = "en"^^tt:iso_lang_code("English") && translated_text = "9876543210")) , v_source_language := source_language, v_translated_text := translated_text => @car.honk_horn() ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && duration <= 30min && rem <= 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1487030400000) && duration = 2h && rem <= 30min && light < 30min && deep = 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @xkcd.get_comic(), ((number = 7 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @builtin.get_random, (number <= 42 || number >= 14 || number < 55 || number < 42) , v_number := number => notify
;;
Main() {
    now => @builtin.get_random(), (number >= 55) , v_number := number => notify;
}
====
now => * => @heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature <= 22C && wind_speed < 5kmph && humidity <= 55 && weather = "23456789") || (wind_speed < 5kmph && humidity >= 55 && weather = "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @heatpad.set_power(power=enum(off)) ;
}
====
now => @gmail.get_latest, true , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), ((subject =~ "ABCDEFGHIJ" || (from = "bob@stanford.edu"^^tt:email_address && subject =~ "12345" && label = "12345") || subject =~ "12345" || (from = "alice@gmail.com"^^tt:email_address && label =~ "0123456788"))) , v_from := from, v_subject := subject, v_label := label => @speaker.play_music() ;
}
====
@scale.source, time = makeDate(1462320000000) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @lg_webos_tv.lower_volume() ;
}
====
* => @almond_couch_market.search, (title = "23456789" || price = 42 || (title = "9876543210" && price >= 42) || price = 42 || title =~ "12345" || (title =~ "0123456789" && price < 55)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "9876543210" && filter =~ "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_couch_market.search(), (((title = "0123456788" && price <= 7 && contact = "+16501234567"^^tt:phone_number) || (title =~ "12345" && contact = "+15551234567"^^tt:phone_number) || (title = "9876543210" && price > 7))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@nasa.asteroid, ((name = "23456789" && estimated_diameter_min <= 42cm && estimated_diameter_max >= 42cm && relative_velocity >= 25mph && closest_distance_to_earth >= 42cm) || (asteroid_id >= 11 && name =~ "0123456789" && estimated_diameter_max < 42cm && dangerous = true && orbiting_body =~ "ABCDEFGHIJ") || (asteroid_id <= 14 && name = "0123456789" && estimated_diameter_min <= 42cm && estimated_diameter_max < 42cm && closest_distance_to_earth > 1000m && orbiting_body =~ "0123456789") || (estimated_diameter_max < 1000m && relative_velocity = 5kmph && closest_distance_to_earth >= 42cm && orbiting_body = "ABCDEFGHIJ")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 14 && name = "9876543210" && estimated_diameter_min > 42cm && estimated_diameter_max > 1000m && closest_distance_to_earth > 1000m && orbiting_body = "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
@rss.new_post, (title = "0123456789" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @light-bulb.color_loop() ;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature >= 73F && humidity = 7 && cloudiness = 14 && fog <= 55 && weather =~ "ABCDEFGHIJ") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((temperature = 73F && humidity > 42 && weather =~ "ABCDEFGHIJ" && status = enum(snowy)) || (temperature <= 73F && cloudiness < 11 && fog < 55) || (temperature >= 22C && wind_speed < 5kmph && humidity = 55 && cloudiness > 55 && fog > 14 && weather =~ "12345") || (location = $context.location.home && wind_speed < 5kmph && humidity > 55 && fog > 7 && weather = "0123456789" && status = enum(sleety)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @thecatapi.get(), ((count = 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @phone.set_ringer(mode=enum(silent)) ;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @nasa.apod(), (description = "0123456788") , v_title := title, v_description := description, v_picture_url := picture_url => @heatpad.set_power(power=enum(on)) ;
}
====
now => @ytranslate.translate, (source_language = "en"^^tt:iso_lang_code("English") && translated_text =~ "ABCDEFGHIJ") , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="12345"), ((translated_text =~ "0123456789" || text = "0123456789")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    @almond_couch_market.monitor(), (title = "0123456789") , v_title := title, v_price := price, v_contact := contact => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @builtin.notify, message = "23456789" 
;;
Main() {
    now => @phone.get_gps(), ((altitude > 1000m && bearing > 7 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @builtin.notify(message="ABCDEFGHIJ") ;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @heatpad.set_power(power=enum(off)) ;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1487030400000) && duration >= 30min && rem < 2h && deep >= 2h) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && rem < 30min && light < 30min && deep < 30min) || (time = makeDate(1462320000000) && place = $context.location.current_location && rem = 2h && light > 30min && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((asleepTime = makeDate(1487030400000) && duration > 2h && rem <= 2h && light < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @instagram.get_pictures(), (filter = "12345") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => @omlet.send_to(to=v_title, message=v_title) ;
}
====
* => @youtube.search_channel, (channel_id = "ABCDEFGHIJ" && description = "0123456789") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @phone.receive_sms(), (((from = "+16501234567"^^tt:phone_number && body =~ "0123456788") || body = "0123456788")) , v_from := from, v_body := body => @youtube.search_channel(query="9876543210"), ((description =~ "0123456788" || (query = "12345" && title = "9876543210") || (query =~ "ABCDEFGHIJ" && channel_id = "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@youtube.source, (channel_id = "0123456788" && channel_title =~ "0123456789" && title = "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_title =~ "12345" && title =~ "ABCDEFGHIJ") || (channel_id = "ABCDEFGHIJ" && video_id =~ "0123456788"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
* => @almond_bike_market.ask, (property =~ "12345" || property =~ "0123456789")  => *
;;
Main() {
    @xkcd.new_comic(), (number > 11) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @almond_bike_market.ask(id=v_title, property="0123456789"), true  => @linkedin.share(status="12345") ;
}
====
@security-camera.new_event, ((has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_sound = false && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (((has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_sound = true && has_motion = true && has_person = true) || has_motion = true || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @twitter.search(query="0123456789", from="alice"^^tt:username), (((query = "12345" && count > 55 && contains(urls, "http://www.google.com"^^tt:url)) || (query =~ "0123456788" && text =~ "ABCDEFGHIJ"))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @imgflip.list, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.list(), ((name = "9876543210" || name = "23456789" || (name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_name := name, v_picture_url := picture_url => @slack.setPresence(presence=enum(away)) ;
}
====
now => @ytranslate.detect_language, detected_language = "zh"^^tt:iso_lang_code("Chinese") , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="12345"), ((text =~ "0123456788" && detected_language = "it"^^tt:iso_lang_code("Italian"))) , v_detected_language := detected_language => notify;
}
====
@twitter.direct_message, ((from = "alice"^^tt:username && message = "0123456789") || __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.direct_message(), (((message = "ABCDEFGHIJ" && __reserved = false) || __reserved = false || message =~ "0123456788")) , v_from := from, v_message := message, v___reserved := __reserved => @dropbox.open(file_name="0123456788"), (url =~ "12345") , v_url := url => notify;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "12345") , v_file_name := file_name => notify;
}
====
now => @almond_couch_market.search, ((price > 42 && contact = "+15551234567"^^tt:phone_number) || title = "0123456788" || price >= 14) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((title = "ABCDEFGHIJ" && price < 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @onedrive.delete_file(file_name="ABCDEFGHIJ") ;
}
====
now => @nasa.apod, ((title =~ "23456789" && description =~ "9876543210") || title = "0123456788") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @phone.set_ringer, (mode = enum(silent) || mode = enum(normal) || mode = enum(silent) || mode = enum(silent)) 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) || summary = "ABCDEFGHIJ" || (start_date = makeDate(1487030400000) && description = "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @phone.set_ringer(mode=enum(silent)) ;
}
====
* => @dropbox.move, (old_name =~ "0123456789" && new_name = "ABCDEFGHIJ") 
;;
Main() {
    @github.new_milestone(repo_name="23456789"), true , v_user := user, v_description := description, v_time := time => @dropbox.move(new_name=v_description, old_name="9876543210") ;
}
====
@almond_couch_market.monitor, ((title =~ "23456789" && price <= 55 && contact = "+15551234567"^^tt:phone_number) || (price < 11 && contact = "+15551234567"^^tt:phone_number) || (title = "12345" && price < 11) || price = 55 || title =~ "0123456789" || (title = "12345" && price <= 14)) , v_title := title, v_price := price, v_contact := contact => * => @slack.send_picture, ((caption = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || caption = "ABCDEFGHIJ" || (channel = "funny"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || caption = v_title) 
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @gmail.get_latest(), (((from = "bob@stanford.edu"^^tt:email_address && subject =~ "9876543210" && label = "23456789") || (from = "bob@stanford.edu"^^tt:email_address && subject = "9876543210" && label =~ "9876543210") || subject = "0123456788")) , v_from := from, v_subject := subject, v_label := label => @slack.send_picture(channel="lol"^^tt:hashtag, caption="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @weatherapi.sunrise, ((location = $context.location.work && date = makeDate(1462320000000)) || sunset = makeDate(1462320000000) || (location = $context.location.work && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 42 || (title = "0123456788" && price > 55 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "12345" || file_name =~ "9876543210") 
;;
Main() {
    now => @almond_bike_market.search(), true  => @google_drive.create_new_drive_file(file_name="12345") ;
}
====
* => @bluetooth_speaker.set_volume, percent > 55 
;;
Main() {
    @github.new_milestone(repo_name="0123456789"), ((repo_name = "12345" && time = makeDate(1487030400000))) , v_user := user, v_description := description, v_time := time => @bluetooth_speaker.set_volume(percent=42) ;
}
====
now => @xkcd.get_comic, (number < 42 && title = "ABCDEFGHIJ") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(number=42), (((title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text = "9876543210") || (number >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1487030400000) && organizer = "12345" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), (description = "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @slack.updateChannelPurpose(channel=v_location, purpose=v_location) ;
}
====
@instagram.new_picture, caption =~ "0123456788" , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter =~ "12345") || contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @twitter.search(query=v_filter, in_reply_to=v_filter, from="bob"^^tt:username), ((count = 11 || (contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @dropbox.get_space_usage, (total = 5KB || total > 20MB) , v_used := used, v_total := total => notify
;;
Main() {
    now => @dropbox.get_space_usage(), ((used < 20MB && total <= 5KB)) , v_used := used, v_total := total => notify;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), ((file_name =~ "0123456788" || file_name = "0123456788" || file_name = "23456789")) , v_file_name := file_name => @uber.time_estimate(start=$context.location.home), (estimate = 2h) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), (((away_name =~ "0123456788" && home_name = "9876543210" && scheduled_time = makeDate(1487030400000) && away_points < 7 && result = enum(lose)) || (game_status = enum(scheduled) && away_points <= 7 && home_points > 14 && result = enum(lose)))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @dropbox.create_new_folder(folder_name=v_away_name) ;
}
====
@weatherapi.monitor, (wind_speed < 25mph && humidity = 11 && cloudiness = 55) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), true , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.get_latest(), ((subject =~ "0123456788" || subject =~ "0123456789" || (from = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@almond_dates.monitor, (message =~ "0123456789" && poster =~ "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "0123456789" && poster =~ "9876543210") || poster =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @nasa.apod(), ((title =~ "ABCDEFGHIJ" || (title =~ "9876543210" && description =~ "0123456789") || description = "0123456788")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@omlet.incomingmessage, (message =~ "0123456788" || message = "12345" || type = enum(text) || message = "9876543210" || message = "ABCDEFGHIJ" || (type = enum(text) && message = "9876543210")) , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.incomingmessage(), (message = "23456789") , v_type := type, v_message := message => @light-bulb.alert_long() ;
}
====
* => @omlet.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && caption = "ABCDEFGHIJ") || (to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788")) 
;;
Main() {
    @sleep-tracker.getsleep(), (((duration > 30min && rem >= 2h && light <= 30min) || (place = $context.location.current_location && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration < 2h && rem = 2h) || (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration <= 30min && deep >= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
* => @phone.get_gps, (bearing <= 42 && speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @xkcd.new_comic(), (((number > 7 && title =~ "12345") || (title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (number > 42 && title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @phone.get_gps(), ((altitude <= 1000m && bearing < 7)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @linkedin.get_profile, ((formatted_name =~ "0123456789" && industry = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "12345" && industry = "ABCDEFGHIJ" && specialties =~ "9876543210")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "ABCDEFGHIJ" && industry =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, (interest = "0123456789" || (message =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "ABCDEFGHIJ" && message =~ "0123456788" && poster = "0123456788") || (message =~ "0123456789" && poster =~ "12345" && phone = "+16501234567"^^tt:phone_number) || (message = "0123456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "12345" && message = "9876543210" && poster =~ "9876543210") || message = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (interest = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @bluetooth_speaker.lower_volume() ;
}
====
* => @bing.web_search, (query =~ "0123456789" && description = "ABCDEFGHIJ") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @onedrive.file_created(), ((file_name = "9876543210" || file_name = "ABCDEFGHIJ")) , v_file_name := file_name => @bing.web_search(query="12345"), (query =~ "0123456788") , v_title := title, v_description := description, v_link := link => notify;
}
====
@youtube.source, (channel_id =~ "0123456788" && channel_title =~ "0123456788" && title = "12345" && video_id =~ "0123456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (channel_title = "9876543210") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @ytranslate.detect_language(text=v_channel_id), true , v_detected_language := detected_language => notify;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (price < 11) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_bike_market.ask_seller, ((id =~ "12345" && public = false) || value = "0123456789" || (property =~ "0123456789" && answer = "12345") || (property = "23456789" && value =~ "12345" && answer =~ "23456789")) , v_answer := answer => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 55 || (asteroid_id = 11 && name =~ "9876543210" && estimated_diameter_min < 42cm && estimated_diameter_max < 1000m && closest_distance_to_earth < 42cm && orbiting_body =~ "ABCDEFGHIJ") || (asteroid_id > 55 && name = "0123456789" && estimated_diameter_min > 1000m && estimated_diameter_max = 42cm && closest_distance_to_earth = 42cm && orbiting_body = "0123456788") || (estimated_diameter_min < 42cm && estimated_diameter_max < 1000m && relative_velocity > 25mph && closest_distance_to_earth >= 42cm && orbiting_body = "0123456789"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.ask_seller(public=v_dangerous, id="0123456789", property="0123456789", value="23456789"), ((value =~ "ABCDEFGHIJ" || (property =~ "9876543210" && value =~ "9876543210" && answer = "0123456788"))) , v_answer := answer => notify;
}
====
now => @nasa.apod, (title =~ "12345" || (title = "0123456789" && description =~ "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), ((description =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@weatherapi.monitor, ((temperature > 22C && wind_speed <= 5kmph && humidity >= 7 && cloudiness >= 14 && fog < 42 && weather = "23456789") || (temperature = 73F && wind_speed < 25mph && humidity < 11 && cloudiness = 11 && fog >= 55 && weather =~ "ABCDEFGHIJ") || (location = $context.location.home && wind_speed > 5kmph && humidity <= 14 && fog < 11 && status = enum(raining)) || (temperature < 73F && wind_speed <= 25mph && humidity < 42 && cloudiness > 55 && fog = 7 && weather = "9876543210" && status = enum(snowy)) || (temperature > 73F && wind_speed < 5kmph && humidity < 11 && fog <= 55 && status = enum(cloudy)) || (temperature < 22C && wind_speed < 5kmph && cloudiness < 7 && fog = 7) || (humidity <= 14 && fog > 42) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature <= 22C && cloudiness <= 55 && fog = 7 && weather =~ "0123456788" && status = enum(snowy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = $context.location.home && temperature < 73F && wind_speed = 5kmph && cloudiness >= 42 && fog > 42 && weather = "0123456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @almond_bike_market.ask, (id =~ "12345" && property = "ABCDEFGHIJ")  => notify
;;
Main() {
    @wall_street_journal.technology(), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @almond_bike_market.ask(id="0123456788", property="ABCDEFGHIJ"), (id = "23456789")  => notify;
}
====
@yahoofinance.stock_div, ((company_name = "23456789" && yield = 14 && div <= 7 && pay_date = makeDate(1487030400000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "23456789")) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((stock_id = "aapl"^^tt:stock_id("Apple") && company_name =~ "0123456789" && yield < 7 && div <= 11 && ex_div_date = makeDate(1487030400000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "0123456788" && yield = 14 && div = 7 && pay_date = makeDate(1487030400000) && ex_div_date = makeDate(1462320000000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "0123456789" && div <= 11 && pay_date = makeDate(1487030400000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
* => @thermostat.set_minmax_temperature, (low > 73F && high <= 73F) 
;;
Main() {
    @sleep-tracker.getsleep(), ((awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration >= 30min && light <= 30min && deep = 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @thermostat.set_minmax_temperature(low=73F, high=73F) ;
}
====
* => @weatherapi.sunrise, ((location = $context.location.work && date = makeDate(1462320000000)) || sunset = makeDate(1462320000000) || (location = $context.location.work && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @thermostat.temperature(), ((value < 73F || value = 22C)) , v_time := time, v_value := value => @weatherapi.sunrise(location=$context.location.work), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@heartrate-monitor.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => *
;;
Main() {
    @heartrate-monitor.getheartrate(), (((place = $context.location.current_location && heartrate <= 11) || (place = $context.location.current_location && heartrate < 42))) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
now => * => @lg_webos_tv.set_power, true 
;;
Main() {
    now => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
@icalendar.event_begin, (end_date = makeDate(1462320000000) && summary =~ "23456789" && description =~ "9876543210" && sequence > 42 && organizer =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (end_date = makeDate(1487030400000)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @almond_couch_market.post, price >= 14 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature >= 22C && wind_speed < 5kmph && humidity >= 11 && cloudiness = 11 && fog >= 42 && weather = "0123456789") || (temperature >= 73F && humidity <= 11 && cloudiness > 42 && fog < 7 && weather = "23456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.post(price=v_fog, title="ABCDEFGHIJ", contact="+16501234567"^^tt:phone_number) ;
}
====
@phone.gps, (bearing > 7 || (altitude >= 1000m && bearing >= 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude < 42cm && bearing <= 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @dropbox.get_space_usage(), ((used <= 20MB || (used >= 5KB && total > 20MB) || (used >= 5KB && total > 20MB))) , v_used := used, v_total := total => notify;
}
====
@washington_post.new_article, ((title = "23456789" && description =~ "23456789") || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && description =~ "9876543210") || (title = "0123456789" && description =~ "12345") || (title =~ "12345" && description =~ "23456789")) , v_title := title, v_link := link, v_description := description => * => notify
;;
Main() {
    @washington_post.new_article(section=enum(world)), true , v_title := title, v_link := link, v_description := description => @youtube.list_subscriptions(), true , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@icalendar.event_begin, ((description =~ "9876543210" && organizer =~ "12345") || (description = "0123456789" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1462320000000) && summary =~ "0123456789" && sequence > 42 && location =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@tumblr.new_photo, ((caption = "12345" && contains(tags, "lol"^^tt:hashtag)) || (date = makeDate(1462320000000) && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag)) || (blog_name =~ "12345" && caption =~ "0123456789" && link = "http://www.google.com"^^tt:url) || (blog_name = "12345" && link = "http://www.google.com"^^tt:url) || (blog_name =~ "23456789" && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), ((date = makeDate(1462320000000) && caption =~ "23456789")) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify;
}
====
@icalendar.event_begin, ((end_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && location = "9876543210") || (end_date = makeDate(1487030400000) && description = "23456789" && sequence >= 55 && location =~ "0123456789") || (start_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && description = "9876543210" && organizer = "ABCDEFGHIJ") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "0123456788" && sequence < 11 && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "23456789" && description = "0123456788" && sequence > 7 && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @linkedin.get_profile(), ((headline = "ABCDEFGHIJ" && specialties = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @fitbit.getbody, (weight <= 82kg && bmi <= 7 && fat <= 7) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((weight = 155lb && fat <= 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @twitter.unfollow, true 
;;
Main() {
    @tumblr.new_photo(blog_name="23456789"), (((caption = "ABCDEFGHIJ" && contains(tags, "lol"^^tt:hashtag)) || blog_name = "23456789" || contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @xkcd.get_comic(number=7), ((title =~ "ABCDEFGHIJ" && alt_text =~ "23456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    @twitter.source(), ((contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && __reserved = false)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @heatpad.set_power(power=enum(off)) ;
}
====
@jawbone_up.getheartrate, ((time = makeDate(1462320000000) && place = $context.location.home && heartrate > 14) || heartrate > 11 || (place = $context.location.work && heartrate >= 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.sink, (status =~ "0123456789" || status =~ "0123456789") 
;;
Main() {
    @jawbone_up.getheartrate(), (((time = makeDate(1462320000000) && heartrate >= 14) || (place = $context.location.current_location && heartrate > 55))) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.sink(status="12345") ;
}
====
@security-camera.new_event, has_motion = true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @builtin.get_date, true , v_date := date => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @builtin.get_date(), true , v_date := date => notify;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && deep < 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && place = $context.location.home && rem >= 30min && light = 30min && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @twitter.search(query="9876543210", count=11), (((count = 11 && contains(hashtags, "lol"^^tt:hashtag)) || (count > 55 && text = "9876543210"))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @fitbit.recordweight, weight >= 82kg 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "12345" && headline =~ "23456789" && industry = "ABCDEFGHIJ")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @fitbit.recordweight(weight=82kg) ;
}
====
now => * => @linkedin.share, (status =~ "0123456788" || status =~ "23456789" || status = "ABCDEFGHIJ") 
;;
Main() {
    now => @nasa.apod(), ((title = "0123456788" || (title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "12345" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => @linkedin.share(status=v_description) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_person = false) || (has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
@nasa.asteroid, (name = "12345" && estimated_diameter_min > 1000m && estimated_diameter_max > 1000m && relative_velocity < 5kmph) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 55 && name =~ "9876543210" && estimated_diameter_min >= 1000m && relative_velocity > 25mph && closest_distance_to_earth >= 42cm && orbiting_body =~ "23456789") || (asteroid_id > 7 && name =~ "12345" && estimated_diameter_max >= 42cm && dangerous = true && closest_distance_to_earth > 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @twitter.search(count=v_asteroid_id, in_reply_to=v_name, query="ABCDEFGHIJ"), (((query =~ "23456789" && count < 11 && text =~ "9876543210" && contains(urls, "http://www.google.com"^^tt:url)) || (query =~ "9876543210" && count >= 55 && contains(hashtags, "cat"^^tt:hashtag)) || (count >= 14 && text = "0123456788" && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @ytranslate.detect_language, text =~ "12345" , v_detected_language := detected_language => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @ytranslate.detect_language(text="0123456788"), ((detected_language = "zh"^^tt:iso_lang_code("Chinese") || text = "9876543210")) , v_detected_language := detected_language => notify;
}
====
now => @ytranslate.translate, true , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="9876543210"), (translated_text = "9876543210") , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @heatpad.set_power, power = enum(off) 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && weight > 82kg && leanMass > 14 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @heatpad.set_power(power=enum(off)) ;
}
====
* => @fitbit.getbody, (fat <= 14 || weight > 82kg || (weight > 82kg && bmi <= 7 && fat < 7) || (weight >= 155lb && fat < 42) || (weight > 82kg && bmi < 7 && fat = 55) || (weight < 82kg && bmi <= 14 && fat <= 55) || (weight > 155lb && bmi >= 14 && fat <= 42) || (weight < 155lb && bmi <= 42) || (weight > 155lb && bmi = 7 && fat <= 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @github.new_issue(repo_name="23456789"), ((repo_name = "23456789" && number < 14)) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @fitbit.getbody(), ((weight <= 155lb && bmi < 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@thermostat.temperature, value <= 22C , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), (value >= 22C) , v_time := time, v_value := value => notify;
}
====
@almond_bike_market.monitor, (info =~ "12345" && price <= 11)  => *
;;
Main() {
    @almond_bike_market.monitor(info="ABCDEFGHIJ"), (((info =~ "12345" && price > 7) || price <= 55 || price >= 14))  => @bluetooth_speaker.set_sink() ;
}
====
@google_drive.new_drive_file, file_name = "23456789" , v_file_name := file_name => @light-bulb.color_loop, true 
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "0123456788") , v_file_name := file_name => @light-bulb.color_loop() ;
}
====
* => @thecatapi.get, ((image_id = "0123456789" && count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || link = "http://www.google.com"^^tt:url || image_id = "9876543210" || (count > 7 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @thecatapi.get(), ((image_id = "9876543210" && count < 11 && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@youtube.source, (channel_id = "0123456788" && video_id = "9876543210") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_bike_market.post, (price > 11 || (title = v_title && price <= 11 && poster =~ v_video_id) || (price = 11 && poster =~ "0123456789") || price > 55 || (title =~ v_video_id && price > 7 && poster =~ "0123456789")) 
;;
Main() {
    @youtube.source(), (title = "0123456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_bike_market.post(title=v_channel_title, info=v_video_id, poster=v_channel_id, price=55) ;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), ((wind_speed > 5kmph && humidity <= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @heatpad.set_power(power=enum(off)) ;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), ((channel = "cat"^^tt:hashtag && timestamp = "12345" && message =~ "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @gmail.send_picture(subject=v_message, message=v_message, picture_url=v_message, to="alice@gmail.com"^^tt:email_address) ;
}
====
* => @youtube.list_subscriptions, title = "9876543210" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.list_subscriptions(), (((channel_id = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @security-camera.new_event(), ((has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @youtube.search_channel(query="12345"), ((query = "23456789" && channel_id = "9876543210")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @google_drive.create_new_drive_file(file_name=v_description) ;
}
====
* => @almond_couch_market.post, ((price <= 14 && contact = "+15551234567"^^tt:phone_number) || (title =~ "0123456788" && price >= 11)) 
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), (((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && body =~ "0123456789") || (blog_name =~ "23456789" && link = "http://www.abc.def"^^tt:url && body =~ "9876543210"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @almond_couch_market.post(title=v_title, contact=v_body, price=42) ;
}
====
* => @tumblr-blog.post_text, (title =~ "23456789" && body = "23456789") 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "9876543210" || interest =~ "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text(title=v_poster, body=v_poster) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || has_sound = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1487030400000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
* => @thermostat.set_minmax_temperature, high > 22C 
;;
Main() {
    @phone.gps(), ((altitude <= 42cm && bearing <= 14 && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
* => @wall_street_journal.get_opinions, (title =~ "0123456788" || (title = "9876543210" && link = "http://www.google.com"^^tt:url && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || (link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000)) || (title = "23456789" && link = "http://www.google.com"^^tt:url && updated = makeDate(1462320000000)) || (title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @omlet.send_to, to = "bob"^^tt:username 
;;
Main() {
    @omlet.newmessage(), (message =~ "9876543210") , v_type := type, v_message := message => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => @omlet.send_to(to=v_title, message="12345") ;
}
====
now => @nasa.apod, ((title =~ "23456789" && description =~ "0123456788") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    now => @nasa.apod(), (description =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_picture_url := picture_url => @builtin.sorry() ;
}
====
* => @twitter.search, from = "alice"^^tt:username , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @bodytrace.source(), ((weight < 155lb || (time = makeDate(1462320000000) && weight <= 82kg))) , v_time := time, v_weight := weight => @twitter.search(query="ABCDEFGHIJ", from="alice"^^tt:username), true , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @bing.web_search, ((query =~ "0123456789" && title =~ "0123456789") || title =~ "0123456789" || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || (query = "0123456788" && description =~ "12345") || description =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    now => @bing.web_search(query="0123456789"), (query =~ "12345") , v_title := title, v_description := description, v_link := link => @heatpad.set_power(power=enum(off)) ;
}
====
now => @dropbox.move, (new_name = "23456789" || new_name =~ "0123456788" || old_name =~ "0123456789") 
;;
Main() {
    now => @dropbox.move(old_name="ABCDEFGHIJ", new_name="0123456788") ;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), ((title = "12345" || price < 14)) , v_title := title, v_price := price, v_contact := contact => @car.honk_horn() ;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @builtin.at(time=makeTime(7,30)), true  => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @bing.image_search, ((query = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width < 11 && height >= 11) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width < 42 && height < 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="23456789"), true , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @bluetooth_speaker.raise_volume() ;
}
====
@twitter.direct_message, true , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), (((message = "9876543210" && __reserved = true) || (from = "bob"^^tt:username && __reserved = false))) , v_from := from, v_message := message, v___reserved := __reserved => @phone.get_gps(), (((altitude > 1000m && speed > 5kmph) || (altitude = 1000m && bearing >= 42))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @instagram.get_pictures, ((count = 7 && filter = "0123456789") || (count <= 11 && filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (caption =~ "0123456788" && filter =~ "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), (((count <= 14 && caption =~ "12345" && filter =~ "0123456789") || (count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @instagram.get_pictures, ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && link = "http://www.google.com"^^tt:url && filter = "9876543210" && contains(hashtags, "funny"^^tt:hashtag)) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (count > 42 && contains(hashtags, "cat"^^tt:hashtag)) || (count > 42 && caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work) || (count < 11 && caption = "12345")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @phone.gps(), ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude <= 42cm && bearing < 7 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @instagram.get_pictures(count=55), (((count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456788" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @almond_dates.post, (interest = "12345" && message =~ "0123456789" && poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @thermostat.get_humidity(), ((value > 14 || value <= 14 || value >= 42)) , v_value := value => @almond_dates.post(interest="9876543210", message="0123456788", poster="9876543210", phone="+15551234567"^^tt:phone_number) ;
}
====
now => @twitter.search, ((query = "9876543210" && count < 55) || (count <= 55 && text = "ABCDEFGHIJ" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query =~ "12345" && count <= 42 && text =~ "ABCDEFGHIJ" && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="23456789", count=7, in_reply_to="bob"^^tt:username), (((query =~ "0123456788" && text = "0123456789" && from = "bob"^^tt:username) || text = "23456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => @bluetooth_speaker.set_volume(percent=7) ;
}
====
@youtube.source, ((channel_title =~ "12345" && title =~ "12345" && video_id =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || channel_id =~ "ABCDEFGHIJ" || (channel_id =~ "9876543210" && title = "0123456789" && video_id = "12345" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "9876543210" && channel_title = "23456789" && video_id = "23456789" && video_url = "http://www.google.com"^^tt:url) || (channel_title = "23456789" && title =~ "0123456789" && video_id = "0123456789") || video_id = "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @builtin.get_date(), true , v_date := date => notify;
}
====
@gmail.receive_important_email, (from_name = "0123456789" && subject = "9876543210" && snippet = "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => @security-camera.set_power, true 
;;
Main() {
    @gmail.receive_important_email(), ((from_name =~ "ABCDEFGHIJ" && subject = "0123456789" && snippet = "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @security-camera.set_power(power=enum(on)) ;
}
====
@bodytrace.source, ((time = makeDate(1462320000000) && weight = 155lb) || time = makeDate(1487030400000) || weight = 155lb) , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), (time = makeDate(1462320000000)) , v_time := time, v_weight := weight => notify;
}
====
now => @fitbit.getsteps, (steps <= 7 || steps > 11) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), (steps = 55) , v_date := date, v_steps := steps => notify;
}
====
@bodytrace.source, (time = makeDate(1462320000000) || weight <= 82kg) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight < 82kg) , v_time := time, v_weight := weight => notify;
}
====
@instagram.new_picture, (link = "http://www.google.com"^^tt:url && filter = "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.get_humidity(), ((value <= 14 || value <= 14 || value >= 7 || value > 55)) , v_value := value => notify;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && leanMass > 14 && bodyFat < 14 && bmi > 11) || (place = $context.location.home && bmi < 11) || (place = $context.location.current_location && weight = 155lb && leanMass < 42 && bodyFat > 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => *
;;
Main() {
    @fitness-tracker.getbody(), (((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight < 155lb && bodyFat > 7) || (weight > 155lb && leanMass < 55 && bmi <= 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @twitter.sink(status="12345") ;
}
====
* => @thermostat.get_hvac_state, ((mode = enum(off) && state = enum(heating)) || (mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => *
;;
Main() {
    @holidays.uk_holiday_event(), (summary = "0123456788") , v_date := date, v_summary := summary, v_description := description => @thermostat.get_hvac_state(), ((state = enum(cooling) || mode = enum(heat) || mode = enum(cool) || state = enum(cooling))) , v_mode := mode, v_state := state => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), (((timestamp =~ "0123456788" && from = "bob"^^tt:username && message =~ "9876543210") || (channel = "lol"^^tt:hashtag && message =~ "9876543210"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @dropbox.move(old_name=v_message, new_name=v_message) ;
}
====
@google_drive.new_drive_file, file_name = "23456789" , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "9876543210") , v_file_name := file_name => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
now => @icalendar.list_events, (end_date = makeDate(1487030400000) && sequence <= 11) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && description =~ "ABCDEFGHIJ" && sequence >= 7 && organizer = "0123456788" && location =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @almond_dates.search, ((interest = "0123456789" && poster = "12345" && phone = "+15551234567"^^tt:phone_number) || poster =~ "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight <= 155lb && leanMass > 11 && bmi > 42) || (time = makeDate(1462320000000) && weight >= 155lb && leanMass < 14 && bodyFat <= 11 && bmi <= 14) || (time = makeDate(1487030400000) && leanMass <= 42 && bodyFat >= 11))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_dates.search(), ((interest = "12345" && message = "12345" && poster =~ "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @almond_bike_market.monitor(), true  => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@heartrate-monitor.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => *
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate < 55) || time = makeDate(1487030400000))) , v_time := time, v_place := place, v_heartrate := heartrate => @slack.updateChannelPurpose(channel="funny"^^tt:hashtag, purpose="0123456789") ;
}
====
* => @linkedin.get_profile, ((headline =~ "ABCDEFGHIJ" && specialties =~ "12345") || contains(positions, "12345") || (headline =~ "0123456788" && specialties = "9876543210" && contains(positions, "0123456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @holidays.uk_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @linkedin.get_profile(), ((formatted_name =~ "23456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @almond_couch_market.search, (price = 7 || (title =~ "12345" && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@nasa.asteroid, (asteroid_id >= 14 && name =~ "0123456788" && estimated_diameter_min = 1000m && estimated_diameter_max <= 42cm && dangerous = true && relative_velocity >= 25mph && orbiting_body = "9876543210") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 14 && name =~ "0123456789" && estimated_diameter_min <= 42cm && estimated_diameter_max < 1000m && relative_velocity < 25mph && closest_distance_to_earth = 1000m) || (asteroid_id < 55 && estimated_diameter_min <= 42cm && estimated_diameter_max <= 42cm && orbiting_body = "23456789") || (asteroid_id >= 7 && estimated_diameter_min < 1000m && relative_velocity < 25mph && closest_distance_to_earth > 1000m && orbiting_body = "0123456788") || (asteroid_id >= 14 && estimated_diameter_min <= 1000m && estimated_diameter_max > 1000m && relative_velocity > 5kmph && closest_distance_to_earth <= 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
now => @wall_street_journal.get_opinions, title =~ "12345" , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @light-bulb.set_power(power=enum(off)) ;
}
====
* => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    @omlet.newmessage(), (message =~ "ABCDEFGHIJ") , v_type := type, v_message := message => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @phone.notify, title = "0123456789" 
;;
Main() {
    now => @phone.notify(title="ABCDEFGHIJ", message="ABCDEFGHIJ") ;
}
====
* => @almond_couch_market.search, (price <= 11 && contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((title = "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @almond_couch_market.search(), ((title =~ "ABCDEFGHIJ" && price = 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => @builtin.get_random_between, ((high < 14 && random < 55) || random <= 42) , v_random := random => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @builtin.get_random_between(low=42, high=55), (((high > 55 && random < 14) || random = 7 || (low >= 55 && high = 42 && random <= 11))) , v_random := random => notify;
}
====
@sportradar.soccer_eu_team, ((game_status = enum(closed) && away_points = 42 && home_points > 42) || (other_team_alias = "bar"^^sportradar:eu_soccer_team("Barcellona") && away_name = "0123456788" && game_status = enum(inprogress) && scheduled_time = makeDate(1487030400000) && home_points >= 55)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="fcb"^^sportradar:eu_soccer_team("Bayern Munchen")), (((away_name = "0123456788" && home_name =~ "0123456788" && away_points < 7) || (game_status = enum(scheduled) && away_points > 14))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
@youtube.source, ((channel_title =~ "12345" && title =~ "12345" && video_id =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || channel_id =~ "ABCDEFGHIJ" || (channel_id =~ "9876543210" && title = "0123456789" && video_id = "12345" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id = "23456789" && title =~ "9876543210" && video_id =~ "0123456788") || channel_title =~ "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@reddit_front_page.newpost, category =~ "12345" , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user = "23456789" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
@gmail.receive_email, (date = makeDate(1487030400000) && contains(labels, "ABCDEFGHIJ") && snippet = "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), (subject =~ "0123456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number) || price >= 11 || price > 11 || (title =~ "12345" && price >= 7))) , v_title := title, v_price := price, v_contact := contact => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
* => @almond_bike_market.post, (title =~ "9876543210" && poster =~ "0123456789") 
;;
Main() {
    @nasa.asteroid(), ((estimated_diameter_min > 42cm && closest_distance_to_earth <= 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.post(title=v_orbiting_body, price=v_asteroid_id, info=v_name, poster=v_name) ;
}
====
@github.new_issue, ((number > 14 && title = "0123456789" && body =~ "12345") || repo_name =~ "ABCDEFGHIJ" || (repo_name = "0123456788" && user = "alice"^^tt:username && number >= 14 && body = "23456789") || (repo_name = "9876543210" && number > 11 && title = "0123456789" && body = "0123456789") || (user = "alice"^^tt:username && number > 7 && title =~ "9876543210" && time = makeDate(1462320000000))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue(repo_name="12345"), ((repo_name =~ "ABCDEFGHIJ" && number < 14 && time = makeDate(1462320000000))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @wall_street_journal.get_opinions(), (title = "9876543210") , v_title := title, v_link := link, v_updated := updated => @dropbox.move(old_name="0123456788", new_name="ABCDEFGHIJ") ;
}
====
* => @linkedin.share, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @gmail.receive_important_email(), true , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @linkedin.share(status=v_subject) ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration <= 30min && rem < 30min && light > 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((place = $context.location.home && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration >= 30min && rem < 30min && light < 2h && deep > 2h) || (time = makeDate(1462320000000) && duration <= 30min && deep > 30min) || light <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@twitter.source, (text = "0123456788" && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && __reserved = false) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.source(), ((contains(urls, "http://www.google.com"^^tt:url) || in_reply_to = "alice"^^tt:username || (from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @thecatapi.get(), (((count <= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "9876543210" && count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @linkedin.share(status="0123456789") ;
}
====
now => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @thermostat.set_hvac_mode(mode=enum(cool)) ;
}
====
* => @nasa.apod, title = "0123456788" , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), ((title =~ "12345" || description =~ "9876543210")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@thermostat.temperature, value = 73F , v_time := time, v_value := value => notify
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1462320000000) || time = makeDate(1462320000000))) , v_time := time, v_value := value => notify;
}
====
* => @builtin.cool, true 
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @builtin.cool() ;
}
====
now => * => @twitter.send_direct_message, (to = "alice"^^tt:username && message =~ "0123456788") 
;;
Main() {
    now => @dropbox.get_space_usage(), (((used <= 20MB && total > 20MB) || used >= 5KB)) , v_used := used, v_total := total => @twitter.send_direct_message(to="alice"^^tt:username, message="23456789") ;
}
====
@youtube.source, (title =~ "9876543210" && video_url = "http://www.abc.def"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((channel_id =~ "12345" && title =~ "23456789" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_couch_market.search(), ((title = "23456789" && price <= 14)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @omlet.send_picture, (to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((updated = makeDate(1487030400000) || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @omlet.send_picture(to=v_title, picture_url=v_title, caption=v_title) ;
}
====
* => @wall_street_journal.get_opinions, updated = makeDate(1462320000000) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "0123456788" || updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@almond_bike_market.monitor, (info = "12345" || (info =~ "0123456789" && price > 11) || price <= 42)  => *
;;
Main() {
    @almond_bike_market.monitor(info="23456789"), ((info =~ "9876543210" && price <= 11))  => @onedrive.rename_file(old_name="0123456789", new_name="9876543210") ;
}
====
* => @giphy.get, true , v_picture_url := picture_url => notify
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), (((watched_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name =~ "ABCDEFGHIJ" && home_name =~ "9876543210" && away_points < 11 && home_points >= 14 && result = enum(unclosed)) || (watched_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && home_name = "12345" && result = enum(win)))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@security-camera.new_event, (has_person = true || (start_time = makeDate(1487030400000) && has_motion = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((has_sound = true && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, (day = "23456789" && distance < 1000m && steps < 42 && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "0123456789" && distance = 42cm && steps < 11 && activeTime > 30min && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
* => @xkcd.random_comic, ((title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 42 && title =~ "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @gmail.receive_email(), (((from_address = "bob@stanford.edu"^^tt:email_address && subject = "23456789" && contains(labels, "ABCDEFGHIJ") && snippet =~ "ABCDEFGHIJ") || (from_address = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && date = makeDate(1462320000000) && contains(labels, "0123456788")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @xkcd.random_comic(), ((number > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@washington_post.new_article, description =~ "0123456789" , v_title := title, v_link := link, v_description := description => * => notify
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (title =~ "9876543210") , v_title := title, v_link := link, v_description := description => @holidays.next_us_holiday_event(), (description = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @lg_webos_tv.set_power, true 
;;
Main() {
    @wall_street_journal.lifestyle(), true , v_title := title, v_link := link => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
* => @security-camera.get_snapshot, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => notify
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
@gmail.receive_email, (date = makeDate(1487030400000) && contains(labels, "ABCDEFGHIJ") && snippet = "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), ((from_name =~ "ABCDEFGHIJ" && from_address = "alice@gmail.com"^^tt:email_address && subject = "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @thermostat.get_hvac_state(), ((mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => notify;
}
====
now => @almond_dates.search, (poster = "12345" || (message = "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "0123456789" && message =~ "12345") || poster =~ "23456789" || (message =~ "12345" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (message =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || phone = "+15551234567"^^tt:phone_number || poster =~ "0123456789" || (message = "0123456789" && poster =~ "9876543210") || phone = "+16501234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @almond_dates.search(), ((poster =~ "23456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@onedrive.file_modified, (file_name =~ "ABCDEFGHIJ" || file_name = "0123456789" || file_name =~ "9876543210") , v_file_name := file_name => @builtin.get_random, (number > 42 || number >= 7 || number >= 55) , v_number := number => notify
;;
Main() {
    @onedrive.file_modified(), (file_name = "12345") , v_file_name := file_name => @builtin.get_random(), true , v_number := number => notify;
}
====
@twitter.direct_message, (message =~ "23456789" && __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => *
;;
Main() {
    @twitter.direct_message(), ((message =~ "0123456789" && __reserved = true)) , v_from := from, v_message := message, v___reserved := __reserved => @light-bulb.set_power(power=enum(on)) ;
}
====
now => @xkcd.get_comic, ((number < 7 && title = "23456789" && alt_text = "23456789") || (number < 14 && title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number >= 11 && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(number=42), ((number = 11 && alt_text =~ "12345")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @tumblr-blog.post_picture, true 
;;
Main() {
    @phdcomics.new_post(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @tumblr-blog.post_picture(caption=v_title, picture_url=v_picture_url) ;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    now => @youtube.search_videos(query="0123456789"), ((query =~ "12345" && video_id =~ "23456789" && channel_id =~ "ABCDEFGHIJ" && channel_title = "23456789" && title =~ "0123456788" && count >= 11)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @light-bulb.color_loop() ;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "0123456789" && industry = "0123456789" && specialties = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @facebook.post(status=v_formatted_name) ;
}
====
@phone.gps, (bearing >= 14 && speed = 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), (((bearing <= 7 && speed > 5kmph) || (altitude <= 1000m && bearing <= 11 && speed >= 25mph) || (altitude >= 1000m && bearing < 14 && speed >= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
* => @almond_couch_market.search, title = "9876543210" , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (price > 11) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@fitness-tracker.getbody, ((leanMass >= 42 && bodyFat >= 7 && bmi < 55) || (leanMass < 14 && bodyFat <= 55) || (time = makeDate(1487030400000) && weight = 155lb && leanMass <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1487030400000) && weight < 155lb && leanMass >= 14 && bodyFat >= 7 && bmi < 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_bike_market.search(info="0123456789"), ((price >= 55 || price < 14))  => notify;
}
====
now => @thecatapi.get, (image_id = "0123456788" && count < 11 && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @security-camera.set_power, true 
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "0123456789" && count <= 14)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @security-camera.set_power(power=enum(off)) ;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1487030400000) && summary = "23456789") || (summary = "23456789" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.uk_holiday_event(), (summary = "12345") , v_date := date, v_summary := summary, v_description := description => @builtin.hello() ;
}
====
@github.new_commit, (repo_name = "9876543210" && user = "bob"^^tt:username) , v_user := user, v_message := message, v_time := time => * => *
;;
Main() {
    @github.new_commit(repo_name="12345"), (((repo_name = "9876543210" && user = "alice"^^tt:username && time = makeDate(1487030400000)) || repo_name = "ABCDEFGHIJ")) , v_user := user, v_message := message, v_time := time => notify;
}
====
@github.new_milestone, description = "0123456789" , v_user := user, v_description := description, v_time := time => @phone.get_gps, ((altitude <= 42cm && bearing > 42 && speed > 25mph) || speed = 5kmph || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 1000m && bearing < 7) || (altitude <= 1000m && bearing < 42 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    @github.new_milestone(repo_name="0123456788"), ((user = "bob"^^tt:username && description =~ "9876543210")) , v_user := user, v_description := description, v_time := time => @phone.get_gps(), ((location = $context.location.work && altitude <= 42cm && bearing < 11 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @car.honk_horn() ;
}
====
now => * => @almond_bike_market.post, title =~ "23456789" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary =~ "23456789" || summary = "12345" || (summary = "0123456789" && description =~ "0123456789") || description = "0123456788" || (date = makeDate(1487030400000) && summary =~ "9876543210" && description = "9876543210"))) , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post(title="0123456789", price=11, info="ABCDEFGHIJ", poster="ABCDEFGHIJ") ;
}
====
now => * => @gmail.send_picture, message =~ "12345" 
;;
Main() {
    now => @builtin.get_random(), true , v_number := number => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="ABCDEFGHIJ", message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @omlet.send_to, (message =~ "12345" || (to = "bob"^^tt:username && message = "0123456789") || message =~ "0123456789") 
;;
Main() {
    @wall_street_journal.technology(), ((title =~ "0123456788" || (title = "9876543210" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => @omlet.send_to(to="alice"^^tt:username, message="12345") ;
}
====
@phdcomics.new_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.abc.def"^^tt:url || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @weatherapi.moon, (date = makeDate(1462320000000) || date = makeDate(1462320000000)) , v_date := date, v_phase := phase => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @weatherapi.moon(location=$context.location.current_location), true , v_date := date, v_phase := phase => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary = "0123456789" && sequence < 42)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @builtin.sorry() ;
}
====
* => @fitbit.getbody, ((bmi >= 7 && fat <= 7) || (weight >= 155lb && fat <= 11) || weight <= 82kg) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && cloudiness <= 11 && fog = 11) || (humidity <= 7 && cloudiness < 42) || (temperature < 73F && humidity < 7 && fog <= 14 && status = enum(drizzling)) || (location = $context.location.home && temperature >= 22C && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 14 && fog >= 7 && weather = "23456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @fitbit.getbody(), ((bmi <= 55 && fat <= 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@xkcd.new_whatif, title = "12345" , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), (title =~ "23456789") , v_title := title, v_link := link => @youtube.search_videos(query=v_title), (((channel_id =~ "ABCDEFGHIJ" && count <= 7) || (video_id =~ "12345" && channel_title =~ "23456789" && count <= 42))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
now => @fitbit.getsteps, (steps >= 42 || (date = makeDate(1487030400000) && steps <= 7)) , v_date := date, v_steps := steps => @light-bulb.set_power, true 
;;
Main() {
    now => @fitbit.getsteps(), ((date = makeDate(1487030400000) || steps >= 7)) , v_date := date, v_steps := steps => @light-bulb.set_power(power=enum(off)) ;
}
====
now => * => @almond_bike_market.post, (price = 42 && info =~ "0123456788") 
;;
Main() {
    now => @almond_bike_market.post(title="9876543210", price=14, info="12345", poster="ABCDEFGHIJ") ;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) && heartrate > 7) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate <= 42 || (place = $context.location.home && heartrate < 14) || heartrate > 42 || heartrate >= 14)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @gmail.send_email, ((subject = "0123456788" && message =~ "0123456789") || (to = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && message = "9876543210")) 
;;
Main() {
    @omlet.newmessage(), (message =~ "0123456788") , v_type := type, v_message := message => @gmail.send_email(subject=v_message, message=v_message, to="bob@stanford.edu"^^tt:email_address) ;
}
====
@phdcomics.new_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @omlet.send_to(to=v_title, message=v_title) ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && label = "12345") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @almond_couch_market.monitor(), (price > 42) , v_title := title, v_price := price, v_contact := contact => @gmail.get_latest(), (label =~ "9876543210") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @almond_dates.search, ((interest = "9876543210" && message = "0123456788") || (interest = "0123456788" && message =~ "ABCDEFGHIJ" && poster = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "9876543210" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @gmail.send_picture, (message =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "0123456788" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @gmail.send_picture(to=v_message, picture_url=v_message, subject="ABCDEFGHIJ", message="12345") ;
}
====
now => @almond_dates.post, poster = "0123456788" 
;;
Main() {
    now => @almond_dates.post(interest="0123456789", message="0123456789", poster="9876543210", phone="+15551234567"^^tt:phone_number) ;
}
====
@almond_dates.monitor, ((interest = "ABCDEFGHIJ" && poster =~ "0123456789") || (interest = "ABCDEFGHIJ" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "ABCDEFGHIJ" && message = "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => * => @builtin.cool, true 
;;
Main() {
    now => @gmail.get_latest(), ((subject = "9876543210" && label =~ "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => @builtin.cool() ;
}
====
now => * => @gmail.send_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @nasa.rover(), (((date_taken = makeDate(1487030400000) && count >= 14 && camera_used =~ "0123456789") || (date_taken = makeDate(1487030400000) && count = 14))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @gmail.send_picture(to=v_camera_used, subject=v_camera_used, message=v_camera_used, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @instagram.get_pictures, (filter =~ "ABCDEFGHIJ" || count >= 42 || (count > 7 && caption = "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @instagram.get_pictures(), ((count < 14 && link = "http://www.abc.def"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject =~ "0123456789" && message =~ "0123456788") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title = "0123456789") , v_title := title, v_link := link, v_updated := updated => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="9876543210") ;
}
====
@tumblr.new_photo, (caption =~ "ABCDEFGHIJ" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), ((blog_name =~ "0123456788" && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify;
}
====
now => @twitter.search_by_hashtag, ((text =~ "9876543210" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count <= 7 && contains(urls, "http://www.google.com"^^tt:url)) || (query_hashtag = "funny"^^tt:hashtag && count <= 7 && text =~ "23456789") || (count >= 7 && text =~ "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username) || (count < 42 && text =~ "ABCDEFGHIJ") || (query_hashtag = "cat"^^tt:hashtag && text = "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username) || (count < 11 && text =~ "ABCDEFGHIJ" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @google_drive.create_new_drive_file, true 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="alice"^^tt:username), (query_hashtag = "lol"^^tt:hashtag) , v_text := text, v_hashtags := hashtags, v_urls := urls => @google_drive.create_new_drive_file(file_name=v_text) ;
}
====
* => @icalendar.list_events, (summary =~ "9876543210" && description =~ "ABCDEFGHIJ" && sequence > 14 && organizer = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary =~ "0123456788" && sequence >= 14 && organizer =~ "0123456789" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @youtube.search_videos, video_url = "http://www.google.com"^^tt:url , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="0123456788"), (((query =~ "12345" && video_id =~ "12345" && channel_id = "ABCDEFGHIJ" && channel_title = "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_title = "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((wind_speed > 5kmph && humidity <= 55 && cloudiness >= 11 && fog < 14) || (wind_speed >= 5kmph && humidity > 55 && fog <= 11 && weather = "9876543210" && status = enum(drizzling)) || (temperature >= 73F && wind_speed < 5kmph && humidity >= 14 && cloudiness >= 55 && fog > 55 && weather =~ "0123456788"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @security-camera.set_power(power=enum(on)) ;
}
====
@jawbone_up.getsleep, ((asleepTime = makeDate(1487030400000) && duration <= 30min && light > 30min) || (awakeTime = makeDate(1487030400000) && duration > 2h && rem < 2h && light > 2h && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @jawbone_up.getsleep(), ((awakeTime = makeDate(1487030400000) && duration < 30min && rem > 30min && light >= 2h && deep = 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@phone.receive_sms, (from = "+16501234567"^^tt:phone_number && body =~ "0123456789") , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), ((body = "9876543210" || (from = "+16501234567"^^tt:phone_number && body =~ "23456789"))) , v_from := from, v_body := body => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && description = "ABCDEFGHIJ" && location =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @fitbit.recordweight, (weight > 155lb || weight >= 155lb) 
;;
Main() {
    now => @twitter.search(query="23456789", in_reply_to="alice"^^tt:username), ((query =~ "12345" && text = "0123456788" && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @fitbit.recordweight(weight=155lb) ;
}
====
now => @onedrive.delete_file, (file_name =~ "23456789" || file_name =~ "0123456789") 
;;
Main() {
    now => @onedrive.delete_file(file_name="23456789") ;
}
====
@heartrate-monitor.getheartrate, (heartrate > 7 || time = makeDate(1487030400000) || (place = $context.location.work && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate > 42)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @almond_dates.post, ((interest =~ "9876543210" && message =~ "ABCDEFGHIJ") || (interest =~ "12345" && message = "9876543210" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @almond_dates.post(interest=v_title, message=v_title, phone=v_title, poster="0123456789") ;
}
====
now => @almond_bike_market.detail, id =~ "9876543210"  => notify
;;
Main() {
    now => @almond_bike_market.detail(id="0123456789"), true  => notify;
}
====
@instagram.new_picture, (link = "http://www.abc.def"^^tt:url && filter = "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.get_hvac_state(), (mode = enum(heat_cool)) , v_mode := mode, v_state := state => notify;
}
====
now => @dropbox.list_folder, ((folder_name = "0123456789" && file_name = "23456789" && file_size >= 20MB && full_path =~ "0123456788") || file_name = "23456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="0123456788"), (((file_name =~ "ABCDEFGHIJ" && file_size <= 5KB && full_path =~ "12345") || (folder_name = "0123456788" && is_folder = true))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@phdcomics.new_post, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "0123456788" && link = "http://www.abc.def"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_dates.search, message =~ "23456789" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_dates.search(), (poster = "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && activeTime > 30min && inactiveTime <= 2h && calories > 500kcal) || (distance > 42cm && steps < 7 && activeTime >= 2h && inactiveTime >= 2h && calories = 500kcal) || (updateTime = makeDate(1487030400000) && day =~ "0123456789" && steps >= 11 && activeTime < 2h && inactiveTime < 2h) || (distance >= 42cm && activeTime < 30min && inactiveTime < 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1487030400000) && day =~ "ABCDEFGHIJ" && distance = 42cm && steps = 55 && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @almond_dates.post, (interest =~ "9876543210" && phone = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @onedrive.file_modified(), ((file_name = "9876543210" || file_name =~ "23456789")) , v_file_name := file_name => @almond_dates.post(interest="9876543210", message="9876543210", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @imgflip.generate, (template = "12345" && text_bottom = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => @facebook.post, true 
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="0123456789", text_bottom="9876543210"), ((template = "23456789" && text_top = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @facebook.post(status=v_name) ;
}
====
@almond_dates.monitor, poster =~ "ABCDEFGHIJ" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (message =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
@instagram.new_picture, (filter =~ "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), (contains(hashtags, "cat"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @linkedin.get_profile, (headline = "23456789" || (formatted_name =~ "ABCDEFGHIJ" && industry = "0123456788") || (formatted_name =~ "0123456788" && specialties =~ "ABCDEFGHIJ" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((specialties =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@reddit_front_page.newpost, ((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category = "ABCDEFGHIJ") || (user = "12345" && category =~ "0123456788") || (title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && category =~ "0123456789") || title = "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => @onedrive.create_file, true 
;;
Main() {
    @reddit_front_page.newpost(), ((title = "0123456789" && user =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @onedrive.create_file(file_name=v_category, contents="0123456789") ;
}
====
* => @google_drive.create_new_drive_file, true 
;;
Main() {
    @builtin.timer(interval=2h), (interval >= 30min)  => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
now => * => @almond_dates.post, phone = "+15551234567"^^tt:phone_number 
;;
Main() {
    now => @almond_bike_market.search(), ((price >= 42 || price >= 7))  => @almond_dates.post(interest="12345", message="23456789", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @builtin.notify, message = "9876543210" 
;;
Main() {
    @almond_dates.monitor(), ((interest = "12345" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.notify(message="9876543210") ;
}
====
@wall_street_journal.markets, true , v_title := title, v_link := link => * => *
;;
Main() {
    @wall_street_journal.markets(), ((title = "23456789" || title =~ "0123456789" || link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @bluetooth_speaker.raise_volume() ;
}
====
@builtin.at, time = makeTime(20,30)  => * => *
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @bluetooth_speaker.play_music() ;
}
====
@phone.gps, ((location = $context.location.work && altitude > 42cm && bearing > 42 && speed < 25mph) || (altitude < 1000m && bearing > 11 && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @phone.gps(), (((altitude < 42cm && bearing = 42 && speed >= 25mph) || (bearing >= 42 && speed = 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@instagram.new_picture, ((caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter =~ "0123456789" && contains(hashtags, "cat"^^tt:hashtag)) || (caption =~ "9876543210" && filter =~ "12345") || (caption = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && filter =~ "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((contains(hashtags, "lol"^^tt:hashtag) || (link = "http://www.google.com"^^tt:url && filter =~ "ABCDEFGHIJ"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => notify;
}
====
now => @dropbox.open, (file_name = "12345" && url =~ "0123456789") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="0123456789"), ((url = "23456789" || (file_name =~ "12345" && url = "0123456789"))) , v_url := url => notify;
}
====
@youtube.source, true , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @google_drive.create_new_drive_file, true 
;;
Main() {
    @youtube.source(), (channel_id =~ "0123456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @google_drive.create_new_drive_file(file_name=v_channel_title) ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && duration <= 2h && light < 2h && deep < 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((duration > 30min && light < 30min && deep > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
@yahoofinance.stock_quote, ((company_name =~ "0123456788" && ask_price >= 42 && bid_price < 14) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "9876543210" && ask_price <= 11) || (company_name =~ "12345" && ask_price >= 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @phone.get_gps, ((altitude > 42cm && bearing <= v_bid_price && speed >= 25mph) || (location = $context.location.work && altitude > 1000m && bearing > v_ask_price && speed <= 25mph) || (bearing > 42 && speed <= 25mph) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 42cm)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), ((ask_price > 42 || (stock_id = "msft"^^tt:stock_id("Microsoft") && ask_price >= 55 && bid_price > 14))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @phone.get_gps(), (((altitude >= 1000m && bearing < 55 && speed > 25mph) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude = 42cm && bearing < 55 && speed >= 5kmph) || location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || (altitude < 1000m && bearing <= 42 && speed > 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => * => @light-bulb.alert_long, true 
;;
Main() {
    now => @almond_bike_market.ask_seller(id="0123456788", property="0123456788", value="0123456789", public=true), ((id = "ABCDEFGHIJ" || id =~ "ABCDEFGHIJ")) , v_answer := answer => @light-bulb.alert_long() ;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @almond_bike_market.post, (title =~ "0123456789" && poster =~ "9876543210") 
;;
Main() {
    now => @ytranslate.detect_language(text="9876543210"), ((text = "23456789" || (text = "23456789" && detected_language = "en"^^tt:iso_lang_code("English")) || text = "ABCDEFGHIJ" || detected_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_detected_language := detected_language => @almond_bike_market.post(title="23456789", price=42, info="ABCDEFGHIJ", poster="9876543210") ;
}
====
now => * => @heatpad.set_power, power = enum(off) 
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="0123456788", text_bottom="9876543210"), (((template = "23456789" && text_bottom =~ "9876543210" && name =~ "0123456788") || (template = "9876543210" && text_top =~ "12345"))) , v_name := name, v_picture_url := picture_url => @heatpad.set_power(power=enum(on)) ;
}
====
now => * => @google_drive.create_new_drive_file, file_name = "23456789" 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
* => @holidays.next_us_holiday_event, (summary =~ "0123456789" || (summary =~ "23456789" && description = "ABCDEFGHIJ") || (summary =~ "0123456788" && description =~ "23456789") || (date = makeDate(1487030400000) && description = "12345") || (date = makeDate(1462320000000) && summary =~ "0123456788") || date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.us_holiday_event(), ((description =~ "0123456789" || (date = makeDate(1487030400000) && summary =~ "12345" && description =~ "9876543210"))) , v_date := date, v_summary := summary, v_description := description => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) && description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@phone.receive_sms, body =~ "ABCDEFGHIJ" , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), ((from = "+15551234567"^^tt:phone_number || from = "+16501234567"^^tt:phone_number)) , v_from := from, v_body := body => notify;
}
====
now => @almond_couch_market.search, (title =~ "0123456789" && price < 14) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((price <= 7 && contact = "+15551234567"^^tt:phone_number) || (title =~ "9876543210" && price < 42 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @twitter.unfollow(user_name=v_title) ;
}
====
* => * => @almond_dates.post, true 
;;
Main() {
    @slack.receive(), ((timestamp =~ "0123456789" && from = "bob"^^tt:username && message =~ "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @almond_dates.post(interest=v_timestamp, message=v_timestamp, poster=v_message, phone=v_timestamp) ;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (category = "0123456788") , v_title := title, v_link := link, v_user := user, v_category := category => @fitbit.recordweight(weight=82kg) ;
}
====
@slack.receive, (timestamp =~ "ABCDEFGHIJ" && from = "alice"^^tt:username && message =~ "23456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "9876543210" || (timestamp = "0123456789" && message = "9876543210"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @phone.get_gps(), ((bearing > 55 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @almond_bike_market.detail, (id = "ABCDEFGHIJ" || id = "12345" || id = "12345" || id = "0123456789" || id = "0123456789" || id = "23456789" || id = "9876543210")  => notify
;;
Main() {
    now => @almond_bike_market.detail(id="0123456788"), true  => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature >= 22C && wind_speed < 25mph && humidity >= 55 && cloudiness >= 7 && fog <= 11 && weather = "9876543210") || (temperature > 73F && humidity > 42 && weather =~ "ABCDEFGHIJ"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @heatpad.set_power(power=enum(off)) ;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && category =~ "12345") || category = "0123456788" || (title = "12345" && user = "0123456788" && category =~ "0123456788") || (title =~ "23456789" && link = "http://www.google.com"^^tt:url && user =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => *
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "0123456789" || title = "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @bluetooth_speaker.lower_volume() ;
}
====
* => @holidays.next_uk_holiday_event, description =~ "0123456788" , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @holidays.next_uk_holiday_event(), (summary =~ "9876543210") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @imgflip.list, name = "0123456789" , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), ((name = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && day = "0123456789" && steps > 42 && activeTime >= 2h && calories <= 500kcal) || (day =~ "12345" && activeTime > 2h && inactiveTime >= 2h && calories <= 500kcal) || (distance < 42cm && activeTime >= 2h && inactiveTime >= 2h && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((day =~ "0123456789" && activeTime <= 2h && inactiveTime <= 2h && calories >= 500kcal) || (day =~ "0123456789" && distance >= 1000m && steps >= 7 && inactiveTime <= 30min) || (day = "23456789" && steps < 11 && activeTime >= 30min && inactiveTime < 30min && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_dates.search(), (poster =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@nasa.asteroid, ((asteroid_id < 55 && estimated_diameter_max < 42cm && relative_velocity >= 25mph && closest_distance_to_earth >= 1000m && orbiting_body =~ "23456789") || (asteroid_id >= 14 && estimated_diameter_max < 1000m && dangerous = true && relative_velocity >= 5kmph && closest_distance_to_earth < 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 7 && name = "ABCDEFGHIJ" && estimated_diameter_max < 42cm && relative_velocity <= 25mph && closest_distance_to_earth < 42cm && orbiting_body = "9876543210") || (asteroid_id > 42 && name =~ "0123456788" && estimated_diameter_min > 42cm && estimated_diameter_max = 42cm && dangerous = true && relative_velocity <= 25mph))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_date(), ((date = makeDate(1462320000000) || date = makeDate(1462320000000) || date = makeDate(1487030400000) || date = makeDate(1462320000000))) , v_date := date => notify;
}
====
@almond_dates.monitor, (message =~ "ABCDEFGHIJ" || (interest =~ "9876543210" && message = "0123456788") || (interest = "12345" && poster =~ "12345") || (interest = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && message = "ABCDEFGHIJ") || (message =~ "23456789" && poster =~ "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (interest = "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @linkedin.get_profile(), ((industry = "ABCDEFGHIJ" && specialties =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@rss.new_post, title =~ "0123456788" , v_title := title, v_link := link => notify
;;
Main() {
    @rss.new_post(), ((title = "12345" || link = "http://www.abc.def"^^tt:url || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify;
}
====
@reddit_front_page.newpost, user = "ABCDEFGHIJ" , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (((user = "0123456788" && category = "9876543210") || title =~ "23456789" || (link = "http://www.google.com"^^tt:url && user =~ "0123456788" && category = "9876543210") || (title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && user = "0123456789" && category = "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @thermostat.set_target_temperature, (value <= 73F || value > 73F) 
;;
Main() {
    now => @gmail.get_latest(), (label =~ "23456789") , v_from := from, v_subject := subject, v_label := label => @thermostat.set_target_temperature(value=22C) ;
}
====
@almond_couch_market.monitor, (price >= 11 || (price > 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => @bing.web_search(query=v_title), ((description = "23456789" || (query = "23456789" && description =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_description := description, v_link := link => notify;
}
====
@activity-tracker.getmove, ((distance = 42cm && steps = 42 && inactiveTime > 2h) || (updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && distance > 1000m && activeTime = 30min && inactiveTime <= 2h) || (day = "12345" && distance >= 1000m && activeTime > 30min && inactiveTime >= 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @twitter.search, ((query =~ v_day && count <= v_steps && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username) || (query = "0123456788" && count > 14 && in_reply_to = "alice"^^tt:username) || (query =~ "9876543210" && count > 11 && text = "23456789" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @activity-tracker.getmove(), ((day = "9876543210" && distance < 1000m && steps >= 42 && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @twitter.search(query="23456789", count=14, from="alice"^^tt:username, in_reply_to="bob"^^tt:username), ((count = 42 && text =~ "0123456788" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @thermostat.set_hvac_mode, true 
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), ((link = "http://www.google.com"^^tt:url && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
@gmail.receive_primary_email, ((from_name = "12345" && subject =~ "0123456788") || subject = "ABCDEFGHIJ" || (from_name = "0123456789" && subject = "0123456788") || (subject = "0123456789" && contains(labels, "0123456788"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => *
;;
Main() {
    @gmail.receive_primary_email(), (((from_address = "alice@gmail.com"^^tt:email_address && snippet =~ "23456789") || (from_name = "23456789" && contains(labels, "ABCDEFGHIJ") && snippet =~ "9876543210"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.hello() ;
}
====
@holidays.us_holiday_event, description = "12345" , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), ((summary = "12345" && description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @xkcd.get_comic(), ((number < 14 && title =~ "12345")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@phdcomics.new_post, (title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @xkcd.get_comic(), (((number < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text =~ "ABCDEFGHIJ") || (number >= 14 && alt_text =~ "ABCDEFGHIJ") || (number > 55 && title = "23456789") || (number >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "0123456789") , v_file_name := file_name => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="0123456788", message="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @twitter.follow, user_name = "alice"^^tt:username 
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((sunrise = makeDate(1487030400000) && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @twitter.follow(user_name="bob"^^tt:username) ;
}
====
now => @almond_dates.search, (message =~ "0123456788" || (message = "0123456789" && poster =~ "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @wall_street_journal.get_opinions, title = "12345" , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @almond_bike_market.monitor(price=42), true  => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1462320000000) && summary = "12345" && description =~ "9876543210") || (date = makeDate(1462320000000) && summary =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @twitter.source(), ((text = "0123456789" && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @holidays.next_uk_holiday_event(), (((date = makeDate(1487030400000) && summary = "9876543210") || (summary = "12345" && description =~ "9876543210") || description = "9876543210")) , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post(title="23456789", price=14, info="ABCDEFGHIJ", poster="23456789") ;
}
====
@tumblr.new_text, (date = makeDate(1487030400000) && body = "12345") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="ABCDEFGHIJ"), (((date = makeDate(1462320000000) && title =~ "23456789" && contains(tags, "lol"^^tt:hashtag)) || (date = makeDate(1462320000000) && title =~ "12345" && link = "http://www.google.com"^^tt:url) || (blog_name = "9876543210" && body = "0123456789") || (date = makeDate(1487030400000) && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag) && body =~ "9876543210") || (blog_name =~ "12345" && title = "12345" && link = "http://www.google.com"^^tt:url))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
* => @ytranslate.detect_language, (text =~ "23456789" || text = "9876543210") , v_detected_language := detected_language => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((temperature >= 73F && cloudiness < 55 && fog >= 11) || (wind_speed = 25mph && humidity > 14 && fog >= 42 && weather =~ "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @ytranslate.detect_language(text=v_weather), (text = "12345") , v_detected_language := detected_language => notify;
}
====
@google_drive.new_drive_file, (file_name = "23456789" || file_name = "9876543210" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "12345") , v_file_name := file_name => @icalendar.list_events(), ((summary = "12345" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @fitbit.getbody, ((weight >= 155lb && bmi >= 55 && fat >= 55) || (bmi <= 42 && fat < 11) || fat > 14 || (weight >= 155lb && fat >= 7) || (bmi <= 11 && fat > 11) || (weight < 155lb && bmi >= 7 && fat = 11) || fat = 11 || bmi >= 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => *
;;
Main() {
    now => @fitbit.getbody(), (((weight >= 155lb && fat <= 11) || (weight > 82kg && bmi > 42) || fat < 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @onedrive.rename_file(old_name="23456789", new_name="0123456789") ;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    @github.new_issue(repo_name="9876543210"), (((repo_name =~ "23456789" && number > 55 && title =~ "0123456789" && body =~ "9876543210" && time = makeDate(1462320000000)) || (repo_name = "0123456789" && body = "23456789" && time = makeDate(1462320000000)) || (repo_name =~ "12345" && user = "alice"^^tt:username && number < 55 && title =~ "12345" && body =~ "ABCDEFGHIJ"))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @thecatapi.get(), (((image_id = "0123456789" && count < 55 && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @facebook.post(status="9876543210") ;
}
====
@activity-tracker.getmove, ((day =~ "ABCDEFGHIJ" && distance < 42cm && inactiveTime >= 2h) || (day =~ "0123456789" && activeTime = 2h && calories <= 500kcal) || (day = "0123456789" && distance >= 1000m && activeTime < 2h && inactiveTime >= 2h && calories < 500kcal) || (updateTime = makeDate(1462320000000) && day =~ "9876543210" && steps <= 7 && inactiveTime < 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance <= 42cm && steps > 11 && activeTime >= 2h && inactiveTime >= 30min && calories > 500kcal) || (day = "12345" && activeTime >= 30min))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thermostat.get_temperature(), ((value > 22C || value < 73F)) , v_value := value => notify;
}
====
@onedrive.file_modified, true , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @heatpad.set_power(power=enum(on)) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "9876543210" && updated = makeDate(1487030400000)) || link = "http://www.google.com"^^tt:url || (title = "23456789" && updated = makeDate(1487030400000)) || updated = makeDate(1462320000000) || updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @slack.receive(), (((channel = "lol"^^tt:hashtag && from = "alice"^^tt:username && message = "23456789") || (timestamp = "12345" && from = "bob"^^tt:username) || timestamp =~ "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@thermostat.humidity, (value >= 11 || (time = makeDate(1487030400000) && value < 42)) , v_time := time, v_value := value => *
;;
Main() {
    @thermostat.humidity(), (((time = makeDate(1462320000000) && value <= 7) || value < 14)) , v_time := time, v_value := value => @light-bulb.alert_long() ;
}
====
@slack.receive, (channel = "cat"^^tt:hashtag && timestamp = "0123456788" && message = "23456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp =~ "12345" && message = "9876543210")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => * => @twitter.post_picture, true 
;;
Main() {
    @almond_couch_market.monitor(), (((price < 14 && contact = "+15551234567"^^tt:phone_number) || (title =~ "0123456789" && price <= 14) || (title = "0123456788" && price > 42))) , v_title := title, v_price := price, v_contact := contact => @twitter.post_picture(caption="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @google_drive.create_new_drive_file, file_name = "ABCDEFGHIJ" 
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
now => * => @tumblr-blog.post_picture, caption = "23456789" 
;;
Main() {
    now => @tumblr-blog.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @almond_couch_market.search, price > 11 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "12345" && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @almond_dates.search, ((interest =~ "23456789" && message = "9876543210" && poster = "0123456789" && phone = "+16501234567"^^tt:phone_number) || (poster =~ "12345" && phone = "+16501234567"^^tt:phone_number) || (message =~ "12345" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || interest = "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest = "9876543210" && poster = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@youtube.source, ((channel_id = "ABCDEFGHIJ" && title =~ "0123456789" && video_url = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456788" && channel_title = "9876543210") || (channel_title =~ "9876543210" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((title =~ "23456789" || (channel_id =~ "0123456789" && video_id = "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @twitter.search_by_hashtag(query_hashtag=v_channel_title, from=v_title, in_reply_to=v_title), ((query_hashtag = "funny"^^tt:hashtag && count >= 55 && text =~ "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @onedrive.delete_file(file_name=v_text) ;
}
====
* => @almond_dates.search, ((interest = "0123456788" && message =~ "9876543210" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number) || (message = "0123456789" && poster =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((interest = "12345" && poster = "0123456789") || message =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@icalendar.event_begin, summary =~ "ABCDEFGHIJ" , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1487030400000) && summary = "9876543210" && organizer = "23456789" && location =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @security-camera.set_power(power=enum(off)) ;
}
====
* => @gmail.get_latest, (subject = "ABCDEFGHIJ" || label =~ "0123456789" || from = "alice@gmail.com"^^tt:email_address || subject = "23456789") , v_from := from, v_subject := subject, v_label := label => @security-camera.set_power, true 
;;
Main() {
    now => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address && subject =~ "9876543210")) , v_from := from, v_subject := subject, v_label := label => @security-camera.set_power(power=enum(on)) ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), ((weight < 155lb || weight > 82kg || weight <= 82kg || weight >= 155lb || weight <= 155lb)) , v_time := time, v_weight := weight => @almond_dates.search(), ((poster = "0123456788" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.alert_long() ;
}
====
now => @youtube.search_videos, ((channel_title = "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 7) || (query =~ "23456789" && video_id = "0123456789" && description = "23456789" && count >= 7)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="23456789"), (((query =~ "23456789" && video_id =~ "0123456788" && channel_id = "12345" && channel_title = "0123456788" && description =~ "0123456788" && count <= 42) || (video_id = "23456789" && title = "9876543210" && description = "ABCDEFGHIJ" && count < 55) || (video_id = "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && video_url = "http://www.google.com"^^tt:url) || (query = "ABCDEFGHIJ" && video_id =~ "0123456788" && channel_id =~ "ABCDEFGHIJ" && channel_title =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 14) || (query = "23456789" && video_id =~ "9876543210" && channel_id = "0123456788" && count <= 7))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => * => @almond_bike_market.post, title = "12345" 
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @almond_bike_market.post(title=v_file_name, info=v_file_name, poster=v_file_name, price=55) ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && label = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @security-camera.new_event(), (has_motion = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address && subject = "23456789" && label =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => @bluetooth_speaker.set_volume(percent=42) ;
}
====
now => * => @google_drive.create_new_drive_file, file_name =~ "23456789" 
;;
Main() {
    now => @ytranslate.detect_language(text="0123456789"), (detected_language = "it"^^tt:iso_lang_code("Italian")) , v_detected_language := detected_language => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
* => @dropbox.move, new_name =~ "23456789" 
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @dropbox.move(old_name="23456789", new_name="0123456788") ;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="23456789", text_bottom="ABCDEFGHIJ"), (((template = "9876543210" && name =~ "0123456788") || (template = "23456789" && text_bottom = "ABCDEFGHIJ") || (template =~ "0123456788" && text_bottom =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template =~ "0123456789" && name = "0123456789"))) , v_name := name, v_picture_url := picture_url => @twitter.send_direct_message(to="bob"^^tt:username, message="9876543210") ;
}
====
now => @xkcd.get_comic, ((number > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "12345") || (title =~ "0123456789" && alt_text = "0123456788")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(), (((number >= 55 && title =~ "0123456788" && alt_text =~ "12345") || (number >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || alt_text = "ABCDEFGHIJ" || number >= 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @almond_dates.post, (poster = "0123456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @instagram.get_pictures(count=55), ((count < 42 && caption = "23456789" && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(interest=v_filter, message=v_filter, poster=v_filter, phone=v_filter) ;
}
====
now => @uber.time_estimate, (start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "0123456788" && estimate > 2h) , v_uber_type := uber_type, v_estimate := estimate => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), true , v_uber_type := uber_type, v_estimate := estimate => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @imgflip.generate, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @washington_post.new_article(section=enum(lifestyle)), ((title = "9876543210" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_description := description => @imgflip.generate(text_top=v_description, text_bottom=v_description, template="9876543210"), (name =~ "0123456789") , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @builtin.get_time, true , v_time := time => notify
;;
Main() {
    now => @builtin.get_time(), true , v_time := time => notify;
}
====
@twitter.direct_message, (message =~ "23456789" && __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => *
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @builtin.sorry() ;
}
====
* => @imgflip.list, (name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="juv"^^sportradar:eu_soccer_team("Juventus")), ((watched_team_alias = "juv"^^sportradar:eu_soccer_team("Juventus") && other_team_alias = "juv"^^sportradar:eu_soccer_team("Juventus") && home_name = "23456789" && scheduled_time = makeDate(1487030400000) && away_points <= 55 && home_points > 55 && result = enum(draw))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @imgflip.list(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @phone.get_gps, (speed > 5kmph || (altitude < 1000m && bearing >= 14 && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), (speed > 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @youtube.list_subscriptions, ((channel_id = "12345" && title = "9876543210" && description = "12345") || (channel_id = "ABCDEFGHIJ" && title =~ "12345") || (title =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "0123456789" && description = "0123456788") || (title =~ "23456789" && description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    @github.new_issue_comment(repo_name="23456789"), ((user = "alice"^^tt:username || (repo_name =~ "9876543210" && issue_number = 55 && body =~ "23456789") || (repo_name =~ "9876543210" && issue_number = 42 && body =~ "12345") || issue_number = 55)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @youtube.list_subscriptions(), (((description =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || title = "23456789" || description =~ "0123456788" || thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @thecatapi.get, (image_id =~ "23456789" && count >= 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @youtube.source(), ((channel_id = "23456789" && channel_title = "0123456789" && title = "0123456789" && video_id = "23456789" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thecatapi.get(), ((image_id =~ "12345" && count > 14)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @omlet.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && caption = "ABCDEFGHIJ") || (to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788")) 
;;
Main() {
    @almond_couch_market.monitor(), (((price <= 11 && contact = "+16501234567"^^tt:phone_number) || price <= 42)) , v_title := title, v_price := price, v_contact := contact => @omlet.send_picture(picture_url=v_title, caption=v_title, to="bob"^^tt:username) ;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "9876543210" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "23456789" && filter = "12345" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @xkcd.get_comic(), ((number <= 42 && title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @instagram.get_pictures, (count < 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && link = "http://www.abc.def"^^tt:url && filter = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), (((count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456789") || (count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "9876543210" && contains(hashtags, "cat"^^tt:hashtag)) || (link = "http://www.abc.def"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)) || (count = 14 && caption =~ "0123456788" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@omlet.incomingmessage, (message =~ "9876543210" || message = "9876543210" || (type = enum(picture) && message = "ABCDEFGHIJ")) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @twitter.search(in_reply_to=v_message, query="12345", count=55), (((query = "9876543210" && count = 7 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)) || (query =~ "23456789" && count = 14))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && label = "12345") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((from = "alice@gmail.com"^^tt:email_address && subject = "23456789" && label =~ "0123456789") || (from = "alice@gmail.com"^^tt:email_address && subject = "23456789" && label = "9876543210"))) , v_from := from, v_subject := subject, v_label := label => @speaker.lower_volume() ;
}
====
now => * => @slack.updateChannelPurpose, purpose =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, count=7), ((count = 11 && text =~ "0123456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => @slack.updateChannelPurpose(channel=v_text, purpose=v_text) ;
}
====
@almond_dates.monitor, (interest =~ "0123456788" && message =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((poster =~ "12345" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @almond_bike_market.ask_seller, ((id =~ "9876543210" && property =~ "9876543210") || (property =~ "ABCDEFGHIJ" && public = false && answer = "ABCDEFGHIJ") || value = "ABCDEFGHIJ") , v_answer := answer => *
;;
Main() {
    now => @almond_bike_market.ask_seller(id="ABCDEFGHIJ", property="ABCDEFGHIJ", value="12345", public=true), (((property =~ "ABCDEFGHIJ" && answer =~ "12345") || (property = "0123456789" && public = true) || (property = "12345" && public = true && answer =~ "23456789") || value = "ABCDEFGHIJ" || (id =~ "9876543210" && value = "23456789" && answer = "12345"))) , v_answer := answer => @heatpad.set_power(power=enum(off)) ;
}
====
* => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "ABCDEFGHIJ" && steps <= 7 && activeTime <= 30min && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
* => @icalendar.list_events, (end_date = makeDate(1462320000000) && sequence < 7 && organizer =~ "0123456788" && location = "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "0123456788" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "23456789" && sequence = 11 && organizer =~ "12345" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @uber.time_estimate, (uber_type = "23456789" || estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), (((uber_type =~ "9876543210" && estimate >= 2h) || (uber_type =~ "0123456788" && estimate = 30min))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @github.comment_issue, repo_name =~ "23456789" 
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1462320000000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @github.comment_issue(repo_name="23456789", issue_number=14, body="0123456788") ;
}
====
now => * => @almond_dates.post, (interest =~ "9876543210" && message =~ "0123456788" && poster = "9876543210") 
;;
Main() {
    now => @imgflip.list(), ((name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @almond_dates.post(interest="12345", message="9876543210", poster="0123456789", phone="+15551234567"^^tt:phone_number) ;
}
====
@github.new_issue_comment, true , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue_comment(repo_name="0123456789"), (((repo_name = "12345" && user = "alice"^^tt:username && issue_number > 42 && time = makeDate(1462320000000)) || (repo_name = "0123456789" && issue_number >= 11 && time = makeDate(1462320000000)))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @almond_bike_market.search(info=v_body, price=v_issue_number), ((info = "23456789" && price <= 7))  => @car.honk_horn() ;
}
====
now => * => @almond_bike_market.post, price = 7 
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "12345" && price > 14 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @almond_bike_market.post(title=v_title, price=v_price, info=v_title, poster=v_title) ;
}
====
@icalendar.event_begin, ((start_date = makeDate(1462320000000) && summary = "12345" && description = "0123456788" && location =~ "ABCDEFGHIJ") || (start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && summary =~ "0123456789" && description = "23456789" && sequence > 14 && organizer = "9876543210") || (description =~ "0123456789" && organizer = "23456789") || (summary =~ "23456789" && description =~ "12345" && sequence = 7 && organizer =~ "0123456789" && location =~ "12345") || (end_date = makeDate(1487030400000) && summary = "12345" && sequence = 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((summary =~ "9876543210" && sequence < 55 && organizer =~ "0123456789") || (start_date = makeDate(1487030400000) && summary =~ "9876543210" && description = "ABCDEFGHIJ" && sequence = 42 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && description = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @builtin.sorry, true 
;;
Main() {
    @xkcd.new_comic(), ((number > 11 && title = "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @builtin.sorry() ;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ" && sequence >= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && organizer = "0123456788") || (end_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && sequence <= 11 && organizer = "9876543210" && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && description =~ "12345" && sequence > 55)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @github.add_email(email=v_description) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => notify
;;
Main() {
    @almond_bike_market.monitor(), ((info = "23456789" || (info =~ "ABCDEFGHIJ" && price >= 42)))  => @thermostat.get_temperature(), (value > 73F) , v_value := value => notify;
}
====
now => * => @almond_bike_market.post, ((title = "23456789" && price <= 55 && info = "ABCDEFGHIJ") || (price > 14 && info =~ "12345")) 
;;
Main() {
    now => @almond_bike_market.post(title="ABCDEFGHIJ", price=42, info="0123456788", poster="0123456788") ;
}
====
@phdcomics.new_post, (title = "23456789" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @weatherapi.sunrise, (sunrise = makeDate(1487030400000) && sunset = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@wall_street_journal.world_news, (title = "12345" || title =~ "0123456788" || link = "http://www.google.com"^^tt:url || title =~ "0123456788") , v_title := title, v_link := link => @security-camera.get_snapshot, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    @wall_street_journal.world_news(), (((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || (title = "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(powerpost)), true , v_title := title, v_link := link => @almond_dates.search(), (((interest =~ "ABCDEFGHIJ" && poster = "12345" && phone = "+15551234567"^^tt:phone_number) || (poster =~ "23456789" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @builtin.thank_you, true 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((summary =~ "9876543210" && description = "0123456788") || summary = "9876543210" || (date = makeDate(1462320000000) && summary =~ "23456789"))) , v_date := date, v_summary := summary, v_description := description => @builtin.thank_you() ;
}
====
now => @icalendar.list_events, ((description = "0123456789" && sequence < 14 && organizer = "0123456788" && location = "9876543210") || (end_date = makeDate(1487030400000) && summary = "23456789" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((end_date = makeDate(1487030400000) && summary = "0123456788" && description = "9876543210") || (start_date = makeDate(1462320000000) && sequence > 42 && organizer = "23456789") || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && summary =~ "9876543210" && description =~ "12345" && location = "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && summary =~ "0123456789" && location = "0123456789") || (summary =~ "12345" && description = "12345" && sequence < 55))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@onedrive.file_created, true , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_created(), (file_name =~ "12345") , v_file_name := file_name => @twitter.send_direct_message(to=v_file_name, message="0123456789") ;
}
====
now => * => @lg_webos_tv.set_volume, percent > 42 
;;
Main() {
    now => @lg_webos_tv.set_volume(percent=11) ;
}
====
* => @fitbit.getbody, ((weight > 155lb && fat >= 11) || (weight < 155lb && bmi > 14) || (weight <= 155lb && fat <= 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => @fitbit.getbody(), (((bmi <= 11 && fat > 42) || (bmi > 7 && fat >= 7))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => * => @almond_dates.post, ((message = "0123456788" && phone = "+16501234567"^^tt:phone_number) || message = "ABCDEFGHIJ" || (interest =~ "ABCDEFGHIJ" && poster =~ "0123456789")) 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="ABCDEFGHIJ", poster="23456789", phone="+15551234567"^^tt:phone_number) ;
}
====
now => * => @almond_bike_market.post, (price <= 55 || (price < 7 && poster =~ "12345")) 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @almond_bike_market.post(title="0123456788", price=7, info="ABCDEFGHIJ", poster="0123456788") ;
}
====
now => * => @linkedin.share, (status =~ "23456789" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @youtube.search_videos(query="9876543210"), (((channel_title = "ABCDEFGHIJ" && title =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count = 42 && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || count < 7 || (video_id = "ABCDEFGHIJ" && channel_id =~ "23456789" && channel_title = "ABCDEFGHIJ" && title =~ "ABCDEFGHIJ" && description =~ "0123456788" && video_url = "http://www.google.com"^^tt:url))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @linkedin.share(status=v_channel_id) ;
}
====
* => @ytranslate.translate, (text =~ "0123456789" && translated_text =~ "0123456788") , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="23456789"), (target_language = "en"^^tt:iso_lang_code("English")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
now => @giphy.get_tag, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="23456789") ;
}
====
@nasa.asteroid, ((asteroid_id >= 42 && estimated_diameter_min >= 1000m && estimated_diameter_max > 42cm && relative_velocity >= 25mph && orbiting_body =~ "0123456788") || (estimated_diameter_min = 1000m && estimated_diameter_max = 1000m && dangerous = true && relative_velocity <= 5kmph && orbiting_body = "12345")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((estimated_diameter_min < 1000m && estimated_diameter_max < 42cm && dangerous = true && relative_velocity >= 25mph && closest_distance_to_earth = 1000m && orbiting_body =~ "ABCDEFGHIJ")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @imgflip.generate(template=v_name, text_top=v_name, text_bottom=v_orbiting_body), ((template =~ "0123456788" && text_bottom =~ "9876543210" && name =~ "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => * => @almond_dates.post, ((message =~ "23456789" && phone = "+16501234567"^^tt:phone_number) || interest = "0123456789" || (interest =~ "9876543210" && poster =~ "0123456789")) 
;;
Main() {
    now => @almond_dates.post(interest="23456789", message="ABCDEFGHIJ", poster="ABCDEFGHIJ", phone="+15551234567"^^tt:phone_number) ;
}
====
@scale.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && value <= 155lb) || value >= 155lb || time = makeDate(1462320000000) || value > 155lb) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (((time = makeDate(1462320000000) && value < 82kg) || (time = makeDate(1487030400000) && value < 82kg))) , v_time := time, v_value := value => @giphy.get_tag(tag="cat"^^tt:hashtag), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
* => @giphy.get, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (caption = "12345" && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.current_location) || contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@almond_couch_market.monitor, (title =~ "12345" && price = 11 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((price > 42 || price > 42)) , v_title := title, v_price := price, v_contact := contact => @almond_couch_market.search(), (((title = "12345" && price < 42) || price >= 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @holidays.next_us_holiday_event, (date = makeDate(1462320000000) || description = "9876543210" || (summary =~ "ABCDEFGHIJ" && description = "ABCDEFGHIJ") || description = "0123456789") , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post, poster =~ "0123456789" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1487030400000) && summary =~ "0123456788") || (summary = "23456789" && description =~ "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post(title=v_description, info=v_description, price=7, poster="12345") ;
}
====
@gmail.receive_email, ((from_name =~ "ABCDEFGHIJ" && subject =~ "9876543210" && contains(labels, "0123456789")) || (subject =~ "0123456789" && date = makeDate(1487030400000) && contains(labels, "0123456789") && snippet = "9876543210")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => *
;;
Main() {
    @gmail.receive_email(), ((from_name = "9876543210" && snippet =~ "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
@twitter.source, (contains(hashtags, "lol"^^tt:hashtag) && from = "bob"^^tt:username && __reserved = true) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.source(), ((text =~ "0123456788" && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username && __reserved = true)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && description = "12345" && sequence = 7 && organizer = "0123456789") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && description = "12345" && organizer =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "23456789" || file_name = "0123456789" || file_name =~ "0123456789" || file_name = "0123456789") 
;;
Main() {
    @jawbone_up.getsleep(), (((awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration > 2h && light >= 30min) || (time = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration > 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
* => @giphy.get, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    @wall_street_journal.opinions(), ((title =~ "12345" || title = "23456789")) , v_title := title, v_link := link => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature <= 22C && wind_speed >= 5kmph && humidity < 55 && cloudiness <= 42 && fog <= 14 && weather = "0123456789") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && humidity <= 14 && cloudiness < 55 && fog <= 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @youtube.channels_by_category, ((channel_id =~ "23456789" && title = "0123456789" && description = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "9876543210" && title =~ "ABCDEFGHIJ")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(recommended)), (((description =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "23456789" && description =~ "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id = "23456789" && title = "12345" && description = "9876543210"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456788") ;
}
====
* => @ytranslate.translate, (text =~ "0123456789" && translated_text =~ "0123456788") , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @wall_street_journal.world_news(), (title = "0123456788") , v_title := title, v_link := link => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="ABCDEFGHIJ"), true , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), (((stock_id = "msft"^^tt:stock_id("Microsoft") && yield = 42 && ex_div_date = makeDate(1462320000000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && yield = 11 && div <= 55) || (yield <= 11 && div <= 11 && pay_date = makeDate(1487030400000) && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @heatpad.set_power(power=enum(off)) ;
}
====
now => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count > 11 && text =~ "12345") , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, count=55, in_reply_to="alice"^^tt:username), ((query_hashtag = "lol"^^tt:hashtag && count = 11 && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
@rss.new_post, (title = "0123456789" || title =~ "9876543210") , v_title := title, v_link := link => @light-bulb.color_loop, true 
;;
Main() {
    @rss.new_post(), ((title = "12345" || title =~ "9876543210" || (title = "23456789" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @light-bulb.color_loop() ;
}
====
@almond_bike_market.monitor, price > 7  => @dropbox.create_new_folder, (folder_name =~ "12345" || folder_name =~ "12345") 
;;
Main() {
    @almond_bike_market.monitor(price=42), ((price < 14 || (info =~ "9876543210" && price < 14)))  => @dropbox.create_new_folder(folder_name="0123456789") ;
}
====
now => @weatherapi.current, (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature > 73F && wind_speed <= 25mph && humidity < 11 && cloudiness < 55 && fog = 11) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed >= 5kmph && humidity < 7 && cloudiness > 7 && fog = 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
now => * => @almond_dates.post, (interest =~ "9876543210" && message =~ "0123456788" && poster = "9876543210") 
;;
Main() {
    now => @xkcd.random_comic(), (alt_text =~ "23456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @almond_dates.post(interest=v_alt_text, message=v_title, poster=v_title, phone="+15551234567"^^tt:phone_number) ;
}
====
* => @dropbox.open, (file_name = "23456789" || file_name = "0123456788") , v_url := url => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) || (time = makeDate(1487030400000) && weight < 82kg))) , v_time := time, v_weight := weight => @dropbox.open(file_name="23456789"), true , v_url := url => notify;
}
====
now => * => @almond_dates.post, ((message =~ "23456789" && phone = "+16501234567"^^tt:phone_number) || interest = "0123456789" || (interest =~ "9876543210" && poster =~ "0123456789")) 
;;
Main() {
    now => @xkcd.get_comic(), ((number < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @almond_dates.post(interest=v_alt_text, message=v_title, poster=v_title, phone=v_title) ;
}
====
* => @bing.web_search, (link = "http://www.google.com"^^tt:url || (query = "0123456788" && description =~ "12345")) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 11 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @bing.web_search(query=v_title), (query =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_link := link => @security-camera.set_power(power=enum(on)) ;
}
====
@onedrive.file_modified, file_name = "ABCDEFGHIJ" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), (file_name = "23456789") , v_file_name := file_name => @youtube.list_subscriptions(), (((channel_id = "9876543210" && description =~ "9876543210") || (channel_id =~ "ABCDEFGHIJ" && description = "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @heatpad.set_power, true 
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && leanMass >= 42) || (weight < 82kg && bmi < 14))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @heatpad.set_power(power=enum(off)) ;
}
====
* => @dropbox.move, new_name =~ "12345" 
;;
Main() {
    @rss.new_post(), ((title =~ "0123456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @dropbox.move(old_name=v_title, new_name=v_title) ;
}
====
@omlet.incomingmessage, (message =~ "ABCDEFGHIJ" || message =~ "ABCDEFGHIJ" || (type = enum(text) && message =~ "0123456788") || message = "0123456789") , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (message = "0123456788") , v_type := type, v_message := message => notify;
}
====
now => * => @twitter.send_direct_message, (to = "alice"^^tt:username || message =~ "23456789") 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @twitter.send_direct_message(to=v_description, message=v_description) ;
}
====
@nasa.asteroid, ((asteroid_id = 14 && estimated_diameter_min > 1000m && estimated_diameter_max >= 42cm && relative_velocity > 5kmph && closest_distance_to_earth <= 1000m && orbiting_body = "9876543210") || (asteroid_id < 42 && estimated_diameter_min >= 1000m && estimated_diameter_max < 1000m && dangerous = true)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 42 && name =~ "12345" && estimated_diameter_max < 1000m && dangerous = false && relative_velocity >= 25mph && closest_distance_to_earth < 42cm && orbiting_body = "9876543210") || (asteroid_id < 11 && name =~ "0123456789" && estimated_diameter_min <= 42cm && estimated_diameter_max <= 1000m && relative_velocity >= 25mph && closest_distance_to_earth < 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_random_between(high=v_asteroid_id, low=55), (random = 11) , v_random := random => notify;
}
====
* => @lg_webos_tv.play_url, true 
;;
Main() {
    @twitter.my_tweet(), (((text =~ "9876543210" && in_reply_to = "bob"^^tt:username) || contains(urls, "http://www.google.com"^^tt:url) || (text = "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) || (text =~ "23456789" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || contains(urls, "http://www.google.com"^^tt:url) || (text =~ "ABCDEFGHIJ" && contains(urls, "http://www.abc.def"^^tt:url)) || (contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
@google_drive.new_drive_file, (file_name =~ "9876543210" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "23456789" || file_name = "0123456789")) , v_file_name := file_name => notify;
}
====
* => @google_drive.create_new_drive_file, file_name = "ABCDEFGHIJ" 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && leanMass <= 7 && bmi <= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
* => @tumblr-blog.post_text, body = "ABCDEFGHIJ" 
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.google.com"^^tt:url && filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @tumblr-blog.post_text(title=v_caption, body=v_caption) ;
}
====
@nasa.asteroid, ((asteroid_id = 14 && name = "23456789" && estimated_diameter_min >= 42cm && estimated_diameter_max > 1000m && relative_velocity = 25mph && closest_distance_to_earth <= 42cm) || (name =~ "9876543210" && estimated_diameter_min < 42cm && estimated_diameter_max > 1000m && dangerous = false && relative_velocity >= 5kmph && orbiting_body =~ "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((dangerous = false && relative_velocity >= 5kmph && orbiting_body =~ "0123456789") || (asteroid_id >= 42 && name = "0123456788" && estimated_diameter_max <= 42cm && closest_distance_to_earth > 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @weatherapi.moon(location=$context.location.home), true , v_date := date, v_phase := phase => notify;
}
====
now => @wall_street_journal.get_opinions, (updated = makeDate(1487030400000) || (title =~ "ABCDEFGHIJ" && updated = makeDate(1487030400000)) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title = "23456789") , v_title := title, v_link := link, v_updated := updated => @speaker.play_music() ;
}
====
now => * => @fitbit.recordweight, (weight <= 155lb || weight = 82kg) 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.work), ((date = makeDate(1462320000000) && phase =~ "12345")) , v_date := date, v_phase := phase => @fitbit.recordweight(weight=82kg) ;
}
====
* => @facebook.post, status = "12345" 
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "23456789" && description =~ "0123456789" && sequence < 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @facebook.post(status=v_location) ;
}
====
* => @fitbit.getsteps, (steps <= 42 || steps > 7 || (date = makeDate(1487030400000) && steps = 11)) , v_date := date, v_steps := steps => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "0123456788" && price <= 14 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @fitbit.getsteps(), true , v_date := date, v_steps := steps => notify;
}
====
@phone.receive_sms, body = "0123456789" , v_from := from, v_body := body => *
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @builtin.debug_log(message="23456789") ;
}
====
now => @google_drive.create_new_drive_file, (file_name =~ "23456789" || file_name =~ "0123456789" || file_name = "ABCDEFGHIJ" || file_name = "9876543210") 
;;
Main() {
    now => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@thermostat.humidity, (value = 55 || (time = makeDate(1462320000000) && value >= 14)) , v_time := time, v_value := value => * => *
;;
Main() {
    @thermostat.humidity(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => notify;
}
====
@yahoofinance.stock_quote, (ask_price > 7 || (ask_price < 42 && bid_price < 11) || ask_price > 7 || bid_price <= 14 || (company_name =~ "23456789" && ask_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && ask_price > 14 && bid_price <= 7) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "12345" && ask_price <= 7 && bid_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && bid_price > 7) || (ask_price < 11 && bid_price < 14) || bid_price >= 14) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), (bid_price <= 11) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @speaker.play_music() ;
}
====
* => @almond_dates.post, (interest =~ "23456789" && poster = "23456789") 
;;
Main() {
    @instagram.new_picture(), (((caption =~ "0123456788" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (filter = "12345" && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(message=v_caption, poster=v_caption, phone=v_caption, interest="0123456789") ;
}
====
@nasa.asteroid, (asteroid_id = 14 && name = "9876543210" && estimated_diameter_min < 1000m && estimated_diameter_max <= 1000m && relative_velocity = 5kmph && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 14 && name = "ABCDEFGHIJ" && estimated_diameter_min > 1000m && estimated_diameter_max < 1000m && relative_velocity < 25mph && closest_distance_to_earth <= 1000m) || (estimated_diameter_min >= 42cm && estimated_diameter_max <= 1000m && dangerous = false && relative_velocity < 25mph && orbiting_body = "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @lg_webos_tv.raise_volume() ;
}
====
now => * => @fitbit.recordweight, (weight < 82kg || weight <= 82kg || weight >= 82kg || weight <= 155lb || weight >= 155lb) 
;;
Main() {
    now => @weatherapi.current(location=$context.location.current_location), ((temperature <= 73F && wind_speed >= 5kmph && humidity < 55 && cloudiness > 42 && weather = "12345")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @fitbit.recordweight(weight=82kg) ;
}
====
now => @holidays.next_us_holiday_event, (summary =~ "0123456789" && description = "0123456788") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary =~ "23456789" && description = "12345")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@almond_dates.monitor, ((interest = "12345" && message = "12345") || (message =~ "23456789" && poster =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((poster =~ "12345" || (interest =~ "23456789" && poster =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (message =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest = "0123456789" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @linkedin.get_profile(), ((formatted_name =~ "23456789" && industry =~ "12345" && specialties =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @youtube.search_channel, ((query =~ "23456789" && channel_id = "0123456789") || query = "ABCDEFGHIJ" || (query =~ "0123456788" && channel_id =~ "0123456789" && title =~ "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (query = "0123456788" && channel_id =~ "0123456789" && description =~ "0123456789") || (query = "0123456788" && title =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @dropbox.move, old_name =~ "23456789" 
;;
Main() {
    now => @youtube.search_channel(query="0123456788"), (((query =~ "0123456788" && channel_id = "12345" && title =~ "9876543210") || (query = "0123456788" && title =~ "ABCDEFGHIJ" && description = "23456789") || thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @dropbox.move(old_name=v_title, new_name="0123456789") ;
}
====
now => @imgflip.list, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), ((name = "0123456788" || (name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "0123456788" && price >= 14) || (title = "0123456788" && price >= 14) || price > 14)) , v_title := title, v_price := price, v_contact := contact => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="0123456789") ;
}
====
@youtube.source, ((title =~ "9876543210" && video_id = "0123456789") || (channel_id =~ "ABCDEFGHIJ" && channel_title = "9876543210" && title = "12345" && video_id =~ "ABCDEFGHIJ")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_id = "0123456789" && channel_title = "0123456789" && title =~ "ABCDEFGHIJ" && video_id = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @bing.image_search(query=v_title), true , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @thermostat.get_temperature, (value > 73F || value > 22C || value <= 73F || value = 73F) , v_value := value => notify
;;
Main() {
    @jawbone_up.getsleep(), (((duration < 30min && rem = 30min && light >= 2h && deep < 2h) || (duration >= 2h && light >= 2h && deep <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && label = "0123456789") || (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") || from = "bob@stanford.edu"^^tt:email_address || label = "23456789" || subject = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @holidays.us_holiday_event(), (description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @security-camera.set_power(power=enum(on)) ;
}
====
now => @fitbit.getsteps, (steps > 7 || (date = makeDate(1462320000000) && steps <= 14)) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), ((steps < 55 || steps < 55 || steps >= 7)) , v_date := date, v_steps := steps => notify;
}
====
* => @linkedin.share, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "ABCDEFGHIJ") , v_file_name := file_name => @linkedin.share(status="23456789") ;
}
====
@sleep-tracker.getsleep, ((duration <= 2h && rem <= 2h && light < 2h && deep <= 2h) || (place = $context.location.home && light <= 30min && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration > 30min && rem <= 2h && light < 30min && deep = 30min) || (awakeTime = makeDate(1462320000000) && duration >= 2h && rem < 2h && light >= 2h && deep < 30min) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration >= 2h && rem < 30min && light < 2h && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((asleepTime = makeDate(1487030400000) && duration <= 2h && rem > 30min) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && duration <= 30min && rem >= 2h && deep < 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @facebook.post, (status =~ "0123456789" || status = "9876543210") 
;;
Main() {
    @omlet.newmessage(), ((message =~ "0123456788" || (type = enum(text) && message = "0123456788"))) , v_type := type, v_message := message => @facebook.post(status="0123456789") ;
}
====
now => * => @almond_dates.post, (poster = "0123456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @almond_dates.post(interest="23456789", message="0123456788", poster="23456789", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @security-camera.set_power, true 
;;
Main() {
    @bodytrace.source(), (weight >= 155lb) , v_time := time, v_weight := weight => @security-camera.set_power(power=enum(off)) ;
}
====
@nasa.asteroid, (asteroid_id <= 14 && name = "12345" && estimated_diameter_max > 1000m && relative_velocity < 25mph && orbiting_body =~ "ABCDEFGHIJ") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((name = "ABCDEFGHIJ" && estimated_diameter_min <= 1000m && relative_velocity > 5kmph && closest_distance_to_earth < 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @instagram.get_pictures(), ((caption = "0123456789" && filter =~ "0123456789" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @linkedin.get_profile, (formatted_name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), (((formatted_name = "ABCDEFGHIJ" && industry =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "23456789" && industry = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && duration < 30min && rem >= 30min && light = 30min && deep > 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((asleepTime = makeDate(1462320000000) && duration < 30min && rem < 30min && light < 2h) || (duration <= 2h && rem >= 2h && deep > 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @dropbox.get_space_usage(), true , v_used := used, v_total := total => notify;
}
====
* => @imgflip.generate, (template =~ "ABCDEFGHIJ" && text_top = "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="0123456789", text_top="0123456789", text_bottom="0123456789"), ((text_bottom = "0123456789" && name = "ABCDEFGHIJ")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "0123456789" || file_name =~ "0123456789" || file_name = "0123456789" || file_name =~ "12345" || file_name =~ "12345") 
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456789"), (((tournament_search_term =~ "9876543210" && tournament_league_name =~ "12345" && away_alias =~ "23456789" && away_name = "23456789" && home_name =~ "12345" && scheduled_time =~ "0123456789" && away_points <= 55 && home_points > 11) || (tournament_search_term =~ "9876543210" && tournament_full_name =~ "12345" && away_name =~ "23456789" && scheduled_time = "23456789" && away_points > 11 && home_points >= 14) || (tournament_full_name =~ "23456789" && away_alias =~ "12345" && away_name = "ABCDEFGHIJ" && away_points >= 14))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @google_drive.create_new_drive_file(file_name=v_tournament_league_name) ;
}
====
@tumblr.new_photo, ((blog_name = "23456789" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || blog_name =~ "23456789" || (link = "http://www.google.com"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), (((blog_name =~ "0123456788" && link = "http://www.google.com"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (date = makeDate(1487030400000) && caption =~ "ABCDEFGHIJ") || contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @builtin.get_date(), (date = makeDate(1462320000000)) , v_date := date => notify;
}
====
@nasa.asteroid, (name =~ "0123456788" && estimated_diameter_min = 42cm && dangerous = true && closest_distance_to_earth < 1000m && orbiting_body = "0123456788") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id <= 55 && estimated_diameter_max < 1000m && dangerous = true && relative_velocity < 25mph && closest_distance_to_earth >= 1000m) || (estimated_diameter_min < 42cm && estimated_diameter_max = 1000m && relative_velocity >= 5kmph && orbiting_body = "ABCDEFGHIJ") || (asteroid_id = 42 && estimated_diameter_min > 1000m && estimated_diameter_max >= 42cm && relative_velocity <= 5kmph && closest_distance_to_earth > 1000m && orbiting_body = "0123456789"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_random_between(low=7, high=11), ((low >= 42 || (low >= 42 && high > 7 && random = 11))) , v_random := random => notify;
}
====
* => @holidays.next_us_holiday_event, (description = "9876543210" || (summary =~ "9876543210" && description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), ((away_name = "12345" && scheduled_time = "23456789" && game_inning = "12345" && home_runs < 14)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) && summary = "12345")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => @omlet.send_to, (message = "0123456789" || message = "ABCDEFGHIJ") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title =~ "9876543210" && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => @omlet.send_to(message=v_title, to="bob"^^tt:username) ;
}
====
now => * => @heatpad.set_power, true 
;;
Main() {
    now => @thecatapi.get(), ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @heatpad.set_power(power=enum(off)) ;
}
====
* => * => @twitter.send_direct_message, ((to = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || message =~ "ABCDEFGHIJ") 
;;
Main() {
    @washington_post.new_blog_post(section=enum(powerpost)), true , v_title := title, v_link := link => @twitter.send_direct_message(to="alice"^^tt:username, message="12345") ;
}
====
now => @thecatapi.get, ((image_id =~ "23456789" && count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || image_id =~ "0123456789" || (image_id = "23456789" && count < 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "23456789" && count <= 11 && link = "http://www.abc.def"^^tt:url) || (image_id =~ "9876543210" && count >= 14 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @bluetooth_speaker.set_volume(percent=42) ;
}
====
* => @phone.get_gps, ((location = $context.location.home && altitude <= 42cm && speed < 5kmph) || (altitude > 1000m && bearing > 11 && speed <= 5kmph) || altitude <= 1000m || (bearing >= 11 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (((altitude = 42cm && bearing > 11 && speed = 5kmph) || (location = $context.location.work && altitude > 42cm && bearing <= 7 && speed <= 25mph) || (bearing > 7 && speed < 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @fitbit.getbody, ((weight < 82kg && bmi > 11 && fat < 14) || fat < 42) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), (((weight = 82kg && bmi > 14 && fat >= 42) || (weight = 82kg && bmi <= 11 && fat >= 42))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @almond_dates.search, message = "ABCDEFGHIJ" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "9876543210" && message =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@security-camera.new_event, (has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
* => @security-camera.get_url, true , v_link := link => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_partisan)), true , v_title := title, v_link := link => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
@reddit_front_page.newpost, (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && category = "9876543210") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "9876543210" && category =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @security-camera.get_url(), ((link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url)) , v_link := link => notify;
}
====
now => @instagram.get_pictures, (count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.current_location) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @instagram.get_pictures(), ((count > 11 && caption =~ "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @ytranslate.detect_language, true , v_detected_language := detected_language => *
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), ((away_name = "0123456788" && home_name = "23456789" && scheduled_time = makeDate(1487030400000) && home_points >= 42 && result = enum(lose))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @ytranslate.detect_language(text="9876543210"), ((text =~ "9876543210" || detected_language = "it"^^tt:iso_lang_code("Italian") || (text =~ "23456789" && detected_language = "it"^^tt:iso_lang_code("Italian")) || text =~ "ABCDEFGHIJ" || text = "23456789")) , v_detected_language := detected_language => notify;
}
====
@phone.gps, ((bearing < 11 && speed >= 5kmph) || (bearing <= 7 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    @phone.gps(), ((location = $context.location.home && altitude >= 42cm && bearing >= 42 && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @thermostat.set_target_temperature(value=22C) ;
}
====
@scale.source, (time = makeDate(1487030400000) && value >= 155lb) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), (value >= 82kg) , v_time := time, v_value := value => @speaker.set_volume(percent=42) ;
}
====
* => @omlet.send_to, ((to = "bob"^^tt:username && message =~ "9876543210") || message =~ "0123456789" || to = "bob"^^tt:username || message = "ABCDEFGHIJ") 
;;
Main() {
    @nasa.asteroid(), ((dangerous = false && relative_velocity = 5kmph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @omlet.send_to(message=v_orbiting_body, to="alice"^^tt:username) ;
}
====
* => @omlet.send_to, (message =~ "9876543210" || message = "12345" || message =~ "12345" || message =~ "0123456789" || to = "alice"^^tt:username || message = "9876543210" || (to = "bob"^^tt:username && message =~ "9876543210")) 
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "23456789" && description = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @omlet.send_to(to="bob"^^tt:username, message="0123456789") ;
}
====
now => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price = 55 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@slack.receive, ((channel = "funny"^^tt:hashtag && timestamp =~ "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && timestamp =~ "0123456788" && message = "9876543210")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), ((timestamp =~ "0123456789" && message =~ "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @speaker.set_sink() ;
}
====
now => @wall_street_journal.get_opinions, (title =~ "9876543210" || (title = "ABCDEFGHIJ" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => @gmail.send_email, (subject =~ "12345" && message =~ "0123456789") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => @gmail.send_email(to=v_title, message=v_title, subject="0123456789") ;
}
====
* => @ytranslate.detect_language, (text =~ "9876543210" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="0123456789"), true , v_detected_language := detected_language => notify;
}
====
@phdcomics.new_post, ((title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (((title = "12345" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "9876543210" || (title = "0123456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @dropbox.open(file_name="0123456788"), ((file_name = "ABCDEFGHIJ" && url = "12345")) , v_url := url => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "0123456789" || file_name =~ "0123456789" || file_name = "0123456789" || file_name =~ "12345" || file_name =~ "12345") 
;;
Main() {
    @gmail.receive_email(), (((from_name = "0123456788" && subject = "ABCDEFGHIJ") || (subject =~ "12345" && snippet =~ "ABCDEFGHIJ") || (from_name =~ "12345" && from_address = "alice@gmail.com"^^tt:email_address && subject = "12345" && date = makeDate(1462320000000)))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @google_drive.create_new_drive_file(file_name=v_snippet) ;
}
====
@gmail.receive_email, (contains(labels, "23456789") || (from_name =~ "9876543210" && snippet =~ "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), (((from_name = "9876543210" && from_address = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && snippet =~ "ABCDEFGHIJ") || (subject = "0123456789" && date = makeDate(1487030400000)))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@activity-tracker.getmove, (day =~ "12345" && distance < 1000m && steps > 14 && inactiveTime < 2h && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1487030400000) && steps > 42 && inactiveTime > 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude > 1000m && bearing = 42 && speed = 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((bearing = 14 && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @security-camera.set_power(power=enum(on)) ;
}
====
* => @youtube.search_channel, ((channel_id = "0123456788" && description =~ "9876543210") || (query =~ "0123456788" && channel_id =~ "0123456789" && title =~ "0123456788" && description =~ "ABCDEFGHIJ") || (query =~ "0123456788" && title =~ "0123456789" && description =~ "0123456788")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @rss.new_post(), (title = "23456789") , v_title := title, v_link := link => @youtube.search_channel(query="9876543210"), ((channel_id =~ "0123456789" || (channel_id = "9876543210" && description = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "23456789" && description = "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (query =~ "12345" && channel_id =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @almond_bike_market.detail, id = "12345"  => notify
;;
Main() {
    now => @almond_bike_market.detail(id="12345"), (id =~ "12345")  => notify;
}
====
@wall_street_journal.opinions, (title = "12345" || (title = "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify
;;
Main() {
    @wall_street_journal.opinions(), ((title =~ "23456789" || title = "23456789")) , v_title := title, v_link := link => notify;
}
====
now => @nasa.rover, (date_taken = makeDate(1487030400000) && count >= 42) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    now => @nasa.rover(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (date_taken = makeDate(1462320000000) && count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@almond_bike_market.monitor, (info =~ "23456789" && price < 55)  => notify
;;
Main() {
    @almond_bike_market.monitor(), ((info =~ "9876543210" || info =~ "12345" || price <= 14))  => notify;
}
====
* => @almond_bike_market.post, ((title =~ "9876543210" && price > 11 && poster = "0123456788") || (title = "23456789" && price = 42 && info = "ABCDEFGHIJ" && poster = "0123456789") || (title = "9876543210" && info = "0123456788" && poster = "23456789") || (title = "12345" && price = 42 && poster =~ "9876543210")) 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && place = $context.location.home && heartrate >= 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_bike_market.post(price=v_heartrate, title="0123456788", info="0123456788", poster="ABCDEFGHIJ") ;
}
====
now => @nasa.apod, ((title = "23456789" && description =~ "0123456789") || (title = "23456789" && description = "23456789") || (title = "23456789" && description =~ "0123456788") || (title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    now => @nasa.apod(), ((title = "ABCDEFGHIJ" && description =~ "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => @bluetooth_speaker.raise_volume() ;
}
====
now => @holidays.next_us_holiday_event, (date = makeDate(1462320000000) && summary =~ "23456789" && description = "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) || (date = makeDate(1462320000000) && summary = "0123456788" && description = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@almond_bike_market.monitor, (info =~ "12345" || info =~ "12345")  => * => notify
;;
Main() {
    @almond_bike_market.monitor(), ((price >= 14 || price < 11 || info =~ "ABCDEFGHIJ" || (info =~ "0123456789" && price < 42) || price >= 7))  => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @weatherapi.current(location=$context.location.work), (((temperature >= 73F && cloudiness <= 11 && fog = 7 && weather = "ABCDEFGHIJ") || (temperature < 73F && wind_speed < 25mph && humidity >= 7 && weather = "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@fitness-tracker.getbody, ((time = makeDate(1462320000000) && weight >= 82kg && bodyFat < 55 && bmi < 42) || (leanMass >= 14 && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && leanMass <= 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @nasa.rover, true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @wall_street_journal.opinions(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @nasa.rover(), ((count >= 14 && camera_used = "12345")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
now => @twitter.sink, status =~ "23456789" 
;;
Main() {
    now => @twitter.sink(status="0123456789") ;
}
====
@phdcomics.new_post, title =~ "23456789" , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (((title = "0123456788" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "23456789")) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_dates.search(), ((message =~ "12345" && poster = "12345" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight < 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight <= 82kg) , v_time := time, v_weight := weight => @imgflip.list(), (name =~ "12345") , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @security-camera.get_snapshot, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    now => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
* => @icalendar.list_events, (description =~ "ABCDEFGHIJ" && sequence <= 7 && organizer =~ "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature <= 73F && wind_speed < 5kmph && humidity >= 42 && cloudiness <= 7 && weather =~ "12345" && status = enum(windy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @icalendar.list_events(), ((sequence = 7 && organizer = "9876543210" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @weatherapi.sunrise, ((date = makeDate(1462320000000) && sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000)) || sunrise = makeDate(1487030400000) || (date = makeDate(1462320000000) && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => *
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.current_location), (sunrise = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@bodytrace.source, (weight < 82kg || weight < 155lb) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1462320000000) && weight < 82kg) || weight > 155lb || weight > 82kg)) , v_time := time, v_weight := weight => @dropbox.open(file_name="0123456788"), (file_name =~ "23456789") , v_url := url => notify;
}
====
* => * => @speaker.play_music, true 
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1462320000000) && summary = "0123456788" && description = "9876543210" && organizer = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @speaker.play_music() ;
}
====
now => @almond_bike_market.ask_seller, (property =~ "ABCDEFGHIJ" && answer = "12345") , v_answer := answer => notify
;;
Main() {
    now => @almond_bike_market.ask_seller(id="ABCDEFGHIJ", property="0123456789", value="0123456789", public=true), ((property =~ "9876543210" && answer =~ "0123456789")) , v_answer := answer => notify;
}
====
* => @almond_couch_market.search, ((title =~ "23456789" && price <= 11 && contact = "+15551234567"^^tt:phone_number) || price >= 11) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @twitter.direct_message(), (message =~ "0123456789") , v_from := from, v_message := message, v___reserved := __reserved => @almond_couch_market.search(), ((price = 7 || (title =~ "0123456789" && price <= 14) || (title =~ "ABCDEFGHIJ" && contact = "+15551234567"^^tt:phone_number) || (title = "9876543210" && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@onedrive.file_modified, (file_name =~ "23456789" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "23456789") , v_file_name := file_name => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @fitbit.getsteps, ((date = makeDate(1462320000000) && steps >= 14) || date = makeDate(1462320000000)) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), ((steps < 7 || steps < 14 || steps > 7 || steps > 14)) , v_date := date, v_steps := steps => notify;
}
====
* => @almond_bike_market.detail, true  => *
;;
Main() {
    now => @almond_bike_market.detail(id="23456789"), ((id =~ "0123456788" || id =~ "0123456788"))  => @lg_webos_tv.lower_volume() ;
}
====
now => @almond_dates.search, ((message = "9876543210" && poster =~ "23456789") || (interest = "12345" && message = "9876543210" && poster = "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest = "23456789" && poster = "0123456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@activity-tracker.getmove, (day = "23456789" && distance < 1000m && steps < 42 && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "9876543210" && distance = 42cm && steps > 7 && activeTime >= 2h && inactiveTime >= 2h && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @thermostat.set_minmax_temperature(low=73F, high=73F) ;
}
====
@youtube.source, (channel_id = "ABCDEFGHIJ" && channel_title =~ "ABCDEFGHIJ" && video_id = "23456789" && video_url = "http://www.abc.def"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @linkedin.share, true 
;;
Main() {
    @youtube.source(), (((channel_id = "23456789" && title =~ "12345") || channel_id = "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @linkedin.share(status=v_video_id) ;
}
====
@reddit_front_page.newpost, ((title =~ "0123456789" && link = "http://www.google.com"^^tt:url && user = "9876543210") || (user = "0123456788" && category =~ "0123456788") || (link = "http://www.abc.def"^^tt:url && user = "ABCDEFGHIJ" && category =~ "ABCDEFGHIJ") || title = "12345") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((link = "http://www.google.com"^^tt:url && user = "ABCDEFGHIJ") || (title = "9876543210" && link = "http://www.google.com"^^tt:url && user =~ "9876543210") || (title = "12345" && link = "http://www.google.com"^^tt:url && user =~ "12345" && category =~ "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => @builtin.get_time(), true , v_time := time => notify;
}
====
* => @holidays.next_us_holiday_event, (date = makeDate(1487030400000) && summary =~ "0123456789") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @activity-tracker.getmove(), (((distance >= 42cm && steps > 11 && activeTime >= 30min && inactiveTime > 30min && calories < 500kcal) || (day = "23456789" && steps = 55 && activeTime > 2h && inactiveTime = 30min && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @holidays.next_us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@sportradar.ncaambb_team, ((away_name =~ "9876543210" && home_name =~ "ABCDEFGHIJ" && game_status = enum(closed)) || (other_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name =~ "0123456789" && away_points <= 14 && home_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => *
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), ((watched_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name = "9876543210" && home_name = "ABCDEFGHIJ" && game_status = enum(inprogress) && scheduled_time = makeDate(1487030400000) && away_points <= 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @phone.call_emergency() ;
}
====
@builtin.timer, interval <= 2h  => notify
;;
Main() {
    @builtin.timer(interval=30min), ((interval = 2h || interval > 2h))  => notify;
}
====
* => @almond_dates.search, (poster = "ABCDEFGHIJ" || poster =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), ((blog_name =~ "9876543210" && body =~ "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @almond_dates.search(), ((message =~ "12345" || phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @dropbox.open, ((file_name =~ "9876543210" && url =~ "0123456789") || url =~ "23456789") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="0123456788"), true , v_url := url => notify;
}
====
@tumblr.new_text, (date = makeDate(1487030400000) && body = "12345") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => *
;;
Main() {
    @tumblr.new_text(blog_name="0123456788"), (blog_name =~ "23456789") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @builtin.debug_log(message=v_title) ;
}
====
now => @almond_couch_market.search, ((price > 42 && contact = "+15551234567"^^tt:phone_number) || price <= 55) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (title = "0123456788") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @dropbox.create_new_folder, (folder_name = "ABCDEFGHIJ" || folder_name = "0123456789" || folder_name =~ "0123456788") 
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || tag = "cat"^^tt:hashtag)) , v_picture_url := picture_url => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
now => @linkedin.get_profile, ((headline = "23456789" && industry = "23456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "23456789" && specialties =~ "12345") || (headline = "0123456789" && industry = "0123456788" && specialties =~ "0123456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), (((headline =~ "ABCDEFGHIJ" && industry = "23456789" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "12345" && headline =~ "0123456788" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "ABCDEFGHIJ" && industry =~ "9876543210" && specialties =~ "0123456788"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@phdcomics.new_post, ((title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456788" || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @uber.time_estimate(start=$context.location.work), (((uber_type =~ "23456789" && estimate = 30min) || estimate <= 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @thermostat.set_target_temperature, (value < 73F || value = 22C) 
;;
Main() {
    now => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @instagram.get_pictures, ((count >= 42 && caption = "0123456789") || (link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456788" && link = "http://www.google.com"^^tt:url && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), ((link = "http://www.google.com"^^tt:url && filter = "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @almond_bike_market.post, ((title =~ "12345" && price > 55 && info =~ "ABCDEFGHIJ") || (price > 11 && info =~ "0123456789" && poster =~ "0123456788") || (title =~ "9876543210" && price <= 11 && info = "12345" && poster = "12345") || info =~ "ABCDEFGHIJ" || (price > 55 && info = "23456789")) 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id < 42 && estimated_diameter_min > 42cm && estimated_diameter_max <= 42cm && relative_velocity > 5kmph) || (asteroid_id <= 55 && estimated_diameter_min = 1000m && closest_distance_to_earth <= 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.post(title=v_name, price=v_asteroid_id, poster=v_orbiting_body, info="0123456788") ;
}
====
* => @bing.image_search, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height >= 11) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="9876543210"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width < 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), ((weight = 155lb || weight < 82kg || weight >= 155lb || weight > 82kg)) , v_time := time, v_weight := weight => @onedrive.create_file(file_name="9876543210", contents="23456789") ;
}
====
@instagram.new_picture, ((caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag)) || filter =~ "9876543210" || (link = "http://www.google.com"^^tt:url && filter =~ "ABCDEFGHIJ")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), (((caption = "23456789" && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (caption = "ABCDEFGHIJ" && filter =~ "0123456788" && contains(hashtags, "cat"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @youtube.search_channel, (title = "23456789" && description =~ "23456789") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.search_channel(query="0123456788"), true , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@nasa.asteroid, (name = "0123456789" && estimated_diameter_max < 42cm && relative_velocity >= 25mph && closest_distance_to_earth < 1000m && orbiting_body = "9876543210") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 11 && name =~ "0123456789" && estimated_diameter_min < 42cm && estimated_diameter_max >= 42cm && relative_velocity >= 5kmph && closest_distance_to_earth > 42cm && orbiting_body =~ "9876543210")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @speaker.play_music() ;
}
====
now => @twitter.search_by_hashtag, ((count > 42 && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count <= 55 && text = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=11, from="alice"^^tt:username, in_reply_to="bob"^^tt:username), (((count = 42 && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || (query_hashtag = "funny"^^tt:hashtag && count > 42 && text =~ "0123456788" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@nasa.asteroid, (name = "23456789" && dangerous = true && relative_velocity <= 5kmph && closest_distance_to_earth <= 42cm && orbiting_body = "12345") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 7 && name = "ABCDEFGHIJ" && estimated_diameter_max = 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @thermostat.get_humidity(), ((value < 42 || value >= 11)) , v_value := value => notify;
}
====
* => * => @almond_bike_market.post, ((title =~ "23456789" && price >= 55 && info = "0123456789" && poster =~ "ABCDEFGHIJ") || (title =~ "12345" && price < 7 && poster = "9876543210")) 
;;
Main() {
    @almond_dates.monitor(), ((message =~ "0123456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_dates.search(), (((message = "0123456789" && poster = "23456789" && phone = "+16501234567"^^tt:phone_number) || interest =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_bike_market.post(title=v_interest, info=v_interest, price=7, poster="ABCDEFGHIJ") ;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate <= 42) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate = 14) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @security-camera.get_url, link = "http://www.abc.def"^^tt:url , v_link := link => notify
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => notify;
}
====
@github.new_milestone, (time = makeDate(1487030400000) || (repo_name = "9876543210" && user = "bob"^^tt:username && description = "0123456788")) , v_user := user, v_description := description, v_time := time => * => notify
;;
Main() {
    @github.new_milestone(repo_name="0123456789"), true , v_user := user, v_description := description, v_time := time => @builtin.get_random(), ((number <= 42 || number > 42 || number <= 42)) , v_number := number => notify;
}
====
now => @thecatapi.get, ((image_id =~ "23456789" && count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || image_id =~ "0123456789" || (image_id = "23456789" && count < 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((count > 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || image_id = "ABCDEFGHIJ")) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @twitter.unfollow(user_name="alice"^^tt:username) ;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && user = "0123456788" && category = "9876543210") || category = "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (category =~ "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => @twitter.search_by_hashtag(query_hashtag=v_title, from=v_title, in_reply_to=v_category), ((query_hashtag = "cat"^^tt:hashtag && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @dropbox.create_new_folder(folder_name=v_text) ;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1487030400000) && summary = "23456789") || (summary = "23456789" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.uk_holiday_event(), (((date = makeDate(1487030400000) && summary =~ "12345") || description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => @slack.send(channel=v_summary, message=v_description) ;
}
====
now => * => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    now => @linkedin.get_profile(), (((formatted_name = "12345" && headline = "0123456788" && industry =~ "0123456788" && specialties =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "0123456788" && industry = "9876543210" && specialties = "0123456788"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
* => @github.add_email, true 
;;
Main() {
    @reddit_front_page.newpost(), ((category = "12345" || (link = "http://www.google.com"^^tt:url && category =~ "12345") || title = "0123456788" || user = "ABCDEFGHIJ" || (title =~ "12345" && user = "9876543210" && category =~ "9876543210") || (title =~ "9876543210" && user =~ "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @builtin.at(time=makeTime(15,0)), (time = makeTime(7,30))  => @security-camera.set_power(power=enum(off)) ;
}
====
@holidays.us_holiday_event, (summary =~ "12345" && description = "23456789") , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @uber.price_estimate(start=$context.location.home, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((uber_type = "0123456789" && low_estimate < 14 && high_estimate >= 11 && currency_code =~ "0123456789" && surge < 55 && duration >= 2h && distance >= 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @almond_dates.search, ((interest = "0123456788" && message =~ "9876543210" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number) || (message = "0123456789" && poster =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @scale.source(), (value <= 155lb) , v_time := time, v_value := value => @almond_dates.search(), (((message = "12345" && poster = "12345") || interest = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@jawbone_up.getbody, ((leanMass <= 11 && bmi < 55) || (weight >= 82kg && leanMass < 14 && bodyFat < 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => *
;;
Main() {
    @jawbone_up.getbody(), (((time = makeDate(1487030400000) && bodyFat <= 42) || (time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight = 82kg && bodyFat > 14 && bmi <= 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="0123456788") ;
}
====
now => @bing.web_search, (query = "0123456789" && title = "0123456788" && description =~ "23456789") , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    now => @bing.web_search(query="12345"), ((query = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => * => @almond_bike_market.post, ((price >= 42 && poster = "0123456789") || title =~ "0123456788") 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 14 && estimated_diameter_min < 1000m && estimated_diameter_max < 1000m && relative_velocity >= 25mph && closest_distance_to_earth < 42cm && orbiting_body =~ "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_bike_market.post(title=v_name, price=v_asteroid_id, info=v_name, poster=v_name) ;
}
====
now => @thecatapi.get, (image_id =~ "0123456788" && count < 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "12345" && count <= 14) || (image_id = "ABCDEFGHIJ" && count >= 7) || (count = 42 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @almond_dates.post(interest="12345", message="23456789", poster="ABCDEFGHIJ", phone="+16501234567"^^tt:phone_number) ;
}
====
@sportradar.soccer_us_tourney, (tournament_full_name =~ "ABCDEFGHIJ" && away_alias =~ "23456789" && game_status = enum(closed) && away_points <= 11 && home_points <= 14) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => * => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="0123456789"), (((away_name =~ "12345" && home_name =~ "23456789" && away_points <= 42) || (tournament_full_name =~ "9876543210" && home_alias = "0123456789" && away_name = "0123456789" && home_name = "ABCDEFGHIJ" && game_status = enum(closed)) || (tournament_search_term =~ "23456789" && away_alias = "0123456788" && home_alias = "0123456788" && away_name = "12345" && away_points < 55 && home_points > 11) || (tournament_search_term = "0123456789" && tournament_full_name = "9876543210" && away_alias =~ "ABCDEFGHIJ" && away_name = "12345" && home_name = "9876543210" && home_points >= 14))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@security-camera.new_event, (has_motion = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.search, (price < 11 || (title = "ABCDEFGHIJ" && contact = "+16501234567"^^tt:phone_number) || price > 42 || (price < 11 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @security-camera.new_event(), (((has_sound = false && has_person = true) || has_motion = false || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || start_time = makeDate(1462320000000) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.search(), (((price >= 14 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456788" && price < 11))) , v_title := title, v_price := price, v_contact := contact => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
now => * => @linkedin.share, (status = "0123456789" || status = "23456789") 
;;
Main() {
    now => @dropbox.get_space_usage(), ((total <= 20MB || used <= 20MB)) , v_used := used, v_total := total => @linkedin.share(status="0123456788") ;
}
====
now => * => @dropbox.move, ((old_name = "9876543210" && new_name = "0123456788") || old_name = "0123456788") 
;;
Main() {
    now => @youtube.list_subscriptions(), ((description =~ "ABCDEFGHIJ" || channel_id = "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @dropbox.move(old_name=v_title, new_name="ABCDEFGHIJ") ;
}
====
@heartrate-monitor.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => @linkedin.share, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate < 14 || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || heartrate = 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @linkedin.share(status="0123456789") ;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((temperature > 22C && wind_speed > 25mph && cloudiness = 11 && fog <= 11 && weather =~ "23456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @security-camera.set_power(power=enum(on)) ;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && label = "12345") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((from = "bob@stanford.edu"^^tt:email_address && label =~ "12345") || (from = "alice@gmail.com"^^tt:email_address && subject = "0123456788"))) , v_from := from, v_subject := subject, v_label := label => @onedrive.create_file(file_name=v_label, contents="ABCDEFGHIJ") ;
}
====
now => @nasa.rover, ((count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (date_taken = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "ABCDEFGHIJ")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1462320000000) && count <= 14)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => @imgflip.generate, (template = "ABCDEFGHIJ" && name =~ "0123456789") , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "12345") , v_file_name := file_name => @imgflip.generate(template="12345", text_top="0123456789", text_bottom="23456789"), ((template = "23456789" && text_bottom = "12345" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @speaker.raise_volume() ;
}
====
* => @instagram.get_pictures, ((count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && link = "http://www.abc.def"^^tt:url && filter =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag)) || (count <= 55 && caption =~ "12345" && filter =~ "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=14), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && link = "http://www.google.com"^^tt:url && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @builtin.get_random_between, ((low <= 11 && high > 42) || (low > 11 && random >= 42) || (low <= 55 && high <= 11 && random >= 14) || random > 7 || (low < 14 && high = 42 && random < 7)) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=11, high=14), ((high <= 7 || (low < 42 && random >= 42))) , v_random := random => notify;
}
====
* => * => @light-bulb.set_power, (power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @slack.receive(), (timestamp = "12345") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @light-bulb.set_power(power=enum(on)) ;
}
====
now => @almond_couch_market.search, ((price > 42 && contact = "+15551234567"^^tt:phone_number) || title = "0123456788" || price >= 14) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "9876543210" && price >= 11 && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && price < 14) || (title = "9876543210" && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @dropbox.create_new_folder(folder_name="9876543210") ;
}
====
@rss.new_post, title = "12345" , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @dropbox.get_space_usage(), ((used >= 20MB || total > 5KB)) , v_used := used, v_total := total => notify;
}
====
now => * => @almond_couch_market.post, (price <= 14 || (title =~ "ABCDEFGHIJ" && price >= 42)) 
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || (image_id =~ "9876543210" && count = 7) || (image_id = "0123456789" && count > 11))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @almond_couch_market.post(title="ABCDEFGHIJ", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
@almond_dates.monitor, ((poster = "0123456789" && phone = "+16501234567"^^tt:phone_number) || phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => @phone.send_sms, true 
;;
Main() {
    @almond_dates.monitor(), (interest =~ "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @phone.send_sms(to=v_poster, body=v_poster) ;
}
====
@google_drive.new_drive_file, (file_name =~ "9876543210" || file_name =~ "12345") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@omlet.incomingmessage, (message =~ "0123456788" || message = "12345" || type = enum(text) || message = "9876543210" || message = "ABCDEFGHIJ" || (type = enum(text) && message = "9876543210")) , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.incomingmessage(), (((type = enum(picture) && message =~ "9876543210") || (type = enum(text) && message = "0123456789"))) , v_type := type, v_message := message => notify;
}
====
* => @uber.price_estimate, ((uber_type = "12345" && low_estimate <= 55 && distance < 42cm) || (start = $context.location.work && low_estimate > 42 && high_estimate <= 14 && surge >= 14 && duration = 30min && distance < 42cm) || (end = $context.location.current_location && uber_type =~ "23456789" && low_estimate < 55 && high_estimate > 7 && currency_code =~ "23456789" && surge <= 11 && duration >= 2h && distance < 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "ABCDEFGHIJ" && channel_title = "12345" && title = "ABCDEFGHIJ") || (channel_title = "23456789" && title =~ "23456789" && video_url = "http://www.abc.def"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=$context.location.home), (((end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "12345" && low_estimate = 7 && high_estimate < 7 && currency_code = "9876543210" && surge > 11 && duration = 2h) || (uber_type =~ "23456789" && high_estimate <= 42 && currency_code = "12345" && surge <= 42 && duration > 30min && distance > 1000m) || (start = $context.location.current_location && surge >= 55 && duration <= 2h && distance < 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count > 11 && text =~ "12345") , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=7, from="bob"^^tt:username), ((query_hashtag = "lol"^^tt:hashtag && count >= 11 && text = "23456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => @facebook.post(status="23456789") ;
}
====
* => @almond_couch_market.search, price < 55 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (title =~ "0123456788") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @twitter.send_direct_message, to = "bob"^^tt:username 
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @twitter.send_direct_message(to=v_user, message=v_user) ;
}
====
now => @phone.get_gps, bearing >= 42 , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 42cm && bearing < 11)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => * => @onedrive.create_file, (file_name =~ "0123456788" && contents =~ "9876543210") 
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && day = "0123456789" && activeTime < 2h) || (day =~ "0123456789" && distance > 42cm && steps >= 11 && activeTime <= 2h && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @onedrive.create_file(file_name="9876543210", contents="12345") ;
}
====
now => * => @thermostat.set_minmax_temperature, (low < 22C && high <= 73F) 
;;
Main() {
    now => @xkcd.random_comic(), ((alt_text =~ "9876543210" || (number >= 42 && link = "http://www.google.com"^^tt:url) || (number > 11 && title =~ "12345" && link = "http://www.google.com"^^tt:url))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => @weatherapi.sunrise, (sunset = makeDate(1462320000000) || location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || sunrise = makeDate(1462320000000) || (date = makeDate(1462320000000) && sunrise = makeDate(1487030400000)) || sunset = makeDate(1487030400000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @fitbit.recordweight, true 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), ((date = makeDate(1462320000000) && sunrise = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @fitbit.recordweight(weight=155lb) ;
}
====
@slack.receive, ((channel = "lol"^^tt:hashtag && timestamp =~ "9876543210" && message = "12345") || message = "12345" || (from = "bob"^^tt:username && message =~ "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @google_drive.create_new_drive_file, true 
;;
Main() {
    @slack.receive(), ((channel = "cat"^^tt:hashtag && timestamp =~ "ABCDEFGHIJ" && from = "bob"^^tt:username && message = "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @google_drive.create_new_drive_file(file_name=v_timestamp) ;
}
====
now => @xkcd.random_comic, ((number < 42 && link = "http://www.abc.def"^^tt:url && alt_text = "0123456789") || (number <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 42 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @imgflip.generate, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="0123456788", text_bottom="12345"), (((text_top = "12345" && text_bottom = "12345" && name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
@thermostat.humidity, (time = makeDate(1487030400000) || value < 11 || value >= 42 || value <= 11) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
@tumblr.new_text, (date = makeDate(1462320000000) && title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && body =~ "23456789") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="0123456788"), (((blog_name = "23456789" && date = makeDate(1462320000000) && link = "http://www.abc.def"^^tt:url && body = "23456789") || (title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag) && body = "23456789"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
now => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count > 11 && text =~ "12345") , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag), true , v_text := text, v_hashtags := hashtags, v_urls := urls => @builtin.discover() ;
}
====
now => @dropbox.list_folder, ((file_name =~ "9876543210" && file_size < 20MB && full_path = "12345") || (file_size > 5KB && full_path = "12345")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), (((folder_name = "9876543210" && file_name =~ "ABCDEFGHIJ" && last_modified = makeDate(1487030400000) && file_size <= 20MB) || (folder_name =~ "9876543210" && file_name =~ "12345" && is_folder = true && last_modified = makeDate(1487030400000) && file_size < 5KB && full_path = "0123456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@nasa.asteroid, ((asteroid_id < 55 && name =~ "9876543210" && estimated_diameter_min <= 1000m && estimated_diameter_max > 42cm && relative_velocity >= 5kmph && closest_distance_to_earth >= 1000m && orbiting_body =~ "0123456789") || (estimated_diameter_min < 1000m && estimated_diameter_max < 1000m && relative_velocity > 25mph && closest_distance_to_earth <= 1000m) || (name =~ "9876543210" && estimated_diameter_min > 42cm && estimated_diameter_max < 1000m && dangerous = true && relative_velocity > 25mph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 55 && name =~ "12345" && estimated_diameter_min < 42cm && closest_distance_to_earth < 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && description =~ "ABCDEFGHIJ" && organizer = "0123456788" && location = "12345") || (sequence = 42 && organizer = "9876543210" && location =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @omlet.send_picture, ((to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") || to = "bob"^^tt:username) 
;;
Main() {
    now => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
@phone.gps, ((bearing > 14 && speed <= 5kmph) || (location = $context.location.home && altitude <= 42cm && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), (((altitude > 1000m && bearing < 7) || (location = $context.location.work && bearing <= 7 && speed >= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @dropbox.get_space_usage(), true , v_used := used, v_total := total => notify;
}
====
@rss.new_post, (title = "23456789" || (title = "23456789" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), ((title =~ "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @builtin.cool() ;
}
====
* => @gmail.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || message = "9876543210") 
;;
Main() {
    @wall_street_journal.world_news(), true , v_title := title, v_link := link => @gmail.send_picture(to=v_title, message=v_title, picture_url=v_title, subject="0123456789") ;
}
====
* => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    @xkcd.new_whatif(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @linkedin.share(status=v_title) ;
}
====
@weatherapi.monitor, (wind_speed >= 5kmph && humidity = 14 && cloudiness <= 11 && fog > 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((temperature <= 22C && wind_speed <= 5kmph && cloudiness = 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @phone.set_ringer, true 
;;
Main() {
    now => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address && label =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => @phone.set_ringer(mode=enum(silent)) ;
}
====
now => @linkedin.get_profile, ((headline =~ "0123456789" && industry =~ "0123456788" && specialties = "ABCDEFGHIJ" && contains(positions, "23456789")) || (formatted_name =~ "23456789" && industry =~ "9876543210" && specialties = "9876543210" && contains(positions, "0123456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), (((formatted_name = "9876543210" && industry =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "ABCDEFGHIJ" && industry = "9876543210" && specialties = "ABCDEFGHIJ" && contains(positions, "12345")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @fitbit.recordweight, (weight <= 155lb || weight < 155lb || weight > 82kg) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "ABCDEFGHIJ" && yield <= 42 && div < 11)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @fitbit.recordweight(weight=82kg) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(info="0123456788"), ((price >= 11 || price >= 42 || price <= 11))  => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="0123456788"), ((target_language = "zh"^^tt:iso_lang_code("Chinese") && text =~ "12345" && translated_text = "23456789")) , v_source_language := source_language, v_translated_text := translated_text => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
@heartrate-monitor.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => *
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate >= 7) , v_time := time, v_place := place, v_heartrate := heartrate => @onedrive.rename_file(old_name="0123456789", new_name="12345") ;
}
====
now => @builtin.get_random, (number <= 42 || number >= 42) , v_number := number => notify
;;
Main() {
    now => @builtin.get_random(), (number <= 55) , v_number := number => notify;
}
====
* => @github.comment_issue, (issue_number < 11 && body =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @github.comment_issue(repo_name="12345", issue_number=7, body="12345") ;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) && heartrate > 7) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate <= 7) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @holidays.uk_holiday_event(), (((summary = "0123456789" && description = "ABCDEFGHIJ") || (summary = "23456789" && description = "12345") || summary =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.ask_seller(id="ABCDEFGHIJ", property="0123456788", value="ABCDEFGHIJ", public=true), (((id =~ "12345" && property = "0123456789" && value = "0123456788") || (property =~ "0123456789" && public = false && answer = "0123456788") || (id =~ "0123456788" && property =~ "12345" && public = false && answer = "ABCDEFGHIJ"))) , v_answer := answer => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @google_drive.create_new_drive_file, file_name =~ "0123456789" 
;;
Main() {
    now => @weatherapi.current(location=$context.location.current_location), ((temperature > 73F && wind_speed < 25mph && cloudiness < 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @google_drive.create_new_drive_file(file_name=v_weather) ;
}
====
now => @thecatapi.get, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id =~ "ABCDEFGHIJ" && count < 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @linkedin.share(status="0123456789") ;
}
====
@github.new_commit, ((repo_name = "0123456789" && user = "bob"^^tt:username && time = makeDate(1462320000000)) || (user = "bob"^^tt:username && time = makeDate(1462320000000))) , v_user := user, v_message := message, v_time := time => * => notify
;;
Main() {
    @github.new_commit(repo_name="0123456788"), ((repo_name = "12345" && message =~ "0123456788" && time = makeDate(1462320000000))) , v_user := user, v_message := message, v_time := time => notify;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary = "0123456789" && location = "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "12345" && sequence <= 42 && organizer = "ABCDEFGHIJ") || organizer =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && sequence > 14 && location = "0123456788") || (description =~ "23456789" && organizer =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @omlet.send_picture(to=v_summary, picture_url=v_summary, caption=v_description) ;
}
====
@sportradar.ncaafb_team, (watched_is_home = true && away_points > 7 && home_points >= 14) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="sta"^^sportradar:ncaafb_team("Stanford Cardinals")), ((other_team_abbr = "sta"^^sportradar:ncaafb_team("Stanford Cardinals") && watched_is_home = false && away_name =~ "0123456789" && home_name = "ABCDEFGHIJ" && game_status = enum(halftime))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
now => * => @twitter.send_direct_message, (message = "ABCDEFGHIJ" || message =~ "9876543210") 
;;
Main() {
    now => @twitter.send_direct_message(to="alice"^^tt:username, message="9876543210") ;
}
====
now => @icalendar.list_events, (summary = "12345" && description =~ "12345" && sequence < 11 && organizer = "23456789" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file, (file_name = v_location || file_name = "23456789") 
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && summary =~ "23456789" && description = "12345" && sequence >= 14 && organizer =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file(file_name=v_summary) ;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "0123456789" && filter =~ "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @twitter.search_by_hashtag, (text = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, from="alice"^^tt:username), ((query_hashtag = "cat"^^tt:hashtag && count >= 42)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@reddit_front_page.newpost, ((link = "http://www.google.com"^^tt:url && user =~ "23456789") || (title = "ABCDEFGHIJ" && category = "9876543210") || (title =~ "23456789" && user =~ "9876543210" && category =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (category =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
@almond_couch_market.monitor, (title = "23456789" || contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => @onedrive.delete_file, (file_name = "0123456788" || file_name = "0123456788" || file_name = "0123456789" || file_name = "0123456789" || file_name = "9876543210") 
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "0123456788" && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @onedrive.delete_file(file_name="ABCDEFGHIJ") ;
}
====
now => * => @almond_couch_market.post, ((title = "0123456789" && price >= 55) || price <= 11 || (title = "9876543210" && price = 55 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="ABCDEFGHIJ", text_bottom="ABCDEFGHIJ"), ((text_top =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @almond_couch_market.post(title=v_name, contact=v_name, price=55) ;
}
====
now => * => @linkedin.share, (status = "9876543210" || status = "0123456789") 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @linkedin.share(status="23456789") ;
}
====
now => * => @heatpad.set_power, power = enum(off) 
;;
Main() {
    now => @fitbit.getbody(), ((weight >= 155lb && bmi >= 11 && fat <= 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @heatpad.set_power(power=enum(on)) ;
}
====
@wall_street_journal.lifestyle, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link => * => notify
;;
Main() {
    @wall_street_journal.lifestyle(), (title = "23456789") , v_title := title, v_link := link => notify;
}
====
* => @almond_dates.post, ((interest = "0123456789" && message = "23456789") || (interest =~ "0123456789" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @xkcd.new_comic(), ((number <= 14 && title =~ "0123456789")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @almond_dates.post(interest=v_title, poster=v_title, phone=v_title, message="23456789") ;
}
====
@heartrate-monitor.getheartrate, ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate < 42) || (time = makeDate(1487030400000) && place = $context.location.work)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate >= 55) || heartrate < 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature < 73F && wind_speed = 5kmph && humidity < 55 && cloudiness = 7) || (location = $context.location.current_location && temperature > 73F && humidity <= 14 && cloudiness > 11))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
* => @almond_dates.post, ((message = "9876543210" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number) || (poster = "23456789" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), ((contains(tags, "funny"^^tt:hashtag) && body = "9876543210")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @almond_dates.post(interest=v_title, message=v_title, poster=v_title, phone=v_title) ;
}
====
* => @almond_couch_market.search, (title = "0123456789" && price < 7) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="ABCDEFGHIJ"), ((tournament_search_term =~ "23456789" && tournament_full_name =~ "ABCDEFGHIJ" && away_alias = "0123456789" && home_alias = "23456789" && away_name = "9876543210" && home_name = "0123456789" && scheduled_time = "9876543210" && away_points <= 11 && home_points <= 14)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @almond_couch_market.search(), ((price >= 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @almond_couch_market.post, (contact = "+16501234567"^^tt:phone_number || (price = 14 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), ((location = $context.location.current_location && date = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @almond_couch_market.post(title="9876543210", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
now => @xkcd.get_comic, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || alt_text = "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @car.honk_horn, true 
;;
Main() {
    now => @xkcd.get_comic(), (((number <= 14 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (number = 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @car.honk_horn() ;
}
====
@instagram.new_picture, ((caption = "9876543210" && filter = "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && filter = "0123456789" && location = $context.location.current_location))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@youtube.source, (channel_title =~ "12345" || video_url = "http://www.abc.def"^^tt:url || (channel_title =~ "23456789" && video_id =~ "9876543210") || (channel_id = "0123456789" && channel_title = "ABCDEFGHIJ" && title =~ "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), (((channel_id =~ "0123456789" && channel_title = "9876543210" && title = "12345" && video_id = "9876543210") || (title = "0123456788" && video_id = "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @uber.price_estimate(start=$context.location.home, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = $context.location.home && high_estimate < 11 && surge <= 42 && duration > 2h && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => @almond_dates.post, message = "12345" 
;;
Main() {
    now => @almond_dates.post(interest="ABCDEFGHIJ", message="9876543210", poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @instagram.get_pictures, (count <= 55 && caption =~ "0123456789" && filter =~ "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.google.com"^^tt:url && user = "0123456788")) , v_title := title, v_link := link, v_user := user, v_category := category => @instagram.get_pictures(), ((count >= 14 && caption =~ "0123456788" && link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@rss.new_post, (title =~ "12345" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), ((title = "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @slack.updateChannelPurpose(channel="lol"^^tt:hashtag, purpose="ABCDEFGHIJ") ;
}
====
@instagram.new_picture, (link = "http://www.abc.def"^^tt:url && filter = "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || (caption =~ "0123456789" && filter = "12345" && contains(hashtags, "lol"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @ytranslate.detect_language(text=v_caption), ((text =~ "23456789" || (text = "9876543210" && detected_language = "zh"^^tt:iso_lang_code("Chinese")))) , v_detected_language := detected_language => notify;
}
====
* => @instagram.get_pictures, (caption =~ "ABCDEFGHIJ" && filter = "12345" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="gsw"^^sportradar:nba_team("Golden State Warriors")), (((watched_team_alias = "gsw"^^sportradar:nba_team("Golden State Warriors") && game_status = enum(inprogress) && scheduled_time = makeDate(1462320000000) && away_points < 11 && home_points >= 11) || (home_name =~ "9876543210" && away_points >= 14 && home_points < 14) || (other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_name =~ "ABCDEFGHIJ" && home_name = "12345" && scheduled_time = makeDate(1462320000000) && away_points >= 7 && home_points = 7))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @instagram.get_pictures(), ((count < 11 && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@yahoofinance.stock_quote, (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "0123456788" && ask_price = 7) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && ask_price < 14 && bid_price = 7)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
* => @onedrive.upload_picture, true 
;;
Main() {
    @gmail.receive_primary_email(), (((from_address = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && snippet = "0123456788") || (from_name = "23456789" && date = makeDate(1487030400000) && contains(labels, "23456789")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @onedrive.upload_picture(file_name=v_subject, picture_url=v_subject) ;
}
====
* => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    @almond_couch_market.monitor(), (contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => @fitbit.recordweight(weight=155lb) ;
}
====
* => @almond_couch_market.search, (title =~ "9876543210" && price > 55 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_dates.monitor(), (((poster = "12345" && phone = "+16501234567"^^tt:phone_number) || (interest = "9876543210" && message = "12345" && poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest = "9876543210" && message =~ "23456789" && poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number) || (message = "23456789" && poster =~ "9876543210"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_couch_market.search(), ((title = "0123456789" && price < 42 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @dropbox.create_new_folder, true 
;;
Main() {
    @omlet.incomingmessage(), (message =~ "ABCDEFGHIJ") , v_type := type, v_message := message => @dropbox.create_new_folder(folder_name=v_message) ;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "23456789" || file_name = "0123456789" || file_name =~ "0123456789" || file_name = "0123456789") 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@heartrate-monitor.getheartrate, heartrate >= 14 , v_time := time, v_place := place, v_heartrate := heartrate => * => @fitbit.recordweight, weight >= 155lb 
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @fitbit.recordweight(weight=155lb) ;
}
====
@phdcomics.new_post, (title = "ABCDEFGHIJ" || (link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => notify
;;
Main() {
    @phdcomics.new_post(), ((link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, ((day =~ "9876543210" && inactiveTime < 30min && calories >= 500kcal) || (day = "9876543210" && distance >= 42cm && steps <= 55 && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "12345" && distance < 1000m && steps >= 11 && activeTime > 2h && inactiveTime < 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @onedrive.upload_picture(file_name=v_day, picture_url=v_day) ;
}
====
* => @tumblr-blog.post_text, (title =~ "23456789" && body = "23456789") 
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "ABCDEFGHIJ" && div >= 55)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @tumblr-blog.post_text(title="12345", body="0123456788") ;
}
====
* => @builtin.configure, true 
;;
Main() {
    @github.new_commit(repo_name="23456789"), ((message =~ "9876543210" && time = makeDate(1487030400000))) , v_user := user, v_message := message, v_time := time => @builtin.configure(device="scale"^^tt:device("my scale")) ;
}
====
@reddit_front_page.newpost, (user = "12345" && category = "12345") , v_title := title, v_link := link, v_user := user, v_category := category => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title = "ABCDEFGHIJ" && user =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
* => * => @almond_couch_market.post, (title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @almond_couch_market.post(title=v_category, contact=v_category, price=7) ;
}
====
@github.new_commit, true , v_user := user, v_message := message, v_time := time => notify
;;
Main() {
    @github.new_commit(repo_name="9876543210"), (time = makeDate(1487030400000)) , v_user := user, v_message := message, v_time := time => notify;
}
====
now => * => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @builtin.get_random(), (number < 14) , v_number := number => @security-camera.set_power(power=enum(off)) ;
}
====
* => @almond_couch_market.post, (title = "12345" && price <= 11 && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1462320000000) && value > 22C)) , v_time := time, v_value := value => @almond_couch_market.post(title="0123456789", price=11, contact="+15551234567"^^tt:phone_number) ;
}
====
@tumblr.new_photo, blog_name = "ABCDEFGHIJ" , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="0123456789"), ((blog_name =~ "ABCDEFGHIJ" || (blog_name = "23456789" && date = makeDate(1462320000000) && caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @phone.get_gps(), (((location = $context.location.current_location && speed >= 25mph) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude = 42cm && bearing <= 7 && speed > 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => * => @light-bulb.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @xkcd.random_comic(), (((number >= 7 && title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && alt_text = "23456789") || (number < 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @light-bulb.set_power(power=enum(on)) ;
}
====
* => @uber.time_estimate, (estimate < 2h || uber_type =~ "ABCDEFGHIJ" || (uber_type = "0123456789" && estimate = 30min) || (start = $context.location.home && estimate < 30min) || (start = $context.location.home && estimate <= 30min) || (start = $context.location.current_location && uber_type =~ "0123456789") || (uber_type =~ "ABCDEFGHIJ" && estimate >= 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = $context.location.work && uber_type =~ "9876543210")) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @almond_bike_market.post, ((title = "9876543210" && poster =~ "ABCDEFGHIJ") || (price >= 42 && info = "23456789")) 
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "12345" && count <= 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @almond_bike_market.post(price=v_count, title="23456789", info="12345", poster="0123456789") ;
}
====
* => @weatherapi.sunrise, (date = makeDate(1462320000000) && sunrise = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), (sunset = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
now => * => @dropbox.create_new_folder, (folder_name =~ "12345" || folder_name = "0123456789" || folder_name =~ "9876543210" || folder_name =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @instagram.get_pictures(), (((contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.current_location) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") || (caption =~ "0123456788" && link = "http://www.abc.def"^^tt:url && filter =~ "23456789"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @dropbox.create_new_folder(folder_name=v_filter) ;
}
====
@yahoofinance.stock_div, ((stock_id = "goog"^^tt:stock_id("Google") && yield < 7 && div = 42 && ex_div_date = makeDate(1462320000000)) || div >= 14 || (stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "0123456789" && yield <= 7 && div >= 14 && ex_div_date = makeDate(1487030400000)) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "0123456788" && yield = 14 && div < 7)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((yield = 11 && div <= 14) || (stock_id = "msft"^^tt:stock_id("Microsoft") && yield <= 14 && pay_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
@phone.gps, (altitude >= 1000m && bearing >= 11) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude < 1000m && bearing >= 14 && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @instagram.get_pictures(), ((count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && filter =~ "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@fitness-tracker.getbody, (weight < 82kg && bodyFat < 11 && bmi <= 42) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight > 155lb && bodyFat <= 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @fitbit.getbody(), ((fat = 14 || fat >= 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => * => @linkedin.share, status =~ "0123456788" 
;;
Main() {
    now => @linkedin.get_profile(), ((industry = "23456789" && specialties = "0123456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @linkedin.share(status=v_headline) ;
}
====
* => @facebook.post, status =~ "0123456789" 
;;
Main() {
    @wall_street_journal.opinions(), ((title =~ "23456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @facebook.post(status="0123456788") ;
}
====
now => @twitter.search_by_hashtag, (query_hashtag = "lol"^^tt:hashtag && count <= 11 && text =~ "0123456789" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag), ((text =~ "9876543210" && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 1000m)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight, true 
;;
Main() {
    now => @phone.get_gps(), ((altitude < 42cm && bearing >= 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight(weight=82kg) ;
}
====
* => @tumblr-blog.post_text, (title =~ "12345" || body =~ "23456789") 
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @tumblr-blog.post_text(body=v_title, title="0123456789") ;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || title = "12345" || title =~ "23456789" || title =~ "0123456789" || link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ" || title = "23456789") , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (((title = "23456789" && link = "http://www.abc.def"^^tt:url) || (title = "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => notify;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @github.new_milestone(repo_name="ABCDEFGHIJ"), ((repo_name = "ABCDEFGHIJ" && description =~ "9876543210")) , v_user := user, v_description := description, v_time := time => @imgflip.generate(template=v_description, text_top="0123456788", text_bottom="23456789"), (((template = "0123456788" && name =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template =~ "0123456789" && text_bottom =~ "ABCDEFGHIJ" && name = "0123456788") || text_top =~ "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @omlet.send_to, message = "23456789" 
;;
Main() {
    @washington_post.new_article(section=enum(lifestyle)), (title =~ "23456789") , v_title := title, v_link := link, v_description := description => @omlet.send_to(to="alice"^^tt:username, message="23456789") ;
}
====
@twitter.my_tweet, text = "12345" , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify
;;
Main() {
    @twitter.my_tweet(), ((contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify;
}
====
* => @almond_bike_market.ask_seller, (id =~ "0123456788" && value = "ABCDEFGHIJ") , v_answer := answer => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @almond_bike_market.ask_seller(id="12345", property="ABCDEFGHIJ", value="9876543210", public=true), ((id = "23456789" && value =~ "9876543210")) , v_answer := answer => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@scale.source, (value > 82kg || value <= 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (time = makeDate(1462320000000)) , v_time := time, v_value := value => notify;
}
====
now => @gmail.send_picture, (to = "alice@gmail.com"^^tt:email_address && subject =~ "12345") 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @linkedin.share, (status = "9876543210" || status = "0123456789") 
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=$context.location.home), ((end = $context.location.current_location && high_estimate = 14 && currency_code =~ "9876543210" && surge <= 7 && duration > 2h && distance >= 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @linkedin.share(status=v_currency_code) ;
}
====
@jawbone_up.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @jawbone_up.getheartrate(), (((time = makeDate(1462320000000) && place = $context.location.work && heartrate <= 14) || heartrate < 11)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @github.comment_issue, ((repo_name =~ "ABCDEFGHIJ" && issue_number > 14 && body =~ "0123456788") || (repo_name = "0123456789" && body = "9876543210") || repo_name =~ "9876543210") 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @github.comment_issue(repo_name="9876543210", issue_number=11, body="12345") ;
}
====
now => * => @car.honk_horn, true 
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @car.honk_horn() ;
}
====
@almond_dates.monitor, ((interest = "0123456789" && poster = "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "9876543210" && message = "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => @lg_webos_tv.play_url, true 
;;
Main() {
    @almond_dates.monitor(), (interest =~ "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @holidays.next_us_holiday_event(), ((summary =~ "0123456789" || summary = "0123456788")) , v_date := date, v_summary := summary, v_description := description => @lg_webos_tv.play_url(url=v_description) ;
}
====
now => * => @almond_dates.post, (message = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), ((is_folder = false && file_size < 20MB && full_path =~ "9876543210")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @almond_dates.post(interest=v_file_name, poster=v_full_path, phone=v_file_name, message="0123456788") ;
}
====
* => @imgflip.generate, (template = "0123456788" && text_top =~ "23456789" && text_bottom =~ "0123456789" && name = "23456789") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="0123456789", text_top="0123456789", text_bottom="12345"), (((text_bottom = "23456789" && name =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top =~ "9876543210" && text_bottom =~ "0123456788"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@xkcd.new_comic, ((number <= 11 && title =~ "9876543210") || (number <= 7 && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), (number <= 7) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @facebook.post, status = "0123456788" 
;;
Main() {
    @twitter.my_tweet(), ((text = "ABCDEFGHIJ" && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @facebook.post(status="23456789") ;
}
====
now => * => @builtin.configure, true 
;;
Main() {
    now => @builtin.configure(device="facebook"^^tt:device("Facebook")) ;
}
====
* => @gmail.send_picture, ((message = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (subject =~ "23456789" && message = "ABCDEFGHIJ") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (subject =~ "23456789" && message =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @activity-tracker.getmove(), (((day =~ "23456789" && distance < 42cm && steps < 7 && activeTime > 2h) || (distance >= 1000m && steps < 55 && activeTime >= 2h && inactiveTime = 30min))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @gmail.send_picture(to=v_day, subject=v_day, picture_url=v_day, message="9876543210") ;
}
====
@tumblr.new_photo, ((blog_name = "ABCDEFGHIJ" && caption =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || caption = "23456789") , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => *
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), ((blog_name = "0123456789" && caption = "12345" && contains(tags, "cat"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
@thermostat.temperature, ((time = makeDate(1487030400000) && value > 73F) || value < 22C) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), (value < 22C) , v_time := time, v_value := value => @almond_bike_market.ask_seller(id="12345", property="12345", value="23456789", public=false), (((property = "0123456788" && value =~ "23456789" && answer = "12345") || (value = "0123456788" && answer = "0123456788") || public = false)) , v_answer := answer => notify;
}
====
* => @linkedin.get_profile, (headline =~ "0123456789" && specialties = "0123456789" && contains(positions, "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => @linkedin.get_profile(), ((industry = "ABCDEFGHIJ" || (industry = "12345" && contains(positions, "23456789")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @bluetooth_speaker.set_volume(percent=14) ;
}
====
now => @omlet.send_to, (message =~ "ABCDEFGHIJ" || message =~ "0123456788") 
;;
Main() {
    now => @omlet.send_to(to="bob"^^tt:username, message="23456789") ;
}
====
now => * => @dropbox.create_new_folder, (folder_name =~ "12345" || folder_name = "0123456789" || folder_name =~ "9876543210" || folder_name =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => @dropbox.create_new_folder(folder_name=v_name) ;
}
====
* => @thermostat.get_humidity, value >= 11 , v_value := value => notify
;;
Main() {
    now => @thermostat.get_humidity(), (value < 55) , v_value := value => notify;
}
====
now => @tumblr-blog.post_text, title = "0123456788" 
;;
Main() {
    now => @tumblr-blog.post_text(title="0123456788", body="0123456788") ;
}
====
* => @bing.image_search, (query = "0123456789" && title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width = 7 && height < 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="0123456789"), (((query = "0123456788" && link = "http://www.google.com"^^tt:url && width >= 14) || (link = "http://www.abc.def"^^tt:url && height = 7))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @slack.updateChannelPurpose(channel=v_title, purpose=v_title) ;
}
====
now => * => @gmail.send_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "12345" && poster =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @gmail.send_picture(to=v_interest, subject=v_poster, message=v_interest, picture_url=v_message) ;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "0123456789" || file_name =~ "12345") 
;;
Main() {
    @builtin.timer(interval=30min), (interval > 30min)  => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @almond_couch_market.post(title="23456789", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @phone.set_ringer, mode = enum(vibrate) 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 55 && name =~ "0123456788" && estimated_diameter_max <= 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.set_ringer(mode=enum(normal)) ;
}
====
@security-camera.new_event, start_time = makeDate(1487030400000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_sound = true && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @slack.send(channel="lol"^^tt:hashtag, message="12345") ;
}
====
now => @gmail.get_latest, subject = "ABCDEFGHIJ" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((subject =~ "12345" && label =~ "9876543210")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 82kg) || (time = makeDate(1487030400000) && weight < 155lb) || weight <= 82kg) , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @onedrive.upload_picture(file_name="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @almond_dates.search, (poster = "12345" || message = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @rss.new_post(), ((link = "http://www.google.com"^^tt:url || title =~ "0123456789" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @almond_dates.search(), ((interest =~ "9876543210" && message =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @car.honk_horn() ;
}
====
now => * => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @phone.get_gps(), (((altitude <= 42cm && bearing >= 7) || (altitude < 42cm && bearing < 11 && speed < 25mph) || location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @heatpad.set_power(power=enum(on)) ;
}
====
* => @linkedin.share, (status =~ "23456789" || status = "9876543210") 
;;
Main() {
    @omlet.incomingmessage(), ((message = "0123456789" || message =~ "0123456789")) , v_type := type, v_message := message => @linkedin.share(status="9876543210") ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @onedrive.file_created(), (file_name = "ABCDEFGHIJ") , v_file_name := file_name => @car.honk_horn() ;
}
====
now => @icalendar.list_events, ((summary =~ "ABCDEFGHIJ" && organizer = "0123456788") || (start_date = makeDate(1487030400000) && summary = "23456789" && description = "12345" && sequence <= 11 && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((description =~ "0123456788" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && leanMass >= 55 && bodyFat >= 7) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.home && weight <= 82kg && leanMass < 55 && bodyFat > 7 && bmi <= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
* => @light-bulb.alert_long, true 
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="12345"), ((tournament_full_name =~ "0123456788" && away_alias =~ "ABCDEFGHIJ" && home_name =~ "0123456788" && away_points <= 11 && home_points >= 42)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @light-bulb.alert_long() ;
}
====
now => * => @twitter.follow, (user_name = "bob"^^tt:username || user_name = "bob"^^tt:username || user_name = "alice"^^tt:username || user_name = "alice"^^tt:username || user_name = "bob"^^tt:username) 
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 55 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @twitter.follow(user_name=v_alt_text) ;
}
====
@scale.source, (value <= 82kg || value >= 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value = 82kg)) , v_time := time, v_value := value => notify;
}
====
* => * => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && distance >= 42cm && activeTime >= 30min && inactiveTime = 2h) || (distance <= 1000m && steps < 7 && activeTime > 30min && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day = "23456789" && distance <= 42cm && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
* => @xkcd.random_comic, ((number <= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "0123456788") || (number < 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @onedrive.delete_file, true 
;;
Main() {
    now => @xkcd.random_comic(), (((number > 55 && title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "12345") || (link = "http://www.google.com"^^tt:url && alt_text = "0123456789"))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @onedrive.delete_file(file_name="23456789") ;
}
====
now => @almond_couch_market.post, (title = "23456789" && price <= 11) 
;;
Main() {
    now => @almond_couch_market.post(title="23456789", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
@weatherapi.monitor, ((wind_speed <= 5kmph && humidity >= 55 && cloudiness < 42) || (temperature >= 73F && humidity = 11 && cloudiness >= 7 && status = enum(raining)) || (location = $context.location.work && wind_speed >= 25mph && fog = 42 && status = enum(sunny))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((location = $context.location.home && wind_speed <= 25mph && humidity > 14 && cloudiness > 55 && fog > 7) || (temperature < 22C && humidity <= 14 && cloudiness >= 14 && fog = 7))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @holidays.next_us_holiday_event(), (((summary = "0123456788" && description = "ABCDEFGHIJ") || summary = "23456789" || (date = makeDate(1462320000000) && summary =~ "0123456789" && description =~ "ABCDEFGHIJ") || summary =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @github.comment_issue, ((issue_number > 14 && body =~ "0123456789") || (repo_name = "0123456789" && body = "23456789")) 
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(heating) || state = enum(heating))) , v_mode := mode, v_state := state => @github.comment_issue(repo_name="0123456789", issue_number=11, body="0123456788") ;
}
====
* => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @wall_street_journal.get_opinions(), ((title = "ABCDEFGHIJ" || title =~ "23456789")) , v_title := title, v_link := link, v_updated := updated => @speaker.lower_volume() ;
}
====
* => @uber.time_estimate, (start = $context.location.work && uber_type =~ "23456789" && estimate < 2h) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @washington_post.new_article(section=enum(sports)), ((description =~ "12345" || (section = enum(business) && title =~ "ABCDEFGHIJ" && description = "12345") || (section = enum(sports) && description = "9876543210") || (title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || title = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_description := description => @uber.time_estimate(start=$context.location.work), (uber_type = "23456789") , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => * => @slack.updateChannelPurpose, (purpose = "ABCDEFGHIJ" || (channel = "lol"^^tt:hashtag && purpose = "12345") || channel = "lol"^^tt:hashtag) 
;;
Main() {
    now => @slack.updateChannelPurpose(channel="funny"^^tt:hashtag, purpose="0123456788") ;
}
====
@phdcomics.new_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), ((title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
now => * => @gmail.send_picture, message = "9876543210" 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @giphy.get_tag, tag = "lol"^^tt:hashtag , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => @car.honk_horn() ;
}
====
* => @almond_dates.post, ((message = "9876543210" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number) || (poster = "23456789" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @thermostat.temperature(), (value < 73F) , v_time := time, v_value := value => @almond_dates.post(interest="9876543210", message="ABCDEFGHIJ", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @twitter.unfollow, true 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="fcb"^^sportradar:eu_soccer_team("Bayern Munchen")), (((watched_is_home = false && away_name =~ "23456789" && home_name =~ "0123456788" && away_points = 42 && home_points >= 11) || (other_team_alias = "bar"^^sportradar:eu_soccer_team("Barcellona") && home_name = "23456789" && away_points < 7 && home_points = 11))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @twitter.unfollow(user_name=v_away_name) ;
}
====
* => @fitbit.getsteps, steps <= 14 , v_date := date, v_steps := steps => @google_drive.create_new_drive_file, true 
;;
Main() {
    now => @fitbit.getsteps(), true , v_date := date, v_steps := steps => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
now => * => @slack.updateChannelPurpose, channel = "funny"^^tt:hashtag 
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && description =~ "23456789" && sequence < 42 && organizer =~ "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @slack.updateChannelPurpose(channel=v_description, purpose=v_organizer) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(info="23456789", price=42), (((info =~ "23456789" && price >= 14) || price < 7 || price >= 42))  => @almond_couch_market.search(), (price >= 11) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @thecatapi.get, count >= 7 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "23456789" && count < 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => * => @facebook.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.current_location), (location = $context.location.work) , v_date := date, v_phase := phase => @facebook.post_picture(picture_url=v_phase, caption=v_phase) ;
}
====
* => @almond_couch_market.search, (title = "ABCDEFGHIJ" && price < 42) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "23456789" && contact = "+15551234567"^^tt:phone_number) || price = 11 || title =~ "ABCDEFGHIJ")) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@icalendar.event_begin, ((end_date = makeDate(1462320000000) && summary =~ "9876543210" && description = "23456789" && sequence <= 7 && organizer = "ABCDEFGHIJ" && location = "12345") || (start_date = makeDate(1487030400000) && summary =~ "12345" && sequence = 7 && organizer =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((description =~ "12345" && sequence > 14 && location = "9876543210") || (end_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && sequence = 42))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @instagram.get_pictures, (count < 42 && filter = "0123456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), ((issue_number <= 7 && body =~ "0123456789")) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @instagram.get_pictures(count=v_issue_number), ((caption =~ "0123456788" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @thermostat.get_hvac_state, ((mode = enum(off) && state = enum(heating)) || (mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => *
;;
Main() {
    @almond_bike_market.monitor(price=42), ((info = "23456789" && price >= 14))  => @thermostat.get_hvac_state(), ((state = enum(off) || state = enum(cooling) || state = enum(off))) , v_mode := mode, v_state := state => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @slack.updateChannelTopic, true 
;;
Main() {
    @almond_dates.monitor(), (message =~ "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @slack.updateChannelTopic(channel=v_message, topic=v_poster) ;
}
====
now => * => @linkedin.share, status = "ABCDEFGHIJ" 
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "23456789" && count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (image_id =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || (image_id = "23456789" && count >= 14 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
@fitness-tracker.getbody, (time = makeDate(1462320000000) && bodyFat = 7 && bmi >= 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight > 155lb && leanMass >= 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @security-camera.get_url(), true , v_link := link => notify;
}
====
@weatherapi.monitor, ((humidity > 11 && cloudiness <= 42 && fog >= 14 && weather = "0123456789" && status = enum(drizzling)) || (location = $context.location.work && temperature < 22C && humidity > 7 && fog > 42 && status = enum(raining))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((temperature < 73F && wind_speed < 5kmph && humidity < 55 && cloudiness < 14 && fog >= 42 && status = enum(snowy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_bike_market.ask(id=v_weather, property="ABCDEFGHIJ"), true  => notify;
}
====
@slack.receive, ((timestamp = "0123456788" && from = "bob"^^tt:username && message =~ "12345") || (from = "bob"^^tt:username && message =~ "0123456788") || (timestamp =~ "0123456789" && message =~ "0123456789") || (timestamp = "9876543210" && from = "bob"^^tt:username && message = "0123456788") || message = "9876543210" || (channel = "cat"^^tt:hashtag && timestamp = "0123456788" && from = "bob"^^tt:username && message =~ "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((timestamp = "23456789" && from = "bob"^^tt:username && message = "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
@thermostat.temperature, time = makeDate(1462320000000) , v_time := time, v_value := value => * => *
;;
Main() {
    @thermostat.temperature(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => @almond_bike_market.detail(id="0123456788"), true  => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="ABCDEFGHIJ") ;
}
====
now => @twitter.search, (count >= 7 && text =~ "23456789" && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search(query="0123456789", count=14), ((query =~ "0123456789" && count < 7 && text =~ "0123456789" && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @gmail.get_latest, (from = "bob@stanford.edu"^^tt:email_address || label = "9876543210" || from = "bob@stanford.edu"^^tt:email_address || from = "bob@stanford.edu"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((label =~ "12345" || label =~ "9876543210" || (subject = "23456789" && label =~ "23456789") || from = "bob@stanford.edu"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && subject =~ "12345" && label = "23456789"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @weatherapi.moon, (phase = "23456789" || phase = "0123456789") , v_date := date, v_phase := phase => *
;;
Main() {
    @jawbone_up.getheartrate(), ((heartrate = 14 || (time = makeDate(1462320000000) && heartrate >= 42) || (time = makeDate(1487030400000) && place = $context.location.current_location && heartrate > 11) || (time = makeDate(1487030400000) && heartrate < 42))) , v_time := time, v_place := place, v_heartrate := heartrate => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((date = makeDate(1487030400000) && phase = "ABCDEFGHIJ")) , v_date := date, v_phase := phase => notify;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (filter = "23456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.getbody(), ((bmi > 14 && fat > 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@sportradar.mlb_team, (watched_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && home_name =~ "12345" && game_status = enum(scheduled) && scheduled_time = "12345" && game_inning =~ "0123456788" && away_runs < 7) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => *
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="chc"^^sportradar:mlb_team("Chicago Cubs")), ((other_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && away_name = "12345" && home_name =~ "0123456789" && result = enum(draw))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @gmail.send_email(to=v_game_inning, subject=v_scheduled_time, message=v_scheduled_time) ;
}
====
* => @car.honk_horn, true 
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), ((user = "alice"^^tt:username && issue_number >= 42 && time = makeDate(1487030400000))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @car.honk_horn() ;
}
====
* => @linkedin.get_profile, (formatted_name = "23456789" && industry = "0123456788" && specialties =~ "ABCDEFGHIJ") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @gmail.receive_primary_email(), ((subject = "ABCDEFGHIJ" && date = makeDate(1487030400000))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @linkedin.get_profile(), (((formatted_name =~ "0123456788" && headline = "0123456788" && specialties = "0123456788") || (formatted_name = "ABCDEFGHIJ" && headline =~ "23456789" && contains(positions, "9876543210")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "0123456789" && sequence = 11 && organizer =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.create_new_folder(folder_name=v_description) ;
}
====
@phdcomics.new_post, title = "ABCDEFGHIJ" , v_title := title, v_link := link, v_picture_url := picture_url => @heatpad.set_power, true 
;;
Main() {
    @phdcomics.new_post(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
@phdcomics.new_post, (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "0123456788" && link = "http://www.abc.def"^^tt:url) || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_link := link, v_picture_url := picture_url => @youtube.list_subscriptions(), (((channel_id = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "0123456788" && title =~ "12345" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @twitter.unfollow(user_name=v_title) ;
}
====
now => * => @tumblr-blog.post_text, title = "0123456788" 
;;
Main() {
    now => @icalendar.list_events(), (((end_date = makeDate(1487030400000) && sequence < 7 && organizer = "0123456789") || (summary = "0123456788" && description =~ "0123456789" && sequence > 11 && organizer = "ABCDEFGHIJ") || (summary = "0123456789" && description =~ "0123456789" && sequence = 7 && organizer =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @tumblr-blog.post_text(title=v_description, body="23456789") ;
}
====
@tumblr.new_photo, (date = makeDate(1487030400000) || (blog_name = "0123456788" && link = "http://www.google.com"^^tt:url)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => @almond_dates.post, ((message = "0123456788" && poster = "0123456788") || (message = "9876543210" && poster =~ v_caption)) 
;;
Main() {
    @tumblr.new_photo(blog_name="0123456789"), ((blog_name =~ "0123456789" && date = makeDate(1462320000000) && link = "http://www.google.com"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @holidays.next_uk_holiday_event(), ((summary = "0123456789" && description = "0123456788")) , v_date := date, v_summary := summary, v_description := description => @almond_dates.post(interest="ABCDEFGHIJ", message="0123456789", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
@heartrate-monitor.getheartrate, heartrate <= 11 , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) && heartrate >= 42)) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
now => @almond_dates.search, ((message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "0123456789" && message =~ "ABCDEFGHIJ" && poster = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((interest =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @heatpad.set_power(power=enum(off)) ;
}
====
* => @holidays.next_uk_holiday_event, description =~ "23456789" , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @thecatapi.get, ((image_id =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || image_id = "ABCDEFGHIJ") , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @speaker.set_volume, percent >= 14 
;;
Main() {
    @wall_street_journal.technology(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @thecatapi.get(), ((count = 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @speaker.set_volume(percent=42) ;
}
====
* => @tumblr-blog.post_text, (title =~ "0123456788" && body =~ "0123456789") 
;;
Main() {
    now => @tumblr-blog.post_text(title="0123456788", body="ABCDEFGHIJ") ;
}
====
* => @tumblr-blog.post_text, (title =~ "0123456788" && body =~ "0123456789") 
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "9876543210") , v_file_name := file_name => @tumblr-blog.post_text(title="0123456788", body="23456789") ;
}
====
@scale.source, value > 155lb , v_time := time, v_value := value => @ytranslate.detect_language, true , v_detected_language := detected_language => @gmail.send_email, ((subject = "0123456789" && message = "9876543210") || message = "0123456788" || message =~ "ABCDEFGHIJ") 
;;
Main() {
    @scale.source(), (value >= 155lb) , v_time := time, v_value := value => @ytranslate.detect_language(text="12345"), (text =~ "9876543210") , v_detected_language := detected_language => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="12345") ;
}
====
* => @imgflip.generate, ((text_top = "ABCDEFGHIJ" && name =~ "23456789") || (template =~ "ABCDEFGHIJ" && text_top =~ "12345" && text_bottom =~ "12345" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "23456789" && text_top = "12345")) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="23456789", text_bottom="12345"), ((template =~ "9876543210" && text_top = "0123456788" && text_bottom =~ "9876543210" && name =~ "0123456789")) , v_name := name, v_picture_url := picture_url => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @builtin.get_random(), ((number = 11 || number >= 42 || number <= 11)) , v_number := number => @security-camera.set_power(power=enum(on)) ;
}
====
* => @twitter.search, ((query = "0123456789" && count >= 55 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)) || (count <= 42 && contains(urls, "http://www.google.com"^^tt:url)) || (query =~ "9876543210" && count > 42 && text = "9876543210" && contains(urls, "http://www.google.com"^^tt:url)) || (query = "9876543210" && count < 42 && contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) || (text = "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username) || (query = "12345" && count >= 14 && text = "9876543210" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "bob"^^tt:username) || (count >= 55 && text =~ "23456789" && from = "bob"^^tt:username) || (query = "ABCDEFGHIJ" && count >= 14 && contains(urls, "http://www.google.com"^^tt:url)) || query =~ "9876543210") , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && place = $context.location.current_location && duration <= 30min && rem >= 2h && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @twitter.search(query="23456789", count=55), ((count = 11 && text = "23456789" && contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @security-camera.set_power, power = enum(on) 
;;
Main() {
    now => @twitter.search(query="ABCDEFGHIJ", count=7, from="bob"^^tt:username), (((count <= 42 && contains(urls, "http://www.abc.def"^^tt:url)) || (count < 55 && text = "9876543210" && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @security-camera.set_power(power=enum(on)) ;
}
====
now => * => @github.comment_issue, ((repo_name = "0123456789" && issue_number > 55 && body =~ "0123456789") || (repo_name =~ "0123456788" && issue_number >= 11 && body =~ "0123456788")) 
;;
Main() {
    now => @twitter.search(query="23456789", count=7, in_reply_to="alice"^^tt:username), ((query = "0123456788" && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @github.comment_issue(repo_name=v_text, issue_number=55, body="0123456789") ;
}
====
now => @phone.get_gps, true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 1000m && bearing >= 55 && speed = 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@youtube.source, ((channel_id = "0123456789" && title = "0123456788" && video_id =~ "0123456788" && video_url = "http://www.abc.def"^^tt:url) || (channel_title = "0123456789" && title =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), (channel_title =~ "9876543210") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @linkedin.share(status=v_channel_title) ;
}
====
* => @youtube.search_videos, (channel_id =~ "12345" && channel_title = "9876543210" && count < 42) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @omlet.newmessage(), ((type = enum(text) && message = "ABCDEFGHIJ")) , v_type := type, v_message := message => @youtube.search_videos(query="9876543210"), ((video_id =~ "0123456788" && channel_id =~ "23456789" && count >= 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@tumblr.new_text, (blog_name =~ "23456789" && date = makeDate(1487030400000) && title =~ "0123456788" && link = "http://www.google.com"^^tt:url && body = "9876543210") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify
;;
Main() {
    @tumblr.new_text(blog_name="0123456788"), ((blog_name =~ "0123456788" && title =~ "0123456789" && body = "ABCDEFGHIJ")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
* => @thermostat.set_hvac_mode, (mode = enum(off) || mode = enum(off)) 
;;
Main() {
    @heartrate-monitor.getheartrate(), (((time = makeDate(1462320000000) && place = $context.location.current_location && heartrate < 7) || time = makeDate(1487030400000))) , v_time := time, v_place := place, v_heartrate := heartrate => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
now => @dropbox.list_folder, (last_modified = makeDate(1487030400000) && file_size < 5KB && full_path =~ "0123456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), ((folder_name = "0123456788" && file_name =~ "23456789" && is_folder = false && file_size <= 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || title =~ "0123456788") 
;;
Main() {
    @security-camera.new_event(), ((has_sound = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @tumblr-blog.post_text(title="0123456789", body="12345") ;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) || heartrate >= 7) , v_time := time, v_place := place, v_heartrate := heartrate => @youtube.channels_by_category, (description =~ "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = $context.location.work || heartrate >= 7 || heartrate <= 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @youtube.channels_by_category(category_id=enum(lifestyle)), ((channel_id = "0123456788" && title = "ABCDEFGHIJ")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@youtube.source, ((channel_title =~ "9876543210" && title =~ "0123456789") || (channel_id =~ "0123456788" && channel_title = "0123456789" && video_id = "0123456789" && video_url = "http://www.google.com"^^tt:url) || (channel_id = "ABCDEFGHIJ" && channel_title = "12345" && title =~ "23456789" && video_id =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id = "23456789" && channel_title =~ "0123456789" && title =~ "ABCDEFGHIJ") || (channel_id = "0123456788" && channel_title = "0123456788" && video_url = "http://www.abc.def"^^tt:url) || (channel_title =~ "0123456789" && video_id =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || title = "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_couch_market.search(), (price > 11) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@google_drive.new_drive_file, file_name =~ "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @security-camera.set_power(power=enum(off)) ;
}
====
* => @imgflip.generate, (template =~ "23456789" && name =~ "23456789") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @imgflip.generate(template="9876543210", text_top="9876543210", text_bottom="0123456789"), ((text_top = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
@instagram.new_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter = "23456789") || (caption =~ "9876543210" && link = "http://www.google.com"^^tt:url) || (caption =~ "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "23456789" && contains(hashtags, "cat"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @giphy.get_tag(tag="funny"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
* => @thermostat.get_hvac_state, ((mode = enum(off) && state = enum(heating)) || (mode = enum(heat_cool) && state = enum(heating))) , v_mode := mode, v_state := state => *
;;
Main() {
    @instagram.new_picture(), ((filter =~ "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.get_hvac_state(), (((mode = enum(cool) && state = enum(off)) || state = enum(off) || (mode = enum(heat) && state = enum(heating)))) , v_mode := mode, v_state := state => @almond_dates.post(interest="12345", message="12345", poster="0123456788", phone="+15551234567"^^tt:phone_number) ;
}
====
now => @wall_street_journal.get_opinions, title =~ "12345" , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title =~ "12345") , v_title := title, v_link := link, v_updated := updated => @twitter.follow(user_name=v_title) ;
}
====
@almond_bike_market.monitor, ((info = "0123456788" && price <= 55) || (info =~ "9876543210" && price <= 55))  => *
;;
Main() {
    @almond_bike_market.monitor(price=7), (price > 11)  => @lg_webos_tv.mute() ;
}
====
now => @giphy.get_tag, (tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), (tag = "lol"^^tt:hashtag) , v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
@xkcd.new_whatif, (link = "http://www.abc.def"^^tt:url || title = "9876543210") , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), ((title =~ "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @xkcd.get_comic(number=14), ((number >= 11 && title = "23456789" && alt_text =~ "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @omlet.send_picture, ((to = "bob"^^tt:username && caption =~ "9876543210") || to = "alice"^^tt:username || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210")) 
;;
Main() {
    now => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
now => @ytranslate.detect_language, text =~ "0123456789" , v_detected_language := detected_language => *
;;
Main() {
    now => @ytranslate.detect_language(text="0123456788"), ((text = "9876543210" && detected_language = "en"^^tt:iso_lang_code("English"))) , v_detected_language := detected_language => @builtin.hello() ;
}
====
* => @instagram.get_pictures, ((count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && link = "http://www.google.com"^^tt:url && filter = "9876543210" && contains(hashtags, "funny"^^tt:hashtag)) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (count > 42 && contains(hashtags, "cat"^^tt:hashtag)) || (count > 42 && caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work) || (count < 11 && caption = "12345")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), (((caption =~ "0123456788" && filter =~ "0123456789") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && location = $context.location.work) || filter =~ "12345" || count >= 42)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name = "12345" || file_name = "9876543210" || file_name = "ABCDEFGHIJ")) , v_file_name := file_name => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, from="alice"^^tt:username, in_reply_to="alice"^^tt:username), (((query_hashtag = "lol"^^tt:hashtag && count > 14 && contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "bob"^^tt:username) || (count > 55 && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @gmail.send_email(to=v_text, subject=v_text, message=v_text) ;
}
====
* => @almond_bike_market.post, (price <= 55 && poster = "0123456788") 
;;
Main() {
    @wall_street_journal.world_news(), true , v_title := title, v_link := link => @almond_bike_market.post(title=v_title, info=v_title, price=7, poster="9876543210") ;
}
====
now => * => @tumblr-blog.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @ytranslate.detect_language(text="0123456789"), ((detected_language = "zh"^^tt:iso_lang_code("Chinese") || detected_language = "en"^^tt:iso_lang_code("English") || text = "ABCDEFGHIJ")) , v_detected_language := detected_language => @tumblr-blog.post_picture(caption="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @youtube.search_channel, (channel_id = "ABCDEFGHIJ" && title =~ "0123456788") , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @youtube.search_channel(query="0123456788"), ((title =~ "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @onedrive.rename_file(old_name=v_channel_id, new_name=v_description) ;
}
====
* => @slack.updateChannelPurpose, ((channel = "cat"^^tt:hashtag && purpose =~ "ABCDEFGHIJ") || purpose = "12345") 
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "0123456788" || file_name =~ "0123456788")) , v_file_name := file_name => @slack.updateChannelPurpose(channel=v_file_name, purpose=v_file_name) ;
}
====
now => * => @almond_dates.post, (message =~ "23456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_bike_market.ask(id="ABCDEFGHIJ", property="12345"), true  => @almond_dates.post(interest="0123456788", message="0123456788", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
now => @giphy.get_tag, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || tag = "funny"^^tt:hashtag || tag = "cat"^^tt:hashtag || tag = "cat"^^tt:hashtag) , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
* => @youtube.channels_by_category, ((category_id = enum(cooking) && channel_id =~ "23456789" && title = "9876543210") || (channel_id = "ABCDEFGHIJ" && title =~ "ABCDEFGHIJ" && description =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(cooking)), ((category_id = enum(lifestyle) && channel_id = "ABCDEFGHIJ" && description = "0123456789")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "0123456789" || file_name = "0123456788")) , v_file_name := file_name => @lg_webos_tv.raise_volume() ;
}
====
@almond_couch_market.monitor, (price <= 14 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), (price > 55) , v_title := title, v_price := price, v_contact := contact => @linkedin.get_profile(), ((headline = "0123456789" && specialties =~ "12345" && contains(positions, "23456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@builtin.timer, interval < 30min  => notify
;;
Main() {
    @builtin.timer(interval=30min), (interval < 30min)  => notify;
}
====
now => @almond_dates.search, (interest =~ "12345" && message =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (((interest = "ABCDEFGHIJ" && message = "0123456788" && poster =~ "0123456788") || (message =~ "12345" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @google_drive.create_new_drive_file(file_name=v_poster) ;
}
====
@phdcomics.new_post, title = "12345" , v_title := title, v_link := link, v_picture_url := picture_url => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
@builtin.timer, true  => *
;;
Main() {
    @builtin.timer(interval=2h), (interval < 30min)  => @security-camera.set_power(power=enum(on)) ;
}
====
@almond_couch_market.monitor, (price >= 55 || (title =~ "23456789" && price <= 42) || (title = "ABCDEFGHIJ" && price <= 55)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "12345" && price = 7)) , v_title := title, v_price := price, v_contact := contact => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || estimate < 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@youtube.source, (channel_id =~ "0123456788" && video_id = "0123456788") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), (((channel_id =~ "0123456789" && channel_title = "12345") || (title = "ABCDEFGHIJ" && video_id = "ABCDEFGHIJ" && video_url = "http://www.abc.def"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @gmail.get_latest(), (from = "bob@stanford.edu"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @thecatapi.get, ((image_id =~ "0123456789" && count <= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id = "ABCDEFGHIJ" && count = 7) || (image_id = "0123456789" && count <= 42) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || count > 55) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (((count >= 11 && link = "http://www.abc.def"^^tt:url) || image_id =~ "23456789")) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @gmail.send_email, ((subject =~ "0123456789" && message = "0123456789") || to = "bob@stanford.edu"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789" && message =~ "23456789") || message =~ "ABCDEFGHIJ" || (subject = "0123456789" && message = "ABCDEFGHIJ")) 
;;
Main() {
    now => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="12345") ;
}
====
* => @uber.time_estimate, estimate > 2h , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    @nasa.asteroid(), (((asteroid_id = 11 && estimated_diameter_min <= 42cm && estimated_diameter_max <= 42cm && closest_distance_to_earth >= 42cm && orbiting_body = "ABCDEFGHIJ") || (asteroid_id >= 11 && relative_velocity <= 5kmph && orbiting_body =~ "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @uber.time_estimate(start=$context.location.work), (uber_type = "23456789") , v_uber_type := uber_type, v_estimate := estimate => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @twitter.unfollow, (user_name = "bob"^^tt:username || user_name = "bob"^^tt:username) 
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => @twitter.unfollow(user_name=v_name) ;
}
====
now => @imgflip.generate, (template = "9876543210" && text_top =~ "9876543210" && text_bottom =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => @car.honk_horn, true 
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="0123456788", text_bottom="0123456788"), (((template = "0123456788" && text_top = "0123456789" && name =~ "0123456788") || (text_bottom =~ "0123456788" && name = "0123456789"))) , v_name := name, v_picture_url := picture_url => @car.honk_horn() ;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), (price <= 42) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(on)) ;
}
====
* => @gmail.get_latest, ((from = "bob@stanford.edu"^^tt:email_address && label = "0123456788") || subject =~ "ABCDEFGHIJ") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1487030400000) && duration > 2h) || (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && rem >= 2h && light <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @gmail.get_latest(), ((subject = "0123456789" && label =~ "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@almond_dates.monitor, ((interest = "0123456789" && message =~ "12345" && phone = "+16501234567"^^tt:phone_number) || (interest = "12345" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "23456789" && message = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@rss.new_post, (link = "http://www.google.com"^^tt:url || title = "23456789") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => @xkcd.get_comic(), ((number < 42 && title =~ "23456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && organizer =~ "ABCDEFGHIJ" && location =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @twitter.sink(status=v_location) ;
}
====
now => * => @linkedin.share, true 
;;
Main() {
    now => @phone.get_gps(), ((altitude <= 42cm || (altitude < 1000m && speed >= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @linkedin.share(status="23456789") ;
}
====
* => @phone.set_ringer, true 
;;
Main() {
    @rss.new_post(), ((title =~ "ABCDEFGHIJ" || title = "9876543210")) , v_title := title, v_link := link => @phone.set_ringer(mode=enum(normal)) ;
}
====
@fitness-tracker.getbody, weight >= 155lb , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && place = $context.location.current_location && bodyFat = 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@security-camera.new_event, true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @lg_webos_tv.lower_volume() ;
}
====
now => * => @omlet.send_picture, ((to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") || to = "bob"^^tt:username) 
;;
Main() {
    now => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
* => @fitbit.recordweight, weight <= 82kg 
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="ABCDEFGHIJ"), ((tournament_league_name = "ABCDEFGHIJ" && away_alias =~ "9876543210" && away_name =~ "9876543210" && away_points < 11)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @fitbit.recordweight(weight=155lb) ;
}
====
* => @google_drive.create_new_drive_file, (file_name = "12345" || file_name =~ "12345") 
;;
Main() {
    @phone.receive_sms(), (body = "23456789") , v_from := from, v_body := body => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @lg_webos_tv.play_url, (url = "http://www.google.com"^^tt:url || url = "http://www.abc.def"^^tt:url) 
;;
Main() {
    now => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
@bodytrace.source, ((time = makeDate(1462320000000) && weight < 155lb) || (time = makeDate(1462320000000) && weight <= 82kg) || (time = makeDate(1462320000000) && weight >= 82kg) || weight <= 82kg) , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), ((weight <= 82kg || weight < 155lb)) , v_time := time, v_weight := weight => notify;
}
====
now => @nasa.apod, ((title = "12345" && description = "0123456789") || title =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (((title =~ "ABCDEFGHIJ" && description =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "9876543210" && description = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => * => @phone.send_sms, body = "ABCDEFGHIJ" 
;;
Main() {
    now => @twitter.search(query="9876543210", from="alice"^^tt:username), (contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @phone.send_sms(body=v_text, to="+16501234567"^^tt:phone_number) ;
}
====
* => @linkedin.share, status =~ "0123456788" 
;;
Main() {
    @almond_dates.monitor(), (((interest = "12345" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || message = "ABCDEFGHIJ" || (message = "12345" && poster =~ "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @linkedin.share(status="0123456789") ;
}
====
* => @gmail.send_picture, (subject =~ "0123456788" && message = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @phdcomics.new_post(), (title = "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => @gmail.send_picture(picture_url=v_picture_url, to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="23456789") ;
}
====
@weatherapi.monitor, ((wind_speed > 5kmph && humidity > 7 && cloudiness <= 55 && fog > 14) || (wind_speed = 5kmph && humidity > 7 && fog > 7 && weather =~ "9876543210") || (temperature > 73F && wind_speed > 5kmph && humidity <= 42 && cloudiness >= 42 && fog > 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), ((location = $context.location.home && temperature >= 73F && humidity >= 42 && cloudiness < 14 && fog <= 7 && weather =~ "0123456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @weatherapi.current, ((temperature > 22C && wind_speed < 5kmph && cloudiness > 42 && fog < 42) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && wind_speed >= 5kmph && humidity >= 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    @scale.source(), ((value <= 82kg || value < 155lb || value <= 155lb || (time = makeDate(1487030400000) && value >= 155lb))) , v_time := time, v_value := value => @weatherapi.current(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature >= 22C && wind_speed <= 5kmph && humidity > 11 && fog <= 14 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
@thermostat.temperature, ((time = makeDate(1487030400000) && value = 22C) || value > 73F || value >= 73F || value > 73F) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), ((value < 22C || time = makeDate(1462320000000) || value < 73F)) , v_time := time, v_value := value => @weatherapi.moon(location=$context.location.work), (phase =~ "12345") , v_date := date, v_phase := phase => notify;
}
====
@weatherapi.monitor, (temperature > 22C && wind_speed > 5kmph && humidity >= 42 && cloudiness <= 55 && fog >= 11 && weather = "0123456788") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = $context.location.work && temperature >= 22C && wind_speed > 5kmph && cloudiness > 7 && fog = 55 && weather =~ "0123456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") , v_from := from, v_subject := subject, v_label := label => @gmail.send_picture, ((subject = "0123456789" && message = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (to = "alice@gmail.com"^^tt:email_address && message = "23456789")) 
;;
Main() {
    now => @gmail.get_latest(), (((subject =~ "0123456789" && label = "0123456789") || (subject =~ "0123456788" && label =~ "9876543210") || label = "12345" || subject =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => @gmail.send_picture(to=v_subject, subject=v_subject, message=v_subject, picture_url=v_subject) ;
}
====
* => @gmail.get_latest, ((subject =~ "23456789" && label =~ "12345") || (from = "bob@stanford.edu"^^tt:email_address && label = "12345")) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @gmail.get_latest(), (((from = "bob@stanford.edu"^^tt:email_address && label =~ "ABCDEFGHIJ") || subject =~ "23456789" || subject =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@youtube.source, ((channel_id =~ "23456789" && channel_title =~ "23456789" && title =~ "9876543210" && video_id = "9876543210") || (channel_title =~ "9876543210" && title =~ "0123456789") || (channel_id =~ "23456789" && title =~ "9876543210" && video_id = "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), (((channel_id =~ "12345" && title =~ "0123456788" && video_id =~ "23456789") || video_id = "0123456788" || (channel_title =~ "23456789" && video_id = "0123456788"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @light-bulb.set_power(power=enum(off)) ;
}
====
@google_drive.new_drive_file, (file_name = "ABCDEFGHIJ" || file_name = "12345" || file_name =~ "0123456789" || file_name = "0123456788") , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "23456789" || file_name =~ "ABCDEFGHIJ")) , v_file_name := file_name => notify;
}
====
now => @onedrive.upload_picture, (file_name = "0123456788" || file_name =~ "12345") 
;;
Main() {
    now => @onedrive.upload_picture(file_name="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    @almond_bike_market.monitor(), ((price > 11 || price <= 11))  => @security-camera.set_power(power=enum(off)) ;
}
====
* => @facebook.post, status =~ "0123456789" 
;;
Main() {
    now => @facebook.post(status="9876543210") ;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @thecatapi.get(), (count > 42) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @facebook.post(status=v_image_id) ;
}
====
* => @almond_bike_market.post, ((title =~ "9876543210" && price > 11 && poster =~ "23456789") || (price >= 7 && info =~ "12345") || (title =~ "12345" && poster = "23456789")) 
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "9876543210") , v_file_name := file_name => @almond_bike_market.post(title="0123456788", price=7, info="12345", poster="9876543210") ;
}
====
* => @wall_street_journal.get_opinions, ((title = "23456789" && updated = makeDate(1487030400000)) || (title =~ "9876543210" && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || (title = "9876543210" && link = "http://www.abc.def"^^tt:url) || (title = "12345" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title = "9876543210" && updated = makeDate(1487030400000)) || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => *
;;
Main() {
    @slack.receive(), (((channel = "lol"^^tt:hashtag && timestamp =~ "0123456788") || (timestamp = "23456789" && message =~ "0123456788") || (from = "bob"^^tt:username && message = "0123456789") || (channel = "lol"^^tt:hashtag && message =~ "0123456788"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @onedrive.rename_file(old_name="12345", new_name="0123456789") ;
}
====
@gmail.receive_email, ((from_name =~ "ABCDEFGHIJ" && from_address = "bob@stanford.edu"^^tt:email_address && snippet = "23456789") || (from_name =~ "9876543210" && contains(labels, "0123456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_email(), (((from_address = "alice@gmail.com"^^tt:email_address && snippet = "0123456789") || subject =~ "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
* => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    @fitness-tracker.getbody(), (((weight >= 82kg && leanMass <= 11 && bodyFat >= 7 && bmi < 42) || (time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && weight = 155lb && leanMass >= 7 && bodyFat = 7) || (weight >= 155lb && leanMass < 11 && bodyFat >= 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && has_sound = false && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_sound = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
* => @almond_bike_market.detail, id = "0123456789"  => notify
;;
Main() {
    now => @almond_bike_market.detail(id="12345"), true  => notify;
}
====
@reddit_front_page.newpost, (link = "http://www.abc.def"^^tt:url && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.abc.def"^^tt:url && user =~ "0123456788")) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
@fitness-tracker.getbody, ((time = makeDate(1487030400000) && leanMass < 55 && bodyFat < 14) || (time = makeDate(1462320000000) && weight > 155lb && bodyFat <= 42 && bmi < 7) || (weight < 155lb && leanMass >= 55 && bmi < 7) || (weight <= 82kg && leanMass = 42 && bodyFat <= 55 && bmi <= 14) || (time = makeDate(1462320000000) && weight = 155lb && leanMass >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_couch_market.search, (title =~ "ABCDEFGHIJ" && price > v_leanMass && contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @fitness-tracker.getbody(), (((weight <= 82kg && leanMass >= 55 && bodyFat < 14 && bmi < 14) || (weight > 82kg && leanMass > 11 && bodyFat >= 7 && bmi < 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_couch_market.search(), ((price > 42 || contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @fitbit.getbody, (weight < 155lb && bmi > 42) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((bmi = 11 && fat < 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @omlet.incomingmessage(), ((type = enum(text) || type = enum(picture))) , v_type := type, v_message := message => @heatpad.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && distance < 1000m && steps <= 42 && activeTime < 2h && inactiveTime < 2h && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && distance <= 1000m && steps < 7 && activeTime >= 30min && inactiveTime < 30min && calories < 500kcal) || (distance >= 1000m && steps >= 14 && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "23456789" && distance < 42cm && steps > 7 && activeTime < 2h && inactiveTime <= 30min && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @light-bulb.alert_long() ;
}
====
@twitter.direct_message, (message =~ "ABCDEFGHIJ" && __reserved = true) , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), (from = "bob"^^tt:username) , v_from := from, v_message := message, v___reserved := __reserved => notify;
}
====
@onedrive.file_created, (file_name =~ "ABCDEFGHIJ" || file_name =~ "0123456788") , v_file_name := file_name => @dropbox.create_new_folder, true 
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "ABCDEFGHIJ" || file_name = "9876543210")) , v_file_name := file_name => @dropbox.create_new_folder(folder_name="23456789") ;
}
====
now => @xkcd.random_comic, ((number > 42 && link = "http://www.abc.def"^^tt:url && alt_text = "0123456788") || (number <= 42 && title =~ "23456789" && alt_text =~ "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @github.comment_issue, body =~ v_title 
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 11 && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @github.comment_issue(repo_name=v_title, issue_number=v_number, body=v_title) ;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1462320000000) && sequence > 14 && organizer =~ "0123456789" && location =~ "23456789") || (summary = "ABCDEFGHIJ" && sequence <= 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @icalendar.list_events(), ((end_date = makeDate(1462320000000) || (start_date = makeDate(1487030400000) && description = "9876543210" && location = "0123456788") || (start_date = makeDate(1487030400000) && summary =~ "23456789" && description = "9876543210" && sequence < 14) || description = "12345" || (start_date = makeDate(1462320000000) && summary = "0123456788" && sequence >= 11 && organizer = "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@youtube.source, ((title =~ "12345" && video_url = "http://www.abc.def"^^tt:url) || (channel_id = "9876543210" && channel_title =~ "0123456789" && title = "0123456789") || (channel_id = "9876543210" && channel_title = "ABCDEFGHIJ" && video_id =~ "12345" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @holidays.next_uk_holiday_event, (summary = "23456789" && description =~ "0123456788") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @youtube.source(), ((channel_title = "ABCDEFGHIJ" && title = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @holidays.next_uk_holiday_event(), ((date = makeDate(1487030400000) || description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @uber.price_estimate, ((low_estimate >= 11 && high_estimate > 11 && currency_code =~ "ABCDEFGHIJ" && surge <= 42 && duration = 2h && distance > 42cm) || (end = $context.location.work && low_estimate = 42 && high_estimate = 55 && surge <= 55 && duration >= 30min) || (start = $context.location.work && end = $context.location.current_location && low_estimate <= 55 && high_estimate < 55 && surge <= 14 && duration = 30min && distance > 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), ((away_name = "0123456789" && home_name = "23456789" && away_points < 14 && home_points > 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @uber.price_estimate(start=$context.location.work, end=$context.location.home), (((low_estimate < 55 && surge < 42 && duration < 30min && distance > 1000m) || (start = $context.location.work && end = $context.location.home && low_estimate > 42 && high_estimate > 55 && surge >= 14))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => * => @slack.setPresence, presence = enum(away) 
;;
Main() {
    now => @phone.get_gps(), (((altitude <= 42cm && bearing <= 42 && speed > 25mph) || (altitude > 42cm && speed <= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @slack.setPresence(presence=enum(away)) ;
}
====
* => @instagram.get_pictures, (filter = "9876543210" || (count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (count > 14 && caption =~ "0123456789" && contains(hashtags, "lol"^^tt:hashtag)) || (count <= 7 && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1487030400000) && description = "ABCDEFGHIJ" && sequence > 11) || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && sequence < 42 && organizer =~ "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @instagram.get_pictures(count=v_sequence), (((count > 11 && caption =~ "12345" && link = "http://www.abc.def"^^tt:url && location = $context.location.work) || (count = 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = $context.location.home))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@sportradar.mlb_team, (game_status = enum(halftime) && scheduled_time =~ "12345" && game_inning =~ "9876543210" && away_runs > 55) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => * => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="chc"^^sportradar:mlb_team("Chicago Cubs")), ((watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && scheduled_time = "0123456788" && away_runs = 14 && home_runs <= 42)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @imgflip.generate(template=v_home_name, text_top=v_game_inning, text_bottom=v_away_name), true , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @instagram.get_pictures, ((count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789" && filter = "12345" && contains(hashtags, "funny"^^tt:hashtag)) || (caption = "12345" && link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @light-bulb.alert_long, true 
;;
Main() {
    now => @instagram.get_pictures(count=42), ((count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @light-bulb.alert_long() ;
}
====
@heartrate-monitor.getheartrate, heartrate >= 11 , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) && heartrate >= 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), ((file_name =~ "23456789" && last_modified = makeDate(1487030400000) && file_size >= 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@heartrate-monitor.getheartrate, ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate > 55) || (time = makeDate(1462320000000) && heartrate = 7)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate >= 11) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @xkcd.random_comic, ((title = "9876543210" && alt_text =~ "ABCDEFGHIJ") || (number > 11 && link = "http://www.abc.def"^^tt:url) || (title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.google.com"^^tt:url && user = "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @xkcd.random_comic(), ((number > 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @builtin.cool() ;
}
====
@phdcomics.new_post, (title = "23456789" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456789" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @fitbit.getbody(), ((weight <= 82kg && bmi >= 7 && fat > 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @tumblr-blog.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @fitbit.recordweight, true 
;;
Main() {
    now => @almond_couch_market.search(), (price > 14) , v_title := title, v_price := price, v_contact := contact => @fitbit.recordweight(weight=82kg) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || (title =~ "0123456789" && updated = makeDate(1487030400000)) || updated = makeDate(1487030400000) || title = "12345" || updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @youtube.source(), ((channel_id =~ "12345" && video_id =~ "23456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @wall_street_journal.get_opinions(), (title = "23456789") , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => @thecatapi.get, ((image_id =~ "9876543210" && count = 11) || (image_id = "0123456788" && count >= 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id = "23456789" && count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "12345" && count > 55) || (image_id =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @phone.notify(title="9876543210", message="0123456789") ;
}
====
* => * => @light-bulb.set_power, (power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @light-bulb.set_power(power=enum(on)) ;
}
====
@phdcomics.new_post, ((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "0123456788" || link = "http://www.google.com"^^tt:url || (title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_bike_market.post(title=v_title, info=v_title, poster=v_title, price=7) ;
}
====
* => @ytranslate.translate, (text =~ "0123456788" || (source_language = "zh"^^tt:iso_lang_code("Chinese") && translated_text = "0123456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @jawbone_up.getheartrate(), ((time = makeDate(1487030400000) && heartrate = 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="0123456788"), (translated_text =~ "23456789") , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && sequence <= 42) || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && description =~ "9876543210" && organizer = "23456789") || (sequence <= 7 && location = "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thermostat.set_target_temperature(value=73F) ;
}
====
@fitness-tracker.getbody, ((leanMass <= 42 && bmi >= 7) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 82kg && bmi >= 14) || (weight = 82kg && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => *
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && weight = 82kg && leanMass >= 11 && bodyFat > 14 && bmi <= 11) || (leanMass <= 55 && bodyFat <= 11))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @almond_couch_market.post(title=v_label, contact=v_subject, price=11) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "ABCDEFGHIJ" || file_name =~ "23456789" || file_name =~ "12345") 
;;
Main() {
    now => @youtube.list_subscriptions(), ((title =~ "0123456788" || (channel_id = "0123456788" && description = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id =~ "0123456789" && title =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id = "12345" && description = "12345"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @google_drive.create_new_drive_file(file_name=v_title) ;
}
====
@omlet.incomingmessage, (type = enum(text) || (type = enum(text) && message =~ "0123456789")) , v_feed := feed, v_type := type, v_message := message => notify
;;
Main() {
    @omlet.incomingmessage(), (type = enum(text)) , v_type := type, v_message := message => notify;
}
====
@github.new_milestone, (user = "bob"^^tt:username && description = "ABCDEFGHIJ") , v_user := user, v_description := description, v_time := time => @fitbit.recordweight, weight >= 155lb 
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), (((repo_name =~ "0123456789" && description =~ "0123456788" && time = makeDate(1462320000000)) || (user = "alice"^^tt:username && description =~ "0123456789" && time = makeDate(1462320000000)))) , v_user := user, v_description := description, v_time := time => @fitbit.recordweight(weight=155lb) ;
}
====
* => @youtube.channels_by_category, ((channel_id =~ "0123456788" && title = "9876543210" && description =~ "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || title = "0123456788" || category_id = enum(cooking) || (category_id = enum(diy) && channel_id =~ "0123456788" && description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(music)), ((title =~ "ABCDEFGHIJ" || (channel_id = "12345" && description =~ "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || title = "23456789")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @holidays.next_us_holiday_event, description = "0123456788" , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_us_holiday_event(), (date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => @onedrive.create_file(file_name=v_summary, contents="0123456789") ;
}
====
@builtin.at, (time = makeTime(15,0) || time = makeTime(20,30))  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), ((time = makeTime(7,30) || time = makeTime(15,0) || time = makeTime(15,0)))  => notify;
}
====
@twitter.source, (contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.source(), ((text =~ "0123456788" || (text = "12345" && contains(hashtags, "cat"^^tt:hashtag) && in_reply_to = "alice"^^tt:username && __reserved = true) || (text =~ "0123456789" && in_reply_to = "alice"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @holidays.next_us_holiday_event(), (summary =~ "12345") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @facebook.post_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption = "12345" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @nasa.rover(), (count < 11) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @facebook.post_picture(picture_url=v_picture_url, caption=v_camera_used) ;
}
====
@builtin.timer, interval >= 2h  => notify
;;
Main() {
    @builtin.timer(interval=2h), true  => notify;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), ((value < 82kg || (time = makeDate(1462320000000) && value > 82kg) || value <= 82kg)) , v_time := time, v_value := value => @thermostat.get_temperature(), (value < 73F) , v_value := value => @almond_couch_market.post(title="ABCDEFGHIJ", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @almond_dates.search, (message = "23456789" && poster =~ "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "0123456789" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.search(), ((interest =~ "0123456789" || (interest = "12345" && poster =~ "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@bodytrace.source, weight <= 155lb , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight < 155lb) , v_time := time, v_weight := weight => @security-camera.get_url(), true , v_link := link => notify;
}
====
* => @imgflip.generate, (template = "ABCDEFGHIJ" && name =~ "0123456789") , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @jawbone_up.getheartrate(), (((time = makeDate(1487030400000) && heartrate < 42) || heartrate < 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @imgflip.generate(template="23456789", text_top="ABCDEFGHIJ", text_bottom="0123456788"), ((template =~ "0123456789" && text_bottom = "23456789" && name = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @linkedin.get_profile, ((formatted_name = "0123456789" && industry =~ "ABCDEFGHIJ" && specialties = "9876543210") || (formatted_name = "23456789" && headline = "0123456788" && industry =~ "0123456789") || (formatted_name = "12345" && industry = "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((specialties =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => @google_drive.create_new_drive_file, file_name =~ v_title 
;;
Main() {
    @washington_post.new_article(section=enum(politics)), (((section = enum(world) && title = "ABCDEFGHIJ" && description = "9876543210") || (section = enum(world) && title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || (section = enum(lifestyle) && description =~ "9876543210"))) , v_title := title, v_link := link, v_description := description => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
@rss.new_post, true , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (title =~ "23456789") , v_title := title, v_link := link => @lg_webos_tv.raise_volume() ;
}
====
@thermostat.humidity, (value <= 55 || value >= 7 || value <= 11 || value > 7) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1487030400000) || time = makeDate(1462320000000) || time = makeDate(1487030400000))) , v_time := time, v_value := value => @holidays.next_us_holiday_event(), ((summary =~ "0123456789" || date = makeDate(1487030400000))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @builtin.get_random_between, low > 11 , v_random := random => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @builtin.get_random_between(low=11, high=55), (((low < 42 && high > 14 && random <= 55) || (low <= 7 && high <= 7 && random < 42))) , v_random := random => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @builtin.debug_log, (message = "9876543210" || message =~ "0123456788") 
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "0123456789") || (caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.home))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.debug_log(message="9876543210") ;
}
====
* => @xkcd.get_comic, ((title = "0123456789" && alt_text = "ABCDEFGHIJ") || (number >= 11 && title =~ "12345")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(), (((number > 42 && alt_text = "23456789") || (number <= 7 && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "ABCDEFGHIJ"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @almond_dates.search, true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.color_loop, true 
;;
Main() {
    now => @almond_dates.search(), (((interest =~ "9876543210" && message =~ "23456789" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "9876543210" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "0123456788" && message =~ "0123456788" && poster =~ "23456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.color_loop() ;
}
====
@slack.receive, ((channel = "funny"^^tt:hashtag && message =~ "9876543210") || (timestamp = "0123456789" && message =~ "0123456788") || (timestamp =~ "0123456789" && from = "bob"^^tt:username && message = "23456789") || timestamp = "23456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => @tumblr-blog.post_text, (body = v_timestamp || title =~ "ABCDEFGHIJ") 
;;
Main() {
    @slack.receive(), ((from = "bob"^^tt:username && message =~ "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @uber.price_estimate(start=$context.location.current_location, end=$context.location.work), ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && low_estimate >= 11 && high_estimate >= 14 && surge < 55)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @tumblr-blog.post_text(title=v_currency_code, body=v_uber_type) ;
}
====
* => @linkedin.get_profile, (headline =~ "ABCDEFGHIJ" && industry = "0123456789" && specialties = "23456789") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="9876543210"), (((tournament_search_term = "12345" && tournament_full_name = "ABCDEFGHIJ" && tournament_league_name =~ "0123456788" && away_alias = "0123456789" && home_alias = "0123456788" && away_points <= 14 && home_points <= 7) || (tournament_league_name =~ "23456789" && home_alias = "9876543210" && home_name =~ "0123456789" && away_points < 55))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @linkedin.get_profile(), (((formatted_name = "23456789" && industry =~ "23456789" && contains(positions, "23456789")) || (formatted_name =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "23456789" && contains(positions, "0123456788")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @bluetooth_speaker.play_music, true 
;;
Main() {
    now => @bluetooth_speaker.play_music() ;
}
====
* => @wall_street_journal.get_opinions, (updated = makeDate(1462320000000) || (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @thermostat.humidity(), (time = makeDate(1487030400000)) , v_time := time, v_value := value => @wall_street_journal.get_opinions(), ((title =~ "23456789" || title = "0123456789")) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @twitter.send_direct_message, to = "bob"^^tt:username 
;;
Main() {
    @slack.receive(), ((timestamp = "0123456789" && message =~ "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @twitter.send_direct_message(to=v_message, message=v_message) ;
}
====
* => @dropbox.move, (new_name = "9876543210" || new_name =~ "0123456789") 
;;
Main() {
    @reddit_front_page.newpost(), ((title = "0123456789" && user = "ABCDEFGHIJ" && category =~ "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @dropbox.move(old_name=v_category, new_name=v_category) ;
}
====
* => @fitbit.recordweight, (weight <= 155lb || weight < 155lb || weight > 82kg) 
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "9876543210") , v_file_name := file_name => @fitbit.recordweight(weight=155lb) ;
}
====
* => @twitter.search_by_hashtag, ((count > 11 && text =~ "9876543210" && contains(urls, "http://www.abc.def"^^tt:url)) || (count = 42 && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=42, from="bob"^^tt:username), ((query_hashtag = "funny"^^tt:hashtag && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @tumblr-blog.post_picture(caption=v_text, picture_url=v_text) ;
}
====
* => @weatherapi.current, ((wind_speed <= 5kmph && humidity >= 14 && fog >= 55 && weather =~ "23456789") || (wind_speed < 25mph && humidity <= 11 && fog = 55 && weather = "0123456789") || (location = $context.location.home && temperature <= 22C && wind_speed >= 25mph && humidity < 7 && cloudiness < 42 && fog <= 55 && weather =~ "12345") || (humidity < 42 && cloudiness = 55) || (temperature <= 73F && wind_speed <= 25mph && humidity > 55 && cloudiness >= 55) || (temperature < 73F && humidity < 42 && cloudiness <= 14 && fog < 11 && weather =~ "9876543210") || (temperature = 73F && wind_speed <= 25mph && fog >= 55 && weather = "0123456789") || (temperature <= 22C && wind_speed < 5kmph && humidity >= 42 && fog > 55 && weather = "23456789")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    @phdcomics.new_post(), ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed > 25mph && humidity < 14 && fog <= 42 && weather =~ "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
@icalendar.event_begin, ((summary =~ "ABCDEFGHIJ" && sequence = 11) || (description =~ "23456789" && sequence >= 55)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => @security-camera.set_power, true 
;;
Main() {
    @icalendar.event_begin(), (((summary = "0123456789" && sequence = 14) || (start_date = makeDate(1462320000000) && summary =~ "0123456789" && description = "0123456788" && sequence <= 42))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.get_space_usage(), (((used > 20MB && total = 5KB) || total = 5KB)) , v_used := used, v_total := total => @security-camera.set_power(power=enum(off)) ;
}
====
now => @security-camera.get_snapshot, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
* => @uber.price_estimate, ((uber_type =~ "9876543210" && currency_code = "ABCDEFGHIJ" && surge < 14 && duration >= 30min) || (start = $context.location.work && uber_type = "9876543210" && low_estimate <= 14 && high_estimate <= 55 && currency_code = "12345" && surge <= 42 && duration > 2h)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), (((blog_name =~ "0123456788" && date = makeDate(1462320000000) && title =~ "23456789" && contains(tags, "cat"^^tt:hashtag) && body = "23456789") || (link = "http://www.abc.def"^^tt:url && body =~ "ABCDEFGHIJ"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "12345" && low_estimate < 14 && high_estimate <= 7 && currency_code =~ "0123456788" && surge < 55 && duration <= 30min && distance > 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@phone.receive_sms, body = "0123456789" , v_from := from, v_body := body => *
;;
Main() {
    @phone.receive_sms(), (((from = "+15551234567"^^tt:phone_number && body = "9876543210") || body = "9876543210")) , v_from := from, v_body := body => @slack.updateChannelPurpose(channel="funny"^^tt:hashtag, purpose="ABCDEFGHIJ") ;
}
====
now => @ytranslate.translate, ((text = "12345" && translated_text =~ "0123456788") || (target_language = "it"^^tt:iso_lang_code("Italian") && text = "ABCDEFGHIJ" && translated_text =~ "23456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="0123456788"), true , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @almond_bike_market.detail, id =~ "9876543210"  => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="0123456789"), ((away_alias =~ "9876543210" && away_name = "9876543210" && away_points < 11 && home_points > 42)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @almond_bike_market.detail(id=v_home_name), true  => notify;
}
====
@phdcomics.new_post, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title = "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => @bluetooth_speaker.play_music, true 
;;
Main() {
    @phdcomics.new_post(), (title = "9876543210") , v_title := title, v_link := link, v_picture_url := picture_url => @bluetooth_speaker.play_music() ;
}
====
* => @tumblr-blog.post_text, (title =~ "0123456789" || (title =~ "0123456789" && body =~ "12345")) 
;;
Main() {
    @slack.receive(), (channel = "cat"^^tt:hashtag) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @tumblr-blog.post_text(title=v_timestamp, body=v_timestamp) ;
}
====
now => @phone.get_gps, (location = $context.location.home && altitude > 1000m) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (((altitude > 1000m && bearing < 7 && speed = 5kmph) || (altitude < 1000m && bearing > 11 && speed > 25mph) || (altitude <= 42cm && bearing >= 14))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@youtube.source, ((channel_id =~ "9876543210" && channel_title =~ "12345" && video_id = "12345" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456788" && title =~ "12345" && video_id =~ "12345") || (channel_id =~ "ABCDEFGHIJ" && title =~ "9876543210") || (channel_id =~ "0123456788" && title =~ "23456789") || (channel_title = "12345" && title = "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @car.honk_horn, true 
;;
Main() {
    @youtube.source(), (((channel_id = "0123456788" && video_id = "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || title =~ "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @car.honk_horn() ;
}
====
@security-camera.new_event, ((has_sound = true && has_person = false) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (((has_sound = false && has_motion = false && has_person = false) || has_sound = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @thermostat.set_target_temperature(value=22C) ;
}
====
* => @almond_dates.post, ((message = "12345" && poster = "ABCDEFGHIJ") || (interest = "12345" && message = "23456789") || message = "0123456789") 
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), (((stock_id = "msft"^^tt:stock_id("Microsoft") && ask_price > 42 && bid_price < 42) || (company_name =~ "0123456788" && bid_price = 55))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @almond_dates.post(interest=v_company_name, poster=v_company_name, phone=v_company_name, message="0123456789") ;
}
====
* => @github.comment_issue, (repo_name =~ "0123456789" && issue_number <= 7) 
;;
Main() {
    now => @github.comment_issue(repo_name="ABCDEFGHIJ", issue_number=14, body="23456789") ;
}
====
* => @dropbox.open, (file_name =~ "12345" && url =~ "23456789") , v_url := url => *
;;
Main() {
    now => @dropbox.open(file_name="12345"), ((file_name = "0123456788" || (file_name =~ "9876543210" && url =~ "0123456788"))) , v_url := url => @heatpad.set_power(power=enum(off)) ;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "ABCDEFGHIJ") , v_file_name := file_name => @facebook.post(status="9876543210") ;
}
====
* => @dropbox.list_folder, ((is_folder = false && file_size < 5KB) || (folder_name = "0123456788" && file_name = "0123456789" && file_size = 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @dropbox.list_folder(folder_name=v_file_name), ((file_name = "0123456789" && last_modified = makeDate(1487030400000) && file_size > 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
@slack.receive, (message = "0123456788" || (channel = "lol"^^tt:hashtag && from = "bob"^^tt:username) || timestamp = "23456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((channel = "funny"^^tt:hashtag && message =~ "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
now => @xkcd.random_comic, title = "23456789" , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 55 && title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @slack.send, message = "0123456788" 
;;
Main() {
    now => @slack.send(channel="lol"^^tt:hashtag, message="9876543210") ;
}
====
* => @builtin.thank_you, true 
;;
Main() {
    @sportradar.soccer_us_team(watched_team_alias="tor"^^sportradar:us_soccer_team("Toronto FC")), ((watched_is_home = false && home_name =~ "0123456788" && scheduled_time = makeDate(1462320000000) && home_points = 42 && result = enum(draw))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @builtin.thank_you() ;
}
====
now => @almond_bike_market.search, ((info =~ "9876543210" && price < 42) || (info = "9876543210" && price >= 14))  => *
;;
Main() {
    now => @almond_bike_market.search(info="0123456789", price=42), ((price >= 7 || (info =~ "23456789" && price >= 14)))  => @light-bulb.color_loop() ;
}
====
@github.new_issue, ((repo_name = "23456789" && user = "alice"^^tt:username && number >= 7) || (repo_name =~ "0123456789" && title =~ "23456789" && body =~ "0123456788") || (repo_name = "12345" && number <= 42 && title = "0123456788")) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => notify
;;
Main() {
    @github.new_issue(repo_name="23456789"), ((number > 14 && title = "0123456789")) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => notify;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on)) 
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && description = "0123456789")) , v_date := date, v_summary := summary, v_description := description => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_couch_market.search, price <= 14 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((price >= 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @almond_dates.search, (interest =~ "23456789" && poster = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1462320000000) && place = $context.location.current_location && leanMass >= 7 && bodyFat >= 14 && bmi = 14) || (weight > 155lb && leanMass >= 7 && bmi > 7))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_dates.search(), ((interest = "9876543210" && message = "12345" && poster = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @almond_bike_market.search, (price > 42 || (info = "0123456789" && price = 55) || price > 11)  => notify
;;
Main() {
    now => @almond_bike_market.search(price=7), true  => notify;
}
====
@yahoofinance.stock_quote, (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "0123456788" && ask_price = 7) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name = "0123456789" && bid_price <= 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @uber.price_estimate(start=$context.location.current_location, end=$context.location.work), ((uber_type = "23456789" && low_estimate = 7 && high_estimate >= 11 && currency_code =~ "0123456788" && distance = 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @tumblr-blog.post_picture(caption=v_currency_code, picture_url=v_currency_code) ;
}
====
now => @phone.get_gps, (speed > 5kmph || (altitude < 1000m && bearing >= 14 && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), (((altitude = 42cm && bearing <= 14) || (altitude >= 1000m && bearing <= 42 && speed = 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.call_emergency() ;
}
====
now => * => @linkedin.share, status = "12345" 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.current_location, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = $context.location.current_location && high_estimate > 55 && surge >= 55 && duration <= 2h && distance > 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @linkedin.share(status=v_uber_type) ;
}
====
* => @giphy.get_tag, (tag = "lol"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify
;;
Main() {
    @thermostat.temperature(), ((value >= 22C || value < 73F)) , v_time := time, v_value := value => @giphy.get_tag(tag="funny"^^tt:hashtag), (tag = "cat"^^tt:hashtag) , v_picture_url := picture_url => notify;
}
====
now => * => @slack.send, (message =~ "0123456788" || message = "0123456789") 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @slack.send(channel="lol"^^tt:hashtag, message="0123456788") ;
}
====
now => * => @gmail.send_email, (subject =~ "ABCDEFGHIJ" && message =~ "23456789") 
;;
Main() {
    now => @uber.time_estimate(start=$context.location.work), ((uber_type =~ "12345" || (start = $context.location.home && uber_type =~ "9876543210") || (start = $context.location.home && uber_type =~ "12345" && estimate < 2h) || (start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "0123456789"))) , v_uber_type := uber_type, v_estimate := estimate => @gmail.send_email(subject=v_uber_type, message=v_uber_type, to="alice@gmail.com"^^tt:email_address) ;
}
====
* => @security-camera.set_power, true 
;;
Main() {
    @omlet.newmessage(), ((type = enum(picture) || message =~ "0123456788")) , v_type := type, v_message := message => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @thermostat.set_minmax_temperature, ((low > 22C && high >= 73F) || high <= 22C || high > 73F || low <= 73F || low < 22C) 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.home), ((date = makeDate(1462320000000) && sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => * => @slack.updateChannelPurpose, ((channel = "lol"^^tt:hashtag && purpose =~ "0123456789") || purpose = "23456789") 
;;
Main() {
    now => @giphy.get_tag(tag="funny"^^tt:hashtag), (((tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @slack.updateChannelPurpose(channel="cat"^^tt:hashtag, purpose="0123456789") ;
}
====
now => * => @facebook.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @fitbit.getbody(), ((weight <= 82kg && fat > 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
@security-camera.new_event, (has_sound = true || (start_time = makeDate(1462320000000) && has_sound = false && has_person = true) || (has_motion = false && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1462320000000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @omlet.send_picture(picture_url=v_picture_url, to="bob"^^tt:username, caption="23456789") ;
}
====
@nasa.asteroid, ((estimated_diameter_max >= 1000m && relative_velocity <= 5kmph && orbiting_body = "23456789") || (asteroid_id < 11 && estimated_diameter_min <= 1000m && estimated_diameter_max > 1000m && relative_velocity < 25mph && orbiting_body =~ "12345") || (name = "23456789" && relative_velocity <= 5kmph && orbiting_body =~ "0123456788") || (asteroid_id >= 55 && name =~ "0123456789" && estimated_diameter_max < 42cm && relative_velocity > 25mph && closest_distance_to_earth > 42cm) || (asteroid_id > 55 && name =~ "23456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((name =~ "ABCDEFGHIJ" && estimated_diameter_min >= 1000m && estimated_diameter_max = 1000m && dangerous = true && closest_distance_to_earth < 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_time(), (time = makeDate(1462320000000)) , v_time := time => notify;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1487030400000) && description = "23456789") || summary =~ "23456789") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @builtin.cool() ;
}
====
* => * => @github.add_email, true 
;;
Main() {
    @slack.receive(), (timestamp =~ "0123456788") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @phone.get_gps(), (((altitude = 1000m && bearing > 7) || speed >= 25mph || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && speed < 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
@yahoofinance.stock_quote, ((stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "0123456788" && ask_price > 55) || (ask_price = 11 && bid_price > 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => @onedrive.create_file, (contents =~ "0123456788" || file_name =~ "0123456789" || (file_name =~ "23456789" && contents =~ "ABCDEFGHIJ") || contents =~ "9876543210") 
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name =~ "9876543210" && ask_price <= 55 && bid_price = 42)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @onedrive.create_file(file_name=v_company_name, contents=v_company_name) ;
}
====
@sportradar.ncaambb_team, ((away_name =~ "9876543210" && home_name =~ "ABCDEFGHIJ" && game_status = enum(closed)) || (other_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name =~ "0123456789" && away_points <= 14 && home_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => *
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), ((other_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && watched_is_home = false && away_name = "0123456788" && home_name = "9876543210" && game_status = enum(halftime) && away_points < 11 && home_points >= 55 && result = enum(win))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @bluetooth_speaker.play_music() ;
}
====
now => @youtube.search_videos, ((query = "0123456788" && video_id = "9876543210" && channel_title = "0123456789" && description = "12345" && count < 42) || (query =~ "9876543210" && video_id =~ "12345" && title =~ "9876543210" && count > 7) || (video_id =~ "12345" && channel_id =~ "12345" && title =~ "0123456789" && description = "0123456789")) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="9876543210"), ((thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count = 11)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@activity-tracker.getmove, (day =~ "23456789" && steps = 42 && activeTime < 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => *
;;
Main() {
    @activity-tracker.getmove(), (((inactiveTime > 30min && calories >= 500kcal) || (steps >= 11 && activeTime < 30min && calories >= 500kcal) || (distance > 1000m && activeTime > 2h && inactiveTime < 2h) || (steps < 55 && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_bike_market.ask_seller(id=v_day, property=v_day, value=v_day, public=true), (((property = "9876543210" && public = true) || (id = "12345" && property =~ "0123456788" && value = "0123456789"))) , v_answer := answer => @onedrive.rename_file(old_name=v_answer, new_name="ABCDEFGHIJ") ;
}
====
* => * => @almond_couch_market.post, (title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @linkedin.get_profile(), (industry = "0123456788") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @almond_couch_market.post(title=v_specialties, contact=v_specialties, price=11) ;
}
====
@scale.source, (value > 82kg || value <= 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @fitbit.getbody(), ((weight >= 155lb && bmi > 7 && fat < 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @twitter.my_tweet(), ((contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @heatpad.set_power(power=enum(off)) ;
}
====
* => @gmail.send_email, ((to = "alice@gmail.com"^^tt:email_address && message =~ "23456789") || subject =~ "0123456788") 
;;
Main() {
    @nasa.asteroid(), ((estimated_diameter_min >= 42cm && relative_velocity > 5kmph && closest_distance_to_earth < 1000m && orbiting_body = "9876543210")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @gmail.send_email(subject=v_name, to="bob@stanford.edu"^^tt:email_address, message="0123456789") ;
}
====
now => @linkedin.get_profile, (formatted_name = "0123456788" && headline =~ "12345" && contains(positions, "9876543210")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((industry = "9876543210" && contains(positions, "0123456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@youtube.source, (channel_title = "23456789" && title = "0123456789" && video_id = "23456789" && video_url = "http://www.google.com"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_id =~ "0123456788" && channel_title = "0123456789" && video_id = "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @dropbox.list_folder(folder_name="0123456788"), ((file_name = "0123456789" && file_size = 20MB && full_path = "9876543210")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
now => * => @speaker.set_volume, (percent >= 14 || percent <= 14) 
;;
Main() {
    now => @builtin.get_random(), (number > 11) , v_number := number => @speaker.set_volume(percent=7) ;
}
====
@almond_dates.monitor, poster = "23456789" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (((interest = "ABCDEFGHIJ" && message = "0123456789") || (message =~ "0123456789" && poster =~ "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
now => * => @linkedin.share, (status = "9876543210" || status = "0123456789") 
;;
Main() {
    now => @linkedin.share(status="9876543210") ;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "0123456788" && user =~ "0123456789") || (link = "http://www.abc.def"^^tt:url && category = "12345") || (title = "0123456788" && user = "0123456789" && category =~ "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => @slack.updateChannelPurpose(channel="funny"^^tt:hashtag, purpose="9876543210") ;
}
====
* => * => @light-bulb.set_power, (power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @dropbox.get_space_usage(), ((used < 20MB || (used <= 20MB && total > 20MB))) , v_used := used, v_total := total => @light-bulb.set_power(power=enum(off)) ;
}
====
now => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((title = "12345" && price < 14 && contact = "+16501234567"^^tt:phone_number) || (title =~ "0123456788" && price < 7))) , v_title := title, v_price := price, v_contact := contact => @lg_webos_tv.raise_volume() ;
}
====
@rss.new_post, title =~ "12345" , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (title = "0123456788") , v_title := title, v_link := link => @phone.call(number="+15551234567"^^tt:phone_number) ;
}
====
@fitness-tracker.getbody, ((time = makeDate(1462320000000) && weight < 155lb && leanMass >= 14 && bodyFat >= 55 && bmi < 42) || (place = $context.location.home && weight >= 82kg && leanMass = 11 && bodyFat < 55 && bmi > 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && bodyFat > 7 && bmi < 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @giphy.get_tag, true , v_picture_url := picture_url => notify
;;
Main() {
    @omlet.incomingmessage(), (message = "ABCDEFGHIJ") , v_type := type, v_message := message => @giphy.get_tag(tag="cat"^^tt:hashtag), ((tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
now => @builtin.get_random, true , v_number := number => *
;;
Main() {
    now => @builtin.get_random(), true , v_number := number => @bluetooth_speaker.play_music() ;
}
====
* => @phone.get_gps, ((altitude <= 1000m && bearing >= 55) || (location = $context.location.home && altitude >= 42cm) || (altitude <= 42cm && bearing <= 55 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 1000m && bearing < 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @linkedin.get_profile, ((formatted_name = "12345" && specialties = "23456789") || (formatted_name = "23456789" && headline = "ABCDEFGHIJ" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @almond_dates.post, poster =~ "23456789" 
;;
Main() {
    now => @linkedin.get_profile(), (((industry =~ "0123456788" && specialties = "ABCDEFGHIJ") || (headline =~ "12345" && industry =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "0123456789" && specialties = "0123456789" && contains(positions, "0123456789")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @almond_dates.post(interest=v_headline, message=v_industry, poster=v_industry, phone=v_headline) ;
}
====
* => @almond_couch_market.search, (price >= 42 || (price >= 55 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price >= 42) || (title =~ "23456789" && price = 14)) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @nasa.asteroid(), (((name =~ "0123456789" && relative_velocity < 25mph && closest_distance_to_earth <= 1000m) || (asteroid_id = 42 && name = "ABCDEFGHIJ" && estimated_diameter_min > 1000m && estimated_diameter_max >= 1000m && relative_velocity >= 25mph && closest_distance_to_earth >= 1000m) || closest_distance_to_earth = 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @almond_couch_market.search(), ((title = "0123456788" && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @speaker.set_sink() ;
}
====
now => * => @omlet.send_to, true 
;;
Main() {
    now => @omlet.send_to(to="bob"^^tt:username, message="12345") ;
}
====
now => @nasa.rover, (count < 42 || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (date_taken = makeDate(1487030400000) && count < 11 && camera_used =~ "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1487030400000) && count >= 55)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1462320000000) && description =~ "0123456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((description = "ABCDEFGHIJ" && sequence <= 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @dropbox.create_new_folder, folder_name = "12345" 
;;
Main() {
    now => @dropbox.create_new_folder(folder_name="ABCDEFGHIJ") ;
}
====
now => * => @light-bulb.alert_long, true 
;;
Main() {
    now => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => @light-bulb.alert_long() ;
}
====
* => @phone.get_gps, ((altitude > 1000m && bearing > 14 && speed > 5kmph) || (bearing < 42 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @phone.get_gps(), ((altitude < 1000m && bearing >= 14 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @almond_bike_market.post, (price >= 42 || (title = "12345" && price < 11 && info = "0123456789")) 
;;
Main() {
    now => @almond_bike_market.post(title="23456789", price=14, info="0123456788", poster="0123456788") ;
}
====
now => @dropbox.list_folder, ((folder_name = "0123456789" && file_name = "23456789" && file_size >= 20MB && full_path =~ "0123456788") || file_name = "23456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="12345"), (file_size < 5KB) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @twitter.search, (count < 7 && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search(query="0123456788", count=11), (((query =~ "9876543210" && contains(hashtags, "cat"^^tt:hashtag)) || text = "12345")) , v_text := text, v_hashtags := hashtags, v_urls := urls => @fitbit.recordweight(weight=155lb) ;
}
====
now => * => @speaker.raise_volume, true 
;;
Main() {
    now => @speaker.raise_volume() ;
}
====
* => * => @builtin.thank_you, true 
;;
Main() {
    now => @builtin.thank_you() ;
}
====
@tumblr.new_photo, ((blog_name =~ "9876543210" && caption =~ "9876543210" && link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || date = makeDate(1462320000000)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="9876543210"), ((link = "http://www.google.com"^^tt:url || (blog_name = "0123456789" && date = makeDate(1487030400000) && contains(tags, "funny"^^tt:hashtag)) || (blog_name = "9876543210" && date = makeDate(1462320000000) && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (date = makeDate(1462320000000) && caption =~ "ABCDEFGHIJ" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (caption =~ "ABCDEFGHIJ" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @twitter.search(query=v_caption, from=v_caption), ((count = 7 && contains(urls, "http://www.abc.def"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@omlet.incomingmessage, ((type = enum(text) && message = "0123456789") || message =~ "0123456789") , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), (message =~ "23456789") , v_type := type, v_message := message => notify;
}
====
@phone.gps, ((altitude > 42cm && speed < 5kmph) || speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), (((altitude <= 1000m && bearing >= 7) || (altitude < 1000m && bearing < 14 && speed > 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="12345"), ((source_language = "it"^^tt:iso_lang_code("Italian") && translated_text = "12345")) , v_source_language := source_language, v_translated_text := translated_text => @onedrive.delete_file(file_name="0123456788") ;
}
====
@instagram.new_picture, location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((caption = "23456789" && link = "http://www.google.com"^^tt:url && contains(hashtags, "cat"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456789") || caption = "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @almond_couch_market.post, (price > 14 || contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_couch_market.post(title=v_message, contact=v_message, price=14) ;
}
====
@almond_dates.monitor, (message =~ "23456789" && poster =~ "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (poster = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @instagram.get_pictures(), (((count > 7 && caption =~ "0123456788" && link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && contains(hashtags, "funny"^^tt:hashtag)) || (count < 7 && caption =~ "9876543210" && location = $context.location.home) || (count > 42 && caption = "0123456789" && filter =~ "23456789"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@twitter.source, (contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.source(), (((contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)) || (text =~ "0123456789" && contains(hashtags, "funny"^^tt:hashtag)) || (contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username && __reserved = false) || (contains(hashtags, "funny"^^tt:hashtag) && from = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @uber.time_estimate(start=$context.location.current_location), ((uber_type = "ABCDEFGHIJ" || (start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "ABCDEFGHIJ" && estimate > 2h) || (uber_type =~ "0123456789" && estimate > 30min) || (uber_type = "23456789" && estimate < 2h))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => @icalendar.list_events, end_date = makeDate(1462320000000) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @speaker.play_music, true 
;;
Main() {
    now => @icalendar.list_events(), ((summary = "23456789" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @speaker.play_music() ;
}
====
@yahoofinance.stock_quote, bid_price <= 42 , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), (((company_name = "9876543210" && ask_price >= 11 && bid_price >= 55) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "ABCDEFGHIJ" && ask_price >= 14 && bid_price > 42) || (ask_price <= 14 && bid_price = 14))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @dropbox.open(file_name="12345"), true , v_url := url => notify;
}
====
@fitness-tracker.getbody, ((weight >= 82kg && leanMass = 42 && bmi <= 7) || (time = makeDate(1462320000000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 155lb && bodyFat < 11 && bmi <= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((weight >= 82kg && leanMass < 42 && bodyFat <= 7 && bmi = 42) || (place = $context.location.current_location && weight > 82kg && bodyFat <= 11 && bmi <= 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @instagram.get_pictures(count=v_leanMass), ((count >= 55 && filter = "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@yahoofinance.stock_quote, (stock_id = "aapl"^^tt:stock_id("Apple") && ask_price > 14 && bid_price = 11) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && company_name =~ "9876543210")) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
now => @fitbit.recordweight, (weight > 155lb || weight > 155lb || weight >= 82kg || weight >= 82kg || weight >= 82kg) 
;;
Main() {
    now => @fitbit.recordweight(weight=82kg) ;
}
====
* => @almond_dates.search, interest =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (interest =~ "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @giphy.get, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @almond_bike_market.post, price < 11 
;;
Main() {
    now => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @almond_bike_market.post(title="0123456789", price=11, info="23456789", poster="12345") ;
}
====
now => @phone.get_gps, ((altitude > 42cm && bearing >= 11) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude > 1000m && speed <= 5kmph) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude > 1000m && bearing = 55 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (((location = $context.location.home && bearing >= 7) || altitude = 1000m)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@github.new_issue, ((repo_name = "12345" && user = "alice"^^tt:username && title = "12345" && body =~ "9876543210") || (number < 14 && title =~ "12345" && body =~ "9876543210") || (repo_name = "23456789" && body =~ "23456789") || (repo_name = "0123456788" && number = 11) || (repo_name = "23456789" && user = "alice"^^tt:username && number > 55 && title = "9876543210" && body = "12345")) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => * => notify
;;
Main() {
    @github.new_issue(repo_name="0123456788"), (((user = "bob"^^tt:username && number < 11 && body = "0123456789") || (repo_name =~ "0123456788" && user = "bob"^^tt:username && number >= 14))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
now => * => @twitter.post_picture, true 
;;
Main() {
    now => @thecatapi.get(), ((count < 55 || link = "http://www.google.com"^^tt:url || (count < 42 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @twitter.post_picture(picture_url=v_image_id, caption="12345") ;
}
====
@almond_couch_market.monitor, price <= 7 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @bluetooth_speaker.set_sink() ;
}
====
@fitness-tracker.getbody, ((leanMass <= 42 && bmi >= 7) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 82kg && bmi >= 14) || (weight = 82kg && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => *
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && leanMass < 55 && bodyFat > 11 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @builtin.debug_log(message="12345") ;
}
====
@omlet.newmessage, message = "ABCDEFGHIJ" , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => notify;
}
====
* => @youtube.search_channel, ((query = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title =~ "9876543210" && description = "23456789") || (query = "0123456789" && title = "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.search_channel(query="0123456789"), ((title =~ "23456789" && description =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @bing.web_search, title = "12345" , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    now => @bing.web_search(query="0123456789"), (title =~ "0123456788") , v_title := title, v_description := description, v_link := link => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && weight = 155lb && leanMass > 14 && bodyFat < 11) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && weight <= 82kg && leanMass >= 11 && bodyFat < 42 && bmi <= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@almond_couch_market.monitor, price > 42 , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 55 || contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "23456789" && filter = "12345") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((caption =~ "9876543210" && filter = "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) || (link = "http://www.google.com"^^tt:url && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || filter =~ "9876543210" || location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @linkedin.get_profile(), ((formatted_name =~ "0123456788" && headline = "0123456789" && industry =~ "0123456788" && contains(positions, "0123456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @slack.updateChannelPurpose, ((channel = "cat"^^tt:hashtag && purpose =~ "ABCDEFGHIJ") || (channel = "cat"^^tt:hashtag && purpose =~ "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && purpose = "ABCDEFGHIJ")) 
;;
Main() {
    now => @slack.updateChannelPurpose(channel="cat"^^tt:hashtag, purpose="12345") ;
}
====
@almond_dates.monitor, ((interest = "0123456789" && message =~ "12345" && phone = "+16501234567"^^tt:phone_number) || (interest = "12345" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "12345" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @wall_street_journal.get_opinions(), ((title = "23456789" && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), ((interest = "0123456788" && message =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @light-bulb.color_loop() ;
}
====
now => * => @dropbox.create_new_folder, (folder_name =~ "9876543210" || folder_name = "ABCDEFGHIJ") 
;;
Main() {
    now => @youtube.search_channel(query="ABCDEFGHIJ"), (((channel_id =~ "ABCDEFGHIJ" && title =~ "9876543210" && description = "12345") || (query =~ "12345" && title = "ABCDEFGHIJ" && description = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @dropbox.create_new_folder(folder_name=v_channel_id) ;
}
====
@holidays.uk_holiday_event, (date = makeDate(1487030400000) && summary = "12345") , v_date := date, v_summary := summary, v_description := description => @dropbox.get_space_usage, (used < 20MB && total >= 5KB) , v_used := used, v_total := total => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @dropbox.get_space_usage(), ((used <= 20MB && total > 20MB)) , v_used := used, v_total := total => notify;
}
====
* => @onedrive.create_file, true 
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value < 155lb)) , v_time := time, v_value := value => @onedrive.create_file(file_name="0123456788", contents="9876543210") ;
}
====
* => @nasa.apod, (title = "0123456789" || (title = "0123456789" && description = "23456789")) , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @xkcd.new_comic(), (title =~ "0123456788") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @nasa.apod(), (title =~ "0123456788") , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@github.new_issue_comment, (repo_name =~ "9876543210" && issue_number < 14 && body = "9876543210" && time = makeDate(1462320000000)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => notify
;;
Main() {
    @github.new_issue_comment(repo_name="0123456789"), (((repo_name =~ "12345" && issue_number <= 42 && body =~ "12345") || (body = "ABCDEFGHIJ" && time = makeDate(1487030400000)) || (issue_number = 11 && body = "ABCDEFGHIJ") || repo_name = "12345" || time = makeDate(1462320000000) || (repo_name = "0123456789" && issue_number >= 7) || body =~ "23456789" || (repo_name =~ "23456789" && issue_number <= 14 && body = "0123456789"))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @thermostat.get_hvac_state(), (mode = enum(cool)) , v_mode := mode, v_state := state => notify;
}
====
* => * => @fitbit.recordweight, weight > 155lb 
;;
Main() {
    @scale.source(), (value < 82kg) , v_time := time, v_value := value => @phone.get_gps(), (((bearing > 11 && speed = 25mph) || (bearing <= 7 && speed <= 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight(weight=82kg) ;
}
====
@almond_couch_market.monitor, (title =~ "12345" && price = 11 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "23456789" || price <= 7 || price < 11)) , v_title := title, v_price := price, v_contact := contact => @twitter.unfollow(user_name=v_title) ;
}
====
@weatherapi.monitor, (wind_speed >= 5kmph && humidity = 14 && cloudiness <= 11 && fog > 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((temperature < 22C && wind_speed <= 25mph && humidity < 11 && fog < 55 && status = enum(cloudy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @facebook.post_picture(picture_url=v_weather, caption=v_weather) ;
}
====
@thermostat.temperature, value < 22C , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), (((time = makeDate(1487030400000) && value >= 22C) || value >= 22C)) , v_time := time, v_value := value => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=$context.location.current_location), ((uber_type =~ "ABCDEFGHIJ" && low_estimate = 14 && currency_code =~ "23456789" && duration > 2h && distance < 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@gmail.receive_email, ((from_name = "23456789" && subject = "0123456788" && date = makeDate(1487030400000) && contains(labels, "0123456789") && snippet =~ "0123456789") || (contains(labels, "9876543210") && snippet =~ "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), ((subject =~ "9876543210" || (from_address = "alice@gmail.com"^^tt:email_address && subject = "9876543210" && contains(labels, "0123456788") && snippet =~ "ABCDEFGHIJ") || (from_name =~ "23456789" && date = makeDate(1487030400000) && contains(labels, "ABCDEFGHIJ") && snippet = "9876543210"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.get_random_between(low=55, high=11), ((low > 55 && high > 7 && random = 14)) , v_random := random => notify;
}
====
@icalendar.event_begin, ((summary =~ "0123456789" && description = "ABCDEFGHIJ" && sequence >= 55) || (sequence > 11 && organizer = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && organizer =~ "12345" && location =~ "ABCDEFGHIJ") || (sequence < 7 && location = "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @instagram.get_pictures, ((count >= 11 && contains(hashtags, "funny"^^tt:hashtag)) || (count = 55 && caption = "0123456788" && filter = "0123456789" && location = $context.location.home) || (count = 42 && contains(hashtags, "lol"^^tt:hashtag)) || (count > 14 && caption = "9876543210" && link = "http://www.abc.def"^^tt:url && filter =~ "12345" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @heatpad.set_power, true 
;;
Main() {
    now => @instagram.get_pictures(count=11), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788" && filter = "0123456788" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @heatpad.set_power(power=enum(on)) ;
}
====
* => @dropbox.move, (new_name = "9876543210" || new_name =~ "0123456789") 
;;
Main() {
    @twitter.direct_message(), (((from = "bob"^^tt:username && message =~ "ABCDEFGHIJ" && __reserved = false) || (from = "alice"^^tt:username && __reserved = true))) , v_from := from, v_message := message, v___reserved := __reserved => @dropbox.move(old_name=v_message, new_name=v_message) ;
}
====
@weatherapi.monitor, ((location = $context.location.home && wind_speed <= 25mph && humidity >= 55 && cloudiness >= 14 && fog = 42 && weather = "9876543210") || (temperature < 73F && wind_speed < 5kmph && humidity <= 14 && fog > 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((temperature >= 22C && wind_speed < 25mph && humidity > 7 && fog < 11 && weather =~ "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_dates.search(), ((interest = "0123456788" && message = "ABCDEFGHIJ" && poster = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@instagram.new_picture, caption =~ "0123456788" , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_couch_market.search(), (((price < 7 && contact = "+16501234567"^^tt:phone_number) || (title = "9876543210" && price <= 11 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@youtube.source, (channel_title =~ "23456789" && title =~ "9876543210" && video_id = "9876543210" && video_url = "http://www.google.com"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => *
;;
Main() {
    @youtube.source(), ((channel_id =~ "0123456789" && channel_title =~ "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @phone.set_ringer(mode=enum(normal)) ;
}
====
* => @phone.call, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 14 && estimated_diameter_min < 1000m && estimated_diameter_max > 1000m && relative_velocity >= 5kmph && closest_distance_to_earth < 1000m && orbiting_body = "0123456789")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.call(number="+15551234567"^^tt:phone_number) ;
}
====
@fitness-tracker.getbody, ((leanMass <= 42 && bmi >= 7) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 82kg && bmi >= 14) || (weight = 82kg && bodyFat > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => *
;;
Main() {
    @fitness-tracker.getbody(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && weight <= 155lb && bodyFat <= 14 && bmi <= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @slack.updateChannelTopic(channel="funny"^^tt:hashtag, topic="9876543210") ;
}
====
* => @fitbit.recordweight, weight >= 82kg 
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="23456789"), (((tournament_search_term =~ "0123456788" && away_alias =~ "23456789" && home_alias =~ "ABCDEFGHIJ" && home_name = "ABCDEFGHIJ" && game_status = enum(inprogress) && home_points > 11) || (tournament_search_term = "0123456789" && tournament_league_name =~ "0123456788" && away_alias =~ "0123456789" && home_name = "ABCDEFGHIJ" && away_points < 14 && home_points <= 7) || (tournament_search_term = "23456789" && tournament_league_name = "12345" && home_alias = "12345" && away_name =~ "23456789" && home_name = "0123456788" && game_status = enum(inprogress) && away_points <= 55 && home_points < 7))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @fitbit.recordweight(weight=155lb) ;
}
====
@tumblr.new_photo, ((blog_name =~ "9876543210" && caption =~ "9876543210" && link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || date = makeDate(1462320000000)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => * => notify
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), ((blog_name =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => notify;
}
====
@youtube.source, title =~ "9876543210" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), true , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @youtube.list_subscriptions(), ((channel_id = "9876543210" && title =~ "12345")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@holidays.us_holiday_event, summary = "12345" , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.us_holiday_event(), ((summary =~ "23456789" && description = "0123456789")) , v_date := date, v_summary := summary, v_description := description => @speaker.raise_volume() ;
}
====
now => @wall_street_journal.get_opinions, (link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((updated = makeDate(1487030400000) || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @lg_webos_tv.set_volume(percent=14) ;
}
====
@rss.new_post, true , v_title := title, v_link := link => notify
;;
Main() {
    @rss.new_post(), (((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || (title = "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => notify;
}
====
now => * => @facebook.post_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption = "12345" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA"), end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((low_estimate > 7 && high_estimate < 7 && surge > 11 && duration >= 30min)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @facebook.post_picture(picture_url=v_currency_code, caption=v_currency_code) ;
}
====
now => * => @almond_couch_market.post, ((title = "0123456789" && price >= 55) || price <= 11 || (title = "9876543210" && price = 55 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @almond_bike_market.ask_seller(id="0123456789", property="0123456789", value="9876543210", public=true), ((property = "0123456788" && value =~ "0123456788")) , v_answer := answer => @almond_couch_market.post(title=v_answer, price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight > 82kg) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight > 155lb)) , v_time := time, v_weight := weight => @icalendar.list_events(), (summary =~ "0123456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @xkcd.random_comic, ((link = "http://www.google.com"^^tt:url && alt_text =~ "0123456789") || number > 42) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number < 42 && title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@youtube.source, ((channel_id =~ "23456789" && channel_title =~ "23456789" && title =~ "9876543210" && video_id = "9876543210") || (channel_title =~ "9876543210" && title =~ "0123456789") || (channel_id =~ "23456789" && title =~ "9876543210" && video_id = "0123456789")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), true , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thermostat.get_humidity(), (value >= 42) , v_value := value => notify;
}
====
now => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.current_location), (phase =~ "0123456788") , v_date := date, v_phase := phase => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link, v_updated := updated => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_motion = true) || (start_time = makeDate(1462320000000) && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => @facebook.post, (status =~ "9876543210" || status = "0123456789" || status =~ "0123456788") 
;;
Main() {
    @security-camera.new_event(), ((has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.search(), (((price >= 11 && contact = "+16501234567"^^tt:phone_number) || price >= 55)) , v_title := title, v_price := price, v_contact := contact => @facebook.post(status="12345") ;
}
====
@onedrive.file_modified, file_name =~ "23456789" , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "0123456788") , v_file_name := file_name => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
* => @facebook.post_picture, (caption = "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || caption =~ "12345") 
;;
Main() {
    @github.new_commit(repo_name="ABCDEFGHIJ"), true , v_user := user, v_message := message, v_time := time => @facebook.post_picture(picture_url=v_message, caption=v_message) ;
}
====
now => @linkedin.get_profile, ((industry = "12345" && specialties = "23456789" && contains(positions, "9876543210")) || (formatted_name = "12345" && specialties = "0123456788" && contains(positions, "0123456788") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || formatted_name =~ "ABCDEFGHIJ" || industry =~ "23456789") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name = "ABCDEFGHIJ" && industry = "0123456788" && specialties =~ "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @twitter.follow(user_name="bob"^^tt:username) ;
}
====
@tumblr.new_text, true , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), (((blog_name = "9876543210" && body = "ABCDEFGHIJ") || (date = makeDate(1487030400000) && contains(tags, "funny"^^tt:hashtag)) || (title =~ "23456789" && contains(tags, "funny"^^tt:hashtag)))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @instagram.get_pictures(), (((count > 14 && caption =~ "23456789" && link = "http://www.google.com"^^tt:url) || (count < 7 && link = "http://www.abc.def"^^tt:url && filter =~ "0123456789" && location = $context.location.current_location))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => @thecatapi.get, ((image_id = "0123456788" && count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (image_id = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "0123456788" && count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @onedrive.rename_file(old_name=v_image_id, new_name=v_image_id) ;
}
====
@washington_post.new_blog_post, ((title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || title = "9876543210") , v_title := title, v_link := link => * => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(world_views)), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => @bing.image_search(query=v_title), ((height < 11 || width >= 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @tumblr-blog.post_text, body = "ABCDEFGHIJ" 
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && duration <= 2h && rem < 30min && light > 2h && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @tumblr-blog.post_text(title="12345", body="0123456789") ;
}
====
@twitter.direct_message, from = "alice"^^tt:username , v_from := from, v_message := message, v___reserved := __reserved => * => @phone.set_ringer, (mode = enum(normal) || mode = enum(normal) || mode = enum(silent)) 
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @phone.set_ringer(mode=enum(vibrate)) ;
}
====
* => @holidays.next_us_holiday_event, (description =~ "12345" || summary =~ "9876543210" || (date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description = "12345")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @reddit_front_page.newpost(), (category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => @holidays.next_us_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "9876543210")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @holidays.next_us_holiday_event, (description =~ "23456789" || (date = makeDate(1462320000000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ") || (summary =~ "0123456788" && description = "0123456788") || description =~ "12345" || (summary =~ "0123456789" && description = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1487030400000) && weight <= 82kg) || weight <= 155lb || (time = makeDate(1487030400000) && weight < 82kg))) , v_time := time, v_weight := weight => @holidays.next_us_holiday_event(), ((summary = "ABCDEFGHIJ" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @slack.send, message =~ "12345" 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.current_location), (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @slack.send(channel="lol"^^tt:hashtag, message="0123456789") ;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), ((title = "0123456788" || (title =~ "12345" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_description := description => @dropbox.create_new_folder(folder_name=v_description) ;
}
====
* => @thermostat.get_humidity, (value <= 7 || value > 14 || value < 7) , v_value := value => notify
;;
Main() {
    @almond_bike_market.monitor(), (price > 7)  => @thermostat.get_humidity(), ((value = 14 || value >= 11)) , v_value := value => notify;
}
====
@almond_couch_market.monitor, ((title = "0123456788" && contact = "+16501234567"^^tt:phone_number) || (title =~ "ABCDEFGHIJ" && contact = "+16501234567"^^tt:phone_number) || (price < 42 && contact = "+15551234567"^^tt:phone_number) || price <= 14 || (title =~ "23456789" && price >= 42) || title =~ "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price > 11 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @holidays.next_us_holiday_event(), (((summary = "9876543210" && description = "12345") || (date = makeDate(1462320000000) && summary = "12345"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @almond_couch_market.search, (title =~ "9876543210" && price > 55 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (title = "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@rss.new_post, (title = "0123456788" || title = "ABCDEFGHIJ" || title = "9876543210") , v_title := title, v_link := link => @linkedin.get_profile, ((industry =~ v_title && specialties =~ "0123456789") || (formatted_name =~ "9876543210" && headline =~ "12345" && contains(positions, "ABCDEFGHIJ")) || (formatted_name =~ "23456789" && industry =~ "9876543210" && specialties = "ABCDEFGHIJ") || (industry = "0123456788" && contains(positions, v_title))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @linkedin.get_profile(), (((industry = "0123456788" && specialties = "ABCDEFGHIJ") || (formatted_name =~ "23456789" && headline =~ "0123456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
@activity-tracker.getmove, (day = "ABCDEFGHIJ" && steps < 14 && activeTime < 30min && calories > 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && distance = 1000m && steps < 14 && activeTime = 30min && inactiveTime >= 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary = "0123456789" && location = "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "0123456788" && description =~ "12345" && sequence <= 42 && organizer = "ABCDEFGHIJ") || organizer =~ "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((summary =~ "12345" && description =~ "9876543210" && sequence = 7 && location = "12345") || (start_date = makeDate(1462320000000) && description =~ "23456789" && sequence < 7 && organizer = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @gmail.send_picture(subject=v_summary, picture_url=v_description, to="alice@gmail.com"^^tt:email_address, message="23456789") ;
}
====
@almond_dates.monitor, (message =~ "12345" || interest = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((poster =~ "0123456788" || message = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
* => @almond_dates.search, (poster = "9876543210" || (interest =~ "0123456789" && message = "0123456788" && poster =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (interest = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @nasa.rover, date_taken = makeDate(1462320000000) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1487030400000) && camera_used = "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @phone.set_ringer, (mode = enum(silent) || mode = enum(vibrate)) 
;;
Main() {
    now => @phone.set_ringer(mode=enum(normal)) ;
}
====
@slack.receive, (from = "bob"^^tt:username && message =~ "12345") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), ((timestamp =~ "9876543210" && message = "12345")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
now => @imgflip.generate, (template =~ "23456789" && text_top = "12345" && text_bottom = "0123456788") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="0123456788", text_bottom="0123456789"), ((template = "23456789" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @almond_couch_market.monitor(), (price > 11) , v_title := title, v_price := price, v_contact := contact => @xkcd.get_comic(), ((number > 14 && title = "0123456789" && link = "http://www.google.com"^^tt:url && alt_text =~ "0123456788")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @security-camera.set_power(power=enum(off)) ;
}
====
* => @nasa.apod, (title = "0123456789" || (title = "0123456789" && description = "23456789")) , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @almond_dates.monitor(), ((message =~ "23456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @nasa.apod(), ((title =~ "ABCDEFGHIJ" && description =~ "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @tumblr-blog.post_picture, (caption = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @thermostat.temperature(), (value = 73F) , v_time := time, v_value := value => @tumblr-blog.post_picture(caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @builtin.cool, true 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((company_name =~ "9876543210" && div < 7) || (yield <= 11 && div >= 42) || (yield <= 7 && div > 11 && ex_div_date = makeDate(1462320000000)) || company_name =~ "0123456789")) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @builtin.cool() ;
}
====
@google_drive.new_drive_file, file_name = "23456789" , v_file_name := file_name => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "ABCDEFGHIJ" || file_name =~ "0123456788")) , v_file_name := file_name => notify;
}
====
* => @fitbit.getbody, (weight > 155lb && bmi <= 42) , v_weight := weight, v_bmi := bmi, v_fat := fat => @security-camera.set_power, true 
;;
Main() {
    now => @fitbit.getbody(), (((weight <= 82kg && bmi > 55 && fat < 42) || bmi = 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @security-camera.set_power(power=enum(off)) ;
}
====
* => @almond_bike_market.post, ((title =~ "9876543210" && price > 11 && poster = "0123456788") || (title = "23456789" && price = 42 && info = "ABCDEFGHIJ" && poster = "0123456789") || (title = "9876543210" && info = "0123456788" && poster = "23456789") || (title = "12345" && price = 42 && poster =~ "9876543210")) 
;;
Main() {
    @omlet.newmessage(), ((type = enum(text) && message =~ "0123456788")) , v_type := type, v_message := message => @almond_bike_market.post(title=v_message, poster=v_message, price=42, info="0123456789") ;
}
====
* => @almond_couch_market.search, ((title =~ "0123456789" && price < 7 && contact = "+16501234567"^^tt:phone_number) || title =~ "23456789" || (title = "9876543210" && price < 11)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @jawbone_up.getmove(), ((updateTime = makeDate(1487030400000) && day =~ "23456789" && distance >= 42cm && inactiveTime = 30min && calories > 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_couch_market.search(), (title = "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @almond_couch_market.post, ((price = 7 && contact = "+15551234567"^^tt:phone_number) || (title =~ "0123456789" && price > 11)) 
;;
Main() {
    now => @dropbox.get_space_usage(), ((used > 5KB && total <= 5KB)) , v_used := used, v_total := total => @almond_couch_market.post(title="23456789", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@thermostat.temperature, value > 22C , v_time := time, v_value := value => * => *
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => notify;
}
====
@washington_post.new_blog_post, true , v_title := title, v_link := link => @almond_couch_market.search, (title = "12345" || contact = "+16501234567"^^tt:phone_number || contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(politics)), (((section = enum(fact_checker) && title =~ "0123456789") || (section = enum(the_plum_line) && title = "23456789") || (section = enum(world_views) && title =~ "0123456789"))) , v_title := title, v_link := link => @almond_couch_market.search(), (((title =~ "12345" && price >= 42) || title = "23456789" || (title =~ "ABCDEFGHIJ" && price > 7))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @thecatapi.get, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (((image_id =~ "0123456789" && count >= 42 && link = "http://www.google.com"^^tt:url) || (image_id = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count >= 42 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @ytranslate.translate, ((source_language = "zh"^^tt:iso_lang_code("Chinese") && text = "9876543210") || translated_text = "0123456788") , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="zh"^^tt:iso_lang_code("Chinese"), text="0123456788"), (((target_language = "en"^^tt:iso_lang_code("English") && text =~ "0123456788" && translated_text = "9876543210") || translated_text =~ "12345" || translated_text = "0123456789" || source_language = "en"^^tt:iso_lang_code("English"))) , v_source_language := source_language, v_translated_text := translated_text => @car.honk_horn() ;
}
====
@security-camera.new_event, start_time = makeDate(1487030400000) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (has_person = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_couch_market.search, (title = "ABCDEFGHIJ" && price < 42) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title = "12345" && price <= 42)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@twitter.my_tweet, text =~ "9876543210" , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify
;;
Main() {
    @twitter.my_tweet(), (((text = "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username) || contains(hashtags, "cat"^^tt:hashtag) || contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => notify;
}
====
now => * => @onedrive.delete_file, true 
;;
Main() {
    now => @dropbox.open(file_name="0123456789"), (url =~ "23456789") , v_url := url => @onedrive.delete_file(file_name=v_url) ;
}
====
* => @dropbox.open, (url = "0123456788" || file_name =~ "ABCDEFGHIJ" || url =~ "0123456789") , v_url := url => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="23456789"), (((tournament_full_name =~ "9876543210" && away_alias = "12345" && away_name = "23456789" && away_points <= 11) || (tournament_search_term = "9876543210" && tournament_full_name =~ "ABCDEFGHIJ" && away_name =~ "12345" && away_points >= 42 && home_points = 42))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @dropbox.open(file_name=v_scheduled_time), true , v_url := url => notify;
}
====
* => @slack.send_picture, true 
;;
Main() {
    @bodytrace.source(), (weight >= 82kg) , v_time := time, v_weight := weight => @slack.send_picture(channel="funny"^^tt:hashtag, caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @slack.updateChannelTopic, ((channel = "cat"^^tt:hashtag && topic =~ "0123456788") || topic = "0123456788" || topic = "ABCDEFGHIJ" || (channel = "lol"^^tt:hashtag && topic =~ "0123456788")) 
;;
Main() {
    @twitter.my_tweet(), (text = "0123456788") , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @slack.updateChannelTopic(channel="funny"^^tt:hashtag, topic="0123456789") ;
}
====
now => * => @tumblr-blog.post_picture, (caption =~ "12345" || caption = "12345" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    now => @nasa.rover(), ((count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @tumblr-blog.post_picture(caption="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@jawbone_up.getmove, (distance <= 42cm && steps >= 14 && activeTime <= 2h && inactiveTime >= 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @jawbone_up.getmove(), (((day =~ "9876543210" && distance <= 1000m && activeTime < 30min) || (updateTime = makeDate(1462320000000) && distance <= 42cm && steps < 42 && activeTime > 30min && calories = 500kcal) || (day =~ "ABCDEFGHIJ" && steps < 7 && inactiveTime > 30min && calories <= 500kcal) || (day = "0123456789" && distance <= 1000m && activeTime >= 30min) || (distance < 1000m && steps >= 7 && inactiveTime = 30min && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
now => * => @twitter.post_picture, caption = "23456789" 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title = "9876543210") , v_title := title, v_link := link, v_updated := updated => @twitter.post_picture(caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @instagram.get_pictures, (count > 11 && caption =~ "23456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=14), ((count = 14 && link = "http://www.google.com"^^tt:url && filter =~ "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @xkcd.get_comic, (number > 11 && title =~ "ABCDEFGHIJ") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @twitter.source(), (((text =~ "12345" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "alice"^^tt:username && __reserved = false) || contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @xkcd.get_comic(), (alt_text =~ "9876543210") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@instagram.new_picture, ((caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter =~ "0123456789" && contains(hashtags, "cat"^^tt:hashtag)) || (caption =~ "9876543210" && filter =~ "12345") || (caption = "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && filter =~ "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @icalendar.list_events(), (((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && summary = "23456789" && organizer =~ "12345") || (summary =~ "9876543210" && description = "23456789" && sequence > 11))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @gmail.send_email, (subject = "23456789" && message = "ABCDEFGHIJ") 
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="12345") ;
}
====
* => @gmail.get_latest, (subject = "0123456788" || label = "9876543210") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @gmail.receive_primary_email(), (((from_address = "alice@gmail.com"^^tt:email_address && subject = "ABCDEFGHIJ") || (from_address = "alice@gmail.com"^^tt:email_address && contains(labels, "0123456788")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && subject = "9876543210")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @slack.updateChannelTopic, ((channel = "cat"^^tt:hashtag && topic =~ "0123456788") || topic = "0123456788" || topic = "ABCDEFGHIJ" || (channel = "lol"^^tt:hashtag && topic =~ "0123456788")) 
;;
Main() {
    @jawbone_up.getheartrate(), (heartrate <= 11) , v_time := time, v_place := place, v_heartrate := heartrate => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="12345") ;
}
====
@thermostat.temperature, (value >= 22C || value <= 22C) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => @youtube.search_videos(query="0123456789"), ((query = "9876543210" && channel_id =~ "ABCDEFGHIJ" && channel_title = "9876543210" && title = "ABCDEFGHIJ" && description =~ "9876543210")) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => @gmail.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (message =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @uber.time_estimate, (uber_type = "0123456788" && estimate < 2h) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    now => @uber.time_estimate(start=$context.location.current_location), ((estimate > 30min || (uber_type = "9876543210" && estimate > 30min))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => @thecatapi.get, (image_id = "0123456788" && count > 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @thecatapi.get(), ((image_id = "12345" && count <= 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@fitness-tracker.getbody, ((time = makeDate(1487030400000) && weight >= 155lb) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && leanMass > 14 && bmi <= 42) || (leanMass >= 11 && bmi < 14) || (leanMass > 55 && bodyFat = 55 && bmi < 55) || (leanMass >= 7 && bodyFat > 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight < 82kg && leanMass < 55 && bodyFat > 7 && bmi < 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@rss.new_post, title = "0123456788" , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || link = "http://www.abc.def"^^tt:url || title =~ "23456789" || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @github.add_email, true 
;;
Main() {
    @jawbone_up.getsleep(), ((rem = 30min && deep <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
now => * => @almond_couch_market.post, (price < 14 || (price > 11 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary =~ "0123456788" || (summary = "0123456788" && description =~ "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => @almond_couch_market.post(contact=v_description, title="12345", price=14) ;
}
====
* => @dropbox.create_new_folder, folder_name = "9876543210" 
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @dropbox.create_new_folder(folder_name=v_title) ;
}
====
now => @linkedin.get_profile, (industry =~ "0123456788" || industry =~ "0123456789" || (formatted_name =~ "0123456789" && industry =~ "9876543210" && contains(positions, "0123456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), ((headline = "0123456789" && industry = "0123456789" && contains(positions, "9876543210"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @light-bulb.alert_long() ;
}
====
* => @builtin.thank_you, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) || (time = makeDate(1462320000000) && heartrate <= 55))) , v_time := time, v_place := place, v_heartrate := heartrate => @builtin.thank_you() ;
}
====
@scale.source, value > 82kg , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
@instagram.new_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((caption =~ "9876543210" && link = "http://www.abc.def"^^tt:url && filter =~ "23456789" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @light-bulb.set_power(power=enum(off)) ;
}
====
* => @youtube.search_videos, ((query = "9876543210" && channel_id = "9876543210") || (video_id =~ "12345" && channel_id =~ "23456789" && channel_title =~ "0123456789" && title = "23456789" && description = "ABCDEFGHIJ" && count = 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 14 && estimated_diameter_max = 1000m && relative_velocity > 5kmph && closest_distance_to_earth < 42cm)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @youtube.search_videos(query=v_name), (((query = "ABCDEFGHIJ" && video_id = "ABCDEFGHIJ" && channel_id =~ "23456789" && channel_title = "0123456788" && description = "0123456789") || (query =~ "0123456788" && video_id =~ "ABCDEFGHIJ" && channel_title =~ "0123456789" && description = "12345" && count < 55))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@yahoofinance.stock_div, yield <= 14 , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((stock_id = "aapl"^^tt:stock_id("Apple") && yield >= 55 && div < 11 && ex_div_date = makeDate(1462320000000)) || (company_name = "ABCDEFGHIJ" && yield <= 42 && div <= 7))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @uber.price_estimate(start=$context.location.home, end=$context.location.current_location), (((end = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type = "12345" && low_estimate > 42 && high_estimate = 14 && duration = 30min && distance < 1000m) || (uber_type = "9876543210" && low_estimate > 55 && high_estimate >= 55 && surge <= 7 && duration >= 30min) || (uber_type = "23456789" && currency_code = "23456789" && surge >= 7 && distance <= 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @ytranslate.detect_language, text = "23456789" , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="ABCDEFGHIJ"), (text = "12345") , v_detected_language := detected_language => notify;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((user =~ "0123456789" && category = "23456789") || (link = "http://www.google.com"^^tt:url && user =~ "12345"))) , v_title := title, v_link := link, v_user := user, v_category := category => @lg_webos_tv.set_volume(percent=11) ;
}
====
* => @dropbox.list_folder, (folder_name = "ABCDEFGHIJ" || (file_name =~ "0123456788" && is_folder = false) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456789" && is_folder = true && file_size > 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    @scale.source(), (value < 82kg) , v_time := time, v_value := value => @dropbox.list_folder(folder_name="0123456788"), (((is_folder = true && file_size <= 5KB) || (folder_name =~ "0123456788" && is_folder = true && last_modified = makeDate(1462320000000) && file_size = 5KB) || (file_size >= 5KB && full_path =~ "0123456788"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @instagram.get_pictures, (filter = "0123456788" || (count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=42), ((count > 11 && caption =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @almond_dates.post, (message =~ "0123456789" && poster = "0123456788") 
;;
Main() {
    @builtin.at(time=makeTime(20,30)), ((time = makeTime(7,30) || time = makeTime(20,30)))  => @almond_dates.post(interest="9876543210", message="0123456789", poster="ABCDEFGHIJ", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @dropbox.move, (new_name = "9876543210" || new_name =~ "0123456789") 
;;
Main() {
    @icalendar.event_begin(), ((summary = "9876543210" && sequence < 7 && organizer =~ "0123456788" && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.move(new_name=v_description, old_name="ABCDEFGHIJ") ;
}
====
* => @gmail.get_latest, subject =~ "12345" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && label = "12345")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @weatherapi.moon, (phase = "23456789" || phase = "0123456789") , v_date := date, v_phase := phase => *
;;
Main() {
    @wall_street_journal.world_news(), ((link = "http://www.google.com"^^tt:url || title = "9876543210")) , v_title := title, v_link := link => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((date = makeDate(1487030400000) && phase = "12345") || (date = makeDate(1462320000000) && phase = "0123456789"))) , v_date := date, v_phase := phase => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @almond_couch_market.search, (price <= 42 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((price >= 55 && contact = "+15551234567"^^tt:phone_number) || price >= 55)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @thermostat.set_minmax_temperature, high >= 22C 
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="0123456788", text_bottom="23456789"), (((template = "9876543210" && text_bottom =~ "0123456789" && name = "12345") || (template = "ABCDEFGHIJ" && text_top =~ "23456789" && text_bottom = "23456789" && name =~ "ABCDEFGHIJ"))) , v_name := name, v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=22C, high=22C) ;
}
====
@sportradar.nba_team, (watched_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && other_team_alias = "lal"^^sportradar:nba_team("LA Lakers") && away_points >= 11 && home_points < 14) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify
;;
Main() {
    @sportradar.nba_team(watched_team_alias="lal"^^sportradar:nba_team("LA Lakers")), ((home_name =~ "9876543210" && game_status = enum(scheduled) && away_points <= 55 && home_points <= 42 && result = enum(lose))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
* => @almond_bike_market.post, ((title =~ "ABCDEFGHIJ" && price >= 7 && info =~ "9876543210" && poster =~ "9876543210") || info = "ABCDEFGHIJ") 
;;
Main() {
    @phone.gps(), ((altitude > 42cm && bearing <= 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_bike_market.post(price=v_bearing, title="12345", info="12345", poster="0123456789") ;
}
====
@gmail.receive_email, ((from_name =~ "0123456789" && date = makeDate(1487030400000)) || (from_name = "23456789" && snippet =~ "23456789") || date = makeDate(1487030400000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => *
;;
Main() {
    @gmail.receive_email(), ((contains(labels, "ABCDEFGHIJ") && snippet =~ "12345")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @almond_couch_market.search(), (((price >= 14 && contact = "+16501234567"^^tt:phone_number) || title = "23456789")) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @almond_dates.post, ((interest =~ "12345" && message = "12345" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || poster = "0123456788") 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="9876543210", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
@scale.source, (value <= 82kg || value >= 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((value <= 82kg || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1462320000000))) , v_time := time, v_value := value => @twitter.search(query="0123456789", count=14), ((count < 55 && text =~ "23456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name = "9876543210" || file_name =~ "23456789" || file_name = "23456789" || file_name = "0123456789") 
;;
Main() {
    @omlet.incomingmessage(), (message =~ "12345") , v_type := type, v_message := message => @google_drive.create_new_drive_file(file_name=v_message) ;
}
====
@fitness-tracker.getbody, (place = $context.location.home && leanMass < 7 && bodyFat < 14 && bmi >= 14) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight < 155lb && leanMass > 7 && bodyFat > 55 && bmi < 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @youtube.list_subscriptions(), ((title =~ "23456789" && description = "9876543210")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @tumblr-blog.post_text, body = "ABCDEFGHIJ" 
;;
Main() {
    @jawbone_up.getbody(), ((weight >= 82kg && leanMass < 7 && bodyFat >= 7 && bmi <= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @tumblr-blog.post_text(title="23456789", body="ABCDEFGHIJ") ;
}
====
now => @uber.time_estimate, (estimate < 30min || (uber_type = "23456789" && estimate >= 2h) || (start = $context.location.current_location && estimate <= 2h) || estimate >= 30min || (start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "0123456789" && estimate <= 30min)) , v_uber_type := uber_type, v_estimate := estimate => @slack.setPresence, true 
;;
Main() {
    now => @uber.time_estimate(start=$context.location.current_location), ((uber_type = "9876543210" && estimate >= 2h)) , v_uber_type := uber_type, v_estimate := estimate => @slack.setPresence(presence=enum(away)) ;
}
====
@nasa.asteroid, ((asteroid_id <= 42 && estimated_diameter_max >= 42cm && closest_distance_to_earth >= 1000m && orbiting_body = "0123456789") || (asteroid_id > 11 && estimated_diameter_min = 42cm && estimated_diameter_max <= 1000m && closest_distance_to_earth <= 42cm) || (asteroid_id < 42 && name = "ABCDEFGHIJ" && estimated_diameter_min < 42cm && relative_velocity > 25mph && closest_distance_to_earth >= 1000m) || (asteroid_id <= 14 && estimated_diameter_min >= 1000m && estimated_diameter_max > 42cm && closest_distance_to_earth <= 42cm) || (asteroid_id > 11 && name = "9876543210" && estimated_diameter_min <= 1000m && estimated_diameter_max = 1000m && dangerous = true && relative_velocity <= 25mph && closest_distance_to_earth >= 42cm) || (asteroid_id <= 7 && name = "0123456789" && relative_velocity <= 25mph && closest_distance_to_earth = 1000m) || (estimated_diameter_min < 1000m && estimated_diameter_max <= 42cm && relative_velocity > 25mph && closest_distance_to_earth < 1000m && orbiting_body = "9876543210")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @dropbox.list_folder, (is_folder = true && file_size < 20MB && full_path =~ "0123456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    @nasa.asteroid(), (((estimated_diameter_min > 1000m && dangerous = true && relative_velocity >= 5kmph) || (asteroid_id <= 55 && estimated_diameter_min <= 42cm && estimated_diameter_max > 42cm && dangerous = true && relative_velocity >= 25mph && closest_distance_to_earth = 42cm) || (name =~ "23456789" && estimated_diameter_min > 42cm && dangerous = true && relative_velocity < 5kmph && closest_distance_to_earth >= 42cm && orbiting_body = "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @dropbox.list_folder(folder_name=v_name), ((file_name =~ "12345" && last_modified = makeDate(1462320000000))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
now => * => @onedrive.rename_file, old_name = "0123456789" 
;;
Main() {
    now => @onedrive.rename_file(old_name="0123456789", new_name="12345") ;
}
====
now => @ytranslate.translate, (text = "12345" || (target_language = "zh"^^tt:iso_lang_code("Chinese") && text =~ "9876543210" && translated_text = "ABCDEFGHIJ")) , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="9876543210"), true , v_source_language := source_language, v_translated_text := translated_text => @onedrive.upload_picture(file_name=v_translated_text, picture_url=v_translated_text) ;
}
====
@phone.gps, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude > 1000m && bearing > 14 && speed = 25mph) || (bearing <= 14 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude > 1000m && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @tumblr-blog.post_text, body = "ABCDEFGHIJ" 
;;
Main() {
    @gmail.receive_primary_email(), ((from_name =~ "0123456788" && from_address = "bob@stanford.edu"^^tt:email_address && subject = "12345" && date = makeDate(1462320000000))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @tumblr-blog.post_text(body=v_from_name, title="0123456789") ;
}
====
* => @google_drive.create_new_drive_file, file_name =~ "0123456788" 
;;
Main() {
    @phone.gps(), true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
@phone.receive_sms, true , v_from := from, v_body := body => @tumblr-blog.post_text, ((title =~ "23456789" && body = "12345") || (title =~ v_body && body = "23456789")) 
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @tumblr-blog.post_text(body=v_body, title="0123456789") ;
}
====
* => @uber.time_estimate, (start = $context.location.current_location && estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((start = $context.location.current_location && uber_type = "12345") || (start = $context.location.work && uber_type = "12345") || (uber_type =~ "23456789" && estimate <= 30min))) , v_uber_type := uber_type, v_estimate := estimate => @light-bulb.set_power(power=enum(on)) ;
}
====
now => @giphy.get, true , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @builtin.get_date, true , v_date := date => notify
;;
Main() {
    now => @builtin.get_date(), true , v_date := date => notify;
}
====
now => * => @almond_couch_market.post, title =~ "9876543210" 
;;
Main() {
    now => @almond_couch_market.post(title="9876543210", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => * => @onedrive.create_file, (file_name =~ "0123456788" && contents =~ "9876543210") 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (summary = "0123456789") , v_date := date, v_summary := summary, v_description := description => @onedrive.create_file(file_name=v_description, contents="ABCDEFGHIJ") ;
}
====
@scale.source, (time = makeDate(1487030400000) && value >= 155lb) , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value = 155lb)) , v_time := time, v_value := value => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
now => * => @almond_bike_market.post, (title =~ "9876543210" && price = 42 && info =~ "0123456788" && poster =~ "0123456789") 
;;
Main() {
    now => @almond_bike_market.post(title="23456789", price=55, info="0123456789", poster="0123456789") ;
}
====
* => @ytranslate.translate, (text =~ "ABCDEFGHIJ" || (target_language = "en"^^tt:iso_lang_code("English") && text = "12345") || (source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "zh"^^tt:iso_lang_code("Chinese") && text =~ "ABCDEFGHIJ" && translated_text = "0123456788") || (source_language = "en"^^tt:iso_lang_code("English") && target_language = "it"^^tt:iso_lang_code("Italian") && text =~ "0123456788" && translated_text = "23456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @phone.gps(), true , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @ytranslate.translate(target_language="it"^^tt:iso_lang_code("Italian"), text="0123456789"), ((translated_text = "0123456788" || (text =~ "9876543210" && translated_text = "9876543210"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
now => @gmail.send_picture, message = "ABCDEFGHIJ" 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@phdcomics.new_post, ((title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || title = "0123456788") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @youtube.search_videos(query=v_title), ((video_id =~ "0123456789" && channel_id = "9876543210" && channel_title =~ "12345" && count < 42 && video_url = "http://www.abc.def"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @holidays.uk_holiday_event(), (((summary = "12345" && description = "0123456789") || description =~ "0123456788" || (date = makeDate(1487030400000) && description =~ "0123456788") || summary = "12345" || (date = makeDate(1487030400000) && summary = "9876543210"))) , v_date := date, v_summary := summary, v_description := description => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @giphy.get_tag, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), (((tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @security-camera.set_power(power=enum(on)) ;
}
====
* => @dropbox.list_folder, ((is_folder = false && file_size < 5KB) || (folder_name = "0123456788" && file_name = "0123456789" && file_size = 20MB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), ((folder_name =~ "12345" && is_folder = true && last_modified = makeDate(1487030400000))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @almond_couch_market.post(title=v_file_name, contact=v_full_path, price=55) ;
}
====
now => @nasa.rover, ((date_taken = makeDate(1462320000000) && camera_used =~ "12345") || (date_taken = makeDate(1487030400000) && camera_used = "23456789") || count >= 14) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1487030400000) || (date_taken = makeDate(1487030400000) && count > 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @google_drive.create_new_drive_file(file_name=v_camera_used) ;
}
====
now => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude <= 1000m && bearing > 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((bearing >= 7 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @thermostat.get_humidity, true , v_value := value => notify
;;
Main() {
    now => @thermostat.get_humidity(), true , v_value := value => notify;
}
====
@sportradar.ncaambb_team, ((away_name =~ "9876543210" && home_name =~ "ABCDEFGHIJ" && game_status = enum(closed)) || (other_team_alias = "cal"^^sportradar:ncaambb_team("California Bears") && away_name =~ "0123456789" && away_points <= 14 && home_points < 42)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => *
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="stan"^^sportradar:ncaambb_team("Stanford Cardinals")), ((watched_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && other_team_alias = "stan"^^sportradar:ncaambb_team("Stanford Cardinals") && away_points >= 55 && home_points < 55)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @github.comment_issue(repo_name=v_home_name, issue_number=v_home_points, body=v_away_name) ;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), ((timestamp =~ "9876543210" && message = "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @bluetooth_speaker.raise_volume() ;
}
====
@reddit_front_page.newpost, (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && category =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_user := user, v_category := category => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @reddit_front_page.newpost(), ((title =~ "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_user := user, v_category := category => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (phase =~ "0123456789") , v_date := date, v_phase := phase => @security-camera.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, (day =~ "0123456789" && distance < 1000m && activeTime <= 30min && inactiveTime <= 30min && calories <= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @slack.send_picture, ((channel = "lol"^^tt:hashtag && caption = "12345") || caption =~ "23456789") 
;;
Main() {
    @activity-tracker.getmove(), ((distance < 1000m && steps > 42 && inactiveTime >= 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @slack.send_picture(channel=v_day, caption=v_day, picture_url=v_day) ;
}
====
@twitter.direct_message, (from = "bob"^^tt:username && message = "12345") , v_from := from, v_message := message, v___reserved := __reserved => * => notify
;;
Main() {
    @twitter.direct_message(), (message = "0123456788") , v_from := from, v_message := message, v___reserved := __reserved => @bing.web_search(query="ABCDEFGHIJ"), (((query = "12345" && title =~ "0123456789" && description =~ "0123456789" && link = "http://www.google.com"^^tt:url) || (query =~ "9876543210" && description =~ "12345"))) , v_title := title, v_description := description, v_link := link => notify;
}
====
now => @thecatapi.get, (image_id =~ "0123456788" && count < 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "12345" && count >= 14 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @github.add_email(email=v_image_id) ;
}
====
@sleep-tracker.getsleep, ((awakeTime = makeDate(1487030400000) && rem < 2h && light <= 30min && deep <= 2h) || time = makeDate(1487030400000)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((awakeTime = makeDate(1487030400000) && rem > 2h && light < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => * => @phone.notify, ((title = "23456789" && message =~ "12345") || (title =~ "0123456788" && message =~ "12345")) 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 11 && name = "12345" && estimated_diameter_min = 1000m && estimated_diameter_max >= 42cm && relative_velocity = 5kmph && closest_distance_to_earth = 42cm && orbiting_body = "12345")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @phone.notify(title="23456789", message="12345") ;
}
====
* => @giphy.get_tag, true , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), true , v_picture_url := picture_url => @speaker.play_music() ;
}
====
* => @light-bulb.set_power, true 
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @light-bulb.set_power(power=enum(off)) ;
}
====
@sportradar.soccer_eu_tourney, ((tournament_search_term = "23456789" && tournament_league_name =~ "0123456788" && home_alias = "ABCDEFGHIJ" && away_name = "9876543210" && home_name = "0123456788" && game_status = enum(closed) && away_points = 7 && home_points > 14) || (away_alias =~ "9876543210" && home_name = "9876543210" && home_points <= 14)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => * => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456788"), (((tournament_full_name = "9876543210" && tournament_league_name = "23456789" && away_alias =~ "0123456789" && away_name = "0123456788" && home_name = "0123456789" && scheduled_time =~ "0123456789" && home_points > 55) || (tournament_full_name = "ABCDEFGHIJ" && away_alias =~ "0123456789" && home_alias = "9876543210" && home_points < 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify;
}
====
* => @holidays.next_us_holiday_event, ((summary = "23456789" && description =~ "9876543210") || date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @almond_dates.post, ((interest = "0123456789" && message =~ "23456789") || interest =~ "9876543210") 
;;
Main() {
    @tumblr.new_photo(blog_name="ABCDEFGHIJ"), ((caption = "0123456788" || (date = makeDate(1462320000000) && caption =~ "0123456788" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @holidays.next_us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @almond_dates.post(interest=v_description, poster=v_summary, phone=v_description, message="9876543210") ;
}
====
* => @fitbit.recordweight, (weight = 155lb || weight <= 82kg) 
;;
Main() {
    @wall_street_journal.us_business(), ((title =~ "ABCDEFGHIJ" || link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url || title =~ "12345")) , v_title := title, v_link := link => @fitbit.recordweight(weight=82kg) ;
}
====
now => * => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @giphy.get_tag(tag="lol"^^tt:hashtag), ((tag = "cat"^^tt:hashtag || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="0123456788") ;
}
====
@scale.source, ((time = makeDate(1462320000000) && value >= 82kg) || value <= 155lb) , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), ((value = 82kg || value = 82kg || value <= 155lb)) , v_time := time, v_value := value => notify;
}
====
@tumblr.new_text, ((blog_name = "0123456789" && contains(tags, "lol"^^tt:hashtag)) || (blog_name =~ "12345" && date = makeDate(1462320000000) && title = "9876543210" && body = "0123456788")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((blog_name =~ "0123456788" && date = makeDate(1462320000000) && body = "0123456788")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
* => * => @tumblr-blog.post_text, true 
;;
Main() {
    @scale.source(), (value < 155lb) , v_time := time, v_value := value => @tumblr-blog.post_text(title="9876543210", body="ABCDEFGHIJ") ;
}
====
* => @xkcd.get_comic, (number > 11 && title =~ "12345" && alt_text = "12345") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "23456789" && message =~ "23456789" && poster = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @xkcd.get_comic(), ((number < 11 && alt_text =~ "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @youtube.search_videos, ((video_id = "12345" && channel_title = "0123456789" && description =~ "ABCDEFGHIJ" && count = 55) || (query =~ "ABCDEFGHIJ" && video_id =~ "0123456789" && title =~ "0123456789" && video_url = "http://www.abc.def"^^tt:url) || (video_id = "12345" && channel_title = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 11)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @jawbone_up.getbody(), ((weight = 82kg && bodyFat > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @youtube.search_videos(query="ABCDEFGHIJ"), ((query = "0123456788" && channel_title = "0123456788" && title =~ "0123456789" && description = "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count < 42)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@fitness-tracker.getbody, ((time = makeDate(1487030400000) && bmi > 14) || (time = makeDate(1462320000000) && leanMass >= 14 && bodyFat < 11 && bmi > 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.current_location && weight >= 155lb && leanMass >= 11 && bodyFat < 42 && bmi <= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @builtin.get_random(), ((number <= 11 || number = 14 || number > 7)) , v_number := number => notify;
}
====
now => * => @omlet.send_to, to = "alice"^^tt:username 
;;
Main() {
    now => @nasa.apod(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || title = "23456789" || (title = "12345" && description =~ "0123456789"))) , v_title := title, v_description := description, v_picture_url := picture_url => @omlet.send_to(to=v_description, message=v_description) ;
}
====
* => @onedrive.upload_picture, ((file_name =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || file_name = "ABCDEFGHIJ" || file_name = "ABCDEFGHIJ" || (file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @onedrive.upload_picture(file_name="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@onedrive.file_created, (file_name =~ "0123456789" || file_name =~ "0123456789" || file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "0123456788" || file_name = "23456789")) , v_file_name := file_name => notify;
}
====
now => * => @tumblr-blog.post_text, (body =~ "23456789" || title =~ "12345") 
;;
Main() {
    now => @almond_dates.search(), (((poster = "0123456788" && phone = "+16501234567"^^tt:phone_number) || (message =~ "12345" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_text(title=v_interest, body=v_message) ;
}
====
now => * => @phone.call_emergency, true 
;;
Main() {
    now => @phone.call_emergency() ;
}
====
now => * => @facebook.post, status = "23456789" 
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((date = makeDate(1487030400000) && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @facebook.post(status="9876543210") ;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "23456789" && contains(hashtags, "funny"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "12345" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @heatpad.set_power, true 
;;
Main() {
    @fitness-tracker.getbody(), ((bmi > 7 || (weight < 155lb && bodyFat <= 55) || (weight < 82kg && leanMass > 55 && bodyFat < 55 && bmi > 7) || (weight = 82kg && leanMass <= 14 && bmi = 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @heatpad.set_power(power=enum(off)) ;
}
====
@reddit_front_page.newpost, ((link = "http://www.abc.def"^^tt:url && category =~ "23456789") || (title =~ "ABCDEFGHIJ" && user =~ "0123456788" && category = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => * => @phone.call_emergency, true 
;;
Main() {
    @reddit_front_page.newpost(), ((title =~ "0123456788" && category =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @phone.call_emergency() ;
}
====
* => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@omlet.newmessage, message = "ABCDEFGHIJ" , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.newmessage(), ((message = "ABCDEFGHIJ" || message =~ "9876543210")) , v_type := type, v_message := message => @almond_bike_market.detail(id="9876543210"), true  => notify;
}
====
* => @bing.image_search, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height <= 55) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @fitness-tracker.getbody(), (((leanMass < 55 && bodyFat >= 14 && bmi > 55) || (place = $context.location.work && weight <= 155lb && leanMass <= 14 && bodyFat = 11 && bmi > 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @bing.image_search(query="ABCDEFGHIJ"), ((width <= 14 || (query =~ "9876543210" && width <= 11))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @nasa.apod, title =~ "0123456789" , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    @xkcd.new_comic(), true , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @nasa.apod(), ((title = "0123456789" && description =~ "0123456788")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @imgflip.list, ((name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), ((title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @imgflip.list(), (name =~ "12345") , v_name := name, v_picture_url := picture_url => @security-camera.set_power(power=enum(off)) ;
}
====
now => * => @linkedin.share, (status = "23456789" || status = "0123456788") 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title = "0123456789") , v_title := title, v_link := link, v_updated := updated => @linkedin.share(status="12345") ;
}
====
* => @github.comment_issue, ((repo_name =~ "ABCDEFGHIJ" && issue_number > 14 && body =~ "0123456788") || (repo_name = "0123456789" && body = "9876543210") || repo_name =~ "9876543210") 
;;
Main() {
    @washington_post.new_article(section=enum(sports)), true , v_title := title, v_link := link, v_description := description => @github.comment_issue(repo_name=v_title, body=v_title, issue_number=11) ;
}
====
now => * => @almond_bike_market.post, (title = "9876543210" && price > 55 && info =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @xkcd.random_comic(), ((number > 7 && title =~ "23456789" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @almond_bike_market.post(title=v_alt_text, poster=v_title, price=7, info="23456789") ;
}
====
* => @thecatapi.get, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id =~ "23456789" && count = 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (image_id =~ "ABCDEFGHIJ" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @almond_bike_market.monitor(price=11), (info =~ "ABCDEFGHIJ")  => @thecatapi.get(), (((image_id = "12345" && count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || count < 11 || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (image_id =~ "0123456788" && count < 7) || (count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id =~ "0123456788" && count <= 7) || (image_id = "0123456789" && count <= 55 && link = "http://www.abc.def"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
@xkcd.new_comic, (number <= 42 || (number <= 7 && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), (((number <= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (number >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @bing.image_search(query=v_title), (((query =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width > 7) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height > 7))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@almond_couch_market.monitor, ((price < 14 && contact = "+16501234567"^^tt:phone_number) || title =~ "23456789") , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @twitter.search_by_hashtag(query_hashtag=v_title, count=v_price, from="alice"^^tt:username, in_reply_to="alice"^^tt:username), ((query_hashtag = "cat"^^tt:hashtag || (count > 7 && contains(urls, "http://www.google.com"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @security-camera.get_url, true , v_link := link => notify
;;
Main() {
    @tumblr.new_photo(blog_name="23456789"), (((caption = "ABCDEFGHIJ" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (blog_name = "23456789" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @security-camera.get_url(), true , v_link := link => notify;
}
====
@onedrive.file_modified, (file_name =~ "0123456789" || file_name = "9876543210" || file_name =~ "0123456789" || file_name = "9876543210") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @twitter.unfollow(user_name=v_file_name) ;
}
====
* => @bing.image_search, ((title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && width < 42 && height > 7) || link = "http://www.abc.def"^^tt:url || (query =~ "23456789" && title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height < 7) || (query = "ABCDEFGHIJ" && height = 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="ABCDEFGHIJ"), true , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@scale.source, value = 155lb , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), (((time = makeDate(1487030400000) && value < 82kg) || time = makeDate(1487030400000))) , v_time := time, v_value := value => notify;
}
====
@builtin.at, (time = makeTime(15,0) || time = makeTime(7,30) || time = makeTime(20,30))  => *
;;
Main() {
    @builtin.at(time=makeTime(20,30)), (time = makeTime(15,0))  => notify;
}
====
* => @almond_bike_market.search, ((info =~ "12345" && price >= 11) || price > 55 || (info =~ "ABCDEFGHIJ" && price < 14) || (info =~ "0123456788" && price < 14) || price > 55 || (info = "12345" && price = 42) || price >= 14)  => notify
;;
Main() {
    @youtube.source(), ((channel_title = "23456789" && video_id = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @almond_bike_market.search(info=v_channel_id), ((info = "0123456789" && price > 42))  => notify;
}
====
now => @bing.image_search, ((query = "0123456788" && width < 42 && height <= 7) || (query =~ "12345" && width >= 55 && height >= 42) || (query = "0123456789" && width >= 11) || (query = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && width = 7 && height >= 11) || (query =~ "23456789" && title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && width < 11) || (title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="0123456788"), (((width >= 11 && height <= 11) || (query = "0123456788" && title =~ "23456789" && link = "http://www.abc.def"^^tt:url && width <= 7 && height <= 14) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || link = "http://www.abc.def"^^tt:url || (query =~ "0123456788" && height > 14) || (title = "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @builtin.thank_you() ;
}
====
* => @builtin.get_date, date = makeDate(1462320000000) , v_date := date => *
;;
Main() {
    now => @builtin.get_date(), (date = makeDate(1462320000000)) , v_date := date => notify;
}
====
@rss.new_post, true , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @fitbit.getbody(), (((weight >= 155lb && fat >= 11) || (weight < 82kg && fat <= 7))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @uber.price_estimate, ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "23456789" && high_estimate < 55 && currency_code =~ "0123456788" && surge > 7 && duration <= 2h && distance < 42cm) || (end = $context.location.home && uber_type = "9876543210" && low_estimate <= 11 && high_estimate > 42 && surge > 14 && duration <= 30min && distance > 1000m) || (start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && low_estimate < 42 && currency_code = "23456789" && surge >= 42 && duration = 2h && distance <= 42cm) || (uber_type =~ "0123456789" && high_estimate >= 42 && currency_code = "12345" && surge <= 55 && duration < 2h)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((low_estimate > 55 && distance >= 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@scale.source, (value = 82kg || value > 82kg || (time = makeDate(1462320000000) && value <= 82kg) || value >= 82kg || value > 155lb) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value < 155lb)) , v_time := time, v_value := value => @xkcd.get_comic(), (((number >= 55 && alt_text =~ "ABCDEFGHIJ") || (link = "http://www.abc.def"^^tt:url && alt_text =~ "12345"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    @sleep-tracker.getsleep(), (((place = $context.location.current_location && rem <= 30min && light >= 30min && deep <= 30min) || (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && light >= 2h && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
@omlet.newmessage, type = enum(text) , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.newmessage(), (type = enum(picture)) , v_type := type, v_message := message => notify;
}
====
@security-camera.new_event, (start_time = makeDate(1462320000000) && has_motion = false && has_person = true) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
@yahoofinance.stock_div, yield >= 55 , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((div < 42 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
* => @almond_dates.post, (message = "ABCDEFGHIJ" || (interest =~ "23456789" && poster = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (message = "23456789" && poster = "12345")) 
;;
Main() {
    @tumblr.new_photo(blog_name="12345"), (((blog_name =~ "12345" && caption =~ "0123456788" && link = "http://www.abc.def"^^tt:url && contains(tags, "funny"^^tt:hashtag)) || (link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (date = makeDate(1462320000000) && caption = "0123456788"))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @almond_dates.post(interest="ABCDEFGHIJ", message="12345", poster="23456789", phone="+16501234567"^^tt:phone_number) ;
}
====
@instagram.new_picture, ((caption =~ "12345" && link = "http://www.abc.def"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && link = "http://www.abc.def"^^tt:url) || (caption =~ "9876543210" && link = "http://www.abc.def"^^tt:url && filter =~ "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @ytranslate.translate, ((text = "0123456788" && translated_text = "0123456789") || (target_language = "zh"^^tt:iso_lang_code("Chinese") && text = "23456789" && translated_text = "12345")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @instagram.new_picture(), (((caption = "0123456788" && filter =~ "0123456789" && location = $context.location.current_location) || location = $context.location.work || (filter =~ "ABCDEFGHIJ" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @ytranslate.translate(target_language=v_filter, text=v_filter), (((source_language = "zh"^^tt:iso_lang_code("Chinese") && target_language = "zh"^^tt:iso_lang_code("Chinese") && text =~ "0123456789" && translated_text = "0123456788") || translated_text = "9876543210")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
now => * => @twitter.sink, (status =~ "23456789" || status = "23456789") 
;;
Main() {
    now => @twitter.sink(status="0123456788") ;
}
====
* => @almond_bike_market.ask_seller, (value = "12345" || (property =~ "12345" && value =~ "12345" && public = false) || id =~ "12345" || id =~ "0123456789" || id =~ "9876543210") , v_answer := answer => *
;;
Main() {
    @security-camera.new_event(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_bike_market.ask_seller(public=v_has_sound, id="ABCDEFGHIJ", property="0123456789", value="12345"), ((value = "23456789" || (id = "ABCDEFGHIJ" && public = false && answer =~ "0123456788") || (public = false && answer =~ "0123456789"))) , v_answer := answer => notify;
}
====
@instagram.new_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && location = $context.location.work) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), (filter = "0123456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @security-camera.set_power(power=enum(on)) ;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight <= 155lb) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (weight = 155lb) , v_time := time, v_weight := weight => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=$context.location.home), (((low_estimate >= 14 && high_estimate < 42 && surge <= 55 && duration < 30min && distance >= 1000m) || (low_estimate <= 42 && high_estimate >= 7 && currency_code =~ "12345" && surge >= 7 && distance > 1000m) || (uber_type =~ "ABCDEFGHIJ" && low_estimate = 14 && high_estimate < 55 && currency_code = "0123456789" && surge >= 42 && duration >= 2h && distance > 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @facebook.post(status=v_uber_type) ;
}
====
now => @icalendar.list_events, (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && summary = "23456789" && description = "ABCDEFGHIJ" && sequence < 14) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && description = "9876543210" && organizer =~ "0123456789" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@icalendar.event_begin, ((start_date = makeDate(1462320000000) && summary =~ "0123456789" && description =~ "0123456788" && sequence >= 55 && organizer =~ "12345" && location =~ "9876543210") || (start_date = makeDate(1462320000000) && summary = "9876543210" && description = "9876543210" && sequence < 55)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1487030400000) && sequence > 55 && organizer = "ABCDEFGHIJ") || (description =~ "0123456788" && organizer =~ "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@nasa.asteroid, (relative_velocity = 25mph && closest_distance_to_earth >= 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id < 7 && name = "23456789" && estimated_diameter_min = 1000m && estimated_diameter_max <= 1000m && dangerous = true && relative_velocity <= 5kmph && closest_distance_to_earth > 1000m && orbiting_body = "9876543210") || (estimated_diameter_min > 1000m && estimated_diameter_max >= 42cm && dangerous = false && relative_velocity >= 25mph && closest_distance_to_earth >= 1000m && orbiting_body =~ "9876543210") || (asteroid_id >= 14 && name =~ "0123456788" && estimated_diameter_min <= 1000m && estimated_diameter_max >= 42cm && dangerous = true && relative_velocity > 5kmph && closest_distance_to_earth > 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    now => @dropbox.open(file_name="12345"), (url = "23456789") , v_url := url => @car.honk_horn() ;
}
====
@xkcd.new_whatif, (title =~ "ABCDEFGHIJ" || title = "23456789") , v_title := title, v_link := link => notify
;;
Main() {
    @xkcd.new_whatif(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => notify;
}
====
* => @almond_bike_market.ask, (property =~ "12345" || property =~ "0123456789")  => *
;;
Main() {
    now => @almond_bike_market.ask(id="23456789", property="23456789"), true  => @bluetooth_speaker.set_sink() ;
}
====
@thermostat.temperature, ((time = makeDate(1487030400000) && value = 22C) || value > 73F || value >= 73F || value > 73F) , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), ((value <= 22C || value <= 22C)) , v_time := time, v_value := value => notify;
}
====
* => * => @onedrive.create_file, (file_name =~ "0123456788" && contents =~ "9876543210") 
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.work && altitude > 1000m && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @onedrive.create_file(file_name="0123456789", contents="23456789") ;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @xkcd.get_comic(number=11), ((number <= 42 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_couch_market.search, ((title =~ "9876543210" && price > 11) || (title =~ "ABCDEFGHIJ" && price > 14)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @builtin.timer(interval=30min), true  => @almond_couch_market.search(), (((title =~ "ABCDEFGHIJ" && price < 55) || (title = "12345" && contact = "+16501234567"^^tt:phone_number) || contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @facebook.post, (status =~ "0123456789" || status = "9876543210") 
;;
Main() {
    @fitness-tracker.getbody(), ((weight >= 82kg && leanMass > 55 && bmi >= 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @facebook.post(status="12345") ;
}
====
now => * => @almond_dates.post, ((interest = "0123456789" && message =~ "0123456789" && poster =~ "ABCDEFGHIJ") || poster =~ "23456789" || (interest =~ "0123456789" && message = "9876543210" && poster =~ "12345")) 
;;
Main() {
    now => @instagram.get_pictures(), ((count < 11 && caption = "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(interest=v_filter, poster=v_filter, phone=v_filter, message="ABCDEFGHIJ") ;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @builtin.timer(interval=30min), (interval > 30min)  => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @holidays.next_uk_holiday_event, (summary =~ "12345" && description = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), ((watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && other_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && game_status = enum(closed) && scheduled_time =~ "12345" && game_inning = "12345" && away_runs >= 55)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @fitbit.getbody, ((weight < 82kg && fat <= 7) || (weight = 82kg && bmi >= 55 && fat = 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), (((weight <= 155lb && bmi <= 14) || (weight > 155lb && bmi >= 14))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @linkedin.get_profile, (specialties =~ "ABCDEFGHIJ" && contains(positions, "0123456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @omlet.send_to, (to = "alice"^^tt:username && message = "12345") 
;;
Main() {
    now => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @omlet.send_to(to=v_formatted_name, message=v_formatted_name) ;
}
====
* => @almond_dates.search, (interest = "9876543210" || (interest =~ "23456789" && message = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "12345") , v_file_name := file_name => @almond_dates.search(), ((message = "0123456789" && poster = "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@rss.new_post, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @nasa.rover(), (((date_taken = makeDate(1487030400000) && count > 14 && camera_used = "9876543210") || (count < 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used = "23456789") || (count = 55 && camera_used = "0123456789") || (count < 11 && camera_used = "9876543210"))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
now => @thermostat.get_temperature, value >= 22C , v_value := value => *
;;
Main() {
    now => @thermostat.get_temperature(), (value >= 22C) , v_value := value => @slack.send(channel="lol"^^tt:hashtag, message="12345") ;
}
====
now => @youtube.channels_by_category, (channel_id =~ "ABCDEFGHIJ" && title =~ "9876543210" && description = "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(causes)), ((title =~ "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @instagram.get_pictures, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "cat"^^tt:hashtag)) || (count > 7 && caption =~ "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(the_fix)), (((section = enum(powerpost) && title = "12345") || section = enum(morning_mix) || title = "12345" || (section = enum(post_partisan) && title =~ "9876543210"))) , v_title := title, v_link := link => @instagram.get_pictures(count=11), (((count >= 11 && caption =~ "12345" && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.current_location) || (count > 7 && caption = "0123456788" && link = "http://www.google.com"^^tt:url && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (count <= 55 && caption =~ "12345" && link = "http://www.abc.def"^^tt:url))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@bodytrace.source, (weight > 82kg || weight > 82kg) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight >= 155lb)) , v_time := time, v_weight := weight => notify;
}
====
now => @imgflip.generate, ((text_top =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || template =~ "ABCDEFGHIJ" || (text_top =~ "0123456788" && text_bottom =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top = "9876543210" && text_bottom =~ "9876543210")) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="0123456788", text_top="12345", text_bottom="ABCDEFGHIJ"), ((template = "0123456788" && text_top =~ "0123456788" && text_bottom =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @weatherapi.moon, (phase = "0123456788" || (date = makeDate(1487030400000) && phase = "0123456788")) , v_date := date, v_phase := phase => notify
;;
Main() {
    now => @weatherapi.moon(location=$context.location.home), true , v_date := date, v_phase := phase => notify;
}
====
@weatherapi.monitor, ((wind_speed > 5kmph && humidity = 11 && cloudiness <= 42 && fog >= 7) || (location = $context.location.current_location && temperature <= 73F && wind_speed <= 5kmph && humidity > 11 && cloudiness >= 55 && fog < 14) || (wind_speed < 5kmph && humidity = 42 && fog <= 55 && weather = "12345" && status = enum(drizzling))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((temperature >= 22C && wind_speed >= 5kmph && cloudiness < 11 && fog <= 14 && weather = "0123456788" && status = enum(sleety)) || (location = $context.location.work && wind_speed = 25mph && humidity >= 55 && cloudiness >= 55 && fog = 11 && weather =~ "23456789") || (wind_speed < 5kmph && fog <= 42 && weather = "ABCDEFGHIJ" && status = enum(sunny)) || (temperature < 22C && wind_speed > 25mph && cloudiness >= 11 && fog > 42 && status = enum(snowy)) || (temperature < 73F && humidity > 14 && cloudiness >= 14 && fog <= 42) || (temperature = 73F && wind_speed >= 25mph && fog < 14 && weather =~ "ABCDEFGHIJ") || (temperature < 73F && wind_speed > 25mph && cloudiness < 42 && status = enum(drizzling)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
* => @linkedin.share, (status =~ "12345" || status = "9876543210") 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) && place = $context.location.current_location && heartrate > 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @linkedin.share(status="23456789") ;
}
====
now => @holidays.next_uk_holiday_event, (description = "9876543210" || (summary = "12345" && description = "9876543210") || (summary = "23456789" && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((description =~ "0123456789" || description = "12345")) , v_date := date, v_summary := summary, v_description := description => @light-bulb.set_power(power=enum(off)) ;
}
====
now => @uber.price_estimate, (start = $context.location.home && low_estimate > 42 && surge < 7 && duration > 30min && distance < 1000m) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && low_estimate > 7 && high_estimate < 7 && currency_code =~ "12345" && surge <= 55 && duration < 30min && distance <= 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
now => @almond_bike_market.ask_seller, ((value =~ "0123456788" && answer =~ "ABCDEFGHIJ") || property =~ "12345" || (public = false && answer = "0123456789") || (id = "0123456789" && property =~ "0123456788" && value = "12345" && public = false && answer =~ "9876543210") || (id = "ABCDEFGHIJ" && property = "9876543210" && answer = "9876543210")) , v_answer := answer => notify
;;
Main() {
    now => @almond_bike_market.ask_seller(id="12345", property="ABCDEFGHIJ", value="23456789", public=false), (property =~ "ABCDEFGHIJ") , v_answer := answer => notify;
}
====
now => @imgflip.generate, true , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="ABCDEFGHIJ", text_bottom="9876543210"), ((template = "12345" && text_top =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @security-camera.set_power(power=enum(off)) ;
}
====
now => @linkedin.get_profile, ((industry = "ABCDEFGHIJ" && specialties = "23456789" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "ABCDEFGHIJ" && industry =~ "0123456789" && specialties =~ "0123456789" && contains(positions, "23456789"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), (((industry = "0123456789" && specialties = "23456789" && contains(positions, "ABCDEFGHIJ") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "0123456789" && headline =~ "ABCDEFGHIJ" && industry =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @xkcd.random_comic(), (alt_text = "23456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @heatpad.set_power(power=enum(off)) ;
}
====
now => @thermostat.set_hvac_mode, (mode = enum(off) || mode = enum(off)) 
;;
Main() {
    now => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
now => * => @gmail.send_email, ((to = "bob@stanford.edu"^^tt:email_address && subject = "ABCDEFGHIJ") || subject = "23456789" || to = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="12345") ;
}
====
* => * => @onedrive.create_file, (file_name =~ "0123456788" && contents =~ "9876543210") 
;;
Main() {
    now => @onedrive.create_file(file_name="12345", contents="0123456788") ;
}
====
* => @almond_dates.search, poster =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), ((stock_id = "msft"^^tt:stock_id("Microsoft") && ask_price < 42 && bid_price < 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @almond_dates.search(), (interest =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@reddit_front_page.newpost, (title = "ABCDEFGHIJ" || link = "http://www.abc.def"^^tt:url || (user =~ "12345" && category =~ "0123456789") || (user = "23456789" && category =~ "23456789") || (title =~ "23456789" && user =~ "12345" && category = "0123456788") || (title = "0123456789" && link = "http://www.abc.def"^^tt:url && user =~ "23456789") || (title =~ "9876543210" && user = "23456789") || (title = "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_user := user, v_category := category => @linkedin.get_profile, ((industry =~ "12345" && specialties = "ABCDEFGHIJ" && contains(positions, v_user)) || (formatted_name =~ "ABCDEFGHIJ" && headline =~ "ABCDEFGHIJ" && specialties = v_category && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || industry =~ "23456789" || (formatted_name = v_category && industry =~ v_user) || (industry =~ "23456789" && specialties = v_category && contains(positions, "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "9876543210") || (title = "0123456788" && link = "http://www.google.com"^^tt:url && user =~ "0123456788" && category = "0123456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @linkedin.get_profile(), ((formatted_name =~ "12345" && specialties = "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @linkedin.get_profile, headline =~ "0123456789" , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    now => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @light-bulb.alert_long() ;
}
====
@slack.receive, ((timestamp = "0123456789" && from = "bob"^^tt:username) || (timestamp = "9876543210" && message = "12345") || (timestamp =~ "12345" && message = "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((channel = "lol"^^tt:hashtag && timestamp = "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((estimate <= 2h || uber_type = "ABCDEFGHIJ" || (uber_type = "ABCDEFGHIJ" && estimate > 2h) || (uber_type =~ "0123456789" && estimate <= 2h))) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => @imgflip.list, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || name =~ "12345") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), ((name =~ "23456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @almond_dates.search, ((interest =~ "23456789" && message =~ "ABCDEFGHIJ") || interest = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (interest =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @heatpad.set_power(power=enum(off)) ;
}
====
* => @onedrive.upload_picture, (file_name = "0123456789" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (file_name =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((temperature >= 73F && wind_speed < 25mph && cloudiness <= 7 && weather = "0123456788") || (location = $context.location.work && temperature = 22C && humidity >= 11 && cloudiness > 7 && fog <= 14 && weather = "12345"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @onedrive.upload_picture(file_name="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @gmail.send_picture, message =~ "0123456788" 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="9876543210", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@sleep-tracker.getsleep, ((duration < 30min && light < 30min && deep > 2h) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && awakeTime = makeDate(1487030400000) && duration = 30min && rem = 2h && light > 30min && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration < 2h && rem <= 30min && light > 2h && deep <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1487030400000) && duration <= 30min && rem = 30min && light > 2h && deep < 30min) || (awakeTime = makeDate(1487030400000) && rem > 2h && deep = 30min) || (place = $context.location.current_location && light = 2h && deep > 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @phone.get_gps(), ((altitude > 42cm && bearing >= 11 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
* => @gmail.get_latest, ((subject =~ "23456789" && label =~ "12345") || (from = "bob@stanford.edu"^^tt:email_address && label = "12345")) , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @rss.new_post(), ((title = "0123456788" || title =~ "ABCDEFGHIJ")) , v_title := title, v_link := link => @gmail.get_latest(), ((subject =~ "ABCDEFGHIJ" && label =~ "0123456789")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => * => @builtin.thank_you, true 
;;
Main() {
    @icalendar.event_begin(), (((summary =~ "9876543210" && description =~ "ABCDEFGHIJ" && organizer =~ "9876543210" && location = "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && description = "0123456789" && organizer =~ "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thecatapi.get(), (((count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || image_id = "9876543210")) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @builtin.thank_you() ;
}
====
* => @speaker.set_sink, true 
;;
Main() {
    now => @speaker.set_sink() ;
}
====
* => @xkcd.get_comic, (number >= 11 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (number <= 11 && alt_text = "0123456788") || (number < 42 && alt_text = "0123456789"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @slack.setPresence(presence=enum(away)) ;
}
====
@jawbone_up.getbody, (place = $context.location.work && leanMass = 42 && bodyFat > 55 && bmi < 14) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @jawbone_up.getbody(), ((time = makeDate(1487030400000) && leanMass = 14 && bodyFat >= 55 && bmi > 42)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
@xkcd.new_comic, ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 14 && title = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @xkcd.new_comic(), ((number >= 55 && title =~ "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @slack.setPresence(presence=enum(auto)) ;
}
====
@yahoofinance.stock_quote, (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "ABCDEFGHIJ" && bid_price >= 11) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), true , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
now => * => @dropbox.move, (old_name =~ "0123456789" || new_name =~ "ABCDEFGHIJ" || new_name =~ "9876543210") 
;;
Main() {
    now => @ytranslate.detect_language(text="23456789"), ((text =~ "0123456789" || (text = "23456789" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || (text = "0123456789" && detected_language = "it"^^tt:iso_lang_code("Italian")))) , v_detected_language := detected_language => @dropbox.move(old_name="0123456788", new_name="12345") ;
}
====
* => @onedrive.delete_file, file_name = "23456789" 
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_partisan)), (section = enum(the_fix)) , v_title := title, v_link := link => @onedrive.delete_file(file_name=v_title) ;
}
====
@phdcomics.new_post, title =~ "0123456789" , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @facebook.post(status=v_title) ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && label = "0123456789") || (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") || from = "bob@stanford.edu"^^tt:email_address || label = "23456789" || subject = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @gmail.receive_email(), ((subject = "12345" && snippet = "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @gmail.get_latest(), (((from = "alice@gmail.com"^^tt:email_address && label = "9876543210") || (from = "alice@gmail.com"^^tt:email_address && subject = "12345") || (subject = "23456789" && label =~ "23456789") || from = "alice@gmail.com"^^tt:email_address)) , v_from := from, v_subject := subject, v_label := label => @phone.call_emergency() ;
}
====
* => @thermostat.get_humidity, (value <= 7 || value = 11 || value < 55 || value < 11) , v_value := value => notify
;;
Main() {
    now => @thermostat.get_humidity(), ((value >= 11 || value >= 55)) , v_value := value => notify;
}
====
* => @bing.image_search, (link = "http://www.google.com"^^tt:url && width >= 7 && height <= 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1487030400000) && distance < 1000m && steps >= 55 && inactiveTime >= 2h && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @bing.image_search(query=v_day), ((query =~ "0123456788" && title =~ "23456789" && link = "http://www.abc.def"^^tt:url && width > 14 && height > 11)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => @dropbox.open, true , v_url := url => @speaker.raise_volume, true 
;;
Main() {
    now => @dropbox.open(file_name="23456789"), (file_name =~ "12345") , v_url := url => @speaker.raise_volume() ;
}
====
* => @parklon_heatpad.set_power, true 
;;
Main() {
    @youtube.source(), ((channel_title = "9876543210" && video_id =~ "0123456789" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @twitter.search_by_hashtag, ((count > 11 && text =~ "9876543210" && contains(urls, "http://www.abc.def"^^tt:url)) || (count = 42 && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, count=42), ((count > 42 && text = "9876543210" && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @lg_webos_tv.raise_volume() ;
}
====
@weatherapi.monitor, (wind_speed < 5kmph && humidity >= 7 && cloudiness >= 14 && fog < 7 && status = enum(cloudy)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((wind_speed >= 25mph && humidity < 14 && cloudiness < 11 && status = enum(drizzling)) || (location = $context.location.current_location && temperature < 73F && wind_speed <= 5kmph && humidity < 11 && cloudiness < 11 && weather = "0123456788"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @imgflip.generate(template="0123456788", text_top="12345", text_bottom="12345"), ((template = "0123456789" && name = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, ((distance >= 42cm && steps = 14 && activeTime < 30min && calories < 500kcal) || (updateTime = makeDate(1487030400000) && distance >= 1000m && activeTime < 2h && inactiveTime < 2h && calories < 500kcal) || (updateTime = makeDate(1462320000000) && distance < 42cm && steps > 14 && inactiveTime > 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((distance >= 1000m && steps = 42 && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @fitbit.getbody(), ((weight >= 82kg && bmi <= 42 && fat > 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@almond_couch_market.monitor, (price >= 11 || (price > 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 55 || (title =~ "ABCDEFGHIJ" && price < 42) || price <= 7)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @instagram.get_pictures, (link = "http://www.abc.def"^^tt:url && filter = "9876543210") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @onedrive.create_file, true 
;;
Main() {
    now => @instagram.get_pictures(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @onedrive.create_file(file_name=v_filter, contents="12345") ;
}
====
now => * => @slack.send, message = "0123456788" 
;;
Main() {
    now => @imgflip.generate(template="0123456788", text_top="23456789", text_bottom="12345"), ((text_top = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @slack.send(channel=v_name, message=v_name) ;
}
====
* => @almond_bike_market.post, ((price < 7 && poster =~ "23456789") || (title =~ "9876543210" && price = 11) || (title =~ "9876543210" && price <= 11 && poster = "0123456788") || (price >= 7 && info = "12345" && poster = "0123456789")) 
;;
Main() {
    @wall_street_journal.lifestyle(), (title =~ "0123456788") , v_title := title, v_link := link => @almond_bike_market.post(title=v_title, info=v_title, poster=v_title, price=11) ;
}
====
now => * => @fitbit.recordweight, (weight = 82kg || weight >= 155lb || weight >= 155lb) 
;;
Main() {
    now => @dropbox.get_space_usage(), (used >= 5KB) , v_used := used, v_total := total => @fitbit.recordweight(weight=82kg) ;
}
====
now => * => @slack.send_picture, true 
;;
Main() {
    now => @bing.image_search(query="12345"), (((link = "http://www.google.com"^^tt:url && height = 11) || (title = "12345" && link = "http://www.google.com"^^tt:url && width <= 7 && height > 14))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @slack.send_picture(channel="lol"^^tt:hashtag, caption="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@xkcd.new_comic, (number < 7 && title = "12345") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @omlet.send_picture, true 
;;
Main() {
    @xkcd.new_comic(), ((number >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @omlet.send_picture(to=v_title, picture_url=v_title, caption=v_title) ;
}
====
@weatherapi.monitor, ((temperature <= 73F && wind_speed >= 25mph && humidity = 55 && fog > 42 && status = enum(snowy)) || (cloudiness < 11 && fog >= 7 && status = enum(raining)) || (location = $context.location.current_location && wind_speed <= 25mph && humidity > 14 && fog > 7 && status = enum(sleety)) || (temperature > 22C && wind_speed >= 25mph && humidity <= 55 && cloudiness <= 14 && fog = 14 && weather = "23456789" && status = enum(drizzling)) || (wind_speed <= 5kmph && humidity = 42 && cloudiness <= 14 && fog = 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((location = $context.location.current_location && temperature < 22C && cloudiness > 42 && fog > 42 && status = enum(cloudy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @bing.web_search(query=v_weather), ((description = "0123456788" || query =~ "12345" || (query =~ "0123456788" && description =~ "0123456789"))) , v_title := title, v_description := description, v_link := link => notify;
}
====
* => @thecatapi.get, (count < 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (count <= 42) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @instagram.get_pictures, (count <= 55 && caption = "0123456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @bluetooth_speaker.lower_volume() ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @phone.get_gps(), ((altitude > 1000m && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @security-camera.set_power(power=enum(off)) ;
}
====
@instagram.new_picture, (caption =~ "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter =~ "0123456788" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter =~ "9876543210" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((filter =~ "12345" || (caption =~ "12345" && link = "http://www.abc.def"^^tt:url && filter =~ "0123456789"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
now => * => @almond_couch_market.post, title = "0123456788" 
;;
Main() {
    now => @phone.get_gps(), (bearing >= 55) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.post(price=v_bearing, title="9876543210", contact="+16501234567"^^tt:phone_number) ;
}
====
@heartrate-monitor.getheartrate, ((time = makeDate(1487030400000) && heartrate >= 55) || heartrate > 42) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @linkedin.get_profile(), (((formatted_name = "12345" && headline =~ "ABCDEFGHIJ" && contains(positions, "9876543210")) || (headline = "23456789" && industry =~ "9876543210") || (specialties =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@google_drive.new_drive_file, file_name =~ "0123456788" , v_file_name := file_name => @thermostat.get_hvac_state, true , v_mode := mode, v_state := state => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => notify;
}
====
now => * => @lg_webos_tv.set_power, true 
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(sports)), true , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @lg_webos_tv.set_power(power=enum(off)) ;
}
====
* => * => @phone.notify, ((title = "23456789" && message =~ "12345") || (title =~ "0123456788" && message =~ "12345")) 
;;
Main() {
    @xkcd.new_whatif(), (title =~ "0123456789") , v_title := title, v_link := link => @phone.notify(title="12345", message="23456789") ;
}
====
@onedrive.file_modified, (file_name =~ "23456789" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "0123456789") , v_file_name := file_name => @youtube.channels_by_category(category_id=enum(comedy)), (((channel_id =~ "0123456789" && title = "23456789" && description = "ABCDEFGHIJ") || (channel_id =~ "0123456788" && title = "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
now => @builtin.debug_log, message =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @builtin.debug_log(message="0123456788") ;
}
====
* => @almond_dates.post, (interest =~ "23456789" && message = "9876543210" && poster =~ "0123456789") 
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @almond_dates.post(message=v_message, poster=v_message, phone=v_message, interest="ABCDEFGHIJ") ;
}
====
now => @holidays.next_us_holiday_event, (summary =~ "ABCDEFGHIJ" && description = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1462320000000) && description =~ "23456789") || (date = makeDate(1462320000000) && description = "ABCDEFGHIJ") || summary =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @almond_couch_market.search, ((price > 42 && contact = "+15551234567"^^tt:phone_number) || title = "0123456788" || price >= 14) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (price <= 55) , v_title := title, v_price := price, v_contact := contact => @twitter.unfollow(user_name=v_title) ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @security-camera.set_power(power=enum(off)) ;
}
====
* => @icalendar.list_events, ((summary =~ "ABCDEFGHIJ" && sequence >= 7) || (start_date = makeDate(1462320000000) && summary = "23456789" && description = "ABCDEFGHIJ" && sequence > 55 && organizer = "12345") || (summary =~ "0123456788" && sequence < 55 && organizer = "0123456789" && location = "0123456789") || (start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && sequence < 14) || (description =~ "ABCDEFGHIJ" && location = "ABCDEFGHIJ") || (summary =~ "23456789" && description = "23456789" && sequence > 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && location =~ "12345") || (end_date = makeDate(1487030400000) && description = "0123456789" && sequence >= 11 && organizer = "12345" && location = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @bing.image_search, (query =~ "ABCDEFGHIJ" && title =~ "0123456788" && width <= 55 && height = 55) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @slack.receive(), ((timestamp = "0123456788" && message =~ "9876543210")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @bing.image_search(query=v_message), (((width < 42 && height <= 7) || (query = "23456789" && height < 7) || (title = "0123456788" && width > 14 && height > 7) || (query =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 11 && height < 14) || (query = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width < 14))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@sportradar.soccer_eu_team, ((other_team_alias = "bar"^^sportradar:eu_soccer_team("Barcellona") && home_name = "23456789" && scheduled_time = makeDate(1462320000000) && away_points <= 11 && home_points <= 11) || (watched_is_home = true && home_name =~ "23456789" && scheduled_time = makeDate(1487030400000) && away_points >= 7 && home_points < 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @dropbox.open, file_name = v_home_name , v_url := url => notify
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="juv"^^sportradar:eu_soccer_team("Juventus")), (((watched_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && watched_is_home = true && away_name = "9876543210" && away_points = 11 && home_points = 42 && result = enum(draw)) || (other_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && away_name =~ "0123456789" && home_name =~ "12345" && home_points >= 42) || (watched_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && watched_is_home = false && away_name = "9876543210" && away_points >= 7))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @dropbox.open(file_name="ABCDEFGHIJ"), ((url =~ "12345" || file_name =~ "0123456789" || url =~ "0123456789" || url = "9876543210")) , v_url := url => notify;
}
====
* => @slack.setPresence, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate > 42 || (time = makeDate(1462320000000) && place = $context.location.work) || heartrate <= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => @slack.setPresence(presence=enum(away)) ;
}
====
now => @almond_dates.search, message =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (((interest =~ "12345" && poster = "9876543210") || (interest =~ "ABCDEFGHIJ" && message =~ "ABCDEFGHIJ"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @github.comment_issue(repo_name=v_message, issue_number=7, body="0123456789") ;
}
====
now => @almond_dates.post, (message = "ABCDEFGHIJ" || poster = "12345" || (interest = "0123456788" && message = "0123456788")) 
;;
Main() {
    now => @almond_dates.post(interest="0123456788", message="12345", poster="0123456789", phone="+15551234567"^^tt:phone_number) ;
}
====
@activity-tracker.getmove, (day = "23456789" && distance < 1000m && steps < 42 && inactiveTime < 30min && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && day =~ "12345" && distance > 42cm && steps = 11 && inactiveTime > 2h && calories < 500kcal) || (distance > 42cm && steps > 7 && activeTime < 30min && inactiveTime <= 30min && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
@activity-tracker.getmove, ((distance < 42cm && steps >= 55 && activeTime < 30min) || (distance > 42cm && steps <= 55 && activeTime > 30min && calories >= 500kcal) || (inactiveTime <= 30min && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day =~ "9876543210" && distance > 42cm && steps > 7 && activeTime >= 30min && inactiveTime < 2h) || (updateTime = makeDate(1487030400000) && day =~ "12345" && distance >= 42cm && activeTime < 30min && inactiveTime = 30min && calories < 500kcal) || (distance > 1000m && activeTime >= 30min && inactiveTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && day = "9876543210" && steps <= 7 && activeTime > 30min && inactiveTime <= 30min && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @heatpad.set_power(power=enum(off)) ;
}
====
* => @wall_street_journal.get_opinions, ((title = "12345" && updated = makeDate(1462320000000)) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1487030400000)) || title =~ "0123456789" || (title =~ "ABCDEFGHIJ" && updated = makeDate(1462320000000)) || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || (title = "0123456788" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000)) || (title =~ "12345" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), ((stock_id = "aapl"^^tt:stock_id("Apple") && bid_price >= 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @wall_street_journal.get_opinions(), ((link = "http://www.google.com"^^tt:url || (title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@youtube.source, (title =~ "23456789" || (channel_id = "9876543210" && title = "12345" && video_url = "http://www.google.com"^^tt:url) || (channel_title =~ "9876543210" && video_id = "ABCDEFGHIJ") || (channel_id =~ "9876543210" && channel_title = "23456789" && video_id =~ "0123456788") || channel_id =~ "23456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify
;;
Main() {
    @youtube.source(), ((channel_id = "12345" && title =~ "9876543210")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@phdcomics.new_post, (title =~ "0123456788" || title =~ "12345") , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title =~ "0123456788" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @fitbit.getsteps(), (steps >= 55) , v_date := date, v_steps := steps => notify;
}
====
@yahoofinance.stock_quote, (company_name =~ "0123456788" && bid_price <= 42) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), ((stock_id = "aapl"^^tt:stock_id("Apple") && bid_price <= 11)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
* => * => @onedrive.delete_file, file_name = "0123456788" 
;;
Main() {
    @holidays.uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @onedrive.delete_file(file_name=v_summary) ;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    now => @linkedin.get_profile(), ((formatted_name =~ "23456789" && headline =~ "23456789" && industry = "0123456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @car.honk_horn() ;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "12345" && category =~ "0123456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @twitter.send_direct_message(to=v_title, message="9876543210") ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && link = "http://www.google.com"^^tt:url) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    @washington_post.new_article(section=enum(national)), ((section = enum(business) || (title =~ "0123456788" && description =~ "ABCDEFGHIJ"))) , v_title := title, v_link := link, v_description := description => @wall_street_journal.get_opinions(), ((updated = makeDate(1462320000000) || title = "ABCDEFGHIJ" || title = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @almond_bike_market.search, ((info =~ "12345" && price >= 11) || price > 55 || (info =~ "ABCDEFGHIJ" && price < 14) || (info =~ "0123456788" && price < 14) || price > 55 || (info = "12345" && price = 42) || price >= 14)  => notify
;;
Main() {
    now => @almond_bike_market.search(price=14), ((info =~ "0123456789" && price >= 55))  => notify;
}
====
@instagram.new_picture, ((caption = "0123456789" && filter =~ "0123456789" && location = $context.location.home) || (caption = "0123456789" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), (filter =~ "23456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @twitter.sink(status=v_caption) ;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1487030400000) && summary =~ "23456789" && description =~ "ABCDEFGHIJ" && sequence >= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1487030400000) && organizer = "0123456788") || (end_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && sequence <= 11 && organizer = "9876543210" && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @holidays.us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @icalendar.list_events(), ((summary =~ "9876543210" && sequence < 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @tumblr-blog.post_picture(caption=v_summary, picture_url=v_description) ;
}
====
@holidays.us_holiday_event, (date = makeDate(1462320000000) && description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => * => *
;;
Main() {
    @holidays.us_holiday_event(), (description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => @twitter.search_by_hashtag(query_hashtag=v_summary, from=v_description, in_reply_to=v_description, count=55), (((count <= 55 && text = "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username) || (text = "23456789" && contains(hashtags, "funny"^^tt:hashtag)) || (query_hashtag = "cat"^^tt:hashtag && count <= 7 && text = "23456789" && contains(urls, "http://www.google.com"^^tt:url)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @bluetooth_speaker.set_sink() ;
}
====
@almond_bike_market.monitor, ((info = "0123456788" && price <= 55) || (info =~ "9876543210" && price <= 55))  => *
;;
Main() {
    @almond_bike_market.monitor(price=14), ((info =~ "ABCDEFGHIJ" && price <= 42))  => @omlet.send_to(to="alice"^^tt:username, message="9876543210") ;
}
====
* => @tumblr-blog.post_text, (body = "9876543210" || title =~ "ABCDEFGHIJ" || (title =~ "0123456789" && body =~ "12345") || body = "ABCDEFGHIJ") 
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @tumblr-blog.post_text(title=v_title, body=v_title) ;
}
====
now => * => @almond_dates.post, (message = "23456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_dates.post(interest="0123456789", message="0123456789", poster="23456789", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @almond_couch_market.search, ((title =~ "23456789" && price <= 11 && contact = "+15551234567"^^tt:phone_number) || price >= 11) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (((title =~ "9876543210" && price < 14 && contact = "+15551234567"^^tt:phone_number) || contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @xkcd.get_comic(number=11), (((number >= 42 && title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 7 && title = "0123456788") || (number < 42 && link = "http://www.abc.def"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text = "12345") || (number < 14 && title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @linkedin.share(status="0123456789") ;
}
====
now => @builtin.get_date, (date = makeDate(1462320000000) || date = makeDate(1487030400000)) , v_date := date => *
;;
Main() {
    now => @builtin.get_date(), true , v_date := date => @builtin.notify(message="0123456789") ;
}
====
now => * => @facebook.post_picture, caption =~ "9876543210" 
;;
Main() {
    now => @security-camera.get_snapshot(), true , v_picture_url := picture_url => @facebook.post_picture(picture_url=v_picture_url, caption="0123456789") ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), (phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @bluetooth_speaker.raise_volume() ;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@slack.receive, ((from = "alice"^^tt:username && message = "9876543210") || (channel = "lol"^^tt:hashtag && timestamp = "0123456789" && from = "bob"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), ((timestamp =~ "23456789" && from = "bob"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @youtube.source(), (title =~ "23456789") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @heatpad.set_power(power=enum(on)) ;
}
====
now => * => @slack.updateChannelTopic, (channel = "cat"^^tt:hashtag && topic = "12345") 
;;
Main() {
    now => @almond_bike_market.search(price=55), (((info = "0123456789" && price > 14) || price < 7 || price <= 11))  => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="0123456789") ;
}
====
@nasa.asteroid, (asteroid_id >= 14 && name =~ "0123456788" && estimated_diameter_min = 1000m && estimated_diameter_max <= 42cm && dangerous = true && relative_velocity >= 25mph && orbiting_body = "9876543210") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id >= 7 && name =~ "9876543210")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @builtin.get_date(), true , v_date := date => notify;
}
====
* => @phone.call, number = "+16501234567"^^tt:phone_number 
;;
Main() {
    @jawbone_up.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @phone.call(number="+15551234567"^^tt:phone_number) ;
}
====
* => @bing.image_search, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && height = 7) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @builtin.at(time=makeTime(7,30)), ((time = makeTime(15,0) || time = makeTime(15,0)))  => @bing.image_search(query="12345"), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 42 && height < 7) || (title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width > 42 && height <= 7) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 14))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @icalendar.list_events, (summary =~ "0123456789" && sequence > 55 && location = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @almond_bike_market.monitor(), (price <= 42)  => @icalendar.list_events(), (((sequence >= 55 && location =~ "23456789") || (description = "23456789" && sequence < 14 && organizer = "12345" && location =~ "12345") || (description =~ "ABCDEFGHIJ" && sequence < 14 && organizer =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @uber.time_estimate, (uber_type =~ "23456789" && estimate <= 30min) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), (uber_type =~ "0123456789") , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @almond_dates.post, (interest = "0123456788" && poster =~ "9876543210") 
;;
Main() {
    @phdcomics.new_post(), ((title =~ "12345" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_dates.post(interest=v_title, message=v_title, poster=v_title, phone=v_title) ;
}
====
* => @bing.web_search, (title = "9876543210" && description =~ "0123456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_description := description, v_link := link => *
;;
Main() {
    @xkcd.new_whatif(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => @bing.web_search(query=v_title), ((query = "12345" && description =~ "12345" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_description := description, v_link := link => notify;
}
====
@reddit_front_page.newpost, (title =~ "23456789" && link = "http://www.abc.def"^^tt:url && user = "23456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), ((title = "12345" && user =~ "0123456789" && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @github.comment_issue(repo_name=v_user, body=v_user, issue_number=14) ;
}
====
* => @almond_dates.post, (interest =~ "9876543210" && phone = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @thermostat.temperature(), (value >= 73F) , v_time := time, v_value := value => @almond_dates.post(interest="23456789", message="23456789", poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
@xkcd.new_comic, (number <= 7 || (number >= 7 && title = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), ((title = "0123456788" && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
now => @giphy.get, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @speaker.play_music() ;
}
====
now => @dropbox.open, ((file_name = "23456789" && url =~ "0123456788") || file_name =~ "12345") , v_url := url => notify
;;
Main() {
    now => @dropbox.open(file_name="ABCDEFGHIJ"), true , v_url := url => notify;
}
====
* => * => @facebook.post, (status = "12345" || status =~ "9876543210" || status =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @fitbit.getsteps(), (steps < 42) , v_date := date, v_steps := steps => @facebook.post(status="12345") ;
}
====
@builtin.at, (time = makeTime(20,30) || time = makeTime(20,30) || time = makeTime(7,30))  => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => notify;
}
====
@yahoofinance.stock_quote, (ask_price > 7 || (ask_price < 42 && bid_price < 11) || ask_price > 7 || bid_price <= 14 || (company_name =~ "23456789" && ask_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && ask_price > 14 && bid_price <= 7) || (stock_id = "goog"^^tt:stock_id("Google") && company_name = "12345" && ask_price <= 7 && bid_price > 42) || (stock_id = "goog"^^tt:stock_id("Google") && bid_price > 7) || (ask_price < 11 && bid_price < 14) || bid_price >= 14) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), ((company_name =~ "23456789" && ask_price < 7 && bid_price = 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => notify;
}
====
@icalendar.event_begin, ((sequence >= 55 && organizer =~ "ABCDEFGHIJ") || (start_date = makeDate(1462320000000) && organizer =~ "12345" && location = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1487030400000) && description = "0123456788" && sequence > 55 && organizer = "9876543210" && location = "0123456788") || (end_date = makeDate(1487030400000) && summary =~ "0123456789") || (sequence < 11 && organizer =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @lg_webos_tv.lower_volume() ;
}
====
now => * => @almond_bike_market.post, (price = 42 && info =~ "0123456788") 
;;
Main() {
    now => @almond_bike_market.post(title="23456789", price=14, info="23456789", poster="ABCDEFGHIJ") ;
}
====
now => * => @tumblr-blog.post_picture, caption = "0123456788" 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @tumblr-blog.post_picture(picture_url=v_description, caption="9876543210") ;
}
====
* => @almond_couch_market.search, (title = "23456789" || price = 42 || (title = "9876543210" && price >= 42) || price = 42 || title =~ "12345" || (title =~ "0123456789" && price < 55)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @twitter.source(), true , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((price >= 42 || (title =~ "9876543210" && price > 7))) , v_title := title, v_price := price, v_contact := contact => @fitbit.getsteps(), (steps <= 14) , v_date := date, v_steps := steps => notify;
}
====
* => @security-camera.get_url, link = "http://www.google.com"^^tt:url , v_link := link => notify
;;
Main() {
    @slack.receive(), ((channel = "lol"^^tt:hashtag && timestamp = "12345" && message = "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => notify;
}
====
now => * => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && message = "ABCDEFGHIJ") 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag), (((text = "0123456788" && from = "alice"^^tt:username) || (count < 42 && text =~ "23456789" && from = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="ABCDEFGHIJ") ;
}
====
@yahoofinance.stock_div, (company_name = "12345" && yield <= 7 && div <= 42) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), (((yield >= 11 && div > 7 && ex_div_date = makeDate(1462320000000)) || (company_name =~ "0123456788" && yield <= 55 && div < 42 && pay_date = makeDate(1487030400000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @almond_dates.search(), (((message =~ "12345" && poster =~ "9876543210") || (message =~ "9876543210" && poster = "9876543210"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @speaker.play_music, true 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "0123456788" || interest =~ "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @speaker.play_music() ;
}
====
@rss.new_post, ((title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || title = "12345" || title =~ "23456789" || title =~ "0123456789" || link = "http://www.abc.def"^^tt:url || title = "ABCDEFGHIJ" || title = "23456789") , v_title := title, v_link := link => *
;;
Main() {
    @rss.new_post(), (((title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456789")) , v_title := title, v_link := link => @dropbox.create_new_folder(folder_name=v_title) ;
}
====
@slack.receive, ((timestamp =~ "9876543210" && from = "alice"^^tt:username && message = "ABCDEFGHIJ") || (channel = "cat"^^tt:hashtag && timestamp = "23456789" && message = "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (from = "alice"^^tt:username) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @dropbox.open(file_name=v_timestamp), true , v_url := url => notify;
}
====
now => @gmail.get_latest, subject =~ "0123456789" , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && label = "9876543210")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@almond_dates.monitor, ((message =~ "ABCDEFGHIJ" && poster = "0123456788") || (interest =~ "0123456788" && message = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_dates.post, (poster =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @almond_dates.monitor(), (((message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "23456789" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_dates.post(message=v_poster, poster=v_message, phone=v_interest, interest="9876543210") ;
}
====
now => @xkcd.get_comic, ((number < 7 && title = "23456789" && alt_text = "23456789") || (number < 14 && title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number >= 11 && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(number=14), ((number > 42 && title =~ "23456789" && link = "http://www.abc.def"^^tt:url && alt_text = "0123456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => @gmail.send_picture, subject = "12345" 
;;
Main() {
    now => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="0123456788", message="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @ytranslate.detect_language, (text = "23456789" && detected_language = "en"^^tt:iso_lang_code("English")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="0123456788"), (((text = "0123456789" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || text = "23456789")) , v_detected_language := detected_language => notify;
}
====
@washington_post.new_article, (link = "http://www.abc.def"^^tt:url && description = "ABCDEFGHIJ") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (((title = "0123456789" && description = "ABCDEFGHIJ") || (section = enum(local) && title =~ "12345"))) , v_title := title, v_link := link, v_description := description => @dropbox.create_new_folder(folder_name="ABCDEFGHIJ") ;
}
====
* => @icalendar.list_events, (start_date = makeDate(1462320000000) && description = "12345" && sequence > 7) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && summary = "0123456789" && description =~ "23456789" && sequence >= 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @twitter.post_picture(caption=v_location, picture_url=v_description) ;
}
====
@almond_bike_market.monitor, true  => @linkedin.share, true 
;;
Main() {
    @almond_bike_market.monitor(price=14), (info =~ "0123456789")  => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
@holidays.us_holiday_event, (summary =~ "12345" && description = "23456789") , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), (description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@instagram.new_picture, filter = "23456789" , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((filter =~ "23456789" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "12345") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @phone.get_gps(), (((altitude > 42cm && bearing >= 11) || (altitude <= 1000m && bearing < 11 && speed >= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @weatherapi.current, (wind_speed >= 5kmph && cloudiness >= 14) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature > 73F && humidity <= 14 && cloudiness < 7 && fog <= 42) || (temperature <= 22C && wind_speed < 5kmph && cloudiness <= 42 && fog >= 7 && weather =~ "0123456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
* => @phone.set_ringer, (mode = enum(silent) || mode = enum(normal) || mode = enum(silent) || mode = enum(silent)) 
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), ((watched_is_home = false && away_name = "9876543210" && game_status = enum(scheduled))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @phone.set_ringer(mode=enum(silent)) ;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight <= 155lb) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), ((weight <= 155lb || (time = makeDate(1487030400000) && weight > 155lb))) , v_time := time, v_weight := weight => @thecatapi.get(), (count = 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @fitbit.recordweight(weight=155lb) ;
}
====
@instagram.new_picture, (link = "http://www.abc.def"^^tt:url && filter = "0123456789" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @dropbox.list_folder(folder_name=v_filter), ((folder_name = "23456789" && file_name = "12345" && file_size <= 5KB && full_path =~ "12345")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @omlet.send_picture, caption = "ABCDEFGHIJ" 
;;
Main() {
    now => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
* => @almond_couch_market.search, (title =~ "9876543210" && price > 55 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((place = $context.location.work && awakeTime = makeDate(1462320000000) && rem > 2h && light <= 2h && deep <= 30min) || (place = $context.location.current_location && awakeTime = makeDate(1462320000000) && duration < 30min && light < 30min) || (time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration < 30min && rem >= 2h && deep <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @almond_couch_market.search(), ((title = "0123456788" && price >= 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @almond_bike_market.post, (price < 14 && info =~ "23456789" && poster =~ "0123456789") 
;;
Main() {
    now => @almond_bike_market.post(title="0123456788", price=7, info="9876543210", poster="12345") ;
}
====
@reddit_front_page.newpost, (title = "12345" || title =~ "9876543210" || (title =~ "23456789" && user =~ "0123456789")) , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title = "0123456788" && user =~ "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @twitter.search(query=v_category, from=v_user, in_reply_to=v_category, count=55), ((query = "9876543210" && count < 14 && text =~ "ABCDEFGHIJ")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@washington_post.new_article, (section = enum(lifestyle) && description =~ "0123456788") , v_title := title, v_link := link, v_description := description => * => notify
;;
Main() {
    @washington_post.new_article(section=enum(sports)), ((section = enum(national) && title =~ "0123456788")) , v_title := title, v_link := link, v_description := description => @security-camera.get_url(), true , v_link := link => notify;
}
====
* => @builtin.thank_you, true 
;;
Main() {
    @jawbone_up.getsleep(), (((asleepTime = makeDate(1487030400000) && duration < 30min && light < 2h) || (asleepTime = makeDate(1462320000000) && rem <= 30min && light > 2h && deep <= 2h) || (place = $context.location.current_location && duration > 30min && rem >= 30min && deep <= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @builtin.thank_you() ;
}
====
@scale.source, (value >= 82kg || value < 155lb) , v_time := time, v_value := value => @fitbit.recordweight, true 
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value > 155lb)) , v_time := time, v_value := value => @fitbit.recordweight(weight=v_value) ;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1487030400000) && summary =~ "12345" && description =~ "23456789") || (date = makeDate(1487030400000) && summary = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), (((summary = "0123456789" && description =~ "23456789") || date = makeDate(1462320000000))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    now => @holidays.next_uk_holiday_event(), (((date = makeDate(1462320000000) && description = "9876543210") || (date = makeDate(1462320000000) && summary = "0123456788" && description = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => @car.honk_horn() ;
}
====
now => @almond_dates.search, (interest =~ "12345" && message =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((message =~ "ABCDEFGHIJ" || (interest = "ABCDEFGHIJ" && message = "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @github.add_email(email=v_poster) ;
}
====
@thermostat.temperature, value <= 22C , v_time := time, v_value := value => * => notify
;;
Main() {
    @thermostat.temperature(), (((time = makeDate(1462320000000) && value > 22C) || value = 22C)) , v_time := time, v_value := value => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @phone.get_gps, (location = $context.location.work && altitude < 42cm && bearing >= 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), (((altitude < 1000m && bearing <= 7 && speed > 25mph) || (location = $context.location.home && bearing < 55) || (bearing < 14 && speed >= 25mph) || (altitude >= 42cm && bearing > 11 && speed < 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @bluetooth_speaker.lower_volume() ;
}
====
* => @nasa.rover, true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    @rss.new_post(), (((title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || title = "0123456788" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @nasa.rover(), ((count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "0123456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
* => * => @almond_bike_market.post, ((price >= 42 && poster = "0123456789") || title =~ "0123456788") 
;;
Main() {
    @bodytrace.source(), ((weight >= 82kg || weight > 155lb || weight = 155lb)) , v_time := time, v_weight := weight => @builtin.get_random(), true , v_number := number => @almond_bike_market.post(price=v_number, title="0123456789", info="23456789", poster="9876543210") ;
}
====
@activity-tracker.getmove, ((day =~ "0123456789" && distance > 42cm && steps > 14 && inactiveTime >= 30min && calories = 500kcal) || (distance < 42cm && steps > 55 && activeTime >= 2h && inactiveTime > 2h && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((day = "23456789" && activeTime < 30min && inactiveTime = 2h && calories <= 500kcal) || (distance < 1000m && inactiveTime <= 30min && calories > 500kcal) || (activeTime > 2h && inactiveTime >= 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=55), (((query_hashtag = "cat"^^tt:hashtag && count >= 7 && in_reply_to = "bob"^^tt:username) || (query_hashtag = "lol"^^tt:hashtag && text =~ "0123456788" && contains(hashtags, "lol"^^tt:hashtag)))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@almond_couch_market.monitor, ((title = "9876543210" && contact = "+15551234567"^^tt:phone_number) || price <= 14) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "ABCDEFGHIJ" && price <= 42 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @builtin.get_random_between(high=v_price, low=7), ((low < 55 && high >= 7 && random <= 7)) , v_random := random => notify;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    now => @gmail.get_latest(), (from = "alice@gmail.com"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (weight >= 82kg) , v_time := time, v_weight := weight => @almond_bike_market.search(), ((info = "0123456789" || (info =~ "12345" && price > 42)))  => @almond_couch_market.post(title="0123456789", price=42, contact="+16501234567"^^tt:phone_number) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(price=7), ((info =~ "0123456788" || price >= 11))  => @tumblr-blog.post_picture(caption="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@almond_dates.monitor, ((interest =~ "23456789" && message =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (message =~ "0123456788" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @almond_dates.monitor(), (message = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @phone.notify(title=v_poster, message=v_interest) ;
}
====
@github.new_issue, (repo_name = "0123456789" && number >= 11 && title = "12345") , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => *
;;
Main() {
    @github.new_issue(repo_name="0123456789"), (((repo_name = "ABCDEFGHIJ" && user = "bob"^^tt:username && number > 55 && body = "9876543210" && time = makeDate(1487030400000)) || (repo_name = "0123456789" && number = 14 && time = makeDate(1462320000000)) || number < 42)) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => notify;
}
====
@almond_couch_market.monitor, contact = "+16501234567"^^tt:phone_number , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), ((price > 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), (((description =~ "ABCDEFGHIJ" && sequence <= 14 && organizer =~ "0123456789" && location =~ "23456789") || (sequence > 42 && organizer =~ "0123456789" && location =~ "12345") || (organizer = "ABCDEFGHIJ" && location = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @dropbox.move(new_name=v_description, old_name="23456789") ;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 22C && humidity > 55 && cloudiness < 11 && fog > 55 && weather = "23456789" && status = enum(sleety)) || (location = $context.location.work && humidity < 14 && cloudiness > 42 && fog < 14 && weather =~ "0123456789" && status = enum(drizzling)) || (cloudiness > 42 && fog < 7 && weather =~ "9876543210" && status = enum(sleety)) || (temperature <= 73F && wind_speed < 25mph && humidity < 14 && fog > 14 && weather =~ "9876543210") || (temperature >= 22C && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((temperature > 22C && cloudiness > 7 && fog > 55 && weather =~ "12345")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @light-bulb.set_power(power=enum(on)) ;
}
====
@omlet.incomingmessage, (type = enum(picture) && message = "9876543210") , v_feed := feed, v_type := type, v_message := message => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1487030400000) && weight < 155lb && leanMass <= 14 && bodyFat <= 55 && bmi >= 42) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => * => notify
;;
Main() {
    @fitness-tracker.getbody(), (((place = $context.location.current_location && weight >= 82kg && leanMass < 14 && bodyFat >= 42 && bmi <= 7) || (weight <= 82kg && leanMass >= 42 && bmi = 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && label =~ "0123456788") || from = "bob@stanford.edu"^^tt:email_address)) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @xkcd.random_comic, true , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.random_comic(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "23456789") || number = 42)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @builtin.configure(device=v_alt_text) ;
}
====
@builtin.at, time = makeTime(7,30)  => *
;;
Main() {
    @builtin.at(time=makeTime(7,30)), (time = makeTime(7,30))  => @speaker.raise_volume() ;
}
====
* => @phone.get_gps, (location = $context.location.home && altitude <= 42cm && bearing > 7 && speed >= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @instagram.new_picture(), (((contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.current_location) || (link = "http://www.abc.def"^^tt:url && filter = "0123456788" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @phone.get_gps(), ((altitude > 1000m || (bearing = 42 && speed >= 25mph) || (location = $context.location.current_location && altitude <= 42cm && bearing >= 55 && speed >= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@activity-tracker.getmove, (steps < 42 && activeTime = 2h && inactiveTime > 30min && calories > 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance > 1000m && steps >= 14 && activeTime = 30min && inactiveTime > 30min && calories < 500kcal) || (distance > 1000m && steps <= 11 && activeTime < 2h && inactiveTime >= 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @nasa.apod(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title = "ABCDEFGHIJ" && description = "12345"))) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @almond_couch_market.post, (title =~ "12345" && price <= 55 && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @scale.source(), (value > 82kg) , v_time := time, v_value := value => @almond_couch_market.post(title="23456789", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@security-camera.new_event, true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1487030400000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @holidays.next_us_holiday_event(), (((summary =~ "12345" && description = "23456789") || (date = makeDate(1487030400000) && summary =~ "0123456789" && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @almond_bike_market.post, ((title =~ "9876543210" && price > 11 && poster = "0123456788") || (title = "23456789" && price = 42 && info = "ABCDEFGHIJ" && poster = "0123456789") || (title = "9876543210" && info = "0123456788" && poster = "23456789") || (title = "12345" && price = 42 && poster =~ "9876543210")) 
;;
Main() {
    @phdcomics.new_post(), (((title =~ "12345" && link = "http://www.abc.def"^^tt:url) || (title = "0123456788" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_bike_market.post(title="12345", price=14, info="12345", poster="9876543210") ;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @wall_street_journal.lifestyle(), (title = "23456789") , v_title := title, v_link := link => @dropbox.create_new_folder(folder_name="0123456789") ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @rss.new_post(), (((title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || (title =~ "9876543210" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => @security-camera.set_power(power=enum(on)) ;
}
====
@reddit_front_page.newpost, (link = "http://www.google.com"^^tt:url && user = "0123456789" && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title = "9876543210" && link = "http://www.google.com"^^tt:url && category = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
* => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    @sleep-tracker.getsleep(), ((light <= 30min && deep > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@github.new_milestone, (repo_name = "ABCDEFGHIJ" || (user = "alice"^^tt:username && description = "0123456789" && time = makeDate(1462320000000))) , v_user := user, v_description := description, v_time := time => notify
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), true , v_user := user, v_description := description, v_time := time => notify;
}
====
@weatherapi.monitor, (temperature > 73F && cloudiness > 14 && fog = 42 && weather = "0123456788") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature < 73F && wind_speed < 5kmph && cloudiness > 7 && fog > 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @gmail.get_latest(), (subject =~ "0123456789") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @bing.image_search, (query =~ "12345" && title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 14 && height > 42) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="bar"^^sportradar:eu_soccer_team("Barcellona")), ((other_team_alias = "fcb"^^sportradar:eu_soccer_team("Bayern Munchen") && watched_is_home = false && home_name =~ "ABCDEFGHIJ" && home_points < 55 && result = enum(win))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @bing.image_search(query=v_home_name), ((title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && width < 11 && height < 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => @xkcd.random_comic, (title =~ "12345" && alt_text =~ "0123456788") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number > 11 && title =~ "9876543210" && link = "http://www.abc.def"^^tt:url && alt_text = "0123456788")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @lg_webos_tv.set_power, true 
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && summary = "12345")) , v_date := date, v_summary := summary, v_description := description => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
@twitter.my_tweet, (contains(hashtags, "funny"^^tt:hashtag) || text = "9876543210") , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => * => notify
;;
Main() {
    @twitter.my_tweet(), ((contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @builtin.get_random(), ((number >= 55 || number = 42 || number <= 55)) , v_number := number => notify;
}
====
@tumblr.new_text, date = makeDate(1462320000000) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((date = makeDate(1487030400000) && title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && body =~ "12345")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
* => * => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    now => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456788") ;
}
====
* => @ytranslate.detect_language, (detected_language = "zh"^^tt:iso_lang_code("Chinese") || detected_language = "it"^^tt:iso_lang_code("Italian")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="23456789"), (text = "0123456788") , v_detected_language := detected_language => notify;
}
====
@jawbone_up.getmove, ((day = "23456789" && activeTime > 2h && inactiveTime <= 30min && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day =~ "0123456789" && inactiveTime > 30min && calories <= 500kcal) || (steps >= 55 && activeTime > 2h && inactiveTime < 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify
;;
Main() {
    @jawbone_up.getmove(), (((updateTime = makeDate(1487030400000) && distance <= 42cm && inactiveTime = 30min) || (day =~ "0123456789" && distance > 1000m && inactiveTime <= 2h && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day = "12345" && distance <= 1000m && steps > 55 && activeTime >= 2h && inactiveTime >= 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature >= 73F && humidity = 7 && cloudiness = 14 && fog <= 55 && weather =~ "ABCDEFGHIJ") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((temperature < 22C && humidity > 7 && cloudiness >= 42 && fog <= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @twitter.follow(user_name=v_weather) ;
}
====
@phdcomics.new_post, ((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "0123456788" || link = "http://www.google.com"^^tt:url || (title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (title = "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => @light-bulb.alert_long() ;
}
====
@holidays.us_holiday_event, summary = "0123456789" , v_date := date, v_summary := summary, v_description := description => * => *
;;
Main() {
    @holidays.us_holiday_event(), (description = "23456789") , v_date := date, v_summary := summary, v_description := description => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && description =~ "0123456788" && sequence = 11 && organizer =~ "23456789" && location = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file(file_name=v_description) ;
}
====
* => @weatherapi.sunrise, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && sunrise = makeDate(1462320000000)) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @holidays.uk_holiday_event(), (((date = makeDate(1487030400000) && summary = "12345") || date = makeDate(1462320000000) || (date = makeDate(1462320000000) && description = "12345") || (date = makeDate(1487030400000) && description = "0123456789") || (summary =~ "12345" && description =~ "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => @weatherapi.sunrise(location=$context.location.work), (((sunrise = makeDate(1462320000000) && sunset = makeDate(1462320000000)) || (date = makeDate(1487030400000) && sunset = makeDate(1487030400000)))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@sportradar.soccer_us_tourney, ((tournament_full_name =~ "12345" && tournament_league_name = "ABCDEFGHIJ" && away_alias = "12345" && home_name =~ "0123456789" && scheduled_time =~ "9876543210") || (tournament_search_term =~ "23456789" && tournament_full_name =~ "12345" && home_alias = "ABCDEFGHIJ" && away_points < 14 && home_points >= 55)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => * => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="ABCDEFGHIJ"), (((tournament_search_term = "23456789" && home_alias =~ "0123456788" && away_name =~ "ABCDEFGHIJ" && home_name =~ "0123456789" && game_status = enum(scheduled) && scheduled_time = "0123456788" && away_points < 14 && home_points <= 14) || (away_alias = "12345" && away_name = "ABCDEFGHIJ" && home_name = "9876543210" && scheduled_time =~ "9876543210" && home_points > 7) || (tournament_search_term =~ "0123456788" && home_alias =~ "12345" && away_name = "ABCDEFGHIJ" && home_name =~ "23456789" && away_points <= 14))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @dropbox.get_space_usage(), ((total <= 20MB || (used <= 5KB && total >= 20MB) || used > 20MB)) , v_used := used, v_total := total => notify;
}
====
* => * => @dropbox.move, true 
;;
Main() {
    now => @nasa.rover(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used =~ "ABCDEFGHIJ")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @dropbox.move(old_name=v_camera_used, new_name="9876543210") ;
}
====
@google_drive.new_drive_file, file_name =~ "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "23456789") , v_file_name := file_name => @builtin.cool() ;
}
====
* => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1462320000000) && has_sound = false && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || has_sound = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.search(), true , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @phone.get_gps, (altitude < 1000m && bearing >= 42 && speed >= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @lg_webos_tv.unmute, true 
;;
Main() {
    @twitter.my_tweet(), true , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @phone.get_gps(), ((altitude <= 1000m && bearing > 11)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @lg_webos_tv.unmute() ;
}
====
* => @imgflip.generate, (template =~ "23456789" && name =~ "23456789") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="ABCDEFGHIJ", text_top="ABCDEFGHIJ", text_bottom="0123456788"), (((text_top = "9876543210" && text_bottom = "23456789" && name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template =~ "23456789" && text_top = "0123456788"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @phone.get_gps, (location = $context.location.home && altitude >= 1000m && speed < 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.post, ((title =~ "9876543210" && price = 11) || title = "12345" || price < v_bearing) 
;;
Main() {
    now => @phone.get_gps(), ((altitude > 42cm && bearing = 14 && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.post(title="0123456789", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@youtube.source, video_id = "9876543210" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_title = "0123456788" && title =~ "0123456789" && video_id =~ "0123456789" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@activity-tracker.getmove, (day =~ "12345" && distance < 1000m && steps > 14 && inactiveTime < 2h && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1487030400000) && distance < 1000m)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @ytranslate.translate(target_language=v_day, text="0123456789"), ((source_language = "zh"^^tt:iso_lang_code("Chinese") && target_language = "en"^^tt:iso_lang_code("English") && text = "ABCDEFGHIJ")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @youtube.search_videos, ((query = "23456789" && channel_title = "0123456788" && title = "23456789" && count < 11) || (query =~ "12345" && video_id =~ "ABCDEFGHIJ" && channel_id = "9876543210" && count <= 14 && video_url = "http://www.abc.def"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="0123456789"), (((tournament_league_name = "0123456788" && away_alias =~ "12345" && away_name = "12345" && home_name =~ "ABCDEFGHIJ" && game_status = enum(closed) && away_points > 42 && home_points <= 14) || (tournament_search_term =~ "23456789" && home_name =~ "12345" && game_status = enum(scheduled) && scheduled_time = "0123456789" && away_points < 42) || (tournament_search_term =~ "ABCDEFGHIJ" && tournament_league_name = "9876543210" && home_alias = "0123456788" && game_status = enum(closed) && away_points < 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @youtube.search_videos(query=v_home_name), (description =~ "0123456788") , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
now => @weatherapi.sunrise, sunrise = makeDate(1487030400000) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.work), ((sunset = makeDate(1462320000000) || location = $context.location.work)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "0123456789" || price > 55)) , v_title := title, v_price := price, v_contact := contact => @car.honk_horn() ;
}
====
now => * => @omlet.send_to, (message = "ABCDEFGHIJ" || to = "bob"^^tt:username || (to = "bob"^^tt:username && message = "9876543210")) 
;;
Main() {
    now => @thermostat.get_humidity(), true , v_value := value => @omlet.send_to(to="alice"^^tt:username, message="0123456788") ;
}
====
now => @imgflip.list, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => @builtin.hello() ;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @reddit_front_page.newpost(), (user = "0123456788") , v_title := title, v_link := link, v_user := user, v_category := category => @google_drive.create_new_drive_file(file_name="12345") ;
}
====
now => @builtin.get_random_between, (low < 42 && high < 55 && random > 7) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=11, high=42), ((low >= 7 && high > 14)) , v_random := random => notify;
}
====
* => @bing.image_search, (link = "http://www.google.com"^^tt:url && width >= 7 && height <= 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    now => @bing.image_search(query="23456789"), ((query =~ "0123456789" && title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && width < 14 && height > 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
now => * => @speaker.set_volume, percent <= 14 
;;
Main() {
    now => @speaker.set_volume(percent=11) ;
}
====
now => * => @almond_couch_market.post, ((title =~ "9876543210" && price >= 11) || (title = "0123456788" && price >= 11)) 
;;
Main() {
    now => @almond_couch_market.post(title="ABCDEFGHIJ", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
now => @xkcd.get_comic, (number >= 7 && title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(number=42), ((number >= 42 && title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature >= 73F && humidity = 7 && cloudiness = 14 && fog <= 55 && weather =~ "ABCDEFGHIJ") , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((humidity < 14 && cloudiness = 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @giphy.get(), true , v_picture_url := picture_url => @almond_bike_market.post(title="ABCDEFGHIJ", price=55, info="ABCDEFGHIJ", poster="ABCDEFGHIJ") ;
}
====
now => @thecatapi.get, ((image_id =~ "23456789" && count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || image_id =~ "0123456789" || (image_id = "23456789" && count < 11)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), (count > 14) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @omlet.send_to(message=v_image_id, to="alice"^^tt:username) ;
}
====
@instagram.new_picture, ((filter =~ "0123456789" && contains(hashtags, "cat"^^tt:hashtag)) || (contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.current_location) || (caption = "0123456789" && filter = "0123456789" && contains(hashtags, "cat"^^tt:hashtag)) || (caption = "0123456788" && link = "http://www.google.com"^^tt:url && filter =~ "0123456789" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption =~ "0123456788" && link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name =~ "23456789" || file_name = "0123456789" || file_name =~ "0123456789" || file_name = "0123456789") 
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1487030400000) && description = "0123456789" && sequence < 14 && organizer =~ "9876543210") || (end_date = makeDate(1462320000000) && sequence < 14 && location =~ "23456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @google_drive.create_new_drive_file(file_name=v_location) ;
}
====
@heartrate-monitor.getheartrate, (place = $context.location.work && heartrate <= 14) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @thermostat.get_temperature(), true , v_value := value => notify;
}
====
* => @phone.get_gps, (altitude > 1000m && bearing > 14 && speed >= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @jawbone_up.getmove(), (((day =~ "0123456788" && steps >= 7 && activeTime > 2h && inactiveTime > 30min && calories < 500kcal) || (distance >= 42cm && steps >= 7 && activeTime <= 30min && inactiveTime > 2h && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.get_gps(), (((bearing = 7 && speed < 25mph) || (location = $context.location.work && altitude > 42cm && bearing >= 14 && speed <= 25mph) || (altitude < 1000m && speed = 5kmph) || speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@gmail.receive_email, ((from_name =~ "0123456789" && date = makeDate(1487030400000)) || (from_name = "23456789" && snippet =~ "23456789") || date = makeDate(1487030400000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => *
;;
Main() {
    @gmail.receive_email(), ((from_name = "23456789" && subject = "ABCDEFGHIJ" && contains(labels, "0123456788") && snippet = "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @xkcd.random_comic(), (title = "12345") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @xkcd.random_comic, (number > 7 && title = "0123456789" && alt_text =~ "ABCDEFGHIJ") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @dropbox.create_new_folder, folder_name = "12345" 
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && duration < 30min && rem >= 2h && light >= 30min && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @xkcd.random_comic(), ((title =~ "0123456788" && alt_text = "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @dropbox.create_new_folder(folder_name=v_title) ;
}
====
@almond_couch_market.monitor, ((price <= 11 && contact = "+16501234567"^^tt:phone_number) || (title = "9876543210" && price < 11)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), (price <= 14) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @holidays.next_us_holiday_event, (date = makeDate(1462320000000) && summary =~ "0123456788") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary = "ABCDEFGHIJ" || summary =~ "12345")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @bing.image_search, ((query =~ "23456789" && title =~ "9876543210" && height = 14) || (query = "23456789" && title =~ "0123456789" && width <= 55 && height = 7) || (query =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @heatpad.set_power, true 
;;
Main() {
    now => @bing.image_search(query="0123456788"), (((title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (query = "0123456788" && width >= 14) || (title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width >= 7))) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @heatpad.set_power(power=enum(off)) ;
}
====
* => @holidays.next_uk_holiday_event, description =~ "0123456788" , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), (summary = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@twitter.source, (text = "0123456788" && contains(urls, "http://www.google.com"^^tt:url) && from = "alice"^^tt:username && __reserved = false) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.source(), (((text =~ "12345" && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username) || (contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @light-bulb.alert_long() ;
}
====
* => @github.comment_issue, ((repo_name = "ABCDEFGHIJ" && issue_number >= 11 && body = "0123456788") || (issue_number < 11 && body =~ "9876543210") || issue_number > 11) 
;;
Main() {
    now => @github.comment_issue(repo_name="0123456788", issue_number=7, body="ABCDEFGHIJ") ;
}
====
now => @phone.get_gps, (bearing <= 11 && speed <= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((altitude > 1000m && bearing >= 11 && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @onedrive.upload_picture(file_name="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @gmail.send_email, (to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && message =~ "12345")) 
;;
Main() {
    now => @gmail.get_latest(), ((subject = "9876543210" && label =~ "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => @gmail.send_email(to=v_from, message=v_label, subject="ABCDEFGHIJ") ;
}
====
* => @phone.call, (number = "+16501234567"^^tt:phone_number || number = "+16501234567"^^tt:phone_number || number = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @phone.call(number="+16501234567"^^tt:phone_number) ;
}
====
@fitness-tracker.getbody, (leanMass < 11 && bodyFat < 55) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_bike_market.post, ((title =~ "0123456789" && price < 55 && poster =~ "ABCDEFGHIJ") || (price >= v_bodyFat && info =~ "0123456789" && poster =~ "12345")) 
;;
Main() {
    @fitness-tracker.getbody(), ((place = $context.location.work && leanMass >= 42 && bodyFat > 14 && bmi < 55)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @almond_bike_market.post(price=v_leanMass, title="ABCDEFGHIJ", info="ABCDEFGHIJ", poster="0123456789") ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && asleepTime = makeDate(1462320000000) && duration > 30min && rem >= 2h && light > 2h && deep >= 30min) || (asleepTime = makeDate(1462320000000) && duration < 2h && light >= 2h && deep = 2h) || (asleepTime = makeDate(1462320000000) && rem < 2h && light >= 30min && deep > 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @weatherapi.sunrise(location=$context.location.home), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && date = makeDate(1462320000000) && sunrise = makeDate(1462320000000)) || sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
now => @nasa.rover, date_taken = makeDate(1487030400000) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
@phone.receive_sms, (from = "+16501234567"^^tt:phone_number && body =~ "0123456788") , v_from := from, v_body := body => @dropbox.move, true 
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @dropbox.move(old_name=v_body, new_name=v_body) ;
}
====
now => @github.comment_issue, (repo_name =~ "ABCDEFGHIJ" && issue_number <= 11) 
;;
Main() {
    now => @github.comment_issue(repo_name="9876543210", issue_number=7, body="23456789") ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && awakeTime = makeDate(1462320000000) && duration < 30min && rem >= 30min && light = 30min && deep > 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1462320000000) && duration <= 2h && rem < 2h && light > 2h && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @linkedin.get_profile(), (((formatted_name =~ "ABCDEFGHIJ" && industry = "12345" && specialties =~ "0123456789") || (formatted_name = "23456789" && headline = "12345" && specialties = "9876543210") || (formatted_name =~ "23456789" && headline = "ABCDEFGHIJ" && specialties = "12345") || (formatted_name =~ "0123456788" && industry =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @wall_street_journal.get_opinions, link = "http://www.abc.def"^^tt:url , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @omlet.newmessage(), (type = enum(text)) , v_type := type, v_message := message => @wall_street_journal.get_opinions(), (title = "0123456789") , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), (((repo_name =~ "0123456789" && user = "alice"^^tt:username && description =~ "0123456789") || (user = "alice"^^tt:username && time = makeDate(1487030400000)))) , v_user := user, v_description := description, v_time := time => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@instagram.new_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((link = "http://www.abc.def"^^tt:url || (filter = "0123456789" && contains(hashtags, "lol"^^tt:hashtag)) || (filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.work) || (filter = "9876543210" && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @lg_webos_tv.set_volume(percent=11) ;
}
====
@almond_couch_market.monitor, true , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "0123456788" && price < 14)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature > 73F && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 7 && fog < 7 && weather = "0123456788") || (temperature = 73F && wind_speed > 5kmph && fog > 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), ((temperature < 22C && wind_speed <= 5kmph && cloudiness >= 7 && fog > 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
@scale.source, value <= 82kg , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (value < 82kg) , v_time := time, v_value := value => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => notify;
}
====
now => * => @gmail.send_email, (to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && message =~ "12345")) 
;;
Main() {
    now => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="12345") ;
}
====
now => * => @fitbit.recordweight, (weight = 82kg || weight >= 155lb || weight >= 155lb) 
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="0123456789", text_bottom="0123456789"), (((text_top = "0123456789" && text_bottom = "0123456788" && name =~ "0123456788") || (text_top =~ "12345" && text_bottom = "12345" && name = "23456789") || (text_bottom = "ABCDEFGHIJ" && name = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "ABCDEFGHIJ" && text_bottom =~ "0123456788") || (text_top =~ "23456789" && name = "9876543210"))) , v_name := name, v_picture_url := picture_url => @fitbit.recordweight(weight=155lb) ;
}
====
* => * => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || (title =~ "0123456788" && body = "12345") || body =~ "12345") 
;;
Main() {
    @scale.source(), ((value < 155lb || (time = makeDate(1462320000000) && value >= 155lb) || (time = makeDate(1462320000000) && value <= 155lb) || value > 155lb)) , v_time := time, v_value := value => @xkcd.random_comic(), ((number <= 42 && alt_text = "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @tumblr-blog.post_text(title=v_title, body=v_title) ;
}
====
@twitter.direct_message, true , v_from := from, v_message := message, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.direct_message(), (__reserved = false) , v_from := from, v_message := message, v___reserved := __reserved => notify;
}
====
@fitness-tracker.getbody, (time = makeDate(1462320000000) && place = $context.location.current_location && weight > 82kg && leanMass < 7 && bmi >= 7) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @linkedin.share, status =~ "0123456789" 
;;
Main() {
    @fitness-tracker.getbody(), (((weight >= 155lb && leanMass > 7 && bodyFat > 7 && bmi > 7) || (leanMass < 55 && bodyFat >= 14 && bmi < 7) || (time = makeDate(1487030400000) && weight <= 155lb && bmi <= 14) || (weight >= 82kg && bodyFat < 55))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @linkedin.share(status="0123456789") ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && message =~ "ABCDEFGHIJ" && poster =~ "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @uber.price_estimate(start=$context.location.home, end=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((start = $context.location.current_location && end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type =~ "0123456789" && low_estimate < 42 && high_estimate >= 42 && currency_code = "12345" && duration > 2h && distance <= 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @weatherapi.sunrise, ((sunrise = makeDate(1462320000000) && sunset = makeDate(1462320000000)) || date = makeDate(1487030400000) || (location = $context.location.home && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @youtube.source(), ((video_id =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @weatherapi.sunrise(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @fitbit.getbody, (weight > 155lb && fat > 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @gmail.receive_email(), (((subject =~ "0123456789" && snippet = "12345") || (from_name = "12345" && from_address = "bob@stanford.edu"^^tt:email_address))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @fitbit.getbody(), ((weight <= 155lb && bmi < 11 && fat > 11)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@scale.source, value >= 155lb , v_time := time, v_value := value => *
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @twitter.sink(status="0123456788") ;
}
====
* => @security-camera.get_snapshot, true , v_picture_url := picture_url => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) && value >= 82kg)) , v_time := time, v_value := value => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
now => @security-camera.get_url, true , v_link := link => *
;;
Main() {
    now => @security-camera.get_url(), true , v_link := link => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1487030400000) && weight > 155lb) || time = makeDate(1487030400000))) , v_time := time, v_weight := weight => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && label = "12345") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((from = "bob@stanford.edu"^^tt:email_address && label =~ "0123456788") || label = "0123456788")) , v_from := from, v_subject := subject, v_label := label => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
* => @omlet.send_to, (message =~ "9876543210" || message = "12345" || message =~ "12345" || message =~ "0123456789" || to = "alice"^^tt:username || message = "9876543210" || (to = "bob"^^tt:username && message =~ "9876543210")) 
;;
Main() {
    @phdcomics.new_post(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @omlet.send_to(to=v_title, message=v_title) ;
}
====
@heartrate-monitor.getheartrate, time = makeDate(1487030400000) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @almond_couch_market.search, ((title = "0123456788" && price > 42) || (price > 11 && contact = "+16501234567"^^tt:phone_number) || price >= 11 || (title =~ "ABCDEFGHIJ" && price >= 55)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "9876543210" || title = "0123456789")) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @github.comment_issue, ((repo_name = "ABCDEFGHIJ" && issue_number >= 42) || (repo_name =~ "23456789" && issue_number >= 55 && body = "9876543210")) 
;;
Main() {
    now => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => @github.comment_issue(repo_name="ABCDEFGHIJ", issue_number=55, body="12345") ;
}
====
now => @phone.get_gps, ((location = $context.location.work && altitude < 42cm && bearing > 55 && speed > 5kmph) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude >= 1000m && bearing = 42 && speed < 25mph) || (bearing < 55 && speed < 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((altitude < 1000m && bearing <= 14)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@phone.gps, ((altitude > 42cm && speed < 5kmph) || speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), ((altitude <= 42cm && bearing = 55 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @builtin.cool() ;
}
====
@security-camera.new_event, (start_time = makeDate(1462320000000) && has_motion = false && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((has_motion = false && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
now => * => @slack.updateChannelTopic, (topic =~ "23456789" || topic =~ "12345" || (channel = "funny"^^tt:hashtag && topic =~ "ABCDEFGHIJ") || channel = "cat"^^tt:hashtag) 
;;
Main() {
    now => @xkcd.random_comic(), (((number >= 42 && title =~ "0123456789") || (number > 14 && title = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "ABCDEFGHIJ"))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @slack.updateChannelTopic(channel="lol"^^tt:hashtag, topic="9876543210") ;
}
====
* => @almond_dates.search, (phone = "+15551234567"^^tt:phone_number || message = "12345" || (interest =~ "9876543210" && message =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (message = "12345" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @twitter.my_tweet(), ((text = "0123456789" && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @almond_dates.search(), ((interest = "23456789" && message = "23456789" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @instagram.get_pictures, (link = "http://www.google.com"^^tt:url && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(), ((count <= 14 && caption = "0123456788" && filter =~ "12345" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@heartrate-monitor.getheartrate, true , v_time := time, v_place := place, v_heartrate := heartrate => @omlet.send_to, (to = "bob"^^tt:username || to = "alice"^^tt:username) 
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate >= 14) , v_time := time, v_place := place, v_heartrate := heartrate => @omlet.send_to(to="alice"^^tt:username, message="0123456789") ;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && organizer =~ "0123456789" && location =~ "23456789") || (summary = "23456789" && sequence <= 55 && organizer = "0123456788" && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @tumblr.new_text(blog_name="ABCDEFGHIJ"), ((blog_name =~ "23456789" && date = makeDate(1462320000000) && link = "http://www.google.com"^^tt:url && contains(tags, "lol"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @icalendar.list_events(), ((description = "9876543210" && sequence < 42 && organizer =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@almond_couch_market.monitor, (title =~ "12345" && price = 11 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (((title =~ "9876543210" && price = 11) || (title = "9876543210" && price <= 7))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @builtin.configure, true 
;;
Main() {
    @tumblr.new_photo(blog_name="23456789"), ((blog_name =~ "0123456788" && link = "http://www.abc.def"^^tt:url && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @builtin.configure(device=v_caption) ;
}
====
* => @almond_bike_market.post, (title =~ "9876543210" && price <= 55 && poster = "0123456788") 
;;
Main() {
    now => @almond_bike_market.post(title="23456789", price=14, info="12345", poster="0123456789") ;
}
====
* => @instagram.get_pictures, (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @bodytrace.source(), (((time = makeDate(1462320000000) && weight > 155lb) || weight >= 155lb)) , v_time := time, v_weight := weight => @instagram.get_pictures(count=11), ((caption =~ "9876543210" && link = "http://www.google.com"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @bluetooth_speaker.set_sink, true 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), (((company_name =~ "12345" && yield <= 7 && div <= 7 && pay_date = makeDate(1487030400000)) || (yield < 7 && div >= 11))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @bluetooth_speaker.set_sink() ;
}
====
* => @twitter.search_by_hashtag, (count <= 42 && contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(the_fix)), true , v_title := title, v_link := link => @twitter.search_by_hashtag(query_hashtag=v_title, from=v_title, in_reply_to=v_title), ((text =~ "12345" && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @youtube.search_channel, description =~ "0123456789" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.search_channel(query="23456789"), ((channel_id = "0123456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@security-camera.new_event, has_motion = true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_motion = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @holidays.next_uk_holiday_event(), (((date = makeDate(1462320000000) && summary =~ "23456789" && description = "12345") || description = "9876543210" || summary =~ "0123456788" || (date = makeDate(1487030400000) && summary = "12345") || summary =~ "ABCDEFGHIJ" || (summary = "9876543210" && description = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @phone.send_sms, (to = "+16501234567"^^tt:phone_number || to = "+15551234567"^^tt:phone_number || (to = "+16501234567"^^tt:phone_number && body =~ "0123456788") || body =~ "ABCDEFGHIJ") 
;;
Main() {
    @github.new_milestone(repo_name="9876543210"), true , v_user := user, v_description := description, v_time := time => @phone.send_sms(body=v_description, to="+16501234567"^^tt:phone_number) ;
}
====
now => * => @security-camera.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    now => @security-camera.set_power(power=enum(on)) ;
}
====
now => @phone.get_gps, (altitude > 1000m || (location = $context.location.home && bearing < 14) || (altitude > 42cm && bearing >= 11 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((bearing < 42 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @holidays.next_us_holiday_event, ((date = makeDate(1487030400000) && description = "ABCDEFGHIJ") || summary = "0123456788") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @builtin.at(time=makeTime(20,30)), (time = makeTime(20,30))  => @holidays.next_us_holiday_event(), (summary =~ "23456789") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @security-camera.get_url, (link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url) , v_link := link => notify
;;
Main() {
    @wall_street_journal.opinions(), true , v_title := title, v_link := link => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
@github.new_milestone, (repo_name = "23456789" || (repo_name =~ "23456789" && description =~ "12345" && time = makeDate(1462320000000))) , v_user := user, v_description := description, v_time := time => * => notify
;;
Main() {
    @github.new_milestone(repo_name="0123456789"), (((repo_name = "ABCDEFGHIJ" && description =~ "9876543210") || (repo_name = "0123456789" && user = "alice"^^tt:username && description = "0123456789"))) , v_user := user, v_description := description, v_time := time => @thermostat.get_temperature(), ((value >= 22C || value > 22C)) , v_value := value => notify;
}
====
now => @onedrive.delete_file, (file_name = "12345" || file_name =~ "23456789") 
;;
Main() {
    now => @onedrive.delete_file(file_name="0123456788") ;
}
====
@youtube.source, ((channel_id = "ABCDEFGHIJ" && title =~ "0123456789" && video_url = "http://www.google.com"^^tt:url) || (title =~ "ABCDEFGHIJ" && video_url = "http://www.google.com"^^tt:url) || (channel_id =~ "0123456788" && channel_title = "9876543210") || (channel_title =~ "9876543210" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => *
;;
Main() {
    @youtube.source(), ((channel_id =~ "23456789" && video_id =~ "0123456788")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
now => * => @almond_couch_market.post, (title = "ABCDEFGHIJ" && price <= 7 && contact = "+15551234567"^^tt:phone_number) 
;;
Main() {
    now => @almond_couch_market.post(title="12345", price=14, contact="+16501234567"^^tt:phone_number) ;
}
====
now => @instagram.get_pictures, (count <= 55 && caption = "0123456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(count=55), ((count >= 42 && caption = "ABCDEFGHIJ" && filter = "0123456788" && contains(hashtags, "lol"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.configure(device="facebook"^^tt:device("Facebook")) ;
}
====
now => * => @gmail.send_email, ((to = "bob@stanford.edu"^^tt:email_address && subject = "ABCDEFGHIJ") || subject = "23456789" || to = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    now => @security-camera.get_url(), ((link = "http://www.abc.def"^^tt:url || link = "http://www.google.com"^^tt:url)) , v_link := link => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="0123456789", message="0123456789") ;
}
====
* => @phone.get_gps, ((location = $context.location.work && bearing <= 14 && speed <= 25mph) || (location = $context.location.work && bearing = 55 && speed < 5kmph) || (altitude < 42cm && speed > 5kmph) || (altitude > 1000m && bearing = 7) || (altitude > 1000m && bearing < 7 && speed < 5kmph) || (altitude < 42cm && bearing < 7 && speed = 25mph) || (altitude < 42cm && bearing < 7) || (altitude > 1000m && speed < 5kmph) || (altitude <= 42cm && bearing = 14 && speed >= 25mph) || (altitude < 42cm && speed < 5kmph) || (altitude = 1000m && bearing < 7 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @tumblr.new_text(blog_name="ABCDEFGHIJ"), ((blog_name = "9876543210" && date = makeDate(1487030400000) && link = "http://www.google.com"^^tt:url && contains(tags, "funny"^^tt:hashtag))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @phone.get_gps(), ((location = $context.location.current_location && altitude <= 42cm && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => * => @almond_couch_market.post, ((title =~ "12345" && price < 14 && contact = "+15551234567"^^tt:phone_number) || (title = "12345" && price < 55 && contact = "+15551234567"^^tt:phone_number) || price > 55 || (title = "12345" && price <= 7 && contact = "+15551234567"^^tt:phone_number) || title = "12345" || (title =~ "ABCDEFGHIJ" && price >= 14)) 
;;
Main() {
    now => @almond_couch_market.post(title="12345", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
@gmail.receive_email, ((from_name =~ "ABCDEFGHIJ" && from_address = "bob@stanford.edu"^^tt:email_address && snippet = "23456789") || (from_name =~ "9876543210" && contains(labels, "0123456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_email(), (((from_name =~ "12345" && subject =~ "23456789" && contains(labels, "0123456789")) || from_name =~ "12345")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1487030400000) && organizer = "12345" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), (((summary = "0123456789" && description = "0123456789" && organizer =~ "0123456789" && location = "ABCDEFGHIJ") || (start_date = makeDate(1462320000000) && description = "0123456788" && sequence < 14 && location = "0123456789") || (end_date = makeDate(1462320000000) && sequence >= 7 && organizer =~ "0123456788") || (end_date = makeDate(1462320000000) && description =~ "9876543210" && sequence <= 11 && organizer = "12345" && location = "0123456789") || (description = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "0123456789" && location = "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="9876543210"), (((source_language = "zh"^^tt:iso_lang_code("Chinese") && translated_text =~ "0123456789") || (source_language = "en"^^tt:iso_lang_code("English") && target_language = "it"^^tt:iso_lang_code("Italian") && text = "ABCDEFGHIJ"))) , v_source_language := source_language, v_translated_text := translated_text => @facebook.post(status=v_translated_text) ;
}
====
@almond_bike_market.monitor, ((info = "ABCDEFGHIJ" && price < 55) || (info = "0123456789" && price >= 11))  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=11), (price < 7)  => @twitter.search(query="23456789", in_reply_to="bob"^^tt:username), ((query =~ "0123456789" && count > 55)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @thermostat.get_hvac_state, (state = enum(cooling) || (mode = enum(off) && state = enum(cooling)) || mode = enum(off)) , v_mode := mode, v_state := state => notify
;;
Main() {
    @github.new_issue(repo_name="0123456788"), (((user = "bob"^^tt:username && time = makeDate(1487030400000)) || number > 42)) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @thermostat.get_hvac_state(), (state = enum(off)) , v_mode := mode, v_state := state => notify;
}
====
now => * => @almond_dates.post, message =~ "23456789" 
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_dates.post(interest=v_interest, message=v_poster, poster=v_poster, phone=v_interest) ;
}
====
now => @almond_bike_market.search, ((info = "ABCDEFGHIJ" && price >= 14) || (info =~ "ABCDEFGHIJ" && price >= 14))  => notify
;;
Main() {
    now => @almond_bike_market.search(info="0123456789"), (price >= 11)  => notify;
}
====
@jawbone_up.getsleep, ((time = makeDate(1487030400000) && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && rem > 2h && light = 2h && deep > 30min) || (place = $context.location.home && rem > 2h && light > 30min && deep <= 30min) || (rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @jawbone_up.getsleep(), (((duration < 30min && rem > 2h && light > 2h) || (asleepTime = makeDate(1462320000000) && duration <= 2h && rem = 2h && light >= 30min && deep > 30min) || (awakeTime = makeDate(1462320000000) && duration >= 30min && rem <= 2h && light >= 2h && deep < 30min) || (time = makeDate(1487030400000) && duration >= 2h && rem > 30min && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @uber.price_estimate(end=v_place, start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((end = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type =~ "9876543210" && low_estimate < 11 && currency_code = "0123456788" && distance >= 42cm) || (end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "ABCDEFGHIJ" && low_estimate <= 11 && high_estimate > 42 && currency_code = "12345") || (currency_code =~ "23456789" && surge > 11 && duration = 30min))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @builtin.notify(message="9876543210") ;
}
====
* => @instagram.get_pictures, (caption =~ "ABCDEFGHIJ" && filter = "12345" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @almond_bike_market.monitor(info="12345"), ((price > 14 || info = "12345" || (info =~ "0123456789" && price = 55)))  => @instagram.get_pictures(), ((count >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), (value <= 155lb) , v_time := time, v_value := value => @car.honk_horn() ;
}
====
@almond_couch_market.monitor, (price > 7 || (price > 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => * => notify
;;
Main() {
    @almond_couch_market.monitor(), ((title = "23456789" && price >= 55)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@phone.gps, (altitude > 1000m && bearing > 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), (((altitude >= 1000m && bearing > 42) || (location = $context.location.home && bearing <= 11 && speed > 5kmph) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude < 42cm && bearing < 42))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @phone.get_gps, (altitude >= 42cm && bearing >= 11 && speed >= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.sink, (status = "0123456788" || status =~ "0123456788") 
;;
Main() {
    now => @phone.get_gps(), ((bearing >= 11 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
@scale.source, value <= 155lb , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (time = makeDate(1462320000000)) , v_time := time, v_value := value => @thermostat.get_hvac_state(), ((state = enum(heating) || (mode = enum(off) && state = enum(off)) || state = enum(heating) || mode = enum(cool))) , v_mode := mode, v_state := state => notify;
}
====
now => @ytranslate.detect_language, (text =~ "23456789" || text = "0123456788") , v_detected_language := detected_language => @lg_webos_tv.lower_volume, true 
;;
Main() {
    now => @ytranslate.detect_language(text="12345"), (((text =~ "12345" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || (text =~ "0123456789" && detected_language = "en"^^tt:iso_lang_code("English")))) , v_detected_language := detected_language => @lg_webos_tv.lower_volume() ;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "9876543210" && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @security-camera.set_power(power=enum(off)) ;
}
====
* => @gmail.send_email, (subject =~ "12345" && message = "9876543210") 
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="ABCDEFGHIJ"), ((tournament_search_term = "ABCDEFGHIJ" && away_alias =~ "ABCDEFGHIJ" && home_name =~ "0123456789" && game_status = enum(halftime) && away_points < 7)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @gmail.send_email(to=v_away_alias, subject=v_tournament_full_name, message=v_tournament_league_name) ;
}
====
* => * => @lg_webos_tv.unmute, true 
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @builtin.get_random(), ((number < 55 || number <= 42 || number <= 7 || number >= 42)) , v_number := number => @lg_webos_tv.unmute() ;
}
====
now => * => @almond_dates.post, message = "ABCDEFGHIJ" 
;;
Main() {
    now => @almond_dates.post(interest="ABCDEFGHIJ", message="12345", poster="9876543210", phone="+15551234567"^^tt:phone_number) ;
}
====
@sleep-tracker.getsleep, (asleepTime = makeDate(1487030400000) && rem <= 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1462320000000) && rem <= 30min && light > 30min && deep > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @uber.time_estimate(start=$context.location.work), ((estimate >= 2h || uber_type = "23456789")) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
now => @onedrive.delete_file, (file_name =~ "23456789" || file_name =~ "0123456789") 
;;
Main() {
    now => @onedrive.delete_file(file_name="ABCDEFGHIJ") ;
}
====
@github.new_issue_comment, ((repo_name = "0123456788" && body = "0123456789" && time = makeDate(1462320000000)) || user = "bob"^^tt:username || (issue_number > 42 && body = "23456789") || (repo_name = "9876543210" && issue_number = 55 && body = "12345") || (user = "alice"^^tt:username && issue_number >= 11 && body =~ "9876543210") || (repo_name = "ABCDEFGHIJ" && user = "bob"^^tt:username && issue_number > 42 && body = "12345") || issue_number <= 42) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => notify
;;
Main() {
    @github.new_issue_comment(repo_name="0123456788"), ((repo_name = "ABCDEFGHIJ" && issue_number > 14)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => notify;
}
====
* => @holidays.next_us_holiday_event, (summary =~ "0123456789" || (summary =~ "23456789" && description = "ABCDEFGHIJ") || (summary =~ "0123456788" && description =~ "23456789") || (date = makeDate(1487030400000) && description = "12345") || (date = makeDate(1462320000000) && summary =~ "0123456788") || date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && day = "0123456788" && distance > 1000m && steps >= 11 && activeTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @xkcd.get_comic, (number > 42 && link = "http://www.abc.def"^^tt:url) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @security-camera.set_power, true 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && sequence < 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @xkcd.get_comic(number=v_sequence), ((number > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @security-camera.set_power(power=enum(on)) ;
}
====
@omlet.incomingmessage, (type = enum(picture) && message = "9876543210") , v_feed := feed, v_type := type, v_message := message => notify
;;
Main() {
    @omlet.incomingmessage(), (message = "23456789") , v_type := type, v_message := message => notify;
}
====
@gmail.receive_important_email, (date = makeDate(1462320000000) && snippet = "23456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_important_email(), ((from_name =~ "23456789" && contains(labels, "9876543210") && snippet =~ "0123456788")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.get_time(), (time = makeDate(1462320000000)) , v_time := time => notify;
}
====
* => @ytranslate.detect_language, ((text =~ "0123456788" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || text =~ "23456789" || text = "ABCDEFGHIJ" || text = "ABCDEFGHIJ") , v_detected_language := detected_language => notify
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @ytranslate.detect_language(text=v_title), ((text = "ABCDEFGHIJ" || text = "12345")) , v_detected_language := detected_language => notify;
}
====
* => @wall_street_journal.get_opinions, title =~ "0123456789" , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @wall_street_journal.markets(), true , v_title := title, v_link := link => @wall_street_journal.get_opinions(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 73F && wind_speed >= 25mph && humidity > 11 && fog > 11 && weather =~ "9876543210") || (temperature < 73F && cloudiness < 14 && fog <= 14 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((temperature >= 73F && wind_speed <= 5kmph && humidity >= 42 && status = enum(snowy)) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && wind_speed > 25mph && humidity <= 11 && cloudiness <= 14 && fog = 7) || (temperature < 22C && wind_speed < 25mph && fog <= 42) || (cloudiness > 7 && fog = 55))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
@sportradar.soccer_us_tourney, ((tournament_league_name = "0123456789" && home_alias =~ "0123456788" && game_status = enum(inprogress) && scheduled_time = "9876543210" && home_points < 7) || (tournament_search_term =~ "9876543210" && tournament_full_name =~ "ABCDEFGHIJ" && tournament_league_name =~ "23456789" && away_alias = "9876543210" && home_name = "12345" && scheduled_time = "12345" && home_points > 11) || (away_alias =~ "9876543210" && home_alias =~ "9876543210" && scheduled_time = "0123456788" && away_points > 42) || (tournament_search_term =~ "23456789" && tournament_full_name = "23456789" && tournament_league_name =~ "0123456789" && away_alias = "9876543210" && scheduled_time = "0123456789" && away_points < 14 && home_points < 42) || (home_alias = "9876543210" && home_name = "ABCDEFGHIJ" && away_points < 55 && home_points >= 14)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => * => notify
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="0123456789"), ((tournament_full_name =~ "9876543210" && scheduled_time =~ "9876543210" && away_points >= 7 && home_points < 14)) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => notify;
}
====
* => @dropbox.list_folder, (folder_name = "0123456788" && file_name =~ "23456789" && last_modified = makeDate(1462320000000) && full_path =~ "0123456789") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    now => @dropbox.list_folder(folder_name="0123456789"), ((file_name = "0123456789" && file_size = 20MB && full_path = "0123456788")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @almond_couch_market.search, (price >= 55 && contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1487030400000) && activeTime >= 30min && inactiveTime <= 2h && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && day = "0123456789" && distance <= 42cm && activeTime > 2h && inactiveTime <= 30min && calories < 500kcal) || (distance > 1000m && steps < 42 && activeTime < 2h && inactiveTime >= 30min && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_couch_market.search(), (title =~ "ABCDEFGHIJ") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @instagram.get_pictures, ((caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter = "ABCDEFGHIJ") || (count > 14 && filter = "23456789" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=55), ((caption =~ "12345" && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@google_drive.new_drive_file, file_name =~ "0123456789" , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "12345" || file_name =~ "ABCDEFGHIJ")) , v_file_name := file_name => @almond_dates.search(), (message =~ "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @almond_dates.search, (message = "9876543210" && poster =~ "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (((poster =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (poster =~ "12345" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @gmail.send_picture, message =~ "12345" 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="0123456789", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@rss.new_post, (title = "23456789" || (title = "23456789" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), (title = "0123456788") , v_title := title, v_link := link => @onedrive.upload_picture(file_name="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @fitbit.getsteps, steps = 14 , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), (steps <= 14) , v_date := date, v_steps := steps => @onedrive.upload_picture(file_name="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @almond_dates.post, (message = "0123456789" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number) 
;;
Main() {
    @sleep-tracker.getsleep(), (((rem > 2h && light >= 2h && deep = 2h) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration > 2h && rem >= 30min && light > 30min && deep = 30min) || (time = makeDate(1487030400000) && place = $context.location.work && duration >= 30min && rem < 2h && light >= 2h && deep < 30min) || (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration < 30min && light >= 2h && deep <= 30min) || (awakeTime = makeDate(1487030400000) && duration >= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @almond_dates.post(interest="ABCDEFGHIJ", message="0123456789", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @almond_dates.post, ((interest = "0123456788" && poster = "9876543210") || phone = "+16501234567"^^tt:phone_number || poster = "23456789") 
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "12345") , v_file_name := file_name => @almond_dates.post(message=v_file_name, phone=v_file_name, interest="ABCDEFGHIJ", poster="0123456788") ;
}
====
@slack.receive, ((channel = "lol"^^tt:hashtag && timestamp =~ "9876543210" && from = "alice"^^tt:username) || timestamp =~ "9876543210") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (timestamp =~ "0123456788") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @dropbox.list_folder(folder_name="0123456789"), (file_name = "0123456788") , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
now => @instagram.get_pictures, (caption = "0123456789" && link = "http://www.google.com"^^tt:url && location = $context.location.current_location) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(), ((count >= 11 && caption = "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @twitter.search_by_hashtag, ((count > 55 && text = "9876543210" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || (contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @jawbone_up.getheartrate(), (((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate = 55) || time = makeDate(1462320000000))) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=55, in_reply_to="alice"^^tt:username), (text = "12345") , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude > 42cm && bearing <= 42 && speed > 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), (((location = $context.location.work && speed < 25mph) || bearing <= 42)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.recordweight(weight=155lb) ;
}
====
@almond_bike_market.monitor, price >= 11  => * => notify
;;
Main() {
    @almond_bike_market.monitor(), (price < 11)  => notify;
}
====
* => @facebook.post, status = "0123456788" 
;;
Main() {
    @phone.receive_sms(), (from = "+15551234567"^^tt:phone_number) , v_from := from, v_body := body => @facebook.post(status=v_body) ;
}
====
* => @fitbit.getbody, fat > 7 , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), ((home_name = "ABCDEFGHIJ" && game_status = enum(closed) && scheduled_time = "0123456789" && game_inning = "23456789" && home_runs <= 11)) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @fitbit.getbody(), ((weight = 82kg && bmi <= 7 && fat = 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @xkcd.random_comic, (number <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number <= 11 && title = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @uber.time_estimate, ((uber_type = "12345" && estimate <= 2h) || (start = $context.location.work && uber_type =~ "9876543210" && estimate = 30min) || (start = $context.location.work && uber_type =~ "0123456788" && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    @omlet.newmessage(), (((type = enum(text) && message =~ "12345") || message = "23456789")) , v_type := type, v_message := message => @uber.time_estimate(start=$context.location.home), ((start = $context.location.current_location && uber_type =~ "23456789")) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@builtin.timer, (interval < 2h || interval = 2h || interval < 2h)  => @instagram.get_pictures, ((count < 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && location = $context.location.home) || (caption =~ "12345" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @builtin.timer(interval=30min), (interval < 2h)  => @instagram.get_pictures(count=42), ((count > 55 && link = "http://www.google.com"^^tt:url && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@yahoofinance.stock_quote, ((ask_price < 55 && bid_price >= 14) || bid_price < 55) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), (((ask_price <= 55 && bid_price < 14) || (company_name =~ "23456789" && ask_price <= 42) || (ask_price < 11 && bid_price >= 11))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @uber.price_estimate(start=$context.location.work, end=$context.location.current_location), ((end = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type = "23456789" && low_estimate >= 11 && high_estimate >= 55 && currency_code = "23456789" && surge < 7 && duration > 30min)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@scale.source, (time = makeDate(1487030400000) && value < 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @imgflip.generate(template="12345", text_top="12345", text_bottom="9876543210"), ((template = "0123456788" && text_bottom = "0123456789" && name =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @thermostat.get_temperature, (value >= 73F || value <= 22C || value > 73F) , v_value := value => notify
;;
Main() {
    now => @thermostat.get_temperature(), ((value = 22C || value > 22C)) , v_value := value => notify;
}
====
now => @dropbox.list_folder, ((folder_name = "12345" && file_size = 20MB) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456789" && full_path =~ "9876543210") || (file_name =~ "ABCDEFGHIJ" && is_folder = true && file_size <= 20MB) || (folder_name =~ "23456789" && file_name = "12345") || (folder_name = "12345" && file_name = "23456789" && last_modified = makeDate(1462320000000) && file_size >= 5KB && full_path =~ "0123456789")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="23456789"), (((folder_name = "0123456789" && file_size < 5KB && full_path = "23456789") || (folder_name = "ABCDEFGHIJ" && file_name = "12345" && is_folder = false && full_path = "0123456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @speaker.raise_volume() ;
}
====
* => @almond_couch_market.search, price < 42 , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @thermostat.humidity(), ((time = makeDate(1462320000000) && value <= 55)) , v_time := time, v_value := value => @almond_couch_market.search(), (contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    @washington_post.new_article(section=enum(world)), (((title =~ "0123456788" && link = "http://www.google.com"^^tt:url) || (section = enum(lifestyle) && description = "12345"))) , v_title := title, v_link := link, v_description := description => @heatpad.set_power(power=enum(off)) ;
}
====
now => @thecatapi.get, (count > 14 && link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((link = "http://www.google.com"^^tt:url || (image_id = "23456789" && count <= 14))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @lg_webos_tv.set_volume(percent=v_count) ;
}
====
@activity-tracker.getmove, ((day =~ "23456789" && distance > 1000m && activeTime >= 30min) || (distance >= 42cm && steps > 42 && inactiveTime >= 30min) || (day =~ "23456789" && distance < 42cm && steps >= 11 && activeTime <= 2h) || (distance >= 42cm && steps = 7 && inactiveTime >= 30min) || (day = "0123456788" && distance > 42cm && steps > 42 && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((distance = 42cm && inactiveTime >= 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) || (date = makeDate(1487030400000) && summary = "9876543210"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @slack.updateChannelTopic, (channel = "cat"^^tt:hashtag && topic = "12345") 
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((summary = "ABCDEFGHIJ" || date = makeDate(1462320000000) || description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @slack.updateChannelTopic(channel=v_description, topic=v_description) ;
}
====
* => @light-bulb.color_loop, true 
;;
Main() {
    @almond_bike_market.monitor(info="9876543210"), (info = "0123456789")  => @light-bulb.color_loop() ;
}
====
@reddit_front_page.newpost, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && user = "0123456788" && category = "9876543210") || category = "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @builtin.thank_you() ;
}
====
* => @icalendar.list_events, ((description = "12345" && sequence < 55 && location = "23456789") || (summary =~ "23456789" && description = "ABCDEFGHIJ" && sequence >= 14 && organizer =~ "12345") || (end_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && sequence >= 11) || (sequence > 42 && organizer =~ "9876543210" && location = "ABCDEFGHIJ") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && summary = "0123456789" && description =~ "23456789" && sequence <= 7) || (end_date = makeDate(1487030400000) && summary =~ "0123456789" && description = "12345" && sequence > 11 && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @twitter.direct_message(), ((from = "alice"^^tt:username && message =~ "23456789")) , v_from := from, v_message := message, v___reserved := __reserved => @icalendar.list_events(), ((summary = "0123456789" || (end_date = makeDate(1487030400000) && description = "23456789") || (end_date = makeDate(1487030400000) && organizer = "12345" && location = "0123456788") || (summary = "0123456789" && description = "23456789" && sequence > 7 && organizer = "23456789" && location = "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => * => @github.comment_issue, true 
;;
Main() {
    @wall_street_journal.technology(), (title =~ "12345") , v_title := title, v_link := link => @builtin.get_time(), (time = makeDate(1487030400000)) , v_time := time => @github.comment_issue(repo_name="23456789", issue_number=55, body="ABCDEFGHIJ") ;
}
====
* => @lg_webos_tv.mute, true 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="fcb"^^sportradar:eu_soccer_team("Bayern Munchen")), ((away_name = "ABCDEFGHIJ" && home_points < 42 && result = enum(unclosed))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @lg_webos_tv.mute() ;
}
====
* => @builtin.get_random, (number > 42 || number >= 55 || number < 55 || number > 14 || number <= 7) , v_number := number => notify
;;
Main() {
    now => @builtin.get_random(), true , v_number := number => notify;
}
====
* => @builtin.get_random, (number < 14 || number >= 55 || number > 14) , v_number := number => notify
;;
Main() {
    now => @builtin.get_random(), (number < 42) , v_number := number => notify;
}
====
@slack.receive, ((timestamp = "12345" && from = "alice"^^tt:username) || (timestamp =~ "9876543210" && from = "alice"^^tt:username && message = "12345")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (timestamp = "ABCDEFGHIJ") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @icalendar.list_events(), (((end_date = makeDate(1462320000000) && summary =~ "12345" && sequence < 42 && organizer =~ "9876543210") || (sequence <= 14 && organizer = "12345") || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence <= 7 && organizer =~ "ABCDEFGHIJ"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @almond_bike_market.ask_seller, (id = "23456789" && property = "12345") , v_answer := answer => notify
;;
Main() {
    now => @almond_bike_market.ask_seller(id="9876543210", property="0123456789", value="ABCDEFGHIJ", public=true), (((id =~ "ABCDEFGHIJ" && property = "ABCDEFGHIJ" && answer =~ "ABCDEFGHIJ") || value =~ "ABCDEFGHIJ")) , v_answer := answer => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @github.new_commit(repo_name="12345"), true , v_user := user, v_message := message, v_time := time => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @linkedin.share, true 
;;
Main() {
    @jawbone_up.getsleep(), (((time = makeDate(1462320000000) && rem >= 30min && light = 2h && deep = 2h) || (time = makeDate(1487030400000) && duration = 2h && rem < 2h && light > 2h && deep = 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @holidays.next_us_holiday_event(), ((summary =~ "0123456789" && description =~ "12345")) , v_date := date, v_summary := summary, v_description := description => @linkedin.share(status=v_summary) ;
}
====
now => @weatherapi.moon, (date = makeDate(1462320000000) && phase = "0123456789") , v_date := date, v_phase := phase => *
;;
Main() {
    now => @weatherapi.moon(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = $context.location.current_location && phase = "23456789") || (date = makeDate(1462320000000) && phase =~ "9876543210"))) , v_date := date, v_phase := phase => @thermostat.set_hvac_mode(mode=enum(heat_cool)) ;
}
====
* => @almond_couch_market.post, (title =~ "23456789" && price = 7) 
;;
Main() {
    @security-camera.new_event(), (((has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_couch_market.post(title="12345", price=55, contact="+15551234567"^^tt:phone_number) ;
}
====
@phone.receive_sms, (body = "0123456788" || (from = "+16501234567"^^tt:phone_number && body =~ "12345") || body =~ "23456789") , v_from := from, v_body := body => notify
;;
Main() {
    @phone.receive_sms(), (body =~ "0123456788") , v_from := from, v_body := body => notify;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "0123456788" && specialties =~ "ABCDEFGHIJ") || industry =~ "12345") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @xkcd.new_whatif(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => @linkedin.get_profile(), ((formatted_name = "12345" && headline =~ "0123456788" && industry = "9876543210")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@jawbone_up.getsleep, ((light = 2h && deep > 30min) || (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration < 30min && rem < 30min && light <= 2h) || (duration < 2h && light >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify
;;
Main() {
    @jawbone_up.getsleep(), (((asleepTime = makeDate(1462320000000) && duration = 30min && rem >= 2h && light = 30min && deep >= 30min) || (awakeTime = makeDate(1462320000000) && duration >= 30min && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
now => @facebook.post_picture, (caption =~ "ABCDEFGHIJ" || caption =~ "0123456788") 
;;
Main() {
    now => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="0123456789") ;
}
====
now => * => @almond_bike_market.post, (price <= 55 || (price < 7 && poster =~ "12345")) 
;;
Main() {
    now => @nasa.apod(), (((title =~ "0123456788" && description =~ "0123456788") || title =~ "12345")) , v_title := title, v_description := description, v_picture_url := picture_url => @almond_bike_market.post(title=v_description, info=v_title, poster=v_title, price=55) ;
}
====
now => * => @light-bulb.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @fitbit.getbody(), ((bmi <= 14 && fat > 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @light-bulb.set_power(power=enum(off)) ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "23456789" || (message = "23456789" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @instagram.get_pictures(count=55), ((count < 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @github.add_email(email=v_caption) ;
}
====
now => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="23456789", text_bottom="0123456789"), ((template = "23456789" || (text_bottom = "23456789" && name =~ "9876543210"))) , v_name := name, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name=v_name) ;
}
====
* => @almond_couch_market.post, ((price <= 14 && contact = "+16501234567"^^tt:phone_number) || (title = "23456789" && price > 55)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && humidity < 14) || (wind_speed < 25mph && cloudiness > 11 && fog >= 14 && weather = "0123456789" && status = enum(drizzling)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_couch_market.post(price=v_cloudiness, title="23456789", contact="+16501234567"^^tt:phone_number) ;
}
====
* => @icalendar.list_events, ((end_date = makeDate(1462320000000) && summary =~ "9876543210" && description = "ABCDEFGHIJ" && sequence < 11) || (summary =~ "0123456788" && sequence > 11) || (end_date = makeDate(1462320000000) && summary = "0123456788" && description = "9876543210" && sequence > 42) || (summary =~ "23456789" && sequence > 11 && organizer =~ "0123456789") || (start_date = makeDate(1462320000000) && description =~ "23456789" && organizer =~ "12345" && location = "9876543210") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && summary = "12345" && description = "0123456788" && sequence >= 14) || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && summary =~ "0123456788" && sequence <= 11 && location =~ "0123456788") || (end_date = makeDate(1487030400000) && description =~ "23456789" && sequence <= 11 && location =~ "12345") || (start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && summary =~ "9876543210" && sequence < 7 && location = "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary = "12345" && description =~ "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "0123456788" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration = 30min && rem <= 2h && deep < 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @icalendar.list_events(), ((end_date = makeDate(1462320000000) && sequence > 55 && organizer =~ "0123456788" && location = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @light-bulb.alert_long, true 
;;
Main() {
    @sportradar.soccer_eu_team(watched_team_alias="juv"^^sportradar:eu_soccer_team("Juventus")), ((away_name =~ "0123456789" && home_name =~ "9876543210" && away_points >= 11 && home_points <= 7)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @light-bulb.alert_long() ;
}
====
* => @lg_webos_tv.mute, true 
;;
Main() {
    @xkcd.new_comic(), true , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @lg_webos_tv.mute() ;
}
====
now => @ytranslate.translate, (text = "12345" || (target_language = "zh"^^tt:iso_lang_code("Chinese") && text =~ "9876543210" && translated_text = "ABCDEFGHIJ")) , v_source_language := source_language, v_translated_text := translated_text => *
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="ABCDEFGHIJ"), ((source_language = "zh"^^tt:iso_lang_code("Chinese") && target_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_source_language := source_language, v_translated_text := translated_text => @speaker.raise_volume() ;
}
====
@phone.gps, (location = $context.location.current_location && altitude < 1000m && bearing < 55 && speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), ((location = $context.location.work && bearing > 7 && speed = 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.getbody(), (((weight <= 155lb && bmi < 7 && fat > 11) || (weight > 82kg && fat < 14))) , v_weight := weight, v_bmi := bmi, v_fat := fat => @speaker.lower_volume() ;
}
====
* => @builtin.get_date, date = makeDate(1462320000000) , v_date := date => *
;;
Main() {
    @slack.receive(), ((timestamp = "9876543210" && from = "alice"^^tt:username && message =~ "12345")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @builtin.get_date(), ((date = makeDate(1462320000000) || date = makeDate(1487030400000))) , v_date := date => @twitter.sink(status="ABCDEFGHIJ") ;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature > 73F && wind_speed < 5kmph && humidity <= 42 && cloudiness >= 7 && fog < 7 && weather = "0123456788") || (temperature = 73F && wind_speed > 5kmph && fog > 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((location = $context.location.home && wind_speed > 5kmph && cloudiness <= 55 && weather = "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @slack.send(channel="funny"^^tt:hashtag, message="12345") ;
}
====
* => @slack.updateChannelPurpose, true 
;;
Main() {
    @xkcd.new_comic(), ((number > 7 && title =~ "12345" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @slack.updateChannelPurpose(channel=v_title, purpose=v_title) ;
}
====
* => * => @light-bulb.alert_long, true 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((stock_id = "goog"^^tt:stock_id("Google") && company_name =~ "ABCDEFGHIJ" && yield < 7 && ex_div_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @light-bulb.alert_long() ;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    @phone.gps(), ((location = $context.location.home && altitude > 42cm && bearing < 7)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @icalendar.list_events(), ((end_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description = "12345" && sequence >= 55 && organizer = "0123456789" && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @light-bulb.set_power(power=enum(off)) ;
}
====
* => @phone.get_gps, ((altitude >= 1000m && speed >= 5kmph) || (location = $context.location.home && altitude > 42cm && bearing >= 14 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    @washington_post.new_blog_post(section=enum(the_plum_line)), true , v_title := title, v_link := link => @phone.get_gps(), ((location = $context.location.current_location && altitude > 42cm && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.set_ringer(mode=enum(normal)) ;
}
====
* => @onedrive.delete_file, true 
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "0123456789" && poster = "0123456789" && phone = "+16501234567"^^tt:phone_number) || (interest = "0123456788" && message =~ "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @onedrive.delete_file(file_name="0123456789") ;
}
====
@nasa.asteroid, (estimated_diameter_max < 1000m && relative_velocity <= 5kmph && closest_distance_to_earth < 1000m && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((name =~ "ABCDEFGHIJ" && estimated_diameter_min >= 42cm && dangerous = true && relative_velocity >= 25mph)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @thermostat.get_hvac_state(), (mode = enum(heat)) , v_mode := mode, v_state := state => notify;
}
====
* => @almond_couch_market.post, ((title = "0123456788" && price > 55 && contact = "+15551234567"^^tt:phone_number) || (price > 42 && contact = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @phdcomics.new_post(), (((title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_link := link, v_picture_url := picture_url => @almond_couch_market.post(title="23456789", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
@google_drive.new_drive_file, file_name =~ "23456789" , v_file_name := file_name => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "12345") , v_file_name := file_name => notify;
}
====
now => * => @builtin.configure, true 
;;
Main() {
    now => @almond_dates.search(), ((interest = "9876543210" || (interest =~ "9876543210" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.configure(device=v_poster) ;
}
====
@holidays.us_holiday_event, ((summary =~ "ABCDEFGHIJ" && description = "12345") || (summary = "0123456789" && description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.us_holiday_event(), (((summary =~ "0123456788" && description =~ "0123456788") || (summary =~ "9876543210" && description = "0123456789"))) , v_date := date, v_summary := summary, v_description := description => @almond_dates.post(interest=v_summary, message=v_summary, poster=v_summary, phone=v_summary) ;
}
====
now => @xkcd.get_comic, ((title = "23456789" && link = "http://www.google.com"^^tt:url && alt_text =~ "23456789") || (number <= 7 && title = "0123456789" && alt_text =~ "ABCDEFGHIJ")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(), (((number <= 11 && link = "http://www.abc.def"^^tt:url) || (number <= 7 && title =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@omlet.incomingmessage, message =~ "12345" , v_feed := feed, v_type := type, v_message := message => * => *
;;
Main() {
    @omlet.incomingmessage(), ((message = "0123456789" || type = enum(text))) , v_type := type, v_message := message => @bluetooth_speaker.lower_volume() ;
}
====
* => @twitter.search, (query =~ "9876543210" && count <= 55 && text = "23456789") , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search(query="12345", count=14), ((query = "9876543210" && count < 42)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@activity-tracker.getmove, (day =~ "12345" && distance < 1000m && steps > 14 && inactiveTime < 2h && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && steps >= 14 && activeTime >= 2h) || (steps <= 55 && inactiveTime >= 30min))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @fitbit.getbody(), ((weight = 155lb && fat <= 11)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@almond_dates.monitor, ((interest =~ "23456789" && message =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (message =~ "0123456788" && poster =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @almond_dates.monitor(), ((message = "9876543210" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @almond_dates.post, phone = "+15551234567"^^tt:phone_number 
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1462320000000) && summary = "0123456788")) , v_date := date, v_summary := summary, v_description := description => @almond_dates.post(interest=v_description, message=v_description, poster=v_description, phone=v_description) ;
}
====
* => @facebook.post, status = "ABCDEFGHIJ" 
;;
Main() {
    @gmail.receive_primary_email(), ((subject = "0123456788" && snippet =~ "9876543210")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @facebook.post(status=v_from_name) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((has_motion = true || (start_time = makeDate(1487030400000) && has_person = true))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @bluetooth_speaker.set_sink() ;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature >= 22C && wind_speed >= 25mph && cloudiness < 55) || (humidity = 11 && cloudiness > 11 && fog <= 55 && status = enum(drizzling)) || (location = $context.location.work && temperature = 22C && fog > 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 73F && humidity >= 55 && cloudiness > 14 && fog < 55 && weather = "23456789" && status = enum(windy)) || (temperature = 22C && wind_speed = 25mph && humidity < 42 && cloudiness < 7 && fog < 55) || (location = $context.location.home && wind_speed <= 5kmph && humidity <= 14 && fog <= 14 && weather = "23456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "ABCDEFGHIJ" || file_name =~ "23456789" || file_name =~ "12345") 
;;
Main() {
    now => @bing.web_search(query="0123456788"), (((query = "ABCDEFGHIJ" && title = "0123456788" && description = "9876543210" && link = "http://www.google.com"^^tt:url) || description =~ "ABCDEFGHIJ" || query =~ "0123456788" || (query = "0123456788" && title = "0123456788" && description = "23456789") || query = "0123456788")) , v_title := title, v_description := description, v_link := link => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
now => @thermostat.get_hvac_state, (state = enum(cooling) || (mode = enum(heat) && state = enum(heating))) , v_mode := mode, v_state := state => notify
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(cooling) || state = enum(cooling) || mode = enum(cool))) , v_mode := mode, v_state := state => notify;
}
====
now => * => @almond_bike_market.post, ((title =~ "0123456789" && price >= 11 && info =~ "0123456789" && poster = "0123456789") || (title =~ "23456789" && price > 55 && info = "0123456788")) 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @almond_bike_market.post(title="0123456789", price=42, info="ABCDEFGHIJ", poster="23456789") ;
}
====
* => @icalendar.list_events, ((summary = "ABCDEFGHIJ" && sequence <= 11 && organizer =~ "ABCDEFGHIJ") || (end_date = makeDate(1462320000000) && summary =~ "9876543210" && sequence < 14)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => *
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && summary = "0123456788" && description =~ "23456789" && sequence < 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @gmail.send_picture(to=v_organizer, subject=v_location, message=v_description, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @car.honk_horn, true 
;;
Main() {
    now => @instagram.get_pictures(count=11), ((count <= 7 && caption = "0123456789" && link = "http://www.abc.def"^^tt:url && filter =~ "0123456789" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @car.honk_horn() ;
}
====
now => @thermostat.get_temperature, (value > 22C || value < 22C) , v_value := value => notify
;;
Main() {
    now => @thermostat.get_temperature(), (value >= 73F) , v_value := value => notify;
}
====
now => * => @almond_dates.post, (message =~ "0123456788" || (interest =~ "23456789" && message =~ "0123456789" && poster =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 42cm && bearing > 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_dates.post(interest="ABCDEFGHIJ", message="0123456789", poster="ABCDEFGHIJ", phone="+15551234567"^^tt:phone_number) ;
}
====
@yahoofinance.stock_div, (yield = 7 && div >= 11 && pay_date = makeDate(1462320000000) && ex_div_date = makeDate(1487030400000)) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((company_name = "0123456788" && yield > 7 && div < 55 && pay_date = makeDate(1462320000000) && ex_div_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => notify;
}
====
* => @linkedin.share, status =~ "0123456789" 
;;
Main() {
    @omlet.newmessage(), (message =~ "12345") , v_type := type, v_message := message => @linkedin.share(status=v_message) ;
}
====
* => @almond_bike_market.detail, id =~ "9876543210"  => notify
;;
Main() {
    now => @almond_bike_market.detail(id="ABCDEFGHIJ"), true  => notify;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && label = "0123456789") || (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") || from = "bob@stanford.edu"^^tt:email_address || label = "23456789" || subject = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @icalendar.event_begin(), ((organizer =~ "ABCDEFGHIJ" || (end_date = makeDate(1462320000000) && summary =~ "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ" && sequence <= 42 && organizer = "0123456788" && location = "ABCDEFGHIJ"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && subject =~ "23456789")) , v_from := from, v_subject := subject, v_label := label => @gmail.send_picture(to=v_label, subject=v_subject, message=v_subject, picture_url=v_label) ;
}
====
now => @tumblr-blog.post_text, title = "23456789" 
;;
Main() {
    now => @tumblr-blog.post_text(title="12345", body="12345") ;
}
====
now => @fitbit.getsteps, true , v_date := date, v_steps := steps => *
;;
Main() {
    now => @fitbit.getsteps(), true , v_date := date, v_steps := steps => notify;
}
====
@weatherapi.monitor, (wind_speed >= 5kmph && humidity = 14 && cloudiness <= 11 && fog > 7) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((temperature < 73F && humidity <= 55 && cloudiness >= 14 && fog <= 42 && weather = "23456789" && status = enum(sleety)) || (temperature < 22C && humidity <= 55 && cloudiness >= 55 && fog > 42))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @tumblr-blog.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1487030400000) && has_sound = false && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify
;;
Main() {
    @security-camera.new_event(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1462320000000) && has_sound = false))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
* => * => @dropbox.create_new_folder, true 
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @dropbox.create_new_folder(folder_name=v_file_name) ;
}
====
now => @uber.time_estimate, (uber_type =~ "23456789" && estimate < 2h) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), ((start = $context.location.work && estimate < 2h)) , v_uber_type := uber_type, v_estimate := estimate => @car.honk_horn() ;
}
====
@github.new_issue_comment, ((repo_name = "12345" && issue_number < 42 && body =~ "0123456789" && time = makeDate(1487030400000)) || (repo_name =~ "0123456788" && body =~ "12345") || user = "bob"^^tt:username || (user = "bob"^^tt:username && issue_number < 11) || (repo_name = "9876543210" && body = "0123456788") || (user = "alice"^^tt:username && issue_number < 11 && body = "12345" && time = makeDate(1462320000000))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => *
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), (((repo_name = "12345" && user = "alice"^^tt:username && body =~ "23456789" && time = makeDate(1462320000000)) || (repo_name =~ "0123456788" && issue_number < 55 && time = makeDate(1462320000000)))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @onedrive.delete_file(file_name="0123456788") ;
}
====
* => * => @phone.send_sms, body = "ABCDEFGHIJ" 
;;
Main() {
    @security-camera.new_event(), ((has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @thecatapi.get(), (((image_id = "12345" && count = 7) || (image_id = "12345" && count = 11 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="0123456789") ;
}
====
@phone.gps, ((location = $context.location.work && altitude = 42cm && speed <= 5kmph) || (location = $context.location.home && altitude < 42cm && speed > 5kmph) || altitude < 42cm) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => @builtin.thank_you, true 
;;
Main() {
    @phone.gps(), (((altitude = 42cm && bearing <= 14 && speed < 5kmph) || (location = $context.location.home && speed <= 5kmph) || (location = $context.location.home && bearing > 42 && speed < 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @builtin.thank_you() ;
}
====
now => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => @google_drive.create_new_drive_file, (file_name =~ "12345" || file_name =~ "12345" || file_name = v_title) 
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title = "23456789" || (title = "9876543210" && updated = makeDate(1462320000000)) || title =~ "23456789")) , v_title := title, v_link := link, v_updated := updated => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @almond_couch_market.post, ((title =~ "0123456789" && price < 14 && contact = "+16501234567"^^tt:phone_number) || (title = "9876543210" && price > 11)) 
;;
Main() {
    now => @almond_couch_market.post(title="23456789", price=55, contact="+16501234567"^^tt:phone_number) ;
}
====
now => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((title =~ "0123456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => @twitter.post_picture(caption=v_title, picture_url=v_title) ;
}
====
@instagram.new_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && link = "http://www.google.com"^^tt:url && filter = "23456789" && contains(hashtags, "lol"^^tt:hashtag)) || (link = "http://www.abc.def"^^tt:url && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && contains(hashtags, "cat"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789" && filter = "23456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (caption = "23456789" && link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
@bodytrace.source, time = makeDate(1487030400000) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) || weight < 82kg)) , v_time := time, v_weight := weight => notify;
}
====
now => * => @almond_couch_market.post, (price < 7 || (title = "0123456789" && price = 55) || (title =~ "23456789" && price > 42)) 
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && description = "ABCDEFGHIJ" && organizer = "23456789" && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @almond_couch_market.post(title=v_summary, price=v_sequence, contact=v_summary) ;
}
====
* => @dropbox.create_new_folder, folder_name = "9876543210" 
;;
Main() {
    @github.new_milestone(repo_name="0123456789"), (time = makeDate(1462320000000)) , v_user := user, v_description := description, v_time := time => @dropbox.create_new_folder(folder_name=v_description) ;
}
====
* => * => @tumblr-blog.post_text, true 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 55 && name =~ "9876543210" && estimated_diameter_min <= 1000m && estimated_diameter_max < 1000m && dangerous = false && relative_velocity >= 5kmph && closest_distance_to_earth >= 42cm) || (asteroid_id <= 7 && name = "ABCDEFGHIJ" && estimated_diameter_max = 1000m && dangerous = true && relative_velocity < 25mph && orbiting_body =~ "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @tumblr-blog.post_text(body=v_orbiting_body, title="ABCDEFGHIJ") ;
}
====
@activity-tracker.getmove, (updateTime = makeDate(1487030400000) && day = "12345" && distance <= 1000m && steps <= 14 && activeTime <= 2h) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "23456789" && distance >= 42cm && steps <= 55 && inactiveTime <= 2h && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
* => @xkcd.random_comic, ((title = "9876543210" && alt_text =~ "ABCDEFGHIJ") || (number > 11 && link = "http://www.abc.def"^^tt:url) || (title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @xkcd.random_comic(), (number <= 11) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
now => * => @dropbox.create_new_folder, folder_name = "0123456789" 
;;
Main() {
    now => @dropbox.get_space_usage(), ((used <= 5KB || (used < 5KB && total > 5KB) || (used > 5KB && total <= 20MB) || (used <= 5KB && total < 20MB) || used > 5KB || used = 20MB)) , v_used := used, v_total := total => @dropbox.create_new_folder(folder_name="0123456789") ;
}
====
now => * => @almond_bike_market.post, (price < 42 && info = "12345" && poster = "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_couch_market.search(), ((title = "0123456789" && price > 7)) , v_title := title, v_price := price, v_contact := contact => @almond_bike_market.post(title="ABCDEFGHIJ", price=42, info="9876543210", poster="0123456788") ;
}
====
* => @giphy.get_tag, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => notify
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, (updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && steps < 55 && activeTime > 30min && inactiveTime >= 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "12345" && distance <= 42cm && steps <= 55 && inactiveTime < 30min && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
now => @gmail.get_latest, (subject = "9876543210" && label =~ "12345") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((from = "bob@stanford.edu"^^tt:email_address && label = "12345") || subject = "12345" || subject =~ "0123456788")) , v_from := from, v_subject := subject, v_label := label => @twitter.unfollow(user_name="bob"^^tt:username) ;
}
====
* => @bing.web_search, ((query =~ "ABCDEFGHIJ" && title =~ "12345" && description =~ "0123456788" && link = "http://www.google.com"^^tt:url) || (description = "12345" && link = "http://www.abc.def"^^tt:url) || query =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title =~ "23456789" && user = "0123456789" && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @bing.web_search(query="23456789"), (link = "http://www.google.com"^^tt:url) , v_title := title, v_description := description, v_link := link => notify;
}
====
@gmail.receive_important_email, (from_address = "alice@gmail.com"^^tt:email_address || (from_name =~ "23456789" && subject =~ "9876543210" && date = makeDate(1487030400000)) || snippet = "0123456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify
;;
Main() {
    @gmail.receive_important_email(), ((from_name =~ "0123456788" && subject = "0123456788" && contains(labels, "0123456788"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @holidays.us_holiday_event(), ((summary =~ "12345" || (date = makeDate(1487030400000) && summary = "0123456789" && description =~ "0123456789") || (summary =~ "0123456789" && description =~ "12345") || (date = makeDate(1462320000000) && description =~ "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => @security-camera.set_power(power=enum(on)) ;
}
====
now => @imgflip.generate, (text_bottom = "9876543210" && name =~ "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="23456789", text_bottom="9876543210"), ((text_bottom =~ "23456789" && name = "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @fitbit.getbody, (weight < 155lb && bmi > 42) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    now => @fitbit.getbody(), ((weight <= 155lb && fat <= 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => * => @github.comment_issue, ((repo_name =~ "23456789" && issue_number <= 14 && body = "12345") || (issue_number < 14 && body = "9876543210") || (repo_name =~ "ABCDEFGHIJ" && issue_number < 55 && body =~ "0123456788") || (repo_name =~ "ABCDEFGHIJ" && issue_number < 11)) 
;;
Main() {
    now => @youtube.search_channel(query="0123456788"), (((query = "ABCDEFGHIJ" && channel_id =~ "12345" && title =~ "12345" && description =~ "0123456789") || (title =~ "23456789" && description =~ "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @github.comment_issue(repo_name=v_title, body=v_title, issue_number=55) ;
}
====
now => @almond_couch_market.search, (price <= 42 && contact = "+15551234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price >= 11 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @speaker.set_volume(percent=7) ;
}
====
@wall_street_journal.us_business, (title = "23456789" && link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @wall_street_journal.us_business(), (title =~ "12345") , v_title := title, v_link := link => @fitbit.getbody(), (((weight <= 155lb && bmi < 42 && fat <= 11) || (weight >= 155lb && bmi >= 14 && fat >= 55))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
@heartrate-monitor.getheartrate, ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate < 42) || (time = makeDate(1487030400000) && place = $context.location.work)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate <= 11 || (time = makeDate(1487030400000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")))) , v_time := time, v_place := place, v_heartrate := heartrate => @thecatapi.get(), (((count > 7 && link = "http://www.abc.def"^^tt:url) || (count = 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @holidays.next_uk_holiday_event, (date = makeDate(1487030400000) || date = makeDate(1462320000000) || (date = makeDate(1462320000000) && summary = "0123456789" && description = "23456789") || (summary = "9876543210" && description =~ "9876543210") || (summary =~ "9876543210" && description =~ "12345") || date = makeDate(1462320000000) || summary = "ABCDEFGHIJ" || description = "9876543210" || (date = makeDate(1462320000000) && description =~ "0123456789")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @holidays.next_uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @thermostat.get_hvac_state, (state = enum(heating) || mode = enum(cool)) , v_mode := mode, v_state := state => @gmail.send_picture, ((subject =~ "0123456788" && message = "ABCDEFGHIJ") || message =~ "23456789") 
;;
Main() {
    now => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="0123456789", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    @almond_couch_market.monitor(), true , v_title := title, v_price := price, v_contact := contact => @light-bulb.color_loop() ;
}
====
now => @builtin.get_random_between, ((low >= 55 && high >= 14) || (low > 14 && high >= 7 && random = 7)) , v_random := random => @google_drive.create_new_drive_file, (file_name =~ "12345" || file_name = "23456789") 
;;
Main() {
    now => @builtin.get_random_between(low=11, high=11), ((low < 7 && high >= 42)) , v_random := random => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
@icalendar.event_begin, (end_date = makeDate(1462320000000) && summary = "9876543210" && sequence >= 11 && location =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1487030400000) && description =~ "0123456788" && sequence = 14 && organizer =~ "0123456789" && location = "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@weatherapi.monitor, ((temperature < 22C && humidity = 55) || (temperature > 22C && humidity <= 11 && cloudiness >= 14 && fog > 14 && weather = "0123456788")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), (((location = $context.location.current_location && temperature = 73F && humidity > 14 && fog <= 14 && status = enum(drizzling)) || (temperature >= 73F && cloudiness = 11 && fog > 14 && weather =~ "0123456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => notify;
}
====
@holidays.uk_holiday_event, (description =~ "9876543210" || description =~ "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@xkcd.new_comic, ((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number < 14 && title = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    @xkcd.new_comic(), ((number > 11 && title = "9876543210" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @light-bulb.set_power(power=enum(off)) ;
}
====
* => * => @linkedin.share, true 
;;
Main() {
    @phdcomics.new_post(), (((title =~ "12345" && link = "http://www.google.com"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (title =~ "23456789" && link = "http://www.google.com"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @imgflip.generate(text_bottom=v_title, template="ABCDEFGHIJ", text_top="9876543210"), (template =~ "ABCDEFGHIJ") , v_name := name, v_picture_url := picture_url => @linkedin.share(status="12345") ;
}
====
@heartrate-monitor.getheartrate, ((time = makeDate(1487030400000) && heartrate > 7) || (time = makeDate(1462320000000) && heartrate <= 14) || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (time = makeDate(1462320000000) && heartrate = 11) || heartrate <= 7 || (time = makeDate(1487030400000) && place = $context.location.current_location) || (time = makeDate(1462320000000) && place = $context.location.work)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_time := time, v_place := place, v_heartrate := heartrate => @builtin.get_time(), true , v_time := time => notify;
}
====
* => @bing.image_search, (query = "9876543210" && width > 7 && height <= 42) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1462320000000) && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @bing.image_search(query="0123456789"), ((query =~ "12345" && title = "0123456788" && width > 14 && height >= 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @thermostat.set_minmax_temperature(low=22C, high=73F) ;
}
====
* => @twitter.search, (count < 7 && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    @heartrate-monitor.getheartrate(), (heartrate = 7) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.search(query="12345"), ((query = "0123456789" && count > 7 && contains(urls, "http://www.abc.def"^^tt:url) && from = "alice"^^tt:username && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@almond_couch_market.monitor, price <= 7 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    @almond_couch_market.monitor(), ((title = "ABCDEFGHIJ" && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @bluetooth_speaker.set_sink() ;
}
====
* => @fitbit.recordweight, true 
;;
Main() {
    @twitter.source(), (((from = "bob"^^tt:username && in_reply_to = "bob"^^tt:username) || from = "bob"^^tt:username || (text = "0123456788" && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @fitbit.recordweight(weight=155lb) ;
}
====
* => @speaker.lower_volume, true 
;;
Main() {
    @slack.receive(), (timestamp = "12345") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @speaker.lower_volume() ;
}
====
@nasa.asteroid, (asteroid_id = 14 && name = "9876543210" && estimated_diameter_min < 1000m && estimated_diameter_max <= 1000m && relative_velocity = 5kmph && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), ((asteroid_id >= 7 && name = "0123456788" && estimated_diameter_min > 42cm && relative_velocity <= 25mph && closest_distance_to_earth = 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @builtin.sorry, true 
;;
Main() {
    now => @builtin.sorry() ;
}
====
now => @almond_dates.search, ((poster =~ "23456789" && phone = "+16501234567"^^tt:phone_number) || (message =~ "0123456789" && poster = "23456789") || poster = "9876543210" || poster =~ "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.discover, true 
;;
Main() {
    now => @almond_dates.search(), (poster =~ "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @builtin.discover() ;
}
====
now => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    now => @imgflip.generate(template="9876543210", text_top="12345", text_bottom="23456789"), (((text_bottom =~ "23456789" && name = "9876543210") || text_top = "0123456789" || (template =~ "23456789" && text_bottom = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_name := name, v_picture_url := picture_url => @security-camera.set_power(power=enum(on)) ;
}
====
* => @nasa.rover, (count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => *
;;
Main() {
    now => @nasa.rover(), ((count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && camera_used = "0123456788")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="0123456788", message="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @instagram.get_pictures, (count = 14 && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(count=14), ((count > 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456788" && filter =~ "12345")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @thermostat.set_minmax_temperature, ((low <= 73F && high > 22C) || low <= 73F || low = 73F || low > 73F) 
;;
Main() {
    @phdcomics.new_post(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @thermostat.set_minmax_temperature(low=73F, high=73F) ;
}
====
now => * => @twitter.send_direct_message, (to = "bob"^^tt:username || (to = "bob"^^tt:username && message =~ "0123456788")) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((summary = "ABCDEFGHIJ" && description =~ "0123456789") || date = makeDate(1462320000000))) , v_date := date, v_summary := summary, v_description := description => @twitter.send_direct_message(to=v_description, message=v_description) ;
}
====
now => * => @facebook.post_picture, caption =~ "9876543210" 
;;
Main() {
    now => @almond_bike_market.ask(id="12345", property="23456789"), (id =~ "0123456788")  => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="12345") ;
}
====
* => @almond_dates.post, (message =~ "0123456789" && poster = "0123456788") 
;;
Main() {
    @github.new_issue_comment(repo_name="0123456788"), ((user = "alice"^^tt:username && issue_number < 7)) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @almond_dates.post(interest=v_body, message=v_body, poster=v_body, phone=v_body) ;
}
====
* => @gmail.get_latest, (subject =~ "ABCDEFGHIJ" && label =~ "9876543210") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((yield > 7 && div >= 55 && pay_date = makeDate(1487030400000)) || (company_name = "0123456788" && div > 11 && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @gmail.get_latest(), ((subject =~ "0123456789" || (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456788" && label = "ABCDEFGHIJ") || subject =~ "0123456788" || (from = "bob@stanford.edu"^^tt:email_address && subject =~ "12345") || label = "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @fitbit.getbody, (weight <= 155lb && bmi >= 42 && fat >= 14) , v_weight := weight, v_bmi := bmi, v_fat := fat => @heatpad.set_power, true 
;;
Main() {
    now => @fitbit.getbody(), ((weight < 82kg && bmi >= 42 && fat = 7)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @heatpad.set_power(power=enum(on)) ;
}
====
now => * => @fitbit.recordweight, weight = 155lb 
;;
Main() {
    now => @thermostat.get_hvac_state(), (state = enum(off)) , v_mode := mode, v_state := state => @fitbit.recordweight(weight=155lb) ;
}
====
* => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @heatpad.set_power(power=enum(off)) ;
}
====
* => @slack.updateChannelTopic, ((channel = "cat"^^tt:hashtag && topic =~ "0123456788") || topic = "0123456788" || topic = "ABCDEFGHIJ" || (channel = "lol"^^tt:hashtag && topic =~ "0123456788")) 
;;
Main() {
    now => @slack.updateChannelTopic(channel="lol"^^tt:hashtag, topic="0123456789") ;
}
====
now => @thecatapi.get, ((image_id = "0123456789" && link = "http://www.google.com"^^tt:url) || count = 7 || count >= 14 || (image_id = "12345" && count > 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id = "12345" || (image_id =~ "0123456789" && count <= 11 && link = "http://www.google.com"^^tt:url) || (image_id = "0123456789" && count >= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text(title="ABCDEFGHIJ", body="0123456789") ;
}
====
* => @slack.setPresence, true 
;;
Main() {
    now => @slack.setPresence(presence=enum(auto)) ;
}
====
@phone.receive_sms, (body = "0123456789" || body =~ "0123456789" || body = "ABCDEFGHIJ") , v_from := from, v_body := body => * => notify
;;
Main() {
    @phone.receive_sms(), (from = "+15551234567"^^tt:phone_number) , v_from := from, v_body := body => @nasa.apod(), (description = "12345") , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && distance < 1000m && steps <= 42 && activeTime < 2h && inactiveTime < 2h && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && distance <= 1000m && steps < 7 && activeTime >= 30min && inactiveTime < 30min && calories < 500kcal) || (distance >= 1000m && steps >= 14 && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => *
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1487030400000) && day =~ "0123456788" && distance >= 1000m && steps = 7 && activeTime > 2h && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && distance > 1000m && steps <= 42 && inactiveTime < 2h && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @bing.web_search(query=v_day), (title =~ "0123456788") , v_title := title, v_description := description, v_link := link => @omlet.send_picture(to=v_title, picture_url=v_description, caption=v_title) ;
}
====
* => @nasa.apod, true , v_title := title, v_description := description, v_picture_url := picture_url => *
;;
Main() {
    @almond_bike_market.monitor(), (price < 42)  => @nasa.apod(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @linkedin.get_profile, (headline =~ "0123456789" && contains(positions, "12345") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @rss.new_post(), ((title = "0123456789" || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @linkedin.get_profile(), (specialties =~ "ABCDEFGHIJ") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @xkcd.get_comic, (number >= 11 && title =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    now => @xkcd.get_comic(), ((number >= 7 && alt_text = "0123456788")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @bluetooth_speaker.play_music() ;
}
====
* => @linkedin.get_profile, (headline =~ "0123456788" || (headline =~ "23456789" && industry =~ "23456789") || (formatted_name =~ "12345" && industry =~ "0123456788" && contains(positions, "0123456789")) || (formatted_name = "0123456789" && headline = "23456789" && specialties = "ABCDEFGHIJ") || (formatted_name =~ "9876543210" && headline = "0123456789" && industry =~ "0123456789" && specialties =~ "0123456789" && contains(positions, "9876543210")) || (headline =~ "23456789" && industry = "0123456788") || (formatted_name =~ "0123456789" && headline =~ "12345" && industry = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline = "0123456788" && specialties =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "9876543210" && specialties = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @almond_bike_market.monitor(info="12345"), (((info =~ "23456789" && price <= 14) || price >= 7))  => @linkedin.get_profile(), ((formatted_name = "23456789" && industry = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@heartrate-monitor.getheartrate, ((time = makeDate(1462320000000) && heartrate = 42) || (place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate > 55)) , v_time := time, v_place := place, v_heartrate := heartrate => *
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate <= 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @dropbox.list_folder, (file_name = "ABCDEFGHIJ" && is_folder = false) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify
;;
Main() {
    @github.new_issue(repo_name="0123456789"), (((user = "alice"^^tt:username && number >= 11) || (number = 14 && title = "9876543210"))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @dropbox.list_folder(folder_name="12345"), (last_modified = makeDate(1462320000000)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
@yahoofinance.stock_div, ((stock_id = "msft"^^tt:stock_id("Microsoft") && yield < 42 && div = 11 && pay_date = makeDate(1487030400000)) || (company_name = "ABCDEFGHIJ" && yield <= 11 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => * => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((div > 55 && pay_date = makeDate(1462320000000) && ex_div_date = makeDate(1462320000000)) || (stock_id = "msft"^^tt:stock_id("Microsoft") && yield <= 55 && div > 7 && ex_div_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @omlet.send_to(to=v_company_name, message=v_company_name) ;
}
====
@holidays.us_holiday_event, summary = "12345" , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.us_holiday_event(), (date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => @car.honk_horn() ;
}
====
* => @weatherapi.sunrise, ((date = makeDate(1462320000000) && sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000)) || sunrise = makeDate(1487030400000) || (date = makeDate(1462320000000) && sunrise = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => *
;;
Main() {
    @nasa.asteroid(), ((asteroid_id >= 55 && name = "0123456789" && estimated_diameter_min > 42cm && estimated_diameter_max < 42cm && closest_distance_to_earth <= 1000m)) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @weatherapi.sunrise(location=$context.location.home), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @dropbox.move(old_name="12345", new_name="ABCDEFGHIJ") ;
}
====
* => @uber.price_estimate, ((end = $context.location.current_location && low_estimate > 7 && currency_code = "0123456788" && duration > 30min && distance > 1000m) || (uber_type = "9876543210" && low_estimate <= 55 && high_estimate >= 42 && currency_code = "0123456789" && duration < 30min && distance > 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @phdcomics.new_post(), true , v_title := title, v_link := link, v_picture_url := picture_url => @uber.price_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA"), end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && end = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && uber_type = "23456789" && low_estimate > 11 && high_estimate >= 42 && currency_code = "12345" && duration <= 30min && distance < 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
@activity-tracker.getmove, ((day =~ "23456789" && distance > 1000m && activeTime >= 30min) || (distance >= 42cm && steps > 42 && inactiveTime >= 30min) || (day =~ "23456789" && distance < 42cm && steps >= 11 && activeTime <= 2h) || (distance >= 42cm && steps = 7 && inactiveTime >= 30min) || (day = "0123456788" && distance > 42cm && steps > 42 && inactiveTime > 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance >= 1000m && steps >= 14 && inactiveTime <= 30min && calories < 500kcal) || (distance < 1000m && activeTime = 30min && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @wall_street_journal.get_opinions(), ((title = "12345" && updated = makeDate(1487030400000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => * => @almond_dates.post, true 
;;
Main() {
    @holidays.uk_holiday_event(), (description = "0123456789") , v_date := date, v_summary := summary, v_description := description => @xkcd.get_comic(), (((number = 42 && title =~ "9876543210" && alt_text = "23456789") || (number < 7 && title = "9876543210" && alt_text =~ "12345"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @almond_dates.post(interest="12345", message="0123456788", poster="ABCDEFGHIJ", phone="+15551234567"^^tt:phone_number) ;
}
====
now => * => @bluetooth_speaker.set_volume, (percent = 11 || percent >= 42) 
;;
Main() {
    now => @ytranslate.detect_language(text="ABCDEFGHIJ"), ((text =~ "ABCDEFGHIJ" || text = "0123456788" || text = "9876543210")) , v_detected_language := detected_language => @bluetooth_speaker.set_volume(percent=14) ;
}
====
now => * => @tumblr-blog.post_picture, (caption =~ "23456789" || caption = "23456789") 
;;
Main() {
    now => @builtin.get_time(), ((time = makeDate(1462320000000) || time = makeDate(1462320000000))) , v_time := time => @tumblr-blog.post_picture(caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@thermostat.temperature, ((time = makeDate(1462320000000) && value >= 73F) || (time = makeDate(1462320000000) && value > 73F)) , v_time := time, v_value := value => notify
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1462320000000) && value = 22C)) , v_time := time, v_value := value => notify;
}
====
now => * => @facebook.post, status = "23456789" 
;;
Main() {
    now => @dropbox.get_space_usage(), (total <= 5KB) , v_used := used, v_total := total => @facebook.post(status="ABCDEFGHIJ") ;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 22C && humidity > 55 && cloudiness < 11 && fog > 55 && weather = "23456789" && status = enum(sleety)) || (location = $context.location.work && humidity < 14 && cloudiness > 42 && fog < 14 && weather =~ "0123456789" && status = enum(drizzling)) || (cloudiness > 42 && fog < 7 && weather =~ "9876543210" && status = enum(sleety)) || (temperature <= 73F && wind_speed < 25mph && humidity < 14 && fog > 14 && weather =~ "9876543210") || (temperature >= 22C && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature > 73F && wind_speed < 25mph && cloudiness = 55 && fog >= 55 && status = enum(windy)) || (temperature < 73F && cloudiness >= 14 && weather =~ "9876543210" && status = enum(sleety)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @tumblr-blog.post_picture(caption=v_weather, picture_url=v_weather) ;
}
====
now => * => @lg_webos_tv.set_power, true 
;;
Main() {
    now => @lg_webos_tv.set_power(power=enum(on)) ;
}
====
* => @builtin.notify, (message =~ "12345" || message = "ABCDEFGHIJ" || message = "0123456788" || message = "0123456789" || message = "12345" || message =~ "23456789") 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && place = $context.location.current_location && heartrate = 55)) , v_time := time, v_place := place, v_heartrate := heartrate => @builtin.notify(message="9876543210") ;
}
====
now => @instagram.get_pictures, contains(hashtags, "funny"^^tt:hashtag) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post, (poster =~ v_filter || (message = "9876543210" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @instagram.get_pictures(), (((count > 55 && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (count >= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && filter = "0123456789"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(phone=v_caption, interest="0123456788", message="12345", poster="0123456788") ;
}
====
@washington_post.new_blog_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link => * => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(politics)), (title =~ "0123456789") , v_title := title, v_link := link => notify;
}
====
* => @gmail.send_email, (subject = "0123456789" || (to = "alice@gmail.com"^^tt:email_address && subject = "0123456789" && message = "ABCDEFGHIJ") || message =~ "23456789" || (to = "bob@stanford.edu"^^tt:email_address && message = "0123456789")) 
;;
Main() {
    @almond_dates.monitor(), (((message = "0123456788" && poster = "23456789") || (interest = "12345" && message =~ "9876543210" && poster = "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="12345", message="ABCDEFGHIJ") ;
}
====
* => @thermostat.get_hvac_state, true , v_mode := mode, v_state := state => notify
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @thermostat.get_hvac_state(), ((state = enum(heating) || (mode = enum(heat) && state = enum(off)))) , v_mode := mode, v_state := state => notify;
}
====
@phone.gps, (altitude > 1000m && bearing > 42) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((altitude > 42cm && bearing > 42 && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @weatherapi.sunrise(location=v_location), (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @builtin.get_random_between, (low >= 55 && high <= 11 && random <= 14) , v_random := random => notify
;;
Main() {
    @twitter.my_tweet(), ((text =~ "0123456789" && contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @builtin.get_random_between(low=55, high=7), (((low >= 14 && random = 14) || (low < 14 && high < 14 && random < 42))) , v_random := random => notify;
}
====
@gmail.receive_email, ((from_name = "23456789" && subject = "0123456788" && date = makeDate(1487030400000) && contains(labels, "0123456789") && snippet =~ "0123456789") || (contains(labels, "9876543210") && snippet =~ "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), ((subject =~ "0123456788" && snippet = "23456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @wall_street_journal.get_opinions(), true , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @almond_dates.post, phone = "+15551234567"^^tt:phone_number 
;;
Main() {
    now => @bing.image_search(query="0123456789"), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && width < 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @almond_dates.post(interest=v_title, poster=v_title, phone=v_title, message="ABCDEFGHIJ") ;
}
====
* => @builtin.get_random_between, (high >= 11 && random <= 7) , v_random := random => notify
;;
Main() {
    @wall_street_journal.world_news(), true , v_title := title, v_link := link => @builtin.get_random_between(low=7, high=7), (((high <= 42 && random >= 55) || (low > 7 && random < 55))) , v_random := random => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name = "0123456788" || file_name =~ "9876543210" || file_name = "0123456789" || file_name =~ "0123456789" || file_name = "0123456788") 
;;
Main() {
    @heartrate-monitor.getheartrate(), (((time = makeDate(1462320000000) && heartrate > 7) || (time = makeDate(1487030400000) && place = $context.location.work && heartrate <= 42))) , v_time := time, v_place := place, v_heartrate := heartrate => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
* => @google_drive.create_new_drive_file, file_name = "0123456788" 
;;
Main() {
    @almond_bike_market.monitor(info="23456789"), (info = "9876543210")  => @google_drive.create_new_drive_file(file_name="9876543210") ;
}
====
* => @builtin.thank_you, true 
;;
Main() {
    @almond_couch_market.monitor(), ((contact = "+15551234567"^^tt:phone_number || (price <= 7 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @builtin.thank_you() ;
}
====
now => @onedrive.delete_file, (file_name = "12345" || file_name =~ "23456789") 
;;
Main() {
    now => @onedrive.delete_file(file_name="12345") ;
}
====
* => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject = "12345") 
;;
Main() {
    @sleep-tracker.getsleep(), (((awakeTime = makeDate(1487030400000) && duration > 30min && rem >= 30min && light < 30min && deep <= 2h) || (time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1487030400000)) || (place = $context.location.current_location && asleepTime = makeDate(1487030400000) && light >= 30min && deep <= 2h) || (light >= 2h && deep >= 30min) || (time = makeDate(1487030400000) && duration < 30min && rem > 2h && light <= 2h) || (awakeTime = makeDate(1462320000000) && duration > 30min && rem >= 2h && light = 2h && deep <= 30min) || (awakeTime = makeDate(1462320000000) && duration < 2h && rem > 30min && light <= 30min && deep < 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="9876543210") ;
}
====
@instagram.new_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456789" && filter =~ "0123456788" && contains(hashtags, "lol"^^tt:hashtag)) || (caption =~ "9876543210" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && location = $context.location.work) || filter =~ "9876543210" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "12345" && filter =~ "12345" && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @icalendar.list_events(), ((summary =~ "23456789" && description = "12345" && sequence >= 55 && organizer = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @omlet.send_to, (to = "alice"^^tt:username || to = "alice"^^tt:username) 
;;
Main() {
    @wall_street_journal.lifestyle(), true , v_title := title, v_link := link => @omlet.send_to(message=v_title, to="alice"^^tt:username) ;
}
====
now => * => @speaker.set_sink, true 
;;
Main() {
    now => @twitter.search(query="23456789"), ((contains(hashtags, "lol"^^tt:hashtag) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @speaker.set_sink() ;
}
====
* => @facebook.post, (status = "ABCDEFGHIJ" || status = "12345") 
;;
Main() {
    @nasa.asteroid(), (((asteroid_id <= 7 && name = "0123456789" && dangerous = false && closest_distance_to_earth > 1000m) || (asteroid_id >= 11 && estimated_diameter_min <= 42cm && estimated_diameter_max < 1000m && relative_velocity <= 25mph) || (asteroid_id <= 11 && estimated_diameter_min > 1000m && relative_velocity <= 5kmph && closest_distance_to_earth > 1000m) || (asteroid_id = 7 && estimated_diameter_max >= 1000m && relative_velocity < 25mph && orbiting_body =~ "12345"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @facebook.post(status="0123456788") ;
}
====
* => @uber.time_estimate, (start = $context.location.work && uber_type =~ "0123456788" && estimate < 30min) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.current_location), (estimate <= 2h) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
* => @fitbit.getsteps, ((date = makeDate(1487030400000) && steps < 7) || steps >= 55 || steps > 7) , v_date := date, v_steps := steps => notify
;;
Main() {
    now => @fitbit.getsteps(), (steps < 7) , v_date := date, v_steps := steps => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && distance < 1000m && steps <= 42 && activeTime < 2h && inactiveTime < 2h && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && distance <= 1000m && steps < 7 && activeTime >= 30min && inactiveTime < 30min && calories < 500kcal) || (distance >= 1000m && steps >= 14 && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => *
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && distance = 42cm && activeTime <= 30min && inactiveTime < 2h && calories < 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @github.comment_issue(body=v_day, repo_name="0123456789", issue_number=7) ;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1487030400000) && end_date = makeDate(1462320000000) && organizer = "0123456788") || (end_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ" && sequence >= 14 && location =~ "23456789") || (start_date = makeDate(1462320000000) && description =~ "23456789" && organizer = "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1487030400000) && summary = "ABCDEFGHIJ" && description = "9876543210" && sequence = 11 && location =~ "ABCDEFGHIJ")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @gmail.send_email, subject = "23456789" 
;;
Main() {
    now => @builtin.get_time(), true , v_time := time => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="0123456789") ;
}
====
@instagram.new_picture, ((filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (filter = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => *
;;
Main() {
    @instagram.new_picture(), ((contains(hashtags, "funny"^^tt:hashtag) || (caption = "0123456788" && link = "http://www.google.com"^^tt:url && filter =~ "0123456788") || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && filter = "9876543210"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.get_random(), ((number <= 42 || number >= 55)) , v_number := number => @security-camera.set_power(power=enum(on)) ;
}
====
* => @weatherapi.moon, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || (location = $context.location.current_location && phase =~ "ABCDEFGHIJ") || location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && date = makeDate(1487030400000)) || date = makeDate(1487030400000)) , v_date := date, v_phase := phase => notify
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @weatherapi.moon(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) , v_date := date, v_phase := phase => notify;
}
====
* => @holidays.next_us_holiday_event, (description =~ "12345" || summary =~ "9876543210" || (date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description = "12345")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), ((watched_team_abbr = "chc"^^sportradar:mlb_team("Chicago Cubs") && watched_is_home = true && home_name = "23456789" && scheduled_time =~ "0123456788" && game_inning =~ "23456789" && away_runs >= 11 && home_runs < 14 && result = enum(lose))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @holidays.next_us_holiday_event(), ((date = makeDate(1487030400000) && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@bodytrace.source, weight > 155lb , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight > 82kg)) , v_time := time, v_weight := weight => notify;
}
====
now => @xkcd.random_comic, (number >= 42 && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), (((number >= 55 && alt_text = "9876543210") || (number <= 11 && title = "23456789" && alt_text =~ "9876543210") || (number > 11 && title = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text =~ "0123456789"))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@activity-tracker.getmove, (day =~ "0123456788" && distance >= 42cm && steps <= 14 && inactiveTime > 30min && calories <= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_dates.search, (interest = "9876543210" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "12345" && distance > 1000m && steps <= 14 && activeTime >= 2h && inactiveTime > 30min && calories <= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_dates.search(), (((interest =~ "ABCDEFGHIJ" && poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (message = "0123456788" && poster = "23456789") || (interest =~ "9876543210" && poster =~ "12345") || (interest =~ "23456789" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest = "9876543210" && message =~ "ABCDEFGHIJ" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (message = "ABCDEFGHIJ" && poster = "12345") || (interest = "0123456789" && message =~ "12345" && poster = "23456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.set_power(power=enum(on)) ;
}
====
* => @imgflip.generate, ((template =~ "12345" && text_top = "9876543210") || (text_top = "0123456789" && text_bottom =~ "ABCDEFGHIJ")) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="0123456789", text_top="12345", text_bottom="23456789"), ((template =~ "0123456789" && text_top =~ "23456789" && text_bottom =~ "12345" && name =~ "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @facebook.post, (status = "9876543210" || status =~ "9876543210" || status = "ABCDEFGHIJ" || status =~ "12345") 
;;
Main() {
    now => @facebook.post(status="12345") ;
}
====
* => @tumblr-blog.post_picture, (caption =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @tumblr.new_photo(blog_name="ABCDEFGHIJ"), (((blog_name =~ "ABCDEFGHIJ" && caption = "0123456789") || (date = makeDate(1462320000000) && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag) && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || contains(tags, "cat"^^tt:hashtag))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@heartrate-monitor.getheartrate, ((time = makeDate(1462320000000) && heartrate >= 55) || place = $context.location.work) , v_time := time, v_place := place, v_heartrate := heartrate => * => @bluetooth_speaker.play_music, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") || heartrate <= 11)) , v_time := time, v_place := place, v_heartrate := heartrate => @thermostat.get_temperature(), (value < 22C) , v_value := value => @bluetooth_speaker.play_music() ;
}
====
@scale.source, value > 155lb , v_time := time, v_value := value => notify
;;
Main() {
    @scale.source(), (value < 155lb) , v_time := time, v_value := value => notify;
}
====
@builtin.at, (time = makeTime(7,30) || time = makeTime(20,30))  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @imgflip.generate(template="9876543210", text_top="0123456788", text_bottom="0123456788"), (((name = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @security-camera.set_power(power=enum(off)) ;
}
====
* => @facebook.post, status = "ABCDEFGHIJ" 
;;
Main() {
    now => @facebook.post(status="23456789") ;
}
====
* => @imgflip.generate, (template = "12345" && text_top =~ "23456789" && text_bottom = "9876543210" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    @jawbone_up.getheartrate(), ((heartrate >= 11 || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate >= 7) || (time = makeDate(1487030400000) && place = $context.location.work && heartrate >= 11) || (place = $context.location.work && heartrate >= 7) || heartrate < 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @imgflip.generate(template="9876543210", text_top="0123456788", text_bottom="ABCDEFGHIJ"), ((template = "0123456789" && text_bottom =~ "0123456788")) , v_name := name, v_picture_url := picture_url => notify;
}
====
* => @light-bulb.alert_long, true 
;;
Main() {
    @reddit_front_page.newpost(), (user = "0123456788") , v_title := title, v_link := link, v_user := user, v_category := category => @light-bulb.alert_long() ;
}
====
now => * => @thermostat.set_target_temperature, (value < 73F || value = 73F) 
;;
Main() {
    now => @almond_couch_market.search(), ((price > 55 || price >= 7)) , v_title := title, v_price := price, v_contact := contact => @thermostat.set_target_temperature(value=73F) ;
}
====
* => @builtin.notify, (message =~ "12345" || message = "ABCDEFGHIJ" || message = "0123456788" || message = "0123456789" || message = "12345" || message =~ "23456789") 
;;
Main() {
    @gmail.receive_primary_email(), (((from_name =~ "0123456788" && subject = "12345" && date = makeDate(1462320000000) && contains(labels, "12345") && snippet = "0123456789") || from_name =~ "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @builtin.notify(message="0123456788") ;
}
====
@jawbone_up.getbody, ((place = $context.location.current_location && leanMass < 7 && bmi >= 55) || (leanMass > 14 && bodyFat > 7 && bmi <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @jawbone_up.getbody(), (((time = makeDate(1487030400000) && place = $context.location.home && weight > 82kg && bodyFat >= 42 && bmi >= 42) || (time = makeDate(1462320000000) && place = $context.location.home && weight <= 82kg && leanMass = 7 && bodyFat = 7 && bmi < 11))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@sportradar.soccer_us_team, ((watched_team_alias = "tor"^^sportradar:us_soccer_team("Toronto FC") && other_team_alias = "sje"^^sportradar:us_soccer_team("San Jose Earthquakes") && watched_is_home = false && away_name =~ "9876543210" && scheduled_time = makeDate(1487030400000) && away_points < 11 && home_points >= 7) || (home_name =~ "9876543210" && away_points >= 11 && home_points >= 55)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.soccer_us_team(watched_team_alias="sje"^^sportradar:us_soccer_team("San Jose Earthquakes")), ((away_name = "23456789" && home_name = "23456789" && away_points >= 11)) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
now => * => @github.comment_issue, (issue_number > 14 || repo_name =~ "9876543210") 
;;
Main() {
    now => @github.comment_issue(repo_name="0123456788", issue_number=7, body="23456789") ;
}
====
@almond_couch_market.monitor, (price < 14 || title =~ "9876543210") , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), (price > 55) , v_title := title, v_price := price, v_contact := contact => @twitter.sink(status=v_title) ;
}
====
@builtin.timer, (interval >= 30min || interval < 30min)  => * => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval = 2h)  => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @heatpad.set_power(power=enum(on)) ;
}
====
@github.new_commit, (message =~ "0123456789" && time = makeDate(1462320000000)) , v_user := user, v_message := message, v_time := time => * => notify
;;
Main() {
    @github.new_commit(repo_name="12345"), true , v_user := user, v_message := message, v_time := time => @builtin.get_random(), (number >= 7) , v_number := number => notify;
}
====
* => @security-camera.set_power, power = enum(on) 
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "0123456788" && description = "9876543210" && organizer = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @security-camera.set_power(power=enum(on)) ;
}
====
@github.new_milestone, ((repo_name =~ "0123456789" && description = "0123456788") || description = "0123456789") , v_user := user, v_description := description, v_time := time => notify
;;
Main() {
    @github.new_milestone(repo_name="ABCDEFGHIJ"), ((repo_name =~ "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ")) , v_user := user, v_description := description, v_time := time => notify;
}
====
* => @car.honk_horn, true 
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "0123456788") , v_file_name := file_name => @car.honk_horn() ;
}
====
* => @almond_couch_market.search, (title = "23456789" && contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), (title =~ "23456789") , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => @thermostat.get_hvac_state, (mode = enum(heat) || state = enum(heating)) , v_mode := mode, v_state := state => notify
;;
Main() {
    now => @thermostat.get_hvac_state(), ((state = enum(cooling) || state = enum(heating) || state = enum(heating) || mode = enum(heat))) , v_mode := mode, v_state := state => notify;
}
====
* => @phone.get_gps, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && bearing >= 14 && speed > 5kmph) || (altitude <= 42cm && speed >= 25mph) || (altitude <= 42cm && bearing >= 14 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), ((bearing <= 7 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @phone.notify, true 
;;
Main() {
    now => @phone.notify(title="ABCDEFGHIJ", message="0123456789") ;
}
====
@heartrate-monitor.getheartrate, (heartrate > 7 || time = makeDate(1487030400000) || (place = $context.location.work && heartrate >= 55)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1462320000000) || heartrate < 7)) , v_time := time, v_place := place, v_heartrate := heartrate => @linkedin.get_profile(), (((formatted_name = "12345" && headline =~ "0123456788" && industry =~ "0123456789" && specialties = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (specialties = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || headline =~ "ABCDEFGHIJ" || formatted_name = "12345")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@icalendar.event_begin, (summary =~ "9876543210" && sequence >= 7 && location = "12345") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @wall_street_journal.get_opinions, ((link = "http://www.google.com"^^tt:url && updated = makeDate(1462320000000)) || title =~ "23456789") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1487030400000) && description =~ "0123456788" && sequence <= 7 && organizer = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @wall_street_journal.get_opinions(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @thecatapi.get, (image_id = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @sportradar.soccer_eu_tourney(tournament_search_term="12345"), (((tournament_search_term =~ "9876543210" && tournament_full_name = "ABCDEFGHIJ" && home_name = "0123456788" && away_points >= 11 && home_points > 11) || (tournament_search_term = "ABCDEFGHIJ" && tournament_full_name =~ "0123456789" && tournament_league_name = "0123456789" && home_alias =~ "12345" && home_name = "0123456789"))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @thecatapi.get(), (((image_id =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url) || (image_id =~ "0123456788" && count > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @onedrive.delete_file, (file_name =~ "23456789" || file_name =~ "0123456789") 
;;
Main() {
    now => @onedrive.delete_file(file_name="9876543210") ;
}
====
* => @slack.send_picture, true 
;;
Main() {
    @almond_dates.monitor(), (((poster = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest = "0123456788" && message = "23456789" && phone = "+16501234567"^^tt:phone_number) || interest =~ "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @slack.send_picture(channel="cat"^^tt:hashtag, caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @linkedin.share, status =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @thecatapi.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @linkedin.share(status=v_image_id) ;
}
====
* => @imgflip.generate, (template = "ABCDEFGHIJ" && name =~ "0123456789") , v_name := name, v_picture_url := picture_url => *
;;
Main() {
    now => @imgflip.generate(template="12345", text_top="23456789", text_bottom="0123456789"), ((text_top =~ "0123456788" && text_bottom =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => @linkedin.share(status=v_name) ;
}
====
* => @phone.get_gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && altitude >= 1000m && bearing >= 7 && speed < 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && rem <= 2h && light < 2h && deep < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @phone.get_gps(), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 1000m && bearing >= 55 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
now => @instagram.get_pictures, true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(count=14), ((count < 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @builtin.discover() ;
}
====
* => @almond_couch_market.post, (title = "9876543210" && price < 14 && contact = "+16501234567"^^tt:phone_number) 
;;
Main() {
    @washington_post.new_article(section=enum(politics)), (((section = enum(opinions) && title = "9876543210" && link = "http://www.google.com"^^tt:url) || title =~ "12345")) , v_title := title, v_link := link, v_description := description => @almond_couch_market.post(title=v_description, price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
* => @dropbox.create_new_folder, folder_name = "9876543210" 
;;
Main() {
    @google_drive.new_drive_file(), (file_name =~ "0123456789") , v_file_name := file_name => @dropbox.create_new_folder(folder_name=v_file_name) ;
}
====
@slack.receive, timestamp =~ "0123456788" , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), (((channel = "lol"^^tt:hashtag && timestamp = "0123456789") || (timestamp = "9876543210" && message =~ "ABCDEFGHIJ"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @speaker.raise_volume() ;
}
====
* => @builtin.discover, true 
;;
Main() {
    now => @builtin.discover() ;
}
====
@phone.gps, (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && bearing < 11 && speed = 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    @phone.gps(), ((altitude >= 1000m && bearing < 42 && speed > 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @slack.updateChannelTopic(channel="funny"^^tt:hashtag, topic="ABCDEFGHIJ") ;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @phone.receive_sms(), true , v_from := from, v_body := body => @gmail.send_email(to=v_body, subject=v_body, message="ABCDEFGHIJ") ;
}
====
@nasa.asteroid, (asteroid_id > 42 && estimated_diameter_min >= 42cm && estimated_diameter_max > 1000m && relative_velocity >= 5kmph && closest_distance_to_earth > 1000m) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 7 && name = "12345" && estimated_diameter_min <= 1000m && estimated_diameter_max > 1000m && relative_velocity < 5kmph) || (asteroid_id > 55 && name = "0123456788" && estimated_diameter_min <= 42cm && estimated_diameter_max >= 1000m && relative_velocity <= 5kmph && closest_distance_to_earth = 42cm && orbiting_body = "12345") || (asteroid_id < 11 && relative_velocity >= 5kmph && closest_distance_to_earth > 1000m))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @ytranslate.detect_language(text="0123456788"), ((text =~ "23456789" || text = "ABCDEFGHIJ")) , v_detected_language := detected_language => notify;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), (weight <= 155lb) , v_time := time, v_weight := weight => @car.honk_horn() ;
}
====
* => * => @builtin.thank_you, true 
;;
Main() {
    @almond_couch_market.monitor(), ((title = "0123456788" || (title = "12345" && price <= 42))) , v_title := title, v_price := price, v_contact := contact => @fitbit.getsteps(), (((date = makeDate(1487030400000) && steps < 11) || steps > 55)) , v_date := date, v_steps := steps => @builtin.thank_you() ;
}
====
@rss.new_post, ((title =~ "0123456788" && link = "http://www.abc.def"^^tt:url) || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url || (title = "0123456789" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), (title =~ "0123456789") , v_title := title, v_link := link => @almond_bike_market.ask_seller(id=v_title, property=v_title, value="12345", public=false), ((value =~ "0123456788" && public = false)) , v_answer := answer => notify;
}
====
@activity-tracker.getmove, ((updateTime = makeDate(1487030400000) && day = "0123456789" && steps > 42 && activeTime >= 2h && calories <= 500kcal) || (day =~ "12345" && activeTime > 2h && inactiveTime >= 2h && calories <= 500kcal) || (distance < 42cm && activeTime >= 2h && inactiveTime >= 2h && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((day = "9876543210" && steps > 14 && activeTime <= 2h && inactiveTime > 2h) || (updateTime = makeDate(1462320000000) && day = "0123456789" && steps < 7 && activeTime < 30min && inactiveTime > 2h && calories > 500kcal) || (distance <= 1000m && steps <= 11 && activeTime < 2h && inactiveTime < 30min && calories > 500kcal) || inactiveTime <= 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @gmail.get_latest(), (label = "23456789") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@almond_bike_market.monitor, (info = "0123456789" && price > 11)  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=7), (((info =~ "12345" && price < 14) || (info =~ "0123456788" && price >= 42)))  => @builtin.get_time(), true , v_time := time => notify;
}
====
now => @icalendar.list_events, ((summary =~ "12345" && sequence >= 14) || (summary =~ "0123456789" && organizer =~ "0123456788")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((summary = "23456789" || (description =~ "12345" && organizer =~ "9876543210") || (end_date = makeDate(1462320000000) && description = "9876543210" && sequence > 7 && organizer =~ "ABCDEFGHIJ" && location =~ "23456789") || description =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@sportradar.ncaafb_team, (other_team_abbr = "cal"^^sportradar:ncaafb_team("California Bears") && away_name = "23456789" && home_points < 11) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify
;;
Main() {
    @sportradar.ncaafb_team(watched_team_abbr="cal"^^sportradar:ncaafb_team("California Bears")), ((away_name =~ "0123456789" && away_points >= 11 && home_points > 42 && result = enum(unclosed))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => notify;
}
====
@security-camera.new_event, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @builtin.get_time(), true , v_time := time => notify;
}
====
@youtube.source, video_id = "9876543210" , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => *
;;
Main() {
    @youtube.source(), ((channel_title =~ "12345" || (channel_id = "ABCDEFGHIJ" && title = "0123456789"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @bluetooth_speaker.raise_volume() ;
}
====
* => * => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || (title =~ "0123456788" && body = "12345") || body =~ "12345") 
;;
Main() {
    @twitter.source(), ((contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @tumblr-blog.post_text(title=v_text, body=v_text) ;
}
====
now => @thecatapi.get, ((image_id = "0123456789" && link = "http://www.google.com"^^tt:url) || count = 7 || count >= 14 || (image_id = "12345" && count > 7)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "12345" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @fitbit.recordweight(weight=155lb) ;
}
====
now => * => @onedrive.rename_file, (old_name =~ "23456789" && new_name = "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_bike_market.ask_seller(id="0123456789", property="12345", value="12345", public=false), (((value = "ABCDEFGHIJ" && answer = "0123456788") || (property =~ "0123456788" && value =~ "ABCDEFGHIJ"))) , v_answer := answer => @onedrive.rename_file(old_name="0123456788", new_name="ABCDEFGHIJ") ;
}
====
@thermostat.temperature, true , v_time := time, v_value := value => @phone.get_gps, ((bearing < 11 && speed = 5kmph) || (altitude <= 42cm && bearing = 55 && speed < 25mph) || altitude >= 42cm || (altitude = 1000m && speed >= 5kmph) || (bearing > 55 && speed >= 25mph) || (altitude = 42cm && bearing > 55 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @thermostat.temperature(), true , v_time := time, v_value := value => @phone.get_gps(), ((bearing <= 11 && speed <= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@google_drive.new_drive_file, file_name = "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @onedrive.upload_picture(file_name=v_file_name, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@youtube.source, ((channel_id =~ "9876543210" && channel_title = "9876543210" && title = "12345") || (channel_id = "9876543210" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_id = "23456789" && channel_title =~ "ABCDEFGHIJ")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
* => @almond_dates.post, ((interest = "0123456789" && message = "23456789") || (interest =~ "0123456789" && phone = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    @twitter.source(), ((contains(urls, "http://www.abc.def"^^tt:url) || (text =~ "23456789" && contains(urls, "http://www.abc.def"^^tt:url) && from = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_from := from, v_in_reply_to := in_reply_to, v___reserved := __reserved => @almond_dates.post(interest=v_text, message=v_text, poster=v_text, phone=v_text) ;
}
====
now => * => @github.comment_issue, true 
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1462320000000) || count < 7 || (date_taken = makeDate(1487030400000) && count = 11))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @github.comment_issue(repo_name=v_camera_used, issue_number=11, body="9876543210") ;
}
====
* => @fitbit.recordweight, (weight < 155lb || weight = 82kg || weight > 155lb) 
;;
Main() {
    @gmail.receive_email(), (((from_name =~ "23456789" && from_address = "bob@stanford.edu"^^tt:email_address && subject = "12345") || from_address = "alice@gmail.com"^^tt:email_address)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @fitbit.recordweight(weight=155lb) ;
}
====
* => @fitbit.recordweight, (weight < 155lb || weight = 82kg || weight > 155lb) 
;;
Main() {
    @instagram.new_picture(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=155lb) ;
}
====
now => @dropbox.get_space_usage, true , v_used := used, v_total := total => notify
;;
Main() {
    now => @dropbox.get_space_usage(), true , v_used := used, v_total := total => notify;
}
====
* => @youtube.channels_by_category, channel_id = "ABCDEFGHIJ" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.channels_by_category(category_id=enum(paid)), (((channel_id = "0123456788" && title = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) || (channel_id = "0123456788" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@yahoofinance.stock_quote, bid_price <= 42 , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), (((stock_id = "aapl"^^tt:stock_id("Apple") && bid_price > 7) || (ask_price > 14 && bid_price >= 11))) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @builtin.get_random(), (number >= 14) , v_number := number => notify;
}
====
@onedrive.file_created, (file_name =~ "0123456789" || file_name =~ "0123456789" || file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @fitbit.getbody(), (((weight > 155lb && bmi <= 14 && fat <= 11) || weight >= 82kg)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @twitter.search_by_hashtag, (query_hashtag = "cat"^^tt:hashtag && text = "9876543210" && contains(urls, "http://www.google.com"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @icalendar.event_begin(), ((sequence < 7 && organizer =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @twitter.search_by_hashtag(query_hashtag=v_organizer, in_reply_to=v_location, count=11), ((query_hashtag = "funny"^^tt:hashtag && count > 7 && text =~ "0123456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => * => @almond_couch_market.post, true 
;;
Main() {
    now => @dropbox.list_folder(folder_name="0123456788"), ((file_name = "ABCDEFGHIJ" && last_modified = makeDate(1462320000000) && full_path = "0123456788")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @almond_couch_market.post(title="ABCDEFGHIJ", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
@youtube.source, ((channel_id = "9876543210" && channel_title = "ABCDEFGHIJ" && title =~ "23456789" && video_id =~ "9876543210") || (channel_title =~ "ABCDEFGHIJ" && title =~ "9876543210" && video_id =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((video_url = "http://www.google.com"^^tt:url || (channel_id = "23456789" && video_id = "0123456788"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @builtin.get_time(), (time = makeDate(1487030400000)) , v_time := time => notify;
}
====
@almond_dates.monitor, ((interest =~ "23456789" && poster = "0123456789") || (message =~ "12345" && poster = "9876543210") || phone = "+16501234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), ((interest = "12345" && message =~ "0123456788" && poster =~ "ABCDEFGHIJ" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @nasa.apod, ((title = "12345" && description = "0123456789") || title =~ "ABCDEFGHIJ") , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @bluetooth_speaker.lower_volume, true 
;;
Main() {
    @fitness-tracker.getbody(), ((time = makeDate(1462320000000) && weight >= 82kg && leanMass < 11 && bodyFat >= 42 && bmi <= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @bluetooth_speaker.lower_volume() ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && label = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @gmail.get_latest(), (label = "23456789") , v_from := from, v_subject := subject, v_label := label => @almond_dates.post(message=v_subject, phone=v_subject, interest="0123456789", poster="12345") ;
}
====
* => @security-camera.get_snapshot, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => *
;;
Main() {
    @builtin.timer(interval=30min), (interval <= 2h)  => @security-camera.get_snapshot(), true , v_picture_url := picture_url => notify;
}
====
* => @ytranslate.detect_language, ((text =~ "12345" && detected_language = "zh"^^tt:iso_lang_code("Chinese")) || text =~ "12345") , v_detected_language := detected_language => *
;;
Main() {
    now => @ytranslate.detect_language(text="0123456789"), (text = "0123456788") , v_detected_language := detected_language => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="ABCDEFGHIJ") ;
}
====
now => @xkcd.random_comic, number < 42 , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number < 7 && title = "12345" && alt_text =~ "9876543210")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@holidays.uk_holiday_event, ((summary =~ "9876543210" && description = "9876543210") || description = "ABCDEFGHIJ" || (summary = "0123456789" && description =~ "23456789") || summary = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((summary =~ "23456789" || (date = makeDate(1487030400000) && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @bluetooth_speaker.set_volume, (percent = 11 || percent >= 42) 
;;
Main() {
    now => @bluetooth_speaker.set_volume(percent=7) ;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (((title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && description =~ "9876543210") || description =~ "23456789")) , v_title := title, v_link := link, v_description := description => @giphy.get_tag(tag="lol"^^tt:hashtag), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @slack.send_picture(channel="cat"^^tt:hashtag, caption="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @almond_bike_market.post, ((price >= 11 && info = "0123456789") || poster = "12345") 
;;
Main() {
    now => @almond_dates.search(), (interest =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @almond_bike_market.post(title=v_interest, info=v_poster, poster=v_poster, price=7) ;
}
====
* => * => @omlet.send_to, message =~ "0123456788" 
;;
Main() {
    now => @omlet.send_to(to="alice"^^tt:username, message="0123456788") ;
}
====
* => @imgflip.generate, ((template =~ "9876543210" && text_top =~ "ABCDEFGHIJ" && name = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (template = "ABCDEFGHIJ" && text_bottom =~ "ABCDEFGHIJ")) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.generate(template="23456789", text_top="0123456789", text_bottom="0123456788"), ((template =~ "0123456788" || (text_top = "23456789" && text_bottom =~ "ABCDEFGHIJ"))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@jawbone_up.getheartrate, ((time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && heartrate <= 42) || time = makeDate(1462320000000)) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @jawbone_up.getheartrate(), (heartrate < 14) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
@gmail.receive_primary_email, (from_name = "9876543210" && date = makeDate(1487030400000) && snippet = "0123456788") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_primary_email(), (((subject =~ "23456789" && contains(labels, "9876543210") && snippet =~ "12345") || from_address = "alice@gmail.com"^^tt:email_address)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
now => @ytranslate.detect_language, text = "0123456789" , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="12345"), true , v_detected_language := detected_language => notify;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1462320000000) && duration >= 30min && rem >= 30min && light > 2h && deep <= 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && awakeTime = makeDate(1462320000000) && rem > 30min && light <= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @holidays.next_us_holiday_event(), (summary = "9876543210") , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@slack.receive, ((from = "alice"^^tt:username && message =~ "0123456788") || (from = "alice"^^tt:username && message = "9876543210") || (channel = "funny"^^tt:hashtag && from = "bob"^^tt:username && message = "12345") || (from = "bob"^^tt:username && message =~ "0123456788")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), ((channel = "cat"^^tt:hashtag && timestamp = "23456789" && from = "bob"^^tt:username && message = "0123456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
@fitness-tracker.getbody, ((time = makeDate(1487030400000) && weight <= 155lb && leanMass > 11 && bodyFat <= 42 && bmi > 7) || (time = makeDate(1462320000000) && place = $context.location.home && leanMass >= 42 && bmi <= 42) || (leanMass <= 11 && bodyFat > 55 && bmi < 11) || (weight <= 82kg && leanMass < 55 && bodyFat > 7 && bmi < 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @security-camera.set_power, true 
;;
Main() {
    @fitness-tracker.getbody(), (((weight < 155lb && leanMass >= 7 && bodyFat >= 11 && bmi > 42) || (weight > 82kg && bmi > 14))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @security-camera.set_power(power=enum(off)) ;
}
====
@reddit_front_page.newpost, (category = "23456789" || (title = "0123456789" && link = "http://www.abc.def"^^tt:url) || (title =~ "0123456789" && link = "http://www.abc.def"^^tt:url) || link = "http://www.abc.def"^^tt:url || (user =~ "0123456788" && category = "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((title = "9876543210" && category =~ "0123456788") || category = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => @dropbox.create_new_folder(folder_name=v_user) ;
}
====
now => * => @light-bulb.alert_long, true 
;;
Main() {
    now => @thermostat.get_temperature(), ((value > 73F || value < 73F || value >= 22C)) , v_value := value => @light-bulb.alert_long() ;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => notify;
}
====
* => @xkcd.random_comic, alt_text = "0123456788" , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @phdcomics.new_post(), (((link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || link = "http://www.google.com"^^tt:url || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url) || (title =~ "23456789" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_picture_url := picture_url => @xkcd.random_comic(), ((number > 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name = "0123456788" || file_name =~ "23456789" || file_name = "12345") 
;;
Main() {
    @almond_dates.monitor(), (message =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
* => @imgflip.list, name = "12345" , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), ((name =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => @security-camera.get_url, link = "http://www.abc.def"^^tt:url , v_link := link => notify
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
now => * => @tumblr-blog.post_picture, (caption =~ "23456789" || caption = "23456789") 
;;
Main() {
    now => @icalendar.list_events(), (((start_date = makeDate(1462320000000) && description = "9876543210" && sequence < 14 && organizer =~ "0123456788") || (end_date = makeDate(1462320000000) && summary = "12345" && description = "23456789" && sequence >= 7 && organizer = "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @tumblr-blog.post_picture(caption=v_location, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @twitter.send_direct_message, (to = "bob"^^tt:username && message =~ "0123456789") 
;;
Main() {
    @yahoofinance.stock_div(stock_id="goog"^^tt:stock_id("Google")), (((yield >= 55 && div < 14) || (stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "0123456788" && yield < 11) || (yield <= 7 && div > 7 && pay_date = makeDate(1462320000000)))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @twitter.send_direct_message(to="bob"^^tt:username, message="ABCDEFGHIJ") ;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1462320000000) && place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && asleepTime = makeDate(1462320000000) && light > 2h) || (time = makeDate(1487030400000) && place = $context.location.home && light < 2h && deep <= 30min) || (awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && rem < 30min && light >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => *
;;
Main() {
    @sleep-tracker.getsleep(), ((asleepTime = makeDate(1462320000000) && rem >= 2h && light <= 2h && deep < 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @dropbox.create_new_folder(folder_name="0123456789") ;
}
====
@yahoofinance.stock_quote, ask_price > 55 , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => *
;;
Main() {
    @yahoofinance.stock_quote(stock_id="msft"^^tt:stock_id("Microsoft")), (((stock_id = "msft"^^tt:stock_id("Microsoft") && company_name = "0123456789" && bid_price <= 42) || bid_price >= 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @phone.call_emergency() ;
}
====
now => * => @dropbox.create_new_folder, (folder_name =~ "12345" || folder_name = "0123456789" || folder_name =~ "9876543210" || folder_name =~ "ABCDEFGHIJ") 
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag), ((query_hashtag = "cat"^^tt:hashtag && count < 55 && text =~ "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @dropbox.create_new_folder(folder_name="0123456789") ;
}
====
* => @bing.image_search, (query =~ "0123456789" && title =~ "0123456788" && height >= 55) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @linkedin.share, status =~ "0123456789" 
;;
Main() {
    @wall_street_journal.world_news(), true , v_title := title, v_link := link => @bing.image_search(query="9876543210"), ((query =~ "23456789" && width = 14 && height >= 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @linkedin.share(status="0123456788") ;
}
====
now => * => @almond_dates.post, (poster =~ "0123456789" || (message =~ "12345" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    now => @phone.get_gps(), ((speed = 5kmph || (bearing < 7 && speed = 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_dates.post(interest="0123456789", message="9876543210", poster="ABCDEFGHIJ", phone="+16501234567"^^tt:phone_number) ;
}
====
now => @almond_bike_market.post, ((title =~ "9876543210" && price <= 14 && poster =~ "0123456788") || (title =~ "12345" && price <= 7 && poster =~ "12345") || (title = "0123456789" && price >= 11 && poster = "23456789") || (price > 7 && info =~ "0123456788" && poster = "0123456789")) 
;;
Main() {
    now => @almond_bike_market.post(title="9876543210", price=55, info="0123456788", poster="ABCDEFGHIJ") ;
}
====
now => @twitter.search, (query =~ "0123456789" && count <= 14 && text =~ "0123456789" && contains(urls, "http://www.abc.def"^^tt:url)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @onedrive.rename_file, old_name = "23456789" 
;;
Main() {
    now => @twitter.search(query="9876543210", count=42), ((count > 55 && text = "12345" && contains(hashtags, "cat"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @onedrive.rename_file(old_name="ABCDEFGHIJ", new_name="0123456789") ;
}
====
@almond_bike_market.monitor, price <= 11  => notify
;;
Main() {
    @almond_bike_market.monitor(price=11), true  => notify;
}
====
now => @almond_bike_market.detail, true  => @github.add_email, true 
;;
Main() {
    now => @almond_bike_market.detail(id="0123456789"), ((id =~ "12345" || id = "9876543210"))  => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @omlet.send_to, (message =~ "0123456788" || message =~ "ABCDEFGHIJ" || message = "0123456788" || to = "bob"^^tt:username || (to = "bob"^^tt:username && message = "12345")) 
;;
Main() {
    now => @omlet.send_to(to="bob"^^tt:username, message="0123456788") ;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @washington_post.new_blog_post(section=enum(post_partisan)), (title =~ "12345") , v_title := title, v_link := link => @phone.get_gps(), (((location = $context.location.home && altitude <= 42cm && bearing <= 42 && speed > 25mph) || (altitude = 1000m && speed <= 25mph) || (location = $context.location.current_location && altitude >= 42cm && bearing >= 11))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @security-camera.set_power(power=enum(off)) ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight < 155lb) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight <= 155lb) , v_time := time, v_weight := weight => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address && subject = "0123456789")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @facebook.post_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    @onedrive.file_created(), true , v_file_name := file_name => @facebook.post_picture(picture_url=v_file_name, caption=v_file_name) ;
}
====
* => @onedrive.rename_file, true 
;;
Main() {
    @bodytrace.source(), (weight <= 82kg) , v_time := time, v_weight := weight => @onedrive.rename_file(old_name="23456789", new_name="9876543210") ;
}
====
* => @youtube.list_subscriptions, (channel_id = "ABCDEFGHIJ" && title =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => *
;;
Main() {
    now => @youtube.list_subscriptions(), (((channel_id = "ABCDEFGHIJ" && title =~ "9876543210" && description =~ "23456789") || channel_id =~ "0123456788")) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @heatpad.set_power(power=enum(off)) ;
}
====
* => * => @fitbit.recordweight, true 
;;
Main() {
    @omlet.newmessage(), ((type = enum(picture) || message = "0123456788")) , v_type := type, v_message := message => @imgflip.list(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_name := name, v_picture_url := picture_url => @fitbit.recordweight(weight=82kg) ;
}
====
now => * => @almond_dates.post, (interest = "23456789" && phone = "+16501234567"^^tt:phone_number) 
;;
Main() {
    now => @nasa.apod(), ((title = "ABCDEFGHIJ" || (title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_description := description, v_picture_url := picture_url => @almond_dates.post(interest=v_description, message=v_description, poster=v_description, phone="+15551234567"^^tt:phone_number) ;
}
====
@almond_dates.monitor, (poster = "23456789" || message = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @almond_dates.monitor(), (((message = "0123456788" && poster =~ "23456789") || (interest =~ "0123456789" && message =~ "23456789" && poster =~ "12345") || (message =~ "0123456789" && poster =~ "0123456789" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "0123456789" && poster = "12345"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @onedrive.upload_picture(file_name="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@weatherapi.monitor, ((temperature >= 22C && wind_speed > 25mph && humidity >= 7 && cloudiness > 7 && fog > 55) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed <= 25mph && humidity = 14 && cloudiness > 7 && fog >= 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((temperature >= 73F && wind_speed > 25mph && humidity >= 14 && cloudiness >= 14 && status = enum(raining)) || (wind_speed > 25mph && humidity >= 14 && cloudiness < 11 && fog < 14 && weather = "23456789" && status = enum(snowy)) || (temperature >= 73F && wind_speed = 5kmph && cloudiness > 55 && weather = "0123456789" && status = enum(sleety)) || (humidity <= 7 && weather =~ "23456789") || (fog > 42 && weather = "0123456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @builtin.get_date(), true , v_date := date => notify;
}
====
* => @gmail.send_email, ((subject = "0123456788" && message =~ "0123456789") || (to = "bob@stanford.edu"^^tt:email_address && subject =~ "0123456789") || (to = "bob@stanford.edu"^^tt:email_address && subject = "12345")) 
;;
Main() {
    @phdcomics.new_post(), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="ABCDEFGHIJ", message="12345") ;
}
====
@almond_dates.monitor, poster = "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "0123456788" && message = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => @gmail.get_latest, ((from = "bob@stanford.edu"^^tt:email_address && subject =~ "ABCDEFGHIJ") || from = "bob@stanford.edu"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && label = "9876543210") || label =~ "9876543210" || subject =~ "0123456788" || subject =~ "9876543210") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), (from = "bob@stanford.edu"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "bob@stanford.edu"^^tt:email_address && subject = "0123456789" && label = "0123456789") || (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") || from = "bob@stanford.edu"^^tt:email_address || label = "23456789" || subject = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    @jawbone_up.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address || subject = "0123456789")) , v_from := from, v_subject := subject, v_label := label => @builtin.hello() ;
}
====
* => @gmail.send_picture, ((to = "alice@gmail.com"^^tt:email_address && subject = "0123456789" && message = "0123456788") || (to = "bob@stanford.edu"^^tt:email_address && message = "9876543210") || (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456788")) 
;;
Main() {
    @reddit_front_page.newpost(), ((title = "ABCDEFGHIJ" || (user =~ "23456789" && category = "0123456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @gmail.send_picture(to=v_user, message=v_user, picture_url=v_user, subject="0123456789") ;
}
====
* => @uber.time_estimate, (uber_type = "0123456789" || (start = $context.location.work && uber_type =~ "9876543210")) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    @fitness-tracker.getbody(), (((time = makeDate(1487030400000) && weight >= 82kg && leanMass < 7 && bodyFat <= 55 && bmi > 42) || (time = makeDate(1462320000000) && weight < 155lb && leanMass > 7 && bodyFat < 14) || (weight > 155lb && bodyFat = 55 && bmi >= 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @uber.time_estimate(start=v_place), (((uber_type =~ "0123456788" && estimate <= 30min) || (uber_type =~ "12345" && estimate > 2h))) , v_uber_type := uber_type, v_estimate := estimate => @facebook.post(status=v_uber_type) ;
}
====
@onedrive.file_modified, (file_name =~ "ABCDEFGHIJ" || file_name = "0123456789" || file_name =~ "23456789" || file_name = "9876543210" || file_name =~ "0123456789") , v_file_name := file_name => notify
;;
Main() {
    @onedrive.file_modified(), (file_name =~ "ABCDEFGHIJ") , v_file_name := file_name => notify;
}
====
* => @xkcd.get_comic, ((number > 7 && link = "http://www.abc.def"^^tt:url) || (link = "http://www.google.com"^^tt:url && alt_text = "23456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @holidays.uk_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @xkcd.get_comic(number=11), (((title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && alt_text = "0123456789") || (number > 14 && title = "0123456789"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@builtin.timer, (interval <= 30min || interval < 30min || interval > 2h)  => * => notify
;;
Main() {
    @builtin.timer(interval=30min), true  => @xkcd.get_comic(), ((title =~ "12345" && alt_text =~ "0123456789")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@instagram.new_picture, (caption = "12345" && filter = "0123456788") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => @parklon_heatpad.set_power, true 
;;
Main() {
    @instagram.new_picture(), ((caption = "23456789" && filter = "9876543210")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.search(), (message =~ "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_sound = true && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || start_time = makeDate(1462320000000) || (has_motion = false && has_person = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_sound = true && has_person = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @uber.time_estimate(start=$context.location.home), (((start = $context.location.home && estimate < 2h) || estimate > 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@jawbone_up.getheartrate, (place = $context.location.home || (time = makeDate(1487030400000) && place = $context.location.work && heartrate <= 11)) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @jawbone_up.getheartrate(), ((time = makeDate(1462320000000) && place = $context.location.home && heartrate >= 14)) , v_time := time, v_place := place, v_heartrate := heartrate => @ytranslate.detect_language(text="23456789"), ((text = "0123456789" && detected_language = "en"^^tt:iso_lang_code("English"))) , v_detected_language := detected_language => notify;
}
====
@security-camera.new_event, (has_sound = false && has_motion = true && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @lg_webos_tv.play_url, true 
;;
Main() {
    @security-camera.new_event(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
* => @omlet.send_picture, ((to = "alice"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || to = "bob"^^tt:username) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), ((company_name = "0123456789" && yield >= 11 && pay_date = makeDate(1487030400000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @omlet.send_picture(to="alice"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
* => @gmail.get_latest, label = "0123456789" , v_from := from, v_subject := subject, v_label := label => @omlet.send_to, message =~ v_subject 
;;
Main() {
    now => @gmail.get_latest(), ((subject =~ "0123456789" || label = "0123456789" || from = "bob@stanford.edu"^^tt:email_address || label = "23456789")) , v_from := from, v_subject := subject, v_label := label => @omlet.send_to(to=v_subject, message=v_subject) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_motion = false || (start_time = makeDate(1487030400000) && has_sound = false))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => notify;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="sf"^^sportradar:mlb_team("SF Giants")), (((watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && away_name = "23456789" && scheduled_time =~ "9876543210" && away_runs <= 55) || (away_name = "23456789" && game_status = enum(scheduled) && scheduled_time =~ "9876543210" && game_inning =~ "ABCDEFGHIJ" && home_runs = 42) || (watched_is_home = true && away_name = "0123456788" && home_name =~ "12345" && game_status = enum(inprogress) && scheduled_time = "23456789" && game_inning = "ABCDEFGHIJ" && away_runs < 14 && home_runs >= 11))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => @almond_bike_market.search(price=v_away_runs), (price >= 42)  => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @nasa.apod, description =~ "23456789" , v_title := title, v_description := description, v_picture_url := picture_url => notify
;;
Main() {
    now => @nasa.apod(), (description =~ "23456789") , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
@weatherapi.monitor, ((temperature >= 22C && wind_speed > 25mph && humidity >= 7 && cloudiness > 7 && fog > 55) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed <= 25mph && humidity = 14 && cloudiness > 7 && fog >= 14)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), ((temperature = 73F && wind_speed = 5kmph && cloudiness <= 11)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @dropbox.open(file_name=v_weather), ((file_name = "9876543210" || (file_name = "ABCDEFGHIJ" && url =~ "9876543210"))) , v_url := url => notify;
}
====
now => * => @omlet.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456788") 
;;
Main() {
    now => @dropbox.list_folder(folder_name="ABCDEFGHIJ"), ((folder_name = "0123456788" && file_name =~ "23456789")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @omlet.send_picture(to=v_file_name, picture_url=v_file_name, caption=v_full_path) ;
}
====
* => @fitbit.getbody, ((weight >= 155lb && fat > 11) || (weight <= 155lb && bmi < 55 && fat >= 55)) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify
;;
Main() {
    @tumblr.new_photo(blog_name="23456789"), ((blog_name = "0123456788" || (blog_name =~ "ABCDEFGHIJ" && date = makeDate(1487030400000) && link = "http://www.abc.def"^^tt:url))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @fitbit.getbody(), (((weight > 82kg && bmi <= 11 && fat <= 55) || (weight > 82kg && bmi >= 14))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
now => @slack.send, (message =~ "ABCDEFGHIJ" || channel = "funny"^^tt:hashtag || (channel = "funny"^^tt:hashtag && message = "23456789")) 
;;
Main() {
    now => @slack.send(channel="lol"^^tt:hashtag, message="23456789") ;
}
====
now => * => @fitbit.recordweight, (weight > 155lb || weight >= 155lb) 
;;
Main() {
    now => @instagram.get_pictures(count=55), (((count > 14 && caption =~ "0123456788" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (count >= 42 && caption = "12345" && filter =~ "9876543210"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @fitbit.recordweight(weight=155lb) ;
}
====
@reddit_front_page.newpost, category = "23456789" , v_title := title, v_link := link, v_user := user, v_category := category => notify
;;
Main() {
    @reddit_front_page.newpost(), (((title =~ "0123456788" && link = "http://www.google.com"^^tt:url && category =~ "0123456788") || (title =~ "ABCDEFGHIJ" && user =~ "23456789" && category =~ "9876543210"))) , v_title := title, v_link := link, v_user := user, v_category := category => notify;
}
====
* => * => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    now => @dropbox.open(file_name="0123456789"), (file_name = "9876543210") , v_url := url => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
* => @gmail.send_picture, (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789") 
;;
Main() {
    @github.new_milestone(repo_name="ABCDEFGHIJ"), ((user = "alice"^^tt:username && description =~ "0123456788")) , v_user := user, v_description := description, v_time := time => @gmail.send_picture(to=v_description, subject=v_description, picture_url=v_description, message="0123456789") ;
}
====
now => * => @thermostat.set_target_temperature, (value < 73F || value = 73F) 
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && summary = "ABCDEFGHIJ" && sequence <= 42 && organizer = "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @thermostat.set_target_temperature(value=22C) ;
}
====
now => @phone.set_ringer, mode = enum(silent) 
;;
Main() {
    now => @phone.set_ringer(mode=enum(silent)) ;
}
====
@bodytrace.source, weight >= 82kg , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight >= 155lb) , v_time := time, v_weight := weight => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => notify;
}
====
@phone.gps, ((altitude > 42cm && speed < 5kmph) || speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), (((location = $context.location.home && altitude >= 42cm && speed < 25mph) || (altitude <= 1000m && speed = 25mph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.call_emergency() ;
}
====
@almond_dates.monitor, ((interest =~ "0123456788" && phone = "+16501234567"^^tt:phone_number) || (interest = "12345" && poster = "9876543210")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), (message = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @twitter.send_direct_message, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate = 7 || place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.send_direct_message(to="alice"^^tt:username, message="9876543210") ;
}
====
* => * => @fitbit.recordweight, true 
;;
Main() {
    @jawbone_up.getbody(), ((weight <= 82kg && leanMass <= 11)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @nasa.rover(), (((count < 55 && camera_used =~ "0123456789") || (date_taken = makeDate(1462320000000) && count > 14 && camera_used =~ "9876543210") || (count = 42 && camera_used = "0123456789") || (date_taken = makeDate(1487030400000) && count > 7 && camera_used =~ "0123456788"))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @fitbit.recordweight(weight=82kg) ;
}
====
@reddit_front_page.newpost, title =~ "12345" , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), (((link = "http://www.abc.def"^^tt:url && user =~ "0123456789" && category =~ "0123456789") || category = "0123456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @dropbox.list_folder(folder_name=v_user), (((file_name =~ "0123456788" && file_size = 5KB) || (folder_name = "ABCDEFGHIJ" && file_name = "ABCDEFGHIJ" && is_folder = false && file_size <= 20MB) || (folder_name = "23456789" && last_modified = makeDate(1462320000000) && file_size <= 20MB) || (folder_name = "0123456788" && last_modified = makeDate(1487030400000) && file_size <= 5KB && full_path = "12345"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => @uber.time_estimate, (uber_type = "0123456788" && estimate > 2h) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.home), ((start = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && estimate > 30min)) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@builtin.timer, interval >= 2h  => * => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval >= 30min)  => @dropbox.open(file_name="9876543210"), (url =~ "23456789") , v_url := url => notify;
}
====
now => @weatherapi.current, ((temperature < 22C && wind_speed <= 25mph && cloudiness > 11) || (wind_speed <= 25mph && fog <= 55) || (temperature < 73F && humidity = 7 && fog >= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((location = $context.location.home && temperature < 73F && wind_speed <= 5kmph && humidity >= 7 && cloudiness <= 42 && fog >= 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
* => @icalendar.list_events, ((start_date = makeDate(1462320000000) && sequence > 14 && organizer =~ "0123456789" && location =~ "23456789") || (summary = "ABCDEFGHIJ" && sequence <= 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), (((description = "ABCDEFGHIJ" && sequence > 11 && location =~ "0123456788") || (description = "23456789" && sequence <= 14 && organizer = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @xkcd.get_comic(number=55), ((number <= 7 && link = "http://www.abc.def"^^tt:url && alt_text =~ "12345")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @security-camera.set_power(power=enum(off)) ;
}
====
* => @almond_dates.search, ((interest =~ "9876543210" && poster = "23456789" && phone = "+15551234567"^^tt:phone_number) || (message = "23456789" && poster =~ "12345")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "0123456789" && category =~ "9876543210")) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_dates.search(), (((interest =~ "0123456788" && message =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && message =~ "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), ((updated = makeDate(1462320000000) || (title = "0123456788" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link, v_updated := updated => @security-camera.set_power(power=enum(off)) ;
}
====
* => @linkedin.get_profile, ((formatted_name = "9876543210" && headline =~ "12345" && industry = "0123456788" && specialties =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name = "12345" && industry = "9876543210") || headline =~ "ABCDEFGHIJ" || (formatted_name = "9876543210" && industry =~ "0123456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), ((headline =~ "0123456788" && contains(positions, "ABCDEFGHIJ"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @thecatapi.get, (image_id = "23456789" && count > 14 && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (count > 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @almond_bike_market.detail, (id = "12345" || id =~ "12345" || id = "23456789" || id =~ "23456789" || id =~ "12345" || id = "23456789" || id =~ "0123456789" || id =~ "0123456788")  => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @almond_bike_market.detail(id="23456789"), true  => notify;
}
====
* => @gmail.send_email, ((to = "alice@gmail.com"^^tt:email_address && subject = "ABCDEFGHIJ") || (to = "bob@stanford.edu"^^tt:email_address && subject = "12345" && message =~ "ABCDEFGHIJ")) 
;;
Main() {
    @tumblr.new_text(blog_name="9876543210"), (((blog_name =~ "0123456788" && link = "http://www.abc.def"^^tt:url && contains(tags, "cat"^^tt:hashtag) && body = "12345") || (blog_name = "12345" && title = "23456789"))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="ABCDEFGHIJ") ;
}
====
now => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && subject = "0123456789") 
;;
Main() {
    now => @builtin.get_time(), true , v_time := time => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @almond_bike_market.post, (title = "0123456788" && price > 11) 
;;
Main() {
    now => @almond_bike_market.post(title="ABCDEFGHIJ", price=7, info="9876543210", poster="0123456789") ;
}
====
now => * => @builtin.notify, message = "23456789" 
;;
Main() {
    now => @builtin.get_random(), true , v_number := number => @builtin.notify(message="23456789") ;
}
====
* => @linkedin.get_profile, (headline =~ "0123456789" && specialties = "0123456789" && contains(positions, "23456789")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => *
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @linkedin.get_profile(), ((formatted_name =~ "0123456788" && headline = "9876543210" && contains(positions, "9876543210"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @uber.time_estimate, (start = $context.location.current_location && estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    @almond_bike_market.monitor(), ((price <= 42 || price >= 11 || price <= 11 || (info = "ABCDEFGHIJ" && price <= 42) || (info = "ABCDEFGHIJ" && price <= 42)))  => @uber.time_estimate(start=$context.location.current_location), ((start = $context.location.current_location && estimate >= 30min)) , v_uber_type := uber_type, v_estimate := estimate => @dropbox.move(old_name=v_uber_type, new_name=v_uber_type) ;
}
====
* => @almond_dates.post, ((message = "12345" && poster = "ABCDEFGHIJ") || (interest = "12345" && message = "23456789") || message = "0123456789") 
;;
Main() {
    @twitter.direct_message(), ((from = "bob"^^tt:username && message = "0123456788")) , v_from := from, v_message := message, v___reserved := __reserved => @almond_dates.post(interest="23456789", message="12345", poster="12345", phone="+15551234567"^^tt:phone_number) ;
}
====
@weatherapi.monitor, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && temperature < 22C && humidity > 55 && cloudiness < 11 && fog > 55 && weather = "23456789" && status = enum(sleety)) || (location = $context.location.work && humidity < 14 && cloudiness > 42 && fog < 14 && weather =~ "0123456789" && status = enum(drizzling)) || (cloudiness > 42 && fog < 7 && weather =~ "9876543210" && status = enum(sleety)) || (temperature <= 73F && wind_speed < 25mph && humidity < 14 && fog > 14 && weather =~ "9876543210") || (temperature >= 22C && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => *
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((temperature >= 22C && wind_speed <= 25mph && cloudiness >= 14 && fog > 7 && weather = "23456789" && status = enum(drizzling))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @bing.image_search(query="ABCDEFGHIJ"), ((query =~ "12345" && title = "ABCDEFGHIJ" && width <= 11 && height > 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @gmail.send_picture(picture_url=v_picture_url, to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="23456789") ;
}
====
* => * => @facebook.post, true 
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), ((title =~ "0123456789" && description =~ "0123456789")) , v_title := title, v_link := link, v_description := description => @thecatapi.get(), (count = 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @facebook.post(status=v_image_id) ;
}
====
@weatherapi.monitor, ((location = $context.location.current_location && wind_speed <= 5kmph && humidity >= 14 && cloudiness <= 7 && fog >= 7 && status = enum(cloudy)) || (wind_speed <= 25mph && humidity > 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature < 73F && wind_speed >= 25mph && humidity <= 11 && cloudiness <= 11 && fog < 55 && weather = "9876543210" && status = enum(snowy)) || (temperature >= 73F && wind_speed <= 25mph && cloudiness < 14 && fog > 42 && weather = "0123456789" && status = enum(sunny)))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @twitter.search_by_hashtag(query_hashtag="funny"^^tt:hashtag, count=7, in_reply_to="alice"^^tt:username), (((query_hashtag = "cat"^^tt:hashtag && count < 55 && contains(urls, "http://www.google.com"^^tt:url)) || (count <= 11 && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@slack.receive, (channel = "funny"^^tt:hashtag && timestamp = "ABCDEFGHIJ" && from = "alice"^^tt:username && message = "23456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), ((channel = "funny"^^tt:hashtag && from = "bob"^^tt:username)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @security-camera.get_snapshot(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => notify;
}
====
* => @gmail.send_email, message =~ "23456789" 
;;
Main() {
    @omlet.incomingmessage(), (((type = enum(picture) && message =~ "ABCDEFGHIJ") || message = "ABCDEFGHIJ" || message =~ "23456789")) , v_type := type, v_message := message => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="12345") ;
}
====
@onedrive.file_created, (file_name = "0123456789" || file_name =~ "ABCDEFGHIJ" || file_name = "0123456789") , v_file_name := file_name => * => notify
;;
Main() {
    @onedrive.file_created(), ((file_name = "23456789" || file_name =~ "ABCDEFGHIJ")) , v_file_name := file_name => @builtin.get_random(), (number <= 42) , v_number := number => notify;
}
====
now => @slack.send_picture, caption =~ "0123456788" 
;;
Main() {
    now => @slack.send_picture(channel="cat"^^tt:hashtag, caption="23456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@yahoofinance.stock_div, (company_name =~ "0123456789" && yield >= 55) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => *
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), (stock_id = "goog"^^tt:stock_id("Google")) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
@washington_post.new_blog_post, true , v_title := title, v_link := link => * => notify
;;
Main() {
    @washington_post.new_blog_post(section=enum(wonkblog)), ((title = "0123456788" || (title =~ "0123456788" && link = "http://www.google.com"^^tt:url))) , v_title := title, v_link := link => @almond_bike_market.ask_seller(id="ABCDEFGHIJ", property="ABCDEFGHIJ", value="0123456788", public=true), (((value = "0123456788" && public = true && answer = "23456789") || answer =~ "12345")) , v_answer := answer => notify;
}
====
now => @twitter.search_by_hashtag, (count > 42 && text = "9876543210" && contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=11, from="alice"^^tt:username), (((text = "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && from = "alice"^^tt:username) || (count >= 11 && text = "0123456789" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url) && from = "bob"^^tt:username) || contains(hashtags, "funny"^^tt:hashtag) || (text = "9876543210" && contains(urls, "http://www.google.com"^^tt:url) && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @instagram.get_pictures, ((count >= 55 && caption =~ "0123456788" && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag) && location = $context.location.home) || (count <= 14 && caption =~ "0123456788" && filter =~ "0123456788" && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (link = "http://www.google.com"^^tt:url && contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=11), ((caption = "9876543210" && link = "http://www.google.com"^^tt:url && filter =~ "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@rss.new_post, (title =~ "0123456789" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => notify
;;
Main() {
    @rss.new_post(), ((link = "http://www.google.com"^^tt:url || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify;
}
====
@activity-tracker.getmove, (distance = 42cm && steps >= 14 && calories < 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day = "ABCDEFGHIJ" && distance <= 1000m && inactiveTime < 30min)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @twitter.send_direct_message(to="bob"^^tt:username, message="0123456789") ;
}
====
@icalendar.event_begin, (summary =~ "0123456789" && description =~ "12345" && sequence <= 14) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((end_date = makeDate(1487030400000) && description =~ "0123456789" && sequence > 42 && location = "0123456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @bing.web_search(query=v_description), true , v_title := title, v_description := description, v_link := link => notify;
}
====
now => * => @slack.send, message = "12345" 
;;
Main() {
    now => @ytranslate.detect_language(text="0123456788"), (text =~ "0123456788") , v_detected_language := detected_language => @slack.send(channel="funny"^^tt:hashtag, message="12345") ;
}
====
now => @thecatapi.get, (count <= 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    now => @thecatapi.get(), (link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @almond_bike_market.post, ((title = "9876543210" && price < 55 && info = "12345" && poster =~ "0123456788") || (title = "0123456788" && price = 7 && info =~ "0123456789") || (price <= 14 && info =~ "12345" && poster = "0123456789")) 
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && day =~ "12345" && distance <= 1000m && activeTime < 30min && inactiveTime <= 2h && calories < 500kcal) || (day =~ "9876543210" && steps > 42 && activeTime <= 2h && inactiveTime >= 2h && calories >= 500kcal) || (updateTime = makeDate(1487030400000) && day = "ABCDEFGHIJ" && distance < 1000m && steps > 42 && activeTime > 30min && calories > 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_bike_market.post(price=v_steps, title="0123456788", info="9876543210", poster="0123456789") ;
}
====
@xkcd.new_comic, number <= 7 , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => * => notify
;;
Main() {
    @xkcd.new_comic(), ((number > 14 && title = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => notify;
}
====
@sleep-tracker.getsleep, ((rem >= 30min && light >= 2h) || (time = makeDate(1487030400000) && awakeTime = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && duration < 2h && rem > 30min && light <= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((place = $context.location.current_location && duration >= 30min && rem >= 30min && deep > 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => notify;
}
====
* => @almond_dates.search, (interest = "12345" || (interest =~ "0123456788" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), ((interest = "12345" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@github.new_issue, ((number > 14 && title = "0123456789" && body =~ "12345") || repo_name =~ "ABCDEFGHIJ" || (repo_name = "0123456788" && user = "alice"^^tt:username && number >= 14 && body = "23456789") || (repo_name = "9876543210" && number > 11 && title = "0123456789" && body = "0123456789") || (user = "alice"^^tt:username && number > 7 && title =~ "9876543210" && time = makeDate(1462320000000))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue(repo_name="23456789"), ((repo_name = "9876543210" && number > 42 && title = "12345")) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @light-bulb.set_power(power=enum(on)) ;
}
====
@almond_dates.monitor, (message =~ "12345" && poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((poster =~ "23456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@nasa.asteroid, ((name = "9876543210" && estimated_diameter_max <= 42cm && relative_velocity > 25mph && closest_distance_to_earth > 1000m) || (asteroid_id >= 14 && estimated_diameter_max >= 42cm && closest_distance_to_earth < 1000m) || (estimated_diameter_max <= 42cm && dangerous = true && relative_velocity < 5kmph && closest_distance_to_earth = 1000m && orbiting_body =~ "0123456788")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((name = "ABCDEFGHIJ" && estimated_diameter_min >= 1000m && relative_velocity < 5kmph && closest_distance_to_earth < 1000m && orbiting_body = "0123456789") || (asteroid_id <= 14 && estimated_diameter_max < 1000m && relative_velocity <= 25mph && closest_distance_to_earth = 42cm && orbiting_body =~ "ABCDEFGHIJ"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @reddit_front_page.newpost(), (((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url && user = "ABCDEFGHIJ") || (title = "9876543210" && category =~ "ABCDEFGHIJ") || (title =~ "0123456789" && category = "23456789") || category =~ "0123456789" || (title = "23456789" && link = "http://www.google.com"^^tt:url) || (title = "9876543210" && link = "http://www.google.com"^^tt:url && user = "23456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @heatpad.set_power(power=enum(on)) ;
}
====
now => * => @gmail.send_email, (message = "ABCDEFGHIJ" || (subject = "12345" && message = "0123456788")) 
;;
Main() {
    now => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="9876543210") ;
}
====
now => @omlet.send_to, (message =~ "ABCDEFGHIJ" || message =~ "0123456788") 
;;
Main() {
    now => @omlet.send_to(to="alice"^^tt:username, message="23456789") ;
}
====
* => @holidays.next_uk_holiday_event, (description =~ "9876543210" || description =~ "ABCDEFGHIJ" || (date = makeDate(1462320000000) && summary = "9876543210" && description = "23456789") || (summary = "ABCDEFGHIJ" && description =~ "0123456788") || (summary = "23456789" && description = "23456789") || description =~ "23456789" || description =~ "12345") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((date = makeDate(1487030400000) && description = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @onedrive.upload_picture, ((file_name = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || file_name = "12345" || (file_name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    now => @onedrive.upload_picture(file_name="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @instagram.get_pictures, (filter =~ "ABCDEFGHIJ" || count >= 42 || (count > 7 && caption = "23456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    now => @instagram.get_pictures(count=42), ((caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter =~ "0123456788")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
* => @phone.get_gps, (altitude > 1000m && bearing > 14 && speed >= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    @scale.source(), (value >= 82kg) , v_time := time, v_value := value => @phone.get_gps(), ((location = $context.location.home && altitude < 1000m && bearing >= 42 && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
* => @wall_street_journal.get_opinions, true , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (title =~ "9876543210") , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@rss.new_post, (link = "http://www.abc.def"^^tt:url || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => notify
;;
Main() {
    @rss.new_post(), (title =~ "23456789") , v_title := title, v_link := link => notify;
}
====
@twitter.direct_message, true , v_from := from, v_message := message, v___reserved := __reserved => * => @linkedin.share, (status =~ "ABCDEFGHIJ" || status = "12345") 
;;
Main() {
    @twitter.direct_message(), true , v_from := from, v_message := message, v___reserved := __reserved => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
@almond_dates.monitor, ((interest = "0123456789" && message = "ABCDEFGHIJ" && poster =~ "9876543210" && phone = "+15551234567"^^tt:phone_number) || (interest = "0123456789" && poster = "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest = "23456789" && message =~ "0123456789" && poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number) || poster = "0123456789" || message = "ABCDEFGHIJ" || (interest = "0123456789" && message = "0123456789" && poster = "9876543210") || message =~ "12345" || phone = "+15551234567"^^tt:phone_number || message =~ "9876543210" || (poster = "9876543210" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @thecatapi.get, (count >= 11 || count <= 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @almond_dates.monitor(), ((poster = "23456789" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @thermostat.get_hvac_state, true , v_mode := mode, v_state := state => *
;;
Main() {
    @youtube.source(), (((channel_id = "9876543210" && title =~ "12345" && video_id = "ABCDEFGHIJ") || (channel_id =~ "0123456789" && channel_title =~ "0123456789" && title =~ "23456789"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
@reddit_front_page.newpost, ((link = "http://www.google.com"^^tt:url && category = "12345") || (user = "0123456789" && category = "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @fitbit.recordweight, true 
;;
Main() {
    @reddit_front_page.newpost(), true , v_title := title, v_link := link, v_user := user, v_category := category => @fitbit.recordweight(weight=82kg) ;
}
====
* => * => @light-bulb.color_loop, true 
;;
Main() {
    @almond_couch_market.monitor(), ((price <= 14 || (title = "23456789" && price <= 11))) , v_title := title, v_price := price, v_contact := contact => @light-bulb.color_loop() ;
}
====
* => @light-bulb.alert_long, true 
;;
Main() {
    now => @light-bulb.alert_long() ;
}
====
now => @youtube.list_subscriptions, channel_id =~ "23456789" , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify
;;
Main() {
    now => @youtube.list_subscriptions(), (((channel_id =~ "23456789" && description = "0123456788") || (channel_id = "12345" && title =~ "0123456789" && description =~ "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
@washington_post.new_article, ((section = enum(lifestyle) && link = "http://www.abc.def"^^tt:url && description =~ "9876543210") || (section = enum(national) && title =~ "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || section = enum(business) || title =~ "ABCDEFGHIJ" || description =~ "12345") , v_title := title, v_link := link, v_description := description => * => notify
;;
Main() {
    @washington_post.new_article(section=enum(sports)), (((link = "http://www.google.com"^^tt:url && description = "23456789") || (title = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && description = "23456789") || (section = enum(local) && title = "0123456789" && description = "23456789"))) , v_title := title, v_link := link, v_description := description => @dropbox.open(file_name="9876543210"), (((file_name = "12345" && url = "9876543210") || url =~ "9876543210")) , v_url := url => notify;
}
====
@almond_bike_market.monitor, price < 42  => * => notify
;;
Main() {
    @almond_bike_market.monitor(price=55), ((info =~ "0123456788" && price <= 14))  => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((wind_speed <= 25mph && humidity <= 42 && cloudiness <= 14 && fog > 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
now => @fitbit.getbody, ((weight < 82kg && bmi < 11 && fat <= 7) || (bmi < 42 && fat > 14)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @bluetooth_speaker.play_music, true 
;;
Main() {
    now => @fitbit.getbody(), ((bmi <= 14 && fat < 42)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @bluetooth_speaker.play_music() ;
}
====
@almond_bike_market.monitor, ((info = "0123456788" && price <= 55) || (info =~ "9876543210" && price <= 55))  => *
;;
Main() {
    @almond_bike_market.monitor(price=42), (((info = "0123456789" && price > 11) || info =~ "12345" || price > 7))  => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
now => * => @gmail.send_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    now => @gmail.send_picture(to="bob@stanford.edu"^^tt:email_address, subject="9876543210", message="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @twitter.search, ((count >= 42 && text = "0123456789" && contains(hashtags, "funny"^^tt:hashtag) && from = "bob"^^tt:username) || (query = "9876543210" && count < 55 && text =~ "0123456789") || (query =~ "23456789" && count <= 14 && in_reply_to = "alice"^^tt:username) || (count < 55 && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @bodytrace.source(), (weight < 82kg) , v_time := time, v_weight := weight => @twitter.search(query="23456789", count=7), ((query =~ "0123456789" && count < 55 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "bob"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
now => @linkedin.get_profile, ((formatted_name =~ "23456789" && headline =~ "0123456788" && industry = "ABCDEFGHIJ" && specialties = "12345") || headline = "12345") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @builtin.notify, (message =~ "0123456789" || message = "23456789") 
;;
Main() {
    now => @linkedin.get_profile(), (((specialties =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "12345" && industry = "23456789" && contains(positions, "23456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @builtin.notify(message=v_specialties) ;
}
====
* => * => @twitter.post_picture, true 
;;
Main() {
    @washington_post.new_blog_post(section=enum(wonkblog)), ((section = enum(capital_weather_gang) && title =~ "ABCDEFGHIJ")) , v_title := title, v_link := link => @twitter.post_picture(caption=v_title, picture_url=v_title) ;
}
====
* => @onedrive.upload_picture, ((file_name =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || file_name = "ABCDEFGHIJ" || file_name = "ABCDEFGHIJ" || (file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @activity-tracker.getmove(), ((steps > 14 && activeTime < 30min && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @onedrive.upload_picture(picture_url=v_day, file_name="ABCDEFGHIJ") ;
}
====
* => @giphy.get_tag, true , v_picture_url := picture_url => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @giphy.get_tag(tag=v_message), ((tag = "lol"^^tt:hashtag || (tag = "cat"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_picture_url := picture_url => notify;
}
====
@google_drive.new_drive_file, (file_name = "23456789" || file_name = "9876543210" || file_name = "23456789") , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "23456789") , v_file_name := file_name => @youtube.search_channel(query="ABCDEFGHIJ"), (thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => notify;
}
====
* => @linkedin.share, status = "ABCDEFGHIJ" 
;;
Main() {
    @nasa.asteroid(), (((name =~ "0123456789" && relative_velocity >= 5kmph && closest_distance_to_earth <= 1000m && orbiting_body = "ABCDEFGHIJ") || (name = "9876543210" && estimated_diameter_min < 42cm && relative_velocity < 25mph && closest_distance_to_earth <= 42cm) || (asteroid_id < 55 && estimated_diameter_min < 42cm && estimated_diameter_max > 1000m && dangerous = false && closest_distance_to_earth = 42cm))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @linkedin.share(status="9876543210") ;
}
====
* => @almond_dates.post, ((interest = "0123456788" && poster = "9876543210") || phone = "+16501234567"^^tt:phone_number || poster = "23456789") 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_sound = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @almond_dates.post(interest="9876543210", message="9876543210", poster="0123456789", phone="+15551234567"^^tt:phone_number) ;
}
====
@bodytrace.source, (weight > 82kg || weight > 82kg) , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), ((weight < 82kg || weight >= 155lb)) , v_time := time, v_weight := weight => @nasa.apod(), true , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
now => * => @almond_couch_market.post, (title = "0123456788" && price > 14) 
;;
Main() {
    now => @nasa.apod(), ((title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_description := description, v_picture_url := picture_url => @almond_couch_market.post(title=v_description, contact=v_title, price=7) ;
}
====
* => @heatpad.set_power, (power = enum(on) || power = enum(off)) 
;;
Main() {
    @jawbone_up.getsleep(), ((time = makeDate(1462320000000) && place = $context.location.work && asleepTime = makeDate(1487030400000) && duration < 30min && rem <= 2h && deep >= 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @heatpad.set_power(power=enum(off)) ;
}
====
* => @google_drive.create_new_drive_file, (file_name = "0123456788" || file_name =~ "23456789" || file_name = "12345") 
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1462320000000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
* => * => @parklon_heatpad.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @gmail.receive_primary_email(), (((from_name = "ABCDEFGHIJ" && snippet = "12345") || snippet =~ "0123456789" || (subject =~ "12345" && date = makeDate(1487030400000) && contains(labels, "9876543210")))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @dropbox.open(file_name=v_subject), ((file_name = "ABCDEFGHIJ" && url =~ "23456789")) , v_url := url => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@rss.new_post, (title =~ "0123456788" && link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link => * => @heatpad.set_power, true 
;;
Main() {
    @rss.new_post(), ((link = "http://www.google.com"^^tt:url || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @heatpad.set_power(power=enum(off)) ;
}
====
@nasa.asteroid, (asteroid_id > 42 && estimated_diameter_min < 42cm && estimated_diameter_max = 42cm && closest_distance_to_earth <= 1000m && orbiting_body = "9876543210") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @tumblr-blog.post_picture, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 55 && name = "9876543210" && relative_velocity > 5kmph && closest_distance_to_earth <= 42cm && orbiting_body = "12345")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @tumblr-blog.post_picture(caption="12345", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @phone.call, true 
;;
Main() {
    @instagram.new_picture(), (caption = "23456789") , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @giphy.get_tag(tag=v_filter), true , v_picture_url := picture_url => @phone.call(number="+15551234567"^^tt:phone_number) ;
}
====
* => @almond_bike_market.ask_seller, (value = "12345" || (property =~ "12345" && value =~ "12345" && public = false) || id =~ "12345" || id =~ "0123456789" || id =~ "9876543210") , v_answer := answer => *
;;
Main() {
    now => @almond_bike_market.ask_seller(id="0123456789", property="0123456789", value="0123456789", public=true), ((id =~ "ABCDEFGHIJ" && answer = "0123456789")) , v_answer := answer => @onedrive.upload_picture(file_name="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @thermostat.humidity(), (value < 7) , v_time := time, v_value := value => @almond_bike_market.ask(id="0123456788", property="0123456789"), true  => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && deep < 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), ((light > 30min && deep > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @ytranslate.detect_language(text="ABCDEFGHIJ"), (text =~ "0123456788") , v_detected_language := detected_language => notify;
}
====
* => * => @almond_bike_market.post, (title =~ "23456789" && price >= 14 && info =~ "12345" && poster = "ABCDEFGHIJ") 
;;
Main() {
    now => @thecatapi.get(), (((image_id = "9876543210" && count > 14 && link = "http://www.abc.def"^^tt:url) || (image_id =~ "0123456789" && count >= 14 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || (image_id =~ "9876543210" && count = 14 && link = "http://www.google.com"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @almond_bike_market.post(title="12345", price=7, info="0123456788", poster="23456789") ;
}
====
now => @almond_dates.search, (poster = "12345" || (message = "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest =~ "0123456789" && message =~ "12345") || poster =~ "23456789" || (message =~ "12345" && poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (message =~ "12345" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || phone = "+15551234567"^^tt:phone_number || poster =~ "0123456789" || (message = "0123456789" && poster =~ "9876543210") || phone = "+16501234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power, (power = enum(on) || power = enum(on)) 
;;
Main() {
    now => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description = "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((description =~ "23456789" && sequence > 7)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@jawbone_up.getmove, (updateTime = makeDate(1487030400000) && steps < 7 && activeTime > 2h && inactiveTime >= 30min && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @jawbone_up.getmove(), (((updateTime = makeDate(1462320000000) && day = "12345" && distance = 1000m && steps = 55 && activeTime < 2h && calories > 500kcal) || (day =~ "0123456789" && distance >= 1000m && activeTime >= 2h && inactiveTime < 2h && calories = 500kcal) || (distance >= 42cm && steps >= 7) || (distance < 1000m && steps <= 11 && inactiveTime >= 30min && calories > 500kcal) || (updateTime = makeDate(1487030400000) && day =~ "ABCDEFGHIJ" && inactiveTime >= 2h && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @builtin.get_date(), (date = makeDate(1487030400000)) , v_date := date => notify;
}
====
* => @dropbox.move, ((old_name =~ "ABCDEFGHIJ" && new_name =~ "9876543210") || (old_name = "12345" && new_name = "12345") || old_name = "23456789") 
;;
Main() {
    @jawbone_up.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @dropbox.move(old_name="0123456788", new_name="0123456788") ;
}
====
* => @bing.image_search, (query = "0123456789" && title = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && width = 7 && height < 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    @xkcd.new_comic(), ((number > 42 || (number < 14 && title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (number <= 14 && link = "http://www.google.com"^^tt:url))) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @bing.image_search(query=v_title), ((query =~ "12345" && title = "0123456788" && link = "http://www.google.com"^^tt:url && width = 7)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@holidays.us_holiday_event, true , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@icalendar.event_begin, (start_date = makeDate(1462320000000) && summary = "9876543210") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "0123456789" && description =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @uber.price_estimate, (end = $context.location.home && uber_type =~ "0123456789" && low_estimate > 11 && high_estimate >= 42 && surge <= 7 && duration > 30min) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify
;;
Main() {
    @wall_street_journal.lifestyle(), (title =~ "12345") , v_title := title, v_link := link => @uber.price_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"), end=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((uber_type =~ "9876543210" && low_estimate > 11 && surge > 7 && duration <= 30min && distance <= 1000m)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => notify;
}
====
* => @thermostat.set_target_temperature, (value = 73F || value = 73F) 
;;
Main() {
    @almond_bike_market.monitor(), (((info =~ "23456789" && price > 55) || price > 14 || info = "9876543210"))  => @thermostat.set_target_temperature(value=22C) ;
}
====
now => * => @almond_dates.post, ((interest =~ "ABCDEFGHIJ" && message =~ "9876543210" && poster =~ "12345") || (poster = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || poster =~ "23456789") 
;;
Main() {
    now => @twitter.search(query="23456789", in_reply_to="bob"^^tt:username), ((text =~ "9876543210" && contains(hashtags, "cat"^^tt:hashtag) && from = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @almond_dates.post(interest=v_text, phone=v_text, message="9876543210", poster="0123456789") ;
}
====
* => @giphy.get_tag, true , v_picture_url := picture_url => *
;;
Main() {
    @youtube.source(), ((channel_title = "0123456788" && title = "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @giphy.get_tag(tag=v_video_id), true , v_picture_url := picture_url => notify;
}
====
now => @light-bulb.color_loop, true 
;;
Main() {
    now => @light-bulb.color_loop() ;
}
====
now => * => @omlet.send_to, message =~ "ABCDEFGHIJ" 
;;
Main() {
    now => @gmail.get_latest(), (from = "alice@gmail.com"^^tt:email_address) , v_from := from, v_subject := subject, v_label := label => @omlet.send_to(to=v_label, message=v_subject) ;
}
====
now => @holidays.next_us_holiday_event, (summary = "9876543210" && description = "23456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_us_holiday_event(), (((date = makeDate(1462320000000) && summary =~ "0123456788" && description = "9876543210") || (date = makeDate(1462320000000) && summary = "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@builtin.timer, (interval >= 30min || interval < 30min)  => * => notify
;;
Main() {
    @builtin.timer(interval=2h), ((interval <= 30min || interval <= 30min))  => @weatherapi.sunrise(location=$context.location.current_location), ((sunset = makeDate(1462320000000) || sunset = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @bing.image_search, (link = "http://www.google.com"^^tt:url && width >= 7 && height <= 14) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify
;;
Main() {
    @holidays.uk_holiday_event(), (summary = "9876543210") , v_date := date, v_summary := summary, v_description := description => @bing.image_search(query=v_description), ((query = "0123456788" && width <= 11 && height <= 14)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
* => @twitter.search_by_hashtag, (text =~ "ABCDEFGHIJ" && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    @jawbone_up.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.search_by_hashtag(count=v_heartrate, query_hashtag="cat"^^tt:hashtag, from="alice"^^tt:username), (((text =~ "12345" && contains(hashtags, "funny"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url)) || (count > 7 && text = "12345"))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@holidays.uk_holiday_event, (description = "0123456789" || summary =~ "0123456788" || summary = "23456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @holidays.uk_holiday_event(), (((date = makeDate(1462320000000) && description = "ABCDEFGHIJ") || description =~ "23456789" || summary =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @almond_bike_market.post, ((title =~ "0123456789" && price > 42) || (title = "ABCDEFGHIJ" && info = "12345") || price < 55 || price < 11 || poster = "12345") 
;;
Main() {
    now => @almond_bike_market.post(title="0123456788", price=14, info="0123456788", poster="12345") ;
}
====
@builtin.at, time = makeTime(7,30)  => *
;;
Main() {
    @builtin.at(time=makeTime(20,30)), (time = makeTime(15,0))  => @builtin.thank_you() ;
}
====
@holidays.us_holiday_event, true , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), (description =~ "0123456789") , v_date := date, v_summary := summary, v_description := description => @imgflip.generate(template=v_summary, text_top=v_description, text_bottom=v_summary), (((text_top =~ "23456789" && name =~ "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (text_top =~ "0123456789" && text_bottom = "23456789" && name = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_name := name, v_picture_url := picture_url => notify;
}
====
@security-camera.new_event, (has_sound = true && has_motion = false) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_sound = false && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @fitbit.getbody(), ((weight <= 82kg || fat >= 14 || (weight >= 82kg && bmi <= 14 && fat <= 42))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @gmail.get_latest, (subject = "0123456789" && label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    now => @gmail.get_latest(), ((subject = "23456789" && label = "0123456788")) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @fitbit.getbody, ((weight >= 155lb && bmi >= 55 && fat >= 55) || (bmi <= 42 && fat < 11) || fat > 14 || (weight >= 155lb && fat >= 7) || (bmi <= 11 && fat > 11) || (weight < 155lb && bmi >= 7 && fat = 11) || fat = 11 || bmi >= 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => *
;;
Main() {
    now => @fitbit.getbody(), (weight > 82kg) , v_weight := weight, v_bmi := bmi, v_fat := fat => @lg_webos_tv.play_url(url="http://www.abc.def"^^tt:url) ;
}
====
* => @xkcd.random_comic, ((title = "9876543210" && alt_text =~ "ABCDEFGHIJ") || (number > 11 && link = "http://www.abc.def"^^tt:url) || (title = "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => *
;;
Main() {
    @github.new_commit(repo_name="12345"), (user = "bob"^^tt:username) , v_user := user, v_message := message, v_time := time => @xkcd.random_comic(), ((number >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@almond_dates.monitor, (message = "12345" || (poster =~ "0123456788" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "0123456789" && message = "9876543210" && poster =~ "9876543210") || (interest =~ "0123456788" && poster = "9876543210" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @slack.updateChannelPurpose, channel = "funny"^^tt:hashtag 
;;
Main() {
    now => @slack.updateChannelPurpose(channel="lol"^^tt:hashtag, purpose="9876543210") ;
}
====
@heartrate-monitor.getheartrate, ((place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && heartrate > 14) || heartrate <= 11 || (time = makeDate(1462320000000) && heartrate > 11)) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && place = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
now => * => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=$context.location.home), ((end = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && low_estimate < 11 && currency_code =~ "0123456789" && surge >= 11 && distance < 42cm)) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => @onedrive.rename_file, ((old_name =~ "23456789" && new_name =~ "0123456788") || new_name =~ "12345" || (old_name = "23456789" && new_name =~ "ABCDEFGHIJ") || old_name = "0123456789") 
;;
Main() {
    now => @onedrive.rename_file(old_name="12345", new_name="23456789") ;
}
====
* => * => @omlet.send_picture, (to = "bob"^^tt:username && caption =~ "ABCDEFGHIJ") 
;;
Main() {
    @youtube.source(), ((video_id = "0123456789" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @omlet.send_picture(to=v_channel_id, picture_url=v_channel_title, caption=v_title) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name =~ "12345" || file_name =~ "9876543210") 
;;
Main() {
    now => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
@github.new_issue, ((repo_name = "ABCDEFGHIJ" && number < 7 && body =~ "12345" && time = makeDate(1487030400000)) || (repo_name =~ "9876543210" && user = "alice"^^tt:username && number = 7) || (repo_name = "12345" && number >= 42 && body =~ "9876543210" && time = makeDate(1487030400000))) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @almond_couch_market.post, ((price >= 7 && contact = "+16501234567"^^tt:phone_number) || price > 14 || (title =~ "ABCDEFGHIJ" && price > 55)) 
;;
Main() {
    @github.new_issue(repo_name="0123456789"), ((repo_name = "ABCDEFGHIJ" && user = "alice"^^tt:username && number > 11)) , v_user := user, v_number := number, v_title := title, v_body := body, v_time := time => @almond_couch_market.post(title=v_title, price=v_number, contact=v_body) ;
}
====
@thermostat.temperature, time = makeDate(1462320000000) , v_time := time, v_value := value => * => *
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1462320000000) || value > 73F)) , v_time := time, v_value := value => @car.honk_horn() ;
}
====
now => * => @almond_bike_market.post, ((info = "0123456789" && poster = "0123456788") || (title =~ "9876543210" && info =~ "0123456789")) 
;;
Main() {
    now => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @almond_bike_market.post(title=v_description, info=v_summary, poster=v_summary, price=55) ;
}
====
@gmail.receive_important_email, (from_address = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && contains(labels, "23456789") && snippet =~ "23456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => *
;;
Main() {
    @gmail.receive_important_email(), (((from_name =~ "0123456789" && subject = "23456789") || (from_name =~ "23456789" && from_address = "bob@stanford.edu"^^tt:email_address && subject =~ "0123456788" && date = makeDate(1462320000000) && contains(labels, "23456789")) || (from_name = "0123456789" && subject = "12345" && date = makeDate(1462320000000) && contains(labels, "0123456789") && snippet = "23456789") || (from_name = "0123456788" && subject =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && contains(labels, "12345") && snippet =~ "0123456788"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @thermostat.set_target_temperature(value=22C) ;
}
====
@google_drive.new_drive_file, (file_name =~ "ABCDEFGHIJ" || file_name =~ "9876543210") , v_file_name := file_name => @linkedin.share, true 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @linkedin.share(status=v_file_name) ;
}
====
@heartrate-monitor.getheartrate, time = makeDate(1487030400000) , v_time := time, v_place := place, v_heartrate := heartrate => * => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @holidays.next_us_holiday_event(), ((description =~ "0123456789" || (date = makeDate(1462320000000) && description =~ "0123456788"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@phone.gps, ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude > 1000m && bearing > 14 && speed = 25mph) || (bearing <= 14 && speed >= 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 42cm && bearing <= 7 && speed < 5kmph) || bearing < 55)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @fitbit.getbody(), (((weight < 82kg && bmi > 7 && fat < 42) || (weight = 155lb && bmi >= 42 && fat > 42) || (weight <= 82kg && bmi <= 11 && fat < 55))) , v_weight := weight, v_bmi := bmi, v_fat := fat => notify;
}
====
* => @linkedin.get_profile, (headline =~ "0123456788" && industry = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @linkedin.get_profile(), (((formatted_name =~ "0123456788" && specialties = "23456789") || specialties =~ "23456789" || (formatted_name =~ "ABCDEFGHIJ" && specialties =~ "0123456789" && contains(positions, "9876543210") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => * => @heatpad.set_power, true 
;;
Main() {
    now => @phone.get_gps(), ((altitude = 42cm && bearing <= 11 && speed < 5kmph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @heatpad.set_power(power=enum(off)) ;
}
====
* => @ytranslate.translate, (text =~ "9876543210" || (source_language = "it"^^tt:iso_lang_code("Italian") && target_language = "zh"^^tt:iso_lang_code("Chinese"))) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1487030400000) || description =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => @ytranslate.translate(target_language=v_summary, text=v_description), ((text =~ "9876543210" && translated_text = "9876543210")) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @almond_couch_market.post, (title = "9876543210" && price < 14 && contact = "+16501234567"^^tt:phone_number) 
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @almond_couch_market.post(title=v_message, contact=v_message, price=14) ;
}
====
now => * => @github.add_email, (email = "alice@gmail.com"^^tt:email_address || email = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    now => @builtin.get_time(), (time = makeDate(1462320000000)) , v_time := time => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
* => @dropbox.list_folder, (folder_name = "ABCDEFGHIJ" || (file_name =~ "0123456788" && is_folder = false) || (folder_name =~ "ABCDEFGHIJ" && file_name =~ "0123456789" && is_folder = true && file_size > 5KB)) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => *
;;
Main() {
    now => @dropbox.list_folder(folder_name="0123456788"), ((folder_name = "23456789" && file_name = "9876543210" && full_path = "0123456788")) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @lg_webos_tv.unmute() ;
}
====
now => * => @speaker.set_volume, percent > 55 
;;
Main() {
    now => @dropbox.get_space_usage(), (((used >= 20MB && total >= 20MB) || total <= 5KB)) , v_used := used, v_total := total => @speaker.set_volume(percent=42) ;
}
====
@youtube.source, (channel_title =~ "23456789" && title =~ "9876543210" && video_id = "9876543210" && video_url = "http://www.google.com"^^tt:url) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => *
;;
Main() {
    @youtube.source(), (channel_title =~ "ABCDEFGHIJ") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
* => @ytranslate.detect_language, (text =~ "0123456789" || detected_language = "it"^^tt:iso_lang_code("Italian")) , v_detected_language := detected_language => notify
;;
Main() {
    now => @ytranslate.detect_language(text="23456789"), (text =~ "0123456788") , v_detected_language := detected_language => notify;
}
====
now => @wall_street_journal.get_opinions, title =~ "12345" , v_title := title, v_link := link, v_updated := updated => *
;;
Main() {
    now => @wall_street_journal.get_opinions(), (((title =~ "23456789" && updated = makeDate(1462320000000)) || (link = "http://www.google.com"^^tt:url && updated = makeDate(1487030400000)))) , v_title := title, v_link := link, v_updated := updated => @facebook.post(status=v_title) ;
}
====
* => * => @phone.send_sms, body = "ABCDEFGHIJ" 
;;
Main() {
    now => @linkedin.get_profile(), (((industry =~ "12345" && specialties = "ABCDEFGHIJ") || (formatted_name =~ "0123456788" && headline = "9876543210" && industry = "9876543210" && specialties = "23456789" && contains(positions, "0123456788") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (formatted_name =~ "12345" && specialties = "9876543210" && contains(positions, "9876543210")))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => @phone.send_sms(to=v_formatted_name, body=v_formatted_name) ;
}
====
now => @thermostat.get_hvac_state, state = enum(cooling) , v_mode := mode, v_state := state => *
;;
Main() {
    now => @thermostat.get_hvac_state(), true , v_mode := mode, v_state := state => notify;
}
====
* => @bing.web_search, (query =~ "0123456789" && description =~ "0123456788") , v_title := title, v_description := description, v_link := link => notify
;;
Main() {
    @holidays.us_holiday_event(), ((date = makeDate(1487030400000) && description =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => @bing.web_search(query="0123456788"), (query =~ "0123456788") , v_title := title, v_description := description, v_link := link => notify;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1487030400000) && summary =~ "9876543210" && description =~ "ABCDEFGHIJ") || date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.uk_holiday_event(), (description =~ "23456789") , v_date := date, v_summary := summary, v_description := description => @bing.image_search(query=v_description), ((title =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url && height = 42)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => notify;
}
====
@youtube.source, ((channel_title = "9876543210" && title = "9876543210") || (channel_id =~ "0123456789" && channel_title =~ "0123456789" && title = "0123456789" && video_id = "0123456788" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify
;;
Main() {
    @youtube.source(), (channel_title =~ "9876543210") , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => notify;
}
====
@xkcd.new_whatif, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), ((title =~ "0123456789" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => @youtube.search_videos, ((title =~ "12345" && description =~ "12345") || (query =~ "12345" && video_id = "9876543210" && channel_id = "0123456788" && channel_title = "12345" && title =~ "ABCDEFGHIJ" && description =~ "9876543210") || (query =~ "0123456789" && channel_id =~ "0123456789" && title =~ "0123456789" && count < 7 && video_url = "http://www.abc.def"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @car.honk_horn, true 
;;
Main() {
    now => @youtube.search_videos(query="23456789"), (((channel_title =~ "23456789" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count >= 7) || (channel_id = "23456789" && channel_title = "0123456789" && title = "23456789" && count >= 55) || (query = "9876543210" && title =~ "0123456788" && description = "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count >= 42) || (query = "9876543210" && description = "ABCDEFGHIJ" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture && count > 11))) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => @car.honk_horn() ;
}
====
@phdcomics.new_post, title =~ "0123456788" , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (title = "ABCDEFGHIJ") , v_title := title, v_link := link, v_picture_url := picture_url => @thecatapi.get(), (count >= 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => * => @almond_bike_market.post, title = "12345" 
;;
Main() {
    now => @phone.get_gps(), (((location = $context.location.current_location && speed >= 5kmph) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && bearing < 42 && speed <= 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_bike_market.post(title="0123456788", price=7, info="ABCDEFGHIJ", poster="ABCDEFGHIJ") ;
}
====
@washington_post.new_article, (title =~ "0123456789" && description =~ "0123456788") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (((link = "http://www.abc.def"^^tt:url && description =~ "9876543210") || (link = "http://www.abc.def"^^tt:url && description =~ "0123456789") || (section = enum(politics) && title = "0123456788"))) , v_title := title, v_link := link, v_description := description => @speaker.set_volume(percent=14) ;
}
====
now => * => @almond_bike_market.post, ((title =~ "0123456789" && price >= 11 && info =~ "0123456789" && poster = "0123456789") || (title =~ "23456789" && price > 55 && info = "0123456788")) 
;;
Main() {
    now => @ytranslate.detect_language(text="12345"), ((text = "9876543210" || (text = "12345" && detected_language = "zh"^^tt:iso_lang_code("Chinese")))) , v_detected_language := detected_language => @almond_bike_market.post(title="9876543210", price=42, info="23456789", poster="12345") ;
}
====
* => @xkcd.get_comic, (title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && alt_text =~ "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @gmail.receive_important_email(), (subject = "0123456789") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @xkcd.get_comic(), (((number < 42 && alt_text = "ABCDEFGHIJ") || link = "http://www.abc.def"^^tt:url || (title =~ "ABCDEFGHIJ" && alt_text =~ "ABCDEFGHIJ") || (number >= 14 && title =~ "0123456789" && link = "http://www.abc.def"^^tt:url && alt_text = "ABCDEFGHIJ"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@almond_bike_market.monitor, ((info = "0123456788" && price <= 55) || (info =~ "9876543210" && price <= 55))  => *
;;
Main() {
    @almond_bike_market.monitor(info="ABCDEFGHIJ", price=7), ((info =~ "9876543210" && price >= 11))  => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
@almond_dates.monitor, (poster = "23456789" || message = "0123456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    @almond_dates.monitor(), ((poster =~ "23456789" || (message =~ "0123456789" && poster = "0123456788"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @security-camera.set_power(power=enum(off)) ;
}
====
* => @icalendar.list_events, ((summary = "0123456788" && description = "23456789" && sequence < 11 && location =~ "23456789") || (end_date = makeDate(1462320000000) && description =~ "23456789" && sequence >= 11)) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @washington_post.new_article(section=enum(opinions)), (((title =~ "9876543210" && description = "0123456789") || title =~ "9876543210" || title =~ "9876543210" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_description := description => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && summary =~ "0123456789" && sequence < 14 && location =~ "23456789") || (summary = "ABCDEFGHIJ" && description =~ "ABCDEFGHIJ" && organizer =~ "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
* => @icalendar.list_events, (start_date = makeDate(1487030400000) && description =~ "0123456789" && location = "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ") || (summary =~ "12345" && sequence >= 11 && organizer = "9876543210" && location = "12345"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => @phone.get_gps, (bearing <= 11 && speed <= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => *
;;
Main() {
    now => @phone.get_gps(), ((altitude >= 1000m && bearing <= 11 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @onedrive.rename_file(old_name="0123456789", new_name="9876543210") ;
}
====
* => @wall_street_journal.get_opinions, ((title =~ "9876543210" && updated = makeDate(1487030400000)) || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @onedrive.file_created(), ((file_name =~ "23456789" || file_name =~ "0123456788")) , v_file_name := file_name => @wall_street_journal.get_opinions(), (((title =~ "9876543210" && link = "http://www.abc.def"^^tt:url) || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url && updated = makeDate(1462320000000)))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => @twitter.post_picture, ((caption =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || caption = "ABCDEFGHIJ" || caption = "0123456788") 
;;
Main() {
    now => @twitter.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @almond_couch_market.post, (title =~ "0123456789" && price < 42) 
;;
Main() {
    now => @almond_couch_market.post(title="ABCDEFGHIJ", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@google_drive.new_drive_file, true , v_file_name := file_name => * => notify
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @nasa.rover(), true , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => notify;
}
====
now => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((title =~ "0123456788" || (title =~ "23456789" && price = 11) || (title =~ "0123456789" && price = 55))) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
@builtin.at, (time = makeTime(7,30) || time = makeTime(20,30))  => * => notify
;;
Main() {
    @builtin.at(time=makeTime(20,30)), true  => @wall_street_journal.get_opinions(), (((title = "9876543210" && link = "http://www.abc.def"^^tt:url) || updated = makeDate(1462320000000) || (title = "12345" && updated = makeDate(1462320000000)))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
now => * => @tumblr-blog.post_picture, caption =~ "0123456788" 
;;
Main() {
    now => @thermostat.get_temperature(), (value < 22C) , v_value := value => @tumblr-blog.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => * => @linkedin.share, status =~ "12345" 
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => @linkedin.share(status="9876543210") ;
}
====
* => @slack.updateChannelTopic, (topic = "23456789" || topic =~ "9876543210") 
;;
Main() {
    now => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="23456789") ;
}
====
* => @wall_street_journal.get_opinions, ((title =~ "9876543210" && updated = makeDate(1462320000000)) || updated = makeDate(1462320000000) || title =~ "0123456789") , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @github.new_milestone(repo_name="23456789"), ((user = "bob"^^tt:username || (repo_name =~ "23456789" && user = "alice"^^tt:username && description =~ "23456789") || user = "alice"^^tt:username || (repo_name =~ "9876543210" && time = makeDate(1487030400000)))) , v_user := user, v_description := description, v_time := time => @wall_street_journal.get_opinions(), ((title = "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
@activity-tracker.getmove, (day = "23456789" && steps < 55 && inactiveTime = 2h && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && day = "12345" && distance < 1000m && steps < 42 && activeTime > 2h && inactiveTime > 2h && calories <= 500kcal) || (updateTime = makeDate(1487030400000) && day =~ "ABCDEFGHIJ" && steps <= 7 && inactiveTime < 30min) || (steps < 42 && activeTime > 2h && inactiveTime < 30min && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @gmail.get_latest(), (((subject =~ "0123456789" && label = "9876543210") || (from = "bob@stanford.edu"^^tt:email_address && label =~ "ABCDEFGHIJ"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@nasa.asteroid, (name =~ "23456789" && estimated_diameter_min > 42cm && estimated_diameter_max > 1000m && relative_velocity = 25mph && closest_distance_to_earth >= 42cm) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => *
;;
Main() {
    @nasa.asteroid(), (((asteroid_id >= 14 && name =~ "12345" && relative_velocity = 5kmph && closest_distance_to_earth > 42cm && orbiting_body =~ "9876543210") || (name = "9876543210" && relative_velocity <= 25mph && closest_distance_to_earth < 42cm && orbiting_body = "23456789"))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @dropbox.create_new_folder(folder_name="12345") ;
}
====
now => * => @dropbox.move, ((old_name = "9876543210" && new_name = "0123456788") || old_name = "0123456788") 
;;
Main() {
    now => @dropbox.list_folder(folder_name="23456789"), (((file_name =~ "9876543210" && file_size >= 5KB && full_path =~ "0123456788") || (file_name = "9876543210" && is_folder = true) || (folder_name =~ "ABCDEFGHIJ" && file_name = "9876543210" && last_modified = makeDate(1462320000000) && file_size < 20MB && full_path = "ABCDEFGHIJ"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @dropbox.move(old_name="12345", new_name="0123456788") ;
}
====
* => @gmail.send_email, (to = "bob@stanford.edu"^^tt:email_address && subject = "12345") 
;;
Main() {
    @wall_street_journal.us_business(), true , v_title := title, v_link := link => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="0123456788", message="23456789") ;
}
====
now => * => @bluetooth_speaker.set_sink, true 
;;
Main() {
    now => @bluetooth_speaker.set_sink() ;
}
====
now => @almond_bike_market.search, price = 14  => notify
;;
Main() {
    now => @almond_bike_market.search(), ((info =~ "0123456788" && price >= 7))  => notify;
}
====
now => * => @thermostat.set_minmax_temperature, low <= 73F 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.current_location), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @thermostat.set_minmax_temperature(low=73F, high=22C) ;
}
====
@google_drive.new_drive_file, file_name = "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "23456789") , v_file_name := file_name => @holidays.next_us_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @giphy.get_tag, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (tag = "funny"^^tt:hashtag && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify
;;
Main() {
    @yahoofinance.stock_div(stock_id="aapl"^^tt:stock_id("Apple")), true , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @giphy.get_tag(tag=v_company_name), true , v_picture_url := picture_url => notify;
}
====
@xkcd.new_whatif, (title = "ABCDEFGHIJ" || (title =~ "0123456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => * => notify
;;
Main() {
    @xkcd.new_whatif(), true , v_title := title, v_link := link => @gmail.get_latest(), (label = "0123456788") , v_from := from, v_subject := subject, v_label := label => notify;
}
====
@tumblr.new_photo, ((blog_name =~ "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (date = makeDate(1462320000000) && caption = "0123456788" && link = "http://www.google.com"^^tt:url)) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @car.honk_horn, true 
;;
Main() {
    @tumblr.new_photo(blog_name="ABCDEFGHIJ"), (((blog_name = "12345" && link = "http://www.google.com"^^tt:url) || caption =~ "ABCDEFGHIJ" || (blog_name =~ "9876543210" && caption = "9876543210" && contains(tags, "lol"^^tt:hashtag) && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)) || (date = makeDate(1487030400000) && caption = "9876543210" && contains(picture_urls, "http://www.abc.def/foo.jpeg"^^tt:picture)))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @car.honk_horn() ;
}
====
* => @phone.get_gps, (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 42cm && bearing = 14) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.notify, ((title =~ "0123456788" && message =~ "12345") || message =~ "23456789") 
;;
Main() {
    now => @phone.get_gps(), ((location = $context.location.home && speed <= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.notify(title="0123456789", message="ABCDEFGHIJ") ;
}
====
@tumblr.new_text, ((blog_name = "ABCDEFGHIJ" && title = "0123456789" && body = "0123456788") || (blog_name = "9876543210" && title =~ "ABCDEFGHIJ" && contains(tags, "cat"^^tt:hashtag)) || title = "0123456789") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="12345"), (link = "http://www.abc.def"^^tt:url) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => notify;
}
====
* => * => @github.comment_issue, (issue_number = 14 && body = "12345") 
;;
Main() {
    now => @weatherapi.current(location=$context.location.work), ((location = $context.location.work && temperature < 73F && wind_speed >= 5kmph && humidity >= 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @github.comment_issue(repo_name=v_weather, issue_number=v_fog, body=v_weather) ;
}
====
@phdcomics.new_post, (title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), ((title = "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @speaker.set_volume(percent=14) ;
}
====
* => @almond_bike_market.post, (price = 7 && info = "9876543210" && poster =~ "ABCDEFGHIJ") 
;;
Main() {
    @thermostat.humidity(), ((value >= 11 || value <= 7)) , v_time := time, v_value := value => @almond_bike_market.post(title="23456789", price=7, info="ABCDEFGHIJ", poster="23456789") ;
}
====
@activity-tracker.getmove, (updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && steps < 55 && activeTime > 30min && inactiveTime >= 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((day =~ "12345" && distance <= 1000m && activeTime > 2h)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @phone.call_emergency() ;
}
====
* => @almond_dates.post, ((interest =~ "9876543210" && poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number) || (interest = "ABCDEFGHIJ" && poster = "0123456789" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    now => @almond_dates.post(interest="9876543210", message="12345", poster="9876543210", phone="+16501234567"^^tt:phone_number) ;
}
====
@nasa.asteroid, (asteroid_id = 7 && name = "ABCDEFGHIJ" && estimated_diameter_min > 1000m && dangerous = true && closest_distance_to_earth > 42cm && orbiting_body = "23456789") , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), (((asteroid_id > 14 && name = "23456789" && estimated_diameter_max >= 1000m && relative_velocity <= 25mph && closest_distance_to_earth > 42cm && orbiting_body = "23456789") || (asteroid_id <= 7 && estimated_diameter_min <= 1000m && estimated_diameter_max >= 42cm && dangerous = false))) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
* => @weatherapi.moon, (phase = "23456789" || phase = "0123456789") , v_date := date, v_phase := phase => *
;;
Main() {
    @onedrive.file_created(), (file_name = "23456789") , v_file_name := file_name => @weatherapi.moon(location=$context.location.home), (date = makeDate(1487030400000)) , v_date := date, v_phase := phase => @phone.call(number=v_phase) ;
}
====
* => * => @almond_dates.post, true 
;;
Main() {
    @instagram.new_picture(), ((caption = "12345" && filter = "ABCDEFGHIJ" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(interest="0123456788", message="0123456788", poster="0123456788", phone="+16501234567"^^tt:phone_number) ;
}
====
* => @almond_dates.post, (interest =~ "23456789" && poster = "23456789") 
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "23456789") , v_file_name := file_name => @almond_dates.post(interest="23456789", message="0123456789", poster="0123456789", phone="+15551234567"^^tt:phone_number) ;
}
====
now => @google_drive.create_new_drive_file, file_name =~ "9876543210" 
;;
Main() {
    now => @google_drive.create_new_drive_file(file_name="12345") ;
}
====
* => @giphy.get, true , v_picture_url := picture_url => @parklon_heatpad.set_power, true 
;;
Main() {
    now => @giphy.get(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_picture_url := picture_url => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
* => * => @gmail.send_email, true 
;;
Main() {
    @sportradar.soccer_us_tourney(tournament_search_term="12345"), (((tournament_full_name =~ "9876543210" && home_alias =~ "0123456789" && scheduled_time = "0123456788" && away_points <= 42) || (tournament_full_name =~ "23456789" && tournament_league_name = "0123456788" && home_alias =~ "0123456789" && away_name =~ "23456789" && away_points > 11))) , v_tournament_full_name := tournament_full_name, v_tournament_league_name := tournament_league_name, v_away_alias := away_alias, v_home_alias := home_alias, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points => @instagram.get_pictures(count=v_home_points), ((contains(hashtags, "funny"^^tt:hashtag) && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @gmail.send_email(to=v_filter, message=v_caption, subject="12345") ;
}
====
* => @almond_dates.search, (interest = "0123456789" || (message =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "ABCDEFGHIJ" && message =~ "0123456788" && poster = "0123456788") || (message =~ "0123456789" && poster =~ "12345" && phone = "+16501234567"^^tt:phone_number) || (message = "0123456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "12345" && message = "9876543210" && poster =~ "9876543210") || message = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), ((phone = "+16501234567"^^tt:phone_number || (interest = "23456789" && poster = "9876543210" && phone = "+16501234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @speaker.set_sink() ;
}
====
* => @holidays.next_us_holiday_event, (date = makeDate(1487030400000) && summary =~ "0123456789") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_us_holiday_event(), ((summary =~ "12345" && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @slack.setPresence(presence=enum(away)) ;
}
====
now => * => @google_drive.create_new_drive_file, (file_name = "9876543210" || file_name = "9876543210") 
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), (((start = $context.location.work && estimate < 30min) || estimate >= 2h)) , v_uber_type := uber_type, v_estimate := estimate => @google_drive.create_new_drive_file(file_name=v_uber_type) ;
}
====
now => * => @dropbox.move, new_name = "0123456789" 
;;
Main() {
    now => @fitbit.getsteps(), true , v_date := date, v_steps := steps => @dropbox.move(old_name="23456789", new_name="23456789") ;
}
====
@instagram.new_picture, link = "http://www.abc.def"^^tt:url , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => notify
;;
Main() {
    @instagram.new_picture(), (((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && contains(hashtags, "funny"^^tt:hashtag) && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (caption =~ "0123456789" && link = "http://www.google.com"^^tt:url) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) || (filter =~ "0123456788" && location = $context.location.work))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @weatherapi.current(location=$context.location.home), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && humidity <= 42 && cloudiness <= 55)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
now => @thecatapi.get, (image_id =~ "0123456788" && count < 11) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => *
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "23456789" && count >= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @github.add_email(email=v_image_id) ;
}
====
now => @imgflip.list, (name = "9876543210" || name = "ABCDEFGHIJ" || (name = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify
;;
Main() {
    now => @imgflip.list(), ((name = "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_name := name, v_picture_url := picture_url => notify;
}
====
now => * => @tumblr-blog.post_text, ((title =~ "ABCDEFGHIJ" && body =~ "ABCDEFGHIJ") || body = "23456789") 
;;
Main() {
    now => @thecatapi.get(), ((image_id =~ "9876543210" && count >= 11 && link = "http://www.abc.def"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => @tumblr-blog.post_text(title=v_image_id, body=v_image_id) ;
}
====
* => @gmail.send_email, ((subject =~ "0123456789" && message = "0123456789") || to = "bob@stanford.edu"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || to = "alice@gmail.com"^^tt:email_address || (to = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789" && message =~ "23456789") || message =~ "ABCDEFGHIJ" || (subject = "0123456789" && message = "ABCDEFGHIJ")) 
;;
Main() {
    @fitness-tracker.getbody(), ((weight < 82kg && leanMass >= 7 && bmi >= 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="12345", message="9876543210") ;
}
====
@twitter.direct_message, true , v_from := from, v_message := message, v___reserved := __reserved => * => *
;;
Main() {
    @twitter.direct_message(), ((message =~ "ABCDEFGHIJ" && __reserved = true)) , v_from := from, v_message := message, v___reserved := __reserved => @almond_dates.search(), true , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @gmail.send_email, (subject = "0123456789" || (to = "alice@gmail.com"^^tt:email_address && subject = "0123456789" && message = "ABCDEFGHIJ") || message =~ "23456789" || (to = "bob@stanford.edu"^^tt:email_address && message = "0123456789")) 
;;
Main() {
    @github.new_issue_comment(repo_name="ABCDEFGHIJ"), ((user = "bob"^^tt:username || body =~ "0123456789" || repo_name =~ "23456789" || (repo_name =~ "0123456788" && user = "bob"^^tt:username && issue_number > 11 && body = "ABCDEFGHIJ") || (repo_name =~ "0123456789" && user = "alice"^^tt:username && time = makeDate(1462320000000)))) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="0123456789", message="9876543210") ;
}
====
* => @linkedin.get_profile, ((headline =~ "ABCDEFGHIJ" && specialties =~ "12345") || contains(positions, "12345") || (headline =~ "0123456788" && specialties = "9876543210" && contains(positions, "0123456789") && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    now => @linkedin.get_profile(), true , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), (value >= 155lb) , v_time := time, v_value := value => @phone.set_ringer(mode=enum(silent)) ;
}
====
* => @builtin.get_date, true , v_date := date => notify
;;
Main() {
    @tumblr.new_text(blog_name="23456789"), ((title =~ "0123456788" && link = "http://www.google.com"^^tt:url)) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @builtin.get_date(), true , v_date := date => notify;
}
====
* => @tumblr-blog.post_picture, (caption = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @almond_dates.monitor(), ((interest = "ABCDEFGHIJ" || (interest = "23456789" && message =~ "9876543210" && poster =~ "23456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @tumblr-blog.post_picture(caption=v_message, picture_url=v_poster) ;
}
====
* => @speaker.play_music, true 
;;
Main() {
    @gmail.receive_primary_email(), ((from_name =~ "ABCDEFGHIJ" && from_address = "bob@stanford.edu"^^tt:email_address && snippet = "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @speaker.play_music() ;
}
====
now => @github.comment_issue, (repo_name = "0123456788" && issue_number >= 14 && body =~ "12345") 
;;
Main() {
    now => @github.comment_issue(repo_name="23456789", issue_number=42, body="ABCDEFGHIJ") ;
}
====
* => @xkcd.get_comic, (number > 11 && title =~ "12345" && alt_text = "12345") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @xkcd.get_comic(), (((title = "12345" && link = "http://www.abc.def"^^tt:url) || title =~ "0123456789" || (number <= 11 && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && alt_text =~ "23456789"))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@slack.receive, true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), (timestamp =~ "12345") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @heatpad.set_power(power=enum(off)) ;
}
====
* => @almond_dates.post, ((message =~ "0123456788" && poster =~ "12345") || message = "9876543210") 
;;
Main() {
    @weatherapi.monitor(location=$context.location.work), ((wind_speed >= 5kmph && humidity <= 55 && fog > 11 && weather = "ABCDEFGHIJ")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_dates.post(interest="12345", message="9876543210", poster="23456789", phone="+15551234567"^^tt:phone_number) ;
}
====
* => @slack.send, (channel = "cat"^^tt:hashtag || message =~ "9876543210") 
;;
Main() {
    @twitter.my_tweet(), (((contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || (contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "alice"^^tt:username) || (contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username) || contains(hashtags, "cat"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @slack.send(message=v_text, channel="cat"^^tt:hashtag) ;
}
====
* => @almond_bike_market.search, ((info =~ "12345" && price >= 11) || price > 55 || (info =~ "ABCDEFGHIJ" && price < 14) || (info =~ "0123456788" && price < 14) || price > 55 || (info = "12345" && price = 42) || price >= 14)  => notify
;;
Main() {
    @builtin.at(time=makeTime(15,0)), true  => @almond_bike_market.search(), (info = "23456789")  => notify;
}
====
* => @parklon_heatpad.set_power, true 
;;
Main() {
    @phone.gps(), ((location = $context.location.work && altitude < 42cm && bearing <= 55 && speed > 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @parklon_heatpad.set_power(power=enum(off)) ;
}
====
now => @almond_dates.search, ((interest =~ "12345" && message =~ "0123456789") || interest =~ "ABCDEFGHIJ" || (message = "12345" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    now => @almond_dates.search(), (interest =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@google_drive.new_drive_file, file_name =~ "ABCDEFGHIJ" , v_file_name := file_name => *
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @slack.setPresence(presence=enum(away)) ;
}
====
now => @holidays.next_uk_holiday_event, description = "9876543210" , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "0123456789" && description =~ "0123456788")) , v_date := date, v_summary := summary, v_description := description => @dropbox.move(new_name=v_summary, old_name="0123456788") ;
}
====
* => @twitter.search_by_hashtag, (text =~ "ABCDEFGHIJ" && in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, from="alice"^^tt:username), (((count = 7 && text =~ "12345" && contains(hashtags, "funny"^^tt:hashtag) && in_reply_to = "bob"^^tt:username) || (contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "bob"^^tt:username))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => @google_drive.create_new_drive_file, (file_name = "12345" || file_name =~ "12345") 
;;
Main() {
    @xkcd.new_whatif(), ((link = "http://www.abc.def"^^tt:url || (title = "0123456788" && link = "http://www.abc.def"^^tt:url))) , v_title := title, v_link := link => @google_drive.create_new_drive_file(file_name="0123456789") ;
}
====
@holidays.us_holiday_event, (description =~ "ABCDEFGHIJ" || summary =~ "23456789") , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), ((summary =~ "23456789" && description =~ "12345")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@omlet.incomingmessage, message =~ "12345" , v_feed := feed, v_type := type, v_message := message => * => *
;;
Main() {
    @omlet.incomingmessage(), ((type = enum(text) && message = "12345")) , v_type := type, v_message := message => @speaker.play_music() ;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), ((time = makeDate(1462320000000) || value >= 82kg || time = makeDate(1487030400000))) , v_time := time, v_value := value => @almond_couch_market.post(title="23456789", price=14, contact="+15551234567"^^tt:phone_number) ;
}
====
now => @xkcd.get_comic, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || alt_text = "0123456789") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @car.honk_horn, true 
;;
Main() {
    now => @xkcd.get_comic(), ((number > 11 || (number >= 7 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @car.honk_horn() ;
}
====
now => * => @almond_bike_market.post, (price <= 55 || (price < 7 && poster =~ "12345")) 
;;
Main() {
    now => @gmail.get_latest(), ((from = "alice@gmail.com"^^tt:email_address || subject = "ABCDEFGHIJ")) , v_from := from, v_subject := subject, v_label := label => @almond_bike_market.post(title=v_label, info=v_label, poster=v_label, price=42) ;
}
====
now => @almond_couch_market.search, (price > 7 || (title = "ABCDEFGHIJ" && price > 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), (((title = "0123456789" && price < 42) || title =~ "12345" || (title = "ABCDEFGHIJ" && price >= 55) || (title = "9876543210" && price <= 14 && contact = "+16501234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @thermostat.temperature(), ((time = makeDate(1487030400000) && value > 73F)) , v_time := time, v_value := value => @heatpad.set_power(power=enum(on)) ;
}
====
* => @almond_bike_market.post, (poster =~ "0123456789" || (title = "0123456788" && price <= 11 && info =~ "ABCDEFGHIJ")) 
;;
Main() {
    @instagram.new_picture(), ((caption = "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && filter =~ "0123456789")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_bike_market.post(info=v_filter, poster=v_filter, title="12345", price=42) ;
}
====
* => @linkedin.get_profile, (headline =~ "ABCDEFGHIJ" && industry = "0123456789" && specialties = "23456789") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @tumblr.new_text(blog_name="12345"), ((title = "0123456789" && body = "23456789")) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @linkedin.get_profile(), ((headline =~ "0123456789" && industry = "ABCDEFGHIJ" && specialties = "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
* => @slack.setPresence, true 
;;
Main() {
    @jawbone_up.getsleep(), (((awakeTime = makeDate(1462320000000) && duration <= 2h && rem <= 30min && deep <= 2h) || (duration = 2h && rem >= 2h && light > 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @slack.setPresence(presence=enum(away)) ;
}
====
@fitness-tracker.getbody, (weight = 82kg && leanMass > 7 && bodyFat >= 42) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify
;;
Main() {
    @fitness-tracker.getbody(), (((weight >= 155lb && leanMass <= 55) || (time = makeDate(1462320000000) && weight = 155lb && leanMass <= 14 && bodyFat >= 42))) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => notify;
}
====
@github.new_issue_comment, true , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => * => *
;;
Main() {
    @github.new_issue_comment(repo_name="12345"), ((user = "alice"^^tt:username && body = "9876543210")) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @tumblr-blog.post_picture(caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @heatpad.set_power, power = enum(on) 
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.google.com"^^tt:url && user = "23456789" && category =~ "23456789")) , v_title := title, v_link := link, v_user := user, v_category := category => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @google_drive.create_new_drive_file, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id > 55 && name = "0123456788" && estimated_diameter_min = 42cm && estimated_diameter_max <= 1000m && dangerous = true && relative_velocity = 25mph && closest_distance_to_earth = 1000m && orbiting_body = "0123456788")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @google_drive.create_new_drive_file(file_name="0123456788") ;
}
====
* => @xkcd.random_comic, alt_text = "0123456788" , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number < 14 && alt_text = "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@builtin.timer, (interval <= 2h || interval > 30min || interval >= 2h)  => notify
;;
Main() {
    @builtin.timer(interval=2h), (interval < 30min)  => notify;
}
====
@github.new_commit, ((message =~ "0123456788" && time = makeDate(1462320000000)) || (user = "alice"^^tt:username && message = "9876543210" && time = makeDate(1487030400000)) || (message = "9876543210" && time = makeDate(1487030400000)) || (user = "alice"^^tt:username && time = makeDate(1487030400000))) , v_user := user, v_message := message, v_time := time => * => notify
;;
Main() {
    @github.new_commit(repo_name="ABCDEFGHIJ"), true , v_user := user, v_message := message, v_time := time => notify;
}
====
now => @omlet.send_to, ((to = "bob"^^tt:username && message =~ "12345") || message =~ "9876543210") 
;;
Main() {
    now => @omlet.send_to(to="alice"^^tt:username, message="9876543210") ;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "ABCDEFGHIJ" && headline = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || formatted_name = "0123456788") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @rss.new_post(), true , v_title := title, v_link := link => @linkedin.get_profile(), ((formatted_name =~ "ABCDEFGHIJ" && industry =~ "0123456789" && specialties = "9876543210" && contains(positions, "12345"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
@reddit_front_page.newpost, title = "23456789" , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((user =~ "ABCDEFGHIJ" || (link = "http://www.abc.def"^^tt:url && user =~ "ABCDEFGHIJ" && category = "12345") || (link = "http://www.google.com"^^tt:url && user =~ "23456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
now => @dropbox.open, file_name =~ "12345" , v_url := url => @light-bulb.alert_long, true 
;;
Main() {
    now => @dropbox.open(file_name="0123456789"), ((url =~ "0123456789" || url =~ "12345" || (file_name = "23456789" && url = "ABCDEFGHIJ") || (file_name = "23456789" && url = "ABCDEFGHIJ"))) , v_url := url => @light-bulb.alert_long() ;
}
====
now => @gmail.get_latest, (subject = "9876543210" && label =~ "12345") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), (((subject =~ "12345" && label =~ "23456789") || subject = "9876543210" || (from = "bob@stanford.edu"^^tt:email_address && subject = "ABCDEFGHIJ" && label =~ "0123456788") || subject = "12345" || (from = "alice@gmail.com"^^tt:email_address && label = "0123456788") || label =~ "9876543210" || (from = "alice@gmail.com"^^tt:email_address && subject =~ "0123456789" && label =~ "23456789"))) , v_from := from, v_subject := subject, v_label := label => @heatpad.set_power(power=enum(on)) ;
}
====
* => @phone.notify, message =~ "23456789" 
;;
Main() {
    now => @phone.notify(title="23456789", message="ABCDEFGHIJ") ;
}
====
now => * => @almond_bike_market.post, (price <= 7 && info = "0123456788" && poster = "ABCDEFGHIJ") 
;;
Main() {
    now => @almond_bike_market.post(title="0123456789", price=14, info="9876543210", poster="0123456788") ;
}
====
@sleep-tracker.getsleep, (time = makeDate(1487030400000) && awakeTime = makeDate(1487030400000) && duration >= 30min && rem > 2h && light <= 2h && deep < 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((awakeTime = makeDate(1462320000000) && duration <= 30min && rem = 30min && light < 30min && deep < 30min) || (time = makeDate(1487030400000) && place = $context.location.work && duration = 30min && rem = 2h) || (awakeTime = makeDate(1462320000000) && rem <= 2h && deep = 2h) || (awakeTime = makeDate(1487030400000) && duration = 2h && rem <= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @thecatapi.get(), ((image_id = "ABCDEFGHIJ" || (count = 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @weatherapi.current, ((temperature = 22C && humidity < 14 && cloudiness <= 55 && fog = 42 && weather =~ "0123456789") || (location = $context.location.home && temperature > 73F && wind_speed < 25mph && humidity >= 7 && weather = "9876543210")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=$context.location.home), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && wind_speed > 25mph && cloudiness < 11 && fog >= 42 && weather = "0123456788")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
now => @wall_street_journal.get_opinions, (title =~ "23456789" && updated = makeDate(1487030400000)) , v_title := title, v_link := link, v_updated := updated => @light-bulb.color_loop, true 
;;
Main() {
    now => @wall_street_journal.get_opinions(), (link = "http://www.abc.def"^^tt:url) , v_title := title, v_link := link, v_updated := updated => @light-bulb.color_loop() ;
}
====
* => @ytranslate.detect_language, text =~ "23456789" , v_detected_language := detected_language => *
;;
Main() {
    now => @ytranslate.detect_language(text="23456789"), ((text =~ "0123456789" || text = "0123456789")) , v_detected_language := detected_language => @dropbox.create_new_folder(folder_name="0123456788") ;
}
====
* => * => @light-bulb.alert_long, true 
;;
Main() {
    @holidays.uk_holiday_event(), (summary = "0123456788") , v_date := date, v_summary := summary, v_description := description => @light-bulb.alert_long() ;
}
====
now => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address || (from = "alice@gmail.com"^^tt:email_address && subject =~ "12345" && label = "12345") || label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), ((from = "bob@stanford.edu"^^tt:email_address && label = "0123456788")) , v_from := from, v_subject := subject, v_label := label => @light-bulb.color_loop() ;
}
====
now => * => @parklon_heatpad.set_power, power = enum(on) 
;;
Main() {
    now => @nasa.rover(), (((date_taken = makeDate(1462320000000) && count > 55) || (date_taken = makeDate(1487030400000) && count < 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
now => * => @almond_bike_market.post, ((info = "0123456789" && poster = "0123456788") || (title =~ "9876543210" && info =~ "0123456789")) 
;;
Main() {
    now => @almond_bike_market.search(info="ABCDEFGHIJ"), (info =~ "12345")  => @almond_bike_market.post(title="23456789", price=14, info="23456789", poster="ABCDEFGHIJ") ;
}
====
now => * => @linkedin.share, (status = "23456789" || status = "0123456788") 
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
* => * => @heatpad.set_power, (power = enum(on) || power = enum(on) || power = enum(on)) 
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => @twitter.search_by_hashtag(count=v_heartrate, query_hashtag="cat"^^tt:hashtag, in_reply_to="bob"^^tt:username), ((count <= 14 && text = "0123456788" && contains(hashtags, "cat"^^tt:hashtag) && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls => @heatpad.set_power(power=enum(on)) ;
}
====
@omlet.incomingmessage, true , v_feed := feed, v_type := type, v_message := message => * => notify
;;
Main() {
    @omlet.incomingmessage(), true , v_type := type, v_message := message => @holidays.next_uk_holiday_event(), ((date = makeDate(1487030400000) && summary = "12345")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && label = "9876543210") , v_from := from, v_subject := subject, v_label := label => *
;;
Main() {
    now => @gmail.get_latest(), true , v_from := from, v_subject := subject, v_label := label => @almond_bike_market.post(title=v_subject, poster=v_subject, price=7, info="0123456789") ;
}
====
* => @thecatapi.get, count < 42 , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @youtube.source(), ((channel_title =~ "9876543210" || (channel_id =~ "0123456788" && video_id =~ "0123456789" && video_url = "http://www.google.com"^^tt:url))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @thecatapi.get(), (((image_id =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) || link = "http://www.google.com"^^tt:url || (image_id =~ "12345" && link = "http://www.abc.def"^^tt:url))) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @security-camera.set_power, true 
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1462320000000) && weight < 155lb)) , v_time := time, v_weight := weight => @security-camera.set_power(power=enum(off)) ;
}
====
@sleep-tracker.getsleep, ((time = makeDate(1487030400000) && place = $context.location.home && awakeTime = makeDate(1462320000000) && duration > 2h && rem <= 30min && light <= 30min) || (place = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && duration < 2h && rem > 30min)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @weatherapi.sunrise, (date = v_asleepTime || (location = $context.location.current_location && sunrise = makeDate(1487030400000) && sunset = makeDate(1462320000000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && asleepTime = makeDate(1487030400000) && duration < 30min && rem > 2h && deep > 30min) || (awakeTime = makeDate(1487030400000) && duration >= 2h && rem >= 30min && light >= 30min) || (time = makeDate(1462320000000) && place = $context.location.work && rem <= 30min && light <= 2h) || (asleepTime = makeDate(1462320000000) && duration >= 30min && rem >= 2h && light = 30min && deep > 30min) || (time = makeDate(1462320000000) && place = $context.location.home && awakeTime = makeDate(1462320000000) && rem >= 2h && deep >= 2h))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((location = $context.location.work && date = makeDate(1462320000000)) || (date = makeDate(1487030400000) && sunset = makeDate(1487030400000)))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
@washington_post.new_article, true , v_title := title, v_link := link, v_description := description => * => *
;;
Main() {
    @washington_post.new_article(section=enum(national)), (((title = "0123456788" && description = "0123456789") || (link = "http://www.abc.def"^^tt:url && description =~ "0123456788"))) , v_title := title, v_link := link, v_description := description => @car.honk_horn() ;
}
====
@tumblr.new_text, (blog_name = "ABCDEFGHIJ" && date = makeDate(1462320000000) && title = "12345" && body = "0123456789") , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => * => notify
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), (((blog_name = "0123456789" && date = makeDate(1462320000000) && link = "http://www.abc.def"^^tt:url && body =~ "0123456789") || (title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url))) , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @thecatapi.get(), (count = 7) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
now => @icalendar.list_events, (summary =~ "9876543210" && sequence < 42 && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    now => @icalendar.list_events(), ((summary =~ "23456789" && sequence > 14 && organizer = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@activity-tracker.getmove, (updateTime = makeDate(1462320000000) && day =~ "ABCDEFGHIJ" && steps < 55 && activeTime > 30min && inactiveTime >= 2h && calories >= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => *
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && day =~ "9876543210" && distance = 1000m && steps >= 7 && activeTime > 2h && inactiveTime < 30min && calories >= 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => notify;
}
====
@google_drive.new_drive_file, file_name =~ "12345" , v_file_name := file_name => * => *
;;
Main() {
    @google_drive.new_drive_file(), (file_name = "ABCDEFGHIJ") , v_file_name := file_name => @fitbit.recordweight(weight=155lb) ;
}
====
@wall_street_journal.lifestyle, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link => * => notify
;;
Main() {
    @wall_street_journal.lifestyle(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => notify;
}
====
* => @bluetooth_speaker.set_volume, percent <= 11 
;;
Main() {
    now => @bluetooth_speaker.set_volume(percent=14) ;
}
====
@washington_post.new_article, (title =~ "0123456789" && description =~ "0123456789") , v_title := title, v_link := link, v_description := description => *
;;
Main() {
    @washington_post.new_article(section=enum(politics)), ((description =~ "12345" || link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_description := description => @car.honk_horn() ;
}
====
@bodytrace.source, weight < 82kg , v_time := time, v_weight := weight => * => notify
;;
Main() {
    @bodytrace.source(), (weight <= 82kg) , v_time := time, v_weight := weight => @fitbit.getsteps(), ((steps > 14 || steps <= 14)) , v_date := date, v_steps := steps => notify;
}
====
* => @phone.get_gps, (bearing <= 11 && speed > 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify
;;
Main() {
    now => @phone.get_gps(), (speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => notify;
}
====
@icalendar.event_begin, true , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => @onedrive.upload_picture, (file_name =~ "0123456788" || file_name = "12345" || file_name = v_description) 
;;
Main() {
    @icalendar.event_begin(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1487030400000) && summary =~ "0123456788" && sequence <= 7 && location =~ "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @xkcd.random_comic(), ((number <= 11 && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @onedrive.upload_picture(file_name=v_title, picture_url=v_picture_url) ;
}
====
* => @almond_couch_market.search, price < 7 , v_title := title, v_price := price, v_contact := contact => @car.honk_horn, true 
;;
Main() {
    now => @almond_couch_market.search(), (price > 42) , v_title := title, v_price := price, v_contact := contact => @car.honk_horn() ;
}
====
@holidays.uk_holiday_event, description =~ "23456789" , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && description = "9876543210")) , v_date := date, v_summary := summary, v_description := description => @xkcd.get_comic(), (link = "http://www.google.com"^^tt:url) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@almond_dates.monitor, (message =~ "0123456789" && poster =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "0123456788" && message =~ "9876543210" && phone = "+16501234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => * => @thermostat.set_hvac_mode, true 
;;
Main() {
    @weatherapi.monitor(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature >= 22C && wind_speed > 25mph && cloudiness >= 7 && fog <= 7 && weather = "0123456788") || (temperature < 22C && wind_speed > 25mph && humidity > 55 && cloudiness > 55 && fog < 7))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @youtube.channels_by_category(category_id=enum(cooking)), ((description = "0123456788" || title = "23456789" || (category_id = enum(music) && channel_id =~ "9876543210" && title =~ "ABCDEFGHIJ"))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
* => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address || message = "0123456788" || (to = "alice@gmail.com"^^tt:email_address && subject =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || message = "0123456789" || subject =~ "12345" || (subject = "12345" && message =~ "9876543210") || to = "bob@stanford.edu"^^tt:email_address) 
;;
Main() {
    @tumblr.new_photo(blog_name="0123456788"), ((blog_name = "12345" && date = makeDate(1462320000000))) , v_date := date, v_caption := caption, v_link := link, v_tags := tags, v_picture_urls := picture_urls => @gmail.send_picture(subject=v_caption, picture_url=v_caption, to="alice@gmail.com"^^tt:email_address, message="0123456789") ;
}
====
@wall_street_journal.opinions, (title = "12345" || (title = "12345" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => notify
;;
Main() {
    @wall_street_journal.opinions(), (title = "23456789") , v_title := title, v_link := link => notify;
}
====
@slack.receive, ((timestamp =~ "23456789" && from = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && from = "bob"^^tt:username && message =~ "0123456789") || (channel = "lol"^^tt:hashtag && message =~ "23456789")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), ((timestamp =~ "23456789" || channel = "cat"^^tt:hashtag)) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
@slack.receive, ((timestamp = "23456789" && from = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (timestamp = "23456789" && from = "bob"^^tt:username && message = "23456789") || timestamp =~ "0123456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), true , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @giphy.get_tag(tag=v_message), true , v_picture_url := picture_url => notify;
}
====
now => * => @speaker.set_volume, percent > 55 
;;
Main() {
    now => @weatherapi.current(location=$context.location.current_location), (((location = $context.location.work && temperature < 73F && wind_speed <= 25mph && humidity > 42 && fog < 55) || (wind_speed < 25mph && humidity < 42 && fog < 55) || (location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature > 22C && wind_speed >= 5kmph && cloudiness >= 14))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => @speaker.set_volume(percent=v_fog) ;
}
====
@security-camera.new_event, ((start_time = makeDate(1462320000000) && has_sound = true && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (start_time = makeDate(1487030400000) && has_motion = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), ((has_motion = false && has_person = false)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @car.honk_horn() ;
}
====
* => @weatherapi.sunrise, ((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && sunrise = makeDate(1462320000000)) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify
;;
Main() {
    now => @weatherapi.sunrise(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (sunrise = makeDate(1462320000000)) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => notify;
}
====
* => @instagram.get_pictures, ((count >= 7 && caption =~ "ABCDEFGHIJ" && filter =~ "ABCDEFGHIJ" && contains(hashtags, "lol"^^tt:hashtag)) || (caption =~ "0123456788" && filter = "12345" && contains(hashtags, "funny"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @activity-tracker.getmove(), ((distance > 42cm && steps <= 55 && inactiveTime <= 30min && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @instagram.get_pictures(count=11), ((count > 55 && contains(hashtags, "lol"^^tt:hashtag) && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @omlet.send_picture(to=v_caption, picture_url=v_filter, caption=v_caption) ;
}
====
* => @security-camera.get_url, true , v_link := link => *
;;
Main() {
    now => @security-camera.get_url(), (link = "http://www.abc.def"^^tt:url) , v_link := link => @thermostat.set_hvac_mode(mode=enum(heat)) ;
}
====
* => @almond_dates.search, ((message =~ "12345" && poster =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "ABCDEFGHIJ" && poster = "ABCDEFGHIJ")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @gmail.receive_email(), ((from_name =~ "0123456789" && contains(labels, "23456789"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @almond_dates.search(), (interest = "ABCDEFGHIJ") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @almond_couch_market.search, true , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), ((time = makeDate(1487030400000) && heartrate > 42)) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_couch_market.search(), (price >= 7) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @almond_bike_market.post, ((title =~ "0123456789" && price >= 11 && info =~ "0123456789" && poster = "0123456789") || (title =~ "23456789" && price > 55 && info = "0123456788")) 
;;
Main() {
    now => @almond_bike_market.post(title="12345", price=55, info="0123456789", poster="0123456789") ;
}
====
@icalendar.event_begin, ((summary = "0123456788" && description = "9876543210" && sequence >= 7 && organizer = "0123456789" && location = "9876543210") || (start_date = makeDate(1487030400000) && end_date = makeDate(1487030400000) && sequence >= 7) || (summary =~ "0123456789" && sequence = 55 && organizer = "23456789") || (end_date = makeDate(1487030400000) && sequence <= 55 && organizer = "ABCDEFGHIJ" && location =~ "ABCDEFGHIJ") || (sequence > 55 && location =~ "23456789")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), (((end_date = makeDate(1487030400000) && description =~ "12345") || (summary =~ "0123456788" && description =~ "12345" && sequence = 11 && organizer = "23456789") || (description = "12345" && organizer = "12345") || (start_date = makeDate(1462320000000) && sequence > 7 && organizer =~ "0123456788"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @holidays.next_us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@sportradar.ncaambb_team, (watched_is_home = true && away_points < 11 && home_points < 42) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => * => notify
;;
Main() {
    @sportradar.ncaambb_team(watched_team_alias="cal"^^sportradar:ncaambb_team("California Bears")), ((watched_is_home = true && away_name = "12345" && game_status = enum(scheduled) && scheduled_time = makeDate(1487030400000) && away_points > 42 && result = enum(lose))) , v_other_team_alias := other_team_alias, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_away_points := away_points, v_home_points := home_points, v_result := result => @thermostat.get_humidity(), (value > 14) , v_value := value => notify;
}
====
@reddit_front_page.newpost, (link = "http://www.abc.def"^^tt:url && category =~ "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((title = "0123456788" || (title =~ "0123456788" && link = "http://www.abc.def"^^tt:url && category =~ "23456789"))) , v_title := title, v_link := link, v_user := user, v_category := category => @almond_dates.search(), ((message =~ "0123456789" && poster = "23456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => @almond_couch_market.search, (title = "12345" && price > 55 && contact = "+16501234567"^^tt:phone_number) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price < 7 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @lg_webos_tv.unmute() ;
}
====
* => @slack.updateChannelTopic, topic =~ "9876543210" 
;;
Main() {
    now => @slack.updateChannelTopic(channel="cat"^^tt:hashtag, topic="9876543210") ;
}
====
* => @almond_couch_market.post, ((price > 55 && contact = "+16501234567"^^tt:phone_number) || (title =~ "ABCDEFGHIJ" && price = 11)) 
;;
Main() {
    @activity-tracker.getmove(), (((updateTime = makeDate(1462320000000) && steps > 7 && activeTime = 30min && inactiveTime >= 2h && calories >= 500kcal) || (day = "ABCDEFGHIJ" && steps <= 11 && activeTime >= 2h && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_couch_market.post(price=v_steps, title="12345", contact="+16501234567"^^tt:phone_number) ;
}
====
@security-camera.new_event, true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => * => notify
;;
Main() {
    @security-camera.new_event(), ((has_sound = false && has_person = false && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration < 30min && light >= 2h && deep <= 30min) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => *
;;
Main() {
    @sleep-tracker.getsleep(), (rem >= 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @twitter.search_by_hashtag(query_hashtag="cat"^^tt:hashtag, from="bob"^^tt:username, in_reply_to="alice"^^tt:username), ((count >= 11 && contains(hashtags, "lol"^^tt:hashtag) && contains(urls, "http://www.google.com"^^tt:url))) , v_text := text, v_hashtags := hashtags, v_urls := urls => @lg_webos_tv.raise_volume() ;
}
====
@weatherapi.monitor, (location = $context.location.current_location && temperature >= 73F && wind_speed < 25mph && humidity >= 7 && cloudiness <= 7 && fog > 11) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), (((location = $context.location.home && temperature <= 73F && wind_speed < 25mph && humidity >= 42 && cloudiness <= 42 && weather = "23456789") || (wind_speed = 5kmph && fog >= 55 && weather =~ "0123456789"))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @almond_dates.search(), (((interest = "0123456788" && message = "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (interest = "0123456788" && message = "0123456789"))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
now => * => @builtin.debug_log, true 
;;
Main() {
    now => @builtin.debug_log(message="ABCDEFGHIJ") ;
}
====
now => * => @twitter.send_direct_message, true 
;;
Main() {
    now => @twitter.send_direct_message(to="bob"^^tt:username, message="0123456789") ;
}
====
@instagram.new_picture, (filter = "23456789" && contains(hashtags, "funny"^^tt:hashtag)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @instagram.new_picture(), ((caption = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) && weight <= 155lb) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight >= 155lb)) , v_time := time, v_weight := weight => @car.honk_horn() ;
}
====
* => @thecatapi.get, (count = 42 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.google.com"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @builtin.at(time=makeTime(7,30)), true  => @thecatapi.get(), ((count <= 11 && link = "http://www.google.com"^^tt:url)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @parklon_heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @parklon_heatpad.set_power(power=enum(on)) ;
}
====
@activity-tracker.getmove, (distance >= 1000m && activeTime <= 30min && calories = 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), ((updateTime = makeDate(1462320000000) && steps = 14 && activeTime >= 2h && inactiveTime >= 2h && calories = 500kcal)) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
* => @wall_street_journal.get_opinions, (updated = makeDate(1487030400000) || link = "http://www.google.com"^^tt:url) , v_title := title, v_link := link, v_updated := updated => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @slack.receive(), (timestamp = "0123456788") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @wall_street_journal.get_opinions(), (title = "12345") , v_title := title, v_link := link, v_updated := updated => @heatpad.set_power(power=enum(on)) ;
}
====
* => * => @slack.send_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (channel = "lol"^^tt:hashtag && caption =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((temperature < 22C && wind_speed > 25mph && humidity = 55 && fog = 55 && status = enum(cloudy))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @thermostat.get_temperature(), (value > 22C) , v_value := value => @slack.send_picture(channel="lol"^^tt:hashtag, caption="0123456789", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => @almond_couch_market.post, contact = "+15551234567"^^tt:phone_number 
;;
Main() {
    @jawbone_up.getsleep(), (((time = makeDate(1462320000000) && asleepTime = makeDate(1462320000000) && light <= 30min) || (awakeTime = makeDate(1487030400000) && asleepTime = makeDate(1462320000000) && duration = 2h && rem = 2h && deep > 30min) || (time = makeDate(1462320000000) && deep <= 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @almond_couch_market.post(title="23456789", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@weatherapi.monitor, ((location = $context.location.work && temperature < 22C && wind_speed <= 5kmph && cloudiness >= 7) || (wind_speed = 25mph && fog >= 42 && weather =~ "ABCDEFGHIJ") || (wind_speed < 25mph && fog >= 7) || (temperature >= 73F && wind_speed > 5kmph && fog < 42 && weather = "0123456788")) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => * => notify
;;
Main() {
    @weatherapi.monitor(location=$context.location.current_location), ((temperature <= 22C && humidity <= 14 && cloudiness <= 55 && fog < 42)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @fitbit.getsteps(), ((date = makeDate(1462320000000) && steps > 42)) , v_date := date, v_steps := steps => notify;
}
====
@rss.new_post, (title = "23456789" || (title = "23456789" && link = "http://www.abc.def"^^tt:url) || title =~ "ABCDEFGHIJ") , v_title := title, v_link := link => * => *
;;
Main() {
    @rss.new_post(), (title =~ "0123456789") , v_title := title, v_link := link => @ytranslate.detect_language(text=v_title), (text = "12345") , v_detected_language := detected_language => @phone.send_sms(to="+15551234567"^^tt:phone_number, body="9876543210") ;
}
====
now => @builtin.get_random_between, (low < 42 && high >= 7 && random >= 7) , v_random := random => notify
;;
Main() {
    now => @builtin.get_random_between(low=11, high=55), (high <= 7) , v_random := random => notify;
}
====
@bodytrace.source, weight <= 155lb , v_time := time, v_weight := weight => notify
;;
Main() {
    @bodytrace.source(), (weight >= 82kg) , v_time := time, v_weight := weight => notify;
}
====
@fitness-tracker.getbody, ((weight <= 82kg && leanMass > 14 && bodyFat < 14 && bmi > 11) || (place = $context.location.home && bmi < 11) || (place = $context.location.current_location && weight = 155lb && leanMass < 42 && bodyFat > 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => *
;;
Main() {
    @fitness-tracker.getbody(), ((bodyFat >= 7 && bmi < 14)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @omlet.send_to(to="alice"^^tt:username, message="23456789") ;
}
====
* => @uber.time_estimate, (start = $context.location.current_location && estimate >= 2h) , v_uber_type := uber_type, v_estimate := estimate => *
;;
Main() {
    @holidays.uk_holiday_event(), ((date = makeDate(1462320000000) && summary =~ "0123456788" && description = "0123456788")) , v_date := date, v_summary := summary, v_description := description => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), ((start = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && uber_type = "0123456788" && estimate <= 2h)) , v_uber_type := uber_type, v_estimate := estimate => @onedrive.rename_file(old_name="ABCDEFGHIJ", new_name="ABCDEFGHIJ") ;
}
====
@slack.receive, (channel = "funny"^^tt:hashtag && timestamp = "0123456789" && message =~ "0123456789") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify
;;
Main() {
    @slack.receive(), (from = "alice"^^tt:username) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
@scale.source, true , v_time := time, v_value := value => * => @security-camera.set_power, (power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @scale.source(), ((value < 155lb || (time = makeDate(1487030400000) && value > 82kg))) , v_time := time, v_value := value => @security-camera.set_power(power=enum(off)) ;
}
====
* => @uber.time_estimate, (start = $context.location.work && uber_type =~ "0123456788" && estimate < 30min) , v_uber_type := uber_type, v_estimate := estimate => notify
;;
Main() {
    now => @uber.time_estimate(start=$context.location.work), (estimate >= 30min) , v_uber_type := uber_type, v_estimate := estimate => notify;
}
====
@bodytrace.source, true , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), ((time = makeDate(1487030400000) && weight >= 155lb)) , v_time := time, v_weight := weight => @gmail.send_email(to="alice@gmail.com"^^tt:email_address, subject="ABCDEFGHIJ", message="9876543210") ;
}
====
@scale.source, ((time = makeDate(1487030400000) && value < 82kg) || time = makeDate(1487030400000)) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (value <= 155lb) , v_time := time, v_value := value => @security-camera.get_snapshot(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => notify;
}
====
now => @phone.send_sms, body = "9876543210" 
;;
Main() {
    now => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="9876543210") ;
}
====
* => @light-bulb.alert_long, true 
;;
Main() {
    @nasa.asteroid(), ((asteroid_id <= 7 && estimated_diameter_min <= 42cm && relative_velocity = 5kmph && closest_distance_to_earth <= 42cm && orbiting_body =~ "0123456788")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => @light-bulb.alert_long() ;
}
====
@holidays.us_holiday_event, (date = makeDate(1462320000000) && summary =~ "12345" && description = "ABCDEFGHIJ") , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.us_holiday_event(), true , v_date := date, v_summary := summary, v_description := description => @icalendar.list_events(), ((summary =~ "ABCDEFGHIJ" && description = "23456789" && sequence = 11 && location = "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
now => * => @facebook.post_picture, (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "ABCDEFGHIJ") 
;;
Main() {
    now => @weatherapi.moon(location=$context.location.home), true , v_date := date, v_phase := phase => @facebook.post_picture(picture_url=v_phase, caption=v_phase) ;
}
====
* => @light-bulb.set_power, true 
;;
Main() {
    now => @light-bulb.set_power(power=enum(off)) ;
}
====
@gmail.receive_primary_email, (from_name =~ "0123456788" && from_address = "bob@stanford.edu"^^tt:email_address && date = makeDate(1462320000000)) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_primary_email(), ((from_name = "9876543210" && subject = "12345" && date = makeDate(1462320000000) && contains(labels, "0123456788"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @ytranslate.translate(text=v_snippet, target_language="en"^^tt:iso_lang_code("English")), true , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
* => @slack.updateChannelPurpose, true 
;;
Main() {
    @almond_bike_market.monitor(info="9876543210"), (((info =~ "12345" && price < 14) || info =~ "12345"))  => @slack.updateChannelPurpose(channel="lol"^^tt:hashtag, purpose="9876543210") ;
}
====
@phdcomics.new_post, link = "http://www.google.com"^^tt:url , v_title := title, v_link := link, v_picture_url := picture_url => * => *
;;
Main() {
    @phdcomics.new_post(), ((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @weatherapi.current(location=$context.location.current_location), ((temperature <= 73F && wind_speed <= 5kmph && humidity < 7 && cloudiness > 7 && fog >= 7)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
@instagram.new_picture, (caption =~ "0123456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    @instagram.new_picture(), ((filter = "0123456789" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @lg_webos_tv.unmute() ;
}
====
* => @gmail.send_email, (subject =~ "12345" && message = "9876543210") 
;;
Main() {
    @almond_dates.monitor(), (((interest =~ "9876543210" && message =~ "12345" && poster =~ "ABCDEFGHIJ" && phone = "+15551234567"^^tt:phone_number) || (interest = "0123456789" && phone = "+15551234567"^^tt:phone_number))) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @gmail.send_email(to=v_poster, subject=v_poster, message=v_message) ;
}
====
* => @github.add_email, (email = "bob@stanford.edu"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address || email = "alice@gmail.com"^^tt:email_address) 
;;
Main() {
    @google_drive.new_drive_file(), true , v_file_name := file_name => @github.add_email(email="alice@gmail.com"^^tt:email_address) ;
}
====
* => @almond_dates.post, ((interest =~ "9876543210" && message = "0123456789" && poster =~ "0123456789") || (poster =~ "9876543210" && phone = "+16501234567"^^tt:phone_number)) 
;;
Main() {
    @instagram.new_picture(), ((caption =~ "9876543210" && filter = "0123456788" && location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @almond_dates.post(interest=v_caption, message=v_caption, poster=v_caption, phone=v_caption) ;
}
====
@gmail.receive_primary_email, (subject = "ABCDEFGHIJ" && snippet =~ "12345") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_primary_email(), ((from_name = "9876543210" && subject = "12345" && snippet = "ABCDEFGHIJ")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@gmail.receive_email, ((from_name =~ "ABCDEFGHIJ" && subject =~ "9876543210" && contains(labels, "0123456789")) || (subject =~ "0123456789" && date = makeDate(1487030400000) && contains(labels, "0123456789") && snippet = "9876543210")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => *
;;
Main() {
    @gmail.receive_email(), ((subject =~ "ABCDEFGHIJ" && date = makeDate(1462320000000) && snippet = "0123456789")) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @omlet.send_to(message=v_from_name, to="alice"^^tt:username) ;
}
====
* => @almond_couch_market.search, ((title =~ "0123456789" && price < 7 && contact = "+16501234567"^^tt:phone_number) || title =~ "23456789" || (title = "9876543210" && price < 11)) , v_title := title, v_price := price, v_contact := contact => notify
;;
Main() {
    now => @almond_couch_market.search(), ((price = 7 || (title = "9876543210" && price > 14) || title =~ "12345")) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
now => * => @tumblr-blog.post_text, (body =~ "ABCDEFGHIJ" || body =~ "23456789" || (title =~ "12345" && body =~ "12345") || body = "23456789") 
;;
Main() {
    now => @icalendar.list_events(), ((start_date = makeDate(1462320000000) && end_date = makeDate(1462320000000) && description =~ "0123456788" && sequence < 11 && location = "12345")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @tumblr-blog.post_text(body=v_description, title="12345") ;
}
====
@builtin.at, time = makeTime(20,30)  => * => *
;;
Main() {
    @builtin.at(time=makeTime(7,30)), true  => @tumblr-blog.post_text(title="12345", body="ABCDEFGHIJ") ;
}
====
now => * => @bluetooth_speaker.play_music, true 
;;
Main() {
    now => @uber.price_estimate(start=$context.location.work, end=$context.location.home), (((end = $context.location.current_location && low_estimate >= 55 && currency_code = "9876543210") || (start = $context.location.current_location && end = $context.location.current_location && uber_type =~ "23456789" && high_estimate < 11 && currency_code =~ "23456789" && surge < 14 && duration <= 30min) || (uber_type =~ "0123456788" && high_estimate = 14 && currency_code = "12345" && surge >= 7 && duration = 30min && distance <= 42cm) || (end = $context.location.home && uber_type =~ "9876543210" && low_estimate <= 55 && surge >= 42 && duration >= 30min && distance <= 1000m))) , v_uber_type := uber_type, v_low_estimate := low_estimate, v_high_estimate := high_estimate, v_currency_code := currency_code, v_surge := surge, v_duration := duration, v_distance := distance => @bluetooth_speaker.play_music() ;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(info="12345"), ((info = "0123456789" && price > 42))  => @car.honk_horn() ;
}
====
* => @linkedin.get_profile, ((formatted_name =~ "0123456788" && specialties =~ "ABCDEFGHIJ") || industry =~ "12345") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @jawbone_up.getbody(), ((time = makeDate(1487030400000) && weight > 82kg && bodyFat = 7 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @linkedin.get_profile(), ((formatted_name = "12345" && headline = "0123456788" && industry = "23456789" && specialties =~ "ABCDEFGHIJ")) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => * => @facebook.post_picture, (caption = "12345" || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "23456789") || caption =~ "12345") 
;;
Main() {
    now => @uber.time_estimate(start=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((uber_type = "12345" && estimate <= 30min) || (start = $context.location.current_location && estimate < 2h))) , v_uber_type := uber_type, v_estimate := estimate => @facebook.post_picture(picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="9876543210") ;
}
====
* => @lg_webos_tv.set_volume, percent >= 11 
;;
Main() {
    @github.new_milestone(repo_name="12345"), true , v_user := user, v_description := description, v_time := time => @lg_webos_tv.set_volume(percent=11) ;
}
====
now => @almond_bike_market.search, (info = "ABCDEFGHIJ" && price < 42)  => notify
;;
Main() {
    now => @almond_bike_market.search(), (price >= 7)  => notify;
}
====
* => @onedrive.upload_picture, ((file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (file_name =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @reddit_front_page.newpost(), (title =~ "ABCDEFGHIJ") , v_title := title, v_link := link, v_user := user, v_category := category => @onedrive.upload_picture(file_name=v_category, picture_url=v_category) ;
}
====
@holidays.uk_holiday_event, ((date = makeDate(1487030400000) && description = "23456789") || summary =~ "23456789") , v_date := date, v_summary := summary, v_description := description => *
;;
Main() {
    @holidays.uk_holiday_event(), ((summary =~ "9876543210" && description =~ "12345")) , v_date := date, v_summary := summary, v_description := description => @lg_webos_tv.unmute() ;
}
====
now => @twitter.send_direct_message, (message = "ABCDEFGHIJ" || (to = "bob"^^tt:username && message =~ "23456789") || to = "alice"^^tt:username || (to = "bob"^^tt:username && message = "12345") || message = "0123456789" || message =~ "12345") 
;;
Main() {
    now => @twitter.send_direct_message(to="bob"^^tt:username, message="9876543210") ;
}
====
now => @almond_couch_market.search, price < 14 , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((title = "9876543210" && price > 42 && contact = "+16501234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => @almond_bike_market.post(price=v_price, title="9876543210", info="9876543210", poster="12345") ;
}
====
now => @twitter.search_by_hashtag, (text = "0123456788" && contains(hashtags, "lol"^^tt:hashtag)) , v_text := text, v_hashtags := hashtags, v_urls := urls => *
;;
Main() {
    now => @twitter.search_by_hashtag(query_hashtag="lol"^^tt:hashtag, count=11, from="alice"^^tt:username), (in_reply_to = "alice"^^tt:username) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
* => * => @twitter.send_direct_message, true 
;;
Main() {
    @phone.gps(), (((bearing < 42 && speed = 5kmph) || (location = $context.location.work && bearing > 7 && speed < 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @twitter.send_direct_message(to="alice"^^tt:username, message="0123456788") ;
}
====
now => * => @heatpad.set_power, (power = enum(off) || power = enum(off) || power = enum(on)) 
;;
Main() {
    now => @giphy.get(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_picture_url := picture_url => @heatpad.set_power(power=enum(off)) ;
}
====
now => @bing.web_search, (link = "http://www.abc.def"^^tt:url || (title = "ABCDEFGHIJ" && description = "12345")) , v_title := title, v_description := description, v_link := link => @security-camera.set_power, true 
;;
Main() {
    now => @bing.web_search(query="0123456789"), ((query = "23456789" && description =~ "12345")) , v_title := title, v_description := description, v_link := link => @security-camera.set_power(power=enum(on)) ;
}
====
now => * => @security-camera.set_power, (power = enum(off) || power = enum(off)) 
;;
Main() {
    now => @giphy.get_tag(tag="cat"^^tt:hashtag), true , v_picture_url := picture_url => @security-camera.set_power(power=enum(off)) ;
}
====
* => * => @tumblr-blog.post_text, true 
;;
Main() {
    @tumblr.new_text(blog_name="0123456789"), true , v_date := date, v_title := title, v_link := link, v_tags := tags, v_body := body => @dropbox.list_folder(folder_name=v_title), (((file_size >= 20MB && full_path = "0123456789") || (folder_name =~ "0123456789" && full_path =~ "0123456789"))) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => @tumblr-blog.post_text(title="9876543210", body="23456789") ;
}
====
@icalendar.event_begin, (start_date = makeDate(1487030400000) && description = "0123456788" && organizer = "0123456788" && location = "0123456788") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => notify
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "12345" && description = "23456789" && sequence < 11 && organizer =~ "23456789" && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @security-camera.get_url(), (link = "http://www.google.com"^^tt:url) , v_link := link => notify;
}
====
now => @builtin.get_random_between, ((low = 14 && high >= 11 && random > 14) || high >= 55 || (low = 11 && high > 55 && random <= 14) || (low > 42 && high >= 7 && random >= 11) || high < 7) , v_random := random => *
;;
Main() {
    now => @builtin.get_random_between(low=7, high=7), ((high <= 14 || (low <= 14 && random < 14) || (low <= 14 && high < 14 && random > 42))) , v_random := random => @bluetooth_speaker.play_music() ;
}
====
now => * => @dropbox.move, new_name =~ "23456789" 
;;
Main() {
    now => @xkcd.random_comic(), ((number = 11 && title =~ "0123456788" && link = "http://www.google.com"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @dropbox.move(new_name=v_title, old_name="12345") ;
}
====
@phdcomics.new_post, (title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (((title =~ "9876543210" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_title := title, v_link := link, v_picture_url := picture_url => @dropbox.list_folder(folder_name="12345"), (is_folder = false) , v_file_name := file_name, v_is_folder := is_folder, v_last_modified := last_modified, v_file_size := file_size, v_full_path := full_path => notify;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @xkcd.new_comic(), ((number > 7 && title = "12345" && link = "http://www.abc.def"^^tt:url)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @google_drive.create_new_drive_file(file_name="23456789") ;
}
====
now => * => @almond_bike_market.post, ((price > 42 && info =~ "9876543210") || poster = "23456789") 
;;
Main() {
    now => @imgflip.list(), true , v_name := name, v_picture_url := picture_url => @almond_bike_market.post(title=v_name, poster=v_name, price=14, info="12345") ;
}
====
* => @linkedin.get_profile, ((formatted_name = "23456789" && headline = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (headline =~ "9876543210" && specialties = "23456789" && contains(positions, "0123456788"))) , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify
;;
Main() {
    @wall_street_journal.markets(), true , v_title := title, v_link := link => @linkedin.get_profile(), (industry =~ "9876543210") , v_formatted_name := formatted_name, v_headline := headline, v_industry := industry, v_specialties := specialties, v_positions := positions, v_picture_url := picture_url => notify;
}
====
now => @almond_couch_market.search, ((price > 42 && contact = "+15551234567"^^tt:phone_number) || title = "0123456788" || price >= 14) , v_title := title, v_price := price, v_contact := contact => *
;;
Main() {
    now => @almond_couch_market.search(), ((price >= 11 || (title =~ "12345" && price >= 14 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @github.comment_issue(issue_number=v_price, body=v_title, repo_name="ABCDEFGHIJ") ;
}
====
now => @youtube.search_videos, (query =~ "12345" && video_id = "12345" && channel_title =~ "0123456788" && description = "ABCDEFGHIJ" && count <= 7 && video_url = "http://www.google.com"^^tt:url) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify
;;
Main() {
    now => @youtube.search_videos(query="12345"), ((description = "0123456788" && count >= 7 && video_url = "http://www.google.com"^^tt:url)) , v_video_id := video_id, v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_description := description, v_thumbnail := thumbnail, v_count := count, v_video_url := video_url => notify;
}
====
@scale.source, ((time = makeDate(1487030400000) && value > 155lb) || value < 82kg) , v_time := time, v_value := value => @heatpad.set_power, true 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @heatpad.set_power(power=enum(on)) ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(off) || power = enum(on) || power = enum(on) || power = enum(off) || power = enum(on)) 
;;
Main() {
    @phdcomics.new_post(), (((title =~ "0123456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || title =~ "12345" || link = "http://www.abc.def"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => @security-camera.set_power(power=enum(on)) ;
}
====
* => @phone.call_emergency, true 
;;
Main() {
    @xkcd.new_comic(), true , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link => @phone.call_emergency() ;
}
====
@jawbone_up.getmove, (updateTime = makeDate(1487030400000) && day =~ "9876543210" && activeTime <= 2h && inactiveTime <= 30min) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @jawbone_up.getmove(), (((day =~ "0123456789" && distance > 1000m && steps > 7 && activeTime <= 30min && calories > 500kcal) || (updateTime = makeDate(1487030400000) && steps >= 11 && activeTime > 30min) || (day = "0123456788" && distance >= 42cm && activeTime < 30min && inactiveTime <= 2h))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @nasa.apod(), ((title =~ "23456789" && description =~ "0123456789")) , v_title := title, v_description := description, v_picture_url := picture_url => notify;
}
====
* => @thermostat.get_temperature, (value < 73F || value > 73F) , v_value := value => notify
;;
Main() {
    @instagram.new_picture(), (((caption =~ "23456789" && link = "http://www.google.com"^^tt:url) || (caption = "12345" && link = "http://www.abc.def"^^tt:url && contains(hashtags, "funny"^^tt:hashtag)))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @thermostat.get_temperature(), (value >= 73F) , v_value := value => notify;
}
====
now => @giphy.get, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture , v_picture_url := picture_url => *
;;
Main() {
    now => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
* => @almond_bike_market.post, ((title =~ "ABCDEFGHIJ" && price = 14 && info = "9876543210") || (title =~ "0123456788" && price = 7 && poster = "ABCDEFGHIJ") || (title = "9876543210" && info =~ "23456789")) 
;;
Main() {
    @activity-tracker.getmove(), (((day =~ "ABCDEFGHIJ" && calories > 500kcal) || (updateTime = makeDate(1462320000000) && distance <= 1000m && activeTime = 2h && inactiveTime <= 2h) || (day =~ "23456789" && steps <= 7 && activeTime <= 2h && inactiveTime <= 30min && calories > 500kcal) || (day =~ "ABCDEFGHIJ" && steps >= 7 && activeTime <= 30min && inactiveTime <= 30min && calories = 500kcal) || (updateTime = makeDate(1487030400000) && day = "ABCDEFGHIJ" && distance > 1000m && steps > 11 && activeTime <= 2h && inactiveTime < 2h && calories >= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @almond_bike_market.post(price=v_steps, title="0123456788", info="23456789", poster="0123456789") ;
}
====
@almond_dates.monitor, (interest = "ABCDEFGHIJ" && poster =~ "9876543210") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => *
;;
Main() {
    @almond_dates.monitor(), ((message =~ "9876543210" || (interest = "12345" && poster =~ "12345") || interest =~ "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @phone.get_gps(), (speed >= 25mph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @car.honk_horn() ;
}
====
* => @icalendar.list_events, sequence <= 7 , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @slack.receive(), ((timestamp =~ "0123456789" || (channel = "cat"^^tt:hashtag && from = "bob"^^tt:username && message =~ "0123456789"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @icalendar.list_events(), (((start_date = makeDate(1487030400000) && description =~ "9876543210" && organizer = "23456789") || (sequence <= 11 && organizer = "9876543210"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@heartrate-monitor.getheartrate, (time = makeDate(1462320000000) && place = $context.location.current_location) , v_time := time, v_place := place, v_heartrate := heartrate => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), true , v_time := time, v_place := place, v_heartrate := heartrate => notify;
}
====
* => @gmail.send_picture, ((message = "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (subject =~ "23456789" && message = "ABCDEFGHIJ") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (subject =~ "23456789" && message =~ "23456789" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) 
;;
Main() {
    @scale.source(), true , v_time := time, v_value := value => @gmail.send_picture(to="alice@gmail.com"^^tt:email_address, subject="23456789", message="9876543210", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @security-camera.set_power, power = enum(off) 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "0123456788" || file_name =~ "ABCDEFGHIJ" || file_name = "ABCDEFGHIJ")) , v_file_name := file_name => @dropbox.open(file_name="ABCDEFGHIJ"), true , v_url := url => @security-camera.set_power(power=enum(off)) ;
}
====
* => @facebook.post, status = "0123456788" 
;;
Main() {
    now => @facebook.post(status="0123456789") ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 155lb) || time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (time = makeDate(1462320000000)) , v_time := time, v_weight := weight => @weatherapi.sunrise(location=$context.location.home), ((date = makeDate(1462320000000) && sunset = makeDate(1487030400000))) , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @speaker.play_music() ;
}
====
@slack.receive, ((timestamp = "0123456788" && from = "alice"^^tt:username) || (channel = "lol"^^tt:hashtag && timestamp = "23456789" && message = "23456789") || (channel = "lol"^^tt:hashtag && timestamp = "23456789" && message =~ "ABCDEFGHIJ")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => *
;;
Main() {
    @slack.receive(), (((timestamp = "0123456788" && from = "bob"^^tt:username && message =~ "ABCDEFGHIJ") || (channel = "funny"^^tt:hashtag && timestamp =~ "ABCDEFGHIJ" && message =~ "0123456789"))) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => @thecatapi.get, (count <= 55 || (image_id =~ "ABCDEFGHIJ" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @fitness-tracker.getbody(), ((weight >= 155lb && leanMass >= 42 && bmi > 7)) , v_time := time, v_place := place, v_weight := weight, v_leanMass := leanMass, v_bodyFat := bodyFat, v_bmi := bmi => @thecatapi.get(), true , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @onedrive.upload_picture, picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture 
;;
Main() {
    @weatherapi.monitor(location=$context.location.home), (((location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA") && temperature < 73F && fog = 11) || (temperature > 73F && humidity <= 55 && cloudiness > 11 && fog <= 14) || (wind_speed > 25mph && fog < 14) || (location = $context.location.work && wind_speed >= 5kmph && humidity <= 55 && cloudiness > 14 && fog > 55))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather, v_status := status => @onedrive.upload_picture(file_name=v_weather, picture_url=v_weather) ;
}
====
* => @bluetooth_speaker.play_music, true 
;;
Main() {
    @heartrate-monitor.getheartrate(), ((heartrate < 7 || heartrate > 7 || (place = $context.location.home && heartrate > 55) || heartrate < 42 || time = makeDate(1462320000000))) , v_time := time, v_place := place, v_heartrate := heartrate => @bluetooth_speaker.play_music() ;
}
====
* => @fitbit.getsteps, ((date = makeDate(1487030400000) && steps < 7) || steps >= 55 || steps > 7) , v_date := date, v_steps := steps => notify
;;
Main() {
    @wall_street_journal.us_business(), ((title = "0123456788" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link => @fitbit.getsteps(), true , v_date := date, v_steps := steps => notify;
}
====
@phone.gps, (location = $context.location.current_location && altitude < 1000m && bearing < 55 && speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => *
;;
Main() {
    @phone.gps(), (((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude >= 1000m && bearing > 42) || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && bearing < 42 && speed < 25mph) || (altitude = 42cm && bearing > 7 && speed < 5kmph) || (altitude > 42cm && speed > 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @holidays.next_uk_holiday_event(), ((description = "0123456789" || description =~ "0123456788" || summary = "12345" || (date = makeDate(1462320000000) && description =~ "12345"))) , v_date := date, v_summary := summary, v_description := description => @fitbit.recordweight(weight=155lb) ;
}
====
now => * => @car.honk_horn, true 
;;
Main() {
    now => @xkcd.random_comic(), ((number < 14 && title =~ "0123456788" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => @car.honk_horn() ;
}
====
* => @wall_street_journal.get_opinions, ((title = "0123456789" && updated = makeDate(1487030400000)) || (title = "12345" && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify
;;
Main() {
    @omlet.newmessage(), true , v_type := type, v_message := message => @wall_street_journal.get_opinions(), ((updated = makeDate(1487030400000) || updated = makeDate(1487030400000) || title = "23456789" || (title = "12345" && link = "http://www.abc.def"^^tt:url) || updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => @holidays.next_uk_holiday_event, (date = makeDate(1487030400000) && summary =~ "0123456788" && description =~ "0123456789") , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    @google_drive.new_drive_file(), ((file_name =~ "12345" || file_name = "0123456788" || file_name = "ABCDEFGHIJ" || file_name =~ "23456789" || file_name =~ "0123456789")) , v_file_name := file_name => @holidays.next_uk_holiday_event(), ((description =~ "9876543210" || summary =~ "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => @xkcd.get_comic, (title = "12345" && alt_text =~ "ABCDEFGHIJ") , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.get_comic(number=11), ((number >= 11 && title = "12345" && link = "http://www.google.com"^^tt:url && alt_text = "9876543210")) , v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@almond_dates.monitor, ((interest =~ "9876543210" && message =~ "0123456788") || poster = "23456789") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => * => notify
;;
Main() {
    @almond_dates.monitor(), ((interest =~ "ABCDEFGHIJ" && message = "0123456789" && poster = "0123456789")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @wall_street_journal.get_opinions(), ((title =~ "ABCDEFGHIJ" && link = "http://www.abc.def"^^tt:url && updated = makeDate(1462320000000))) , v_title := title, v_link := link, v_updated := updated => notify;
}
====
* => * => @phone.notify, true 
;;
Main() {
    now => @phone.get_gps(), (((bearing > 55 && speed <= 25mph) || (altitude <= 42cm && bearing < 55 && speed = 5kmph))) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @phone.notify(title="0123456789", message="0123456788") ;
}
====
@gmail.receive_email, ((date = makeDate(1462320000000) && snippet = "23456789") || subject =~ "ABCDEFGHIJ") , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => * => notify
;;
Main() {
    @gmail.receive_email(), ((from_address = "alice@gmail.com"^^tt:email_address || (from_name = "12345" && from_address = "alice@gmail.com"^^tt:email_address && date = makeDate(1462320000000)) || (from_name =~ "0123456789" && date = makeDate(1487030400000) && snippet = "ABCDEFGHIJ"))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => notify;
}
====
@reddit_front_page.newpost, true , v_title := title, v_link := link, v_user := user, v_category := category => * => *
;;
Main() {
    @reddit_front_page.newpost(), (((title = "ABCDEFGHIJ" && user =~ "0123456788" && category = "23456789") || (title =~ "9876543210" && link = "http://www.google.com"^^tt:url && category =~ "0123456788") || title = "12345")) , v_title := title, v_link := link, v_user := user, v_category := category => @slack.updateChannelPurpose(channel=v_user, purpose=v_user) ;
}
====
@scale.source, (value = 82kg || value > 82kg || (time = makeDate(1462320000000) && value <= 82kg) || value >= 82kg || value > 155lb) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), ((time = makeDate(1487030400000) && value = 155lb)) , v_time := time, v_value := value => notify;
}
====
now => @almond_bike_market.ask, property = "12345"  => notify
;;
Main() {
    now => @almond_bike_market.ask(id="23456789", property="12345"), ((id = "0123456788" || id = "0123456788" || (id = "12345" && property =~ "0123456788") || (id =~ "0123456788" && property =~ "ABCDEFGHIJ") || (id = "0123456788" && property = "9876543210") || (id = "12345" && property = "0123456788")))  => notify;
}
====
@instagram.new_picture, ((filter =~ "9876543210" && location = makeLocation(37.442156, -122.1634471, "Palo Alto, CA")) || (filter = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => * => *
;;
Main() {
    @instagram.new_picture(), ((filter = "ABCDEFGHIJ" && location = $context.location.home)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @instagram.get_pictures(), true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @facebook.post(status=v_caption) ;
}
====
now => * => @onedrive.upload_picture, true 
;;
Main() {
    now => @weatherapi.moon(location=makeLocation(37.442156, -122.1634471, "Palo Alto, CA")), true , v_date := date, v_phase := phase => @onedrive.upload_picture(file_name=v_phase, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
now => @almond_couch_market.post, (title = "ABCDEFGHIJ" || (title = "0123456789" && price > 42 && contact = "+15551234567"^^tt:phone_number)) 
;;
Main() {
    now => @almond_couch_market.post(title="0123456788", price=11, contact="+16501234567"^^tt:phone_number) ;
}
====
@sleep-tracker.getsleep, (awakeTime = makeDate(1462320000000) && duration > 30min && rem >= 2h && light >= 2h && deep <= 2h) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => * => notify
;;
Main() {
    @sleep-tracker.getsleep(), (((time = makeDate(1462320000000) && rem = 2h && deep < 2h) || (light = 2h && deep > 30min))) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @ytranslate.detect_language(text="ABCDEFGHIJ"), true , v_detected_language := detected_language => notify;
}
====
@bodytrace.source, (time = makeDate(1487030400000) || (time = makeDate(1487030400000) && weight <= 82kg) || weight < 155lb || (time = makeDate(1487030400000) && weight = 155lb)) , v_time := time, v_weight := weight => * => *
;;
Main() {
    @bodytrace.source(), (time = makeDate(1487030400000)) , v_time := time, v_weight := weight => @bluetooth_speaker.set_sink() ;
}
====
* => @twitter.sink, (status =~ "ABCDEFGHIJ" || status =~ "0123456789") 
;;
Main() {
    @github.new_issue_comment(repo_name="9876543210"), ((user = "bob"^^tt:username && issue_number >= 55 && body =~ "9876543210")) , v_user := user, v_issue_number := issue_number, v_body := body, v_time := time => @twitter.sink(status=v_body) ;
}
====
now => @dropbox.open, url = "23456789" , v_url := url => @speaker.set_sink, true 
;;
Main() {
    now => @dropbox.open(file_name="23456789"), (((file_name =~ "12345" && url =~ "ABCDEFGHIJ") || url = "0123456789")) , v_url := url => @speaker.set_sink() ;
}
====
now => @weatherapi.current, ((temperature > 73F && wind_speed > 5kmph && humidity > 42 && cloudiness > 11 && fog > 14 && weather = "0123456788") || (location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && humidity < 11 && weather = "9876543210") || (temperature > 73F && wind_speed <= 5kmph && humidity >= 42 && fog > 11)) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify
;;
Main() {
    now => @weatherapi.current(location=makeLocation(34.0543942, -118.2439408, "Los Angeles, CA")), (((temperature <= 73F && weather = "9876543210") || (temperature > 22C && humidity = 14) || (location = $context.location.work && temperature > 22C && wind_speed < 25mph && humidity < 55))) , v_temperature := temperature, v_wind_speed := wind_speed, v_humidity := humidity, v_cloudiness := cloudiness, v_fog := fog, v_weather := weather => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1487030400000) && organizer = "12345" && location =~ "23456789") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => * => *
;;
Main() {
    @icalendar.event_begin(), ((summary =~ "9876543210" && sequence > 7 && location =~ "9876543210")) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => @bluetooth_speaker.raise_volume() ;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "23456789") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @tumblr-blog.post_picture(caption=v_file_name, picture_url=v_file_name) ;
}
====
* => * => @github.add_email, email = "bob@stanford.edu"^^tt:email_address 
;;
Main() {
    now => @fitbit.getbody(), (((weight >= 82kg && bmi < 14 && fat < 11) || (weight < 82kg && bmi <= 7))) , v_weight := weight, v_bmi := bmi, v_fat := fat => @github.add_email(email="bob@stanford.edu"^^tt:email_address) ;
}
====
now => @xkcd.random_comic, (number <= 42 && link = "http://www.abc.def"^^tt:url && alt_text = "23456789") , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify
;;
Main() {
    now => @xkcd.random_comic(), ((number >= 11 && alt_text =~ "ABCDEFGHIJ")) , v_number := number, v_title := title, v_picture_url := picture_url, v_link := link, v_alt_text := alt_text => notify;
}
====
@reddit_front_page.newpost, ((title =~ "12345" && category =~ "ABCDEFGHIJ") || user =~ "23456789") , v_title := title, v_link := link, v_user := user, v_category := category => @gmail.send_email, (subject =~ v_user && message =~ "0123456789") 
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.abc.def"^^tt:url && user =~ "23456789" && category = "ABCDEFGHIJ")) , v_title := title, v_link := link, v_user := user, v_category := category => @gmail.send_email(to=v_title, subject=v_user, message=v_user) ;
}
====
* => @bluetooth_speaker.play_music, true 
;;
Main() {
    @jawbone_up.getsleep(), ((asleepTime = makeDate(1462320000000) && rem >= 30min && light = 30min && deep >= 2h)) , v_time := time, v_place := place, v_awakeTime := awakeTime, v_asleepTime := asleepTime, v_duration := duration, v_rem := rem, v_light := light, v_deep := deep => @bluetooth_speaker.play_music() ;
}
====
@reddit_front_page.newpost, (title =~ "12345" && category = "0123456789") , v_title := title, v_link := link, v_user := user, v_category := category => * => notify
;;
Main() {
    @reddit_front_page.newpost(), ((category =~ "0123456788" || (title = "9876543210" && user = "0123456788" && category =~ "12345"))) , v_title := title, v_link := link, v_user := user, v_category := category => @security-camera.get_url(), true , v_link := link => notify;
}
====
* => @instagram.get_pictures, (filter = "0123456788" || (count <= 11 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "9876543210" && location = $context.location.current_location)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="aapl"^^tt:stock_id("Apple")), ((ask_price >= 7 && bid_price <= 55)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @instagram.get_pictures(), ((link = "http://www.google.com"^^tt:url && filter = "12345" && contains(hashtags, "cat"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@phdcomics.new_post, ((title = "ABCDEFGHIJ" && link = "http://www.google.com"^^tt:url) || title =~ "9876543210" || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => *
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => @thermostat.set_hvac_mode(mode=enum(off)) ;
}
====
* => @thecatapi.get, (image_id = "12345" && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && link = "http://www.abc.def"^^tt:url) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify
;;
Main() {
    @reddit_front_page.newpost(), ((link = "http://www.abc.def"^^tt:url || (user =~ "9876543210" && category =~ "0123456788"))) , v_title := title, v_link := link, v_user := user, v_category := category => @thecatapi.get(), ((image_id =~ "9876543210" && count >= 42)) , v_image_id := image_id, v_count := count, v_picture_url := picture_url, v_link := link => notify;
}
====
* => @facebook.post_picture, caption = "9876543210" 
;;
Main() {
    @youtube.source(), (((channel_id =~ "23456789" && channel_title = "0123456789" && title =~ "12345") || (channel_id =~ "0123456789" && channel_title = "0123456789" && title = "9876543210"))) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @facebook.post_picture(picture_url=v_channel_id, caption="9876543210") ;
}
====
now => * => @speaker.set_sink, true 
;;
Main() {
    now => @thermostat.get_humidity(), ((value >= 11 || value <= 7)) , v_value := value => @speaker.set_sink() ;
}
====
* => @bing.image_search, (query = "9876543210" && width > 7 && height <= 42) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => *
;;
Main() {
    now => @bing.image_search(query="0123456788"), ((link = "http://www.abc.def"^^tt:url && width > 11 && height <= 55)) , v_title := title, v_picture_url := picture_url, v_link := link, v_width := width, v_height := height => @omlet.send_to(to=v_title, message="9876543210") ;
}
====
now => @fitbit.getbody, ((weight >= 155lb && bmi >= 55 && fat >= 55) || (bmi <= 42 && fat < 11) || fat > 14 || (weight >= 155lb && fat >= 7) || (bmi <= 11 && fat > 11) || (weight < 155lb && bmi >= 7 && fat = 11) || fat = 11 || bmi >= 55) , v_weight := weight, v_bmi := bmi, v_fat := fat => *
;;
Main() {
    now => @fitbit.getbody(), (((weight <= 82kg && fat > 42) || (weight <= 82kg && bmi >= 14 && fat >= 14) || (weight > 155lb && bmi >= 11) || weight < 82kg)) , v_weight := weight, v_bmi := bmi, v_fat := fat => @light-bulb.alert_long() ;
}
====
now => * => @bluetooth_speaker.set_sink, true 
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="12345"), (translated_text = "ABCDEFGHIJ") , v_source_language := source_language, v_translated_text := translated_text => @bluetooth_speaker.set_sink() ;
}
====
now => @tumblr-blog.post_picture, true 
;;
Main() {
    now => @tumblr-blog.post_picture(caption="0123456788", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@scale.source, (value < 82kg || time = makeDate(1487030400000) || (time = makeDate(1462320000000) && value < 82kg)) , v_time := time, v_value := value => * => *
;;
Main() {
    @scale.source(), ((value <= 82kg || (time = makeDate(1487030400000) && value > 155lb))) , v_time := time, v_value := value => @bluetooth_speaker.raise_volume() ;
}
====
@youtube.source, ((channel_id = "9876543210" && channel_title = "ABCDEFGHIJ" && title =~ "23456789" && video_id =~ "9876543210") || (channel_title =~ "ABCDEFGHIJ" && title =~ "9876543210" && video_id =~ "12345")) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => * => notify
;;
Main() {
    @youtube.source(), ((channel_id = "9876543210" && channel_title = "23456789" && video_id = "12345" && video_url = "http://www.abc.def"^^tt:url)) , v_channel_id := channel_id, v_channel_title := channel_title, v_title := title, v_video_id := video_id, v_video_url := video_url => @builtin.get_time(), ((time = makeDate(1487030400000) || time = makeDate(1487030400000))) , v_time := time => notify;
}
====
now => * => @tumblr-blog.post_picture, caption =~ "9876543210" 
;;
Main() {
    now => @bing.web_search(query="0123456788"), ((query = "0123456789" && title = "0123456789" && description =~ "0123456788" && link = "http://www.abc.def"^^tt:url)) , v_title := title, v_description := description, v_link := link => @tumblr-blog.post_picture(caption=v_description, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@activity-tracker.getmove, (day = "9876543210" && distance > 1000m && steps = 42 && activeTime < 2h && inactiveTime >= 30min && calories <= 500kcal) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => * => notify
;;
Main() {
    @activity-tracker.getmove(), (((distance > 42cm && steps < 11 && calories >= 500kcal) || (distance <= 42cm && steps <= 55 && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && distance > 42cm && steps <= 11 && activeTime = 30min && inactiveTime > 2h && calories <= 500kcal) || (updateTime = makeDate(1487030400000) && day =~ "23456789" && distance <= 1000m && inactiveTime <= 30min) || (day = "ABCDEFGHIJ" && inactiveTime >= 2h && calories < 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @imgflip.list(), (name =~ "0123456789") , v_name := name, v_picture_url := picture_url => notify;
}
====
now => * => @onedrive.upload_picture, true 
;;
Main() {
    now => @nasa.rover(), ((date_taken = makeDate(1462320000000) && count < 11 && camera_used =~ "23456789")) , v_date_taken := date_taken, v_count := count, v_picture_url := picture_url, v_camera_used := camera_used => @onedrive.upload_picture(file_name=v_camera_used, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
@almond_couch_market.monitor, (price < 14 || title =~ "9876543210") , v_title := title, v_price := price, v_contact := contact => * => *
;;
Main() {
    @almond_couch_market.monitor(), ((title =~ "ABCDEFGHIJ" || price > 42 || (title = "ABCDEFGHIJ" && price <= 55 && contact = "+15551234567"^^tt:phone_number) || (title = "0123456789" && price < 7 && contact = "+15551234567"^^tt:phone_number))) , v_title := title, v_price := price, v_contact := contact => @light-bulb.set_power(power=enum(off)) ;
}
====
@phone.gps, (location = $context.location.home && altitude >= 1000m && speed <= 5kmph) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => * => notify
;;
Main() {
    @phone.gps(), ((location = makeLocation(34.0543942, -118.2439408, "Los Angeles, CA") && altitude <= 1000m && speed >= 25mph)) , v_location := location, v_altitude := altitude, v_bearing := bearing, v_speed := speed => @almond_couch_market.search(), ((title = "12345" && price <= 7 && contact = "+15551234567"^^tt:phone_number)) , v_title := title, v_price := price, v_contact := contact => notify;
}
====
* => @almond_couch_market.post, (price >= 55 || title =~ "23456789" || title =~ "0123456788" || (title =~ "0123456789" && price < 7)) 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name = "ABCDEFGHIJ" || file_name = "0123456789")) , v_file_name := file_name => @almond_couch_market.post(title="12345", price=7, contact="+15551234567"^^tt:phone_number) ;
}
====
@bodytrace.source, ((time = makeDate(1487030400000) && weight <= 82kg) || (time = makeDate(1487030400000) && weight < 155lb) || weight <= 82kg) , v_time := time, v_weight := weight => *
;;
Main() {
    @bodytrace.source(), true , v_time := time, v_weight := weight => @phone.send_sms(to="+16501234567"^^tt:phone_number, body="12345") ;
}
====
@yahoofinance.stock_quote, (ask_price >= 14 && bid_price >= 42) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => * => notify
;;
Main() {
    @yahoofinance.stock_quote(stock_id="goog"^^tt:stock_id("Google")), ((ask_price < 55 && bid_price > 7)) , v_company_name := company_name, v_ask_price := ask_price, v_bid_price := bid_price => @twitter.search_by_hashtag(query_hashtag=v_company_name, count=v_ask_price, from=v_company_name, in_reply_to=v_company_name), (((query_hashtag = "funny"^^tt:hashtag && count >= 7) || text = "23456789")) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@omlet.incomingmessage, (message =~ "0123456788" || message = "12345" || type = enum(text) || message = "9876543210" || message = "ABCDEFGHIJ" || (type = enum(text) && message = "9876543210")) , v_feed := feed, v_type := type, v_message := message => *
;;
Main() {
    @omlet.incomingmessage(), (message =~ "23456789") , v_type := type, v_message := message => @car.honk_horn() ;
}
====
* => * => @omlet.send_picture, ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "0123456789") || picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || (to = "bob"^^tt:username && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption =~ "ABCDEFGHIJ")) 
;;
Main() {
    @google_drive.new_drive_file(), ((file_name = "0123456789" || file_name =~ "23456789" || file_name =~ "0123456789")) , v_file_name := file_name => @omlet.send_picture(to="bob"^^tt:username, picture_url="http://www.abc.def/foo.jpeg"^^tt:picture, caption="23456789") ;
}
====
* => @almond_dates.search, phone = "+15551234567"^^tt:phone_number , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @twitter.my_tweet(), ((text = "12345" && contains(urls, "http://www.abc.def"^^tt:url) && in_reply_to = "alice"^^tt:username)) , v_text := text, v_hashtags := hashtags, v_urls := urls, v_in_reply_to := in_reply_to => @almond_dates.search(), (interest = "12345") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
* => @holidays.next_uk_holiday_event, ((date = makeDate(1487030400000) && summary =~ "12345" && description =~ "23456789") || (date = makeDate(1487030400000) && summary = "ABCDEFGHIJ")) , v_date := date, v_summary := summary, v_description := description => notify
;;
Main() {
    now => @holidays.next_uk_holiday_event(), ((date = makeDate(1487030400000) && summary =~ "23456789")) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
@phdcomics.new_post, title =~ "23456789" , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), ((title = "23456789" && link = "http://www.google.com"^^tt:url)) , v_title := title, v_link := link, v_picture_url := picture_url => notify;
}
====
now => @phone.send_sms, body = "9876543210" 
;;
Main() {
    now => @phone.send_sms(to="+15551234567"^^tt:phone_number, body="9876543210") ;
}
====
* => * => @gmail.send_picture, (to = "bob@stanford.edu"^^tt:email_address && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) 
;;
Main() {
    @yahoofinance.stock_div(stock_id="msft"^^tt:stock_id("Microsoft")), ((company_name =~ "23456789" && div > 11 && ex_div_date = makeDate(1462320000000))) , v_company_name := company_name, v_yield := yield, v_div := div, v_pay_date := pay_date, v_ex_div_date := ex_div_date => @gmail.send_picture(to=v_company_name, subject=v_company_name, picture_url=v_company_name, message="23456789") ;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @security-camera.new_event(), ((picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture || start_time = makeDate(1487030400000))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @youtube.channels_by_category(category_id=enum(sports)), ((category_id = enum(film_and_entertainment) && title =~ "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @google_drive.create_new_drive_file(file_name=v_title) ;
}
====
* => * => @google_drive.create_new_drive_file, (file_name = "0123456789" || file_name = "9876543210" || file_name = "0123456788") 
;;
Main() {
    @gmail.receive_important_email(), ((from_name = "12345" || (from_name = "23456789" && subject = "0123456789" && date = makeDate(1462320000000)))) , v_from_name := from_name, v_from_address := from_address, v_subject := subject, v_date := date, v_labels := labels, v_snippet := snippet => @google_drive.create_new_drive_file(file_name="ABCDEFGHIJ") ;
}
====
@security-camera.new_event, ((has_sound = true && has_person = false) || (start_time = makeDate(1462320000000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (start_time = makeDate(1487030400000)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @linkedin.share(status="ABCDEFGHIJ") ;
}
====
now => @almond_dates.search, ((message =~ "0123456788" && phone = "+15551234567"^^tt:phone_number) || (poster =~ "23456789" && phone = "+15551234567"^^tt:phone_number) || (interest =~ "0123456789" && message =~ "ABCDEFGHIJ" && poster = "0123456788")) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => *
;;
Main() {
    now => @almond_dates.search(), (message =~ "0123456788") , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => @twitter.sink(status=v_message) ;
}
====
@holidays.uk_holiday_event, (date = makeDate(1487030400000) && description =~ "0123456788") , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.uk_holiday_event(), (date = makeDate(1462320000000)) , v_date := date, v_summary := summary, v_description := description => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
@sportradar.mlb_team, (game_status = enum(halftime) && scheduled_time =~ "12345" && game_inning =~ "9876543210" && away_runs > 55) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => * => notify
;;
Main() {
    @sportradar.mlb_team(watched_team_abbr="chc"^^sportradar:mlb_team("Chicago Cubs")), ((watched_team_abbr = "sf"^^sportradar:mlb_team("SF Giants") && watched_is_home = true && away_runs < 11 && home_runs < 42 && result = enum(unclosed))) , v_other_team_abbr := other_team_abbr, v_watched_is_home := watched_is_home, v_away_name := away_name, v_home_name := home_name, v_game_status := game_status, v_scheduled_time := scheduled_time, v_game_inning := game_inning, v_away_runs := away_runs, v_home_runs := home_runs, v_result := result => notify;
}
====
now => * => @dropbox.create_new_folder, true 
;;
Main() {
    now => @youtube.search_channel(query="0123456789"), (((query =~ "0123456789" && channel_id = "12345" && description =~ "0123456789") || (title = "9876543210" && thumbnail = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_channel_id := channel_id, v_title := title, v_description := description, v_thumbnail := thumbnail => @dropbox.create_new_folder(folder_name=v_description) ;
}
====
now => @ytranslate.translate, ((text = "12345" && translated_text =~ "0123456788") || (target_language = "it"^^tt:iso_lang_code("Italian") && text = "ABCDEFGHIJ" && translated_text =~ "23456789")) , v_source_language := source_language, v_translated_text := translated_text => notify
;;
Main() {
    now => @ytranslate.translate(target_language="en"^^tt:iso_lang_code("English"), text="0123456789"), (((source_language = "en"^^tt:iso_lang_code("English") && text = "ABCDEFGHIJ" && translated_text =~ "ABCDEFGHIJ") || source_language = "it"^^tt:iso_lang_code("Italian"))) , v_source_language := source_language, v_translated_text := translated_text => notify;
}
====
@security-camera.new_event, true , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => *
;;
Main() {
    @security-camera.new_event(), (((start_time = makeDate(1487030400000) && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) || (has_sound = true && has_person = false) || (has_motion = false && has_person = true && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture))) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @gmail.send_email(to="bob@stanford.edu"^^tt:email_address, subject="23456789", message="23456789") ;
}
====
* => @security-camera.set_power, (power = enum(off) || power = enum(on) || power = enum(off) || power = enum(off) || power = enum(off) || power = enum(off)) 
;;
Main() {
    @jawbone_up.getmove(), (((day = "12345" && distance <= 42cm && steps >= 55 && activeTime >= 2h && inactiveTime < 30min && calories >= 500kcal) || (distance < 42cm && activeTime > 30min && inactiveTime <= 30min && calories <= 500kcal))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @security-camera.set_power(power=enum(off)) ;
}
====
* => @gmail.get_latest, (from = "alice@gmail.com"^^tt:email_address && subject =~ "23456789" && label =~ "0123456788") , v_from := from, v_subject := subject, v_label := label => notify
;;
Main() {
    @security-camera.new_event(), ((start_time = makeDate(1487030400000) && has_sound = true && has_motion = true && has_person = true)) , v_start_time := start_time, v_has_sound := has_sound, v_has_motion := has_motion, v_has_person := has_person, v_picture_url := picture_url => @gmail.get_latest(), ((subject =~ "9876543210" || label = "0123456789" || (subject =~ "12345" && label = "23456789") || subject = "ABCDEFGHIJ" || from = "alice@gmail.com"^^tt:email_address || (subject =~ "0123456789" && label =~ "0123456788"))) , v_from := from, v_subject := subject, v_label := label => notify;
}
====
now => @instagram.get_pictures, true , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => *
;;
Main() {
    now => @instagram.get_pictures(count=7), ((count <= 55 && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && caption = "0123456788" && filter =~ "12345" && location = $context.location.work)) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => @omlet.send_to(to=v_filter, message="23456789") ;
}
====
* => @dropbox.get_space_usage, (used >= 20MB || used > 20MB || used < 20MB) , v_used := used, v_total := total => notify
;;
Main() {
    @omlet.newmessage(), (message = "ABCDEFGHIJ") , v_type := type, v_message := message => @dropbox.get_space_usage(), ((total >= 20MB || used > 5KB)) , v_used := used, v_total := total => notify;
}
====
* => @almond_dates.search, interest =~ "0123456788" , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify
;;
Main() {
    @heartrate-monitor.getheartrate(), (((time = makeDate(1462320000000) && heartrate > 7) || (time = makeDate(1487030400000) && heartrate < 55) || heartrate > 42 || heartrate < 55 || (place = $context.location.home && heartrate > 11))) , v_time := time, v_place := place, v_heartrate := heartrate => @almond_dates.search(), ((interest = "ABCDEFGHIJ" && message = "ABCDEFGHIJ" && poster =~ "0123456789" && phone = "+15551234567"^^tt:phone_number)) , v_interest := interest, v_message := message, v_poster := poster, v_phone := phone => notify;
}
====
@icalendar.event_begin, (end_date = makeDate(1462320000000) && summary = "9876543210" && sequence >= 11 && location =~ "ABCDEFGHIJ") , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify
;;
Main() {
    @icalendar.event_begin(), (((start_date = makeDate(1462320000000) && description =~ "0123456789" && organizer =~ "12345") || (start_date = makeDate(1487030400000) && summary =~ "ABCDEFGHIJ" && description =~ "9876543210" && organizer =~ "23456789" && location =~ "12345") || (description =~ "9876543210" && sequence > 11 && organizer = "0123456789" && location = "0123456789"))) , v_start_date := start_date, v_end_date := end_date, v_summary := summary, v_description := description, v_sequence := sequence, v_organizer := organizer, v_location := location => notify;
}
====
@nasa.asteroid, ((asteroid_id >= 55 && name = "0123456788" && estimated_diameter_min <= 1000m && estimated_diameter_max < 42cm && closest_distance_to_earth > 42cm && orbiting_body = "9876543210") || (asteroid_id <= 11 && estimated_diameter_max < 1000m && orbiting_body =~ "ABCDEFGHIJ")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => * => notify
;;
Main() {
    @nasa.asteroid(), ((asteroid_id < 7 && estimated_diameter_min > 42cm && closest_distance_to_earth > 42cm && orbiting_body = "9876543210")) , v_asteroid_id := asteroid_id, v_name := name, v_estimated_diameter_min := estimated_diameter_min, v_estimated_diameter_max := estimated_diameter_max, v_dangerous := dangerous, v_relative_velocity := relative_velocity, v_closest_distance_to_earth := closest_distance_to_earth, v_orbiting_body := orbiting_body => notify;
}
====
@scale.source, (time = makeDate(1487030400000) && value <= 82kg) , v_time := time, v_value := value => * => notify
;;
Main() {
    @scale.source(), (((time = makeDate(1462320000000) && value <= 82kg) || value < 155lb || time = makeDate(1462320000000))) , v_time := time, v_value := value => @twitter.search(query="9876543210"), ((count <= 7 && contains(hashtags, "funny"^^tt:hashtag))) , v_text := text, v_hashtags := hashtags, v_urls := urls => notify;
}
====
@onedrive.file_modified, (file_name =~ "9876543210" || file_name =~ "23456789") , v_file_name := file_name => *
;;
Main() {
    @onedrive.file_modified(), true , v_file_name := file_name => @twitter.send_direct_message(to=v_file_name, message=v_file_name) ;
}
====
* => * => @onedrive.delete_file, file_name = "0123456788" 
;;
Main() {
    now => @weatherapi.sunrise(location=$context.location.home), true , v_date := date, v_sunrise := sunrise, v_sunset := sunset => @onedrive.delete_file(file_name="12345") ;
}
====
@slack.receive, (channel = "cat"^^tt:hashtag && timestamp =~ "0123456788" && message = "ABCDEFGHIJ") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => * => notify
;;
Main() {
    @slack.receive(), (timestamp = "9876543210") , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => notify;
}
====
* => * => @car.honk_horn, true 
;;
Main() {
    @slack.receive(), ((timestamp =~ "0123456788" && message =~ "9876543210")) , v_channel := channel, v_timestamp := timestamp, v_from := from, v_message := message => @car.honk_horn() ;
}
====
now => @builtin.get_time, (time = makeDate(1487030400000) || time = makeDate(1462320000000)) , v_time := time => notify
;;
Main() {
    now => @builtin.get_time(), ((time = makeDate(1462320000000) || time = makeDate(1462320000000) || time = makeDate(1487030400000))) , v_time := time => notify;
}
====
* => @instagram.get_pictures, ((caption = "ABCDEFGHIJ" && filter =~ "0123456789") || (caption = "9876543210" && contains(hashtags, "lol"^^tt:hashtag))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify
;;
Main() {
    @jawbone_up.getmove(), (((distance < 42cm && steps > 11 && activeTime > 30min && inactiveTime > 30min && calories >= 500kcal) || (distance > 1000m && steps <= 42 && calories <= 500kcal) || (updateTime = makeDate(1462320000000) && distance <= 1000m && steps < 55 && inactiveTime = 2h && calories >= 500kcal) || (updateTime = makeDate(1462320000000) && day =~ "0123456789" && distance = 1000m && steps <= 14 && activeTime <= 30min))) , v_updateTime := updateTime, v_day := day, v_distance := distance, v_steps := steps, v_activeTime := activeTime, v_inactiveTime := inactiveTime, v_calories := calories => @instagram.get_pictures(), (((count <= 11 && caption = "0123456789" && contains(hashtags, "lol"^^tt:hashtag)) || (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture && filter = "9876543210"))) , v_media_id := media_id, v_picture_url := picture_url, v_caption := caption, v_link := link, v_filter := filter, v_hashtags := hashtags, v_location := location => notify;
}
====
@almond_bike_market.monitor, price >= 11  => * => *
;;
Main() {
    @almond_bike_market.monitor(price=7), true  => @twitter.post_picture(caption="ABCDEFGHIJ", picture_url="http://www.abc.def/foo.jpeg"^^tt:picture) ;
}
====
* => * => @phone.notify, ((title = "23456789" && message =~ "12345") || (title =~ "0123456788" && message =~ "12345")) 
;;
Main() {
    now => @holidays.next_uk_holiday_event(), (date = makeDate(1487030400000)) , v_date := date, v_summary := summary, v_description := description => @phone.notify(title=v_summary, message=v_description) ;
}
====
@phdcomics.new_post, (title =~ "0123456789" && link = "http://www.google.com"^^tt:url && picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => * => notify
;;
Main() {
    @phdcomics.new_post(), (picture_url = "http://www.abc.def/foo.jpeg"^^tt:picture) , v_title := title, v_link := link, v_picture_url := picture_url => @giphy.get(), true , v_picture_url := picture_url => notify;
}
====
now => * => @almond_couch_market.post, ((title = "0123456789" && price > 55) || price = 7 || price < 55) 
;;
Main() {
    now => @almond_couch_market.post(title="0123456789", price=7, contact="+16501234567"^^tt:phone_number) ;
}
====
@holidays.uk_holiday_event, date = makeDate(1462320000000) , v_date := date, v_summary := summary, v_description := description => * => notify
;;
Main() {
    @holidays.uk_holiday_event(), (((date = makeDate(1462320000000) && description =~ "0123456788") || (summary = "9876543210" && description =~ "0123456789") || (summary =~ "ABCDEFGHIJ" && description = "ABCDEFGHIJ"))) , v_date := date, v_summary := summary, v_description := description => notify;
}
====
now => * => @almond_bike_market.post, ((title =~ "0123456789" && price >= 11 && info =~ "0123456789" && poster = "0123456789") || (title =~ "23456789" && price > 55 && info = "0123456788")) 
;;
Main() {
    now => @almond_bike_market.post(title="ABCDEFGHIJ", price=14, info="ABCDEFGHIJ", poster="9876543210") ;
}
====
